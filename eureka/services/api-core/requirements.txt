# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.7.0
pydantic-settings==2.6.1

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Redis & Caching
redis==5.0.1
hiredis==2.3.2

# Authentication & Security
python-jose[cryptography]>=3.4.0
passlib[bcrypt,argon2]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt>=4.1; pin until passlib 1.8
argon2-cffi>=23.1,<24  # active password scheme (Session 3.3, 2026-05); bcrypt kept for legacy hashes
pyotp>=2.9,<3  # TOTP MFA (Session 3.3)
cryptography>=42  # Fernet envelope for MFA secrets
python-multipart>=0.0.7
pyjwt==2.8.0

# Validation & Serialization
email-validator==2.1.0
python-dateutil==2.8.2
aiosmtplib==3.0.1

# HTTP Client
httpx==0.26.0
aiohttp==3.9.1

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-mock==3.12.0
faker==22.0.0
# async sqlite driver for integration tests of AsyncSession endpoints
# (srs, user_progress, patent_bar). Prod uses asyncpg; in-memory
# aiosqlite is the test-only stand-in that shares the same Base
# metadata so model definitions don't drift.
aiosqlite==0.19.0

# Linting & Formatting
ruff==0.1.13
mypy==1.8.0

# Monitoring & Logging
python-json-logger==2.0.7
sentry-sdk==1.39.2
structlog>=24.1,<25  # structured JSON logs (Session 3.4)

# OpenTelemetry — distributed tracing (Session 3.4, 2026-05)
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
opentelemetry-instrumentation-redis>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0
opentelemetry-instrumentation-logging>=0.48b0  # adds trace_id/span_id to log records

# Utilities
python-dotenv==1.0.0
tenacity==8.2.3

# CORS
fastapi-cors==0.0.6

# AWS/S3 (MinIO compatible)
boto3==1.34.17
botocore==1.34.17

# Background Tasks
celery[redis]==5.3.6

# API Documentation
pyyaml==6.0.1
anthropic
python-docx

# Phase 16 — Graduate-school research tools (Sessions 16.6 + 16.7)
# Symbolic + numeric math, stats, plotting, units, chem, bio.
sympy>=1.12,<2          # 16.6 symbolic_math backend (algebra, calc, ODE, linear)
numpy>=1.26,<3          # 16.6 numeric_math + plotting + stats foundation
scipy>=1.11,<2          # 16.6 numeric_math (integrate, optimize, linalg, fft)
statsmodels>=0.14,<1    # 16.6 statistics backend (regression, ANOVA, GLM)
matplotlib>=3.8,<4      # 16.6 plotting backend (renders PNG to MinIO)
pint>=0.23,<1           # 16.7 units backend (dimensional analysis)
biopython>=1.83,<2      # 16.7 biology backend (sequence I/O, BLAST helpers)
# RDKit is heavy and pip-fragile on some platforms.
# We import lazily inside the chemistry backend; if it's missing, the backend
# falls back to a PubChem REST lookup. Comment in when ready to ship.
# rdkit-pypi>=2022.9.5

stripe==11.4.1
