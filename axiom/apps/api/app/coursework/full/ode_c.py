"""Full course: ODE.U3.N1-ODE.U4.N2 - Nonhomogeneous methods and the Laplace transform.

Six FC-template full-course modules (20 pages each) matching the gold standard
calc1_a_limits.py: undetermined coefficients, resonance and the modification rule,
variation of parameters, assembling the full nonhomogeneous solution, and the start
of the Laplace transform (elementary functions, inverse via partial fractions).
"""

L = "reading"
E = "example"
P = "pitfall"
K = "check"

LESSONS: dict[str, dict] = {
    "ODE.U3.N1": {
        "summary": (
            "A complete course on undetermined coefficients for polynomial and "
            "exponential forcing: the split y = y_h + y_p, the sharpened guess table, "
            "full-degree polynomial guesses, P_n(x)e^{kx} forcing, the factor-out-e^{kx} "
            "shortcut, and why you must check k against the characteristic roots first."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "You have spent the last unit solving HOMOGENEOUS linear equations "
                "$ay'' + by' + cy = 0$, where the right side is zero and nothing "
                "external drives the system. Real systems are driven: a spring is "
                "pushed, a circuit is fed a voltage, a structure is loaded. That "
                "external input lives on the right-hand side as a FORCING term $g(x)$, "
                "and the equation becomes NONHOMOGENEOUS: $ay'' + by' + cy = g(x)$. "
                "This unit is about solving driven equations, and undetermined "
                "coefficients is the first and most practical tool.\n\n"
                "The master fact that organizes everything: the general solution of a "
                "nonhomogeneous linear equation splits into two pieces, "
                "$y = y_h + y_p$. Here $y_h$ (the homogeneous or COMPLEMENTARY "
                "solution) carries the two arbitrary constants and describes the "
                "system's own natural behavior; $y_p$ (any single PARTICULAR "
                "solution) is one specific response to the forcing. Add them and you "
                "have EVERY solution. ODE.U3.N4 assembles this sum formally; the "
                "middle three nodes - this one, N2, N3 - are all about producing that "
                "one $y_p$.\n\n"
                "Undetermined coefficients is an EDUCATED GUESS method. When the "
                "forcing $g$ is a polynomial, an exponential, a sine or cosine, or a "
                "product of those, we guess that $y_p$ has the same SHAPE with "
                "unknown ('undetermined') coefficients, substitute into the equation, "
                "and solve for the coefficients by matching. It needs no integration, "
                "and it covers the majority of textbook and engineering forcings.\n\n"
                "This node handles the two workhorse forcings - polynomials and "
                "exponentials - and their product $P_n(x)e^{kx}$. You will learn the "
                "sharpened guess table, why the guess must be a FULL-degree "
                "polynomial, the shortcut of factoring out $e^{kx}$, and the one "
                "check you must run first: compare $k$ against the characteristic "
                "roots. Skip that check and the method silently fails - which is "
                "exactly the resonance story of N2."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "You need three things, all from the previous unit or from calculus.\n\n"
                "1. SOLVING THE HOMOGENEOUS EQUATION. Given $ay'' + by' + cy = 0$, you "
                "write the characteristic equation $ar^2 + br + c = 0$, find its roots, "
                "and build $y_h$: real distinct roots $r_1, r_2$ give "
                "$c_1 e^{r_1 x} + c_2 e^{r_2 x}$; a repeated root $r$ gives "
                "$(c_1 + c_2 x)e^{rx}$; complex roots $\\alpha \\pm \\beta i$ give "
                "$e^{\\alpha x}(c_1\\cos\\beta x + c_2\\sin\\beta x)$. This is "
                "ODE.U2.N5, and undetermined coefficients LEANS on it: the roots decide "
                "whether your guess collides with $y_h$.\n\n"
                "2. DIFFERENTIATION FLUENCY. You must differentiate polynomials, "
                "$e^{kx}$ (chain rule: $\\frac{d}{dx}e^{kx} = k e^{kx}$), and products "
                "(product rule) quickly and without error, because the method is "
                "'differentiate the guess twice, substitute, collect.' Rust here "
                "produces sign slips that are hard to find.\n\n"
                "3. LINEAR ALGEBRA OF MATCHING COEFFICIENTS. After substituting, you "
                "equate coefficients of like terms ($x^2$ with $x^2$, $e^{kx}$ with "
                "$e^{kx}$) to get a small linear system in the unknowns, then solve it.\n\n"
                "Quick self-check (answers in parentheses):\n\n"
                "- Solve $y'' - 3y' + 2y = 0$. (Roots 1, 2: "
                "$y_h = c_1 e^x + c_2 e^{2x}$.)\n"
                "- $\\frac{d}{dx}\\big[(3x+1)e^{4x}\\big]$? "
                "($3e^{4x} + 4(3x+1)e^{4x} = (12x+7)e^{4x}$.)\n"
                "- If $2A x^2 + (2B - 6A)x = 5x^2$, what are $A$ and the equation for "
                "$B$? ($A = 5/2$, and $2B - 6A = 0$.)\n\n"
                "If the homogeneous step felt shaky, review ODE.U2.N5 before "
                "continuing - it is the foundation this node stands on."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Why should GUESSING ever be a legitimate method? The answer is a "
                "closure property that polynomials and exponentials happen to have.\n\n"
                "Look at what the differential operator $Ly = ay'' + by' + cy$ does to "
                "a polynomial. If $y$ is a polynomial of degree $n$, then $y'$ and "
                "$y''$ are polynomials of degree $\\le n$, so $Ly$ is again a "
                "polynomial of degree $\\le n$. The family 'polynomials of degree "
                "$\\le n$' is CLOSED under $L$: apply $L$ and you stay inside the "
                "family. So if the forcing $g$ is a degree-$n$ polynomial, it is "
                "reasonable that SOME degree-$n$ polynomial is mapped by $L$ exactly "
                "onto $g$. That polynomial is $y_p$, and we find it by writing down "
                "the most general member of the family (all coefficients unknown) and "
                "forcing $L$ to hit $g$.\n\n"
                "Exponentials are even cleaner. Differentiating $e^{kx}$ just "
                "multiplies by $k$, so $L[e^{kx}] = (ak^2 + bk + c)e^{kx}$ - the same "
                "exponential, scaled by the number $p(k) = ak^2 + bk + c$ (the "
                "characteristic polynomial evaluated at $k$). To solve "
                "$Ly = e^{kx}$, guess $y_p = Ae^{kx}$; then $A\\,p(k) = 1$, so "
                "$A = 1/p(k)$. One line, provided $p(k) \\ne 0$.\n\n"
                "That proviso is the whole subtlety. $p(k) = 0$ means $k$ is a "
                "characteristic root, which means $e^{kx}$ ALREADY solves the "
                "homogeneous equation - so $L[Ae^{kx}] = 0$, never $e^{kx}$, and no "
                "choice of $A$ works. The guess must be modified (N2). Products "
                "$P_n(x)e^{kx}$ inherit both stories at once: the family "
                "'(polynomial of degree $\\le n$) times $e^{kx}$' is closed under $L$, "
                "so we guess a full-degree polynomial times $e^{kx}$ - unless $k$ is a "
                "root, in which case we modify. Everything below is bookkeeping around "
                "these two facts."
            )),
            # ---------------------------------------------------------- 4
            (L, "The method and the guess table", (
                "Here is the method for $ay'' + by' + cy = g(x)$ with CONSTANT "
                "coefficients and $g$ from the special family.\n\n"
                "STEP 0 - solve the homogeneous equation first and record the "
                "characteristic roots. STEP 1 - read $g$ and write the trial $y_p$ "
                "from the table below, ONE undetermined coefficient per basis "
                "function. STEP 2 - if any piece of the trial already appears in "
                "$y_h$, multiply that piece by $x^s$ (N2); otherwise proceed. STEP 3 "
                "- compute $y_p'$ and $y_p''$, substitute, collect like terms. STEP 4 "
                "- match coefficients, solve the linear system, write $y_p$.\n\n"
                "THE SHARPENED GUESS TABLE (no root collision):\n"
                "- $g = $ constant $c$ $\\;\\to\\;$ $y_p = A$.\n"
                "- $g = P_n(x)$, a degree-$n$ polynomial $\\;\\to\\;$ "
                "$y_p = A_n x^n + A_{n-1}x^{n-1} + \\cdots + A_1 x + A_0$ "
                "(ALL powers down to the constant).\n"
                "- $g = e^{kx}$ $\\;\\to\\;$ $y_p = A e^{kx}$.\n"
                "- $g = P_n(x)e^{kx}$ $\\;\\to\\;$ "
                "$y_p = (A_n x^n + \\cdots + A_1 x + A_0)e^{kx}$.\n"
                "- $g = \\cos\\omega x$ or $\\sin\\omega x$ $\\;\\to\\;$ "
                "$y_p = A\\cos\\omega x + B\\sin\\omega x$ (BOTH, even if only one "
                "appears) - developed fully with resonance in N2.\n\n"
                "THE CHECK-$k$-FIRST RULE. Before you commit to $y_p = Ae^{kx}$ or "
                "$(\\ldots)e^{kx}$, ask: is $k$ a root of $ar^2+br+c=0$? For a pure "
                "polynomial forcing, the hidden exponent is $k = 0$, so the question "
                "becomes: is $0$ a root (i.e., is $c = 0$)? If yes, the polynomial "
                "guess collides with a constant homogeneous solution and must be "
                "bumped by $x$. Running this check first is the difference between a "
                "method that works and one that mysteriously yields $0 = g$."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples", (
                "EXAMPLE 1 (linear polynomial forcing). Solve for a particular "
                "solution of $y'' - 3y' + 2y = 4x$.\n\n"
                "Roots of $r^2 - 3r + 2 = (r-1)(r-2)$ are $1, 2$; here $k = 0$ is NOT "
                "a root, so the plain guess is fine. Trial "
                "$y_p = Ax + B$, so $y_p' = A$, $y_p'' = 0$. Substitute:\n"
                "$$0 - 3A + 2(Ax + B) = 2Ax + (2B - 3A) = 4x.$$\n"
                "Match: $2A = 4 \\Rightarrow A = 2$; and $2B - 3A = 0 \\Rightarrow "
                "2B = 6 \\Rightarrow B = 3$. So $y_p = 2x + 3$. Check: "
                "$0 - 3(2) + 2(2x+3) = -6 + 4x + 6 = 4x$. Correct.\n\n"
                "Notice the constant $B = 3$ was needed even though the forcing $4x$ "
                "has no constant term - the $y'$ term fed the $x$ into the constant "
                "slot. That is why the guess must be the FULL polynomial.\n\n"
                "EXAMPLE 2 (pure exponential). Solve $y'' - 3y' + 2y = 3e^{4x}$ for a "
                "particular solution.\n\n"
                "Is $k = 4$ a root? $p(4) = 16 - 12 + 2 = 6 \\ne 0$ - not a root, "
                "guess is safe. Trial $y_p = Ae^{4x}$, $y_p' = 4Ae^{4x}$, "
                "$y_p'' = 16Ae^{4x}$. Substitute:\n"
                "$$(16A - 12A + 2A)e^{4x} = 6A\\,e^{4x} = 3e^{4x} \\Rightarrow "
                "A = \\tfrac{1}{2}.$$\n"
                "So $y_p = \\tfrac{1}{2}e^{4x}$. Notice $6 = p(4)$ - the coefficient "
                "you divide by is exactly the characteristic polynomial evaluated at "
                "$k$. This is the shortcut $A = (\\text{forcing coeff})/p(k)$, valid "
                "whenever $p(k)\\ne 0$. Had $k$ been $1$ or $2$, $p(k)$ would be $0$ "
                "and this division would blow up - the algebraic signature of "
                "resonance."
            )),
            # ---------------------------------------------------------- 6
            (L, "Properties: full polynomials, superposition, the p(k) shortcut", (
                "Three properties make the method reliable and fast.\n\n"
                "FULL-DEGREE POLYNOMIALS ARE NON-NEGOTIABLE. If $g$ is degree $n$, the "
                "trial must include every power $x^n, x^{n-1}, \\ldots, x, 1$, even "
                "the ones missing from $g$. Reason: the operator $L$ MIXES degrees "
                "downward. Substituting $Ax^2$ alone produces an $x$ term (from the "
                "$y'$ part) and a constant (from $y''$ and $y'$), so to match a pure "
                "$x^2$ forcing you still need lower coefficients to absorb those "
                "spillovers. Omit them and the linear system is inconsistent.\n\n"
                "SUPERPOSITION SPLITS COMPOUND FORCINGS. Because $L$ is linear, if "
                "$Ly_1 = g_1$ and $Ly_2 = g_2$, then $L(y_1 + y_2) = g_1 + g_2$. So "
                "for $g = 4x + 3e^{4x}$ you solve the two pieces SEPARATELY (as in "
                "Examples 1 and 2) and add: $y_p = (2x + 3) + \\tfrac{1}{2}e^{4x}$. "
                "This keeps each subproblem small and lets you apply the "
                "check-$k$ rule to each exponent independently.\n\n"
                "THE $p(k)$ SHORTCUT FOR PURE EXPONENTIALS. For $Ly = c\\,e^{kx}$ with "
                "$p(k) \\ne 0$, you never need to substitute: "
                "$y_p = \\dfrac{c}{p(k)}e^{kx}$ where $p(r) = ar^2 + br + c_0$ is the "
                "characteristic polynomial. Example: for $y'' + y' + y = 5e^{2x}$, "
                "$p(2) = 4 + 2 + 1 = 7$, so instantly $y_p = \\tfrac{5}{7}e^{2x}$. "
                "This is the single most time-saving fact in the node, and it also "
                "makes the resonance condition visible: $y_p$ is undefined exactly "
                "when $p(k) = 0$, i.e. when $k$ is a characteristic root. For "
                "polynomial pieces there is no such one-liner - you must substitute "
                "and match - but the exponential shortcut alone covers a large share "
                "of exam forcings."
            )),
            # ---------------------------------------------------------- 7
            (E, "Standard worked examples", (
                "EXAMPLE 1 (quadratic forcing - why lower terms matter). Solve "
                "$y'' - 3y' + 2y = 2x^2$.\n\n"
                "Roots $1, 2$; $k = 0$ not a root. Trial $y_p = Ax^2 + Bx + C$, "
                "$y_p' = 2Ax + B$, $y_p'' = 2A$. Substitute:\n"
                "$$2A - 3(2Ax + B) + 2(Ax^2 + Bx + C)$$\n"
                "$$= 2Ax^2 + (-6A + 2B)x + (2A - 3B + 2C) = 2x^2.$$\n"
                "Match powers: $x^2:\\; 2A = 2 \\Rightarrow A = 1$. "
                "$x^1:\\; -6A + 2B = 0 \\Rightarrow B = 3$. "
                "$x^0:\\; 2A - 3B + 2C = 0 \\Rightarrow 2 - 9 + 2C = 0 \\Rightarrow "
                "C = \\tfrac{7}{2}$. So $y_p = x^2 + 3x + \\tfrac{7}{2}$. The forcing "
                "$2x^2$ had NO $x$ and NO constant, yet $B$ and $C$ are nonzero - "
                "concrete proof that the guess needs the full polynomial.\n\n"
                "EXAMPLE 2 (polynomial times exponential, direct guess). Solve "
                "$y'' - 3y' + 2y = x e^{3x}$.\n\n"
                "Is $k = 3$ a root? $p(3) = 9 - 9 + 2 = 2 \\ne 0$ - safe. Because the "
                "forcing is (degree-1 polynomial)$\\times e^{3x}$, the trial is "
                "$y_p = (Ax + B)e^{3x}$. Then\n"
                "$$y_p' = e^{3x}(3Ax + 3B + A), \\quad "
                "y_p'' = e^{3x}(9Ax + 9B + 6A).$$\n"
                "Substitute and factor out $e^{3x}$:\n"
                "$$(9Ax + 9B + 6A) - 3(3Ax + 3B + A) + 2(Ax + B) = 2Ax + (2B + 3A).$$\n"
                "Set equal to $x$: $2A = 1 \\Rightarrow A = \\tfrac{1}{2}$; "
                "$2B + 3A = 0 \\Rightarrow 2B = -\\tfrac{3}{2} \\Rightarrow "
                "B = -\\tfrac{3}{4}$. So "
                "$y_p = \\big(\\tfrac{1}{2}x - \\tfrac{3}{4}\\big)e^{3x}$. Note the "
                "coefficient of $x$ came out as $2A$, matching $p(3) = 2$ - the "
                "exponential shortcut peeking through even in the product case."
            )),
            # ---------------------------------------------------------- 8
            (L, "Technique: the factor-out-e^{kx} shortcut", (
                "Products $P_n(x)e^{kx}$ are the fiddliest guesses because every "
                "differentiation invokes the product rule. There is a substitution "
                "that converts them into pure polynomial problems, which are cleaner.\n\n"
                "Write $y = e^{kx}u(x)$ for an unknown polynomial $u$. Differentiating,\n"
                "$$y' = e^{kx}(u' + ku), \\qquad y'' = e^{kx}(u'' + 2ku' + k^2 u).$$\n"
                "Substitute into $ay'' + by' + cy = P_n(x)e^{kx}$ and cancel the "
                "common $e^{kx}$:\n"
                "$$a\\,u'' + (2ak + b)\\,u' + (ak^2 + bk + c)\\,u = P_n(x).$$\n"
                "The exponential is GONE. The bracket on $u$ is $p(k) = ak^2+bk+c$, "
                "the same characteristic value as before, and the $u'$ coefficient is "
                "$p'(k) = 2ak + b$. You now solve a polynomial-forcing problem for "
                "$u$ (Example set 7, style 1), then multiply back by $e^{kx}$.\n\n"
                "When $p(k) \\ne 0$ (no resonance), the $u$ term is present, so a "
                "degree-$n$ polynomial $u$ matches a degree-$n$ forcing - exactly the "
                "table's product rule. When $p(k) = 0$ (single root), the $u$ term "
                "vanishes and the LOWEST surviving term is $u'$; the equation becomes "
                "$a u'' + p'(k)u' = P_n$, which is really an equation for $u'$, so $u$ "
                "picks up an extra degree - this is the $x^s$ modification of N2, "
                "derived rather than memorized. When $p(k) = p'(k) = 0$ (double root), "
                "both $u$ and $u'$ terms vanish, leaving $a u'' = P_n$, and $u$ gains "
                "TWO degrees ($x^2$ factor). The substitution thus explains WHY the "
                "modification exponent equals the root multiplicity. Use the shortcut "
                "whenever the algebra of the direct product guess feels heavy; the two "
                "routes always agree."
            )),
            # ---------------------------------------------------------- 9
            (E, "Harder worked examples", (
                "EXAMPLE 1 (same product via factor-out - compare with 7.2). Solve "
                "$y'' - 3y' + 2y = x e^{3x}$ by the substitution $y = e^{3x}u$.\n\n"
                "Here $a = 1$, $b = -3$, $c = 2$, $k = 3$. The reduced equation is "
                "$u'' + (2\\cdot 3 - 3)u' + p(3)\\,u = x$, i.e. "
                "$u'' + 3u' + 2u = x$. Trial $u = Ax + B$: "
                "$3A + 2(Ax + B) = 2Ax + (3A + 2B) = x$, so $A = \\tfrac{1}{2}$ and "
                "$3A + 2B = 0 \\Rightarrow B = -\\tfrac{3}{4}$. Thus "
                "$u = \\tfrac{1}{2}x - \\tfrac{3}{4}$ and "
                "$y_p = \\big(\\tfrac{1}{2}x - \\tfrac{3}{4}\\big)e^{3x}$ - identical "
                "to Example 7.2, with far less product-rule bookkeeping.\n\n"
                "EXAMPLE 2 (compound forcing by superposition). Solve "
                "$y'' - 3y' + 2y = 4x + 3e^{4x}$.\n\n"
                "Split. The $4x$ piece gave $y_{p,1} = 2x + 3$ (Example 5.1); the "
                "$3e^{4x}$ piece gave $y_{p,2} = \\tfrac{1}{2}e^{4x}$ (Example 5.2). "
                "By superposition $y_p = 2x + 3 + \\tfrac{1}{2}e^{4x}$. The full "
                "general solution is "
                "$y = c_1 e^x + c_2 e^{2x} + 2x + 3 + \\tfrac{1}{2}e^{4x}$.\n\n"
                "EXAMPLE 3 (quadratic-times-exponential, shortcut). Solve "
                "$y'' + y' - 2y = (x^2)e^{x}$? First check $k = 1$: "
                "$p(1) = 1 + 1 - 2 = 0$ - a ROOT. The direct table guess would "
                "collide; this is a resonance case, deferred to N2. Instead take a "
                "safe exponent: solve $y'' + y' - 2y = x^2 e^{3x}$. Then $k = 3$, "
                "$p(3) = 9 + 3 - 2 = 10 \\ne 0$, $p'(3) = 2(3) + 1 = 7$. Reduced "
                "equation $u'' + 7u' + 10u = x^2$. Trial $u = Ax^2 + Bx + C$: "
                "$2A + 7(2Ax + B) + 10(Ax^2 + Bx + C) = x^2$ gives "
                "$10A = 1$ ($A = \\tfrac{1}{10}$), $14A + 10B = 0$ "
                "($B = -\\tfrac{7}{50}$), $2A + 7B + 10C = 0$ "
                "($C = \\tfrac{29}{500}$). So "
                "$y_p = \\big(\\tfrac{1}{10}x^2 - \\tfrac{7}{50}x + "
                "\\tfrac{29}{500}\\big)e^{3x}$."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: when the guess collides", (
                "Everything above assumed the trial $y_p$ shares no term with the "
                "homogeneous solution $y_h$. The edge case - and it is common - is "
                "when it does.\n\n"
                "The tell-tale signs: (a) you are forcing with $e^{kx}$ and $k$ turns "
                "out to be a characteristic root, so $p(k) = 0$ and the shortcut "
                "$A = c/p(k)$ divides by zero; (b) you substitute the trial and every "
                "term cancels on the left, leaving the impossible statement "
                "$0 = g(x)$. Both mean the same thing: your trial function is "
                "annihilated by $L$ because it already solves $Ly = 0$.\n\n"
                "Consider the hidden-exponent version. A pure polynomial forcing has "
                "$k = 0$. If $c = 0$ in $ay'' + by' + cy$ (no undifferentiated $y$ "
                "term), then $r = 0$ IS a characteristic root, so constants solve the "
                "homogeneous equation, and a polynomial trial collides. Example: "
                "$y'' - 3y' = 6$ has roots $0$ and $3$; the constant guess $y_p = A$ "
                "gives $0 - 0 = 6$, false. You must bump the guess.\n\n"
                "The FIX, previewed here and proven in N2, is the modification rule: "
                "multiply the offending part of the trial by $x^s$, where $s$ is the "
                "MULTIPLICITY of the colliding root (1 for a simple root, 2 for a "
                "double root). The factor-out derivation of page 8 explains the "
                "arithmetic: when $p(k) = 0$ the reduced equation loses its $u$ term "
                "and effectively governs $u'$ instead of $u$, so $u$ needs one extra "
                "degree; when $p(k) = p'(k) = 0$ it governs $u''$, so $u$ needs two. "
                "Recognizing the collision is the skill this node builds; executing "
                "the fix cleanly is N2. The one habit that saves you: run the "
                "check-$k$ test at STEP 0, before writing any guess, so a collision is "
                "caught by design rather than discovered after a page of failed "
                "algebra."
            )),
            # ---------------------------------------------------------- 11
            (E, "Edge-case worked example: a collision, diagnosed", (
                "Solve $y'' - 3y' + 2y = e^{2x}$ - and watch the plain guess fail on "
                "purpose, so you can recognize the symptom.\n\n"
                "STEP 0. Roots of $r^2 - 3r + 2$ are $1$ and $2$, so "
                "$y_h = c_1 e^x + c_2 e^{2x}$. The forcing exponent is $k = 2$, which "
                "IS a root. The check-$k$ test already flags a collision. But suppose "
                "we ignore the warning to see what breaks.\n\n"
                "NAIVE ATTEMPT. Trial $y_p = Ae^{2x}$, "
                "$y_p' = 2Ae^{2x}$, $y_p'' = 4Ae^{2x}$. Substitute:\n"
                "$$(4A - 6A + 2A)e^{2x} = 0\\cdot e^{2x} = 0.$$\n"
                "We are asked to make this equal $e^{2x}$, i.e. $0 = e^{2x}$ - "
                "impossible for every $A$. The left side collapsed because "
                "$p(2) = 4 - 6 + 2 = 0$: the trial IS a homogeneous solution, so $L$ "
                "sends it to zero. That $0 = g$ outcome is the universal signature of "
                "a missed collision.\n\n"
                "THE FIX (modification, developed in N2). Multiply by $x$: "
                "$y_p = Ax e^{2x}$. Then "
                "$y_p' = Ae^{2x}(1 + 2x)$, "
                "$y_p'' = Ae^{2x}(4 + 4x)$. Substitute:\n"
                "$$Ae^{2x}(4 + 4x) - 3Ae^{2x}(1 + 2x) + 2Axe^{2x} "
                "= Ae^{2x}\\big[(4 + 4x) - (3 + 6x) + 2x\\big] = Ae^{2x}.$$\n"
                "Set $Ae^{2x} = e^{2x}$: $A = 1$, so $y_p = x e^{2x}$. Check: "
                "$y_p'' - 3y_p' + 2y_p = e^{2x}(4+4x) - 3e^{2x}(1+2x) + 2xe^{2x} "
                "= e^{2x}$. Correct. The single factor of $x$ - because $2$ is a "
                "SIMPLE root - lifts the trial out of the homogeneous space and the "
                "method works again."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'Guess only the terms that appear in $g$.' For a "
                "forcing $2x^2$, students try $y_p = Ax^2$ and get an inconsistent "
                "system. The operator mixes degrees downward, so a degree-$n$ forcing "
                "needs the FULL degree-$n$ polynomial trial. Same for products: "
                "$x e^{3x}$ needs $(Ax + B)e^{3x}$, not $Axe^{3x}$.\n\n"
                "MISCONCEPTION 2: 'The exponential coefficient is obvious - it is just "
                "the forcing coefficient.' No: for $Ly = c e^{kx}$ the coefficient is "
                "$A = c/p(k)$, scaled by the characteristic value $p(k)$, not by $c$ "
                "alone. Only when $p(k) = 1$ do they coincide.\n\n"
                "MISCONCEPTION 3: 'Undetermined coefficients works for any forcing.' "
                "It works ONLY when $g$ is a polynomial, exponential, sine/cosine, or "
                "a finite product/sum of those - functions whose derivative family is "
                "finite-dimensional. For $g = \\tan x$, $\\sec x$, $\\ln x$, or "
                "$1/x$, no finite guess closes, and you must use variation of "
                "parameters (N3).\n\n"
                "MISCONCEPTION 4: 'I can skip solving the homogeneous equation and "
                "just find $y_p$.' You cannot check for a collision without the "
                "characteristic roots, and you cannot write the GENERAL solution "
                "(needed for initial conditions in N4) without $y_h$. STEP 0 is not "
                "optional.\n\n"
                "MISCONCEPTION 5: 'A collision means the problem has no solution.' It "
                "means the naive guess fails; the equation still has a perfectly good "
                "particular solution, found by the $x^s$ modification. Collision is a "
                "signal to reshape the guess, not a dead end - exactly parallel to how "
                "$\\tfrac{0}{0}$ in limits signals 'simplify,' not 'does not exist.'"
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational and procedural errors", (
                "ERROR 1: Skipping the check-$k$ step. If you write the guess before "
                "comparing $k$ to the roots, a collision surfaces only after you have "
                "differentiated and substituted - a page of algebra wasted, ending in "
                "$0 = g$. Do STEP 0 first, every time.\n\n"
                "ERROR 2: Dropping lower-order polynomial coefficients. Writing "
                "$y_p = Ax^2$ for a quadratic forcing, or $(Ax)e^{kx}$ for "
                "$xe^{kx}$, omits the coefficients that absorb the operator's "
                "downward mixing. Always write the complete polynomial.\n\n"
                "ERROR 3: Product-rule slips. For $y_p = (Ax + B)e^{kx}$, the "
                "derivative is $e^{kx}\\big[A + k(Ax + B)\\big]$, and the second "
                "derivative doubles the cross term. A dropped $A$ or a missing factor "
                "of $k$ silently corrupts the linear system. The factor-out-$e^{kx}$ "
                "substitution (page 8) sidesteps most of these.\n\n"
                "ERROR 4: Matching coefficients across different basis functions. You "
                "may equate $x^2$ with $x^2$ and $e^{kx}$ with $e^{kx}$, but NEVER a "
                "polynomial term with an exponential term - they are linearly "
                "independent, so each family gives its own separate equations. Collect "
                "by basis function before matching.\n\n"
                "ERROR 5: Forgetting that the shortcut $A = c/p(k)$ requires "
                "$p(k)\\ne 0$. If you mechanically apply it when $k$ is a root, you "
                "divide by zero; the correct response is to modify the guess, not to "
                "report 'undefined.'\n\n"
                "ERROR 6: Stopping at $y_p$ when the problem wants the general "
                "solution or an IVP. A particular solution is one function; the "
                "general solution is $y_h + y_p$ with its two constants, and only THEN "
                "do you apply initial data (N4). Read the question: 'find a particular "
                "solution' and 'solve the initial value problem' ask for different "
                "final objects."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: forced systems with polynomial and exponential inputs", (
                "Polynomial and exponential forcings are not artificial - they are the "
                "two most common real inputs to linear systems.\n\n"
                "1. EXPONENTIAL DRIVE (decay or growth). An RC or RL circuit fed a "
                "decaying source $V(t) = V_0 e^{-t/\\tau}$, a structure under a load "
                "that relaxes exponentially, or a population with an exponentially "
                "changing immigration rate all give $ay'' + by' + cy = c_0 e^{kt}$. "
                "The particular solution $\\tfrac{c_0}{p(k)}e^{kt}$ is the system's "
                "forced response, and $p(k)$ - the characteristic polynomial at the "
                "drive rate - controls its size. When $k$ nears a root, $p(k)\\to 0$ "
                "and the response amplifies: the exponential analogue of resonance.\n\n"
                "2. RAMP AND POLYNOMIAL INPUTS. A control input that rises linearly "
                "(a ramp, $g = mt$) or quadratically models a steadily increasing "
                "load, a linearly opening valve, or a constant-acceleration command. "
                "The polynomial $y_p$ is the steady tracking response; in a damped "
                "system it is what the output settles onto after transients die (N4).\n\n"
                "3. CONSTANT FORCING = EQUILIBRIUM SHIFT. The simplest case "
                "$g = \\text{const}$ gives a constant $y_p$ - the new equilibrium the "
                "system relaxes to. A spring with gravity, $my'' + cy' + ky = -mg$, "
                "has $y_p = -mg/k$: gravity does not change the oscillation, it just "
                "lowers the rest position by the static stretch $mg/k$.\n\n"
                "4. SUPERPOSED INPUTS. Real drives are often a constant bias plus a "
                "ramp plus an exponential turn-on. Superposition lets you solve each "
                "piece with its own guess and add - the mathematical version of "
                "'analyze one input at a time,' the engineer's default habit. In every "
                "case the recurring question is the same one this node answers: given "
                "the input's shape, what shape does the forced response take, and how "
                "big is it?"
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: a spring with an exponential push", (
                "A mass-spring-damper obeys $y'' + 4y' + 3y = 10e^{-t}$? Check first: "
                "roots of $r^2 + 4r + 3 = (r+1)(r+3)$ are $-1, -3$, and the drive "
                "exponent $k = -1$ IS a root - a resonance case for N2. To keep this "
                "example clean, take the drive $10e^{-2t}$ instead, so "
                "$y'' + 4y' + 3y = 10e^{-2t}$ with $y(0) = 0$, $y'(0) = 0$ (mass "
                "starting at rest at equilibrium).\n\n"
                "STEP 1 - homogeneous. Roots $-1, -3$: "
                "$y_h = c_1 e^{-t} + c_2 e^{-3t}$. Both decay, so this is the "
                "TRANSIENT.\n\n"
                "STEP 2 - particular. Drive exponent $k = -2$: "
                "$p(-2) = 4 - 8 + 3 = -1 \\ne 0$, safe. Shortcut: "
                "$y_p = \\dfrac{10}{p(-2)}e^{-2t} = \\dfrac{10}{-1}e^{-2t} = "
                "-10e^{-2t}$.\n\n"
                "STEP 3 - general solution: "
                "$y = c_1 e^{-t} + c_2 e^{-3t} - 10e^{-2t}$.\n\n"
                "STEP 4 - apply the data LAST, to the full solution. "
                "$y(0) = c_1 + c_2 - 10 = 0 \\Rightarrow c_1 + c_2 = 10$. "
                "$y' = -c_1 e^{-t} - 3c_2 e^{-3t} + 20e^{-2t}$, so "
                "$y'(0) = -c_1 - 3c_2 + 20 = 0 \\Rightarrow c_1 + 3c_2 = 20$. "
                "Subtract: $2c_2 = 10 \\Rightarrow c_2 = 5$, then $c_1 = 5$. So\n"
                "$$y = 5e^{-t} + 5e^{-3t} - 10e^{-2t}.$$\n"
                "Check the data: $y(0) = 5 + 5 - 10 = 0$; "
                "$y'(0) = -5 - 15 + 20 = 0$. Both hold. As $t \\to \\infty$ every "
                "term decays to $0$ - the mass, briefly displaced by the push, "
                "returns to equilibrium. This is the complete four-step workflow that "
                "N4 formalizes; here you have already run it end to end."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: where this node plugs in", (
                "BACKWARD. This node stands on ODE.U2.N5 (constant-coefficient "
                "homogeneous solutions): STEP 0 is entirely that machinery, and the "
                "characteristic roots are what the check-$k$ rule consults. It also "
                "leans on calculus differentiation rules - the product rule for "
                "$(Ax+B)e^{kx}$ and the chain rule for $e^{kx}$ (C202-level "
                "technique).\n\n"
                "SIDEWAYS AND FORWARD, within the unit:\n"
                "- ODE.U3.N2 (resonance and the modification rule) is the direct "
                "sequel: it handles exactly the collisions this node learned to "
                "DETECT, multiplying by $x^s$ and giving them their physical "
                "interpretation.\n"
                "- ODE.U3.N3 (variation of parameters) is the universal fallback for "
                "forcings OUTSIDE the polynomial/exponential/trig family "
                "($\\tan x$, $\\sec x$, $1/x$), where no finite guess exists.\n"
                "- ODE.U3.N4 (full solution $y_h + y_p$) assembles the pieces and "
                "applies initial data - the four-step workflow you already ran in "
                "Example 15.\n\n"
                "FURTHER OUT. The entire Laplace unit (ODE.U4.N1-N3) is an "
                "ALTERNATIVE to undetermined coefficients for the same driven "
                "IVPs: transforming turns $ay'' + by' + cy = g$ into an algebraic "
                "equation whose solution, after partial fractions and inversion, "
                "reproduces $y_h + y_p$ and the constants in one pass - the $p(k)$ "
                "you divide by here reappears there as the characteristic polynomial "
                "in the denominator. Systems of ODEs and matrix exponentials extend "
                "the same forced-response idea to vector unknowns. Learning to "
                "recognize input shape and predict response shape - the core reflex "
                "of this node - is what every one of those methods automates."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: annihilators and why the family is special", (
                "Undetermined coefficients feels like a bag of tricks until you see "
                "the operator theory beneath it, which explains precisely which "
                "forcings it can handle and why the guesses take the forms they do.\n\n"
                "Write $D = \\tfrac{d}{dx}$, so the equation is $P(D)y = g$ where "
                "$P(D) = aD^2 + bD + c$. A function $g$ is a valid forcing for this "
                "method exactly when some polynomial differential operator $A(D)$ "
                "ANNIHILATES it: $A(D)g = 0$. For example $D$ annihilates constants; "
                "$D^{n+1}$ annihilates degree-$n$ polynomials; $(D - k)$ annihilates "
                "$e^{kx}$; $(D-k)^{n+1}$ annihilates $x^n e^{kx}$; "
                "$(D^2 + \\omega^2)$ annihilates $\\cos\\omega x$ and "
                "$\\sin\\omega x$. These are precisely the functions with a "
                "finite-dimensional space of derivatives - the closure property of "
                "page 3, now stated exactly.\n\n"
                "The payoff: apply the annihilator to both sides. $P(D)y = g$ becomes "
                "$A(D)P(D)y = A(D)g = 0$, a HOMOGENEOUS equation of higher order. Its "
                "solution space is spanned by the roots of $A(D)P(D)$. The roots "
                "coming from $P$ give $y_h$; the EXTRA roots coming from $A$ give "
                "exactly the trial form for $y_p$ - and when a root of $A$ coincides "
                "with a root of $P$, its multiplicity rises, which is the $x^s$ "
                "modification, now forced by algebra rather than guessed. So the guess "
                "table is not memorized folklore; it is read off the annihilator's "
                "roots.\n\n"
                "This viewpoint also draws the boundary of the method. $\\tan x$ has "
                "infinitely many linearly independent derivatives, so no polynomial "
                "$A(D)$ annihilates it, so no finite guess exists - and variation of "
                "parameters (N3) becomes mandatory. The Laplace transform (U4) can be "
                "seen as the same operator algebra done in the $s$-domain, where "
                "$P(D)$ becomes the polynomial $P(s)$ and annihilation becomes "
                "clearing denominators."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "THE SPLIT. For $ay'' + by' + cy = g(x)$: general solution "
                "$y = y_h + y_p$. Find $y_h$ from the characteristic roots "
                "(ODE.U2.N5); find $y_p$ by an educated guess matching the shape of "
                "$g$.\n\n"
                "THE GUESS TABLE (no root collision):\n"
                "- constant $\\to A$;  degree-$n$ polynomial "
                "$\\to A_n x^n + \\cdots + A_0$ (all powers);\n"
                "- $e^{kx} \\to A e^{kx}$;  "
                "$P_n(x)e^{kx} \\to (A_n x^n + \\cdots + A_0)e^{kx}$;\n"
                "- $\\cos\\omega x$ or $\\sin\\omega x \\to A\\cos\\omega x + "
                "B\\sin\\omega x$.\n\n"
                "CHECK $k$ FIRST. Compare the forcing exponent $k$ (use $k = 0$ for "
                "pure polynomials) against the characteristic roots. If $k$ is a root "
                "of multiplicity $s$, multiply the trial by $x^s$ (N2).\n\n"
                "EXPONENTIAL SHORTCUT. For $Ly = c\\,e^{kx}$ with $p(k)\\ne 0$: "
                "$y_p = \\dfrac{c}{p(k)}e^{kx}$, where $p(r) = ar^2+br+c$.\n\n"
                "FACTOR-OUT SUBSTITUTION. For $P_n(x)e^{kx}$, set $y = e^{kx}u$ to get "
                "$a u'' + (2ak+b)u' + p(k)u = P_n(x)$ - a pure polynomial problem.\n\n"
                "SUPERPOSITION. $g = g_1 + g_2 \\Rightarrow y_p = y_{p,1} + y_{p,2}$; "
                "solve each piece separately.\n\n"
                "WORKFLOW. (0) Roots and $y_h$. (1) Read $g$, write trial. (2) "
                "Collision check; bump by $x^s$ if needed. (3) Differentiate, "
                "substitute, collect by basis function. (4) Match coefficients, solve, "
                "write $y_p$; add to $y_h$; apply data last. RED FLAGS: $0 = g$ after "
                "substitution means a missed collision; an inconsistent system usually "
                "means a dropped lower-order term."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all six before reading solutions.\n\n"
                "1. Particular solution of $y'' - y' - 6y = 12x$?\n"
                "2. Particular solution of $y'' - y' - 6y = 5e^{x}$?\n"
                "3. For $y'' + y = 4e^{2x}$, use the shortcut to write $y_p$.\n"
                "4. Set up (do not fully solve) the trial for "
                "$y'' - 4y = x^2 e^{5x}$, and state the reduced $u$-equation from the "
                "factor-out substitution.\n"
                "5. Does the plain guess $Ae^{3x}$ work for $y'' - 9y = e^{3x}$? Why?\n"
                "6. Particular solution of $y'' - 2y' + y = 3$ (constant forcing).\n\n"
                "SOLUTIONS.\n"
                "1. Roots of $r^2 - r - 6 = (r-3)(r+2)$: $3, -2$; $k=0$ not a root. "
                "$y_p = Ax + B$: $-A - 6(Ax+B) = -6Ax + (-A - 6B) = 12x$, so "
                "$A = -2$, $-A - 6B = 0 \\Rightarrow B = \\tfrac{1}{3}$. "
                "$y_p = -2x + \\tfrac{1}{3}$.\n"
                "2. $k = 1$: $p(1) = 1 - 1 - 6 = -6 \\ne 0$. "
                "$y_p = \\tfrac{5}{-6}e^{x} = -\\tfrac{5}{6}e^{x}$.\n"
                "3. $p(2) = 4 + 1 = 5$; $y_p = \\tfrac{4}{5}e^{2x}$.\n"
                "4. $k = 5$, $p(5) = 25 - 4 = 21$, $p'(5) = 2\\cdot 5 = 10$ (since "
                "$b=0$). Trial $y_p = (Ax^2 + Bx + C)e^{5x}$; reduced equation "
                "$u'' + 10u' + 21u = x^2$.\n"
                "5. No. Roots of $r^2 - 9$ are $\\pm 3$, and $k = 3$ is a root, so "
                "$Ae^{3x}$ is homogeneous and substitution gives $0 = e^{3x}$. Modify "
                "to $Axe^{3x}$ (N2).\n"
                "6. Double root $r = 1$; $k = 0$ not a root ($c = 1 \\ne 0$). "
                "$y_p = A$ constant: $0 - 0 + A = 3$, so $y_p = 3$.\n\n"
                "Scoring: 6/6 - proceed to N2. 4-5 - reread the guess table (page 4) "
                "and the shortcut (page 6). $\\le 3$ - rework pages 3-9 before "
                "practicing."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Check every box honestly before moving on:\n\n"
                "- State the split $y = y_h + y_p$ and explain what each piece "
                "represents physically.\n"
                "- Read a forcing $g$ and write the correct trial from the guess "
                "table, using a FULL-degree polynomial where required.\n"
                "- Run the check-$k$-first test and recognize a root collision BEFORE "
                "substituting.\n"
                "- Apply the exponential shortcut $y_p = c/p(k)\\,e^{kx}$ and say "
                "exactly when it is valid.\n"
                "- Use the factor-out-$e^{kx}$ substitution to turn a product forcing "
                "into a polynomial problem, and explain why it converts the "
                "modification exponent into a root multiplicity.\n"
                "- Split a compound forcing by superposition and reassemble $y_p$.\n"
                "- Diagnose the $0 = g$ symptom of a missed collision and know the fix "
                "is the $x^s$ bump (N2).\n"
                "- Substitute a candidate $y_p$ back into the equation to VERIFY it, "
                "not just to produce it.\n"
                "- Distinguish 'find a particular solution' from 'solve the IVP,' and "
                "carry a problem through the full four-step workflow with data applied "
                "LAST.\n"
                "- State the boundary of the method: which forcings it cannot handle, "
                "and which node (N3) takes over.\n\n"
                "When every box is checked, practice until collision-checking is "
                "automatic, then proceed to ODE.U3.N2, where the collisions you now "
                "detect become the physics of resonance."
            )),
        ],
    },
    "ODE.U3.N2": {
        "summary": (
            "A complete course on resonance and the modification rule: when the "
            "undetermined-coefficients guess collides with a homogeneous solution, "
            "multiply by x^s (s = root multiplicity); the algebra behind the fix; and "
            "physical resonance, where forcing at the natural frequency produces "
            "amplitude that grows without bound - the swing, the bridge, the "
            "t-growing solution."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "In N1 you learned to solve driven equations by guessing the shape of "
                "$y_p$ - and you learned to DETECT one failure mode: when the guess "
                "collides with the homogeneous solution, substitution collapses to the "
                "impossible $0 = g$. This node is the cure, and it is also one of the "
                "most physically consequential ideas in the whole subject: RESONANCE.\n\n"
                "The mathematical statement is small. When a term of your trial $y_p$ "
                "already solves $ay'' + by' + cy = 0$, the operator annihilates it, so "
                "no coefficient can match the forcing. The MODIFICATION RULE fixes "
                "this: multiply the offending part of the trial by $x^s$, where $s$ is "
                "the MULTIPLICITY of the characteristic root causing the collision - "
                "$s = 1$ for a simple root, $s = 2$ for a double root. That single "
                "factor of $x$ (or $x^2$) lifts the guess out of the homogeneous space "
                "and the method works again.\n\n"
                "The physics is enormous. A collision happens exactly when the system "
                "is driven at its OWN natural frequency (or natural decay rate). The "
                "extra factor of $x$ (or $t$, in time) means the response does not "
                "stay bounded - its amplitude GROWS LINEARLY with time. Push a swing "
                "in perfect time with its natural rhythm and it climbs higher and "
                "higher; that unbounded climb is the $t$ in $y_p = Ct\\sin\\omega_0 t$. "
                "The same phenomenon shakes bridges, shatters wine glasses at the "
                "right pitch, and lets a radio pick one station out of thousands.\n\n"
                "This node ties the algebra to the physics: you will learn to apply "
                "$x^s$ correctly, understand WHY the exponent equals the multiplicity, "
                "compute the resonant response of an undamped oscillator, and read the "
                "$t$-growth as the signature of forcing at the natural frequency."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "This node is a direct continuation of N1, so you need its machinery "
                "solid.\n\n"
                "1. THE N1 METHOD AND ITS COLLISION SYMPTOM. You should be able to "
                "write the trial $y_p$ for polynomial and exponential forcing, run the "
                "check-$k$ test against the characteristic roots, and recognize the "
                "$0 = g$ outcome as a collision. If that is fuzzy, reread N1 pages "
                "10-11.\n\n"
                "2. ROOT MULTIPLICITY. From ODE.U2.N5: a characteristic equation can "
                "have a SIMPLE root (appears once, e.g. $r = 2$ in $(r-1)(r-2)$) or a "
                "REPEATED/double root (appears twice, e.g. $r = 2$ in $(r-2)^2$). The "
                "multiplicity is how many times the factor appears. Complex roots "
                "$\\alpha \\pm \\beta i$ come as a conjugate pair; forcing "
                "$\\cos\\beta x$ or $\\sin\\beta x$ collides with them when "
                "$\\alpha = 0$.\n\n"
                "3. TRIG DERIVATIVES AND THE UNDAMPED OSCILLATOR. You must "
                "differentiate $t\\sin\\omega t$ and $t\\cos\\omega t$ with the "
                "product rule, and you should know that $y'' + \\omega_0^2 y = 0$ has "
                "solution $c_1\\cos\\omega_0 t + c_2\\sin\\omega_0 t$ - the "
                "undamped natural oscillation at frequency $\\omega_0$.\n\n"
                "Quick self-check (answers in parentheses):\n\n"
                "- Multiplicity of the root causing the collision in "
                "$y'' - 4y' + 4y = e^{2x}$? (Double root $r = 2$; multiplicity 2, so "
                "bump by $x^2$.)\n"
                "- Natural frequency of $y'' + 9y = 0$? ($\\omega_0 = 3$; solutions "
                "$\\cos 3t$, $\\sin 3t$.)\n"
                "- $\\frac{d}{dt}\\big[t\\sin 3t\\big]$? "
                "($\\sin 3t + 3t\\cos 3t$.)\n\n"
                "If multiplicity is unclear, revisit ODE.U2.N5 - it decides the "
                "exponent $s$."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Why does multiplying by $x$ rescue a collided guess, and why exactly "
                "$x^s$ and not something else?\n\n"
                "Start with the pushing-a-swing picture. A swing has a natural rhythm. "
                "If you push at random times, some pushes help and some fight the "
                "motion; the swing settles into a modest steady oscillation (the "
                "bounded $y_p$ of N1). But if you time every push to the swing's own "
                "rhythm - always pushing as it moves away - every push ADDS energy, "
                "nothing cancels, and the arc grows on each cycle. Mathematically, "
                "forcing at the natural frequency means the forcing looks just like a "
                "free oscillation, so the system has no way to 'balance' it into a "
                "steady response; instead the amplitude accumulates. Accumulation over "
                "time is what a factor of $t$ encodes.\n\n"
                "Now the algebra, from the factor-out substitution of N1 page 8. "
                "Writing $y = e^{kx}u$ turned $Ly = P_n(x)e^{kx}$ into\n"
                "$$a u'' + (2ak + b)u' + p(k)\\,u = P_n(x).$$\n"
                "When $k$ is NOT a root, $p(k)\\ne 0$: the lowest term is $p(k)u$, and "
                "a degree-$n$ $u$ matches a degree-$n$ forcing. When $k$ is a SIMPLE "
                "root, $p(k) = 0$ but $p'(k) = 2ak + b \\ne 0$: the $u$ term vanishes, "
                "the lowest surviving term is $u'$, so the equation really governs "
                "$u'$ - meaning $u$ must be ONE degree higher, i.e. carry an extra "
                "factor of $x$. When $k$ is a DOUBLE root, $p(k) = p'(k) = 0$: both "
                "$u$ and $u'$ terms vanish, leaving $a u'' = P_n$, so $u$ must be TWO "
                "degrees higher - an extra $x^2$. The exponent $s$ is thus forced to "
                "equal the multiplicity, not chosen. The modification rule is a "
                "theorem in disguise, and the $t$-growth is its physical shadow."
            )),
            # ---------------------------------------------------------- 4
            (L, "The modification rule, stated precisely", (
                "THE RULE. To solve $ay'' + by' + cy = g(x)$ when $g$ is from the "
                "N1 family: form the usual trial $y_p$ from the guess table. If any "
                "GROUP of terms in that trial coincides with a homogeneous solution, "
                "multiply that ENTIRE group by $x^s$, where $s$ is the multiplicity of "
                "the corresponding characteristic root. Use the smallest $s$ that "
                "removes all overlap with $y_h$.\n\n"
                "By forcing type:\n"
                "- $g = e^{kx}$. If $k$ is a simple root, trial $Axe^{kx}$; if a "
                "double root, $Ax^2 e^{kx}$.\n"
                "- $g = P_n(x)$ (hidden $k = 0$). If $0$ is a simple root (i.e. "
                "$c = 0$, $b\\ne 0$), multiply the WHOLE polynomial trial by $x$; if "
                "$0$ is a double root ($b = c = 0$), by $x^2$.\n"
                "- $g = P_n(x)e^{kx}$. Same as the exponential case: multiply the full "
                "$(\\ldots)e^{kx}$ trial by $x^s$ for root $k$ of multiplicity $s$.\n"
                "- $g = \\cos\\omega x$ or $\\sin\\omega x$. If $\\pm i\\omega$ are "
                "roots (i.e. the homogeneous solution already contains "
                "$\\cos\\omega x$, $\\sin\\omega x$ - the undamped resonant case), "
                "multiply $A\\cos\\omega x + B\\sin\\omega x$ by $x$.\n\n"
                "TWO GUARD-RAILS. First, multiply the whole GROUP, keeping BOTH "
                "coefficients (e.g. $x(A\\cos + B\\sin)$, never just $Ax\\cos$) - "
                "otherwise the trial is not general enough and matching fails. Second, "
                "the extra $x^s$ multiplies only the trial's colliding piece; "
                "non-colliding pieces of a compound forcing keep their ordinary "
                "guesses. The reliable procedure: solve the homogeneous equation "
                "first, write $y_h$ explicitly, then compare each trial group against "
                "$y_h$ term by term and bump exactly the ones that appear."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples", (
                "EXAMPLE 1 (simple-root collision). Solve $y'' - 3y' + 2y = e^{2x}$.\n\n"
                "Roots $1, 2$; $y_h = c_1 e^x + c_2 e^{2x}$. Forcing exponent $k = 2$ "
                "is a SIMPLE root, so bump by $x^1$: trial $y_p = Axe^{2x}$. "
                "Differentiate: $y_p' = Ae^{2x}(1 + 2x)$, "
                "$y_p'' = Ae^{2x}(4 + 4x)$. Substitute:\n"
                "$$Ae^{2x}\\big[(4 + 4x) - 3(1 + 2x) + 2x\\big] = Ae^{2x}\\cdot 1.$$\n"
                "So $A = 1$ and $y_p = xe^{2x}$. The lone factor of $x$ - matching the "
                "multiplicity 1 - did the job.\n\n"
                "EXAMPLE 2 (double-root collision). Solve $y'' - 4y' + 4y = e^{2x}$.\n\n"
                "Characteristic $r^2 - 4r + 4 = (r-2)^2$: DOUBLE root $2$, so "
                "$y_h = (c_1 + c_2 x)e^{2x}$. Forcing $k = 2$ is that double root, so "
                "bump by $x^2$: trial $y_p = Ax^2 e^{2x}$. Differentiate: "
                "$y_p' = Ae^{2x}(2x + 2x^2)$, "
                "$y_p'' = Ae^{2x}(2 + 8x + 4x^2)$. Substitute:\n"
                "$$Ae^{2x}\\big[(2 + 8x + 4x^2) - 4(2x + 2x^2) + 4x^2\\big] "
                "= Ae^{2x}\\cdot 2.$$\n"
                "The $x$ and $x^2$ terms cancel (they must - that is the double "
                "collision), leaving $2Ae^{2x} = e^{2x}$, so $A = \\tfrac{1}{2}$ and "
                "$y_p = \\tfrac{1}{2}x^2 e^{2x}$. Had we used only $x^1$, the trial "
                "$Axe^{2x}$ is STILL homogeneous (it is $c_2 x e^{2x}$), so it would "
                "have collapsed to $0 = e^{2x}$ again - which is why the exponent must "
                "match the FULL multiplicity, not just 'multiply by $x$ once.'"
            )),
            # ---------------------------------------------------------- 6
            (L, "Properties: why s equals the multiplicity, and near-resonance", (
                "TWO STRUCTURAL FACTS worth internalizing.\n\n"
                "FIRST, $s$ = multiplicity, always, and the smallest such $s$ works. "
                "The factor-out derivation (page 3) shows a root of multiplicity $m$ "
                "makes the coefficients $p(k), p'(k), \\ldots, p^{(m-1)}(k)$ all "
                "vanish, so the reduced $u$-equation's lowest surviving term is the "
                "$u^{(m)}$ term; $u$ must therefore climb $m$ degrees, i.e. carry "
                "$x^m$. Using $s$ SMALLER than the multiplicity leaves the trial "
                "inside $y_h$ (collapse persists); using $s$ LARGER wastes effort and "
                "introduces coefficients that come out zero. Match it exactly.\n\n"
                "SECOND, resonance is the limit of near-resonance, and this explains "
                "the $t$-growth. Consider $y'' + \\omega_0^2 y = \\cos\\omega t$ with "
                "$\\omega$ CLOSE to but not equal to $\\omega_0$. The bounded "
                "particular solution is "
                "$y_p = \\dfrac{\\cos\\omega t}{\\omega_0^2 - \\omega^2}$; its "
                "amplitude $\\dfrac{1}{\\omega_0^2 - \\omega^2}$ blows up as "
                "$\\omega \\to \\omega_0$. Exactly AT $\\omega = \\omega_0$ the "
                "formula is undefined (the collision), and the response that replaces "
                "it is the unbounded $y_p = \\dfrac{t}{2\\omega_0}\\sin\\omega_0 t$. "
                "So the $t$ is the 'infinity' of the amplitude formula, made into a "
                "growing-in-time function rather than a divide-by-zero.\n\n"
                "A related bounded phenomenon: for $\\omega$ near $\\omega_0$, the "
                "superposition of the natural and forced oscillations produces BEATS - "
                "a slow amplitude throb at frequency "
                "$|\\omega_0 - \\omega|/2$, like two slightly detuned guitar strings. "
                "Beats are the audible warning that you are approaching resonance; at "
                "exact resonance the beat period stretches to infinity and the "
                "envelope becomes the straight line $t/(2\\omega_0)$. This continuity "
                "- bounded, then beating, then linearly growing - is the cleanest way "
                "to see that resonance is not a special exception but the extreme edge "
                "of ordinary forced response."
            )),
            # ---------------------------------------------------------- 7
            (E, "Standard worked examples", (
                "EXAMPLE 1 (polynomial forcing with $0$ a root). Solve "
                "$y'' - 3y' = 6x$.\n\n"
                "Roots of $r^2 - 3r = r(r-3)$: $0$ and $3$. Since $c = 0$, the hidden "
                "exponent $k = 0$ IS a simple root, so the polynomial trial must be "
                "bumped by $x$. Ordinary trial would be $Ax + B$; modified trial "
                "$y_p = x(Ax + B) = Ax^2 + Bx$. Then $y_p' = 2Ax + B$, $y_p'' = 2A$. "
                "Substitute: $2A - 3(2Ax + B) = -6Ax + (2A - 3B) = 6x$. Match: "
                "$-6A = 6 \\Rightarrow A = -1$; $2A - 3B = 0 \\Rightarrow "
                "B = -\\tfrac{2}{3}$. So $y_p = -x^2 - \\tfrac{2}{3}x$. (The constant "
                "term of the ordinary trial got absorbed into $y_h$'s constant, which "
                "is exactly why the bump was needed.)\n\n"
                "EXAMPLE 2 (product forcing colliding at a simple root). Solve "
                "$y'' - y = x e^{x}$.\n\n"
                "Roots of $r^2 - 1$: $\\pm 1$; $k = 1$ is a simple root. Ordinary "
                "product trial $(Ax + B)e^x$ collides (its $Be^x$ piece is "
                "homogeneous), so bump by $x$: $y_p = x(Ax + B)e^x = (Ax^2 + Bx)e^x$. "
                "Use the factor-out shortcut with $k = 1$: "
                "$y = e^x u$ gives $u'' + 2u' + p(1)u = x$ with $p(1) = 0$, i.e. "
                "$u'' + 2u' = x$. Since $0$ is now a root of THIS reduced equation, "
                "$u$ needs a factor of $x$: $u = Ax^2 + Bx$, $u' = 2Ax + B$, "
                "$u'' = 2A$. Then $2A + 2(2Ax + B) = 4Ax + (2A + 2B) = x$, so "
                "$A = \\tfrac{1}{4}$ and $2A + 2B = 0 \\Rightarrow B = -\\tfrac{1}{4}$. "
                "Thus $u = \\tfrac{1}{4}x^2 - \\tfrac{1}{4}x$ and "
                "$y_p = \\big(\\tfrac{1}{4}x^2 - \\tfrac{1}{4}x\\big)e^x$ - the extra "
                "$x$ appears automatically because the collision reduced the "
                "$u$-equation's order."
            )),
            # ---------------------------------------------------------- 8
            (L, "Technique: resonance in the undamped oscillator", (
                "The purest and most important resonance problem is the undamped "
                "oscillator driven at its natural frequency: "
                "$y'' + \\omega_0^2 y = F_0\\cos\\omega_0 t$. Master this template and "
                "you own the physics.\n\n"
                "Homogeneous: $r^2 + \\omega_0^2 = 0 \\Rightarrow r = \\pm i\\omega_0$, "
                "so $y_h = c_1\\cos\\omega_0 t + c_2\\sin\\omega_0 t$ - undamped "
                "oscillation at $\\omega_0$. The forcing $\\cos\\omega_0 t$ has the "
                "SAME frequency, so the ordinary trial "
                "$A\\cos\\omega_0 t + B\\sin\\omega_0 t$ is entirely homogeneous: a "
                "collision at the imaginary roots $\\pm i\\omega_0$. Bump by $x$ (here "
                "$t$): trial $y_p = t(A\\cos\\omega_0 t + B\\sin\\omega_0 t)$.\n\n"
                "To find $A, B$, it is cleanest to try $y_p = Ct\\sin\\omega_0 t$ "
                "(symmetry kills the cosine part for a pure cosine drive). "
                "Differentiate: "
                "$y_p' = C\\sin\\omega_0 t + C\\omega_0 t\\cos\\omega_0 t$, "
                "$y_p'' = 2C\\omega_0\\cos\\omega_0 t - C\\omega_0^2 t\\sin\\omega_0 t$. "
                "Then\n"
                "$$y_p'' + \\omega_0^2 y_p = 2C\\omega_0\\cos\\omega_0 t,$$\n"
                "because the two $t\\sin\\omega_0 t$ terms cancel exactly (the "
                "collision, again). Match to $F_0\\cos\\omega_0 t$: "
                "$2C\\omega_0 = F_0$, so $C = \\dfrac{F_0}{2\\omega_0}$ and\n"
                "$$y_p = \\frac{F_0}{2\\omega_0}\\,t\\sin\\omega_0 t.$$\n"
                "Read it: the amplitude is $\\dfrac{F_0}{2\\omega_0}\\,t$, growing "
                "WITHOUT BOUND as $t$ increases. There is no steady state - the "
                "oscillation swells forever (in the idealized undamped model). This is "
                "the resonance catastrophe. Real systems have damping, which caps the "
                "growth at a large but finite amplitude (N4's steady state), but the "
                "$t\\sin\\omega_0 t$ solution is the mathematical heart of why driving "
                "at the natural frequency is dangerous."
            )),
            # ---------------------------------------------------------- 9
            (E, "Harder worked examples", (
                "EXAMPLE 1 (resonant oscillator with data). Solve "
                "$y'' + 4y = 3\\cos 2t$, $y(0) = 0$, $y'(0) = 0$.\n\n"
                "Natural frequency $\\omega_0 = 2$ matches the drive - resonance. From "
                "the template with $\\omega_0 = 2$, $F_0 = 3$: "
                "$y_p = \\dfrac{3}{2\\cdot 2}t\\sin 2t = \\dfrac{3}{4}t\\sin 2t$. "
                "General solution "
                "$y = c_1\\cos 2t + c_2\\sin 2t + \\tfrac{3}{4}t\\sin 2t$. Apply data: "
                "$y(0) = c_1 = 0$. "
                "$y' = -2c_1\\sin 2t + 2c_2\\cos 2t + \\tfrac{3}{4}\\sin 2t + "
                "\\tfrac{3}{2}t\\cos 2t$; $y'(0) = 2c_2 = 0 \\Rightarrow c_2 = 0$. So "
                "$y = \\tfrac{3}{4}t\\sin 2t$ - a clean, purely growing resonant "
                "response, amplitude $\\tfrac{3}{4}t$.\n\n"
                "EXAMPLE 2 (collision on only part of a compound forcing). Solve "
                "$y'' - y = e^{x} + e^{2x}$.\n\n"
                "Roots $\\pm 1$; $y_h = c_1 e^x + c_2 e^{-x}$. Split by superposition. "
                "For $e^x$: $k = 1$ is a simple root, so bump - trial $A x e^x$. "
                "$\\tfrac{d^2}{dx^2}(Axe^x) - Axe^x = A e^x(x + 2) - Axe^x = 2Ae^x$; "
                "set $= e^x$, so $A = \\tfrac{1}{2}$, giving "
                "$y_{p,1} = \\tfrac{1}{2}xe^x$. For $e^{2x}$: $k = 2$ is NOT a root, "
                "ordinary trial $Be^{2x}$, and $p(2) = 4 - 1 = 3$, so "
                "$y_{p,2} = \\tfrac{1}{3}e^{2x}$. Combine: "
                "$y_p = \\tfrac{1}{2}xe^x + \\tfrac{1}{3}e^{2x}$. The lesson: the "
                "modification touched ONLY the resonant piece; the non-resonant piece "
                "kept its plain guess. Always test each forcing term against $y_h$ "
                "separately."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: damping, and 'almost' collisions", (
                "Two edges sharpen your judgment about when to modify.\n\n"
                "DAMPING MOVES THE ROOTS OFF THE IMAGINARY AXIS, so a DAMPED "
                "oscillator driven at 'the' natural frequency does NOT strictly "
                "resonate in the $x^s$ sense. Consider "
                "$y'' + 2y' + 5y = \\cos\\omega t$. Roots are $-1 \\pm 2i$ (complex "
                "with nonzero real part), so $y_h = e^{-t}(c_1\\cos 2t + "
                "c_2\\sin 2t)$ decays. The forcing $\\cos\\omega t$ corresponds to "
                "$\\pm i\\omega$ on the IMAGINARY axis, which never equals "
                "$-1 \\pm 2i$; there is no collision, so the ordinary trial "
                "$A\\cos\\omega t + B\\sin\\omega t$ works for EVERY $\\omega$. The "
                "response stays bounded - damping prevents the catastrophe. What "
                "survives is a PEAK: the steady-state amplitude is largest near "
                "$\\omega \\approx 2$ but finite, and its height is set by how small "
                "the damping is. Pure $x^s$ resonance is the zero-damping idealization.\n\n"
                "'ALMOST' COLLISIONS DO NOT COUNT. If the drive rate is $2.0001$ and a "
                "root is $2$, they are DIFFERENT numbers - no modification. The "
                "ordinary trial applies and the amplitude $1/p(k)$ is merely large, "
                "not infinite. Students sometimes bump a guess because the numbers "
                "look 'close'; the rule is exact equality of $k$ with a root, nothing "
                "less. (The large-but-finite amplitude is the beats/near-resonance "
                "story of page 6.)\n\n"
                "A THIRD EDGE: repeated modification. If a forcing collides with a "
                "double root, one factor of $x$ is not enough - $xe^{kx}$ is still "
                "homogeneous when $r = k$ is doubled. You need $x^2$. The safe habit "
                "is always to write $y_h$ explicitly and keep multiplying by $x$ until "
                "NO term of the trial appears in $y_h$; the number of factors you "
                "needed is $s$, and it will equal the multiplicity every time."
            )),
            # ---------------------------------------------------------- 11
            (E, "Edge-case worked example: insufficient bump, then fixed", (
                "Solve $y'' - 4y' + 4y = 3e^{2x}$, and see why $x^1$ fails before "
                "$x^2$ succeeds.\n\n"
                "Homogeneous: $(r-2)^2 = 0$, double root $2$, "
                "$y_h = (c_1 + c_2 x)e^{2x}$. Forcing $k = 2$ collides with a "
                "multiplicity-2 root.\n\n"
                "WRONG BUMP ($x^1$). Try $y_p = Axe^{2x}$. But $xe^{2x}$ is precisely "
                "the $c_2$ term of $y_h$ - still homogeneous! Substituting: "
                "$y_p' = Ae^{2x}(1 + 2x)$, $y_p'' = Ae^{2x}(4 + 4x)$, and "
                "$y_p'' - 4y_p' + 4y_p = Ae^{2x}\\big[(4+4x) - 4(1+2x) + 4x\\big] "
                "= Ae^{2x}\\cdot 0 = 0$. We are told this equals $3e^{2x}$: "
                "$0 = 3e^{2x}$, impossible. One factor of $x$ was not enough because "
                "the root has multiplicity 2.\n\n"
                "CORRECT BUMP ($x^2$). Try $y_p = Ax^2 e^{2x}$. From Example 5.2's "
                "computation, $y_p'' - 4y_p' + 4y_p = 2Ae^{2x}$. Set "
                "$2Ae^{2x} = 3e^{2x}$: $A = \\tfrac{3}{2}$, so "
                "$y_p = \\tfrac{3}{2}x^2 e^{2x}$. Verify: with "
                "$y_p = \\tfrac{3}{2}x^2 e^{2x}$, the $x^2$ and $x$ terms cancel by "
                "the double collision and the surviving constant gives $3e^{2x}$. "
                "Correct.\n\n"
                "MORAL. The exponent must equal the FULL multiplicity, found by "
                "checking the trial against every term of $y_h$. When in doubt, write "
                "$y_h$ out and multiply by $x$ until the trial shares nothing with it. "
                "The general solution here is "
                "$y = (c_1 + c_2 x)e^{2x} + \\tfrac{3}{2}x^2 e^{2x}$ - three powers of "
                "$x$ on the same exponential, a hallmark of double-root resonance."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'Always multiply by $x$ when there is a collision.' "
                "Only for a SIMPLE root. A double root needs $x^2$; multiplying by "
                "$x$ once leaves the trial inside $y_h$ and the collapse persists "
                "(page 11). The exponent is the multiplicity.\n\n"
                "MISCONCEPTION 2: 'Resonance means the solution blows up instantly / "
                "the system breaks immediately.' The resonant solution "
                "$\\tfrac{F_0}{2\\omega_0}t\\sin\\omega_0 t$ grows LINEARLY, not "
                "explosively - amplitude proportional to $t$. It becomes destructive "
                "over many cycles, which is why breaking a glass or a bridge takes "
                "sustained driving, not one push.\n\n"
                "MISCONCEPTION 3: 'Any driven oscillator resonates.' Only forcing at "
                "(or, with damping, near) the natural frequency does. Drive well away "
                "from $\\omega_0$ and the response is a modest bounded oscillation. "
                "Resonance is a frequency-matching phenomenon.\n\n"
                "MISCONCEPTION 4: 'Damped systems have unbounded resonance too.' No - "
                "damping moves the roots off the imaginary axis, so no exact collision "
                "occurs and the steady-state amplitude stays finite, however large. "
                "The unbounded $t$-growth is the undamped idealization (page 10).\n\n"
                "MISCONCEPTION 5: 'When I bump the trig trial, I only need the term "
                "that appears in the forcing.' You must keep BOTH: "
                "$xt(A\\cos + B\\sin)$, both coefficients. Dropping one makes the "
                "trial too narrow and the match fails - even a pure-cosine drive can "
                "require the sine term (and vice versa) once the $x$ factor introduces "
                "cross-terms through differentiation.\n\n"
                "MISCONCEPTION 6: 'Tacoma Narrows was simple resonance.' The famous "
                "bridge failure was primarily aeroelastic FLUTTER, a feedback "
                "instability, not textbook forced resonance. Cleaner honest examples "
                "are a child's swing, soldiers breaking step on a footbridge, and a "
                "wine glass driven at its ringing pitch."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational and procedural errors", (
                "ERROR 1: Using the wrong exponent $s$. The fix is $s$ = root "
                "multiplicity, determined from the FACTORED characteristic polynomial. "
                "A double root needs $x^2$; a simple root, $x^1$. Always factor first "
                "and read the multiplicity off the repeated factor.\n\n"
                "ERROR 2: Not writing $y_h$ before bumping. Without $y_h$ in front of "
                "you, it is easy to miss that $xe^{2x}$ is still homogeneous for a "
                "double root. Write $y_h$ explicitly, then compare.\n\n"
                "ERROR 3: Bumping only part of a group. For $g = \\cos\\omega t$ at "
                "resonance, the trial is $x(A\\cos\\omega t + B\\sin\\omega t)$ - the "
                "$x$ multiplies BOTH terms. Writing $Ax\\cos\\omega t + B\\sin\\omega "
                "t$ (bump on one term only) is wrong and inconsistent.\n\n"
                "ERROR 4: Product-rule mistakes on $t\\sin\\omega t$ and "
                "$t\\cos\\omega t$. Recall "
                "$\\frac{d}{dt}(t\\sin\\omega t) = \\sin\\omega t + \\omega "
                "t\\cos\\omega t$; the free (non-$t$) term is what survives to match "
                "the forcing after the $t$-terms cancel. Dropping it loses the whole "
                "answer.\n\n"
                "ERROR 5: Bumping a non-collision. If $k$ is merely CLOSE to a root "
                "but not equal, do NOT multiply by $x$; use the ordinary trial. The "
                "large amplitude $1/p(k)$ is correct and finite. The rule triggers on "
                "exact equality only.\n\n"
                "ERROR 6: Forgetting the extra $x$ propagates into derivatives when "
                "you apply initial conditions. With $y_p = \\tfrac{3}{4}t\\sin 2t$, "
                "$y_p'$ has a free $\\tfrac{3}{4}\\sin 2t$ term that contributes at "
                "$t = 0$ only through $\\sin 0 = 0$ here, but in general the resonant "
                "$y_p$ and its derivative BOTH feed the data equations - do not apply "
                "conditions to $y_h$ alone (N4)."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: resonance as friend and foe", (
                "Resonance is one of the few pieces of ODE theory that a "
                "non-specialist has felt directly.\n\n"
                "1. MECHANICAL RESONANCE (the danger). Rotating machinery, vehicle "
                "suspensions, tall buildings in wind, and footbridges all have natural "
                "frequencies. If a periodic force - an unbalanced motor, a gust "
                "pattern, a crowd walking in step - matches a natural frequency, the "
                "$t$-growing response builds large oscillations that can fatigue or "
                "destroy the structure. Engineers compute natural frequencies "
                "specifically to keep operating forces away from them, and add damping "
                "(which caps the growth) where they cannot. The Millennium Bridge in "
                "London famously had to be retrofitted with dampers after pedestrian "
                "footfall synchronized near a lateral natural frequency.\n\n"
                "2. ELECTRICAL RESONANCE (the friend). An LC or RLC circuit has a "
                "resonant frequency $\\omega_0 = 1/\\sqrt{LC}$. Tuning a radio means "
                "adjusting $C$ so $\\omega_0$ equals the desired station's carrier "
                "frequency; at resonance that station's signal produces a large "
                "response while others produce little. Here resonance is the whole "
                "point - selective amplification.\n\n"
                "3. ACOUSTIC AND MOLECULAR RESONANCE. A wine glass shatters when "
                "driven at its ringing frequency; musical instruments use resonant "
                "cavities to amplify particular pitches; magnetic resonance imaging "
                "(MRI) drives nuclear spins at their resonant frequency to make them "
                "respond detectably.\n\n"
                "4. THE DESIGN PRINCIPLE. In every case the mathematics is this node's: "
                "the response is bounded and modest UNLESS the drive frequency matches "
                "a natural frequency, at which point the $x^s$ factor signals "
                "unbounded (undamped) or peaked (damped) growth. Whether you are "
                "avoiding resonance (structures) or exploiting it (radios, MRI), the "
                "quantity you compute first is the natural frequency - the root the "
                "forcing must, or must not, hit."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: driving a swing at resonance", (
                "Model a swing (a light pendulum, small oscillations) as an undamped "
                "oscillator with natural frequency $\\omega_0 = 2$ rad/s: "
                "$y'' + 4y = F_0\\cos 2t$, started from rest at the bottom, "
                "$y(0) = 0$, $y'(0) = 0$. Take $F_0 = 1$ for concreteness. Question: "
                "describe the motion and find when the amplitude first reaches, say, "
                "$5$ (in the model's units).\n\n"
                "STEP 1 - homogeneous: $y_h = c_1\\cos 2t + c_2\\sin 2t$, the swing's "
                "free oscillation at $\\omega_0 = 2$.\n\n"
                "STEP 2 - particular: the drive is at $\\omega_0$, so resonance. "
                "Template gives "
                "$y_p = \\dfrac{F_0}{2\\omega_0}t\\sin 2t = \\dfrac{1}{4}t\\sin 2t$.\n\n"
                "STEP 3 - general: "
                "$y = c_1\\cos 2t + c_2\\sin 2t + \\tfrac{1}{4}t\\sin 2t$.\n\n"
                "STEP 4 - data last. $y(0) = c_1 = 0$. "
                "$y' = -2c_1\\sin 2t + 2c_2\\cos 2t + \\tfrac{1}{4}\\sin 2t + "
                "\\tfrac{1}{2}t\\cos 2t$; at $t = 0$, $y'(0) = 2c_2 = 0$, so "
                "$c_2 = 0$. Thus $y = \\tfrac{1}{4}t\\sin 2t$.\n\n"
                "INTERPRETATION. The swing oscillates at its natural rhythm, but "
                "inside an envelope $\\pm\\tfrac{1}{4}t$ that grows LINEARLY in time. "
                "Every perfectly-timed push adds a little, and the arc widens steadily "
                "- exactly what a child does by pumping in rhythm. The amplitude "
                "envelope reaches $5$ when $\\tfrac{1}{4}t = 5$, i.e. $t = 20$ s (the "
                "peaks near that time have height about $5$). In reality air "
                "resistance and the pendulum's nonlinearity cap the growth, but the "
                "resonant $t\\sin 2t$ solution captures the essential truth: matching "
                "the natural frequency turns small repeated pushes into large motion."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: where this node plugs in", (
                "BACKWARD. This node is the sequel to ODE.U3.N1: it resolves exactly "
                "the collision that N1 taught you to detect, and it relies on "
                "ODE.U2.N5 for root multiplicity (simple vs double, real vs complex) - "
                "the multiplicity IS the modification exponent. The undamped-oscillator "
                "template rests on the complex-roots case of the homogeneous theory.\n\n"
                "SIDEWAYS. ODE.U3.N3 (variation of parameters) reaches the SAME "
                "resonant particular solutions without a special rule: its integral "
                "formula automatically produces the $x^s$ factor (the integrand "
                "becomes a constant or a polynomial, integrating to a power of $x$), "
                "so it is a useful cross-check when you are unsure whether or how to "
                "bump.\n\n"
                "FORWARD. ODE.U3.N4 (the full solution) combines the resonant $y_p$ "
                "with $y_h$ and applies data; in DAMPED systems it contrasts the "
                "decaying transient $y_h$ with the persistent steady-state $y_p$, "
                "which is the bounded cousin of resonance. The Laplace transform "
                "(ODE.U4) makes resonance especially transparent: a repeated factor "
                "$(s - k)^2$ in the transformed solution inverts to $te^{kt}$, and "
                "$(s^2 + \\omega_0^2)^2$ inverts to $t\\sin\\omega_0 t$ - the "
                "$t$-growth appears as a REPEATED denominator, the frequency-domain "
                "signature of driving at a natural frequency (ODE.U4.N2).\n\n"
                "FURTHER OUT. Forced-vibration analysis, the frequency response "
                "$H(\\omega)$ and resonance peaks, quality factor $Q$, and Bode plots "
                "in signals and control all generalize this one calculation. Whenever "
                "an engineer says 'stay away from the natural frequency' or 'tune to "
                "resonance,' they are invoking the $x^s$ modification you learned "
                "here."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: resonance as a repeated root", (
                "The deepest way to see the modification rule is that FORCING pushes "
                "a root's multiplicity up by one, and repeated roots always carry "
                "extra powers of $x$.\n\n"
                "Recall from the annihilator viewpoint (N1 page 17): to solve "
                "$P(D)y = g$ you apply the annihilator $A(D)$ of $g$ and study the "
                "homogeneous equation $A(D)P(D)y = 0$. Now suppose $g = e^{kx}$, "
                "annihilated by $(D - k)$, and $k$ is already a root of $P$ with "
                "multiplicity $m$. Then in $A(D)P(D)$ the factor $(D - k)$ appears "
                "$m + 1$ times: the forcing has PROMOTED the root to multiplicity "
                "$m + 1$. And the homogeneous solutions for a root of multiplicity "
                "$m + 1$ are $e^{kx}, xe^{kx}, \\ldots, x^m e^{kx}$ - the highest, "
                "$x^m e^{kx}$, is the NEW basis function not already in $y_h$, and it "
                "is exactly the modified trial. So $x^s$ with $s = m$ is not a patch; "
                "it is the extra solution the promoted root contributes.\n\n"
                "This unifies resonance across all cases. Undamped mechanical "
                "resonance is the imaginary-root version: $\\cos\\omega_0 x$ is "
                "annihilated by $(D^2 + \\omega_0^2)$, and if $\\pm i\\omega_0$ are "
                "already roots of $P$, they get promoted to double roots, whose extra "
                "solutions are $x\\cos\\omega_0 x$ and $x\\sin\\omega_0 x$ - the "
                "$t$-growing resonant response. Electrical, acoustic, and mechanical "
                "resonance are one theorem.\n\n"
                "One more layer: in the Laplace domain (U4), a promoted root becomes a "
                "REPEATED pole $(s - k)^{m+1}$, and the inverse transform of "
                "$1/(s-k)^{m+1}$ is $\\tfrac{t^m}{m!}e^{kt}$ - the same power of $t$, "
                "read off the pole's order. Resonance, root promotion, repeated poles, "
                "and $t$-growth are four faces of a single mathematical fact, and "
                "seeing them as one is a mark of real fluency with linear systems."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "THE MODIFICATION RULE. Form the ordinary trial (N1 table). If a "
                "group of terms coincides with a homogeneous solution, multiply that "
                "whole group by $x^s$, where $s$ = multiplicity of the offending "
                "characteristic root. Use the smallest $s$ that removes all overlap "
                "with $y_h$.\n\n"
                "BY CASE:\n"
                "- $e^{kx}$, $k$ a simple root $\\to Axe^{kx}$; double root "
                "$\\to Ax^2 e^{kx}$.\n"
                "- polynomial with $0$ a simple root $\\to x(A_n x^n + \\cdots)$; "
                "double $\\to x^2(\\cdots)$.\n"
                "- $\\cos\\omega x/\\sin\\omega x$ with $\\pm i\\omega$ roots "
                "(undamped resonance) $\\to x(A\\cos\\omega x + B\\sin\\omega x)$.\n\n"
                "UNDAMPED RESONANCE TEMPLATE. "
                "$y'' + \\omega_0^2 y = F_0\\cos\\omega_0 t$ has "
                "$y_p = \\dfrac{F_0}{2\\omega_0}\\,t\\sin\\omega_0 t$: amplitude "
                "$\\dfrac{F_0}{2\\omega_0}t$ grows linearly - the resonance "
                "catastrophe.\n\n"
                "WHY $s$ = MULTIPLICITY. A root of multiplicity $m$ zeros "
                "$p(k), \\ldots, p^{(m-1)}(k)$; the reduced $u$-equation then governs "
                "$u^{(m)}$, so $u$ climbs $m$ degrees. Equivalently, forcing promotes "
                "the root to multiplicity $m+1$, whose new basis function is "
                "$x^m e^{kx}$.\n\n"
                "DAMPING. Damped systems have roots off the imaginary axis, so exact "
                "collisions do not occur; the response is bounded with a finite "
                "resonance PEAK, not $t$-growth.\n\n"
                "RED FLAGS. $0 = g$ after substitution = you did not bump, or did not "
                "bump enough. If $xe^{kx}$ is still homogeneous (double root), you "
                "need $x^2$. Always write $y_h$ first and keep multiplying by $x$ "
                "until the trial shares nothing with it."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all six before reading solutions.\n\n"
                "1. Correct trial form (do not solve) for $y'' - 5y' + 6y = e^{2x}$?\n"
                "2. Correct trial form for $y'' - 4y' + 4y = 7e^{2x}$?\n"
                "3. Solve $y'' + 9y = \\cos 3t$ for a particular solution.\n"
                "4. Correct trial form for $y'' + y' = 4x$ (note the missing "
                "constant term)?\n"
                "5. True or false: a damped oscillator driven at $\\omega_0$ has an "
                "unbounded resonant response.\n"
                "6. Solve $y'' - 2y' + y = e^x$ for a particular solution (double "
                "root).\n\n"
                "SOLUTIONS.\n"
                "1. Roots $2, 3$; $k = 2$ simple root, so trial $y_p = Axe^{2x}$ "
                "(bump by $x$).\n"
                "2. Roots: double $2$; $k = 2$ double root, so trial "
                "$y_p = Ax^2 e^{2x}$ (bump by $x^2$).\n"
                "3. $\\omega_0 = 3$ matches drive - resonance. "
                "$y_p = \\dfrac{1}{2\\cdot 3}t\\sin 3t = \\dfrac{1}{6}t\\sin 3t$.\n"
                "4. Roots of $r^2 + r = r(r+1)$: $0, -1$. Since $0$ is a simple root, "
                "bump the polynomial trial: $y_p = x(Ax + B) = Ax^2 + Bx$.\n"
                "5. FALSE. Damping moves the roots off the imaginary axis, so there "
                "is no exact collision; the steady-state amplitude is finite (a peak), "
                "not $t$-growing.\n"
                "6. Double root $1$; $k = 1$ double root, trial $y_p = Ax^2 e^x$. "
                "Then $y_p'' - 2y_p' + y_p = 2Ae^x$ (the $x$, $x^2$ terms cancel), so "
                "$2A = 1$, $y_p = \\tfrac{1}{2}x^2 e^x$.\n\n"
                "Scoring: 6/6 - proceed to N3. 4-5 - reread the rule (page 4) and the "
                "double-root example (page 11). $\\le 3$ - rework pages 3-11."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Check every box honestly:\n\n"
                "- State the modification rule and explain that $s$ equals the root "
                "multiplicity, not just 'multiply by $x$.'\n"
                "- Determine the multiplicity from the factored characteristic "
                "polynomial and choose the correct $x^s$.\n"
                "- Recognize that $xe^{kx}$ is still homogeneous for a double root, so "
                "a double-root collision needs $x^2$.\n"
                "- Derive the undamped resonant response "
                "$\\tfrac{F_0}{2\\omega_0}t\\sin\\omega_0 t$ and read its "
                "linear-in-$t$ amplitude growth.\n"
                "- Explain physically why forcing at the natural frequency accumulates "
                "energy (the swing), and give an honest real example.\n"
                "- Bump only the resonant piece of a compound forcing, keeping "
                "non-resonant pieces ordinary.\n"
                "- Keep BOTH coefficients when bumping a trig group "
                "($x(A\\cos + B\\sin)$).\n"
                "- Explain why a DAMPED system has a bounded resonance peak rather "
                "than $t$-growth (roots off the imaginary axis).\n"
                "- Distinguish exact resonance from near-resonance (large but finite "
                "amplitude, beats).\n"
                "- Connect $t$-growth to a repeated characteristic root and (looking "
                "ahead) to a repeated Laplace pole.\n\n"
                "When every box is checked, practice until you can classify a "
                "collision and write the right $x^s$ at a glance, then proceed to "
                "ODE.U3.N3, the universal method that handles forcings no guess can "
                "touch."
            )),
        ],
    },
    "ODE.U3.N3": {
        "summary": (
            "A complete course on variation of parameters: the universal "
            "particular-solution formula via the Wronskian (u1' = -y2 g / W, "
            "u2' = y1 g / W), the requirement to normalize the leading coefficient, "
            "why it works for ANY continuous forcing (tan x, sec x, 1/x), and how to "
            "decide when to use it versus undetermined coefficients."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "Undetermined coefficients (N1-N2) is fast, but it is a specialist: it "
                "only works when the forcing $g$ is a polynomial, exponential, "
                "sine/cosine, or a finite product of those - functions with a finite "
                "closed family of derivatives. The moment the forcing is "
                "$\\tan x$, $\\sec x$, $\\ln x$, $1/x$, or $e^x/x$, no finite guess "
                "closes and the method has nothing to offer. This node is the "
                "universal tool that never fails: VARIATION OF PARAMETERS.\n\n"
                "The idea is beautiful. You already have two independent homogeneous "
                "solutions $y_1, y_2$ (from ODE.U2.N5). Instead of guessing $y_p$, you "
                "let the CONSTANTS in $y_h = c_1 y_1 + c_2 y_2$ become FUNCTIONS: "
                "$y_p = u_1(x)y_1 + u_2(x)y_2$. The constants that were fixed in the "
                "homogeneous solution now 'vary' to absorb the forcing - hence the "
                "name. With one clever extra condition to keep the algebra manageable, "
                "$u_1$ and $u_2$ are pinned down by a small linear system whose "
                "solution is\n"
                "$$u_1' = -\\frac{y_2\\,g}{W}, \\qquad u_2' = \\frac{y_1\\,g}{W},$$\n"
                "where $W = y_1 y_2' - y_2 y_1'$ is the WRONSKIAN of the two "
                "solutions. Integrate to get $u_1, u_2$, and $y_p = u_1 y_1 + "
                "u_2 y_2$.\n\n"
                "That formula is the whole node. It works for EVERY continuous "
                "forcing - the price is that you must actually integrate, and the "
                "integrals can be hard or even non-elementary. This node teaches the "
                "derivation (so the formula is not magic), the non-negotiable "
                "normalization step, worked examples on forcings undetermined "
                "coefficients cannot touch, and the strategic judgment of which method "
                "to reach for. It is the mathematical safety net beneath the entire "
                "unit."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Variation of parameters combines homogeneous solutions, "
                "determinants, and integration, so gather all three.\n\n"
                "1. TWO INDEPENDENT HOMOGENEOUS SOLUTIONS. From ODE.U2.N5 you can "
                "produce $y_1, y_2$ for any constant-coefficient equation (and the "
                "method actually works for VARIABLE coefficients too, given a "
                "fundamental set). Examples: $y'' + y = 0$ gives "
                "$y_1 = \\cos x, y_2 = \\sin x$; $y'' - 2y' + y = 0$ gives "
                "$y_1 = e^x, y_2 = xe^x$.\n\n"
                "2. THE WRONSKIAN. $W(y_1, y_2) = y_1 y_2' - y_2 y_1'$ - a "
                "$2\\times 2$ determinant $\\begin{vmatrix} y_1 & y_2 \\\\ y_1' & y_2' "
                "\\end{vmatrix}$. It is nonzero wherever $y_1, y_2$ are independent, so "
                "dividing by $W$ is safe. You will compute it constantly.\n\n"
                "3. INTEGRATION TECHNIQUE (C202-level). The whole method ends in two "
                "integrals $\\int u_1'\\,dx$ and $\\int u_2'\\,dx$. You need "
                "substitution, integration by parts, and the standard trig integrals - "
                "notably $\\int\\sec x\\,dx = \\ln|\\sec x + \\tan x|$ and "
                "$\\int\\tan x\\,dx = -\\ln|\\cos x|$, which appear the moment the "
                "forcing is trigonometric.\n\n"
                "Quick self-check (answers in parentheses):\n\n"
                "- $W$ for $y_1 = \\cos x, y_2 = \\sin x$? "
                "($\\cos x\\cos x - \\sin x(-\\sin x) = 1$.)\n"
                "- $W$ for $y_1 = e^x, y_2 = e^{3x}$? "
                "($e^x\\cdot 3e^{3x} - e^{3x}\\cdot e^x = 2e^{4x}$.)\n"
                "- $\\int \\sec x\\,dx$? ($\\ln|\\sec x + \\tan x| + C$.)\n\n"
                "If Wronskians or the sec/tan integrals are unfamiliar, review them "
                "now - they are the two most common stumbling points."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Undetermined coefficients guesses the ANSWER's shape. Variation of "
                "parameters instead reuses the STRUCTURE you already have - the "
                "homogeneous solutions - and asks them to carry the forcing.\n\n"
                "Start from $y_h = c_1 y_1 + c_2 y_2$. The constants $c_1, c_2$ make "
                "$y_h$ solve $Ly = 0$. To solve $Ly = g$, promote the constants to "
                "functions: $y_p = u_1(x)y_1 + u_2(x)y_2$. This is a genuine "
                "generalization - if $u_1, u_2$ were constant we would be back to "
                "$y_h$. Now we have TWO unknown functions but only ONE equation "
                "($Ly_p = g$), so we are free to impose one extra condition of our "
                "choosing. The clever choice is\n"
                "$$u_1' y_1 + u_2' y_2 = 0.$$\n"
                "Why this? Differentiating $y_p$ gives "
                "$y_p' = (u_1' y_1 + u_2' y_2) + (u_1 y_1' + u_2 y_2')$; the imposed "
                "condition kills the first bracket, so $y_p'$ has NO $u''$ terms "
                "lurking when we differentiate again. This keeps the second derivative "
                "clean and prevents a messy second-order equation in $u_1, u_2$.\n\n"
                "Differentiating once more and substituting into $Ly_p = g$ (using "
                "that $y_1, y_2$ solve $Ly = 0$, so all the $u_1, u_2$ terms without "
                "primes cancel) leaves a SECOND simple condition:\n"
                "$$u_1' y_1' + u_2' y_2' = g.$$\n"
                "So $u_1', u_2'$ satisfy a $2\\times 2$ LINEAR system:\n"
                "$$\\begin{cases} u_1' y_1 + u_2' y_2 = 0 \\\\ "
                "u_1' y_1' + u_2' y_2' = g. \\end{cases}$$\n"
                "The coefficient determinant is exactly the Wronskian $W$, and "
                "Cramer's rule solves it instantly: "
                "$u_1' = -\\dfrac{y_2 g}{W}$, $u_2' = \\dfrac{y_1 g}{W}$. Integrate "
                "and you are done. The 'magic' extra condition is really just a smart "
                "bookkeeping choice that turns a hard problem into two integrals."
            )),
            # ---------------------------------------------------------- 4
            (L, "The formula, stated precisely (and normalization)", (
                "THE METHOD for a second-order linear equation. First put the equation "
                "in STANDARD (normalized) form with leading coefficient 1:\n"
                "$$y'' + p(x)y' + q(x)y = g(x).$$\n"
                "This normalization is NON-NEGOTIABLE: the $g$ in the formulas is the "
                "right-hand side AFTER dividing through by the coefficient of $y''$. "
                "If your equation is $2y'' + \\ldots = 6\\tan x$, then "
                "$g = 3\\tan x$, not $6\\tan x$. Forgetting to divide is the single "
                "most common error in the whole method.\n\n"
                "Then, with a fundamental set $y_1, y_2$ of the homogeneous equation "
                "and Wronskian $W = y_1 y_2' - y_2 y_1'$:\n"
                "$$u_1' = -\\frac{y_2\\,g}{W}, \\qquad u_2' = \\frac{y_1\\,g}{W},$$\n"
                "$$u_1 = \\int -\\frac{y_2\\,g}{W}\\,dx, \\qquad "
                "u_2 = \\int \\frac{y_1\\,g}{W}\\,dx,$$\n"
                "$$\\boxed{\\,y_p = u_1 y_1 + u_2 y_2\\,}.$$\n"
                "In compact integral form,\n"
                "$$y_p = -y_1\\!\\int\\!\\frac{y_2\\,g}{W}\\,dx "
                "+ y_2\\!\\int\\!\\frac{y_1\\,g}{W}\\,dx.$$\n\n"
                "PRACTICAL NOTES. (1) You may drop integration constants when finding "
                "$u_1, u_2$: any constant times $y_1$ or $y_2$ is homogeneous and gets "
                "absorbed into $y_h$ later. (2) Mind the MINUS sign on $u_1'$ - it is "
                "the most frequently dropped symbol. (3) The formula needs only "
                "$y_1, y_2$ and $g$; it does NOT care whether the coefficients are "
                "constant, so it also solves variable-coefficient equations once you "
                "have a fundamental set (e.g. from a Cauchy-Euler equation). (4) The "
                "result $y_p$ is a genuine particular solution; the general solution is "
                "$y = c_1 y_1 + c_2 y_2 + y_p$ (N4), with data applied last."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples", (
                "EXAMPLE 1 (the classic $\\tan x$ - impossible for undetermined "
                "coefficients). Solve $y'' + y = \\tan x$.\n\n"
                "Homogeneous: $y_1 = \\cos x$, $y_2 = \\sin x$; "
                "$W = \\cos x\\cos x - \\sin x(-\\sin x) = 1$. Already normalized "
                "($g = \\tan x$). Then\n"
                "$$u_1' = -\\frac{y_2 g}{W} = -\\sin x\\tan x "
                "= -\\frac{\\sin^2 x}{\\cos x} = -\\frac{1 - \\cos^2 x}{\\cos x} "
                "= \\cos x - \\sec x,$$\n"
                "$$u_2' = \\frac{y_1 g}{W} = \\cos x\\tan x = \\sin x.$$\n"
                "Integrate: $u_1 = \\sin x - \\ln|\\sec x + \\tan x|$, "
                "$u_2 = -\\cos x$. Assemble:\n"
                "$$y_p = u_1 y_1 + u_2 y_2 = \\big(\\sin x - \\ln|\\sec x + \\tan "
                "x|\\big)\\cos x + (-\\cos x)\\sin x.$$\n"
                "The $\\sin x\\cos x$ terms cancel, leaving "
                "$y_p = -\\cos x\\,\\ln|\\sec x + \\tan x|$. Undetermined coefficients "
                "could never produce that logarithm.\n\n"
                "EXAMPLE 2 (secant). Solve $y'' + y = \\sec x$. Same $y_1, y_2, W$. "
                "$u_1' = -\\sin x\\sec x = -\\tan x$, so $u_1 = \\ln|\\cos x|$. "
                "$u_2' = \\cos x\\sec x = 1$, so $u_2 = x$. Then\n"
                "$$y_p = \\cos x\\,\\ln|\\cos x| + x\\sin x.$$\n"
                "Two forcings that stop undetermined coefficients cold, dispatched by "
                "the same three moves: Wronskian, two quotients, two integrals."
            )),
            # ---------------------------------------------------------- 6
            (L, "Properties: universality, the Wronskian, sign structure", (
                "THREE facts that make the method trustworthy.\n\n"
                "UNIVERSALITY. For ANY continuous $g$ on an interval, and any "
                "fundamental set $y_1, y_2$, the formula produces a valid particular "
                "solution - existence is guaranteed because $W \\ne 0$ (independence) "
                "means you can always solve the $2\\times 2$ system, and continuity of "
                "$g$ means the integrals exist (as functions, even if not in "
                "elementary closed form). This is why variation of parameters is the "
                "theoretical backbone: it PROVES that a particular solution exists, "
                "not merely that a lucky guess sometimes works.\n\n"
                "THE WRONSKIAN NEVER VANISHES for an independent pair, and for "
                "constant-coefficient equations it has clean closed forms: "
                "$y_1 = e^{r_1 x}, y_2 = e^{r_2 x}$ give "
                "$W = (r_2 - r_1)e^{(r_1 + r_2)x}$; a double root pair "
                "$e^{rx}, xe^{rx}$ gives $W = e^{2rx}$; a complex pair "
                "$e^{\\alpha x}\\cos\\beta x, e^{\\alpha x}\\sin\\beta x$ gives "
                "$W = \\beta e^{2\\alpha x}$. Abel's identity even predicts $W$ up to a "
                "constant from the coefficient $p$ alone: "
                "$W = C\\exp(-\\int p\\,dx)$ - a useful check.\n\n"
                "SIGN AND SYMMETRY. Note the asymmetry: $u_1'$ carries the MINUS and "
                "pairs $y_2$ with $g$; $u_2'$ is positive and pairs $y_1$ with $g$. A "
                "reliable mnemonic: 'the other solution, over the Wronskian, times the "
                "forcing, with a minus on the first.' The two formulas share the "
                "denominator $W$ and the numerator factor $g$; only the surviving "
                "$y_i$ and the sign differ. Because any multiple of $y_1$ or $y_2$ "
                "landing in $u_1, u_2$ is absorbed into $y_h$, you can and should drop "
                "integration constants - and you may simplify $y_p$ by discarding any "
                "homogeneous pieces that fall out (as happened with the "
                "$\\sin x\\cos x$ cancellation in Example 5.1)."
            )),
            # ---------------------------------------------------------- 7
            (E, "Standard worked examples", (
                "EXAMPLE 1 (exponential-over-polynomial forcing). Solve "
                "$y'' - 2y' + y = \\dfrac{e^x}{x}$ (undetermined coefficients cannot "
                "handle $e^x/x$).\n\n"
                "Homogeneous: double root $1$, $y_1 = e^x$, $y_2 = xe^x$. Wronskian: "
                "$W = e^x(e^x + xe^x) - xe^x\\cdot e^x = e^{2x} + xe^{2x} - xe^{2x} "
                "= e^{2x}$. Normalized already ($g = e^x/x$). Then\n"
                "$$u_1' = -\\frac{y_2 g}{W} = -\\frac{xe^x\\cdot e^x/x}{e^{2x}} "
                "= -\\frac{e^{2x}}{e^{2x}} = -1 \\;\\Rightarrow\\; u_1 = -x,$$\n"
                "$$u_2' = \\frac{y_1 g}{W} = \\frac{e^x\\cdot e^x/x}{e^{2x}} "
                "= \\frac{1}{x} \\;\\Rightarrow\\; u_2 = \\ln|x|.$$\n"
                "Assemble: $y_p = u_1 y_1 + u_2 y_2 = -xe^x + xe^x\\ln|x| "
                "= xe^x(\\ln|x| - 1)$. The $-xe^x$ piece is a multiple of $y_2$, so it "
                "may be absorbed into $y_h$; a clean representative is "
                "$y_p = xe^x\\ln|x|$.\n\n"
                "EXAMPLE 2 (distinct real roots - cross-check against N1). Solve "
                "$y'' - 3y' + 2y = e^{3x}$ by variation of parameters, and confirm it "
                "agrees with the undetermined-coefficients shortcut.\n\n"
                "Homogeneous: $y_1 = e^x, y_2 = e^{2x}$, "
                "$W = e^x\\cdot 2e^{2x} - e^{2x}\\cdot e^x = e^{3x}$. Normalized "
                "already ($g = e^{3x}$). Then\n"
                "$$u_1' = -\\frac{y_2 g}{W} = -\\frac{e^{2x}\\cdot e^{3x}}{e^{3x}} "
                "= -e^{2x} \\;\\Rightarrow\\; u_1 = -\\tfrac{1}{2}e^{2x},$$\n"
                "$$u_2' = \\frac{y_1 g}{W} = \\frac{e^{x}\\cdot e^{3x}}{e^{3x}} "
                "= e^{x} \\;\\Rightarrow\\; u_2 = e^{x}.$$\n"
                "(Watch the exponents: $e^x\\cdot e^{3x} = e^{4x}$, divided by "
                "$e^{3x}$ gives $e^{x}$ - not $e^{2x}$.) Assemble:\n"
                "$$y_p = u_1 y_1 + u_2 y_2 = -\\tfrac{1}{2}e^{2x}\\cdot e^{x} "
                "+ e^{x}\\cdot e^{2x} = -\\tfrac{1}{2}e^{3x} + e^{3x} "
                "= \\tfrac{1}{2}e^{3x}.$$\n"
                "Cross-check with N1's exponential shortcut: "
                "$p(3) = 9 - 9 + 2 = 2$, so $y_p = \\tfrac{1}{2}e^{3x}$ - identical. "
                "The two methods agree, and the shortcut is a fast sanity check on the "
                "(slower) variation-of-parameters route."
            )),
            # ---------------------------------------------------------- 8
            (L, "Technique: when to use which method", (
                "You now have two ways to find $y_p$. Choosing well saves time.\n\n"
                "USE UNDETERMINED COEFFICIENTS WHEN the coefficients are CONSTANT and "
                "$g$ is in the special family (polynomial, exponential, sine/cosine, "
                "and finite products/sums of these). It is faster - no integration, "
                "just differentiate-and-match - and it directly gives clean answers. "
                "This covers most textbook drives and nearly all standard engineering "
                "inputs (steps, ramps, exponentials, sinusoids). Reach for it FIRST "
                "whenever it applies.\n\n"
                "USE VARIATION OF PARAMETERS WHEN any of these hold: (a) $g$ is "
                "OUTSIDE the family - $\\tan x$, $\\sec x$, $\\csc x$, $\\ln x$, "
                "$1/x$, $e^x/x$, or any quotient/transcendental that undetermined "
                "coefficients cannot guess; (b) the equation has VARIABLE coefficients "
                "but you already possess a fundamental set (e.g. from a Cauchy-Euler "
                "equation); (c) you want a GENERAL formula or an existence guarantee "
                "rather than a specific number. Variation of parameters always works, "
                "at the cost of doing integrals that may be tedious or non-elementary.\n\n"
                "A DECISION CHECKLIST. (1) Constant coefficients AND family forcing? "
                "-> undetermined coefficients (check for resonance, N2). (2) Otherwise "
                "-> variation of parameters. (3) If the variation integrals turn out "
                "non-elementary, you may still LEAVE $y_p$ in integral form - it is a "
                "valid answer, and numerics or Laplace (U4) can take over. A common "
                "hybrid on exams: constant coefficients with a family forcing PLUS one "
                "exotic term - solve the family part by undetermined coefficients and "
                "the exotic part by variation of parameters, then add by "
                "superposition. Fluency means picking the cheaper tool for each piece, "
                "not dogmatically using one throughout."
            )),
            # ---------------------------------------------------------- 9
            (E, "Harder worked examples", (
                "EXAMPLE 1 (cosecant, with cancellation). Solve $y'' + y = \\csc x$.\n\n"
                "$y_1 = \\cos x, y_2 = \\sin x, W = 1$, $g = \\csc x = 1/\\sin x$.\n"
                "$$u_1' = -\\sin x\\cdot\\frac{1}{\\sin x} = -1 "
                "\\;\\Rightarrow\\; u_1 = -x,$$\n"
                "$$u_2' = \\cos x\\cdot\\frac{1}{\\sin x} = \\cot x "
                "\\;\\Rightarrow\\; u_2 = \\ln|\\sin x|.$$\n"
                "So $y_p = -x\\cos x + \\sin x\\,\\ln|\\sin x|$. Two different "
                "transcendental pieces - impossible to guess, routine to integrate.\n\n"
                "EXAMPLE 2 (hybrid: family part + exotic part). Solve "
                "$y'' + y = \\sec x + 4x$.\n\n"
                "Split by superposition. The $\\sec x$ piece is Example 5.2: "
                "$y_{p,1} = \\cos x\\ln|\\cos x| + x\\sin x$. The $4x$ piece is in the "
                "family, so use undetermined coefficients: $k = 0$ is not a root of "
                "$r^2 + 1$, trial $Ax + B$, and $y'' + y = Ax + B$ forces $A = 4, "
                "B = 0$, so $y_{p,2} = 4x$. Combine: "
                "$y_p = \\cos x\\ln|\\cos x| + x\\sin x + 4x$. Using the cheaper tool "
                "on the cheaper piece is the whole point of page 8.\n\n"
                "EXAMPLE 3 (variable-coefficient, fundamental set given). For "
                "$x^2 y'' - 2xy' + 2y = x^3$ (a Cauchy-Euler equation) the homogeneous "
                "solutions are $y_1 = x, y_2 = x^2$. NORMALIZE first: divide by $x^2$ "
                "to get $y'' - \\tfrac{2}{x}y' + \\tfrac{2}{x^2}y = x$, so $g = x$ (not "
                "$x^3$!). $W = x\\cdot 2x - x^2\\cdot 1 = x^2$. "
                "$u_1' = -\\dfrac{x^2\\cdot x}{x^2} = -x \\Rightarrow "
                "u_1 = -\\tfrac{1}{2}x^2$; "
                "$u_2' = \\dfrac{x\\cdot x}{x^2} = 1 \\Rightarrow u_2 = x$. Then "
                "$y_p = -\\tfrac{1}{2}x^2\\cdot x + x\\cdot x^2 = \\tfrac{1}{2}x^3$. "
                "The normalization ($g = x$, not $x^3$) was essential - the boxed "
                "warning of page 4 in action."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: non-elementary integrals and intervals", (
                "Variation of parameters always PRODUCES a formula, but two edges "
                "shape what that formula looks like.\n\n"
                "NON-ELEMENTARY INTEGRALS. Because the method ends in integrals, the "
                "answer is only as elementary as $\\int (y_i g / W)\\,dx$. For "
                "$y'' + y = \\dfrac{1}{x}$ (with $x>0$), you get "
                "$u_1' = -\\sin x / x$ and $u_2' = \\cos x / x$, whose integrals are "
                "the sine and cosine integrals $\\mathrm{Si}(x), \\mathrm{Ci}(x)$ - "
                "perfectly good functions, but not expressible in elementary terms. "
                "The correct, complete answer LEAVES them as integrals: "
                "$y_p = -\\cos x\\!\\int\\!\\frac{\\sin x}{x}dx + "
                "\\sin x\\!\\int\\!\\frac{\\cos x}{x}dx$. This is not a failure of the "
                "method - it is the honest statement that the solution is a "
                "non-elementary function. Undetermined coefficients cannot even "
                "start here.\n\n"
                "INTERVALS OF VALIDITY. The formula requires $g$ (and $p, q$) "
                "continuous on the working interval, and $W \\ne 0$ there. For "
                "$\\tan x$ the solution is valid on an interval avoiding "
                "$x = \\pm\\pi/2$ (where $\\tan x$ blows up); for $\\ln x$ or $1/x$, "
                "on $x > 0$. Always state or respect the interval - the logarithms and "
                "reciprocals in your answer carry domain restrictions inherited from "
                "$g$.\n\n"
                "ABSORBING HOMOGENEOUS PIECES. It is common for $u_1 y_1 + u_2 y_2$ to "
                "contain a term that is itself a multiple of $y_1$ or $y_2$ (as in "
                "Example 7.1's $-xe^x$). Since the general solution already includes "
                "arbitrary multiples of $y_1, y_2$, you may DROP such pieces from "
                "$y_p$ without changing the general solution - the tidiest $y_p$ is "
                "the one with all homogeneous parts removed. This is a feature: it "
                "explains why variation of parameters and undetermined coefficients "
                "can give $y_p$'s that look different yet describe the same family."
            )),
            # ---------------------------------------------------------- 11
            (E, "Edge-case worked example: the normalization trap", (
                "Solve $3y'' + 3y = 6\\tan x$ - deliberately un-normalized, to drill "
                "the step everyone forgets.\n\n"
                "WRONG (no normalization). If you carelessly use $g = 6\\tan x$ with "
                "$y_1 = \\cos x, y_2 = \\sin x, W = 1$, you get $u_1' = -6\\sin x\\tan "
                "x$ and $u_2' = 6\\cos x\\tan x$ - every coefficient six times too "
                "big, and the final $y_p$ is wrong by a factor of the leading "
                "coefficient. The equation's $y''$ has coefficient $3$, and the "
                "formula's $g$ is defined for a UNIT leading coefficient.\n\n"
                "RIGHT. First divide the whole equation by $3$: "
                "$y'' + y = 2\\tan x$, so $g = 2\\tan x$. Now "
                "$y_1 = \\cos x, y_2 = \\sin x, W = 1$, and\n"
                "$$u_1' = -\\sin x\\cdot 2\\tan x = -2\\frac{\\sin^2 x}{\\cos x} "
                "= 2\\cos x - 2\\sec x,$$\n"
                "$$u_2' = \\cos x\\cdot 2\\tan x = 2\\sin x.$$\n"
                "Integrate: $u_1 = 2\\sin x - 2\\ln|\\sec x + \\tan x|$, "
                "$u_2 = -2\\cos x$. Assemble and cancel the $\\sin x\\cos x$ terms: "
                "$y_p = -2\\cos x\\,\\ln|\\sec x + \\tan x|$ - exactly twice the "
                "$\\tan x$ answer of Example 5.1, as it should be, since the effective "
                "forcing is $2\\tan x$.\n\n"
                "TAKEAWAY. Before touching $W$ or the quotients, write the equation "
                "with a $1$ in front of $y''$. If the leading coefficient is a "
                "FUNCTION (variable-coefficient case, Example 9.3), divide by that "
                "function - $g$ becomes $g_{\\text{original}}/a(x)$. This one habit "
                "prevents the most common wrong answer in the entire node."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'Use the forcing as given.' The $g$ in the formula "
                "is the right-hand side AFTER normalizing the leading coefficient to "
                "$1$. For $2y'' + \\ldots = 6\\tan x$, $g = 3\\tan x$. Skipping this "
                "scales the whole answer wrongly (page 11).\n\n"
                "MISCONCEPTION 2: 'The minus sign is optional / on the second "
                "formula.' The minus is on $u_1'$ specifically: "
                "$u_1' = -y_2 g / W$, $u_2' = +y_1 g / W$. Swapping or dropping it "
                "flips the sign of $y_p$.\n\n"
                "MISCONCEPTION 3: 'Variation of parameters is only for weird "
                "forcings.' It works for EVERY continuous forcing, including "
                "polynomials and exponentials; it is simply slower there, so you "
                "prefer undetermined coefficients when both apply. But when in doubt, "
                "variation of parameters never lets you down.\n\n"
                "MISCONCEPTION 4: 'I must keep the integration constants in "
                "$u_1, u_2$.' You may drop them: they multiply $y_1, y_2$ and merge "
                "into $y_h$. Keeping them is not wrong, just redundant - and confusing "
                "when you then apply initial conditions.\n\n"
                "MISCONCEPTION 5: 'If the integral is not elementary, the method "
                "failed.' No - leaving $y_p$ as an integral (e.g. via "
                "$\\mathrm{Si}, \\mathrm{Ci}$) is a complete, correct answer. Not "
                "every solution is elementary; variation of parameters faithfully "
                "reports that.\n\n"
                "MISCONCEPTION 6: 'I can use variation of parameters without a "
                "fundamental set.' You need TWO independent homogeneous solutions "
                "first; the method reuses them. For constant coefficients that is "
                "ODE.U2.N5; for variable coefficients you must obtain them some other "
                "way (given, or via reduction of order)."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational and procedural errors", (
                "ERROR 1: Not normalizing. The leading coefficient must be $1$ before "
                "you read off $g$. If it is a constant, divide; if it is a function "
                "$a(x)$ (variable-coefficient case), $g = g_{\\text{orig}}/a(x)$. This "
                "is the number-one wrong answer.\n\n"
                "ERROR 2: Dropping the minus on $u_1'$. Write the pattern every time: "
                "$u_1' = -y_2 g / W$ (minus, OTHER solution), "
                "$u_2' = +y_1 g / W$.\n\n"
                "ERROR 3: Wronskian order/sign. $W = y_1 y_2' - y_2 y_1'$ - first "
                "solution times derivative of second, MINUS second times derivative of "
                "first. Reversing gives $-W$ and flips both quotients. Compute it as "
                "the determinant to stay consistent.\n\n"
                "ERROR 4: Botching the trig integrals. "
                "$\\int\\sec x\\,dx = \\ln|\\sec x + \\tan x|$ and "
                "$\\int\\tan x\\,dx = -\\ln|\\cos x|$ appear constantly; a wrong "
                "antiderivative here poisons the answer even when the setup is "
                "perfect. Know them cold.\n\n"
                "ERROR 5: Forgetting to simplify $\\sin^2 x/\\cos x$ before "
                "integrating. Rewrite $\\sin^2 x = 1 - \\cos^2 x$ to split into "
                "$\\sec x - \\cos x$; integrating $\\sin^2 x/\\cos x$ blindly is much "
                "harder. Algebraic simplification of the quotient BEFORE integrating "
                "is usually the key step.\n\n"
                "ERROR 6: Leaving homogeneous debris in $y_p$, then double-counting it "
                "when adding $y_h$. Cancel or absorb any $c\\,y_1$ or $c\\,y_2$ pieces "
                "in $y_p$; otherwise your 'general solution' has redundant terms and "
                "your constants come out wrong when you apply data (N4)."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: forcings the real world actually delivers", (
                "Variation of parameters earns its keep whenever the drive is not a "
                "textbook sinusoid.\n\n"
                "1. NON-SINUSOIDAL AND SINGULAR FORCINGS. Real inputs are often "
                "quotients or transcendentals: a friction law proportional to "
                "$1/v$, an illumination profile $\\propto 1/x$, a signal shaped like "
                "$\\ln t$ or $\\tan(\\omega t)$. None fits the undetermined-"
                "coefficients family, yet each is a routine variation-of-parameters "
                "integral. The method is the general-purpose engine behind "
                "'plug in whatever forcing the physics hands you.'\n\n"
                "2. THE GREEN'S FUNCTION AND IMPULSE RESPONSE. Rewriting the "
                "integral-form $y_p$ with variable limits gives\n"
                "$$y_p(x) = \\int_{x_0}^{x} "
                "\\frac{y_1(t)y_2(x) - y_1(x)y_2(t)}{W(t)}\\,g(t)\\,dt,$$\n"
                "which expresses the response as a superposition of the system's "
                "reactions to tiny impulses of forcing at each time $t$. The kernel "
                "$G(x,t) = \\dfrac{y_1(t)y_2(x) - y_1(x)y_2(t)}{W(t)}$ is the GREEN'S "
                "FUNCTION - the impulse response of the system. This is the "
                "conceptual bridge to Laplace transforms and to convolution "
                "(ODE.U4.N3): 'output = impulse response convolved with input.'\n\n"
                "3. VARIABLE-COEFFICIENT MODELS. Many physical equations - Bessel's, "
                "Legendre's, Cauchy-Euler - have variable coefficients. Once you have "
                "a fundamental set (from series or special-function theory), variation "
                "of parameters bolts on ANY forcing, which undetermined coefficients "
                "cannot do at all for variable coefficients.\n\n"
                "4. EXISTENCE THEORY. Beyond computation, the formula PROVES that "
                "every continuously-forced linear equation has a solution - a "
                "cornerstone of the general theory. When a textbook asserts 'the "
                "nonhomogeneous problem is solvable,' this integral is usually the "
                "reason."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: a damped system with reciprocal forcing", (
                "A system obeys $y'' + y = \\sec x$ on the interval "
                "$(-\\pi/2, \\pi/2)$, with $y(0) = 0$, $y'(0) = 0$ - modeling an "
                "oscillator driven by an input that grows toward the interval's edges "
                "(where $\\sec x \\to \\infty$). Solve the full IVP.\n\n"
                "STEP 1 - homogeneous: $y_h = c_1\\cos x + c_2\\sin x$.\n\n"
                "STEP 2 - particular by variation of parameters (Example 5.2): "
                "$y_p = \\cos x\\,\\ln|\\cos x| + x\\sin x$. On "
                "$(-\\pi/2, \\pi/2)$, $\\cos x > 0$, so $\\ln|\\cos x| = \\ln\\cos x$.\n\n"
                "STEP 3 - general: "
                "$y = c_1\\cos x + c_2\\sin x + \\cos x\\ln\\cos x + x\\sin x$.\n\n"
                "STEP 4 - data last. At $x = 0$: $\\cos 0 = 1, \\sin 0 = 0, "
                "\\ln\\cos 0 = \\ln 1 = 0$, so $y(0) = c_1 + 0 + 1\\cdot 0 + 0 = c_1 "
                "= 0$. Differentiate: "
                "$y' = -c_1\\sin x + c_2\\cos x + \\big(-\\sin x\\ln\\cos x + "
                "\\cos x\\cdot\\tfrac{-\\sin x}{\\cos x}\\big) + (\\sin x + x\\cos x)$. "
                "The middle bracket is $-\\sin x\\ln\\cos x - \\sin x$. At $x = 0$ "
                "every $\\sin x$ term vanishes and $\\ln\\cos 0 = 0$, leaving "
                "$y'(0) = c_2 + 0 + (0 + 0) = c_2 = 0$. So both constants are zero and\n"
                "$$y = \\cos x\\,\\ln\\cos x + x\\sin x.$$\n"
                "INTERPRETATION. With no initial displacement or velocity, the entire "
                "motion is the forced response to $\\sec x$. As $x \\to \\pm\\pi/2$, "
                "$\\ln\\cos x \\to -\\infty$ and the drive diverges, so the oscillator "
                "is pushed ever harder near the edges - behavior no polynomial-or-"
                "sinusoid guess could ever have captured, delivered cleanly by "
                "variation of parameters."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: where this node plugs in", (
                "BACKWARD. Variation of parameters consumes the output of ODE.U2.N5 "
                "(the fundamental set $y_1, y_2$ and the Wronskian) and of the "
                "calculus integration track (C202): the method's final step is always "
                "two integrals, and its difficulty is exactly the difficulty of those "
                "integrals.\n\n"
                "SIDEWAYS. It is the universal partner to ODE.U3.N1-N2 (undetermined "
                "coefficients): the two methods solve the SAME nonhomogeneous "
                "problems and agree on $y_p$ up to homogeneous pieces. Use "
                "undetermined coefficients when the forcing is in the family "
                "(faster); use variation of parameters otherwise (universal). On "
                "resonant problems, variation of parameters reproduces the $x^s$ "
                "growth automatically, serving as a check on N2.\n\n"
                "FORWARD. ODE.U3.N4 assembles $y = y_h + y_p$ and applies data - the "
                "four-step workflow you executed in Example 15. The integral form of "
                "$y_p$ introduces the GREEN'S FUNCTION / impulse response, which is the "
                "conceptual seed of the Laplace transform unit: ODE.U4.N1-N3 "
                "re-derives forced responses as convolutions of the impulse response "
                "with the input, turning the integral of this node into an algebraic "
                "product in the $s$-domain.\n\n"
                "FURTHER OUT. The same variation-of-parameters idea generalizes to "
                "systems (matrix form, with the fundamental matrix replacing "
                "$y_1, y_2$ and $\\int \\Phi^{-1} g$ replacing the scalar integrals), "
                "to higher-order equations (an $n\\times n$ Wronskian system), and to "
                "partial differential equations via Green's functions. The reflex you "
                "build here - reuse the homogeneous structure, let constants vary, "
                "integrate against the forcing - is one of the most transferable ideas "
                "in applied mathematics."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: the Green's function viewpoint", (
                "The single most illuminating way to read variation of parameters is "
                "as building the system's IMPULSE RESPONSE and superposing.\n\n"
                "Take the integral form with running upper limit and combine the two "
                "integrals under one sign:\n"
                "$$y_p(x) = \\int_{x_0}^{x} G(x,t)\\,g(t)\\,dt, \\qquad "
                "G(x,t) = \\frac{y_1(t)y_2(x) - y_2(t)y_1(x)}{W(t)}.$$\n"
                "Read $g(t)\\,dt$ as a tiny 'kick' of forcing delivered at time $t$. "
                "The kernel $G(x,t)$ is precisely the system's response, observed at "
                "time $x$, to a unit impulse applied at time $t$ - the GREEN'S "
                "FUNCTION. The total response $y_p(x)$ is then the sum (integral) of "
                "the responses to all the little kicks that make up $g$. This is the "
                "superposition principle taken to the continuum: linear systems "
                "respond to a complicated input by adding up their responses to its "
                "infinitesimal pieces.\n\n"
                "Several deep facts fall out. $G(x,t)$ built from the homogeneous "
                "solutions automatically satisfies the boundary/causality structure of "
                "the problem; it depends only on the SYSTEM ($y_1, y_2, W$), not on "
                "the particular forcing, so once you know $G$ you can solve for ANY "
                "$g$ by a single integral. For constant-coefficient equations $G(x,t)$ "
                "depends only on $x - t$, and the integral becomes a CONVOLUTION "
                "$y_p = G * g$ - which is exactly what the Laplace transform "
                "diagonalizes into a product $Y(s) = H(s)G(s)$ (ODE.U4). The impulse "
                "response $G$, the transfer function $H(s)$, and the frequency "
                "response are three views of one object, and variation of parameters "
                "is where that object first appears. What looks like a computational "
                "recipe is, underneath, the definition of how linear systems process "
                "inputs - the foundation of signals, control, and much of "
                "mathematical physics."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "THE IDEA. Let the constants of $y_h = c_1 y_1 + c_2 y_2$ become "
                "functions: $y_p = u_1 y_1 + u_2 y_2$, with the extra condition "
                "$u_1' y_1 + u_2' y_2 = 0$.\n\n"
                "NORMALIZE FIRST. Write $y'' + p y' + q y = g$ with leading "
                "coefficient $1$; the formula's $g$ is this right-hand side (divide "
                "through by the coefficient of $y''$).\n\n"
                "THE FORMULA. With $W = y_1 y_2' - y_2 y_1'$:\n"
                "$$u_1' = -\\frac{y_2 g}{W}, \\quad u_2' = \\frac{y_1 g}{W}, \\quad "
                "y_p = u_1 y_1 + u_2 y_2.$$\n"
                "Mind the MINUS on $u_1'$; drop integration constants; absorb any "
                "homogeneous debris into $y_h$.\n\n"
                "WHEN TO USE IT. Constant coefficients + family forcing "
                "(polynomial/exponential/trig) -> undetermined coefficients (faster). "
                "Anything else - $\\tan x$, $\\sec x$, $1/x$, $\\ln x$, variable "
                "coefficients, or a need for a general/existence result -> variation "
                "of parameters (universal).\n\n"
                "WRONSKIAN QUICK VALUES. $e^{r_1 x}, e^{r_2 x}: (r_2 - r_1)"
                "e^{(r_1+r_2)x}$; $e^{rx}, xe^{rx}: e^{2rx}$; "
                "$\\cos x, \\sin x: 1$.\n\n"
                "EDGE CASES. Non-elementary integrals -> leave $y_p$ in integral form "
                "(valid answer). Respect the interval where $g, p, q$ are continuous "
                "and $W \\ne 0$.\n\n"
                "GREEN'S FUNCTION. "
                "$y_p = \\int G(x,t)g(t)\\,dt$ with "
                "$G = (y_1(t)y_2(x) - y_2(t)y_1(x))/W(t)$ - the impulse response; the "
                "conceptual link to Laplace and convolution."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all six before reading solutions.\n\n"
                "1. Compute $W$ for $y_1 = e^{2x}, y_2 = e^{-x}$.\n"
                "2. Solve $y'' + y = \\cot x$ for a particular solution.\n"
                "3. For $4y'' + 4y = 8\\tan x$, what is the $g$ you put in the "
                "formula?\n"
                "4. Which method for $y'' - y = e^x$? Which for "
                "$y'' - y = \\dfrac{1}{1 + e^x}$?\n"
                "5. True or false: variation of parameters requires constant "
                "coefficients.\n"
                "6. Set up (do not integrate) $u_1', u_2'$ for "
                "$y'' - 3y' + 2y = \\dfrac{e^x}{1 + x}$, given "
                "$y_1 = e^x, y_2 = e^{2x}$.\n\n"
                "SOLUTIONS.\n"
                "1. $W = e^{2x}(-e^{-x}) - e^{-x}(2e^{2x}) = -e^{x} - 2e^{x} "
                "= -3e^{x}$.\n"
                "2. $y_1 = \\cos x, y_2 = \\sin x, W = 1$, $g = \\cot x = \\cos "
                "x/\\sin x$. $u_1' = -\\sin x\\cot x = -\\cos x \\Rightarrow "
                "u_1 = -\\sin x$; $u_2' = \\cos x\\cot x = \\cos^2 x/\\sin x = "
                "(1-\\sin^2 x)/\\sin x = \\csc x - \\sin x \\Rightarrow u_2 = "
                "\\ln|\\csc x - \\cot x| + \\cos x$. Then "
                "$y_p = -\\sin x\\cos x + \\sin x(\\ln|\\csc x - \\cot x| + \\cos x) "
                "= \\sin x\\,\\ln|\\csc x - \\cot x|$.\n"
                "3. Divide by $4$: $g = 2\\tan x$ (NOT $8\\tan x$).\n"
                "4. First: undetermined coefficients (family), but $k=1$ is a root of "
                "$r^2-1$, so resonance - trial $Axe^x$. Second: variation of "
                "parameters ($1/(1+e^x)$ is outside the family).\n"
                "5. FALSE - it works for variable coefficients too, given a "
                "fundamental set.\n"
                "6. $W = e^{3x}$; $u_1' = -\\dfrac{e^{2x}\\cdot e^x/(1+x)}{e^{3x}} "
                "= -\\dfrac{1}{1+x}$; $u_2' = \\dfrac{e^x\\cdot e^x/(1+x)}{e^{3x}} "
                "= \\dfrac{e^{-x}}{1+x}$.\n\n"
                "Scoring: 6/6 - proceed to N4. 4-5 - reread the formula and "
                "normalization (pages 4, 11). $\\le 3$ - rework pages 3-9."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Check every box honestly:\n\n"
                "- State the variation-of-parameters formula "
                "($u_1' = -y_2 g/W$, $u_2' = y_1 g/W$) with the minus sign in the "
                "right place.\n"
                "- Normalize the equation to a unit leading coefficient BEFORE reading "
                "off $g$, including the variable-coefficient case.\n"
                "- Compute a $2\\times 2$ Wronskian correctly as "
                "$y_1 y_2' - y_2 y_1'$.\n"
                "- Solve a forcing that undetermined coefficients cannot handle "
                "($\\tan x$, $\\sec x$, $\\csc x$, $1/x$, $e^x/x$).\n"
                "- Simplify the quotient (e.g. $\\sin^2 x/\\cos x$) before integrating, "
                "and recall the sec/tan/csc integrals.\n"
                "- Decide correctly between undetermined coefficients and variation of "
                "parameters, and split a hybrid forcing between them.\n"
                "- Drop integration constants and absorb homogeneous debris from "
                "$y_p$ into $y_h$.\n"
                "- Recognize when the integrals are non-elementary and leave $y_p$ in "
                "integral form as a valid answer.\n"
                "- Apply the method to a variable-coefficient (Cauchy-Euler) equation "
                "given its fundamental set.\n"
                "- Explain the Green's-function/impulse-response reading of the "
                "integral form and its link to Laplace convolution.\n\n"
                "When every box is checked, practice mixed forcings until method "
                "selection is instant, then proceed to ODE.U3.N4, where $y_h$ and "
                "$y_p$ finally combine into the full solution with initial data."
            )),
        ],
    },
    "ODE.U3.N4": {
        "summary": (
            "A complete course on assembling the full nonhomogeneous solution: the "
            "four-step workflow (homogeneous -> particular -> add -> apply data LAST), "
            "why initial conditions go on y_h + y_p and never on y_h alone, transient "
            "versus steady state in damped driven systems, and verifying a solution by "
            "substitution."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "You now have every piece: $y_h$ from the characteristic equation "
                "(ODE.U2.N5), and $y_p$ from undetermined coefficients (N1-N2) or "
                "variation of parameters (N3). This node assembles them into the "
                "complete answer and - crucially - teaches the ORDER of operations "
                "that most beginners get wrong.\n\n"
                "The full solution of $ay'' + by' + cy = g(x)$ is\n"
                "$$y = y_h + y_p = c_1 y_1 + c_2 y_2 + y_p,$$\n"
                "and the two arbitrary constants $c_1, c_2$ live ENTIRELY in $y_h$. "
                "For an initial value problem you then determine $c_1, c_2$ from the "
                "data $y(x_0), y'(x_0)$. The one rule that governs everything: apply "
                "the data to the FULL solution $y_h + y_p$, never to $y_h$ alone. The "
                "particular solution shifts the whole curve, so the constants that "
                "match the initial point depend on $y_p$ being present. Fitting "
                "constants to $y_h$ first and then adding $y_p$ gives the WRONG answer "
                "- a mistake so common it deserves its own warning label.\n\n"
                "The four-step workflow is: (1) solve the homogeneous equation for "
                "$y_h$; (2) find any particular solution $y_p$; (3) ADD to get the "
                "general solution $y = y_h + y_p$; (4) apply initial/boundary data "
                "LAST, to that sum, and solve for the constants.\n\n"
                "This node also draws out the physical meaning of the split in DAMPED "
                "driven systems: $y_h$ decays to zero (the TRANSIENT - how the system "
                "reacts to its starting conditions and forgets them), while $y_p$ "
                "persists (the STEADY STATE - the long-run response dictated by the "
                "forcing). Understanding which part is which lets you predict "
                "long-term behavior at a glance, and it is the language of every "
                "engineering discussion of circuits, vibrations, and control."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "This is the capstone of the unit, so it draws on all of it.\n\n"
                "1. HOMOGENEOUS SOLUTIONS (ODE.U2.N5). Given $ay'' + by' + cy = 0$, "
                "produce $y_h$ with its two constants from the characteristic roots: "
                "real distinct, repeated, or complex. The complex case "
                "$e^{\\alpha x}(c_1\\cos\\beta x + c_2\\sin\\beta x)$ is central here "
                "because damping ($\\alpha < 0$) is what makes $y_h$ the transient.\n\n"
                "2. PARTICULAR SOLUTIONS (N1-N3). Produce a $y_p$ by the appropriate "
                "method: undetermined coefficients for family forcings (with the "
                "resonance modification), variation of parameters otherwise. You do "
                "not need to re-derive them here - just have one $y_p$ in hand.\n\n"
                "3. SOLVING A $2\\times 2$ LINEAR SYSTEM. Applying two data conditions "
                "gives two equations in $c_1, c_2$; solve by substitution or "
                "elimination. Differentiating the general solution to form the "
                "$y'(x_0)$ equation must be done AFTER adding $y_p$, so the derivative "
                "includes $y_p'$.\n\n"
                "Quick self-check (answers in parentheses):\n\n"
                "- General solution of $y'' - 3y' + 2y = 4x$ (using "
                "$y_p = 2x + 3$ from N1)? "
                "($y = c_1 e^x + c_2 e^{2x} + 2x + 3$.)\n"
                "- In $y = c_1 e^{-t} + c_2 e^{-2t} - 3\\cos t + \\sin t$, which part "
                "is the transient? (The $e^{-t}, e^{-2t}$ terms - they decay; the "
                "trig part is steady state.)\n"
                "- If $y = c_1\\cos t + c_2\\sin t + t$ and $y(0) = 0$, what is "
                "$c_1$? ($y(0) = c_1 + 0 = 0$, so $c_1 = 0$ - note $y_p = t$ "
                "contributes $0$ at $t = 0$ here, but you still evaluate the full "
                "$y$.)\n\n"
                "If any piece feels shaky, revisit its node before assembling."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Why does the general solution split as $y_h + y_p$, and why must data "
                "wait until the end?\n\n"
                "THE SPLIT comes from linearity. Suppose $Y$ is ANY solution of "
                "$Ly = g$ and $y_p$ is the particular one we found. Then "
                "$L(Y - y_p) = Ly - Ly_p = g - g = 0$, so the difference $Y - y_p$ "
                "solves the HOMOGENEOUS equation - it is some $y_h$. Rearranging, "
                "$Y = y_h + y_p$. Every solution is a homogeneous solution plus our "
                "one particular solution. So $y_h + y_p$, with $y_h$ ranging over all "
                "its constants, is the COMPLETE list of solutions - the general "
                "solution. The homogeneous part supplies the freedom (two constants "
                "for a second-order equation); the particular part supplies one "
                "specific response to the forcing.\n\n"
                "WHY DATA GOES LAST follows immediately. The initial conditions must "
                "be satisfied by the ACTUAL solution $y = y_h + y_p$, not by $y_h$. "
                "The forcing shifts the solution curve by $y_p$, so at the initial "
                "point $x_0$ the value is $y_h(x_0) + y_p(x_0)$ and the slope is "
                "$y_h'(x_0) + y_p'(x_0)$. Fitting $c_1, c_2$ to make $y_h(x_0)$ match "
                "the data ignores the $y_p(x_0)$ contribution - you would be solving "
                "the wrong problem. The constants exist precisely to reconcile the "
                "COMBINED curve with the starting conditions.\n\n"
                "A concrete image: $y_p$ is the response the forcing WANTS the system "
                "to have; $y_h$ is the adjustable natural motion that bends the total "
                "curve to also pass through your chosen starting point with your "
                "chosen starting velocity. You cannot know how much natural motion to "
                "add until you know where the forced response already sits at $x_0$. "
                "Hence: build the whole curve first, then fit the constants."
            )),
            # ---------------------------------------------------------- 4
            (L, "The four-step workflow, stated precisely", (
                "For $ay'' + by' + cy = g(x)$ with data $y(x_0) = y_0$, "
                "$y'(x_0) = y_0'$:\n\n"
                "STEP 1 - HOMOGENEOUS. Solve $ar^2 + br + c = 0$; write "
                "$y_h = c_1 y_1 + c_2 y_2$ with two arbitrary constants. Keep the "
                "constants symbolic; do NOT try to pin them yet.\n\n"
                "STEP 2 - PARTICULAR. Find one $y_p$. Choose the method: undetermined "
                "coefficients if $g$ is polynomial/exponential/trig with constant "
                "coefficients (check for resonance and apply the $x^s$ bump if a "
                "collision occurs); variation of parameters otherwise. No arbitrary "
                "constants belong in $y_p$.\n\n"
                "STEP 3 - ADD. Form the general solution "
                "$y = y_h + y_p = c_1 y_1 + c_2 y_2 + y_p$. This is the answer to "
                "'find the general solution.' If the problem asked only for that, "
                "stop here.\n\n"
                "STEP 4 - DATA LAST. Impose the initial (or boundary) conditions on "
                "the FULL $y$ and its derivative. Compute $y(x_0)$ and $y'(x_0)$ from "
                "$y = c_1 y_1 + c_2 y_2 + y_p$ - the derivative must include $y_p'$. "
                "Set them equal to $y_0, y_0'$, obtaining a $2\\times 2$ linear system "
                "in $c_1, c_2$; solve it. Substitute back for the unique solution of "
                "the IVP.\n\n"
                "TWO CHECKS BUILT IN. (a) After Step 3, VERIFY $y_p$ by substituting "
                "it into the equation - a wrong $y_p$ poisons everything downstream. "
                "(b) After Step 4, VERIFY the data: plug $x_0$ into your final $y$ and "
                "$y'$ and confirm you recover $y_0, y_0'$. These two substitutions "
                "catch nearly every arithmetic slip. The workflow is deliberately "
                "linear - each step's output feeds the next - so a mistake caught "
                "early saves the whole computation."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples", (
                "EXAMPLE 1 (the full workflow on a clean problem). Solve "
                "$y'' - 3y' + 2y = 4x$, $y(0) = 0$, $y'(0) = 1$.\n\n"
                "STEP 1: roots $1, 2$, so $y_h = c_1 e^x + c_2 e^{2x}$. STEP 2: from "
                "N1, $y_p = 2x + 3$. STEP 3: "
                "$y = c_1 e^x + c_2 e^{2x} + 2x + 3$. STEP 4 - data on the full "
                "solution:\n"
                "$$y(0) = c_1 + c_2 + 3 = 0 \\;\\Rightarrow\\; c_1 + c_2 = -3.$$\n"
                "Differentiate the FULL $y$: $y' = c_1 e^x + 2c_2 e^{2x} + 2$, so\n"
                "$$y'(0) = c_1 + 2c_2 + 2 = 1 \\;\\Rightarrow\\; c_1 + 2c_2 = -1.$$\n"
                "Subtract: $c_2 = 2$, then $c_1 = -5$. Final: "
                "$y = -5e^x + 2e^{2x} + 2x + 3$. VERIFY the data: "
                "$y(0) = -5 + 2 + 3 = 0$; $y'(0) = -5 + 4 + 2 = 1$. Both hold.\n\n"
                "EXAMPLE 2 (why data-last matters - the same problem done WRONG). If "
                "you fit the constants to $y_h$ alone: '$y_h(0) = c_1 + c_2 = 0$ and "
                "$y_h'(0) = c_1 + 2c_2 = 1$' gives $c_2 = 1, c_1 = -1$, then tack on "
                "$y_p$: $y = -e^x + e^{2x} + 2x + 3$. Check this against the data: "
                "$y(0) = -1 + 1 + 3 = 3 \\ne 0$. It fails the initial condition "
                "because the $+3$ from $y_p(0)$ was never accounted for. The "
                "constants must absorb $y_p(0) = 3$, which only happens when you apply "
                "the data to $y_h + y_p$. Same equation, same $y_h$, same $y_p$ - "
                "wrong ORDER, wrong answer."
            )),
            # ---------------------------------------------------------- 6
            (L, "Properties: transient and steady state", (
                "In a DAMPED driven system the split $y_h + y_p$ acquires a sharp "
                "physical meaning that is worth stating as a property.\n\n"
                "TRANSIENT = $y_h$ (WHEN IT DECAYS). If the homogeneous solutions all "
                "decay to zero as $x \\to \\infty$ - which happens exactly when every "
                "characteristic root has NEGATIVE real part (the signature of "
                "damping) - then $y_h \\to 0$. Its terms $e^{-t}$, $e^{-2t}$, "
                "$e^{-t}\\cos\\omega t$ carry the memory of the initial conditions, "
                "and that memory FADES. This part is the transient: how the system "
                "reacts to how it was started, and then forgets.\n\n"
                "STEADY STATE = $y_p$ (WHAT PERSISTS). The particular solution, driven "
                "by the forcing, does not decay (for a bounded periodic or constant "
                "forcing it is bounded and periodic/constant). As $t \\to \\infty$, "
                "$y \\to y_p$ regardless of the initial conditions - the system settles "
                "onto the response dictated by the input. For a sinusoidal drive this "
                "is the steady-state oscillation at the driving frequency, with a "
                "definite amplitude and phase lag; for a constant drive it is the new "
                "equilibrium.\n\n"
                "TWO CONSEQUENCES. First, LONG-RUN behavior is read off $y_p$ ALONE: "
                "you can predict where a damped system ends up without ever finding "
                "$c_1, c_2$, because they multiply decaying terms. Second, the initial "
                "conditions affect only HOW the system approaches steady state (the "
                "shape and size of the transient), not the steady state itself. This "
                "is why engineers speak of 'transient response' and 'steady-state "
                "response' as separate design targets - fast settling is a transient "
                "property (set by the damping/roots), while the final amplitude is a "
                "steady-state property (set by the forcing and $y_p$). The caveat: "
                "this clean picture requires damping (roots with negative real part). "
                "Undamped systems ($y_h$ purely oscillatory) have no decaying "
                "transient, and resonance (N2) makes $y_p$ itself grow - neither "
                "settles."
            )),
            # ---------------------------------------------------------- 7
            (E, "Standard worked examples", (
                "EXAMPLE 1 (transient plus steady state). Solve "
                "$y'' + 3y' + 2y = 10\\sin t$, $y(0) = 0$, $y'(0) = 0$, and identify "
                "the transient and steady-state parts.\n\n"
                "STEP 1: roots of $r^2 + 3r + 2 = (r+1)(r+2)$ are $-1, -2$ (both "
                "negative - damped), $y_h = c_1 e^{-t} + c_2 e^{-2t}$. STEP 2: "
                "undetermined coefficients, trial $A\\cos t + B\\sin t$. "
                "$y_p' = -A\\sin t + B\\cos t$, $y_p'' = -A\\cos t - B\\sin t$. "
                "Substitute: $(-A\\cos t - B\\sin t) + 3(-A\\sin t + B\\cos t) + "
                "2(A\\cos t + B\\sin t) = (A + 3B)\\cos t + (B - 3A)\\sin t = "
                "10\\sin t$. So $A + 3B = 0$ and $B - 3A = 10$; from the first "
                "$A = -3B$, then $B - 3(-3B) = 10B = 10$, giving $B = 1, A = -3$. "
                "$y_p = -3\\cos t + \\sin t$ (steady state). STEP 3: "
                "$y = c_1 e^{-t} + c_2 e^{-2t} - 3\\cos t + \\sin t$. STEP 4: "
                "$y(0) = c_1 + c_2 - 3 = 0 \\Rightarrow c_1 + c_2 = 3$. "
                "$y' = -c_1 e^{-t} - 2c_2 e^{-2t} + 3\\sin t + \\cos t$; "
                "$y'(0) = -c_1 - 2c_2 + 1 = 0 \\Rightarrow c_1 + 2c_2 = 1$. Subtract: "
                "$c_2 = -2$, $c_1 = 5$. Final: "
                "$y = 5e^{-t} - 2e^{-2t} - 3\\cos t + \\sin t$.\n\n"
                "IDENTIFY: TRANSIENT $= 5e^{-t} - 2e^{-2t} \\to 0$; STEADY STATE "
                "$= -3\\cos t + \\sin t$, amplitude $\\sqrt{9 + 1} = \\sqrt{10}$. "
                "After a few time units the exponentials vanish and the system "
                "oscillates as $\\sqrt{10}\\sin(t - \\phi)$ - the initial conditions "
                "are forgotten."
            )),
            # ---------------------------------------------------------- 8
            (L, "Technique: verification and organizing the computation", (
                "A long assembly has many places to slip; disciplined verification is "
                "the technique that makes your answer trustworthy.\n\n"
                "VERIFY $y_p$ BEFORE ADDING. Substitute $y_p$ (and $y_p', y_p''$) into "
                "$ay'' + by' + cy$ and confirm you get exactly $g(x)$. This is a "
                "30-second check that isolates the particular-solution step from the "
                "constant-fitting step. If it fails, fix $y_p$ before proceeding - "
                "otherwise the data equations are built on sand.\n\n"
                "DIFFERENTIATE THE FULL $y$ FOR STEP 4. The single most common "
                "structural error is forming $y'$ from $y_h$ only. Write "
                "$y = c_1 y_1 + c_2 y_2 + y_p$ and differentiate the WHOLE thing, so "
                "$y' = c_1 y_1' + c_2 y_2' + y_p'$. The $y_p'$ term contributes to the "
                "$y'(x_0)$ equation and changes the constants.\n\n"
                "VERIFY THE DATA AT THE END. Plug $x_0$ into the final $y$ and $y'$; "
                "you must recover $y_0$ and $y_0'$ exactly. This catches sign errors "
                "and arithmetic slips in the linear system. (Example 5.2 shows the "
                "failure this check would flag.)\n\n"
                "ORGANIZE FOR CLARITY. Keep $y_h$, $y_p$, and the data work in three "
                "visually separate blocks. Evaluate known constants ($e^0 = 1$, "
                "$\\cos 0 = 1$, $\\sin 0 = 0$) explicitly when forming the data "
                "equations - most errors hide in a mis-evaluated $y_p(x_0)$ or "
                "$y_p'(x_0)$. When the initial point is $x_0 = 0$ (the usual case), "
                "the exponentials and cosines become $1$ and the sines become $0$, "
                "which simplifies the system; for $x_0 \\ne 0$ carry the exponentials "
                "symbolically. A tidy layout is not cosmetic - it is how you make the "
                "final verification easy enough that you actually do it."
            )),
            # ---------------------------------------------------------- 9
            (E, "Harder worked examples", (
                "EXAMPLE 1 (resonant $y_p$, full IVP). Solve $y'' + 4y = 3\\cos 2t$, "
                "$y(0) = 1$, $y'(0) = 0$.\n\n"
                "STEP 1: $y_h = c_1\\cos 2t + c_2\\sin 2t$. STEP 2: drive at "
                "$\\omega_0 = 2$ is resonant (N2), so "
                "$y_p = \\tfrac{3}{4}t\\sin 2t$. STEP 3: "
                "$y = c_1\\cos 2t + c_2\\sin 2t + \\tfrac{3}{4}t\\sin 2t$. STEP 4: "
                "$y(0) = c_1 = 1$. "
                "$y' = -2c_1\\sin 2t + 2c_2\\cos 2t + \\tfrac{3}{4}\\sin 2t + "
                "\\tfrac{3}{2}t\\cos 2t$; $y'(0) = 2c_2 = 0 \\Rightarrow c_2 = 0$. "
                "Final: $y = \\cos 2t + \\tfrac{3}{4}t\\sin 2t$. No damping here, so "
                "there is NO transient/steady-state split - instead the resonant term "
                "grows without bound. Verify: $y(0) = 1$, and "
                "$y'(0) = 0 + 0 + 0 + 0 = 0$. Good.\n\n"
                "EXAMPLE 2 (nonzero initial point). Solve $y'' - y = 2e^{t}$, "
                "$y(0) = 0$, $y'(0) = 1$. Roots $\\pm 1$; $k = 1$ is a root, so "
                "resonance: $y_p = At e^{t}$. Substitute: "
                "$y_p'' - y_p = Ae^t(t + 2) - Ate^t = 2Ae^t = 2e^t \\Rightarrow "
                "A = 1$, so $y_p = te^t$. General: "
                "$y = c_1 e^t + c_2 e^{-t} + te^t$. Data: "
                "$y(0) = c_1 + c_2 = 0$. "
                "$y' = c_1 e^t - c_2 e^{-t} + e^t + te^t$; "
                "$y'(0) = c_1 - c_2 + 1 = 1 \\Rightarrow c_1 - c_2 = 0$. Together "
                "$c_1 = c_2$ and $c_1 + c_2 = 0$ give $c_1 = c_2 = 0$. Final: "
                "$y = te^t$. Verify: $y(0) = 0$; "
                "$y'(0) = (e^t + te^t)|_0 = 1$. Both hold - a case where the transient "
                "constants vanish and the response is purely the resonant particular "
                "solution."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: boundary-value problems and existence", (
                "Initial value problems (data at ONE point) always have a unique "
                "solution for a well-posed linear equation. BOUNDARY value problems "
                "(data at TWO different points, e.g. $y(0)$ and $y(L)$) behave "
                "differently, and this edge is worth knowing.\n\n"
                "For a boundary value problem you still build "
                "$y = c_1 y_1 + c_2 y_2 + y_p$, but now impose conditions at two "
                "points, giving a $2\\times 2$ system in $c_1, c_2$ whose coefficient "
                "matrix depends on $y_1, y_2$ at the two boundary points. That matrix "
                "can be SINGULAR, and then - unlike an IVP - the problem may have NO "
                "solution or INFINITELY MANY. Classic example: "
                "$y'' + y = 0$ with $y(0) = 0, y(\\pi) = 0$ has the whole family "
                "$y = c_2\\sin t$ as solutions (infinitely many), because $\\sin t$ "
                "vanishes at both ends. Change the right endpoint or add forcing and "
                "you may get none. Boundary value problems are where eigenvalue "
                "problems and resonance-like non-uniqueness appear; ODEs treats them "
                "carefully in later nodes.\n\n"
                "STEADY STATE WITHOUT DAMPING. The transient/steady-state language "
                "REQUIRES roots with negative real part. If the system is undamped "
                "(purely imaginary roots), $y_h$ oscillates forever and never becomes "
                "negligible - there is no steady state in the settling sense, and the "
                "long-run motion is the superposition of $y_h$ and $y_p$, sensitive to "
                "initial conditions for all time. If a root has POSITIVE real part "
                "(an unstable system), $y_h$ GROWS and dominates - the 'transient' "
                "blows up. Always check the sign of the real parts before claiming "
                "$y \\to y_p$.\n\n"
                "A PRACTICAL EDGE: when $g$ itself decays (e.g. $g = e^{-t}$), the "
                "'steady state' may be zero, and both $y_h$ and $y_p$ vanish at "
                "infinity - the system returns to rest. Read the long-run behavior "
                "from whichever surviving term dies slowest."
            )),
            # ---------------------------------------------------------- 11
            (E, "Edge-case worked example: a boundary-value problem", (
                "Solve the boundary value problem $y'' + y = x$, $y(0) = 0$, "
                "$y(\\pi/2) = 0$ - and see how the two-point data differs from an "
                "IVP.\n\n"
                "STEP 1: $y_h = c_1\\cos x + c_2\\sin x$. STEP 2: $g = x$ is a "
                "polynomial, $k = 0$ not a root of $r^2 + 1$; trial $Ax + B$ gives "
                "$y_p'' + y_p = 0 + Ax + B = x$, so $A = 1, B = 0$, "
                "$y_p = x$. STEP 3: $y = c_1\\cos x + c_2\\sin x + x$.\n\n"
                "STEP 4 - impose BOTH boundary conditions on the full $y$. At "
                "$x = 0$: $y(0) = c_1\\cos 0 + c_2\\sin 0 + 0 = c_1 = 0$. At "
                "$x = \\pi/2$: "
                "$y(\\pi/2) = c_1\\cos\\tfrac{\\pi}{2} + c_2\\sin\\tfrac{\\pi}{2} "
                "+ \\tfrac{\\pi}{2} = 0 + c_2 + \\tfrac{\\pi}{2} = 0 "
                "\\Rightarrow c_2 = -\\tfrac{\\pi}{2}$. Final: "
                "$y = -\\tfrac{\\pi}{2}\\sin x + x$. Verify: $y(0) = 0$; "
                "$y(\\pi/2) = -\\tfrac{\\pi}{2}\\cdot 1 + \\tfrac{\\pi}{2} = 0$. Both "
                "boundary values hold.\n\n"
                "CONTRAST WITH THE SINGULAR CASE. Had the interval been $[0, \\pi]$ "
                "with $y(0) = y(\\pi) = 0$, the conditions would read $c_1 = 0$ and "
                "$c_1\\cos\\pi + c_2\\sin\\pi + \\pi = -c_1 + 0 + \\pi = \\pi = 0$ - "
                "IMPOSSIBLE, so NO solution exists. The coefficient of $c_2$ "
                "($\\sin\\pi = 0$) collapsed, leaving an unsatisfiable equation. This "
                "is the boundary-value pathology of page 10: with data at two points, "
                "existence and uniqueness are not guaranteed, unlike the initial value "
                "problems that dominate the rest of this unit. Notice the WORKFLOW was "
                "identical - build $y_h + y_p$, apply data last - only the data's "
                "solvability changed."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'Apply the initial conditions to $y_h$, then add "
                "$y_p$.' The cardinal error of the whole unit. Constants must be fit "
                "to the FULL solution $y_h + y_p$; the forcing shifts the curve, so "
                "the constants depend on $y_p(x_0)$ and $y_p'(x_0)$. Example 5.2 shows "
                "the wrong answer this produces.\n\n"
                "MISCONCEPTION 2: 'The arbitrary constants live in $y_p$.' No - $y_p$ "
                "is ONE specific function with no free constants. All the freedom "
                "(two constants for second order) is in $y_h$. A $y_p$ carrying a $+C$ "
                "is just hiding a homogeneous term.\n\n"
                "MISCONCEPTION 3: 'Steady state is the average, or the biggest term.' "
                "Steady state is specifically $y_p$ (the persisting part) when the "
                "transient $y_h$ decays. It requires damping - roots with negative "
                "real part. Without damping there is no steady state.\n\n"
                "MISCONCEPTION 4: 'Initial conditions change the steady state.' They "
                "do not - $c_1, c_2$ multiply the decaying transient terms, so they "
                "affect only HOW the system approaches steady state, not the "
                "steady state itself (page 6).\n\n"
                "MISCONCEPTION 5: 'Every boundary value problem has a unique solution "
                "like every IVP.' False - two-point boundary conditions can give no "
                "solution or infinitely many when the coefficient matrix is singular "
                "(page 11). Uniqueness is an IVP guarantee, not a BVP one.\n\n"
                "MISCONCEPTION 6: 'I can skip verifying because I followed the "
                "steps.' The assembly has many arithmetic steps; a single sign error "
                "in the data system silently corrupts the constants. The two "
                "substitution checks (verify $y_p$, verify the data) are what "
                "separate a right answer from a plausible-looking wrong one."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational and procedural errors", (
                "ERROR 1: Fitting constants before adding $y_p$. Do STEP 3 (add) "
                "BEFORE STEP 4 (data). Always impose conditions on "
                "$y = y_h + y_p$.\n\n"
                "ERROR 2: Differentiating only $y_h$ for the $y'(x_0)$ equation. The "
                "derivative must include $y_p'$: "
                "$y' = c_1 y_1' + c_2 y_2' + y_p'$. Omitting $y_p'$ is one of the two "
                "most frequent slips.\n\n"
                "ERROR 3: Mis-evaluating $y_p$ or $y_p'$ at $x_0$. At $x_0 = 0$, "
                "remember $y_p(0)$ and $y_p'(0)$ are usually NONZERO (e.g. "
                "$y_p = 2x + 3$ has $y_p(0) = 3$). Compute them explicitly and feed "
                "them into the data equations.\n\n"
                "ERROR 4: Sign errors in the $2\\times 2$ system. Line up the two "
                "equations, eliminate carefully, and back-substitute; then VERIFY the "
                "data. A flipped sign here changes both constants.\n\n"
                "ERROR 5: Claiming $y \\to y_p$ without checking the roots. The "
                "long-run limit is $y_p$ ONLY if every homogeneous root has negative "
                "real part. For undamped or unstable systems this is false - state the "
                "damping condition before invoking steady state.\n\n"
                "ERROR 6: Leaving a homogeneous term inside $y_p$ (common after "
                "variation of parameters) and then treating it as part of the forced "
                "response. Absorb such terms into $y_h$ first; otherwise the constants "
                "double-count and the data system gives wrong values. ERROR 7: "
                "Stopping at the general solution when the problem gave initial data - "
                "or fitting constants when it only asked for the general solution. "
                "Read what the final object should be."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: transient design and steady-state response", (
                "The $y_h + y_p$ split is not just bookkeeping - it is how engineers "
                "think about every driven linear system.\n\n"
                "1. RLC CIRCUITS. A resistor-inductor-capacitor circuit driven by an "
                "AC source obeys a second-order linear ODE. The transient $y_h$ "
                "(decaying, set by $R, L, C$) is the switch-on surge; the steady state "
                "$y_p$ is the sinusoidal current the circuit carries once settled, at "
                "the source frequency with a phase lag. Designers size $R$ to kill the "
                "transient fast (avoid overshoot) while the steady-state amplitude is "
                "fixed by the drive and the impedance. Both are read directly off "
                "$y_h$ and $y_p$.\n\n"
                "2. VIBRATION ISOLATION. A machine on springs and dampers, shaken by a "
                "periodic force, has a transient rattle at start-up ($y_h$) and a "
                "steady vibration ($y_p$) at the forcing frequency. Mounts are "
                "designed so the steady-state amplitude is small (drive far from "
                "resonance, N2) and the transient decays quickly (enough damping). The "
                "settling time is a transient property; the residual vibration is a "
                "steady-state property.\n\n"
                "3. CONTROL AND STEP RESPONSE. Apply a sudden constant input (a "
                "'step') to a damped system: $y_p$ is the new equilibrium the output "
                "moves to, and $y_h$ describes the approach - overshoot, ringing, or "
                "smooth rise, depending on the roots (underdamped vs overdamped). "
                "'Rise time,' 'settling time,' and 'steady-state error' are the "
                "vocabulary of exactly this decomposition.\n\n"
                "4. THE UNIFYING PRINCIPLE. In every application you predict the "
                "long-run behavior from $y_p$ (needs no initial data) and the "
                "start-up behavior from $y_h$ (needs the data, via the constants). "
                "Separating the two - and applying the data LAST to get the right "
                "transient - is the daily practice this node is training."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: switch-on of a damped driven circuit", (
                "A series RLC-type system obeys $y'' + 2y' + 5y = 10\\cos t$ with the "
                "system initially at rest, $y(0) = 0$, $y'(0) = 0$ (switch closed at "
                "$t = 0$). Find the response and separate transient from steady state.\n\n"
                "STEP 1: roots of $r^2 + 2r + 5 = 0$ are $r = -1 \\pm 2i$ (negative "
                "real part - damped). $y_h = e^{-t}(c_1\\cos 2t + c_2\\sin 2t)$ - the "
                "TRANSIENT. STEP 2: trial $A\\cos t + B\\sin t$. "
                "$y_p' = -A\\sin t + B\\cos t$, $y_p'' = -A\\cos t - B\\sin t$. "
                "Substitute: $(-A\\cos t - B\\sin t) + 2(-A\\sin t + B\\cos t) + "
                "5(A\\cos t + B\\sin t) = (4A + 2B)\\cos t + (4B - 2A)\\sin t = "
                "10\\cos t$. So $4A + 2B = 10$ and $4B - 2A = 0 \\Rightarrow A = 2B$. "
                "Then $4(2B) + 2B = 10B = 10 \\Rightarrow B = 1, A = 2$. "
                "$y_p = 2\\cos t + \\sin t$ - the STEADY STATE (amplitude "
                "$\\sqrt{5}$).\n\n"
                "STEP 3: $y = e^{-t}(c_1\\cos 2t + c_2\\sin 2t) + 2\\cos t + \\sin t$. "
                "STEP 4: $y(0) = c_1 + 2 = 0 \\Rightarrow c_1 = -2$. Differentiate "
                "(product rule on the transient): "
                "$y' = e^{-t}\\big[(-c_1 + 2c_2)\\cos 2t + (-c_2 - 2c_1)\\sin 2t\\big] "
                "- 2\\sin t + \\cos t$. At $t = 0$: "
                "$y'(0) = (-c_1 + 2c_2) + 1 = 0$. With $c_1 = -2$: "
                "$2 + 2c_2 + 1 = 0 \\Rightarrow c_2 = -\\tfrac{3}{2}$. Final:\n"
                "$$y = e^{-t}\\big(-2\\cos 2t - \\tfrac{3}{2}\\sin 2t\\big) "
                "+ 2\\cos t + \\sin t.$$\n"
                "INTERPRETATION. The $e^{-t}(\\ldots)$ transient decays within a few "
                "seconds; thereafter the system oscillates as the steady state "
                "$2\\cos t + \\sin t$ at the drive frequency, independent of the "
                "at-rest start. Verify: $y(0) = -2 + 2 = 0$; the $y'(0)$ computation "
                "returns $0$. This is the complete four-step workflow on a realistic "
                "damped, driven problem."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: where this node plugs in", (
                "BACKWARD. This node consumes ODE.U2.N5 (the homogeneous $y_h$ and its "
                "constants) and ODE.U3.N1-N3 (the particular $y_p$, by whichever "
                "method fits). It is the place where all three earlier skills combine "
                "into a single answer, so weakness in any of them surfaces here first.\n\n"
                "SIDEWAYS. The transient/steady-state reading depends on the "
                "complex-root, damped case of ODE.U2.N5 (roots with negative real "
                "part). The resonance edge (Example 9.1) ties back to ODE.U3.N2: "
                "undamped resonance has no steady state because $y_p$ itself grows.\n\n"
                "FORWARD. The Laplace transform (ODE.U4.N1-N3) is an ALTERNATIVE route "
                "to this same full solution: transforming an IVP folds the initial "
                "data INTO the algebra from the start (via $\\mathcal{L}\\{y'\\} = "
                "sY - y(0)$), so Laplace produces $y_h + y_p$ WITH the constants "
                "already determined - no separate data-last step. Comparing the two "
                "methods on the same IVP is one of the best ways to understand what "
                "each is doing: undetermined coefficients / variation of parameters "
                "plus data-last here, versus algebraic bookkeeping of the data in the "
                "$s$-domain there.\n\n"
                "FURTHER OUT. Systems of ODEs generalize $y_h + y_p$ to vector form "
                "(homogeneous solution from the matrix exponential $e^{At}$, "
                "particular solution from a matrix variation-of-parameters integral), "
                "and the transient/steady-state split becomes stability analysis via "
                "the eigenvalues of $A$. Control theory's step response, frequency "
                "response, and stability margins are all the transient/steady-state "
                "decomposition, scaled up. The habit of separating 'how it starts' "
                "from 'where it ends up' is one of the most durable ideas you take "
                "from this unit."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: superposition and the structure of solution sets", (
                "The $y_h + y_p$ decomposition is a special case of a structural fact "
                "that runs through all of linear mathematics, and seeing it that way "
                "makes the whole subject cohere.\n\n"
                "A linear differential operator $L$ acts on functions the way a matrix "
                "$A$ acts on vectors. Solving $Ly = g$ is the differential analogue of "
                "solving $Ax = b$. In linear algebra, the solution set of $Ax = b$ is "
                "'one particular solution plus the null space of $A$' - "
                "$x = x_p + x_{\\text{null}}$. Here $y_p$ is the particular solution "
                "and $y_h$ (the homogeneous solutions) is the NULL SPACE of $L$: "
                "exactly the functions $L$ sends to zero. The two constants $c_1, c_2$ "
                "are the coordinates in that two-dimensional null space. So "
                "'general solution = particular + homogeneous' is the same theorem as "
                "'solution set of $Ax = b$ = particular + null space,' transplanted "
                "from vectors to functions.\n\n"
                "This viewpoint explains several things at once. WHY two constants? "
                "Because the null space of a second-order $L$ is two-dimensional "
                "(existence-uniqueness gives exactly two independent homogeneous "
                "solutions). WHY data determines them uniquely? Because specifying "
                "$y(x_0), y'(x_0)$ is choosing a unique point in the affine solution "
                "set - the differential version of pinning down the free parameters "
                "in $x = x_p + x_{\\text{null}}$. WHY boundary value problems can fail? "
                "Because two-point conditions may not correspond to an invertible map "
                "on the null space (the singular-matrix case of page 11) - precisely "
                "when $Ax = b$ has no or many solutions.\n\n"
                "Superposition is the engine underneath: $L$ linear means responses "
                "ADD, so complicated forcings decompose into simple pieces solved "
                "separately (N1's superposition, N3's Green's function integral). The "
                "same principle powers Fourier methods, Laplace transforms, and the "
                "entire theory of linear systems. What you assemble by hand in this "
                "node is one instance of the most reused structure in applied "
                "mathematics."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "THE FULL SOLUTION. "
                "$y = y_h + y_p = c_1 y_1 + c_2 y_2 + y_p$; both arbitrary constants "
                "live in $y_h$, none in $y_p$.\n\n"
                "FOUR-STEP WORKFLOW. (1) Homogeneous $y_h$ (ODE.U2.N5). (2) Particular "
                "$y_p$ (N1-N2 undetermined coefficients, or N3 variation of "
                "parameters). (3) ADD: $y = y_h + y_p$. (4) DATA LAST: impose "
                "$y(x_0), y'(x_0)$ on the FULL $y$ (derivative includes $y_p'$), solve "
                "the $2\\times 2$ system for $c_1, c_2$.\n\n"
                "GOLDEN RULE. Never fit constants to $y_h$ alone - the forcing shifts "
                "the curve by $y_p$, so the data must see $y_h + y_p$.\n\n"
                "TRANSIENT vs STEADY STATE (damped systems, roots with negative real "
                "part). $y_h \\to 0$ = TRANSIENT (carries and forgets the initial "
                "data); $y_p$ persists = STEADY STATE (long-run response to the "
                "forcing). Then $y \\to y_p$; initial conditions affect only the "
                "approach, not the steady state. Requires damping - undamped or "
                "unstable systems have no settling.\n\n"
                "VERIFY TWICE. (a) Substitute $y_p$ into the equation to confirm it "
                "gives $g$. (b) Plug $x_0$ into the final $y, y'$ to confirm the data.\n\n"
                "EDGE - BOUNDARY VALUE PROBLEMS. Two-point data can give a singular "
                "system: no solution or infinitely many (unlike IVPs). Same workflow, "
                "different solvability.\n\n"
                "STRUCTURE. General solution = particular + null space of $L$ - the "
                "differential twin of $x = x_p + x_{\\text{null}}$ for $Ax = b$."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all six before reading solutions.\n\n"
                "1. Solve $y'' - y' - 2y = 4$, $y(0) = 0$, $y'(0) = 0$.\n"
                "2. In $y = c_1 e^{-3t} + c_2 e^{-t} + \\sin t - \\cos t$, name the "
                "transient and steady state.\n"
                "3. True or false: initial conditions determine the steady state.\n"
                "4. For $y'' + y = t$ with $y_p = t$, write the general solution and "
                "the $y(0)$ equation if $y(0) = 2$.\n"
                "5. Solve $y'' + 4y = 8$, $y(0) = 3$, $y'(0) = 0$.\n"
                "6. Why does the workflow apply data LAST? (One sentence.)\n\n"
                "SOLUTIONS.\n"
                "1. Roots $2, -1$: $y_h = c_1 e^{2t} + c_2 e^{-t}$. Constant forcing, "
                "$k=0$ not a root: $y_p = A$ with $-2A = 4 \\Rightarrow y_p = -2$. "
                "$y = c_1 e^{2t} + c_2 e^{-t} - 2$. Data: $c_1 + c_2 - 2 = 0$; "
                "$y' = 2c_1 e^{2t} - c_2 e^{-t}$, $2c_1 - c_2 = 0$. So "
                "$c_2 = 2c_1$, $3c_1 = 2 \\Rightarrow c_1 = \\tfrac{2}{3}, "
                "c_2 = \\tfrac{4}{3}$. $y = \\tfrac{2}{3}e^{2t} + \\tfrac{4}{3}e^{-t} "
                "- 2$.\n"
                "2. Transient: $c_1 e^{-3t} + c_2 e^{-t}$ (decay). Steady state: "
                "$\\sin t - \\cos t$.\n"
                "3. FALSE - they set the transient (via $c_1, c_2$), not the steady "
                "state.\n"
                "4. $y = c_1\\cos t + c_2\\sin t + t$; $y(0) = c_1 + 0 = 2 "
                "\\Rightarrow c_1 = 2$.\n"
                "5. $y_h = c_1\\cos 2t + c_2\\sin 2t$; $y_p = 2$ (since $4A = 8$). "
                "$y = c_1\\cos 2t + c_2\\sin 2t + 2$. $y(0) = c_1 + 2 = 3 "
                "\\Rightarrow c_1 = 1$; $y'(0) = 2c_2 = 0 \\Rightarrow c_2 = 0$. "
                "$y = \\cos 2t + 2$.\n"
                "6. Because the forcing shifts the solution by $y_p$, so the constants "
                "must be fit to the full $y_h + y_p$ to satisfy the data.\n\n"
                "Scoring: 6/6 - unit complete, on to Laplace (U4). 4-5 - reread the "
                "workflow and the wrong-order example (pages 4-5). $\\le 3$ - rework "
                "pages 3-9."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Check every box honestly:\n\n"
                "- State the general solution $y = y_h + y_p$ and explain that both "
                "constants live in $y_h$.\n"
                "- Execute the four-step workflow in order, keeping constants symbolic "
                "until Step 4.\n"
                "- Apply initial data to the FULL solution and explain why fitting "
                "$y_h$ alone is wrong.\n"
                "- Differentiate the full $y$ (including $y_p'$) when forming the "
                "$y'(x_0)$ equation.\n"
                "- Solve the resulting $2\\times 2$ system and VERIFY the data by "
                "back-substitution.\n"
                "- Verify a particular solution by substitution before building the "
                "general solution.\n"
                "- Identify transient ($y_h$, decaying) and steady state ($y_p$, "
                "persisting) in a damped system, and state the negative-real-part "
                "condition.\n"
                "- Explain that initial conditions shape the approach to steady state, "
                "not the steady state itself.\n"
                "- Recognize that boundary value problems can have no or many "
                "solutions, unlike IVPs.\n"
                "- Connect general = particular + homogeneous to "
                "$x = x_p + x_{\\text{null}}$ for $Ax = b$, and name Laplace (U4) as "
                "the alternative that bakes the data into the algebra.\n\n"
                "When every box is checked, you can solve any constant-coefficient "
                "nonhomogeneous IVP end to end. Proceed to ODE.U4.N1, where the "
                "Laplace transform turns these same problems into algebra."
            )),
        ],
    },
    "ODE.U4.N1": {
        "summary": (
            "A complete course on the Laplace transform of elementary functions: the "
            "definition L{f} = int_0^inf e^{-st} f dt, linearity, the core table "
            "(1, t^n, e^{at}, sin, cos), regions of convergence and exponential order, "
            "and the derivative property L{f'} = sF(s) - f(0) that turns "
            "differentiation into multiplication by s."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "You can now solve nonhomogeneous linear ODEs by hand (U3). The "
                "Laplace transform is a completely different strategy that will solve "
                "the SAME problems - and more - by turning calculus into algebra. This "
                "node builds the foundation: what the transform IS, the table of "
                "elementary transforms, and the one property that makes the whole "
                "enterprise pay off.\n\n"
                "The Laplace transform of a function $f(t)$ (defined for $t \\ge 0$) "
                "is\n"
                "$$\\mathcal{L}\\{f\\}(s) = F(s) = \\int_0^{\\infty} e^{-st}f(t)\\,dt.$$\n"
                "It converts a function of TIME $t$ into a function of a new variable "
                "$s$ (frequency-like). The magic, developed across this unit, is that "
                "DIFFERENTIATION in $t$ becomes MULTIPLICATION by $s$ in the "
                "transform: $\\mathcal{L}\\{f'\\} = sF(s) - f(0)$. A differential "
                "equation in $t$ therefore becomes an ALGEBRAIC equation in $s$, which "
                "you solve by ordinary algebra, then transform back (N2) to recover "
                "$y(t)$. Initial conditions are swallowed automatically by that "
                "$-f(0)$ term - no separate data-last step (contrast U3.N4).\n\n"
                "Why learn a whole new machine when U3 already works? Because Laplace "
                "handles things U3 struggles with: discontinuous forcings (a switch "
                "flipping on, an impulse hammer-blow), it bundles the initial data "
                "into the algebra, and it exposes the system's TRANSFER FUNCTION - the "
                "central object of signals and control. It is the working language of "
                "electrical engineering, control theory, and much of applied "
                "mathematics.\n\n"
                "This node stays on the ground floor: the definition as an improper "
                "integral, linearity, the transforms of the five elementary functions "
                "you will use constantly ($1, t^n, e^{at}, \\sin\\omega t, "
                "\\cos\\omega t$), where those integrals converge, and the derivative "
                "property. N2 inverts; N3 applies the machine to solve IVPs."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "The Laplace transform is built from calculus you already own, applied "
                "in a new way.\n\n"
                "1. IMPROPER INTEGRALS (C205). "
                "$\\int_0^{\\infty} h(t)\\,dt = \\lim_{b\\to\\infty}\\int_0^{b} "
                "h(t)\\,dt$; it CONVERGES if the limit is finite. You must be "
                "comfortable evaluating $\\int_0^{\\infty} e^{-kt}\\,dt$ and knowing "
                "$e^{-kt} \\to 0$ as $t \\to \\infty$ exactly when $k > 0$. This "
                "decay-at-infinity condition is what carves out the region of "
                "convergence.\n\n"
                "2. INTEGRATION BY PARTS (C202). "
                "$\\int u\\,dv = uv - \\int v\\,du$. It is the engine behind "
                "$\\mathcal{L}\\{t^n\\}$ and $\\mathcal{L}\\{f'\\}$: each application "
                "lowers a power or moves a derivative off $f$. You will use it "
                "repeatedly.\n\n"
                "3. EXPONENTIALS AND EULER'S FORMULA. Fluency with $e^{at}$, and "
                "$e^{i\\omega t} = \\cos\\omega t + i\\sin\\omega t$, which gives the "
                "sine and cosine transforms in one stroke by transforming the complex "
                "exponential and splitting real/imaginary parts.\n\n"
                "Quick self-check (answers in parentheses):\n\n"
                "- $\\int_0^{\\infty} e^{-3t}\\,dt$? "
                "($[-\\tfrac{1}{3}e^{-3t}]_0^{\\infty} = 0 - (-\\tfrac{1}{3}) = "
                "\\tfrac{1}{3}$.)\n"
                "- For which $s$ does $\\int_0^{\\infty} e^{-st}\\,dt$ converge? "
                "($s > 0$, so $e^{-st} \\to 0$.)\n"
                "- Real and imaginary parts of $e^{i\\omega t}$? "
                "($\\cos\\omega t$ and $\\sin\\omega t$.)\n\n"
                "If improper integrals are rusty, review C205 first - the entire "
                "transform is one improper integral evaluated in a parameter $s$."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "What is the Laplace transform really DOING? Read the integral "
                "$\\int_0^{\\infty} e^{-st}f(t)\\,dt$ as a weighted total of the "
                "entire history of $f$, where the weight $e^{-st}$ discounts later "
                "times more heavily the larger $s$ is. For big $s$, only the "
                "behavior of $f$ near $t = 0$ matters (everything later is crushed by "
                "the exponential); for $s$ just above the threshold, distant times "
                "still contribute. So $F(s)$ packages HOW $f$ behaves across all its "
                "time scales into a single function of $s$ - a kind of continuous "
                "fingerprint.\n\n"
                "The deep reason it tames ODEs is that the exponential $e^{-st}$ is an "
                "EIGEN-object of differentiation. Watch what integration by parts does "
                "to $\\mathcal{L}\\{f'\\}$:\n"
                "$$\\int_0^{\\infty} e^{-st}f'(t)\\,dt = "
                "\\big[e^{-st}f(t)\\big]_0^{\\infty} + s\\int_0^{\\infty} "
                "e^{-st}f(t)\\,dt = -f(0) + sF(s).$$\n"
                "The derivative on $f$ was TRADED for a multiplication by $s$ (plus a "
                "boundary term carrying the initial value). A second derivative trades "
                "for $s^2$, and so on. So the transform converts the operations of "
                "calculus - differentiate, integrate - into the operations of algebra "
                "- multiply, divide - by $s$. A linear constant-coefficient ODE, all "
                "derivatives, becomes a polynomial equation in $s$.\n\n"
                "This is the same move as logarithms turning multiplication into "
                "addition, or Fourier turning convolution into multiplication: find a "
                "transform in which your hard operation becomes an easy one, work in "
                "the easy domain, then transform back. The 'discounting weight' picture "
                "explains convergence too: if $f$ grows slower than some exponential "
                "$e^{ct}$, then for $s > c$ the weight $e^{-st}$ wins and the integral "
                "converges; if $f$ grows too fast (like $e^{t^2}$), no $s$ can "
                "discount it and the transform does not exist. Everything below makes "
                "these ideas concrete."
            )),
            # ---------------------------------------------------------- 4
            (L, "The definition, linearity, and existence", (
                "THE DEFINITION. For $f$ defined on $t \\ge 0$,\n"
                "$$\\mathcal{L}\\{f\\}(s) = F(s) = \\int_0^{\\infty} e^{-st}f(t)\\,dt "
                "= \\lim_{b\\to\\infty}\\int_0^{b} e^{-st}f(t)\\,dt,$$\n"
                "defined for those $s$ making the improper integral converge. The "
                "transform takes a function of $t$ to a function of $s$; we write "
                "lower case for the original ($f, y$) and upper case for the transform "
                "($F, Y$).\n\n"
                "LINEARITY. Because integration is linear,\n"
                "$$\\mathcal{L}\\{a f(t) + b g(t)\\} = a\\mathcal{L}\\{f\\} + "
                "b\\mathcal{L}\\{g\\} = aF(s) + bG(s).$$\n"
                "This one property lets you transform any combination of table "
                "entries term by term - it is what makes a short table so powerful.\n\n"
                "EXISTENCE (sufficient conditions). The transform exists (the integral "
                "converges for all large $s$) if $f$ is (i) PIECEWISE CONTINUOUS on "
                "$[0,\\infty)$ - finitely many jumps on any finite interval - and "
                "(ii) of EXPONENTIAL ORDER: there are constants $M, c$ with "
                "$|f(t)| \\le M e^{ct}$ for large $t$. Condition (ii) says $f$ grows no "
                "faster than some exponential, so a large enough $s$ can discount it. "
                "Under these conditions $F(s)$ exists for $s > c$ and, moreover, "
                "$F(s) \\to 0$ as $s \\to \\infty$ - a handy sanity check (a "
                "candidate transform that does NOT decay to zero, like a nonzero "
                "constant or $s/(s+1)$, cannot be the transform of such an $f$).\n\n"
                "These conditions are SUFFICIENT, not necessary: some functions "
                "outside them still have transforms (e.g. $t^{-1/2}$, which has a jump "
                "to infinity at $0$ yet a finite transform). But essentially every "
                "function you meet in ODEs - polynomials, exponentials, sinusoids, "
                "step and impulse forcings - is piecewise continuous of exponential "
                "order, so the machinery applies. The famous EXCLUSION is $e^{t^2}$: "
                "it outruns every exponential, so no $s$ works and its Laplace "
                "transform does not exist."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples: 1 and e^{at} from the definition", (
                "EXAMPLE 1 ($\\mathcal{L}\\{1\\}$). With $f(t) = 1$:\n"
                "$$\\mathcal{L}\\{1\\} = \\int_0^{\\infty} e^{-st}\\,dt = "
                "\\left[-\\frac{1}{s}e^{-st}\\right]_0^{\\infty}.$$\n"
                "For $s > 0$, $e^{-st} \\to 0$ as $t \\to \\infty$, so the bracket is "
                "$0 - \\big(-\\tfrac{1}{s}\\big) = \\tfrac{1}{s}$. Thus "
                "$\\mathcal{L}\\{1\\} = \\dfrac{1}{s}$, valid for $s > 0$. If "
                "$s \\le 0$ the exponential does not decay and the integral diverges - "
                "there is the region of convergence, falling straight out of the "
                "computation.\n\n"
                "EXAMPLE 2 ($\\mathcal{L}\\{e^{at}\\}$). With $f(t) = e^{at}$:\n"
                "$$\\mathcal{L}\\{e^{at}\\} = \\int_0^{\\infty} e^{-st}e^{at}\\,dt "
                "= \\int_0^{\\infty} e^{-(s-a)t}\\,dt "
                "= \\left[-\\frac{1}{s-a}e^{-(s-a)t}\\right]_0^{\\infty}.$$\n"
                "The integral converges when the exponent is negative, i.e. "
                "$s - a > 0$, and then the bracket is $\\dfrac{1}{s-a}$. So\n"
                "$$\\mathcal{L}\\{e^{at}\\} = \\frac{1}{s-a}, \\qquad s > a.$$\n"
                "The region of convergence has MOVED to $s > a$ - the faster $e^{at}$ "
                "grows (larger $a$), the larger $s$ must be to discount it. Note "
                "Example 1 is the special case $a = 0$: $\\mathcal{L}\\{1\\} = "
                "1/(s-0) = 1/s$.\n\n"
                "Already these two entries, with linearity, transform any combination "
                "of exponentials and constants: e.g. "
                "$\\mathcal{L}\\{5 - 3e^{2t}\\} = \\dfrac{5}{s} - "
                "\\dfrac{3}{s-2}$, valid for $s > 2$ (the more restrictive of $s>0$ "
                "and $s>2$)."
            )),
            # ---------------------------------------------------------- 6
            (L, "Properties: the core table (t^n, sin, cos) and ROC", (
                "$\\mathcal{L}\\{t^n\\}$ BY INTEGRATION BY PARTS. For "
                "$\\mathcal{L}\\{t\\}$, take $u = t, dv = e^{-st}dt$: "
                "$\\int_0^{\\infty} t e^{-st}dt = "
                "\\big[-\\tfrac{t}{s}e^{-st}\\big]_0^{\\infty} + "
                "\\tfrac{1}{s}\\int_0^{\\infty} e^{-st}dt = 0 + "
                "\\tfrac{1}{s}\\cdot\\tfrac{1}{s} = \\tfrac{1}{s^2}$. Each further "
                "power drops one $t$ and one factor of $s$, giving by induction\n"
                "$$\\mathcal{L}\\{t^n\\} = \\frac{n!}{s^{n+1}}, \\qquad s > 0.$$\n"
                "So $\\mathcal{L}\\{t\\} = 1/s^2$, $\\mathcal{L}\\{t^2\\} = 2/s^3$, "
                "$\\mathcal{L}\\{t^3\\} = 6/s^4$.\n\n"
                "$\\mathcal{L}\\{\\sin\\omega t\\}$ AND "
                "$\\mathcal{L}\\{\\cos\\omega t\\}$ VIA EULER. Transform "
                "$e^{i\\omega t}$ using Example 5.2 with $a = i\\omega$: "
                "$\\mathcal{L}\\{e^{i\\omega t}\\} = \\dfrac{1}{s - i\\omega} "
                "= \\dfrac{s + i\\omega}{s^2 + \\omega^2}$ (multiply by the "
                "conjugate). Since $e^{i\\omega t} = \\cos\\omega t + i\\sin\\omega "
                "t$, split real and imaginary parts:\n"
                "$$\\mathcal{L}\\{\\cos\\omega t\\} = \\frac{s}{s^2 + \\omega^2}, "
                "\\qquad \\mathcal{L}\\{\\sin\\omega t\\} = "
                "\\frac{\\omega}{s^2 + \\omega^2}, \\qquad s > 0.$$\n"
                "Note the numerators: COSINE gets $s$, SINE gets $\\omega$ - a "
                "distinction that dominates the inverse-transform work of N2.\n\n"
                "REGIONS OF CONVERGENCE (ROC) SUMMARY. The abscissa of convergence is "
                "the growth rate of $f$: $s > 0$ for $1, t^n, \\sin, \\cos$ (they "
                "grow slower than any exponential), and $s > a$ for $e^{at}$. For a "
                "SUM, the ROC is the intersection - the largest threshold wins "
                "(Example 5's $5 - 3e^{2t}$ needs $s > 2$). While ODE work usually "
                "treats $F(s)$ formally (the ROC takes care of itself once you invert "
                "back to a valid $f$), knowing the ROC is what makes the transform "
                "well-defined and flags impossible candidates (those not decaying as "
                "$s \\to \\infty$)."
            )),
            # ---------------------------------------------------------- 7
            (E, "Standard worked examples: using linearity and the table", (
                "EXAMPLE 1 (polynomial). "
                "$\\mathcal{L}\\{3 + 2t - t^2\\}$. Term by term with "
                "$\\mathcal{L}\\{t^n\\} = n!/s^{n+1}$: "
                "$\\mathcal{L}\\{3\\} = 3/s$, $\\mathcal{L}\\{2t\\} = 2/s^2$, "
                "$\\mathcal{L}\\{t^2\\} = 2/s^3$. So\n"
                "$$\\mathcal{L}\\{3 + 2t - t^2\\} = \\frac{3}{s} + \\frac{2}{s^2} "
                "- \\frac{2}{s^3}, \\qquad s > 0.$$\n"
                "(The $t^2$ contributes $2!/s^3 = 2/s^3$ - do not forget the "
                "factorial.)\n\n"
                "EXAMPLE 2 (shifted exponential combination). "
                "$\\mathcal{L}\\{5e^{-2t}\\} = \\dfrac{5}{s-(-2)} = \\dfrac{5}{s+2}$, "
                "$s > -2$. And "
                "$\\mathcal{L}\\{4e^{3t} - e^{-t}\\} = \\dfrac{4}{s-3} - "
                "\\dfrac{1}{s+1}$, $s > 3$.\n\n"
                "EXAMPLE 3 (sinusoids with a coefficient). "
                "$\\mathcal{L}\\{4\\sin 3t\\} = 4\\cdot\\dfrac{3}{s^2 + 9} = "
                "\\dfrac{12}{s^2 + 9}$. Note the numerator is $4\\omega = 12$, not "
                "$4$ - the sine transform carries $\\omega$ upstairs. And "
                "$\\mathcal{L}\\{2\\cos 5t - 3\\sin 5t\\} = "
                "\\dfrac{2s}{s^2 + 25} - \\dfrac{15}{s^2 + 25} = "
                "\\dfrac{2s - 15}{s^2 + 25}$.\n\n"
                "EXAMPLE 4 (mixed). "
                "$\\mathcal{L}\\{2 + e^{4t} + \\cos 2t\\} = \\dfrac{2}{s} + "
                "\\dfrac{1}{s-4} + \\dfrac{s}{s^2 + 4}$, valid for $s > 4$. Every one "
                "of these is a single application of linearity plus the five-entry "
                "table - which is why memorizing that table cold is the highest-yield "
                "investment in the whole unit."
            )),
            # ---------------------------------------------------------- 8
            (L, "Technique: the transform of derivatives", (
                "The property that turns ODEs into algebra deserves its own page, "
                "because it is the whole point of the method.\n\n"
                "FIRST DERIVATIVE. Integrate by parts with $u = e^{-st}$, "
                "$dv = f'(t)\\,dt$:\n"
                "$$\\mathcal{L}\\{f'\\} = \\int_0^{\\infty} e^{-st}f'(t)\\,dt "
                "= \\big[e^{-st}f(t)\\big]_0^{\\infty} + s\\int_0^{\\infty} "
                "e^{-st}f(t)\\,dt.$$\n"
                "For $f$ of exponential order and $s$ large, the boundary term at "
                "$\\infty$ is $0$ and at $0$ is $-f(0)$, leaving\n"
                "$$\\boxed{\\;\\mathcal{L}\\{f'\\} = sF(s) - f(0)\\;}.$$\n"
                "Differentiation became multiplication by $s$, minus a boundary term "
                "that INJECTS THE INITIAL VALUE. This is why Laplace handles initial "
                "conditions automatically.\n\n"
                "SECOND DERIVATIVE. Apply the rule twice (treat $f'$ as the function): "
                "$\\mathcal{L}\\{f''\\} = s\\mathcal{L}\\{f'\\} - f'(0) = "
                "s(sF - f(0)) - f'(0)$, i.e.\n"
                "$$\\mathcal{L}\\{f''\\} = s^2 F(s) - s f(0) - f'(0).$$\n"
                "In general $\\mathcal{L}\\{f^{(n)}\\} = s^n F - s^{n-1}f(0) - "
                "\\cdots - f^{(n-1)}(0)$: multiply by $s^n$ and subtract a "
                "polynomial in $s$ built from the initial data.\n\n"
                "THE PAYOFF PREVIEW. Transform an equation like "
                "$y'' + 3y' + 2y = g(t)$: it becomes "
                "$\\big[s^2 Y - sy(0) - y'(0)\\big] + 3\\big[sY - y(0)\\big] + 2Y = "
                "G(s)$, an ALGEBRAIC equation you solve for $Y(s)$ by dividing. The "
                "coefficient of $Y$ is $s^2 + 3s + 2$ - the CHARACTERISTIC POLYNOMIAL, "
                "now appearing as a denominator. The initial conditions are already "
                "inside. Inverting $Y(s)$ back to $y(t)$ (N2) finishes the job. This "
                "node builds the transforms; page 8 is the hinge on which the whole "
                "method swings (executed fully in N3)."
            )),
            # ---------------------------------------------------------- 9
            (E, "Harder worked examples: derivations and the s-shift", (
                "EXAMPLE 1 ($\\mathcal{L}\\{t^2\\}$ from the derivative rule, as a "
                "cross-check). Let $f = t^2$, so $f(0) = 0$, $f' = 2t$, "
                "$f'(0) = 0$, $f'' = 2$. Then "
                "$\\mathcal{L}\\{f''\\} = \\mathcal{L}\\{2\\} = 2/s$. But also "
                "$\\mathcal{L}\\{f''\\} = s^2 F - s f(0) - f'(0) = s^2 F$. Equate: "
                "$s^2 F = 2/s$, so $F = 2/s^3$ - matching the table. The derivative "
                "rule and direct integration agree.\n\n"
                "EXAMPLE 2 (the first-shift / $s$-shift theorem, a preview of N2's "
                "completing-the-square). Multiplying $f$ by $e^{at}$ SHIFTS the "
                "transform: "
                "$\\mathcal{L}\\{e^{at}f(t)\\} = F(s - a)$. Quick derivation: "
                "$\\int_0^{\\infty} e^{-st}e^{at}f\\,dt = \\int_0^{\\infty} "
                "e^{-(s-a)t}f\\,dt = F(s-a)$. Consequences you will reuse constantly:\n"
                "$$\\mathcal{L}\\{e^{at}\\cos\\omega t\\} = "
                "\\frac{s - a}{(s-a)^2 + \\omega^2}, \\quad "
                "\\mathcal{L}\\{e^{at}\\sin\\omega t\\} = "
                "\\frac{\\omega}{(s-a)^2 + \\omega^2},$$\n"
                "$$\\mathcal{L}\\{t^n e^{at}\\} = \\frac{n!}{(s-a)^{n+1}}.$$\n"
                "Each is the plain table entry with $s$ replaced by $s - a$. For "
                "instance $\\mathcal{L}\\{e^{-t}\\sin 2t\\} = "
                "\\dfrac{2}{(s+1)^2 + 4}$.\n\n"
                "EXAMPLE 3 (combine everything). "
                "$\\mathcal{L}\\{t^2 e^{3t} + e^{-t}\\cos 4t\\}$. First term (shift "
                "with $n = 2, a = 3$): $\\dfrac{2}{(s-3)^3}$. Second term (shift with "
                "$a = -1, \\omega = 4$): $\\dfrac{s+1}{(s+1)^2 + 16}$. Sum: "
                "$\\dfrac{2}{(s-3)^3} + \\dfrac{s+1}{(s+1)^2 + 16}$. Reading these "
                "shifted forms BACKWARD is exactly the inverse-transform skill of N2, "
                "so getting comfortable with the $s$-shift now pays off immediately "
                "there."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: convergence, jumps, and non-existence", (
                "Three edges sharpen your understanding of when and how the transform "
                "behaves.\n\n"
                "GROWTH TOO FAST - NO TRANSFORM. $f(t) = e^{t^2}$ is not of "
                "exponential order: for ANY $c$, $e^{t^2}$ eventually exceeds "
                "$Me^{ct}$, so $e^{-st}e^{t^2} = e^{t^2 - st}$ still blows up as "
                "$t \\to \\infty$ for every fixed $s$. The integral diverges for all "
                "$s$; $\\mathcal{L}\\{e^{t^2}\\}$ does not exist. Exponential order is "
                "the dividing line between having a transform and not.\n\n"
                "JUMPS ARE FINE. Piecewise-continuous functions with finite jumps "
                "(a step turning on, a square wave) have perfectly good transforms - "
                "the integral does not care about finitely many jump points. This "
                "TOLERANCE of discontinuity is a major advantage over U3's methods, "
                "which want smooth forcings; Laplace eats step and impulse inputs "
                "(unit step, Dirac delta - ODE.U4.N3) without complaint.\n\n"
                "THE DECAY TEST FOR CANDIDATE TRANSFORMS. Any $F(s)$ that is the "
                "transform of an exponential-order function must satisfy "
                "$F(s) \\to 0$ as $s \\to \\infty$. So a proposed transform like "
                "$F(s) = \\dfrac{s^2}{s+1}$ (which $\\to \\infty$) or $F(s) = 2$ "
                "(constant, does not decay) CANNOT arise this way - a useful smell "
                "test when you invert (N2). Every table entry obeys it: $1/s$, "
                "$n!/s^{n+1}$, $1/(s-a)$, $\\omega/(s^2+\\omega^2)$, "
                "$s/(s^2+\\omega^2)$ all vanish at infinity.\n\n"
                "A SUBTLETY AT $t = 0$. The transform integrates from $0$, and the "
                "derivative rule uses $f(0)$. For functions with a jump AT $0$, one "
                "must be careful whether $f(0)$ means $f(0^-)$ or $f(0^+)$; the "
                "standard (unilateral) Laplace convention integrates over "
                "$[0,\\infty)$ and uses $f(0)$ from the right. This matters only for "
                "forcings that switch exactly at $t = 0$, and N3's step-function "
                "machinery handles it cleanly. For the smooth elementary functions of "
                "this node, $f(0)$ is unambiguous."
            )),
            # ---------------------------------------------------------- 11
            (E, "Edge-case worked example: a piecewise forcing", (
                "Transform a function with a JUMP - the kind U3 dislikes but Laplace "
                "handles. Let\n"
                "$$f(t) = \\begin{cases} 3 & 0 \\le t < 2 \\\\ 0 & t \\ge 2. "
                "\\end{cases}$$\n"
                "This is a pulse: value $3$ until $t = 2$, then off. Compute directly "
                "from the definition, splitting the integral at the jump:\n"
                "$$\\mathcal{L}\\{f\\} = \\int_0^{2} 3e^{-st}\\,dt + "
                "\\int_2^{\\infty} 0\\cdot e^{-st}\\,dt = "
                "3\\int_0^{2} e^{-st}\\,dt.$$\n"
                "The second integral is zero, and the first is\n"
                "$$3\\left[-\\frac{1}{s}e^{-st}\\right]_0^{2} = "
                "3\\left(-\\frac{1}{s}e^{-2s} + \\frac{1}{s}\\right) "
                "= \\frac{3}{s}\\big(1 - e^{-2s}\\big), \\qquad s > 0.$$\n"
                "The factor $(1 - e^{-2s})$ encodes the pulse ending at $t = 2$; the "
                "$e^{-2s}$ is the hallmark of a shift/switch at time $2$ (the "
                "second-shift theorem, ODE.U4.N3, makes this systematic). Notice the "
                "jump caused no trouble at all - the integral simply split into "
                "continuous pieces.\n\n"
                "CONTRAST. Undetermined coefficients cannot even write a guess for "
                "this discontinuous forcing, and variation of parameters would require "
                "integrating against a piecewise $g$ by hand. The Laplace transform "
                "reduced it to a two-line computation. This is precisely why "
                "engineering problems with switches, pulses, and impulses are done in "
                "the Laplace domain: the transform is BUILT to swallow "
                "discontinuities. Keep this example in mind when N3 introduces the "
                "unit step function $u(t - a)$, whose transform "
                "$\\dfrac{e^{-as}}{s}$ generalizes exactly the $e^{-2s}$ factor you "
                "just produced."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: '$\\mathcal{L}\\{f\\cdot g\\} = "
                "\\mathcal{L}\\{f\\}\\cdot\\mathcal{L}\\{g\\}$.' FALSE - the transform "
                "is linear (respects sums and scalar multiples), but the transform of "
                "a PRODUCT is NOT the product of transforms. (The product of "
                "transforms corresponds to CONVOLUTION in $t$, ODE.U4.N3, not to "
                "$fg$.) So $\\mathcal{L}\\{t\\cdot e^{2t}\\} \\ne "
                "\\tfrac{1}{s^2}\\cdot\\tfrac{1}{s-2}$; use the shift theorem: "
                "$\\mathcal{L}\\{te^{2t}\\} = 1/(s-2)^2$.\n\n"
                "MISCONCEPTION 2: 'The sine and cosine transforms have the same "
                "numerator.' No - COSINE gets $s$ upstairs "
                "($s/(s^2+\\omega^2)$), SINE gets $\\omega$ "
                "($\\omega/(s^2+\\omega^2)$). Swapping them is a top-frequency error, "
                "and it wrecks inverse transforms (N2).\n\n"
                "MISCONCEPTION 3: 'Forget the factorial in $\\mathcal{L}\\{t^n\\}$.' "
                "It is $n!/s^{n+1}$, not $1/s^{n+1}$. $\\mathcal{L}\\{t^2\\} = 2/s^3$, "
                "$\\mathcal{L}\\{t^3\\} = 6/s^4$.\n\n"
                "MISCONCEPTION 4: 'Every function has a Laplace transform.' Only "
                "functions of exponential order (roughly) do. $e^{t^2}$ has none. The "
                "growth condition is real.\n\n"
                "MISCONCEPTION 5: 'The initial condition is separate from the "
                "transform.' The derivative rule $\\mathcal{L}\\{f'\\} = sF - f(0)$ "
                "BAKES IN $f(0)$; that is the whole reason Laplace handles IVPs "
                "without a data-last step. Dropping the $-f(0)$ (or $-sf(0) - f'(0)$ "
                "for $f''$) discards the initial data and gives the wrong solution.\n\n"
                "MISCONCEPTION 6: 'The variable $s$ is just a dummy.' It is the "
                "genuine independent variable of the transformed function; the ROC "
                "($s > a$) constrains it, and its role as 'multiply here = "
                "differentiate there' is what the whole method exploits."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational and procedural errors", (
                "ERROR 1: Sign in the exponential kernel. The definition has "
                "$e^{-st}$ (minus). Writing $e^{st}$ makes every integral diverge and "
                "flips the algebra. Anchor on the minus.\n\n"
                "ERROR 2: Wrong shift direction. $\\mathcal{L}\\{e^{at}f(t)\\} = "
                "F(s - a)$: multiplying by $e^{at}$ replaces $s$ with $s - a$ "
                "(shifts RIGHT by $a$). A common slip is $F(s + a)$ or shifting the "
                "wrong way; check with $\\mathcal{L}\\{e^{2t}\\} = 1/(s-2)$ "
                "($f = 1, F = 1/s \\to 1/(s-2)$).\n\n"
                "ERROR 3: Dropping the factorial or the $\\omega$. "
                "$\\mathcal{L}\\{t^3\\} = 6/s^4$ (not $1/s^4$); "
                "$\\mathcal{L}\\{\\sin 3t\\} = 3/(s^2+9)$ (numerator $3$, not $1$).\n\n"
                "ERROR 4: Mishandling the boundary term in "
                "$\\mathcal{L}\\{f'\\}$. It is $-f(0)$ (the value at the LOWER limit), "
                "and for $f''$ it is $-sf(0) - f'(0)$. Getting the signs or the number "
                "of initial-condition terms wrong is the top mistake when solving IVPs "
                "in N3.\n\n"
                "ERROR 5: Ignoring the region of convergence when it matters. For a "
                "sum of exponentials the ROC is the intersection; the transform "
                "formula is only valid there. In routine ODE work you carry $F(s)$ "
                "formally, but if asked for the ROC, take the largest threshold.\n\n"
                "ERROR 6: Trying to transform a product as a product of transforms "
                "(see Misconception 1) - or, conversely, forgetting linearity and "
                "failing to split a SUM term by term. Sums split; products do not. "
                "ERROR 7: Confusing lower-case and upper-case - $F(s)$ is the "
                "transform, $f(t)$ the original; mixing them mid-problem produces "
                "nonsense dimensions like a function of $s$ set equal to a function of "
                "$t$."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: why engineers live in the s-domain", (
                "The Laplace transform is not a classroom curiosity - it is the "
                "primary analysis tool of several engineering disciplines, and the "
                "reasons trace directly to this node.\n\n"
                "1. CIRCUITS AND THE TRANSFER FUNCTION. Transforming a circuit's "
                "differential equations replaces $\\tfrac{d}{dt}$ with $s$, turning "
                "inductors and capacitors into algebraic 'impedances' ($sL$ and "
                "$1/(sC)$). The whole circuit becomes an algebra problem, and the "
                "ratio $H(s) = Y(s)/G(s)$ of output transform to input transform is "
                "the TRANSFER FUNCTION - a single function summarizing how the system "
                "responds to any input. This is the foundational object of electrical "
                "engineering.\n\n"
                "2. CONTROL SYSTEMS. Stability, response speed, and steady-state error "
                "are all read from the transfer function's structure in the "
                "$s$-plane - specifically the roots of the denominator (the POLES, "
                "which are exactly the characteristic roots of U2/U3). A system is "
                "stable iff all poles have negative real part; the transient/steady "
                "state split of U3.N4 becomes 'poles in the left half-plane decay.' "
                "Bode plots, root locus, and PID tuning all live in the $s$-domain "
                "this node opens.\n\n"
                "3. DISCONTINUOUS AND IMPULSIVE INPUTS. Switches, digital pulses, and "
                "hammer-blow impulses are routine in engineering and awkward for U3's "
                "methods. Laplace transforms them cleanly (the pulse of Example 11, "
                "the step and delta of N3), which is why signal processing and "
                "mechanical shock analysis are done here.\n\n"
                "4. THE COMMON THREAD. In every case the value is the same trade this "
                "node introduced: differentiation becomes multiplication by $s$, so "
                "differential equations become algebra, initial conditions ride along "
                "for free, and discontinuities are no obstacle. Master the definition, "
                "the table, and the derivative rule, and you hold the key to the "
                "entire $s$-domain toolkit."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: transforming an IVP (setup)", (
                "See the payoff of the derivative rule by TRANSFORMING (not yet "
                "solving) an initial value problem: "
                "$y'' + 3y' + 2y = e^{-t}$, $y(0) = 1$, $y'(0) = 0$.\n\n"
                "Apply $\\mathcal{L}$ to both sides, using linearity and the "
                "derivative rules with $Y = \\mathcal{L}\\{y\\}$:\n"
                "$$\\mathcal{L}\\{y''\\} = s^2 Y - s\\,y(0) - y'(0) = s^2 Y - s,$$\n"
                "$$\\mathcal{L}\\{y'\\} = sY - y(0) = sY - 1,$$\n"
                "$$\\mathcal{L}\\{y\\} = Y, \\qquad "
                "\\mathcal{L}\\{e^{-t}\\} = \\frac{1}{s+1}.$$\n"
                "Substitute:\n"
                "$$(s^2 Y - s) + 3(sY - 1) + 2Y = \\frac{1}{s+1}.$$\n"
                "Collect the $Y$ terms and move the constants (which carry the initial "
                "data) to the right:\n"
                "$$(s^2 + 3s + 2)Y = \\frac{1}{s+1} + s + 3.$$\n"
                "Solve algebraically for $Y$:\n"
                "$$Y(s) = \\frac{1}{(s+1)(s^2 + 3s + 2)} + \\frac{s + 3}{s^2 + 3s + "
                "2}.$$\n"
                "The differential equation has become ALGEBRA. The denominator "
                "$s^2 + 3s + 2 = (s+1)(s+2)$ is the characteristic polynomial (roots "
                "$-1, -2$, the same as U3); the initial data $y(0) = 1$ produced the "
                "$+s$, and $y'(0) = 0$ contributed nothing extra. Everything - "
                "homogeneous behavior, forced response, AND initial conditions - is "
                "packed into this one expression for $Y(s)$.\n\n"
                "The remaining task is to INVERT $Y(s)$ back to $y(t)$ using partial "
                "fractions and the table read backward - which is exactly ODE.U4.N2. "
                "This node has delivered you to the doorstep: the transform table and "
                "the derivative rule turned a calculus problem into an algebra "
                "problem. N2 walks you back."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: where this node plugs in", (
                "BACKWARD. The definition rests on improper integrals (C205) and "
                "integration by parts (C202) - the transform IS an improper integral, "
                "and the key derivations ($t^n$, the derivative rule) ARE integration "
                "by parts. Euler's formula supplies the sine/cosine transforms from "
                "the exponential one.\n\n"
                "SIDEWAYS. The transform is an ALTERNATIVE to the U3 methods for the "
                "same nonhomogeneous IVPs. Where U3.N4 solved by 'homogeneous + "
                "particular, data last,' Laplace bakes the data into the algebra via "
                "$\\mathcal{L}\\{y'\\} = sY - y(0)$. The characteristic polynomial "
                "$s^2 + bs + c$ that governed U2/U3 reappears here as the denominator "
                "of $Y(s)$; its roots are the same, now called POLES.\n\n"
                "FORWARD. ODE.U4.N2 (inverse Laplace via partial fractions) is the "
                "immediate sequel: it reads this node's table BACKWARD to recover "
                "$y(t)$ from $Y(s)$, and the $s$-shift theorem of Example 9 becomes "
                "the completing-the-square technique for irreducible quadratics. "
                "ODE.U4.N3 adds the step and impulse functions (generalizing the "
                "pulse of Example 11) and the convolution theorem, then assembles the "
                "full IVP-solving method previewed in Example 15.\n\n"
                "FURTHER OUT. The transfer function $H(s)$, poles and zeros, "
                "stability, and frequency response in signals and control theory all "
                "begin with this transform. The related Fourier transform (drop the "
                "decay, integrate over all $t$ with $e^{-i\\omega t}$) analyzes "
                "steady-state frequency content; the $z$-transform does the same for "
                "discrete-time systems. Every one is the same idea this node "
                "introduced: move to a domain where differentiation (or shifting) "
                "becomes multiplication, and hard problems become easy."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: the transform as an operator and its inverse", (
                "Step back and view $\\mathcal{L}$ as a LINEAR OPERATOR that maps a "
                "space of time functions to a space of $s$-functions - and ask the "
                "structural questions that make the theory deep.\n\n"
                "IS IT INVERTIBLE? For the transform to solve ODEs we must recover "
                "$f(t)$ from $F(s)$ uniquely. It nearly is: LERCH'S THEOREM says two "
                "piecewise-continuous functions of exponential order with the same "
                "transform can differ only at their jump points (a set of measure "
                "zero) - so for continuous $f$, the inverse is unique. This is what "
                "licenses the table-lookup inversion of N2: if $F(s) = 1/(s-2)$, then "
                "$f(t) = e^{2t}$ is THE answer, not merely AN answer.\n\n"
                "WHAT DOES THE INVERSE LOOK LIKE? There is an explicit formula - the "
                "Bromwich integral, a contour integral in the complex $s$-plane: "
                "$f(t) = \\tfrac{1}{2\\pi i}\\int_{\\gamma - i\\infty}^{\\gamma + "
                "i\\infty} e^{st}F(s)\\,ds$. It requires complex analysis (residues, "
                "the poles of $F$), and in practice we NEVER use it directly - "
                "partial fractions plus a table (N2) recovers $f$ far more easily for "
                "the rational $F(s)$ that ODEs produce. But its existence is why the "
                "method is theoretically sound, and it reveals that the POLES of $F$ "
                "(where $F$ blows up) determine the exponential/oscillatory pieces of "
                "$f$ - a pole at $s = a$ gives an $e^{at}$ term, a pair at "
                "$\\pm i\\omega$ gives $\\cos\\omega t, \\sin\\omega t$. The pole "
                "locations ARE the system's behavior.\n\n"
                "WHY $e^{-st}$ AND NOT SOMETHING ELSE? Because $e^{-st}$ is the "
                "eigenfunction of $\\tfrac{d}{dt}$ (differentiating scales it by "
                "$-s$), the transform DIAGONALIZES the differentiation operator - the "
                "same reason Fourier uses $e^{-i\\omega t}$ and matrix diagonalization "
                "uses eigenvectors. Constant-coefficient linear ODEs are exactly the "
                "operators that $e^{st}$ diagonalizes, which is precisely why Laplace "
                "solves them so cleanly and why it is the natural language for linear "
                "time-invariant systems everywhere in engineering and physics."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "DEFINITION. "
                "$\\mathcal{L}\\{f\\}(s) = F(s) = \\int_0^{\\infty} e^{-st}f(t)\\,dt$, "
                "for $s$ in the region of convergence. LINEAR: "
                "$\\mathcal{L}\\{af + bg\\} = aF + bG$.\n\n"
                "CORE TABLE (memorize):\n"
                "$$\\mathcal{L}\\{1\\} = \\frac{1}{s}\\;(s>0), \\qquad "
                "\\mathcal{L}\\{t^n\\} = \\frac{n!}{s^{n+1}}\\;(s>0),$$\n"
                "$$\\mathcal{L}\\{e^{at}\\} = \\frac{1}{s-a}\\;(s>a), \\qquad "
                "\\mathcal{L}\\{\\sin\\omega t\\} = \\frac{\\omega}{s^2+\\omega^2}, "
                "\\qquad \\mathcal{L}\\{\\cos\\omega t\\} = "
                "\\frac{s}{s^2+\\omega^2}.$$\n"
                "COSINE carries $s$; SINE carries $\\omega$. Do not drop the "
                "factorial.\n\n"
                "DERIVATIVE RULE (the whole point): "
                "$\\mathcal{L}\\{f'\\} = sF - f(0)$; "
                "$\\mathcal{L}\\{f''\\} = s^2 F - sf(0) - f'(0)$. Differentiation "
                "becomes multiplication by $s$; initial data rides along.\n\n"
                "$s$-SHIFT (first-shift): "
                "$\\mathcal{L}\\{e^{at}f(t)\\} = F(s-a)$. Hence "
                "$\\mathcal{L}\\{t^n e^{at}\\} = n!/(s-a)^{n+1}$, "
                "$\\mathcal{L}\\{e^{at}\\cos\\omega t\\} = (s-a)/((s-a)^2+\\omega^2)$, "
                "and similarly for sine.\n\n"
                "EXISTENCE. Piecewise continuous + exponential order "
                "($|f| \\le Me^{ct}$) guarantees $F(s)$ for $s > c$, and "
                "$F(s) \\to 0$ as $s \\to \\infty$. $e^{t^2}$ has NO transform. Jumps "
                "are allowed.\n\n"
                "STRATEGY. To transform: split by linearity, apply the table, use the "
                "$s$-shift for $e^{at}\\times$(table entry), and the derivative rule "
                "for $y', y''$. Products do NOT split (that is convolution, N3). Next "
                "stop: invert $Y(s)$ back to $y(t)$ (N2)."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all seven before reading solutions.\n\n"
                "1. $\\mathcal{L}\\{t^3\\}$?\n"
                "2. $\\mathcal{L}\\{\\cos 4t\\}$ and $\\mathcal{L}\\{\\sin 4t\\}$?\n"
                "3. $\\mathcal{L}\\{2 - 5t + e^{3t}\\}$?\n"
                "4. $\\mathcal{L}\\{e^{-2t}\\sin 3t\\}$ (use the shift theorem).\n"
                "5. Using $\\mathcal{L}\\{f'\\} = sF - f(0)$, transform "
                "$y' + 2y = 0$, $y(0) = 5$, and solve for $Y(s)$.\n"
                "6. Does $\\mathcal{L}\\{e^{t^2}\\}$ exist? Why?\n"
                "7. True or false: "
                "$\\mathcal{L}\\{t\\,e^{t}\\} = \\tfrac{1}{s^2}\\cdot\\tfrac{1}{s-1}$.\n\n"
                "SOLUTIONS.\n"
                "1. $3!/s^4 = 6/s^4$.\n"
                "2. $\\mathcal{L}\\{\\cos 4t\\} = s/(s^2+16)$; "
                "$\\mathcal{L}\\{\\sin 4t\\} = 4/(s^2+16)$.\n"
                "3. $\\dfrac{2}{s} - \\dfrac{5}{s^2} + \\dfrac{1}{s-3}$ (using "
                "$\\mathcal{L}\\{t\\} = 1/s^2$).\n"
                "4. Shift $a = -2$ on $\\mathcal{L}\\{\\sin 3t\\} = 3/(s^2+9)$: "
                "$\\dfrac{3}{(s+2)^2 + 9}$.\n"
                "5. $(sY - 5) + 2Y = 0 \\Rightarrow (s+2)Y = 5 \\Rightarrow "
                "Y = \\dfrac{5}{s+2}$ (which inverts to $5e^{-2t}$, matching the "
                "obvious solution).\n"
                "6. NO - $e^{t^2}$ is not of exponential order; $e^{t^2 - st}$ "
                "diverges for every $s$.\n"
                "7. FALSE - the transform of a product is not the product of "
                "transforms. Correct: $\\mathcal{L}\\{te^t\\} = 1/(s-1)^2$ (shift "
                "$a=1$ on $\\mathcal{L}\\{t\\} = 1/s^2$).\n\n"
                "Scoring: 7/7 - on to inversion (N2). 5-6 - re-memorize the table "
                "(pages 6, 18) and the shift (page 9). $\\le 4$ - rework pages 4-9."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Check every box honestly:\n\n"
                "- State the definition "
                "$\\mathcal{L}\\{f\\} = \\int_0^{\\infty} e^{-st}f\\,dt$ and evaluate "
                "it for $1$ and $e^{at}$ from scratch, finding the ROC.\n"
                "- Use linearity to transform any sum/scalar combination term by "
                "term.\n"
                "- Reproduce the core table from memory: $1, t^n, e^{at}, "
                "\\sin\\omega t, \\cos\\omega t$, with the factorial and the "
                "$s$-vs-$\\omega$ numerators correct.\n"
                "- Derive $\\mathcal{L}\\{t^n\\}$ or $\\mathcal{L}\\{f'\\}$ by "
                "integration by parts.\n"
                "- State and apply the derivative rules "
                "$\\mathcal{L}\\{f'\\} = sF - f(0)$ and "
                "$\\mathcal{L}\\{f''\\} = s^2F - sf(0) - f'(0)$, explaining how they "
                "inject initial data.\n"
                "- Apply the first-shift theorem $\\mathcal{L}\\{e^{at}f\\} = F(s-a)$ "
                "for $t^n e^{at}$ and $e^{at}$ times sine/cosine.\n"
                "- State the existence conditions (piecewise continuous + exponential "
                "order) and give the standard non-example $e^{t^2}$.\n"
                "- Use the decay test ($F \\to 0$ as $s \\to \\infty$) to reject "
                "impossible transforms.\n"
                "- Transform a piecewise/pulse forcing directly by splitting the "
                "integral.\n"
                "- Transform a full second-order IVP into an algebraic equation for "
                "$Y(s)$ and identify the characteristic polynomial in the "
                "denominator.\n\n"
                "When every box is checked, you can move any linear problem into the "
                "$s$-domain. Proceed to ODE.U4.N2 to learn the return trip - inverting "
                "$Y(s)$ back to $y(t)$."
            )),
        ],
    },
    "ODE.U4.N2": {
        "summary": (
            "A complete course on the inverse Laplace transform: inversion by table "
            "plus partial fractions, the three factor types (distinct linear, "
            "repeated linear, irreducible quadratic), completing the square with the "
            "s-shift, fixing the omega on sine/cosine terms, and the cover-up "
            "(Heaviside) method for fast coefficients."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "In N1 you learned to send a problem INTO the $s$-domain: transform an "
                "ODE and its data into an algebraic equation, solve for $Y(s)$. This "
                "node is the RETURN TRIP - given $Y(s)$, recover the time function "
                "$y(t) = \\mathcal{L}^{-1}\\{Y(s)\\}$. Without inversion, the Laplace "
                "method is a road that only goes one way; with it, the loop closes "
                "and you can actually solve differential equations.\n\n"
                "The strategy is simple to state: the $Y(s)$ that ODEs produce is "
                "always a RATIONAL function (polynomial over polynomial in $s$), and "
                "the transform table (N1) tells you the inverse of each SIMPLE rational "
                "piece. So the whole job is to break $Y(s)$ into table-sized pieces by "
                "PARTIAL FRACTIONS, then read each piece backward through the table by "
                "linearity:\n"
                "$$\\mathcal{L}^{-1}\\{aF + bG\\} = a\\,\\mathcal{L}^{-1}\\{F\\} + "
                "b\\,\\mathcal{L}^{-1}\\{G\\}.$$\n"
                "Three kinds of denominator factor arise, and each maps to a specific "
                "time function: DISTINCT LINEAR factors $(s - a)$ give exponentials "
                "$e^{at}$; REPEATED linear factors $(s-a)^k$ give $t^{k-1}e^{at}$; "
                "IRREDUCIBLE QUADRATIC factors $(s-\\alpha)^2 + \\omega^2$ give "
                "$e^{\\alpha t}\\cos\\omega t$ and $e^{\\alpha t}\\sin\\omega t$. "
                "Handling the quadratic case cleanly requires COMPLETING THE SQUARE "
                "and the $s$-shift theorem, plus the ever-present bookkeeping of "
                "FIXING THE $\\omega$ on sine terms.\n\n"
                "This node teaches all three cases, the completing-the-square "
                "technique, and the COVER-UP (Heaviside) method for finding "
                "coefficients fast. Master it and, combined with N1, you can solve any "
                "constant-coefficient linear IVP by Laplace - the goal N3 assembles "
                "into a single streamlined procedure."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Inversion is table-reading plus algebra, so gather both.\n\n"
                "1. THE FORWARD TABLE, READ BACKWARD (N1). You must instantly "
                "recognize: $\\dfrac{1}{s-a} \\to e^{at}$; "
                "$\\dfrac{n!}{s^{n+1}} \\to t^n$; "
                "$\\dfrac{\\omega}{s^2+\\omega^2} \\to \\sin\\omega t$; "
                "$\\dfrac{s}{s^2+\\omega^2} \\to \\cos\\omega t$; and the shifted "
                "forms $\\dfrac{1}{(s-a)^{n+1}} \\to \\dfrac{t^n}{n!}e^{at}$, "
                "$\\dfrac{\\omega}{(s-a)^2+\\omega^2} \\to e^{at}\\sin\\omega t$, "
                "$\\dfrac{s-a}{(s-a)^2+\\omega^2} \\to e^{at}\\cos\\omega t$. If the "
                "table is not automatic, re-drill N1 first.\n\n"
                "2. PARTIAL FRACTION DECOMPOSITION (C202-level algebra). Splitting a "
                "rational function into a sum over its factors: distinct linear "
                "$\\to A/(s-a) + B/(s-b)$; repeated $\\to A/(s-a) + B/(s-a)^2$; "
                "irreducible quadratic $\\to (Bs + C)/(s^2 + \\omega^2)$. You find the "
                "constants by clearing denominators and matching, or by cover-up.\n\n"
                "3. COMPLETING THE SQUARE. Rewriting $s^2 + bs + c$ as "
                "$(s + b/2)^2 + (c - b^2/4)$. Essential for irreducible quadratics, "
                "where it exposes the shift $\\alpha = -b/2$ and frequency "
                "$\\omega = \\sqrt{c - b^2/4}$.\n\n"
                "Quick self-check (answers in parentheses):\n\n"
                "- $\\mathcal{L}^{-1}\\{1/(s-5)\\}$? ($e^{5t}$.)\n"
                "- $\\mathcal{L}^{-1}\\{1/(s^2+9)\\}$? "
                "($\\tfrac{1}{3}\\sin 3t$ - note the $1/\\omega$.)\n"
                "- Complete the square: $s^2 + 4s + 13$. "
                "($(s+2)^2 + 9$.)\n\n"
                "If partial fractions are rusty, review them now - they are 80% of the "
                "work in this node."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Why does inversion reduce to partial fractions? Because of a lucky "
                "match between the ALGEBRA of rational functions and the CONTENTS of "
                "the transform table.\n\n"
                "Every $Y(s)$ that a constant-coefficient linear ODE produces is a "
                "ratio of polynomials, $Y(s) = P(s)/Q(s)$, where the denominator $Q$ "
                "is (a multiple of) the characteristic polynomial times whatever the "
                "forcing contributed. The FUNDAMENTAL THEOREM OF ALGEBRA says $Q$ "
                "factors completely into linear factors $(s - a)$ and irreducible "
                "quadratic factors $(s - \\alpha)^2 + \\omega^2$ - nothing else can "
                "occur over the reals. And here is the fortunate part: the transform "
                "table has an entry for EXACTLY those building blocks. A simple linear "
                "factor inverts to an exponential; a repeated one to a polynomial "
                "times an exponential; an irreducible quadratic to a (possibly "
                "decaying) sinusoid. So if you can decompose $Y(s)$ into a sum of "
                "these elementary fractions, you can invert term by term - and partial "
                "fractions is precisely the algebra that produces that decomposition.\n\n"
                "There is a deeper reading. The ROOTS of $Q(s)$ - the POLES of $Y(s)$, "
                "the places where $Y$ blows up - ARE the characteristic roots of the "
                "ODE, so they dictate the exponentials and frequencies in $y(t)$. A "
                "real pole at $s = a$ contributes $e^{at}$; a complex pair "
                "$\\alpha \\pm i\\omega$ contributes $e^{\\alpha t}\\cos\\omega t$ and "
                "$e^{\\alpha t}\\sin\\omega t$; a repeated pole contributes the "
                "$t$-growth of resonance (U3.N2). So inverting $Y(s)$ is really "
                "reading the system's natural behavior off the pole locations, exactly "
                "as U2/U3 read it off the characteristic roots. Partial fractions is "
                "the mechanical means; 'poles determine the time behavior' is the idea. "
                "Everything below is technique for carrying it out reliably."
            )),
            # ---------------------------------------------------------- 4
            (L, "The three cases and fixing omega", (
                "Decompose $Y(s) = P(s)/Q(s)$ (with $\\deg P < \\deg Q$; if not, "
                "divide first) according to the factors of $Q$.\n\n"
                "CASE 1 - DISTINCT LINEAR FACTORS. "
                "$\\dfrac{P(s)}{(s-a)(s-b)} = \\dfrac{A}{s-a} + \\dfrac{B}{s-b}$, "
                "inverting to $A e^{at} + B e^{bt}$. Each simple real pole gives one "
                "exponential.\n\n"
                "CASE 2 - REPEATED LINEAR FACTOR. A factor $(s-a)^k$ needs ALL powers "
                "up to $k$: "
                "$\\dfrac{A_1}{s-a} + \\dfrac{A_2}{(s-a)^2} + \\cdots + "
                "\\dfrac{A_k}{(s-a)^k}$. Since "
                "$\\mathcal{L}^{-1}\\{1/(s-a)^{j}\\} = "
                "\\dfrac{t^{j-1}}{(j-1)!}e^{at}$, this inverts to "
                "$\\big(A_1 + A_2 t + \\tfrac{A_3}{2}t^2 + \\cdots\\big)e^{at}$ - a "
                "polynomial times $e^{at}$. Omitting a lower power makes the "
                "decomposition impossible, just like the full-polynomial rule of "
                "U3.N1.\n\n"
                "CASE 3 - IRREDUCIBLE QUADRATIC FACTOR. For "
                "$s^2 + \\omega^2$ (or completed-square "
                "$(s-\\alpha)^2 + \\omega^2$), use a LINEAR numerator: "
                "$\\dfrac{Bs + C}{s^2 + \\omega^2}$. Split it to match the table: "
                "the $Bs$ part gives $B\\cos\\omega t$, the $C$ part gives "
                "$\\dfrac{C}{\\omega}\\sin\\omega t$.\n\n"
                "FIXING THE $\\omega$ - the most common slip. The sine entry is "
                "$\\dfrac{\\omega}{s^2+\\omega^2} \\to \\sin\\omega t$, so "
                "$\\mathcal{L}^{-1}\\{\\dfrac{1}{s^2+\\omega^2}\\} = "
                "\\dfrac{1}{\\omega}\\sin\\omega t$ - you must INSERT the $1/\\omega$. "
                "For $\\dfrac{1}{s^2 + 9}$, $\\omega = 3$, so the inverse is "
                "$\\tfrac{1}{3}\\sin 3t$, NOT $\\sin 3t$. The cosine numerator is "
                "already $s$, so cosines need no such factor. Whenever your numerator "
                "is a bare constant over a quadratic, remember to divide by $\\omega$; "
                "whenever it is $s$ over the quadratic, it is a clean cosine. Getting "
                "this factor right is what separates a correct inverse from a "
                "plausible wrong one."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples: distinct linear factors", (
                "EXAMPLE 1 (two distinct real poles). Invert "
                "$Y(s) = \\dfrac{1}{(s-1)(s-2)}$.\n\n"
                "Decompose: $\\dfrac{1}{(s-1)(s-2)} = \\dfrac{A}{s-1} + "
                "\\dfrac{B}{s-2}$. Clear denominators: "
                "$1 = A(s-2) + B(s-1)$. Set $s = 1$: $1 = A(-1) \\Rightarrow A = -1$. "
                "Set $s = 2$: $1 = B(1) \\Rightarrow B = 1$. So "
                "$Y = \\dfrac{-1}{s-1} + \\dfrac{1}{s-2}$, and inverting each term,\n"
                "$$y(t) = -e^{t} + e^{2t}.$$\n"
                "(Choosing $s = 1$ and $s = 2$ - the roots - made each equation "
                "collapse to one unknown; that shortcut is the cover-up method of "
                "page 6.)\n\n"
                "EXAMPLE 2 (a numerator that matters). Invert "
                "$Y(s) = \\dfrac{s + 7}{(s-1)(s+2)}$. "
                "$\\dfrac{s+7}{(s-1)(s+2)} = \\dfrac{A}{s-1} + \\dfrac{B}{s+2}$, so "
                "$s + 7 = A(s+2) + B(s-1)$. At $s = 1$: $8 = 3A \\Rightarrow "
                "A = \\tfrac{8}{3}$. At $s = -2$: $5 = -3B \\Rightarrow "
                "B = -\\tfrac{5}{3}$. Thus\n"
                "$$y(t) = \\tfrac{8}{3}e^{t} - \\tfrac{5}{3}e^{-2t}.$$\n"
                "Verify the decomposition by checking one other value, say $s = 0$: "
                "left side $\\tfrac{7}{(-1)(2)} = -\\tfrac{7}{2}$; right side "
                "$\\tfrac{8/3}{-1} + \\tfrac{-5/3}{2} = -\\tfrac{8}{3} - "
                "\\tfrac{5}{6} = -\\tfrac{16}{6} - \\tfrac{5}{6} = -\\tfrac{21}{6} = "
                "-\\tfrac{7}{2}$. Matches - the coefficients are correct, and the "
                "inverse follows termwise from the table."
            )),
            # ---------------------------------------------------------- 6
            (L, "Technique: the cover-up (Heaviside) method", (
                "For DISTINCT linear factors, there is a shortcut that finds each "
                "coefficient in one step, no linear system required - the COVER-UP or "
                "HEAVISIDE method.\n\n"
                "To find the coefficient $A$ over the factor $(s - a)$ in "
                "$Y(s) = \\dfrac{P(s)}{(s-a)(\\cdots)}$: multiply $Y$ by $(s - a)$ "
                "(which 'covers up' that factor), then evaluate the rest at "
                "$s = a$:\n"
                "$$A = \\Big[(s-a)\\,Y(s)\\Big]_{s=a} = "
                "\\frac{P(a)}{(\\text{the other factors, evaluated at } a)}.$$\n"
                "It works because setting $s = a$ kills every other term in the "
                "partial-fraction sum (each still has an $(s-a)$ factor), leaving only "
                "$A$. Example: for $\\dfrac{1}{(s-1)(s-2)}$, cover $(s-1)$ and set "
                "$s = 1$: $A = \\dfrac{1}{1-2} = -1$; cover $(s-2)$, set $s = 2$: "
                "$B = \\dfrac{1}{2-1} = 1$ - the Example 5.1 answer instantly.\n\n"
                "SCOPE AND LIMITS. Cover-up gives the coefficient over each DISTINCT "
                "linear factor directly. For a REPEATED factor $(s-a)^k$, cover-up "
                "finds only the HIGHEST-power coefficient $A_k$ (multiply by "
                "$(s-a)^k$ and set $s = a$); the lower coefficients "
                "$A_1, \\ldots, A_{k-1}$ still require differentiation or matching (a "
                "reason repeated factors are more work). For IRREDUCIBLE QUADRATICS, "
                "plain cover-up does not apply directly - you match coefficients, or "
                "cover-up with the COMPLEX root and split real/imaginary parts (an "
                "advanced variant).\n\n"
                "PRACTICAL HABIT. Use cover-up for all distinct linear factors "
                "(fast, error-resistant), then handle any repeated or quadratic "
                "pieces by clearing denominators and matching the coefficients that "
                "cover-up could not reach. This hybrid is the quickest reliable route "
                "through a general partial-fraction problem, and it scales to the "
                "multi-factor $Y(s)$ that real IVPs produce."
            )),
            # ---------------------------------------------------------- 7
            (E, "Standard worked examples: repeated factors and fixing omega", (
                "EXAMPLE 1 (repeated linear factor). Invert "
                "$Y(s) = \\dfrac{s}{(s-1)^2}$.\n\n"
                "Repeated factor needs both powers: "
                "$\\dfrac{s}{(s-1)^2} = \\dfrac{A}{s-1} + \\dfrac{B}{(s-1)^2}$. Clear: "
                "$s = A(s-1) + B$. At $s = 1$: $1 = B$. Compare $s$-coefficients: "
                "$A = 1$. So $Y = \\dfrac{1}{s-1} + \\dfrac{1}{(s-1)^2}$, and since "
                "$\\mathcal{L}^{-1}\\{1/(s-1)^2\\} = te^{t}$,\n"
                "$$y(t) = e^{t} + t e^{t} = (1 + t)e^{t}.$$\n"
                "Check by transforming back: "
                "$\\mathcal{L}\\{(1+t)e^t\\} = \\tfrac{1}{s-1} + \\tfrac{1}{(s-1)^2} "
                "= \\tfrac{(s-1)+1}{(s-1)^2} = \\tfrac{s}{(s-1)^2}$. Correct.\n\n"
                "EXAMPLE 2 (pure quadratic - fixing $\\omega$). Invert "
                "$Y(s) = \\dfrac{2s + 5}{s^2 + 4}$.\n\n"
                "Here $\\omega^2 = 4$, so $\\omega = 2$. Split: "
                "$\\dfrac{2s + 5}{s^2 + 4} = 2\\cdot\\dfrac{s}{s^2 + 4} "
                "+ 5\\cdot\\dfrac{1}{s^2 + 4}$. The first term is $2\\cos 2t$. The "
                "second needs the $1/\\omega$ fix: "
                "$\\mathcal{L}^{-1}\\{1/(s^2+4)\\} = \\tfrac{1}{2}\\sin 2t$, so it "
                "contributes $5\\cdot\\tfrac{1}{2}\\sin 2t = \\tfrac{5}{2}\\sin 2t$. "
                "Thus\n"
                "$$y(t) = 2\\cos 2t + \\tfrac{5}{2}\\sin 2t.$$\n"
                "The $\\tfrac{5}{2}$ (not $5$) is the whole lesson: a bare constant "
                "over $s^2 + \\omega^2$ picks up a $1/\\omega$ on inversion. Forget it "
                "and the amplitude of the sine is wrong by a factor of $\\omega$."
            )),
            # ---------------------------------------------------------- 8
            (L, "Technique: completing the square and the s-shift", (
                "When the quadratic in the denominator is NOT of the pure form "
                "$s^2 + \\omega^2$ - it has an $s$ term, e.g. $s^2 + 4s + 13$ - you "
                "COMPLETE THE SQUARE and use the $s$-shift theorem from N1. This is "
                "the technique for damped-oscillation terms.\n\n"
                "STEP 1 - complete the square in the denominator. "
                "$s^2 + 4s + 13 = (s + 2)^2 + 9 = (s - \\alpha)^2 + \\omega^2$ with "
                "$\\alpha = -2$, $\\omega = 3$. (In general "
                "$s^2 + bs + c = (s + \\tfrac{b}{2})^2 + (c - \\tfrac{b^2}{4})$; it is "
                "irreducible exactly when $c - \\tfrac{b^2}{4} > 0$, so the leftover "
                "is a genuine $+\\omega^2$.)\n\n"
                "STEP 2 - rewrite the NUMERATOR in terms of the shift $(s - \\alpha)$. "
                "The $s$-shift theorem says "
                "$\\mathcal{L}^{-1}\\{F(s - \\alpha)\\} = e^{\\alpha t}f(t)$, and the "
                "shifted table entries are\n"
                "$$\\frac{s - \\alpha}{(s-\\alpha)^2 + \\omega^2} \\to "
                "e^{\\alpha t}\\cos\\omega t, \\qquad "
                "\\frac{\\omega}{(s-\\alpha)^2 + \\omega^2} \\to "
                "e^{\\alpha t}\\sin\\omega t.$$\n"
                "So the numerator must be expressed as "
                "$B(s - \\alpha) + C\\omega$ form to read off the cosine and sine "
                "amplitudes.\n\n"
                "THE RECIPE. Given "
                "$\\dfrac{\\text{linear in }s}{(s-\\alpha)^2 + \\omega^2}$: (1) "
                "complete the square to find $\\alpha, \\omega$; (2) write the "
                "numerator as $B\\,(s - \\alpha) + \\text{(constant)}$; (3) the "
                "$B(s-\\alpha)$ piece inverts to $B e^{\\alpha t}\\cos\\omega t$; (4) "
                "the leftover constant, call it $D$, gives "
                "$\\dfrac{D}{\\omega}e^{\\alpha t}\\sin\\omega t$ (the $1/\\omega$ fix "
                "again). The result is a decaying (if $\\alpha < 0$) or growing "
                "oscillation - exactly the complex-root behavior of U2/U3, now read "
                "off a completed square. The single most common mistake is "
                "forgetting to re-center the numerator on $(s - \\alpha)$ before "
                "splitting; do that first and the rest is table lookup."
            )),
            # ---------------------------------------------------------- 9
            (E, "Harder worked examples: completing the square", (
                "EXAMPLE 1 (the full completing-the-square routine). Invert "
                "$Y(s) = \\dfrac{2s + 5}{s^2 + 4s + 13}$.\n\n"
                "STEP 1: $s^2 + 4s + 13 = (s+2)^2 + 9$, so $\\alpha = -2, \\omega = "
                "3$. STEP 2 - re-center the numerator on $(s + 2)$: write "
                "$2s + 5 = 2(s + 2) + (5 - 4) = 2(s + 2) + 1$. So\n"
                "$$Y = \\frac{2(s+2)}{(s+2)^2 + 9} + \\frac{1}{(s+2)^2 + 9}.$$\n"
                "STEP 3: the first term is $2e^{-2t}\\cos 3t$. STEP 4: the second is "
                "$\\dfrac{1}{(s+2)^2 + 9}$; with $\\omega = 3$ the $1/\\omega$ fix "
                "gives $\\tfrac{1}{3}e^{-2t}\\sin 3t$. Therefore\n"
                "$$y(t) = 2e^{-2t}\\cos 3t + \\tfrac{1}{3}e^{-2t}\\sin 3t.$$\n"
                "A decaying oscillation - the damped case, with $\\alpha = -2 < 0$.\n\n"
                "EXAMPLE 2 (linear times irreducible quadratic - full IVP inverse). "
                "Invert $Y(s) = \\dfrac{1}{(s-1)(s^2 + 4)}$. Decompose: "
                "$\\dfrac{1}{(s-1)(s^2+4)} = \\dfrac{A}{s-1} + "
                "\\dfrac{Bs + C}{s^2 + 4}$. Cover-up for $A$: set $s = 1$ in "
                "$\\dfrac{1}{s^2+4}$, giving $A = \\tfrac{1}{5}$. Clear denominators: "
                "$1 = A(s^2 + 4) + (Bs + C)(s - 1)$. Match $s^2$: $A + B = 0 "
                "\\Rightarrow B = -\\tfrac{1}{5}$. Match $s^0$: $4A - C = 1 "
                "\\Rightarrow C = 4A - 1 = -\\tfrac{1}{5}$. So "
                "$Y = \\dfrac{1/5}{s-1} + \\dfrac{-\\tfrac15 s - \\tfrac15}{s^2 + 4}$. "
                "Invert: $\\tfrac{1}{5}e^{t}$, then $-\\tfrac{1}{5}\\cos 2t$ (from the "
                "$s$ term), then the constant $-\\tfrac15$ over $s^2+4$ gives "
                "$-\\tfrac15\\cdot\\tfrac12\\sin 2t = -\\tfrac{1}{10}\\sin 2t$. Total:\n"
                "$$y(t) = \\tfrac{1}{5}e^{t} - \\tfrac{1}{5}\\cos 2t - "
                "\\tfrac{1}{10}\\sin 2t.$$"
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: improper fractions, repeated quadratics, resonance", (
                "Three edges round out the inversion toolkit.\n\n"
                "IMPROPER RATIONAL FUNCTIONS. Partial fractions require "
                "$\\deg P < \\deg Q$ (numerator degree strictly below denominator). "
                "If $Y(s)$ has $\\deg P \\ge \\deg Q$, POLYNOMIAL-DIVIDE first: "
                "$Y = (\\text{polynomial in } s) + (\\text{proper remainder})$. But a "
                "polynomial in $s$ has no table inverse among ordinary functions - a "
                "constant inverts to a Dirac DELTA $\\delta(t)$, and higher powers to "
                "its derivatives (ODE.U4.N3 territory). For the IVPs of this unit, "
                "$Y(s)$ is always proper, so this rarely bites; but recognizing an "
                "improper $Y$ prevents a nonsensical partial-fraction attempt.\n\n"
                "REPEATED IRREDUCIBLE QUADRATICS. A factor "
                "$\\big((s-\\alpha)^2 + \\omega^2\\big)^2$ needs numerators over BOTH "
                "the first and second powers, and it inverts to terms including "
                "$t\\sin\\omega t$ and $t\\cos\\omega t$ - the $t$-GROWTH of "
                "RESONANCE (U3.N2), now appearing as a repeated pole pair on the "
                "imaginary axis. So the frequency-domain fingerprint of resonance is "
                "a REPEATED quadratic factor, precisely mirroring the repeated-root "
                "modification of undetermined coefficients. Seeing "
                "$1/(s^2 + \\omega^2)^2 \\to \\tfrac{1}{2\\omega^3}(\\sin\\omega t - "
                "\\omega t\\cos\\omega t)$ makes the connection concrete.\n\n"
                "COMPLEX POLES = OSCILLATION; SIGN OF $\\alpha$ = FATE. After "
                "completing the square, $\\alpha < 0$ gives a DECAYING oscillation "
                "(stable, the transient of U3.N4), $\\alpha = 0$ a PURE sustained "
                "oscillation (undamped), and $\\alpha > 0$ a GROWING one (unstable). "
                "You can predict the qualitative behavior of $y(t)$ - decay, sustain, "
                "or blow up - just from where the poles sit relative to the imaginary "
                "axis, WITHOUT finishing the inversion. This pole-location reading is "
                "the bridge to stability theory: 'all poles in the left half-plane' is "
                "the Laplace-domain statement of 'the system settles.'"
            )),
            # ---------------------------------------------------------- 11
            (E, "Edge-case worked example: repeated factor plus quadratic", (
                "Invert a $Y(s)$ combining a repeated linear factor and an irreducible "
                "quadratic - the kind a real IVP produces. Take "
                "$Y(s) = \\dfrac{s + 1}{s^2(s^2 + 1)}$ (note $s^2$ is a repeated "
                "linear factor at $s = 0$, and $s^2 + 1$ is irreducible with "
                "$\\omega = 1$).\n\n"
                "Decompose with ALL required pieces: "
                "$$\\frac{s+1}{s^2(s^2+1)} = \\frac{A}{s} + \\frac{B}{s^2} "
                "+ \\frac{Cs + D}{s^2 + 1}.$$\n"
                "Clear denominators: "
                "$s + 1 = A\\,s(s^2+1) + B(s^2+1) + (Cs + D)s^2$. Expand: "
                "$A s^3 + A s + B s^2 + B + C s^3 + D s^2 = (A + C)s^3 + (B + D)s^2 "
                "+ A s + B$. Match against $s + 1$ (i.e. $0\\cdot s^3 + 0\\cdot s^2 "
                "+ 1\\cdot s + 1$):\n"
                "$s^0:\\; B = 1$. $s^1:\\; A = 1$. $s^2:\\; B + D = 0 \\Rightarrow "
                "D = -1$. $s^3:\\; A + C = 0 \\Rightarrow C = -1$.\n"
                "So $Y = \\dfrac{1}{s} + \\dfrac{1}{s^2} + \\dfrac{-s - 1}{s^2 + 1}$. "
                "Invert term by term: $\\dfrac{1}{s} \\to 1$; "
                "$\\dfrac{1}{s^2} \\to t$; $\\dfrac{-s}{s^2+1} \\to -\\cos t$; "
                "$\\dfrac{-1}{s^2+1} \\to -\\sin t$ (here $\\omega = 1$, so the "
                "$1/\\omega$ factor is just $1$). Total:\n"
                "$$y(t) = 1 + t - \\cos t - \\sin t.$$\n"
                "VERIFY at a checkpoint - transform back the two easy pieces: "
                "$\\mathcal{L}\\{1 + t\\} = \\tfrac1s + \\tfrac1{s^2}$, and "
                "$\\mathcal{L}\\{-\\cos t - \\sin t\\} = "
                "\\tfrac{-s}{s^2+1} + \\tfrac{-1}{s^2+1}$ - summing reproduces $Y$. "
                "The example shows the full discipline: every power of the repeated "
                "factor, a linear numerator over the quadratic, and the $1/\\omega$ "
                "check on the sine."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'Forget the $1/\\omega$ on sine terms.' "
                "$\\mathcal{L}^{-1}\\{1/(s^2+\\omega^2)\\} = "
                "\\tfrac{1}{\\omega}\\sin\\omega t$, not $\\sin\\omega t$. For "
                "$1/(s^2 + 9)$ it is $\\tfrac{1}{3}\\sin 3t$. The bare-constant "
                "numerator ALWAYS picks up $1/\\omega$; the $s$-numerator (cosine) "
                "does not.\n\n"
                "MISCONCEPTION 2: 'A repeated factor needs only its highest power.' "
                "$(s-a)^2$ requires BOTH $A/(s-a)$ AND $B/(s-a)^2$. Dropping the "
                "lower power makes the decomposition unsolvable - the same "
                "full-list rule as U3.N1's polynomials.\n\n"
                "MISCONCEPTION 3: 'Use a constant numerator over an irreducible "
                "quadratic.' It needs a LINEAR numerator $Bs + C$, because the "
                "quadratic contributes both a cosine (from $s$) and a sine (from the "
                "constant). A lone constant misses the cosine.\n\n"
                "MISCONCEPTION 4: 'Cover-up works on everything.' It gives distinct-"
                "linear coefficients and the top power of a repeated factor, but NOT "
                "the lower repeated coefficients or (directly) quadratic numerators. "
                "Use matching there.\n\n"
                "MISCONCEPTION 5: 'Complete the square only sometimes.' Any quadratic "
                "denominator with a middle ($s$) term MUST be completed before you can "
                "read off $\\alpha$ and $\\omega$; skipping it and pretending it is "
                "$s^2 + \\omega^2$ gives the wrong frequency and misses the "
                "$e^{\\alpha t}$ decay.\n\n"
                "MISCONCEPTION 6: 'The inverse might not be unique, so any answer is "
                "fine.' For the continuous functions ODEs produce, the inverse IS "
                "unique (Lerch's theorem, N1). A wrong-looking answer is wrong, not an "
                "alternate valid form - verify by transforming back."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational and procedural errors", (
                "ERROR 1: Missing the $1/\\omega$. The number-one inversion mistake. "
                "Write the sine entry as $\\dfrac{\\omega}{s^2+\\omega^2}$ every time, "
                "so a bare $\\dfrac{1}{s^2+\\omega^2}$ visibly needs a compensating "
                "$1/\\omega$.\n\n"
                "ERROR 2: Wrong $\\omega$ from an un-completed square. For "
                "$1/(s^2 + 4s + 13)$, $\\omega$ is $3$ (from $(s+2)^2 + 9$), NOT "
                "$\\sqrt{13}$. Complete the square before reading $\\omega$.\n\n"
                "ERROR 3: Forgetting to re-center the numerator on $(s - \\alpha)$. "
                "After completing the square you must rewrite the numerator in terms "
                "of $(s + 2)$ (or whatever the shift is) BEFORE splitting into "
                "cosine/sine; splitting the original numerator gives wrong "
                "amplitudes.\n\n"
                "ERROR 4: Incomplete repeated-factor expansion. $(s-a)^3$ needs "
                "three terms $A/(s-a) + B/(s-a)^2 + C/(s-a)^3$; a missing power breaks "
                "the algebra.\n\n"
                "ERROR 5: Constant numerator over a quadratic. Always use $Bs + C$ "
                "over an irreducible quadratic; a bare constant cannot represent the "
                "general term.\n\n"
                "ERROR 6: Sign and arithmetic slips in the coefficients. After "
                "decomposing, VERIFY by plugging one convenient $s$ value (not a root) "
                "into both sides, or transform your $y(t)$ back and confirm you "
                "recover $Y(s)$. This catches nearly every slip. ERROR 7: Attempting "
                "partial fractions on an IMPROPER $Y$ ($\\deg P \\ge \\deg Q$) without "
                "dividing first - the decomposition is invalid until the fraction is "
                "proper."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: closing the Laplace loop", (
                "Inversion is the step that makes the Laplace transform USEFUL rather "
                "than merely elegant - it is where the answer to a real problem "
                "finally appears.\n\n"
                "1. SOLVING IVPs (the immediate application). N1 transformed an ODE "
                "into an algebraic $Y(s)$; this node converts $Y(s)$ back to $y(t)$, "
                "the actual solution. The full method - transform, solve algebraically, "
                "invert by partial fractions - is what N3 packages, and it is the "
                "standard engineering way to solve linear ODEs, especially with "
                "discontinuous or impulsive forcing where U3's methods stall.\n\n"
                "2. READING SYSTEM BEHAVIOR FROM POLES. Because the poles of $Y(s)$ "
                "(roots of the denominator) determine the exponential and oscillatory "
                "content of $y(t)$, engineers often SKIP the full inversion and read "
                "qualitative behavior straight off the pole locations: left-half-plane "
                "poles decay (stable), imaginary poles sustain, right-half-plane poles "
                "grow (unstable); pole imaginary parts set oscillation frequencies, "
                "real parts set decay rates. Partial fractions makes this rigorous, "
                "but the pole picture is the daily intuition of control design.\n\n"
                "3. TRANSFER FUNCTIONS AND RESPONSE. The transfer function "
                "$H(s) = Y(s)/G(s)$ is a rational function whose partial-fraction "
                "inverse is the system's IMPULSE RESPONSE $h(t)$. Every technique in "
                "this node - distinct poles to exponentials, repeated poles to "
                "$t$-growth, quadratic poles to damped sinusoids - is how one turns a "
                "transfer function into a time-domain response.\n\n"
                "4. THE PAYOFF. Whether you want the exact $y(t)$, the stability "
                "verdict, or the impulse response, the operation is the same: "
                "decompose the rational $Y(s)$ or $H(s)$ into table pieces and read "
                "them back. Inversion is the door between the algebra of the "
                "$s$-domain and the physics of the time domain."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: finishing an IVP by inversion", (
                "Complete the IVP begun in N1's Example 15: "
                "$y'' + 3y' + 2y = e^{-t}$, $y(0) = 1$, $y'(0) = 0$, which transformed "
                "to\n"
                "$$Y(s) = \\frac{1}{(s+1)(s^2 + 3s + 2)} + \\frac{s + 3}{s^2 + 3s + "
                "2}.$$\n"
                "Factor $s^2 + 3s + 2 = (s+1)(s+2)$. The first fraction becomes "
                "$\\dfrac{1}{(s+1)^2(s+2)}$ (a REPEATED factor at $-1$), the second "
                "$\\dfrac{s+3}{(s+1)(s+2)}$. Combine over the common denominator "
                "$(s+1)^2(s+2)$: the second is "
                "$\\dfrac{(s+3)(s+1)}{(s+1)^2(s+2)}$, so\n"
                "$$Y(s) = \\frac{1 + (s+3)(s+1)}{(s+1)^2(s+2)} "
                "= \\frac{s^2 + 4s + 4}{(s+1)^2(s+2)} "
                "= \\frac{(s+2)^2}{(s+1)^2(s+2)} = \\frac{s+2}{(s+1)^2}.$$\n"
                "A lucky cancellation left $Y(s) = \\dfrac{s+2}{(s+1)^2}$. Decompose "
                "the repeated factor: $\\dfrac{s+2}{(s+1)^2} = \\dfrac{A}{s+1} "
                "+ \\dfrac{B}{(s+1)^2}$, so $s + 2 = A(s+1) + B$. At $s = -1$: "
                "$B = 1$. Compare $s$: $A = 1$. Thus "
                "$Y = \\dfrac{1}{s+1} + \\dfrac{1}{(s+1)^2}$, and with "
                "$\\mathcal{L}^{-1}\\{1/(s+1)^2\\} = te^{-t}$,\n"
                "$$y(t) = e^{-t} + t e^{-t} = (1 + t)e^{-t}.$$\n"
                "VERIFY the data: $y(0) = 1$; "
                "$y' = -e^{-t} + e^{-t} - te^{-t} = -te^{-t}$, so $y'(0) = 0$. Both "
                "hold. And substituting confirms $y'' + 3y' + 2y = e^{-t}$. The "
                "Laplace loop is closed: N1 sent the IVP into the $s$-domain with its "
                "data baked in, and this node brought back the unique solution - no "
                "separate constant-fitting, unlike the U3.N4 workflow."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: where this node plugs in", (
                "BACKWARD. Inversion reads the N1 table BACKWARD, so it depends "
                "entirely on knowing those transforms cold, including the $s$-shift "
                "forms. Its algebra is partial-fraction decomposition and completing "
                "the square (C202-level technique). The poles it decomposes over are "
                "the characteristic roots of ODE.U2.N5 / U3, reappearing as "
                "denominators.\n\n"
                "SIDEWAYS AND FORWARD. Together with N1, this node makes ODE.U4.N3 "
                "possible: N3 adds the unit step and Dirac delta (whose transforms "
                "produce the $e^{-as}$ shift factors and improper-fraction pieces "
                "flagged on page 10), the second-shift theorem, and the convolution "
                "theorem, then presents the COMPLETE Laplace method for solving IVPs - "
                "the loop of transform, solve, invert that Example 15 walked through. "
                "The repeated-quadratic resonance of page 10 ties directly back to "
                "ODE.U3.N2's $t$-growth, and the pole-location reading connects to the "
                "transient/steady-state split of ODE.U3.N4.\n\n"
                "FURTHER OUT. Partial-fraction inversion by poles is the "
                "computational heart of control theory (impulse and step responses "
                "from transfer functions), signal processing, and circuit analysis. "
                "The same decomposition, done with the $z$-transform, inverts "
                "discrete-time systems; done with residues, it becomes the "
                "complex-analysis Bromwich integral (N1's going-deeper page). Every "
                "time an engineer turns a transfer function $H(s)$ into a time "
                "response, or reads stability off pole locations, they are using the "
                "skills of this node. It is the return half of the single most-used "
                "transform method in applied mathematics."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: poles, residues, and the shape of the answer", (
                "The partial-fraction recipe is really the theory of POLES AND "
                "RESIDUES in disguise, and seeing it that way explains why the answer "
                "takes the shape it does - and lets you predict $y(t)$ almost by "
                "inspection.\n\n"
                "A rational $Y(s) = P(s)/Q(s)$ is completely determined (up to the "
                "polynomial part) by its POLES - the roots of $Q$ - and the RESIDUE at "
                "each pole - essentially the partial-fraction coefficient there. The "
                "cover-up method is exactly residue computation: "
                "$\\text{Res}_{s=a}Y = \\lim_{s\\to a}(s-a)Y(s)$ is the coefficient $A$ "
                "over $(s - a)$. So partial fractions is the statement that a rational "
                "function is the SUM of its residue contributions, one per pole, and "
                "inversion turns each pole into a characteristic time behavior:\n"
                "- a simple REAL pole at $s = a$ contributes $A\\,e^{at}$;\n"
                "- a COMPLEX conjugate pair at $\\alpha \\pm i\\omega$ contributes "
                "$e^{\\alpha t}(\\ldots\\cos\\omega t + \\ldots\\sin\\omega t)$;\n"
                "- a REPEATED pole of order $k$ contributes "
                "$(\\ldots + \\ldots t + \\cdots + \\ldots t^{k-1})e^{at}$.\n\n"
                "This is why the Bromwich contour integral (N1 page 17) equals the "
                "SUM OF RESIDUES of $e^{st}Y(s)$: closing the contour picks up one "
                "residue per pole, and each residue is exactly the time-domain term "
                "partial fractions produces. The two methods - elementary partial "
                "fractions and advanced residue calculus - give the identical answer "
                "because they are the same computation at different levels of "
                "abstraction.\n\n"
                "The payoff for intuition is enormous: you can SKETCH $y(t)$ from the "
                "pole diagram without inverting. Poles far into the left half-plane "
                "give fast-decaying terms; poles near the imaginary axis give "
                "long-lived oscillations; a pole in the right half-plane means "
                "instability. Control engineers design systems by MOVING POLES (via "
                "feedback) to shape this time response. What begins in this node as "
                "'break the fraction into table pieces' matures into the pole-zero "
                "picture that governs the analysis of every linear time-invariant "
                "system."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "GOAL. Recover $y(t) = \\mathcal{L}^{-1}\\{Y(s)\\}$ from a rational "
                "$Y(s)$ by partial fractions + the table (read backward), using "
                "linearity.\n\n"
                "THREE CASES.\n"
                "- DISTINCT LINEAR $(s-a)(s-b)$: "
                "$\\tfrac{A}{s-a} + \\tfrac{B}{s-b} \\to Ae^{at} + Be^{bt}$.\n"
                "- REPEATED LINEAR $(s-a)^k$: all powers "
                "$\\tfrac{A_1}{s-a} + \\cdots + \\tfrac{A_k}{(s-a)^k} \\to "
                "(A_1 + A_2 t + \\cdots)e^{at}$, since "
                "$\\tfrac{1}{(s-a)^j} \\to \\tfrac{t^{j-1}}{(j-1)!}e^{at}$.\n"
                "- IRREDUCIBLE QUADRATIC: linear numerator "
                "$\\tfrac{Bs+C}{(s-\\alpha)^2+\\omega^2}$; the $s$-part gives "
                "$e^{\\alpha t}\\cos\\omega t$, the constant gives "
                "$\\tfrac{(\\cdot)}{\\omega}e^{\\alpha t}\\sin\\omega t$.\n\n"
                "FIX THE $\\omega$. "
                "$\\mathcal{L}^{-1}\\{1/(s^2+\\omega^2)\\} = "
                "\\tfrac{1}{\\omega}\\sin\\omega t$ - insert $1/\\omega$ on bare "
                "constants over a quadratic; cosine ($s$ numerator) needs none.\n\n"
                "COMPLETE THE SQUARE. $s^2 + bs + c = (s + \\tfrac b2)^2 + "
                "(c - \\tfrac{b^2}{4})$; re-center the numerator on "
                "$(s - \\alpha)$ before splitting; $\\alpha = -\\tfrac b2$, "
                "$\\omega = \\sqrt{c - \\tfrac{b^2}{4}}$.\n\n"
                "COVER-UP. Coefficient over $(s-a)$: $[(s-a)Y]_{s=a}$. Works for "
                "distinct linear factors and the top repeated power; matching handles "
                "the rest.\n\n"
                "CHECKS. Keep $\\deg P < \\deg Q$ (divide if not). Verify by "
                "transforming $y(t)$ back, or plug a non-root $s$ into the "
                "decomposition. Poles set the behavior: left-half decay, imaginary "
                "sustain, right-half growth; repeated imaginary poles = resonance "
                "$t$-growth."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all seven before reading solutions.\n\n"
                "1. $\\mathcal{L}^{-1}\\{1/(s^2 + 16)\\}$?\n"
                "2. $\\mathcal{L}^{-1}\\{(3s)/(s^2 + 16)\\}$?\n"
                "3. Invert $\\dfrac{1}{(s-2)(s+3)}$ by cover-up.\n"
                "4. Invert $\\dfrac{1}{(s-4)^2}$.\n"
                "5. Complete the square and invert "
                "$\\dfrac{s + 3}{s^2 + 2s + 5}$.\n"
                "6. What numerator form goes over $s^2 + 9$ in a partial-fraction "
                "decomposition, and why?\n"
                "7. True or false: $\\mathcal{L}^{-1}\\{1/(s^2+25)\\} = \\sin 5t$.\n\n"
                "SOLUTIONS.\n"
                "1. $\\omega = 4$: $\\tfrac{1}{4}\\sin 4t$ (the $1/\\omega$).\n"
                "2. $s$ numerator = cosine, no fix: $3\\cos 4t$.\n"
                "3. Cover-up: $A = \\tfrac{1}{2+3} = \\tfrac15$ at $s=2$; "
                "$B = \\tfrac{1}{-3-2} = -\\tfrac15$ at $s=-3$. "
                "$y = \\tfrac15 e^{2t} - \\tfrac15 e^{-3t}$.\n"
                "4. $\\tfrac{1}{(s-4)^2} \\to te^{4t}$.\n"
                "5. $s^2 + 2s + 5 = (s+1)^2 + 4$, $\\alpha = -1, \\omega = 2$. "
                "Re-center: $s + 3 = (s+1) + 2$. So "
                "$\\tfrac{s+1}{(s+1)^2+4} + \\tfrac{2}{(s+1)^2+4} \\to "
                "e^{-t}\\cos 2t + \\tfrac{2}{2}e^{-t}\\sin 2t = e^{-t}\\cos 2t + "
                "e^{-t}\\sin 2t$.\n"
                "6. A LINEAR numerator $Bs + C$, because an irreducible quadratic "
                "contributes both a cosine (from $s$) and a sine (from the "
                "constant).\n"
                "7. FALSE - $\\omega = 5$, so it is $\\tfrac{1}{5}\\sin 5t$ (missing "
                "the $1/\\omega$).\n\n"
                "Scoring: 7/7 - ready for the full method (N3). 5-6 - re-drill the "
                "$1/\\omega$ fix and completing the square (pages 4, 8). $\\le 4$ - "
                "rework pages 3-9."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Check every box honestly:\n\n"
                "- Invert the elementary table entries by inspection, including the "
                "shifted forms.\n"
                "- Decompose a rational $Y(s)$ by partial fractions, choosing the "
                "correct term structure for distinct linear, repeated linear, and "
                "irreducible quadratic factors.\n"
                "- Insert the $1/\\omega$ factor on bare-constant-over-quadratic "
                "(sine) terms, and recognize that $s$-over-quadratic (cosine) needs "
                "none.\n"
                "- Complete the square on a quadratic with a middle term and re-center "
                "the numerator on $(s - \\alpha)$ before splitting.\n"
                "- Invert a completed-square form to a decaying/growing sinusoid and "
                "read the sign of $\\alpha$ as decay vs growth.\n"
                "- Use the cover-up method for distinct linear coefficients (and the "
                "top repeated power), and matching for the rest.\n"
                "- Handle a mixed $Y(s)$ with both a repeated factor and a quadratic, "
                "writing every required term.\n"
                "- Check for and fix an improper fraction ($\\deg P \\ge \\deg Q$) "
                "before decomposing.\n"
                "- Verify an inverse by transforming back or by a non-root "
                "substitution.\n"
                "- Explain the pole picture: poles set the exponentials and "
                "frequencies, real parts set decay/growth, repeated imaginary poles "
                "signal resonance.\n\n"
                "When every box is checked, you can close the Laplace loop on any "
                "constant-coefficient IVP. Proceed to ODE.U4.N3, where step functions, "
                "impulses, and convolution complete the transform method."
            )),
        ],
    },
}
