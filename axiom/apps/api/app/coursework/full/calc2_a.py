"""Full courses: C201 — Substitution, C202 — Integration by parts,
C203 — Trigonometric integrals, C204 — Trigonometric substitution.

FC-template lessons: 20 full pages each, matching the gold standard set by
calc1_a_limits.py (C101) in depth, structure, and voice.
"""

L = "reading"
E = "example"
P = "pitfall"
K = "check"

LESSONS: dict[str, dict] = {
    "C201": {
        "summary": (
            "A complete course on u-substitution: reversing the chain rule, "
            "recognizing inner functions and their derivatives, the full "
            "indefinite and definite mechanics (change the bounds or "
            "back-substitute), adjusting constants legally, substitution with "
            "trig, exponential, and logarithmic integrands, and reading the "
            "signal when substitution fails."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "Welcome to Calculus 2, and to a genuine change of terrain. In C118 the "
                "Fundamental Theorem handed you a magnificent bargain: to evaluate "
                "$\\int_a^b f(x)\\,dx$, you need only find an antiderivative and subtract "
                "two of its values. The entire problem of areas, totals, and accumulations "
                "reduces to one question — CAN YOU ANTIDIFFERENTIATE?\n\n"
                "Here is the uncomfortable truth that shapes this whole unit: "
                "differentiation is a MECHANICAL skill, but antidifferentiation is a "
                "RECOGNITION skill. Given any elementary function, the rules of C105-C108 "
                "grind out its derivative with no creativity required. Run the machine "
                "backwards and everything changes: there is no algorithm you can execute "
                "blindly. Instead there is a small kit of techniques, each of which "
                "reverses one differentiation rule, and a trained eye that recognizes "
                "which technique a given integrand is asking for.\n\n"
                "Substitution is the first, most used, and most important tool in that "
                "kit. It reverses the CHAIN RULE — the rule responsible for most of the "
                "complexity in the derivatives you have computed. Since the chain rule "
                "fires whenever functions are composed, and composition is everywhere, "
                "substitution unlocks more integrals than every other technique "
                "combined. Master it and perhaps half of all textbook integrals fall "
                "immediately; the other techniques (parts in C202, trig methods in "
                "C203-C204, partial fractions in C205) handle the rest.\n\n"
                "In this chapter you will learn: how to SEE the chain rule's fingerprint "
                "inside an integrand, the clean $u$-$du$ bookkeeping that converts a "
                "hard integral into an easy one, both correct ways to handle definite "
                "integrals, exactly which constant-adjustments are legal, and — just as "
                "valuable — how to recognize when substitution CANNOT work and what "
                "that failure is telling you."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Substitution is the chain rule read right-to-left, so you need the "
                "chain rule cold, plus the basic antiderivative table.\n\n"
                "1. THE CHAIN RULE (C108). You must be able to differentiate composites "
                "instantly and — more important here — to look at a derivative and see "
                "its anatomy: in $\\frac{d}{dx}\\sin(x^2) = \\cos(x^2)\\cdot 2x$, the "
                "$\\cos(x^2)$ is the outer derivative evaluated at the inner function, "
                "and the $2x$ is the inner derivative tagging along.\n\n"
                "2. THE ANTIDERIVATIVE TABLE (C116). Fluency with "
                "$\\int x^n\\,dx = \\frac{x^{n+1}}{n+1} + C$ ($n \\ne -1$), "
                "$\\int \\frac{dx}{x} = \\ln|x| + C$, $\\int e^x\\,dx = e^x + C$, "
                "$\\int \\cos x\\,dx = \\sin x + C$, $\\int \\sin x\\,dx = -\\cos x + C$, "
                "$\\int \\sec^2 x\\,dx = \\tan x + C$, and "
                "$\\int \\frac{dx}{1+x^2} = \\arctan x + C$. Substitution's whole job is "
                "to convert a strange integral into one of these.\n\n"
                "3. FTC MECHANICS (C118). Evaluating "
                "$\\int_a^b f = F(b) - F(a)$ with correct bound handling.\n\n"
                "Quick self-check (answers follow each item):\n\n"
                "• $\\frac{d}{dx} e^{x^3}$? ($3x^2 e^{x^3}$ — outer derivative times "
                "inner derivative.)\n"
                "• $\\int \\sec^2 x\\,dx$? ($\\tan x + C$.)\n"
                "• $\\int_0^1 e^x\\,dx$? ($e^1 - e^0 = e - 1$.)\n"
                "• In $\\frac{d}{dx}(x^2+1)^{10} = 10(x^2+1)^9 \\cdot 2x$, which factor "
                "is the inner derivative? (The $2x$.)\n\n"
                "If the chain-rule anatomy questions felt slow, spend twenty minutes "
                "re-differentiating composites from C108 before continuing — this "
                "chapter is that skill, mirrored."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Differentiate $F(g(x))$ and the chain rule produces\n"
                "$$\\frac{d}{dx} F(g(x)) = F'(g(x))\\,g'(x).$$\n"
                "Read that equation BACKWARDS and you have the entire idea of this "
                "chapter: any integrand shaped like 'a function OF an inner function, "
                "TIMES the derivative of that inner function' is secretly the "
                "derivative of a composite, and its antiderivative is that "
                "composite.\n\n"
                "Look at $\\int 2x \\cos(x^2)\\,dx$. A beginner sees a product of "
                "unrelated pieces. A trained eye sees the chain rule's FINGERPRINT: "
                "there is an inner function $x^2$ sitting inside the cosine, and its "
                "derivative $2x$ is riding along as a factor. So the integrand must be "
                "what you get by differentiating $\\sin(x^2)$ — and indeed "
                "$\\int 2x\\cos(x^2)\\,dx = \\sin(x^2) + C$. Differentiate to confirm; "
                "it works.\n\n"
                "Substitution is the BOOKKEEPING SYSTEM that makes this recognition "
                "systematic instead of clairvoyant. We give the inner function a name, "
                "$u = x^2$, and write $du = 2x\\,dx$ for its differential. The "
                "integral then RELABELS itself:\n"
                "$$\\int \\underbrace{\\cos(x^2)}_{\\cos u}\\,"
                "\\underbrace{2x\\,dx}_{du} = \\int \\cos u\\,du = \\sin u + C "
                "= \\sin(x^2) + C.$$\n"
                "Nothing deep happened — we renamed the moving parts so that a "
                "composite-plus-derivative pattern in $x$ became a TABLE INTEGRAL in "
                "$u$. That is the whole game: hunt for an inner function whose "
                "derivative is present as a factor, rename, integrate the easy thing, "
                "rename back.\n\n"
                "Two phrases to carry through the chapter: the INNER FUNCTION is the "
                "candidate for $u$; its derivative 'riding along' as a factor is the "
                "license that makes the substitution legal. When the license is "
                "missing, substitution stalls — pages 10-11 study exactly that."
            )),
            # ---------------------------------------------------------- 4
            (L, "The method, stated precisely", (
                "THE SUBSTITUTION RULE. If $u = g(x)$ is differentiable with a "
                "continuous derivative, and $f$ is continuous on the range of $g$, "
                "then\n"
                "$$\\int f(g(x))\\,g'(x)\\,dx = \\int f(u)\\,du.$$\n"
                "Meaning: if $F$ is an antiderivative of $f$, both sides equal "
                "$F(g(x)) + C$. The proof is one line of chain rule: "
                "$\\frac{d}{dx} F(g(x)) = F'(g(x))g'(x) = f(g(x))g'(x)$, which says "
                "$F(g(x))$ is an antiderivative of the left integrand. ∎\n\n"
                "The notation $du = g'(x)\\,dx$ deserves a comment. At this stage it "
                "is best treated as reliable SHORTHAND: 'replace $g'(x)\\,dx$ by "
                "$du$.' The theorem above is what justifies the shorthand; the "
                "differentials themselves get independent meaning later (C304 and "
                "REALAN). Used as bookkeeping, the notation never lies — that is why "
                "Leibniz designed it this way.\n\n"
                "THE MECHANICS, as a five-step checklist you should follow "
                "explicitly for your first fifty substitutions:\n\n"
                "1. CHOOSE $u$: an inner function whose derivative appears as a "
                "factor (up to a constant).\n"
                "2. COMPUTE $du = u'\\,dx$, and solve for the piece of the integrand "
                "you need (e.g. $x\\,dx = \\frac{du}{2}$).\n"
                "3. REWRITE the ENTIRE integral in $u$ — no $x$ may survive. If an "
                "$x$ cannot be eliminated, the substitution fails; pick another $u$ "
                "or another technique.\n"
                "4. INTEGRATE in $u$ using the table.\n"
                "5. SUBSTITUTE BACK: replace $u$ by $g(x)$, and append $+C$.\n\n"
                "Then the sixth, unofficial step that separates strong students: "
                "DIFFERENTIATE YOUR ANSWER. The check takes ten seconds, uses the "
                "chain rule you already trust, and catches essentially every error "
                "this chapter's pitfall pages will catalog."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples", (
                "EXAMPLE 1 (the fingerprint in plain sight). "
                "$\\int 3x^2 (x^3 + 5)^9\\,dx$.\n\n"
                "Inner function: $u = x^3 + 5$ (it sits inside the 9th power). Its "
                "derivative $3x^2$ is present as a factor — license granted. Then "
                "$du = 3x^2\\,dx$, and the integral relabels completely:\n"
                "$$\\int (x^3+5)^9 \\cdot 3x^2\\,dx = \\int u^9\\,du "
                "= \\frac{u^{10}}{10} + C = \\frac{(x^3+5)^{10}}{10} + C.$$\n"
                "Check: differentiating gives $10\\cdot\\frac{(x^3+5)^9}{10}\\cdot "
                "3x^2 = 3x^2(x^3+5)^9$. Correct.\n\n"
                "EXAMPLE 2 (adjusting a constant). $\\int x\\sqrt{x^2+1}\\,dx$.\n\n"
                "Inner function $u = x^2 + 1$, so $du = 2x\\,dx$. The integrand has "
                "$x\\,dx$, not $2x\\,dx$ — off by the constant 2. Solve: "
                "$x\\,dx = \\frac{1}{2}\\,du$. Then\n"
                "$$\\int \\sqrt{x^2+1}\\;x\\,dx = \\frac{1}{2}\\int \\sqrt{u}\\,du "
                "= \\frac{1}{2}\\cdot\\frac{2}{3}u^{3/2} + C "
                "= \\frac{1}{3}(x^2+1)^{3/2} + C.$$\n"
                "Missing CONSTANTS are repairable, because constants slide through "
                "integral signs. Page 6 states exactly how far this repair "
                "extends (spoiler: constants and nothing else).\n\n"
                "EXAMPLE 3 (linear inside — the most common case of all). "
                "$\\int e^{5x}\\,dx$.\n\n"
                "Let $u = 5x$, $du = 5\\,dx$, so $dx = \\frac{du}{5}$:\n"
                "$$\\int e^{5x}\\,dx = \\frac{1}{5}\\int e^u\\,du "
                "= \\frac{1}{5}e^{5x} + C.$$\n"
                "Every linear substitution $u = ax + b$ works this way and produces "
                "the shortcut worth memorizing: $\\int f(ax+b)\\,dx = "
                "\\frac{1}{a}F(ax+b) + C$. Integrate as usual, divide by the inner "
                "slope. You will use this reflex dozens of times per week."
            )),
            # ---------------------------------------------------------- 6
            (L, "The full method: constants, and definite integrals two ways", (
                "WHAT MAY BE ADJUSTED. If $du$ misses the integrand by a "
                "multiplicative CONSTANT, fix it by solving $du = c\\,dx$ for the "
                "needed piece, as in Example 2. What you may NOT do is adjust by "
                "anything containing $x$: from $\\int \\cos(x^2)\\,dx$ you cannot "
                "write $dx = \\frac{du}{2x}$ and then 'pull $\\frac{1}{2x}$ out' — "
                "only CONSTANTS pass through an integral sign. An $x$ left in a "
                "$u$-integral is not a technicality; it is the method telling you "
                "this integrand does not have the chain-rule shape (page 10 pursues "
                "this).\n\n"
                "DEFINITE INTEGRALS — TWO CORRECT METHODS.\n\n"
                "METHOD A (change the bounds — usually cleaner). The substitution "
                "rule for definite integrals states:\n"
                "$$\\int_a^b f(g(x))\\,g'(x)\\,dx = \\int_{g(a)}^{g(b)} f(u)\\,du.$$\n"
                "When you substitute, convert the $x$-bounds to $u$-bounds via "
                "$u = g(x)$, then finish entirely in $u$-land — no return trip. Note "
                "the theorem does NOT require $g(a) < g(b)$; if $g$ is decreasing "
                "the new bounds arrive reversed, and you leave them reversed — the "
                "orientation is part of the answer.\n\n"
                "METHOD B (back-substitute). Compute the indefinite integral, "
                "return to $x$, then apply the ORIGINAL bounds. Both methods always "
                "agree; Method B costs one extra conversion but keeps everything in "
                "familiar variables.\n\n"
                "THE ONE FATAL MIX: computing the $u$-antiderivative and then "
                "plugging the $x$-bounds into it. That evaluates the wrong function "
                "at the right numbers. Protect yourself with a hard rule: THE "
                "MOMENT YOU WRITE $u$, THE OLD BOUNDS ARE INVALID. Either convert "
                "them (Method A) or come back to $x$ before evaluating (Method B). "
                "Page 7's Example 3 runs one integral through both methods so you "
                "can watch them agree."
            )),
            # ---------------------------------------------------------- 7
            (E, "Worked examples: standard problems", (
                "EXAMPLE 1 (trig integrand). $\\int \\tan x\\,dx$.\n\n"
                "Write $\\tan x = \\frac{\\sin x}{\\cos x}$ and spot the pattern: "
                "inner function $u = \\cos x$ in the denominator, derivative "
                "$-\\sin x$ present up to sign. With $du = -\\sin x\\,dx$:\n"
                "$$\\int \\frac{\\sin x}{\\cos x}\\,dx = -\\int \\frac{du}{u} "
                "= -\\ln|u| + C = -\\ln|\\cos x| + C = \\ln|\\sec x| + C.$$\n"
                "A table entry is born — this is how the 'known' integral of "
                "tangent is actually derived, and C203 will lean on it.\n\n"
                "EXAMPLE 2 (log integrand). $\\int \\frac{(\\ln x)^2}{x}\\,dx$.\n\n"
                "Inner function $u = \\ln x$; its derivative $\\frac{1}{x}$ is "
                "right there as the $\\frac{1}{x}\\,dx$ piece. So\n"
                "$$\\int (\\ln x)^2 \\cdot \\frac{dx}{x} = \\int u^2\\,du "
                "= \\frac{u^3}{3} + C = \\frac{(\\ln x)^3}{3} + C.$$\n"
                "Whenever $\\frac{1}{x}$ multiplies a function OF $\\ln x$, this "
                "move fires.\n\n"
                "EXAMPLE 3 (a definite integral, both methods). "
                "$\\int_0^2 x e^{x^2}\\,dx$, with $u = x^2$, $du = 2x\\,dx$, so "
                "$x\\,dx = \\frac{du}{2}$.\n\n"
                "METHOD A (change bounds): $x = 0 \\Rightarrow u = 0$; "
                "$x = 2 \\Rightarrow u = 4$. Then\n"
                "$$\\int_0^2 x e^{x^2}dx = \\frac{1}{2}\\int_0^4 e^u\\,du "
                "= \\frac{1}{2}(e^4 - 1).$$\n"
                "METHOD B (back-substitute): indefinite first, "
                "$\\frac{1}{2}\\int e^u du = \\frac{1}{2}e^{x^2} + C$; now apply the "
                "ORIGINAL bounds: $\\frac{1}{2}e^{x^2}\\big|_0^2 = "
                "\\frac{1}{2}(e^4 - e^0) = \\frac{1}{2}(e^4 - 1)$.\n\n"
                "Identical answers, as promised. What would NOT have worked: "
                "evaluating $\\frac{1}{2}e^u$ at $u = 0$ and $u = 2$ — that computes "
                "$\\frac{1}{2}(e^2-1)$, wrong function meets right numbers. Choose a "
                "method per problem and execute it whole."
            )),
            # ---------------------------------------------------------- 8
            (L, "Strategy: how to find u", (
                "Choosing $u$ is the entire skill, and it is learnable. Scan the "
                "integrand for these structures, in roughly this order:\n\n"
                "1. INSIDE A COMPOSITE. Anything wrapped by another function — "
                "$(\\;)^n$, $e^{(\\;)}$, $\\sin(\\;)$, $\\ln(\\;)$, "
                "$\\sqrt{(\\;)}$ — is a $u$-candidate. In $e^{x^3}x^2\\,dx$, try "
                "$u = x^3$.\n\n"
                "2. UNDER A RADICAL. $u = $ the radicand: for "
                "$x\\sqrt{1 - x^2}$, try $u = 1 - x^2$.\n\n"
                "3. IN A DENOMINATOR. $u = $ the denominator: for "
                "$\\frac{x^2}{x^3+1}$, try $u = x^3 + 1$ (this is where "
                "$\\ln|u|$ answers come from).\n\n"
                "4. IN AN EXPONENT. $u = $ the exponent: for $x e^{-x^2}$, "
                "$u = -x^2$.\n\n"
                "5. A FUNCTION WHOSE DERIVATIVE YOU SEE ELSEWHERE. The pairs to "
                "know on sight: $\\ln x$ with $\\frac1x$; $\\sin x$ with "
                "$\\cos x$; $\\tan x$ with $\\sec^2 x$; $\\sec x$ with "
                "$\\sec x\\tan x$; $\\arctan x$ with $\\frac{1}{1+x^2}$; $e^x$ "
                "with itself.\n\n"
                "Then run the LICENSE CHECK: differentiate your candidate. If "
                "$u'$ appears as a factor up to a CONSTANT, proceed. If not, "
                "abandon the candidate fast — good integrators are fast "
                "quitters. Try the other structure in the integrand, or "
                "conclude that this problem belongs to a later technique.\n\n"
                "Two habits complete the strategy. TRY THE SIMPLE $u$ FIRST: "
                "prefer $u = x^2 + 1$ over $u = \\sqrt{x^2+1}$; the simpler "
                "inner function usually has the derivative that is actually "
                "present. And ALWAYS DIFFERENTIATE THE ANSWER: substitution "
                "errors are almost always visible in a ten-second check, and "
                "the check itself trains the pattern recognition this page is "
                "teaching. Speed comes from failing fast and verifying "
                "cheaply, not from staring."
            )),
            # ---------------------------------------------------------- 9
            (E, "Worked examples: harder problems", (
                "EXAMPLE 1 (an extra x, absorbed by algebra). "
                "$\\int x^3\\sqrt{x^2+1}\\,dx$.\n\n"
                "Try $u = x^2 + 1$, $du = 2x\\,dx$. The integrand is "
                "$x^2 \\cdot \\sqrt{x^2+1}\\cdot x\\,dx$: the $x\\,dx$ becomes "
                "$\\frac{du}{2}$, but an $x^2$ remains. Here is the extra move: "
                "the substitution ITSELF eliminates it, since $x^2 = u - 1$. "
                "Then\n"
                "$$\\int x^2\\sqrt{x^2+1}\\,x\\,dx "
                "= \\frac{1}{2}\\int (u-1)\\sqrt{u}\\,du "
                "= \\frac{1}{2}\\int \\big(u^{3/2} - u^{1/2}\\big)\\,du "
                "= \\frac{u^{5/2}}{5} - \\frac{u^{3/2}}{3} + C,$$\n"
                "so the answer is $\\frac{(x^2+1)^{5/2}}{5} - "
                "\\frac{(x^2+1)^{3/2}}{3} + C$. Moral: leftover EVEN powers of "
                "$x$ can often be expressed in $u$; that rescue is legal because "
                "it is exact algebra, not smuggling.\n\n"
                "EXAMPLE 2 (nested standard forms). "
                "$\\int \\frac{dx}{x \\ln x}$.\n\n"
                "The denominator is a product, but look structurally: "
                "$\\frac{1}{\\ln x}\\cdot\\frac{1}{x}\\,dx$ is a function of "
                "$\\ln x$ times the derivative of $\\ln x$. With $u = \\ln x$, "
                "$du = \\frac{dx}{x}$:\n"
                "$$\\int \\frac{1}{u}\\,du = \\ln|u| + C = \\ln|\\ln x| + C.$$\n"
                "A logarithm of a logarithm — strange-looking, easily verified "
                "by differentiating.\n\n"
                "EXAMPLE 3 (recognizing a table form after substituting). "
                "$\\int \\frac{e^x}{1 + e^{2x}}\\,dx$.\n\n"
                "Note $e^{2x} = (e^x)^2$, so the integrand is "
                "$\\frac{1}{1 + (e^x)^2}\\cdot e^x\\,dx$ — a function of $e^x$ "
                "times the derivative of $e^x$. With $u = e^x$:\n"
                "$$\\int \\frac{du}{1+u^2} = \\arctan u + C = \\arctan(e^x) + C.$$\n"
                "The hardest step was rewriting $e^{2x}$ to expose the "
                "composite. Harder problems are rarely harder mechanics — they "
                "are better-disguised patterns."
            )),
            # ---------------------------------------------------------- 10
            (L, "Special cases and edge behavior", (
                "WHEN SUBSTITUTION FAILS. Attempt $\\int \\cos(x^2)\\,dx$ with "
                "$u = x^2$: then $dx = \\frac{du}{2x} = \\frac{du}{2\\sqrt{u}}$, "
                "and the $u$-integral still contains the variable tangled in a "
                "new way. No choice of $u$ repairs this, because the inner "
                "derivative $2x$ is genuinely ABSENT from the integrand. "
                "Contrast $\\int x\\cos(x^2)\\,dx$, which falls in one line. One "
                "factor of $x$ is the difference between trivial and "
                "impossible-by-this-method.\n\n"
                "And sometimes 'impossible by this method' is the truth about "
                "every method: $\\int \\cos(x^2)\\,dx$, $\\int e^{-x^2}\\,dx$, "
                "and $\\int \\frac{\\sin x}{x}\\,dx$ have NO elementary "
                "antiderivative at all — provably (page 17). Their definite "
                "integrals exist and matter (optics, probability, signal "
                "processing); they simply are not expressible in the functions "
                "you know. A failed substitution is DATA: either another "
                "technique applies (C202-C205), or you have met a genuinely "
                "new function.\n\n"
                "BOUNDS THAT REVERSE. If $u = g(x)$ is DECREASING, Method A "
                "delivers reversed bounds: $u$ runs from a larger number down "
                "to a smaller one. Leave them in the order the substitution "
                "gives; $\\int_1^0 = -\\int_0^1$ handles the sign "
                "automatically. Re-sorting bounds 'to look normal' silently "
                "negates answers.\n\n"
                "BOUNDS THAT COLLAPSE. If $g(a) = g(b)$, Method A yields "
                "$\\int_{g(a)}^{g(a)} f(u)\\,du = 0$ — instantly. This is how "
                "substitution proves the symmetry facts: for odd $f$, "
                "$\\int_{-a}^{a} f = 0$ (substitute $u = -x$ on the left half "
                "and watch it cancel the right half), and for even $f$, "
                "$\\int_{-a}^a f = 2\\int_0^a f$. These shortcuts are exam "
                "gold and conceptual bedrock for Fourier work later."
            )),
            # ---------------------------------------------------------- 11
            (E, "Worked example: edge cases diagnosed", (
                "Four integrals; for each, the diagnosis first, then the "
                "resolution.\n\n"
                "(a) $\\int x\\sin(x^2)\\,dx$. Diagnosis: inner $x^2$, "
                "derivative $2x$ present up to a constant. Healthy. "
                "$u = x^2$: $\\frac{1}{2}\\int \\sin u\\,du = "
                "-\\frac{1}{2}\\cos(x^2) + C$.\n\n"
                "(b) $\\int \\sin(x^2)\\,dx$. Diagnosis: inner $x^2$, but its "
                "derivative is NOWHERE in the integrand, and no constant "
                "adjustment can conjure an $x$. Substitution fails — and so "
                "does everything else: this is the Fresnel integral of optics, "
                "provably non-elementary. The correct 'answer' at this level: "
                "recognize it, say WHY it resists ($u'$ absent), and know its "
                "definite versions are computed numerically (NUMERICS) or by "
                "series (C213).\n\n"
                "(c) $\\int_{-1}^{1} \\frac{x}{(1+x^2)^2}\\,dx$. Substitute "
                "$u = 1 + x^2$, $du = 2x\\,dx$: BOTH bounds map to $u = 2$. So "
                "the integral is $\\frac{1}{2}\\int_2^2 \\frac{du}{u^2} = 0$ — "
                "collapsed bounds, zero, no antiderivative needed. Cross-check "
                "by symmetry: the integrand is ODD on a symmetric interval, so "
                "0 was forced. Two independent methods, one answer — that is "
                "what confidence feels like.\n\n"
                "(d) $\\int_0^{\\pi/2} \\sin x\\cos^4 x\\,dx$. Substitute "
                "$u = \\cos x$, $du = -\\sin x\\,dx$. The bounds transform "
                "DOWNHILL: $x = 0 \\mapsto u = 1$, $x = \\frac{\\pi}{2} "
                "\\mapsto u = 0$. Faithfully:\n"
                "$$\\int_1^0 -u^4\\,du = \\int_0^1 u^4\\,du = \\frac{1}{5}.$$\n"
                "The reversed bounds and the minus sign from $du$ cancel — "
                "they always conspire correctly if you touch nothing. "
                "Students who 'fix' the bounds by hand and also keep the "
                "minus sign get $-\\frac15$: the sign error of page 13, "
                "manufactured by helpfulness."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: '$dx$ is decoration.' Students treat the "
                "differential as a period at the end of the integral sentence. "
                "It is a working part: $du = g'(x)\\,dx$ is the entire "
                "engine, and integrals cannot mix variables — an expression "
                "like $\\int u^2\\,x\\,dx$ is meaningless. If you cannot "
                "convert every $x$ INCLUDING the $dx$, the substitution has "
                "not happened.\n\n"
                "MISCONCEPTION 2: 'Some substitution always works.' No — "
                "substitution only reverses the chain rule, so it only "
                "succeeds on integrands with composite-times-inner-derivative "
                "structure. $\\int x e^x\\,dx$ has no such structure (the "
                "derivative of the exponent $x$ is 1, which does not help "
                "absorb the leading $x$); it needs C202. Diagnosing WHICH "
                "structure an integrand has is the real skill this unit "
                "builds toward (C205's strategy page).\n\n"
                "MISCONCEPTION 3: 'You can pull anything out of an "
                "integral.' Only CONSTANTS. Writing "
                "$\\int \\cos(x^2)\\,dx = \\frac{1}{2x}\\int \\cos u\\,du$ "
                "treats $\\frac{1}{2x}$ as a constant. It is not, and the "
                "step is nonsense — differentiate the 'answer' and watch it "
                "fail. The legality test is always the same: constants "
                "commute with integration; variables never do.\n\n"
                "MISCONCEPTION 4: 'The $u$-world answer is the answer.' An "
                "indefinite integral posed in $x$ must be ANSWERED in $x$. "
                "$\\frac{u^{10}}{10} + C$ is a correct statement about $u$ "
                "and an incomplete answer to the original question.\n\n"
                "MISCONCEPTION 5: 'Definite integrals: substitute, then use "
                "the old bounds.' The bounds are values of $x$; after "
                "substitution the variable is $u$. Old bounds with new "
                "variable is the single most common substitution error on "
                "exams. Page 6's rule: the moment you write $u$, the old "
                "bounds are invalid — convert or return."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational errors", (
                "ERROR 1: DROPPED CONSTANT FACTOR. From $u = 3x^2$, "
                "$du = 6x\\,dx$, students write $x\\,dx = du$ instead of "
                "$x\\,dx = \\frac{du}{6}$. Prevention: always SOLVE the $du$ "
                "equation explicitly for the exact piece appearing in the "
                "integrand, on its own line, before rewriting the "
                "integral.\n\n"
                "ERROR 2: SIGN LOSS WITH DECREASING $u$. $u = \\cos x$ gives "
                "$du = -\\sin x\\,dx$; the minus must surface either in the "
                "integrand or (for definite integrals) in reversed bounds — "
                "never both, never neither. Prevention: write the $du$ line "
                "with its sign and substitute mechanically; do not 'fix' "
                "anything by inspection.\n\n"
                "ERROR 3: FORGETTING TO BACK-SUBSTITUTE. Answering "
                "$\\frac{u^3}{3} + C$ to a question posed in $x$. Prevention: "
                "before writing $+C$, scan for surviving $u$'s.\n\n"
                "ERROR 4: FORGETTING $+C$ entirely, or attaching it "
                "mid-computation and then multiplying it by constants. Add "
                "$+C$ once, at the final line. (If you multiply "
                "$\\frac{1}{2}(e^u + C)$ out, the resulting $\\frac{C}{2}$ is "
                "still just an arbitrary constant — but writing it invites "
                "confusion; keep $C$ outside all coefficients.)\n\n"
                "ERROR 5: ABSOLUTE-VALUE AMNESIA. $\\int \\frac{du}{u} = "
                "\\ln|u| + C$, with bars. For $u = \\cos x$ or $u = x - 5$, "
                "the bars carry real content (the argument goes negative). "
                "They may be dropped only when $u > 0$ always, e.g. "
                "$u = x^2 + 1$ — and you should be able to say so.\n\n"
                "ERROR 6: PARTIAL CONVERSION. An integral half in $x$, half "
                "in $u$, then integrated anyway. Prevention is page 4's "
                "step 3 as a hard gate: no $x$ survives, or no integration "
                "happens. Every one of these six errors is caught by the "
                "same net: differentiate your final answer. Ten seconds; do "
                "it every time."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications", (
                "Substitution is not a parlor trick for textbook integrals; "
                "it is how integration reaches the composite structures that "
                "real models are made of.\n\n"
                "1. EXPONENTIAL PROCESSES. Radioactive decay, drug "
                "elimination, RC circuits, Newton cooling — all produce "
                "rates like $r(t) = A e^{-kt}$, and totals like "
                "$\\int_0^T A e^{-kt}\\,dt$, which is a linear substitution "
                "$u = -kt$. Any time a rate constant sits inside an "
                "exponential, the $\\frac{1}{a}$-shortcut of page 5 is doing "
                "the accounting.\n\n"
                "2. PROBABILITY (PF.U1.N2). Densities are integrated over "
                "intervals, and standardizing a normal variable — replacing "
                "$x$ by $z = \\frac{x - \\mu}{\\sigma}$ — IS a substitution; "
                "it is the reason one $z$-table serves every normal "
                "distribution. Changes of variables in densities are exactly "
                "this chapter's rule wearing different clothes.\n\n"
                "3. PHYSICS AND ENGINEERING. Work done by a variable force "
                "along a path, charge from a time-varying current "
                "$\\int i(t)\\,dt$ with oscillatory or decaying $i$, energy "
                "in signals $\\int v(t)^2\\,dt$ — the integrands arrive as "
                "composites because the physical laws compose (a field "
                "evaluated along a trajectory is $f(g(t))$ by "
                "construction).\n\n"
                "4. DIFFERENTIAL EQUATIONS (ODE.U1.N1). Separable equations "
                "are solved by writing $\\frac{dy}{dx} = f(x)g(y)$ as "
                "$\\int \\frac{dy}{g(y)} = \\int f(x)\\,dx$ — and BOTH sides "
                "are then substitution problems. The technique you are "
                "learning this week is the computational engine of the "
                "entire first ODE unit.\n\n"
                "The applied page that follows works one of these end to "
                "end; notice there that the modeling step (setting up the "
                "integral) and the technique step (substitution) are "
                "separate skills, and you need both."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: total accumulation from a decaying rate", (
                "A factory spill sends contaminant into a holding pond at "
                "the rate\n"
                "$$r(t) = 12\\,t\\,e^{-t^2/16} \\quad \\text{kg per day},$$\n"
                "$t$ in days after the spill. The rate rises at first (the "
                "$12t$), then dies off as cleanup takes hold (the "
                "exponential). QUESTION: how much contaminant enters during "
                "the first 4 days? And over all time, if the model runs "
                "forever?\n\n"
                "STEP 1 — set up. Total amount = integral of the rate:\n"
                "$$Q = \\int_0^4 12\\,t\\,e^{-t^2/16}\\,dt.$$\n\n"
                "STEP 2 — diagnose. Inner function in the exponent: "
                "$u = -\\frac{t^2}{16}$. Its derivative, $-\\frac{t}{8}$, is "
                "present up to a constant (there is a factor $t\\,dt$). "
                "License granted. From $du = -\\frac{t}{8}\\,dt$: "
                "$t\\,dt = -8\\,du$.\n\n"
                "STEP 3 — convert everything, bounds included (Method A). "
                "$t = 0 \\mapsto u = 0$; $t = 4 \\mapsto u = -1$. So\n"
                "$$Q = 12\\int_0^{-1} e^{u}\\,(-8)\\,du "
                "= -96\\int_0^{-1} e^u\\,du = 96\\int_{-1}^{0} e^u\\,du "
                "= 96\\big(e^0 - e^{-1}\\big).$$\n\n"
                "STEP 4 — evaluate and interpret. "
                "$Q = 96\\big(1 - \\tfrac{1}{e}\\big) \\approx 96(0.6321) "
                "\\approx 60.7$ kg in the first 4 days. Note how the "
                "reversed bounds appeared (decreasing $u$) and were handled "
                "by flipping with a sign — never by silent reordering.\n\n"
                "STEP 5 — the long run. As $T \\to \\infty$, "
                "$e^{-T^2/16} \\to 0$, and the same computation gives "
                "$Q_\\infty = 96(1 - 0) = 96$ kg: the model predicts a "
                "finite total from an infinite time horizon — your first "
                "taste of the improper integrals of C206. The physical "
                "sanity check: 60.7 of 96 kg, about 63%, arrives in the "
                "first 4 days, consistent with a rate that peaks early "
                "(maximize $r$: at $t = \\sqrt{8} \\approx 2.8$ days)."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections", (
                "BACKWARD. Substitution is C108's chain rule, inverted — "
                "every $u$ you choose is an exercise in seeing composite "
                "structure, which is chain-rule literacy. The antiderivative "
                "table it targets is C116; the definite-integral mechanics "
                "are C117-C118, and Method A's bound-changing rule is "
                "really the FTC applied twice.\n\n"
                "FORWARD, within this unit:\n"
                "• C202 (integration by parts) handles the products that "
                "substitution cannot — $x e^x$, $\\ln x$, $x\\sin x$ — by "
                "reversing the PRODUCT rule instead. The two techniques "
                "also combine: parts frequently needs a substitution to "
                "finish.\n"
                "• C203 (trig integrals) is substitution specialized: "
                "odd-power splitting exists precisely to manufacture the "
                "$du$ that $u = \\sin x$ or $u = \\cos x$ requires.\n"
                "• C204 (trig substitution) runs today's rule in the other "
                "direction — instead of $u = g(x)$ simplifying the inside, "
                "you set $x = g(\\theta)$ to make a radical collapse. Same "
                "theorem, opposite choreography.\n"
                "• C205 (partial fractions) preprocesses rational "
                "functions into pieces that substitution and the table "
                "finish; C206 (improper integrals) reuses today's "
                "techniques under limits, as page 15 previewed.\n\n"
                "FURTHER OUT. Series methods (C210-C216) integrate the "
                "non-elementary holdouts like $e^{-x^2}$ term by term. In "
                "multivariable calculus, substitution grows into the "
                "change-of-variables theorem with the Jacobian playing the "
                "role of $g'$ (the C304 track), and in probability "
                "(PF.U1.N2) the same theorem transforms densities. In "
                "ODE.U1.N1, separable equations make substitution a daily "
                "tool. Very few pages of mathematics you meet from here on "
                "are substitution-free."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: rigor, history, and impossibility", (
                "WHY THE NOTATION WORKS. Leibniz chose $\\frac{du}{dx}$ and "
                "$\\int \\ldots dx$ in the 1680s precisely so that the "
                "substitution rule would LOOK like algebra: $du = "
                "\\frac{du}{dx}dx$ appears to be a fraction identity. For "
                "two centuries mathematicians used it while quietly unsure "
                "what $du$ WAS. The modern resolutions: in analysis "
                "(REALAN), the substitution rule is a theorem about "
                "compositions of functions, proved from the chain rule and "
                "FTC, with differentials as pure notation; in differential "
                "geometry, $du$ is given independent life as a "
                "DIFFERENTIAL FORM, and substitution becomes the statement "
                "that integration is invariant under pullback — one of the "
                "cleanest ideas in modern mathematics, and the same "
                "machinery that powers the change-of-variables theorem "
                "with Jacobians in higher dimensions.\n\n"
                "THE IMPOSSIBILITY RESULTS. Page 10 asserted that "
                "$e^{-x^2}$ has no elementary antiderivative. That is a "
                "THEOREM (Liouville, 1835): an elementary antiderivative "
                "of $f e^g$ (with $f, g$ rational) exists only if a "
                "certain auxiliary rational function exists, and for "
                "$e^{-x^2}$ one can prove it does not. Risch (1969) "
                "extended this into a full decision procedure — an "
                "algorithm that, given an elementary function, either "
                "produces an elementary antiderivative or PROVES none "
                "exists. Computer algebra systems implement large parts of "
                "it; this is why software can say 'no closed form' with "
                "confidence rather than 'I gave up.'\n\n"
                "The philosophical takeaway: differentiation is closed on "
                "elementary functions, integration is not. Integration "
                "keeps creating new functions — $\\operatorname{erf}$, the "
                "Fresnel integrals, elliptic integrals — and much of "
                "18th-19th century analysis is the story of naming, "
                "taming, and tabulating integration's escapees. When your "
                "substitution fails on page 11's problem (b), you are "
                "standing at the exact spot where those fields began."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "THE RULE. $\\int f(g(x))\\,g'(x)\\,dx = \\int f(u)\\,du$ "
                "where $u = g(x)$, $du = g'(x)\\,dx$. Justified by the "
                "chain rule; answer must return to $x$ for indefinite "
                "integrals.\n\n"
                "DEFINITE VERSION. "
                "$\\int_a^b f(g(x))g'(x)\\,dx = \\int_{g(a)}^{g(b)} "
                "f(u)\\,du$. Either change the bounds and stay in $u$ "
                "(Method A), or back-substitute and use the original "
                "bounds (Method B). Never mix. Reversed bounds are "
                "legitimate; collapsed bounds mean the answer is 0.\n\n"
                "WORKFLOW. (1) Spot a $u$: inside a composite / under a "
                "radical / in a denominator / in an exponent / a function "
                "whose derivative appears elsewhere. (2) Write $du$ and "
                "solve for the exact piece present. (3) Convert "
                "EVERYTHING — no $x$ survives, bounds included if "
                "definite. (4) Integrate via the table. (5) Back-"
                "substitute, $+C$. (6) Differentiate to check.\n\n"
                "LEGAL vs ILLEGAL ADJUSTMENT. Missing CONSTANT: fix with "
                "$c\\,dx = du$ algebra. Missing VARIABLE factor: cannot be "
                "fixed; only constants pass through $\\int$.\n\n"
                "SHORTCUT. $\\int f(ax+b)\\,dx = \\frac{1}{a}F(ax+b)+C$.\n\n"
                "PATTERNS TO KNOW ON SIGHT.\n"
                "$$\\int \\frac{g'}{g}\\,dx = \\ln|g| + C, \\qquad "
                "\\int g'e^{g}\\,dx = e^{g} + C, \\qquad "
                "\\int g'\\,g^{n}\\,dx = \\frac{g^{n+1}}{n+1} + C.$$\n\n"
                "FAILURE SIGNALS. An $x$ that will not convert; a needed "
                "$u'$ absent (not just misplaced). Response: try the other "
                "candidate $u$; then consider parts (C202), trig methods "
                "(C203-C204), partial fractions (C205); some integrands "
                "($e^{-x^2}$, $\\cos(x^2)$, $\\frac{\\sin x}{x}$) are "
                "provably non-elementary — recognize, don't grind."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all eight before reading the solutions.\n\n"
                "1. $\\int x^2 (x^3 - 2)^5\\,dx$?\n"
                "2. $\\int \\cos x\\; e^{\\sin x}\\,dx$?\n"
                "3. $\\int_1^e \\frac{(\\ln x)^3}{x}\\,dx$?\n"
                "4. $\\int \\frac{x}{x^2+9}\\,dx$?\n"
                "5. $\\int \\sec^2 x \\sqrt{\\tan x}\\,dx$ (on an interval "
                "where $\\tan x \\ge 0$)?\n"
                "6. One of $\\int x e^{x^2}dx$ and $\\int e^{x^2}dx$ "
                "yields to substitution. Which, and why?\n"
                "7. $\\int_0^{\\pi/4} \\tan x \\sec^2 x\\,dx$?\n"
                "8. $\\int \\frac{dx}{\\sqrt{x}\\,(1+x)}$?\n\n"
                "SOLUTIONS.\n"
                "1. $u = x^3 - 2$, $x^2 dx = \\frac{du}{3}$: "
                "$\\frac{1}{3}\\cdot\\frac{u^6}{6} = "
                "\\frac{(x^3-2)^6}{18} + C$.\n"
                "2. $u = \\sin x$, $du = \\cos x\\,dx$: "
                "$e^{\\sin x} + C$.\n"
                "3. $u = \\ln x$, $du = \\frac{dx}{x}$; bounds "
                "$1 \\mapsto 0$, $e \\mapsto 1$: $\\int_0^1 u^3 du = "
                "\\frac{1}{4}$.\n"
                "4. $u = x^2 + 9$, $x\\,dx = \\frac{du}{2}$: "
                "$\\frac{1}{2}\\ln(x^2+9) + C$ (bars optional: "
                "$x^2 + 9 > 0$).\n"
                "5. $u = \\tan x$, $du = \\sec^2 x\\,dx$: "
                "$\\int u^{1/2} du = \\frac{2}{3}\\tan^{3/2}x + C$.\n"
                "6. $\\int x e^{x^2}dx$ succeeds: $u = x^2$ has "
                "derivative $2x$ PRESENT up to a constant, giving "
                "$\\frac{1}{2}e^{x^2}+C$. In $\\int e^{x^2}dx$ the needed "
                "$x$ is absent, no constant adjustment supplies a "
                "variable, and in fact no elementary antiderivative "
                "exists.\n"
                "7. $u = \\tan x$: bounds $0 \\mapsto 0$, "
                "$\\frac{\\pi}{4} \\mapsto 1$; $\\int_0^1 u\\,du = "
                "\\frac{1}{2}$. (Via $u = \\sec x$ instead: "
                "$\\frac{\\sec^2 x}{2}\\big|_0^{\\pi/4} = "
                "\\frac{2-1}{2} = \\frac12$ — different route, same "
                "answer; the two antiderivatives differ by a "
                "constant.)\n"
                "8. $u = \\sqrt{x}$, $x = u^2$, $dx = 2u\\,du$: "
                "$\\int \\frac{2u\\,du}{u(1+u^2)} = "
                "2\\arctan\\sqrt{x} + C$.\n\n"
                "Scoring guide: 8/8 — proceed. 6-7 — reread the pages "
                "matching your misses (constant slips → 13; definite "
                "bounds → 6-7; recognition → 8). ≤5 — rework pages 3-9 "
                "before practicing."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Before leaving this chapter you should be able to check "
                "every box honestly:\n\n"
                "□ Explain, via the chain rule, WHY "
                "$\\int f(g(x))g'(x)\\,dx = F(g(x)) + C$ — not just "
                "execute it.\n"
                "□ Scan an integrand and name the $u$-candidates: inside "
                "composites, radicands, denominators, exponents, "
                "derivative-pairs.\n"
                "□ Run the license check: confirm $u'$ is present up to "
                "a CONSTANT, and reject candidates fast when it is "
                "not.\n"
                "□ Execute the five-step mechanics with no $x$ surviving "
                "into the $u$-integral.\n"
                "□ Adjust constants correctly, and articulate why "
                "variable factors can never be 'pulled out.'\n"
                "□ Handle definite integrals BOTH ways — changed bounds "
                "(including reversed and collapsed bounds) and "
                "back-substitution — and never mix the two.\n"
                "□ Integrate the standard families on sight: "
                "$\\frac{g'}{g}$, $g' e^g$, $g' g^n$, and linear-inside "
                "$f(ax+b)$.\n"
                "□ Use $u$-algebra ($x^2 = u - 1$ moves) to absorb "
                "leftover even powers.\n"
                "□ Recognize failure honestly: distinguish 'wrong $u$, "
                "try again' from 'wrong technique, go to C202+' from "
                "'provably non-elementary.'\n"
                "□ Differentiate every answer as a reflex.\n\n"
                "When every box is checked, hit Practice on this node "
                "until $u$-spotting feels like reading, not solving. "
                "Then proceed to C202, where the products that defeated "
                "substitution — $x e^x$, $\\ln x$, $x\\cos x$ — meet the "
                "technique built exactly for them."
            )),
        ],
    },
    "C202": {
        "summary": (
            "A complete course on integration by parts: the formula and its "
            "derivation from the product rule, choosing u and dv (LIATE as a "
            "heuristic and where it breaks), repeated parts, the cyclic "
            "boomerang trick, parts for lone logarithms and inverse trig, "
            "tabular integration, and definite integrals by parts."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "C201 ended with a diagnosis: $\\int x e^x\\,dx$ resists "
                "substitution. Check for yourself — the exponent's derivative is 1, "
                "which cannot absorb the leading $x$; the composite-times-inner-"
                "derivative fingerprint simply is not there. Yet $x e^x$ is about "
                "as innocent-looking as integrands get, and its relatives are "
                "everywhere: $x\\cos x$ (moments of oscillating loads), "
                "$t e^{-rt}$ (present value of growing income), $x^2 e^{-x}$ "
                "(quantum mechanics, gamma functions), $\\ln x$ standing alone "
                "(entropy, information).\n\n"
                "What these integrands share is PRODUCT structure: two dissimilar "
                "functions multiplied, neither the derivative of anything inside "
                "the other. Substitution reverses the chain rule, so it is blind "
                "to products. The tool that sees them is INTEGRATION BY PARTS, "
                "the reverse of the product rule — and it is the second pillar of "
                "integration technique, arguably the most consequential formula "
                "in this course beyond the FTC itself.\n\n"
                "Parts has a different personality than substitution, and you "
                "should meet it expecting that difference. Substitution either "
                "works or it doesn't. Parts is a TRADE: it converts your integral "
                "into a boundary term plus a DIFFERENT integral, and the art is "
                "trading downhill — arranging the exchange so the new integral is "
                "simpler than the old. Sometimes one trade finishes the job; "
                "sometimes you trade twice or three times; sometimes (the famous "
                "'boomerang') the original integral comes back around and you "
                "capture it with algebra.\n\n"
                "In this chapter: the formula and its one-line derivation, the "
                "$u$/$dv$ choice (with the LIATE heuristic and its honest "
                "limitations), repeated and tabular parts, the cyclic trick for "
                "$e^x \\sin x$, the sneaky applications to $\\ln x$ and "
                "$\\arctan x$ alone, and definite integrals by parts."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Three prerequisites, all recent.\n\n"
                "1. THE PRODUCT RULE, backwards and forwards: "
                "$(uv)' = u'v + uv'$. Parts IS this rule rearranged, and the "
                "derivation on page 4 is two lines only if this identity is "
                "instant.\n\n"
                "2. SUBSTITUTION (C201). Parts produces a second integral, and "
                "that integral frequently needs substitution to finish. The two "
                "techniques interleave constantly; if your $u$-spotting is "
                "shaky, this chapter will expose it.\n\n"
                "3. THE DERIVATIVE/ANTIDERIVATIVE TABLE, in both directions "
                "at speed. Parts requires you to differentiate one factor AND "
                "antidifferentiate the other in the same breath: from "
                "$dv = \\cos x\\,dx$ you must produce $v = \\sin x$ without "
                "ceremony, and from $u = \\ln x$ you need $du = \\frac{dx}{x}$ "
                "immediately.\n\n"
                "Quick self-check (answers follow):\n\n"
                "• $\\frac{d}{dx}\\big(x \\sin x\\big)$? "
                "($\\sin x + x\\cos x$ — product rule, two terms.)\n"
                "• An antiderivative of $e^{3x}$? ($\\frac{1}{3}e^{3x}$ — the "
                "linear-inside shortcut from C201.)\n"
                "• $\\frac{d}{dx} \\arctan x$? ($\\frac{1}{1+x^2}$.)\n"
                "• $\\int \\sin x\\,dx$? ($-\\cos x + C$; the sign here is the "
                "single most common slip in all of parts.)\n\n"
                "Notice what the self-check emphasizes: signs and constants "
                "under differentiation and antidifferentiation of the basic "
                "zoo. Parts computations are short, but they braid these "
                "micro-operations together three or four at a time, so a 95% "
                "reliable sign becomes a 20% error rate per problem. If any "
                "item above cost you more than a moment, drill the table "
                "first; the chapter will feel twice as easy."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Differentiate a product and the product rule scatters the "
                "derivative across both factors:\n"
                "$$(uv)' = u'v + uv'.$$\n"
                "Integrate both sides and rearrange, and you get a machine for "
                "MOVING A DERIVATIVE FROM ONE FACTOR TO THE OTHER. That is the "
                "right mental model for parts: not a formula to plug into, but "
                "a lever that lifts the burden of integration off one factor "
                "and drops it onto the other — at the cost of a sign and a "
                "boundary term.\n\n"
                "Why would moving the derivative help? Because factors respond "
                "differently to differentiation. Some functions SIMPLIFY when "
                "differentiated: $x$ becomes $1$; $x^2$ steps down to $2x$; "
                "$\\ln x$ becomes the rational function $\\frac1x$; "
                "$\\arctan x$ becomes $\\frac{1}{1+x^2}$. Others merely "
                "circulate without getting worse: $e^x$ stays $e^x$; "
                "$\\sin x$ cycles to $\\cos x$. Parts lets you aim the "
                "derivative at the factor that will simplify, while the "
                "circulating factor absorbs an antidifferentiation without "
                "complaint.\n\n"
                "Run that story on $\\int x e^x\\,dx$. The $x$ is dead weight "
                "we would love to differentiate away; the $e^x$ is "
                "indestructible either way. So: differentiate the $x$ "
                "(turning it into 1), antidifferentiate the $e^x$ (still "
                "$e^x$), and the traded-for integral is $\\int e^x\\,dx$ — "
                "trivial. The original was impossible for substitution; one "
                "downhill trade made it routine.\n\n"
                "The whole craft of parts is choosing WHICH factor to "
                "differentiate. Choose well and each trade strictly "
                "simplifies; choose badly and you trade uphill — the new "
                "integral is worse (page 5 demonstrates deliberately). "
                "Pages 6 and 8 turn 'choose well' into an explicit "
                "decision procedure, and the boomerang trick of page 9 "
                "handles the delightful case where the trade goes in a "
                "circle."
            )),
            # ---------------------------------------------------------- 4
            (L, "The formula and its derivation", (
                "THE DERIVATION — watch how short it is. Start from the "
                "product rule, $(uv)' = u'v + uv'$, where $u = u(x)$ and "
                "$v = v(x)$ are differentiable. Integrate both sides with "
                "respect to $x$:\n"
                "$$uv = \\int u'v\\,dx + \\int uv'\\,dx.$$\n"
                "Rearrange:\n"
                "$$\\int u\\,v'\\,dx = uv - \\int v\\,u'\\,dx,$$\n"
                "or in the differential notation everyone actually uses "
                "($dv = v'dx$, $du = u'dx$):\n"
                "$$\\boxed{\\int u\\,dv = uv - \\int v\\,du.}$$\n"
                "That is integration by parts. ∎\n\n"
                "Read the formula as the trade described on page 3. You "
                "split the integrand into a piece $u$ (which will be "
                "DIFFERENTIATED — it appears as $du$ on the right) and a "
                "piece $dv$ (which will be ANTIDIFFERENTIATED — it appears "
                "as $v$ twice). The price of the trade is the boundary term "
                "$uv$ and a minus sign.\n\n"
                "MECHANICS. Given $\\int(\\text{stuff})\\,dx$:\n"
                "1. Split: choose $u$ and let $dv$ be everything else, "
                "INCLUDING the $dx$.\n"
                "2. Differentiate $u$ to get $du$; antidifferentiate $dv$ "
                "to get $v$. (No $+C$ here — any single antiderivative "
                "works; page 12 explains why.)\n"
                "3. Assemble $uv - \\int v\\,du$ and evaluate the new "
                "integral by any means available — table, substitution, or "
                "parts again.\n\n"
                "THE DEFINITE VERSION carries bounds through the boundary "
                "term:\n"
                "$$\\int_a^b u\\,dv = \\Big[uv\\Big]_a^b - "
                "\\int_a^b v\\,du.$$\n"
                "The $[uv]_a^b$ piece is evaluated immediately — and in "
                "applied problems it often vanishes at one or both ends, "
                "which is precisely why parts is beloved in physics and "
                "probability (pages 14-15)."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples", (
                "EXAMPLE 1 (the flagship). $\\int x e^x\\,dx$.\n\n"
                "Choose $u = x$ (it simplifies when differentiated) and "
                "$dv = e^x\\,dx$ (harmless to antidifferentiate). Then "
                "$du = dx$ and $v = e^x$:\n"
                "$$\\int x e^x dx = x e^x - \\int e^x\\,dx "
                "= x e^x - e^x + C = (x-1)e^x + C.$$\n"
                "Check by product rule: $\\frac{d}{dx}\\big[(x-1)e^x\\big] "
                "= e^x + (x-1)e^x = x e^x$. Correct.\n\n"
                "EXAMPLE 2 (trig partner). $\\int x \\cos x\\,dx$.\n\n"
                "Same logic: $u = x$, $dv = \\cos x\\,dx$, so $du = dx$, "
                "$v = \\sin x$:\n"
                "$$\\int x\\cos x\\,dx = x\\sin x - \\int \\sin x\\,dx "
                "= x\\sin x + \\cos x + C.$$\n"
                "Mind the sign on the last step: "
                "$-\\int\\sin x\\,dx = -(-\\cos x) = +\\cos x$. Two stacked "
                "negatives — the classic slip site.\n\n"
                "EXAMPLE 3 (the same integral, sabotaged on purpose). Take "
                "$\\int x e^x dx$ again but choose backwards: $u = e^x$, "
                "$dv = x\\,dx$. Then $du = e^x dx$, $v = \\frac{x^2}{2}$:\n"
                "$$\\int x e^x dx = \\frac{x^2}{2}e^x - "
                "\\frac{1}{2}\\int x^2 e^x\\,dx.$$\n"
                "Nothing is WRONG — the equation is true — but the new "
                "integral has $x^2$ where the old had $x$: we traded "
                "UPHILL. Iterate this choice and the powers grow forever. "
                "The lesson, stated as a slogan you will use every day: "
                "$u$ IS THE FACTOR THAT IMPROVES WHEN DIFFERENTIATED; $dv$ "
                "IS THE FACTOR YOU CAN AFFORD TO ANTIDIFFERENTIATE. When "
                "a parts computation is getting worse, you likely have "
                "the split backwards — flip it and try once more before "
                "abandoning the technique."
            )),
            # ---------------------------------------------------------- 6
            (L, "Choosing u and dv: LIATE, its logic, and its limits", (
                "The choice of split has a famous mnemonic. LIATE ranks "
                "factor types by priority for the role of $u$:\n\n"
                "L — Logarithmic ($\\ln x$, $\\log_a x$)\n"
                "I — Inverse trig ($\\arctan x$, $\\arcsin x$)\n"
                "A — Algebraic ($x^n$, polynomials)\n"
                "T — Trigonometric ($\\sin x$, $\\cos x$)\n"
                "E — Exponential ($e^{kx}$, $a^x$)\n\n"
                "Whichever factor appears EARLIER in LIATE becomes $u$; "
                "the rest is $dv$. For $\\int x\\ln x\\,dx$: L beats A, so "
                "$u = \\ln x$. For $\\int x^2 \\sin x\\,dx$: A beats T, so "
                "$u = x^2$.\n\n"
                "WHY it works — LIATE is not arbitrary. The list is "
                "ordered by two gradients running in opposite directions. "
                "Functions at the TOP simplify dramatically under "
                "differentiation ($\\ln x \\to \\frac1x$, "
                "$\\arctan x \\to \\frac{1}{1+x^2}$: transcendental "
                "becomes algebraic) but are awkward or mysterious to "
                "antidifferentiate. Functions at the BOTTOM are "
                "effortless to antidifferentiate ($e^x \\to e^x$, "
                "$\\sin x \\to -\\cos x$: no growth, no complication) but "
                "gain nothing from being differentiated. So the top of "
                "the list wants to be $u$, the bottom wants to be $dv$, "
                "and the ranking encodes exactly the trade-downhill "
                "principle of page 3.\n\n"
                "ITS LIMITS — treat LIATE as a default, not a law:\n"
                "• It presumes parts is the right technique at all. "
                "$\\int x e^{x^2}\\,dx$ contains A and E, but it is a "
                "SUBSTITUTION problem ($u = x^2$); LIATE applied blindly "
                "produces a mess. Always run C201's fingerprint check "
                "first.\n"
                "• Ties and repeats: in $\\int e^x \\sin x\\,dx$ (T vs E, "
                "adjacent ranks) either choice works — the cyclic trick "
                "of page 9 resolves it, and consistency matters more "
                "than the pick.\n"
                "• Some splits are invisible to it: $\\int \\ln x\\,dx$ "
                "has ONE visible factor; the winning move is "
                "$dv = dx$ (page 9). The mnemonic guides; the "
                "trade-downhill principle DECIDES."
            )),
            # ---------------------------------------------------------- 7
            (E, "Worked examples: standard problems", (
                "EXAMPLE 1 (repeated parts). $\\int x^2 \\sin x\\,dx$.\n\n"
                "LIATE: $u = x^2$, $dv = \\sin x\\,dx$, so $du = 2x\\,dx$, "
                "$v = -\\cos x$:\n"
                "$$\\int x^2\\sin x\\,dx = -x^2\\cos x + "
                "2\\int x\\cos x\\,dx.$$\n"
                "The new integral is one degree simpler — trade accepted, "
                "but not yet finished. Apply parts AGAIN (Example 2 of "
                "page 5 already did it): $\\int x\\cos x\\,dx = "
                "x\\sin x + \\cos x + C$. Assemble:\n"
                "$$\\int x^2 \\sin x\\,dx = -x^2\\cos x + 2x\\sin x + "
                "2\\cos x + C.$$\n"
                "Each round of parts knocks the polynomial degree down by "
                "one; a degree-$n$ polynomial costs $n$ rounds. (Page 9 "
                "introduces the tabular shortcut that compresses all $n$ "
                "rounds into one table.)\n\n"
                "EXAMPLE 2 (the lone logarithm). $\\int \\ln x\\,dx$.\n\n"
                "Only one factor is visible — so make the invisible one "
                "explicit: $\\ln x = \\ln x \\cdot 1$. Take $u = \\ln x$, "
                "$dv = 1\\,dx$; then $du = \\frac{dx}{x}$, $v = x$:\n"
                "$$\\int \\ln x\\,dx = x\\ln x - \\int x\\cdot"
                "\\frac{dx}{x} = x\\ln x - \\int 1\\,dx "
                "= x\\ln x - x + C.$$\n"
                "The trick generalizes: any function easy to "
                "DIFFERENTIATE but mysterious to integrate ($\\arctan x$, "
                "$\\arcsin x$, $(\\ln x)^2$) yields to $dv = dx$.\n\n"
                "EXAMPLE 3 (definite, boundary term evaluated). "
                "$\\int_0^1 x e^{-x}\\,dx$.\n\n"
                "$u = x$, $dv = e^{-x}dx$, $du = dx$, $v = -e^{-x}$:\n"
                "$$\\int_0^1 x e^{-x}dx = \\Big[-x e^{-x}\\Big]_0^1 + "
                "\\int_0^1 e^{-x}\\,dx = -e^{-1} + "
                "\\Big[-e^{-x}\\Big]_0^1 = -\\frac{1}{e} + "
                "\\Big(1 - \\frac{1}{e}\\Big) = 1 - \\frac{2}{e}.$$\n"
                "Numerically $\\approx 0.264$ — positive, as it must be "
                "for a positive integrand. Evaluate the boundary term the "
                "moment it appears; carrying it symbolically invites "
                "sign rot."
            )),
            # ---------------------------------------------------------- 8
            (L, "Strategy: a decision guide for parts", (
                "Facing a product integrand, run this sequence.\n\n"
                "STEP 0 — RULE OUT SUBSTITUTION. Is one factor the "
                "derivative of something inside the other "
                "($x e^{x^2}$, $\\sin x\\cos^4 x$)? Then C201 finishes "
                "faster. Parts is for products of UNRELATED functions.\n\n"
                "PATTERN 1 — polynomial × (exponential or sine/cosine): "
                "$u = $ the polynomial. Expect one round per degree; use "
                "the TABULAR method (page 9) for degree ≥ 2.\n\n"
                "PATTERN 2 — (anything) × (logarithm or inverse trig): "
                "$u = $ the log / inverse trig, even if the 'anything' is "
                "just $1$ (Examples 2, page 7; arctan on page 9). "
                "Differentiation converts these to algebraic functions, "
                "and the new integral is usually a substitution or "
                "partial-fractions problem.\n\n"
                "PATTERN 3 — exponential × sine/cosine: neither factor "
                "ever simplifies; parts TWICE with consistent choices "
                "brings the original integral back with a coefficient — "
                "then SOLVE FOR IT algebraically (the boomerang, page 9). "
                "Not a failure mode; a technique.\n\n"
                "PATTERN 4 — none of the above (e.g. "
                "$\\int \\sin\\sqrt{x}\\,dx$): try a SUBSTITUTION FIRST to "
                "reshape the integrand ($w = \\sqrt x$ turns it into "
                "$2\\int w\\sin w\\,dw$ — Pattern 1), then parts. "
                "Substitute-then-parts is among the most common exam "
                "combinations.\n\n"
                "SANITY METER. After each round ask: did the integral get "
                "simpler? Polynomial degree should drop; logs and inverse "
                "trig should disappear; a boomerang should return the "
                "EXACT original (up to coefficient). If instead powers "
                "grow, flip your split (page 5, Example 3). If nothing "
                "improves after two honest attempts, parts is probably "
                "the wrong tool — return to the C205 strategy overview "
                "mindset: identify structure first, then commit."
            )),
            # ---------------------------------------------------------- 9
            (E, "Worked examples: harder problems", (
                "EXAMPLE 1 (the boomerang). $I = \\int e^x \\sin x\\,dx$.\n\n"
                "Round 1: $u = \\sin x$, $dv = e^x dx$; $du = \\cos x\\,dx$, "
                "$v = e^x$:\n"
                "$$I = e^x \\sin x - \\int e^x \\cos x\\,dx.$$\n"
                "Round 2 on the new integral, SAME convention (trig is $u$): "
                "$u = \\cos x$, $dv = e^x dx$:\n"
                "$$\\int e^x\\cos x\\,dx = e^x\\cos x + \\int e^x \\sin x\\,dx "
                "= e^x \\cos x + I.$$\n"
                "Substitute back:\n"
                "$$I = e^x\\sin x - e^x\\cos x - I "
                "\\;\\Longrightarrow\\; 2I = e^x(\\sin x - \\cos x) "
                "\\;\\Longrightarrow\\; I = \\frac{e^x(\\sin x - \\cos x)}{2} + C.$$\n"
                "The integral returned like a boomerang and we caught it with "
                "algebra. Two warnings: keep the SAME type of split both rounds "
                "(flip-flopping undoes round 1 and yields $I = I$), and add "
                "$+C$ at the end.\n\n"
                "EXAMPLE 2 (inverse trig alone). $\\int \\arctan x\\,dx$.\n\n"
                "$u = \\arctan x$, $dv = dx$; $du = \\frac{dx}{1+x^2}$, "
                "$v = x$:\n"
                "$$\\int \\arctan x\\,dx = x\\arctan x - "
                "\\int \\frac{x}{1+x^2}\\,dx = x\\arctan x - "
                "\\frac{1}{2}\\ln(1+x^2) + C,$$\n"
                "where the second integral fell to C201 "
                "($u = 1 + x^2$, the $\\frac{g'}{g}$ pattern). Parts and "
                "substitution, hand in hand.\n\n"
                "EXAMPLE 3 (tabular integration). $\\int x^3 e^{2x}\\,dx$.\n\n"
                "Repeated parts on a polynomial is mechanical, so mechanize "
                "it. Columns: derivatives of $x^3$ down to 0; antiderivatives "
                "of $e^{2x}$; alternating signs $+,-,+,-$:\n\n"
                "$x^3 \\;|\\; 3x^2 \\;|\\; 6x \\;|\\; 6 \\;|\\; 0$\n"
                "$\\tfrac{e^{2x}}{2} \\;|\\; \\tfrac{e^{2x}}{4} \\;|\\; "
                "\\tfrac{e^{2x}}{8} \\;|\\; \\tfrac{e^{2x}}{16}$\n\n"
                "Multiply along the diagonals with the signs:\n"
                "$$\\int x^3 e^{2x} dx = e^{2x}\\Big(\\frac{x^3}{2} - "
                "\\frac{3x^2}{4} + \\frac{3x}{4} - \\frac{3}{8}\\Big) + C.$$\n"
                "One table replaces three rounds of bookkeeping — and "
                "differentiating the answer confirms it in seconds."
            )),
            # ---------------------------------------------------------- 10
            (L, "Special cases and edge behavior", (
                "THE GENERAL CYCLIC FORMULA. The boomerang works for any "
                "$\\int e^{ax}\\sin(bx)\\,dx$ or $\\int e^{ax}\\cos(bx)\\,dx$; "
                "running the two rounds symbolically gives entries worth "
                "keeping on your reference sheet:\n"
                "$$\\int e^{ax}\\sin bx\\,dx = "
                "\\frac{e^{ax}(a\\sin bx - b\\cos bx)}{a^2+b^2} + C, \\qquad "
                "\\int e^{ax}\\cos bx\\,dx = "
                "\\frac{e^{ax}(a\\cos bx + b\\sin bx)}{a^2+b^2} + C.$$\n"
                "The $a^2 + b^2$ in the denominator is the algebraic residue "
                "of 'solve for $I$.'\n\n"
                "A FAMOUS 'PARADOX.' Apply parts to $\\int \\frac{dx}{x}$ "
                "with $u = \\frac1x$, $dv = dx$: $v = x$, "
                "$du = -\\frac{dx}{x^2}$, so\n"
                "$$\\int \\frac{dx}{x} = \\frac{1}{x}\\cdot x - "
                "\\int x\\cdot\\Big(-\\frac{1}{x^2}\\Big)dx = 1 + "
                "\\int\\frac{dx}{x}.$$\n"
                "Subtract the integral from both sides: $0 = 1$?! The "
                "resolution: an indefinite integral is a FAMILY of functions, "
                "defined up to a constant. '$\\int\\frac{dx}x$' on the two "
                "sides denotes antiderivative families, and families "
                "differing by the constant 1 are the SAME family. The "
                "cancellation was the illegal step. Moral: parts identities "
                "relate families; only the boomerang's 'solve for $I$' — "
                "where the coefficient of $I$ changes — extracts new "
                "information.\n\n"
                "REDUCTION FORMULAS. Parts applied symbolically to a power "
                "generates a recursion, e.g. for $n \\ge 1$:\n"
                "$$\\int x^n e^x dx = x^n e^x - n\\int x^{n-1}e^x dx, "
                "\\qquad \\int (\\ln x)^n dx = x(\\ln x)^n - "
                "n\\int (\\ln x)^{n-1} dx.$$\n"
                "Each application steps $n$ down by one; C203 builds the "
                "same machinery for $\\int \\sin^n x\\,dx$. Reduction "
                "formulas are parts industrialized — derive one, use it "
                "forever."
            )),
            # ---------------------------------------------------------- 11
            (E, "Worked example: edge cases diagnosed", (
                "Three cases, each illustrating one page-10 phenomenon.\n\n"
                "CASE (a) — cyclic with constants: "
                "$\\int e^{2x}\\cos 3x\\,dx$. Two rounds, keeping trig as "
                "$u$ throughout: round 1 gives "
                "$\\frac{1}{2}e^{2x}\\cos 3x + "
                "\\frac{3}{2}\\int e^{2x}\\sin 3x\\,dx$ "
                "(here it is cleaner to take $dv = e^{2x}dx$ each time); "
                "round 2 turns the new integral into "
                "$\\frac{1}{2}e^{2x}\\sin 3x - "
                "\\frac{3}{2}\\int e^{2x}\\cos 3x\\,dx$. Let $I$ be the "
                "original:\n"
                "$$I = \\frac{e^{2x}\\cos 3x}{2} + "
                "\\frac{3e^{2x}\\sin 3x}{4} - \\frac{9}{4}I "
                "\\;\\Longrightarrow\\; \\frac{13}{4}I = "
                "\\frac{e^{2x}(2\\cos 3x + 3\\sin 3x)}{4},$$\n"
                "so $I = \\frac{e^{2x}(2\\cos 3x + 3\\sin 3x)}{13} + C$ — "
                "matching page 10's formula with $a = 2$, $b = 3$, "
                "$a^2 + b^2 = 13$.\n\n"
                "CASE (b) — iterated logs: $\\int (\\ln x)^2\\,dx$. Use "
                "$dv = dx$: $u = (\\ln x)^2$, $du = \\frac{2\\ln x}{x}dx$, "
                "$v = x$:\n"
                "$$\\int(\\ln x)^2 dx = x(\\ln x)^2 - 2\\int \\ln x\\,dx "
                "= x(\\ln x)^2 - 2x\\ln x + 2x + C,$$\n"
                "using page 7's result for $\\int \\ln x\\,dx$. This is "
                "page 10's reduction formula with $n = 2$, unrolled.\n\n"
                "CASE (c) — substitute first, then parts: "
                "$\\int \\sin\\sqrt{x}\\,dx$. No product is visible and no "
                "inner derivative is present — but the substitution "
                "$w = \\sqrt x$ ($x = w^2$, $dx = 2w\\,dw$) MANUFACTURES a "
                "product:\n"
                "$$\\int \\sin\\sqrt x\\,dx = 2\\int w\\sin w\\,dw "
                "= 2(-w\\cos w + \\sin w) + C "
                "= -2\\sqrt{x}\\cos\\sqrt{x} + 2\\sin\\sqrt{x} + C.$$\n"
                "Diagnosis order mattered: substitution reshaped, parts "
                "finished. When an integrand fits no pattern, ask what "
                "substitution would MAKE it fit one."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'LIATE is a theorem.' It is a default "
                "with counterexamples. $\\int x e^{x^2}dx$: LIATE says "
                "$u = x$, but the integral is pure substitution, and "
                "parts turns it into a genuinely harder problem "
                "($\\int e^{x^2}$-type integrals are non-elementary!). "
                "$\\int x^3 e^{x^2} dx$: the winning split is "
                "$u = x^2$, $dv = x e^{x^2}dx$ — LIATE cannot see it, "
                "because the right $dv$ is chosen for INTEGRABILITY "
                "(via $w = x^2$), not by type. Principle over mnemonic: "
                "$u$ improves under differentiation; $dv$ must be "
                "something you can actually integrate.\n\n"
                "MISCONCEPTION 2: 'Parts failed because the new integral "
                "isn't elementary yet.' Parts usually takes SEVERAL "
                "rounds ($x^2\\sin x$ needs two) or returns the original "
                "for capture (boomerang). One non-trivial remainder is "
                "not failure; growth without simplification is.\n\n"
                "MISCONCEPTION 3: 'You need $+C$ when computing $v$.' "
                "Any single antiderivative of $dv$ works: if $v$ is "
                "replaced by $v + c$, the identity becomes "
                "$u(v+c) - \\int (v+c)\\,du = uv - \\int v\\,du + "
                "\\big(uc - c\\int du\\big)$, and the parenthesis is "
                "zero. Choosing $c$ cleverly is occasionally USEFUL "
                "(e.g. $v = \\frac{x^2+1}{2}$ tidies "
                "$\\int x\\arctan x\\,dx$) but never required.\n\n"
                "MISCONCEPTION 4: 'The boomerang means I went in a "
                "circle, so I made an error.' Getting the ORIGINAL "
                "integral back WITH A DIFFERENT COEFFICIENT is the "
                "success condition — solve for it. Getting it back with "
                "coefficient exactly $+1$ ($I = \\ldots + I$ with the "
                "boundary terms cancelling) means your second split "
                "undid your first; keep the same convention both "
                "rounds.\n\n"
                "MISCONCEPTION 5: 'Parts is only for products.' "
                "$\\int \\ln x\\,dx$ and $\\int \\arctan x\\,dx$ are "
                "single-factor integrals cracked by $dv = dx$. The "
                "formula requires a split, not a visible product."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational errors", (
                "ERROR 1: SIGN PROPAGATION. The minus in "
                "$uv - \\int v\\,du$ applies to the ENTIRE new integral, "
                "and new integrals often carry their own minus signs "
                "($v = -\\cos x$, $v = -e^{-x}$). Three stacked negatives "
                "is routine in parts. Prevention: write the $u, du, v, "
                "dv$ block explicitly, then substitute into the formula "
                "in one careful line — never 'simplify signs in your "
                "head' mid-substitution.\n\n"
                "ERROR 2: FORGETTING $dx$ BELONGS TO $dv$. The split of "
                "$\\int x\\cos x\\,dx$ is $u = x$, $dv = \\cos x\\,dx$ — "
                "not $dv = \\cos x$. Antidifferentiating a $dv$ with no "
                "differential is how mystery factors appear.\n\n"
                "ERROR 3: TABULAR SIGN COLUMN. The signs alternate "
                "$+,-,+,-$ starting from $+$. An even-length table "
                "ending on a minus catches people; also remember each "
                "antiderivative of $e^{2x}$ contributes ANOTHER "
                "$\\frac12$. Prevention: after any tabular run, "
                "differentiate the answer — tabular errors are "
                "instantly visible.\n\n"
                "ERROR 4: DEFINITE INTEGRALS — evaluating "
                "$\\int_a^b v\\,du$ but forgetting to evaluate "
                "$[uv]_a^b$, or vice versa. Both pieces carry the "
                "bounds. Prevention: compute $[uv]_a^b$ numerically the "
                "moment it appears (page 7, Example 3).\n\n"
                "ERROR 5: BOOMERANG ALGEBRA. After "
                "$I = (\\text{terms}) - \\frac94 I$, students add "
                "$\\frac94 I$ but forget to divide the terms by "
                "$\\frac{13}{4}$, or divide only some terms. Prevention: "
                "move ALL $I$'s left, factor, divide ONCE, and only "
                "then attach $+C$.\n\n"
                "ERROR 6: DIFFERENTIATING $dv$ AND INTEGRATING $u$ — "
                "the formula run backwards, surprisingly common under "
                "exam pressure. The block format ($u = \\ldots$, "
                "$du = \\ldots$ on one side; $dv = \\ldots$, "
                "$v = \\ldots$ on the other) makes the direction of "
                "each arrow visible and kills the error."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications", (
                "Parts is disproportionately represented in applied "
                "mathematics because so many models multiply a POLYNOMIAL "
                "or POWER trend by an EXPONENTIAL or OSCILLATORY factor.\n\n"
                "1. PRESENT VALUE (economics/finance). An income stream "
                "$R(t)$ received continuously and discounted at rate $r$ "
                "has present value $\\int_0^T R(t)e^{-rt}\\,dt$. Any "
                "linear or polynomial growth in $R$ makes this a parts "
                "problem — page 15 works one in full.\n\n"
                "2. EXPECTED VALUES AND MOMENTS (PF.U1.N2). The mean of a "
                "continuously distributed quantity is "
                "$\\int x f(x)\\,dx$; for the exponential distribution "
                "$f(x) = \\lambda e^{-\\lambda x}$ on $[0,\\infty)$, "
                "computing $\\int_0^\\infty x\\lambda e^{-\\lambda x}dx = "
                "\\frac{1}{\\lambda}$ is exactly Example 3 of page 7 "
                "stretched to an improper bound (C206). Variances bring "
                "$x^2$ and a second round of parts.\n\n"
                "3. TRANSFORMS (engineering). The Laplace transform "
                "$\\int_0^\\infty f(t)e^{-st}dt$ and Fourier coefficients "
                "$\\int f(t)\\sin(n\\omega t)\\,dt$ are parts factories: "
                "integrating polynomial, ramp, and sawtooth signals "
                "against exponentials and sinusoids IS this chapter. The "
                "deeper pattern — parts converts the transform of a "
                "DERIVATIVE into $s$ times the transform of the function "
                "— is the very property that turns differential equations "
                "into algebra (the ODE track's transform methods).\n\n"
                "4. PROOF ENGINE. Parts derives reduction formulas "
                "(page 10), the Taylor remainder in integral form "
                "(C213), and — in advanced settings — the identities "
                "behind weak derivatives (page 17). It is the standard "
                "way to move derivatives off an unknown function and "
                "onto a known one, which is why it appears in nearly "
                "every integration-adjacent proof you will meet from "
                "here to graduate school."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: present value of a growing income stream", (
                "A rental property produces income at the continuous "
                "rate $R(t) = 50{,}000 + 2{,}000t$ dollars per year "
                "($t$ in years): \\$50{,}000 now, growing \\$2{,}000 per "
                "year. Money arriving at time $t$ is discounted by "
                "$e^{-0.05t}$ (5% continuous rate). QUESTION: what is "
                "the present value of the next 10 years of income?\n\n"
                "STEP 1 — set up.\n"
                "$$PV = \\int_0^{10} (50000 + 2000t)\\,e^{-0.05t}\\,dt.$$\n\n"
                "STEP 2 — split. Polynomial × exponential: Pattern 1. "
                "$u = 50000 + 2000t$ (simplifies to a constant when "
                "differentiated), $dv = e^{-0.05t}dt$. Then "
                "$du = 2000\\,dt$ and $v = \\frac{e^{-0.05t}}{-0.05} = "
                "-20e^{-0.05t}$.\n\n"
                "STEP 3 — apply parts.\n"
                "$$PV = \\Big[-20(50000+2000t)e^{-0.05t}\\Big]_0^{10} + "
                "20\\int_0^{10} 2000\\,e^{-0.05t}\\,dt.$$\n"
                "Boundary term: at $t = 10$: "
                "$-20(70000)e^{-0.5} = -1{,}400{,}000e^{-0.5}$; at "
                "$t = 0$: $-20(50000) = -1{,}000{,}000$. Difference: "
                "$1{,}000{,}000 - 1{,}400{,}000e^{-0.5}$.\n\n"
                "STEP 4 — the remaining integral.\n"
                "$$40000\\int_0^{10} e^{-0.05t}dt = "
                "40000\\Big[-20e^{-0.05t}\\Big]_0^{10} = "
                "800{,}000\\big(1 - e^{-0.5}\\big).$$\n\n"
                "STEP 5 — assemble and evaluate. With "
                "$e^{-0.5} \\approx 0.60653$:\n"
                "$$PV = 1{,}000{,}000 - 1{,}400{,}000(0.60653) + "
                "800{,}000(0.39347) \\approx 465{,}600.$$\n\n"
                "SANITY CHECKS. Ignore discounting entirely: 10 years "
                "of income totals \\$600{,}000, and $PV$ must be less — "
                "it is. Ignore growth: a flat \\$50{,}000 stream has "
                "$PV = 1{,}000{,}000(1 - e^{-0.5}) \\approx 393{,}500$, "
                "and growth should add something — it does. The modeling "
                "produced the integral; parts, plus two bounding "
                "estimates, produced a defensible number."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections", (
                "BACKWARD. Parts is the product rule of differentiation, "
                "integrated and rearranged — it stands to the product "
                "rule exactly as C201's substitution stands to the chain "
                "rule (C108). Together the pair answer the natural "
                "question 'what do the two big differentiation rules "
                "give us when run backwards?' — and between them they "
                "generate most of integration technique. The FTC (C118) "
                "supplies the definite version's boundary-term "
                "mechanics.\n\n"
                "FORWARD, within this unit:\n"
                "• C203 (trig integrals) uses parts to crack the odd "
                "secant powers — the $\\sec^3$ classic is a boomerang — "
                "and to derive the sine/cosine power reduction "
                "formulas.\n"
                "• C204 (trig substitution) routinely dumps its output "
                "into $\\int \\sec^3\\theta\\,d\\theta$, so parts "
                "quietly underwrites that chapter too.\n"
                "• C205 (partial fractions) shares the workload on "
                "rational-times-transcendental integrands: parts "
                "converts $\\arctan$'s and logs into rational "
                "integrals, partial fractions finishes.\n"
                "• C206 (improper integrals): boundary terms like "
                "$[-xe^{-x}]_0^\\infty$ demand limit evaluation — "
                "parts plus limits defines the gamma function "
                "$\\Gamma(n) = \\int_0^\\infty x^{n-1}e^{-x}dx$, whose "
                "reduction formula $\\Gamma(n+1) = n\\Gamma(n)$ IS "
                "integration by parts.\n\n"
                "FURTHER OUT. The Taylor-remainder derivation in the "
                "series unit (C210-C216, especially C213) is repeated "
                "parts; transform methods for ODEs (the ODE track "
                "beyond ODE.U1.N1) rest on the parts identity "
                "$\\mathcal{L}[f'] = s\\mathcal{L}[f] - f(0)$; and "
                "expected-value computations throughout probability "
                "(PF.U1.N2) are parts on densities. In analysis, parts "
                "becomes the definition of weak derivatives — page 17 "
                "tells that story."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: parts as a structural principle", (
                "Brook Taylor published the formula in 1715, but its "
                "modern significance dwarfs its origin as a "
                "computational trick.\n\n"
                "PARTS AS 'MOVE THE DERIVATIVE.' Rewrite the definite "
                "version as\n"
                "$$\\int_a^b u'v\\,dx = [uv]_a^b - \\int_a^b u v'\\,dx.$$\n"
                "Read structurally: a derivative on one factor can be "
                "transferred to the other factor, at the cost of a sign "
                "and boundary data. In this form, parts is the "
                "one-dimensional case of the DIVERGENCE THEOREM and "
                "Green's identities of multivariable calculus — the same "
                "transfer principle in higher dimensions, with boundary "
                "terms becoming surface integrals.\n\n"
                "WEAK DERIVATIVES. Twentieth-century analysis turned the "
                "formula into a DEFINITION. If $f$ is too rough to "
                "differentiate classically, we can still ask for a "
                "function $g$ such that $\\int f\\varphi'\\,dx = "
                "-\\int g\\varphi\\,dx$ for every smooth 'test function' "
                "$\\varphi$ vanishing at the boundary — that is, a $g$ "
                "that BEHAVES like $f'$ under parts. Such a $g$ is "
                "called the weak derivative; distributions "
                "(generalized functions like the Dirac delta) and "
                "Sobolev spaces — the natural habitat of modern PDE "
                "theory and the finite element method (NUMERICS) — are "
                "built on exactly this move. When an engineer's solver "
                "computes the 'weak form' of an equation, it is doing "
                "integration by parts, deliberately, to lower the "
                "smoothness the solution must have.\n\n"
                "ORTHOGONALITY AND EIGENFUNCTIONS. Parts also proves "
                "the symmetry of differential operators "
                "($\\int u''v = \\int u v''$ plus boundary terms), which "
                "is why Sturm-Liouville eigenfunctions are orthogonal — "
                "the fact underlying Fourier series convergence. A "
                "formula you learned to integrate $x e^x$ turns out to "
                "organize the spectral theory of vibrating strings. Few "
                "ideas in this course scale so far."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "THE FORMULA. $\\int u\\,dv = uv - \\int v\\,du$; "
                "definite: $\\int_a^b u\\,dv = [uv]_a^b - "
                "\\int_a^b v\\,du$. Derived by integrating the product "
                "rule. No $+C$ when computing $v$; one $+C$ at the "
                "end.\n\n"
                "CHOOSING THE SPLIT. Principle: $u$ = the factor that "
                "IMPROVES when differentiated; $dv$ = a factor (with "
                "$dx$) that you can integrate. Default ranking for $u$: "
                "LIATE (Log, Inverse trig, Algebraic, Trig, "
                "Exponential) — a heuristic, overruled whenever "
                "substitution structure is present ($x e^{x^2}$!) or "
                "$dv$-integrability demands otherwise.\n\n"
                "THE FOUR PATTERNS.\n"
                "1. Polynomial × exp/trig: $u$ = polynomial; one round "
                "per degree; tabular method for degree ≥ 2 "
                "(derivatives column to 0, antiderivatives column, "
                "signs $+,-,+,\\ldots$, multiply diagonally).\n"
                "2. Lone log / inverse trig (or × polynomial): "
                "$u$ = log/inverse-trig, $dv = $ the rest (possibly "
                "just $dx$): $\\int\\ln x\\,dx = x\\ln x - x + C$; "
                "$\\int\\arctan x\\,dx = x\\arctan x - "
                "\\tfrac12\\ln(1+x^2) + C$.\n"
                "3. Exp × sin/cos: parts twice, same convention, solve "
                "for $I$:\n"
                "$$\\int e^{ax}\\sin bx\\,dx = "
                "\\frac{e^{ax}(a\\sin bx - b\\cos bx)}{a^2+b^2}+C, "
                "\\quad \\int e^{ax}\\cos bx\\,dx = "
                "\\frac{e^{ax}(a\\cos bx + b\\sin bx)}{a^2+b^2}+C.$$\n"
                "4. No visible pattern: substitute to CREATE one, then "
                "parts ($\\sin\\sqrt x$).\n\n"
                "REDUCTION FORMULAS. "
                "$\\int x^n e^x = x^n e^x - n\\int x^{n-1}e^x$; "
                "$\\int (\\ln x)^n = x(\\ln x)^n - n\\int(\\ln x)^{n-1}$.\n\n"
                "SANITY METER. Each round must simplify (degree drops, "
                "log vanishes) or boomerang (original returns with new "
                "coefficient — solve for it). Anything else: flip the "
                "split or change technique. Always differentiate the "
                "final answer."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all eight before reading the solutions.\n\n"
                "1. $\\int x e^{2x}\\,dx$?\n"
                "2. $\\int x^2 \\ln x\\,dx$?\n"
                "3. $\\int_0^1 \\arctan x\\,dx$?\n"
                "4. $\\int e^{-x}\\cos x\\,dx$?\n"
                "5. $\\int x^2 e^{-3x}\\,dx$ (tabular recommended)?\n"
                "6. $\\int \\frac{\\ln x}{x^2}\\,dx$?\n"
                "7. $\\int_0^{\\pi} x\\cos(2x)\\,dx$?\n"
                "8. True or false: LIATE always identifies the correct "
                "$u$. Give a counterexample if false.\n\n"
                "SOLUTIONS.\n"
                "1. $u = x$, $dv = e^{2x}dx$, $v = \\frac{e^{2x}}2$: "
                "$\\frac{xe^{2x}}{2} - \\frac{1}{2}\\int e^{2x}dx = "
                "e^{2x}\\big(\\frac{x}{2} - \\frac{1}{4}\\big) + C$.\n"
                "2. $u = \\ln x$ (L before A), $dv = x^2 dx$: "
                "$\\frac{x^3\\ln x}{3} - \\frac13\\int x^2 dx = "
                "\\frac{x^3 \\ln x}{3} - \\frac{x^3}{9} + C$.\n"
                "3. $u = \\arctan x$, $dv = dx$: "
                "$[x\\arctan x]_0^1 - \\int_0^1 \\frac{x}{1+x^2}dx = "
                "\\frac{\\pi}{4} - \\frac{\\ln 2}{2}$.\n"
                "4. Cyclic, $a = -1$, $b = 1$: "
                "$\\frac{e^{-x}(-\\cos x + \\sin x)}{2} + C = "
                "\\frac{e^{-x}(\\sin x - \\cos x)}{2} + C$.\n"
                "5. Table: $x^2, 2x, 2, 0$ against "
                "$-\\frac{e^{-3x}}{3}, \\frac{e^{-3x}}{9}, "
                "-\\frac{e^{-3x}}{27}$ with signs $+,-,+$: "
                "$-e^{-3x}\\big(\\frac{x^2}{3} + \\frac{2x}{9} + "
                "\\frac{2}{27}\\big) + C$.\n"
                "6. $u = \\ln x$, $dv = x^{-2}dx$, $v = -x^{-1}$: "
                "$-\\frac{\\ln x}{x} + \\int x^{-2}dx = "
                "-\\frac{\\ln x}{x} - \\frac{1}{x} + C$.\n"
                "7. $u = x$, $dv = \\cos 2x\\,dx$, "
                "$v = \\frac{\\sin 2x}{2}$: "
                "$\\big[\\frac{x\\sin 2x}{2}\\big]_0^{\\pi} - "
                "\\frac12\\int_0^\\pi \\sin 2x\\,dx = 0 + "
                "\\big[\\frac{\\cos 2x}{4}\\big]_0^{\\pi} = "
                "\\frac{1}{4} - \\frac{1}{4} = 0$.\n"
                "8. FALSE. $\\int x e^{x^2}dx$: LIATE nominates "
                "$u = x$, but the integral is a one-line substitution "
                "($\\frac{e^{x^2}}{2} + C$), and parts on it produces "
                "the non-elementary $\\int e^{x^2}dx$. Heuristic, not "
                "law.\n\n"
                "Scoring guide: 8/8 — proceed. 6-7 — reread pages "
                "matching your misses (signs → 13; cyclic → 9-11; "
                "choice of split → 5-6 and 8). ≤5 — rework pages 3-9 "
                "before practicing."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Before leaving this chapter you should be able to "
                "check every box honestly:\n\n"
                "□ Derive the parts formula from the product rule in "
                "two lines, unprompted.\n"
                "□ Split any product integrand into $u$ and $dv$ using "
                "the trade-downhill principle, citing LIATE only as a "
                "tiebreak.\n"
                "□ State when parts is the WRONG tool — substitution "
                "structure present — and check for that first.\n"
                "□ Execute repeated parts with clean sign propagation, "
                "and compress polynomial cases into a tabular "
                "computation.\n"
                "□ Run the boomerang: recognize a returning integral, "
                "keep conventions consistent across rounds, solve for "
                "$I$, and explain why coefficient $+1$ means you "
                "undid yourself.\n"
                "□ Integrate $\\ln x$, $(\\ln x)^2$, $\\arctan x$, and "
                "$\\arcsin x$ via the $dv = dx$ device.\n"
                "□ Handle definite integrals: evaluate $[uv]_a^b$ "
                "immediately and carry bounds through the remaining "
                "integral.\n"
                "□ Combine techniques: substitute to create a product, "
                "then parts; parts producing a substitution "
                "remainder.\n"
                "□ Reproduce the two cyclic formulas "
                "($e^{ax}\\sin bx$, $e^{ax}\\cos bx$) or re-derive "
                "them in under three minutes.\n"
                "□ Differentiate every answer as a reflex.\n\n"
                "When every box is checked, hit Practice until the "
                "$u$/$dv$ choice feels instant. Then proceed to C203, "
                "where products of SINES AND COSINES with themselves — "
                "powers like $\\sin^3 x\\cos^2 x$ — get their own "
                "dedicated identity-driven toolkit, with both C201 and "
                "this chapter doing the heavy lifting."
            )),
        ],
    },
    "C203": {
        "summary": (
            "A complete course on trigonometric integrals: powers and products "
            "of sine and cosine (odd-power splitting and even-power half-angle "
            "reduction), powers of secant and tangent with the strategy table "
            "by parity, the csc/cot mirror, products sin(mx)cos(nx) via "
            "product-to-sum identities, and the sec-cubed classic."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "You now own two general-purpose tools: substitution (C201) "
                "reverses the chain rule, parts (C202) reverses the product rule. "
                "This chapter is different in kind — it is a SPECIALIST'S toolkit "
                "for one family of integrands: products and powers of "
                "trigonometric functions. $\\int \\sin^3 x\\,dx$. "
                "$\\int \\sin^2 x\\cos^2 x\\,dx$. $\\int \\tan^3 x\\sec^4 x\\,dx$. "
                "$\\int \\sin 5x \\cos 3x\\,dx$. And the small legend that is "
                "$\\int \\sec^3 x\\,dx$.\n\n"
                "Why does one family deserve its own chapter? Two reasons. "
                "First, DEMAND: these integrals are not curiosities. Fourier "
                "analysis — the decomposition of every signal, sound, and image "
                "into sinusoids — runs on integrals of products of sines and "
                "cosines. Alternating-current power calculations average "
                "$\\sin^2$. And the very next chapter (C204) will convert "
                "integrals full of radicals into EXACTLY these trig integrals, "
                "so today's toolkit is tomorrow's engine room.\n\n"
                "Second, STRUCTURE: trig functions are entangled by identities "
                "— $\\sin^2 x + \\cos^2 x = 1$ and its relatives — and those "
                "identities let you RESHAPE integrands until a substitution "
                "fires. That is the chapter in one sentence: use identities to "
                "manufacture the composite-times-derivative pattern that C201 "
                "taught you to finish. No genuinely new integration happens "
                "here; what is new is a systematic, parity-driven strategy for "
                "the reshaping.\n\n"
                "You will learn: the odd-power split and even-power half-angle "
                "reduction for $\\sin^m x\\cos^n x$; the parallel system for "
                "$\\sec$/$\\tan$ (and its $\\csc$/$\\cot$ mirror); a strategy "
                "table you can reconstruct from two derivative facts; "
                "product-to-sum identities for $\\sin(mx)\\cos(nx)$; and the "
                "boomerang-powered $\\sec^3$ integral you will meet again in "
                "arc-length problems."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Four items, all load-bearing.\n\n"
                "1. THE PYTHAGOREAN IDENTITIES: "
                "$\\sin^2 x + \\cos^2 x = 1$, and its two divided forms "
                "$\\tan^2 x + 1 = \\sec^2 x$ and $1 + \\cot^2 x = \\csc^2 x$. "
                "You will use these to convert EVEN powers of one function "
                "into the other.\n\n"
                "2. THE HALF-ANGLE (POWER-REDUCTION) IDENTITIES:\n"
                "$$\\sin^2 x = \\frac{1 - \\cos 2x}{2}, \\qquad "
                "\\cos^2 x = \\frac{1 + \\cos 2x}{2}.$$\n"
                "These trade a SQUARE for a DOUBLED ANGLE — the only move "
                "that works when all powers are even.\n\n"
                "3. TRIG DERIVATIVES, instantly: "
                "$(\\sin x)' = \\cos x$, $(\\cos x)' = -\\sin x$, "
                "$(\\tan x)' = \\sec^2 x$, $(\\sec x)' = \\sec x\\tan x$, "
                "$(\\cot x)' = -\\csc^2 x$, $(\\csc x)' = -\\csc x\\cot x$. "
                "The whole strategy table of page 8 is generated by these "
                "derivative pairings.\n\n"
                "4. THE TOOLS: substitution mechanics (C201) and parts with "
                "the boomerang (C202) — $\\sec^3$ needs both.\n\n"
                "Quick self-check (answers follow):\n\n"
                "• Express $\\cos^2 x$ using $\\cos 2x$. "
                "($\\frac{1+\\cos 2x}{2}$.)\n"
                "• $\\frac{d}{dx}\\tan x$? ($\\sec^2 x$.)\n"
                "• $\\int \\cos(2x)\\,dx$? ($\\frac{\\sin 2x}{2} + C$ — "
                "linear-inside shortcut.)\n"
                "• Rewrite $\\sin^2 x$ in terms of $\\cos^2 x$. "
                "($1 - \\cos^2 x$.)\n"
                "• From C201: $\\int \\tan x\\,dx$? "
                "($-\\ln|\\cos x| + C = \\ln|\\sec x| + C$.)\n\n"
                "If the identities are rusty, rebuild rather than memorize: "
                "both half-angle formulas fall out of "
                "$\\cos 2x = \\cos^2 x - \\sin^2 x$ plus the Pythagorean "
                "identity in under a minute. This chapter rewards students "
                "who can DERIVE identities on demand, because exam pressure "
                "erases memorized ones."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Every technique in this chapter is the same three-beat "
                "move:\n\n"
                "RESHAPE with an identity → SET UP a substitution → FINISH "
                "with the power rule.\n\n"
                "Watch it once, slowly, on $\\int \\sin^3 x\\,dx$. As it "
                "stands, no substitution fires: $u = \\sin x$ needs a "
                "$\\cos x$ factor that is not there. But peel off ONE sine —\n"
                "$$\\sin^3 x = \\sin^2 x \\cdot \\sin x = "
                "(1 - \\cos^2 x)\\sin x$$\n"
                "— and the landscape changes completely. The integrand is "
                "now A FUNCTION OF $\\cos x$, times $\\sin x$ — and "
                "$\\sin x$ is (up to sign) exactly the derivative of "
                "$\\cos x$. That is C201's fingerprint. Substitute "
                "$u = \\cos x$, $du = -\\sin x\\,dx$:\n"
                "$$\\int (1 - u^2)(-du) = \\int (u^2 - 1)\\,du "
                "= \\frac{u^3}{3} - u + C = \\frac{\\cos^3 x}{3} - "
                "\\cos x + C.$$\n\n"
                "Notice what the odd power BOUGHT us. From $\\sin^3$ we "
                "spent one factor to serve as $du$, and the remaining "
                "$\\sin^2$ — an EVEN power — converted losslessly to "
                "cosines via Pythagoras. That accounting works whenever "
                "the power is odd: odd = one factor for the differential "
                "+ an even remainder for the identity. It fails when the "
                "power is even: spend one factor and an ODD remainder is "
                "left, which Pythagoras cannot convert cleanly.\n\n"
                "So parity is destiny, and the chapter's strategy divides "
                "there. ODD powers: split off a factor, convert, "
                "substitute — a chain-rule play. EVEN powers only: no "
                "factor can be spared, so change WEAPONS entirely — the "
                "half-angle identities lower the power at the cost of "
                "doubling the angle, and you repeat until integrable. "
                "Everything else in the chapter — the sec/tan system, the "
                "strategy table — is this parity logic applied to "
                "different derivative pairings."
            )),
            # ---------------------------------------------------------- 4
            (L, "The method for sin^m x cos^n x, stated fully", (
                "To evaluate $\\int \\sin^m x\\,\\cos^n x\\,dx$ with "
                "$m, n \\ge 0$ integers:\n\n"
                "CASE 1 — $n$ ODD (cosine power odd). Save one cosine "
                "factor for the differential; convert the remaining even "
                "cosine power to sines:\n"
                "$$\\int \\sin^m x\\,(1 - \\sin^2 x)^{\\frac{n-1}{2}}"
                "\\,\\cos x\\,dx, \\qquad u = \\sin x,\\; du = \\cos x\\,dx,$$\n"
                "yielding a polynomial integral in $u$.\n\n"
                "CASE 2 — $m$ ODD (sine power odd). Mirror image: save one "
                "sine, convert the rest to cosines, substitute "
                "$u = \\cos x$, $du = -\\sin x\\,dx$ (mind the sign).\n\n"
                "If BOTH are odd, either case works — choose the smaller "
                "power to save from, and expect the two correct answers to "
                "differ by a constant (page 11 exhibits this "
                "phenomenon).\n\n"
                "CASE 3 — BOTH EVEN. No factor can be spared. Apply the "
                "power-reduction identities\n"
                "$$\\sin^2 x = \\frac{1 - \\cos 2x}{2}, \\qquad "
                "\\cos^2 x = \\frac{1 + \\cos 2x}{2}, \\qquad "
                "\\sin x\\cos x = \\frac{\\sin 2x}{2},$$\n"
                "expand, and integrate term by term. Any surviving even "
                "powers (a $\\cos^2 2x$, say) get reduced AGAIN at angle "
                "$4x$; any odd powers that appear fall back to Cases "
                "1-2. Each reduction halves the power, so the recursion "
                "terminates quickly.\n\n"
                "WHY the case split is exhaustive: $m$ and $n$ are each "
                "odd or even, and the three cases cover all four "
                "combinations (both-odd lands in Cases 1-2 twice over). "
                "The method never fails on this family — "
                "$\\sin^m x\\cos^n x$ ALWAYS has an elementary "
                "antiderivative, in stark contrast to the fragile "
                "situation of C201's page 10. Structure this rigid is "
                "rare in integration; enjoy it.\n\n"
                "One practical note: for both-even integrands, expect the "
                "answer to contain $x$ itself (from integrating the "
                "constant $\\frac12$) — a visible fingerprint that "
                "half-angle reduction happened."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples", (
                "EXAMPLE 1 (odd cosine power). "
                "$\\int \\sin^2 x\\cos^3 x\\,dx$.\n\n"
                "Cosine power is odd: save one $\\cos x$, convert the "
                "remaining $\\cos^2 x = 1 - \\sin^2 x$:\n"
                "$$\\int \\sin^2 x\\,(1 - \\sin^2 x)\\,\\cos x\\,dx.$$\n"
                "Substitute $u = \\sin x$, $du = \\cos x\\,dx$:\n"
                "$$\\int u^2(1 - u^2)\\,du = \\frac{u^3}{3} - "
                "\\frac{u^5}{5} + C = \\frac{\\sin^3 x}{3} - "
                "\\frac{\\sin^5 x}{5} + C.$$\n"
                "Check by differentiating: $\\sin^2 x\\cos x - "
                "\\sin^4 x\\cos x = \\sin^2 x\\cos x(1 - \\sin^2 x) = "
                "\\sin^2 x\\cos^3 x$. Correct.\n\n"
                "EXAMPLE 2 (odd sine power — the sign case). "
                "$\\int \\sin^3 x\\,dx$, worked on page 3; here is the "
                "compressed replay to fix the pattern: "
                "$\\sin^3 x = (1 - \\cos^2 x)\\sin x$; $u = \\cos x$, "
                "$du = -\\sin x\\,dx$;\n"
                "$$-\\int (1 - u^2)\\,du = \\frac{u^3}{3} - u + C = "
                "\\frac{\\cos^3 x}{3} - \\cos x + C.$$\n"
                "The leading minus from $du$ is the step to respect.\n\n"
                "EXAMPLE 3 (both powers even — half-angle). "
                "$\\int \\cos^2 x\\,dx$.\n\n"
                "No spare factor exists ($\\cos^2$ alone, $m = 0$ even, "
                "$n = 2$ even). Reduce the power:\n"
                "$$\\int \\frac{1 + \\cos 2x}{2}\\,dx = \\frac{x}{2} + "
                "\\frac{\\sin 2x}{4} + C.$$\n"
                "Note both fingerprints of the even case: the bare "
                "$\\frac{x}{2}$ term, and the doubled angle. The "
                "companion $\\int \\sin^2 x\\,dx = \\frac{x}{2} - "
                "\\frac{\\sin 2x}{4} + C$ follows identically, and the "
                "pair should live in your permanent memory: their "
                "definite versions over $[0, 2\\pi]$ both equal $\\pi$ — "
                "'the average of $\\sin^2$ is $\\frac12$' — a fact "
                "page 15 turns into watts."
            )),
            # ---------------------------------------------------------- 6
            (L, "The sec/tan system (and the csc/cot mirror)", (
                "Secants and tangents form a second self-contained system, "
                "built on two facts:\n"
                "$$\\frac{d}{dx}\\tan x = \\sec^2 x, \\qquad "
                "\\frac{d}{dx}\\sec x = \\sec x\\tan x, \\qquad "
                "\\text{linked by} \\qquad \\sec^2 x = 1 + \\tan^2 x.$$\n"
                "For $\\int \\tan^m x\\sec^n x\\,dx$:\n\n"
                "CASE A — $n$ EVEN ($n \\ge 2$). Save a $\\sec^2 x$ for "
                "the differential; convert the remaining even secant "
                "power to tangents via $\\sec^2 = 1 + \\tan^2$; "
                "substitute $u = \\tan x$. Everything becomes a "
                "polynomial in $u$.\n\n"
                "CASE B — $m$ ODD (and $n \\ge 1$). Save a "
                "$\\sec x\\tan x$ pair for the differential; convert the "
                "remaining even tangent power via "
                "$\\tan^2 = \\sec^2 - 1$; substitute $u = \\sec x$.\n\n"
                "CASE C — $m$ EVEN and $n$ ODD (the hard corner, e.g. "
                "$\\sec^3 x$, $\\tan^2 x\\sec x$). Neither substitution "
                "fires. Convert everything to secants via "
                "$\\tan^2 = \\sec^2 - 1$ and handle the resulting odd "
                "secant powers with PARTS — page 9 works the $\\sec^3$ "
                "classic in full.\n\n"
                "Also in the kit: pure tangent powers "
                "($\\int \\tan^m x\\,dx$): peel off $\\tan^2 x = "
                "\\sec^2 x - 1$ to split into a $u = \\tan x$ piece plus "
                "a lower power — a two-line reduction. And the two base "
                "integrals every case eventually needs:\n"
                "$$\\int \\tan x\\,dx = \\ln|\\sec x| + C, \\qquad "
                "\\int \\sec x\\,dx = \\ln|\\sec x + \\tan x| + C,$$\n"
                "the first from C201, the second by the famous "
                "multiply-by-$\\frac{\\sec x + \\tan x}{\\sec x + \\tan x}$ "
                "trick — after which $u = \\sec x + \\tan x$ makes the "
                "numerator exactly $du$.\n\n"
                "THE MIRROR. Cosecant/cotangent duplicates all of this "
                "under the swaps $\\sec \\to \\csc$, $\\tan \\to \\cot$, "
                "with EXTRA MINUS SIGNS from $(\\cot x)' = -\\csc^2 x$ "
                "and $(\\csc x)' = -\\csc x\\cot x$. Same strategy, "
                "shadowed by sign care."
            )),
            # ---------------------------------------------------------- 7
            (E, "Worked examples: standard problems", (
                "EXAMPLE 1 (even secant power — Case A). "
                "$\\int \\tan^3 x\\sec^4 x\\,dx$.\n\n"
                "Save $\\sec^2 x$; convert the leftover "
                "$\\sec^2 x = 1 + \\tan^2 x$:\n"
                "$$\\int \\tan^3 x\\,(1 + \\tan^2 x)\\,\\sec^2 x\\,dx "
                "\\;\\xrightarrow{u = \\tan x}\\; \\int u^3(1 + u^2)\\,du "
                "= \\frac{u^4}{4} + \\frac{u^6}{6} + C,$$\n"
                "giving $\\frac{\\tan^4 x}{4} + \\frac{\\tan^6 x}{6} + C$.\n\n"
                "EXAMPLE 2 (full even-power reduction). "
                "$\\int \\sin^4 x\\,dx$.\n\n"
                "Both powers even ($m = 4$, $n = 0$): reduce twice.\n"
                "$$\\sin^4 x = \\Big(\\frac{1 - \\cos 2x}{2}\\Big)^2 = "
                "\\frac{1 - 2\\cos 2x + \\cos^2 2x}{4},$$\n"
                "and the surviving even power reduces again at the "
                "doubled angle: $\\cos^2 2x = \\frac{1 + \\cos 4x}{2}$. "
                "So the integrand is\n"
                "$$\\frac{1}{4} - \\frac{\\cos 2x}{2} + \\frac{1}{8} + "
                "\\frac{\\cos 4x}{8} = \\frac{3}{8} - \\frac{\\cos 2x}{2} "
                "+ \\frac{\\cos 4x}{8},$$\n"
                "and termwise integration gives\n"
                "$$\\int \\sin^4 x\\,dx = \\frac{3x}{8} - "
                "\\frac{\\sin 2x}{4} + \\frac{\\sin 4x}{32} + C.$$\n"
                "The cascade $2x \\to 4x$ with shrinking coefficients is "
                "what even-power answers always look like.\n\n"
                "EXAMPLE 3 (pure odd tangent power). "
                "$\\int \\tan^3 x\\,dx$.\n\n"
                "Peel: $\\tan^3 x = \\tan x(\\sec^2 x - 1)$:\n"
                "$$\\int \\tan x\\sec^2 x\\,dx - \\int \\tan x\\,dx "
                "= \\frac{\\tan^2 x}{2} + \\ln|\\cos x| + C,$$\n"
                "the first integral via $u = \\tan x$, the second from "
                "the base table. One peel, one split, two known "
                "integrals — the reduction pattern in miniature."
            )),
            # ---------------------------------------------------------- 8
            (L, "The strategy table, and products of different angles", (
                "Here is the whole chapter as a decision table. For "
                "$\\int \\sin^m x\\cos^n x\\,dx$:\n\n"
                "• $n$ odd → save $\\cos x$, rest to sines, "
                "$u = \\sin x$.\n"
                "• $m$ odd → save $\\sin x$, rest to cosines, "
                "$u = \\cos x$ (sign!).\n"
                "• both even → half-angle reduction, repeat as needed.\n\n"
                "For $\\int \\tan^m x\\sec^n x\\,dx$:\n\n"
                "• $n$ even → save $\\sec^2 x$, rest to tangents, "
                "$u = \\tan x$.\n"
                "• $m$ odd → save $\\sec x\\tan x$, rest to secants, "
                "$u = \\sec x$.\n"
                "• $m$ even, $n$ odd → convert all to secants; odd "
                "secant powers by parts (page 9).\n\n"
                "Do not memorize this table — RECONSTRUCT it. Each row "
                "asks one question: which substitution's $du$ can this "
                "integrand afford to donate? $u = \\sin$ costs a "
                "$\\cos$; $u = \\tan$ costs a $\\sec^2$; $u = \\sec$ "
                "costs a $\\sec\\tan$ pair. Parity determines which "
                "donation leaves a CONVERTIBLE (even) remainder. Two "
                "derivative facts and one identity regenerate every "
                "row.\n\n"
                "DIFFERENT ANGLES. Products like $\\sin(mx)\\cos(nx)$ "
                "with $m \\ne n$ defeat all of the above — no identity "
                "relates the two angles multiplicatively. The right "
                "tool is the PRODUCT-TO-SUM identities:\n"
                "$$\\sin A\\cos B = \\tfrac12[\\sin(A+B) + \\sin(A-B)], \\quad "
                "\\sin A\\sin B = \\tfrac12[\\cos(A-B) - \\cos(A+B)], \\quad "
                "\\cos A\\cos B = \\tfrac12[\\cos(A-B) + \\cos(A+B)].$$\n"
                "They convert the product into two easy single-angle "
                "integrals. Derive them on demand by adding/subtracting "
                "the angle-addition formulas — thirty seconds, no "
                "memory risk. These identities are also the "
                "mathematical content of 'beats' in acoustics and of "
                "orthogonality in Fourier analysis (page 10)."
            )),
            # ---------------------------------------------------------- 9
            (E, "Worked examples: harder problems", (
                "EXAMPLE 1 (the $\\sec^3$ classic — Case C by parts). "
                "$I = \\int \\sec^3 x\\,dx$.\n\n"
                "Split $\\sec^3 = \\sec x \\cdot \\sec^2 x$ and take "
                "$u = \\sec x$, $dv = \\sec^2 x\\,dx$; then "
                "$du = \\sec x\\tan x\\,dx$, $v = \\tan x$:\n"
                "$$I = \\sec x\\tan x - \\int \\sec x\\tan^2 x\\,dx "
                "= \\sec x\\tan x - \\int \\sec x(\\sec^2 x - 1)\\,dx,$$\n"
                "using $\\tan^2 = \\sec^2 - 1$. The right side contains "
                "$\\int \\sec^3 x\\,dx = I$ again — a boomerang:\n"
                "$$I = \\sec x\\tan x - I + \\int \\sec x\\,dx "
                "\\;\\Longrightarrow\\; 2I = \\sec x\\tan x + "
                "\\ln|\\sec x + \\tan x|,$$\n"
                "$$I = \\frac{\\sec x\\tan x + \\ln|\\sec x + \\tan x|}{2} "
                "+ C.$$\n"
                "Memorize the RESULT and the ROUTE: C204's arc-length and "
                "area problems will hand you $\\sec^3$ repeatedly.\n\n"
                "EXAMPLE 2 (product-to-sum). "
                "$\\int \\sin 5x\\cos 3x\\,dx$.\n\n"
                "With $A = 5x$, $B = 3x$: $\\sin 5x\\cos 3x = "
                "\\tfrac12[\\sin 8x + \\sin 2x]$. So\n"
                "$$\\int \\sin 5x\\cos 3x\\,dx = \\frac{1}{2}\\Big(-"
                "\\frac{\\cos 8x}{8} - \\frac{\\cos 2x}{2}\\Big) + C = "
                "-\\frac{\\cos 8x}{16} - \\frac{\\cos 2x}{4} + C.$$\n"
                "Two lines, once the identity does the work.\n\n"
                "EXAMPLE 3 (even-even with a shortcut identity). "
                "$\\int \\sin^2 x\\cos^2 x\\,dx$.\n\n"
                "Rather than reducing each square separately, spot "
                "$\\sin x\\cos x = \\frac{\\sin 2x}{2}$:\n"
                "$$\\int \\Big(\\frac{\\sin 2x}{2}\\Big)^2 dx = "
                "\\frac14\\int \\sin^2 2x\\,dx = "
                "\\frac14\\int \\frac{1 - \\cos 4x}{2}\\,dx = "
                "\\frac{x}{8} - \\frac{\\sin 4x}{32} + C.$$\n"
                "Same destination as the long route, half the algebra. "
                "Identities are interchangeable roads; experienced "
                "integrators pick the shortest, and checking by "
                "differentiation makes any road safe."
            )),
            # ---------------------------------------------------------- 10
            (L, "Special cases and edge behavior", (
                "ORTHOGONALITY — the definite-integral crown jewels. For "
                "positive integers $m \\ne n$:\n"
                "$$\\int_{-\\pi}^{\\pi} \\sin mx\\sin nx\\,dx = 0, \\qquad "
                "\\int_{-\\pi}^{\\pi} \\cos mx\\cos nx\\,dx = 0, \\qquad "
                "\\int_{-\\pi}^{\\pi} \\sin mx\\cos nx\\,dx = 0,$$\n"
                "the last holding for ALL $m, n$ (odd integrand). Proof: "
                "product-to-sum converts each to integrals of "
                "$\\cos(kx)$ or $\\sin(kx)$ with $k \\ne 0$ over full "
                "periods — all zero. When $m = n$, the sin-sin and "
                "cos-cos integrals equal $\\pi$ (the $\\cos(A-B)$ term "
                "becomes $\\cos 0 = 1$). Distinct frequencies are "
                "'perpendicular'; a frequency against itself has "
                "'length' $\\sqrt\\pi$. This is the geometry on which "
                "Fourier series stand, and it fell out of page 8's "
                "identities.\n\n"
                "FULL-PERIOD SHORTCUTS. Over $[0, 2\\pi]$: any "
                "$\\int \\sin^{\\text{odd}}$ or $\\int\\cos^{\\text{odd}}$ "
                "vanishes by symmetry; $\\int_0^{2\\pi}\\sin^2 = "
                "\\int_0^{2\\pi}\\cos^2 = \\pi$ (they must be equal by a "
                "quarter-period shift and sum to $\\int_0^{2\\pi} 1 = "
                "2\\pi$). Check symmetry BEFORE computing; half of "
                "periodic definite integrals are free.\n\n"
                "TWO ANSWERS, BOTH RIGHT. When both powers are odd, the "
                "two available substitutions give answers that look "
                "different — $\\frac{\\tan^4 x}{4} + \\frac{\\tan^6x}{6}$ "
                "versus $\\frac{\\sec^6 x}{6} - \\frac{\\sec^4 x}{4}$ for "
                "page 7's Example 1 — yet differ by a CONSTANT, as "
                "antiderivatives may. Page 11 verifies one such pair "
                "explicitly. Never grade yourself wrong by comparing "
                "forms; compare derivatives.\n\n"
                "RADICALS OF SQUARES. Simplifying "
                "$\\sqrt{\\sin^2 x} = |\\sin x|$ (NOT $\\sin x$) matters "
                "the moment your interval lets the function go negative "
                "— page 11's case (c) shows a definite integral where "
                "forgetting the absolute value silently halves the "
                "answer."
            )),
            # ---------------------------------------------------------- 11
            (E, "Worked example: edge cases diagnosed", (
                "CASE (a) — two methods, two faces, one antiderivative "
                "family. $\\int \\tan x\\sec^2 x\\,dx$.\n\n"
                "Route 1, $u = \\tan x$: $\\int u\\,du = "
                "\\frac{\\tan^2 x}{2} + C$. Route 2, $u = \\sec x$, "
                "$du = \\sec x\\tan x\\,dx$: $\\int u\\,du = "
                "\\frac{\\sec^2 x}{2} + C$. Contradiction? Subtract:\n"
                "$$\\frac{\\sec^2 x}{2} - \\frac{\\tan^2 x}{2} = "
                "\\frac{\\sec^2 x - \\tan^2 x}{2} = \\frac{1}{2},$$\n"
                "a constant — absorbed by $C$. Both answers are fully "
                "correct. Diagnosis: expect this whenever multiple "
                "substitutions apply; verify by differentiating, not by "
                "matching forms.\n\n"
                "CASE (b) — symmetry before labor. "
                "$\\int_{-\\pi}^{\\pi} \\sin 3x\\cos 5x\\,dx$.\n\n"
                "The integrand is odd (odd × even) on a symmetric "
                "interval: the answer is 0 with NO integration. "
                "Product-to-sum confirms: "
                "$\\tfrac12[\\sin 8x + \\sin(-2x)]$, both integrating to "
                "cosines evaluated over full periods — zero. Two "
                "methods, one answer, thirty seconds saved.\n\n"
                "CASE (c) — the absolute-value trap. "
                "$\\int_0^{\\pi} \\sqrt{1 + \\cos 2x}\\,dx$.\n\n"
                "Reshape with the half-angle identity RUN BACKWARDS: "
                "$1 + \\cos 2x = 2\\cos^2 x$, so the integrand is "
                "$\\sqrt{2}\\,|\\cos x|$ — with bars, because on "
                "$(\\frac{\\pi}{2}, \\pi]$ the cosine is negative. "
                "Split at the sign change:\n"
                "$$\\sqrt2\\int_0^{\\pi}|\\cos x|\\,dx = "
                "\\sqrt2\\Big(\\int_0^{\\pi/2}\\cos x\\,dx - "
                "\\int_{\\pi/2}^{\\pi}\\cos x\\,dx\\Big) = "
                "\\sqrt2\\,(1 + 1) = 2\\sqrt2.$$\n"
                "Dropping the bars gives $\\sqrt2(1 - 1)\\cdot\\ldots$ — "
                "actually $\\sqrt{2}\\int_0^\\pi \\cos x\\,dx = 0$: a "
                "plainly absurd zero area for a positive integrand. The "
                "absurdity is the diagnostic: positive integrand, "
                "positive answer, always."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'Integrate powers like polynomials: "
                "$\\int \\sin^2 x\\,dx = \\frac{\\sin^3 x}{3} + C$.' The "
                "power rule applies to $\\int u^n\\,du$ — and $du$ here "
                "would be $\\cos x\\,dx$, which is ABSENT. Differentiate "
                "the claim: $\\sin^2 x\\cos x \\ne \\sin^2 x$. Every "
                "power of a trig function must go through this "
                "chapter's machinery; there are no shortcuts by "
                "analogy.\n\n"
                "MISCONCEPTION 2: 'The parity rules are arbitrary "
                "memorization.' They are three derivative facts in "
                "disguise: $u = \\sin$ costs a $\\cos$ factor; "
                "$u = \\tan$ costs $\\sec^2$; $u = \\sec$ costs "
                "$\\sec\\tan$. Odd powers can pay the toll and leave an "
                "even, convertible remainder; even powers cannot. "
                "Students who learn the WHY reconstruct the table under "
                "pressure; students who memorize rows scramble them.\n\n"
                "MISCONCEPTION 3: 'Half-angle identities are "
                "approximations.' $\\sin^2 x = \\frac{1-\\cos 2x}{2}$ is "
                "an exact identity, valid for every $x$ — nothing was "
                "rounded. (The confusion imports 'small-angle "
                "approximation' vibes from physics.) Identities are "
                "REWRITES; the integrand never changes value.\n\n"
                "MISCONCEPTION 4: 'My answer differs from the key, so "
                "I'm wrong.' Trig antiderivatives wear many equivalent "
                "costumes ($\\frac{\\tan^2 x}{2}$ vs "
                "$\\frac{\\sec^2 x}{2}$; half-angle vs double-angle "
                "forms). The equality test is differentiation, or "
                "subtracting and simplifying to a constant — page 11, "
                "case (a).\n\n"
                "MISCONCEPTION 5: '$\\sin(5x)\\cos(3x)$ can be handled "
                "by $u$-substitution on one factor.' No: the angles "
                "differ, so neither factor's derivative appears. "
                "Different angles REQUIRE product-to-sum first. "
                "Matching angles is the hidden hypothesis of the whole "
                "parity table."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational errors", (
                "ERROR 1: THE MISSING 2. Writing "
                "$\\sin^2 x = \\frac{1 - \\cos x}{2}$ — the identity has "
                "$\\cos 2x$, doubled angle, not $\\cos x$. Prevention: "
                "re-derive from $\\cos 2x = 1 - 2\\sin^2 x$ whenever "
                "unsure; the derivation carries the 2 visibly.\n\n"
                "ERROR 2: CHAIN-RULE CONSTANTS AT DOUBLED ANGLES. "
                "$\\int \\cos 2x\\,dx = \\frac{\\sin 2x}{2} + C$, and "
                "after two reductions $\\int \\cos 4x\\,dx = "
                "\\frac{\\sin 4x}{4} + C$. Each doubled angle brings its "
                "own divisor; even-power answers stack them "
                "($\\frac{\\sin 4x}{32}$ in $\\sin^4$'s answer has a "
                "$\\frac18$ from expansion times $\\frac14$ from the "
                "angle).\n\n"
                "ERROR 3: THE $u = \\cos x$ SIGN. $du = -\\sin x\\,dx$: "
                "the minus must enter the moment you substitute, not be "
                "recalled later. Symptom: answers like "
                "$\\cos x - \\frac{\\cos^3 x}{3}$ (differentiate: gives "
                "$-\\sin^3 x$, wrong sign).\n\n"
                "ERROR 4: MISREMEMBERED PRODUCT-TO-SUM. The three "
                "formulas are easily scrambled (which gets the minus? "
                "sines or cosines on the right?). Prevention: derive "
                "from the addition formulas — add "
                "$\\sin(A+B) = \\sin A\\cos B + \\cos A\\sin B$ to "
                "$\\sin(A-B) = \\sin A\\cos B - \\cos A\\sin B$ and the "
                "cross terms cancel before your eyes.\n\n"
                "ERROR 5: DERIVATIVE MIX-UPS IN THE SEC/TAN SYSTEM. "
                "$(\\sec x)' = \\sec x\\tan x$, not $\\sec^2 x$ — that "
                "one belongs to $\\tan$. The mirror family adds minus "
                "signs: $(\\cot x)' = -\\csc^2 x$. One wrong derivative "
                "corrupts an entire parity decision.\n\n"
                "ERROR 6: DROPPED ABSOLUTE VALUES: in "
                "$\\ln|\\sec x + \\tan x|$ and in "
                "$\\sqrt{\\cos^2 x} = |\\cos x|$ (page 11c). The "
                "universal audit applies: differentiate the answer; for "
                "definite integrals, check the SIGN of the result "
                "against the sign of the integrand."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications", (
                "1. AC POWER AND RMS (electrical engineering). Mains "
                "electricity delivers $v(t) = V_0\\sin(\\omega t)$; the "
                "power dissipated in a resistor is proportional to "
                "$v^2$, so the ENERGY per cycle involves "
                "$\\int \\sin^2$ — the even-power reduction of page 5. "
                "The root-mean-square voltage — the '120 V' on the "
                "outlet — is defined through exactly this integral: "
                "$V_{rms} = V_0/\\sqrt2$ BECAUSE the average of "
                "$\\sin^2$ over a cycle is $\\frac12$. Page 15 computes "
                "a full power problem.\n\n"
                "2. FOURIER ANALYSIS (signal processing, PDEs). To "
                "extract the amplitude of the $n$-th harmonic from a "
                "signal $f$, you integrate $f(t)\\sin(nt)$ or "
                "$f(t)\\cos(nt)$ over a period. That the harmonics can "
                "be extracted INDEPENDENTLY is page 10's "
                "orthogonality — sin-times-cos integrals of different "
                "frequencies vanish. Every equalizer, every JPEG, every "
                "spectrogram rests on integrals from this chapter.\n\n"
                "3. BEATS AND MODULATION (acoustics, radio). "
                "Product-to-sum is physics: two tones multiplied (or "
                "superposed and squared in the ear) produce sum and "
                "difference frequencies — the 'beat' a piano tuner "
                "listens for, and the sidebands of AM radio. The "
                "identity $\\sin A\\cos B = \\frac12[\\sin(A{+}B) + "
                "\\sin(A{-}B)]$ is literally how a mixer circuit "
                "works.\n\n"
                "4. GEOMETRY FEEDSTOCK (C204, C207-C208). Trig "
                "substitution turns $\\sqrt{a^2 - x^2}$-type radicals "
                "into powers of $\\sin, \\cos, \\sec, \\tan$; arc "
                "lengths of parabolas produce $\\sec^3$; surface areas "
                "produce mixed powers. Today's toolkit is the "
                "downstream processor for the next three chapters' "
                "geometry — thin motivation now, constant use within "
                "two weeks."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: average AC power with a phase lag", (
                "A resistive-inductive load draws current lagging the "
                "voltage by phase $\\varphi$: "
                "$v(t) = V_0\\sin(\\omega t)$, "
                "$i(t) = I_0\\sin(\\omega t - \\varphi)$. Instantaneous "
                "power is $p(t) = v(t)\\,i(t)$. QUESTION: find the "
                "average power over one period "
                "$T = \\frac{2\\pi}{\\omega}$, then evaluate for "
                "$V_0 = 170$ V, $I_0 = 10$ A, $\\varphi = 30°$.\n\n"
                "STEP 1 — set up.\n"
                "$$P_{avg} = \\frac{1}{T}\\int_0^{T} V_0 I_0 "
                "\\sin(\\omega t)\\sin(\\omega t - \\varphi)\\,dt.$$\n\n"
                "STEP 2 — reshape (different 'angles' → product-to-sum). "
                "With $A = \\omega t$, $B = \\omega t - \\varphi$:\n"
                "$$\\sin A\\sin B = \\tfrac12[\\cos(A - B) - "
                "\\cos(A + B)] = \\tfrac12[\\cos\\varphi - "
                "\\cos(2\\omega t - \\varphi)].$$\n"
                "The product of two oscillations is a CONSTANT plus a "
                "double-frequency ripple.\n\n"
                "STEP 3 — integrate over the period. The constant term "
                "contributes $\\frac{\\cos\\varphi}{2}\\cdot T$; the "
                "ripple $\\cos(2\\omega t - \\varphi)$ completes exactly "
                "two full cycles in $[0, T]$ and integrates to ZERO "
                "(page 10's full-period shortcut). Hence\n"
                "$$P_{avg} = \\frac{V_0 I_0}{2}\\cos\\varphi.$$\n\n"
                "STEP 4 — evaluate. "
                "$P_{avg} = \\frac{170 \\times 10}{2}\\cos 30° = "
                "850 \\times 0.866 \\approx 736$ W.\n\n"
                "STEP 5 — interpret. The factor $\\cos\\varphi$ is the "
                "POWER FACTOR on industrial electric bills: at "
                "$\\varphi = 0$ the full $850$ W flows; at "
                "$\\varphi = 90°$ the average is ZERO — current sloshes "
                "without delivering energy. One product-to-sum identity "
                "plus one full-period-vanishing argument produced the "
                "central formula of AC power engineering. Notice no "
                "antiderivative of the ripple was ever needed — "
                "structure did the integrating."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections", (
                "BACKWARD. The engine here is C201's substitution — "
                "every parity rule exists to manufacture a $du$ — with "
                "C202's parts summoned for the odd-secant corner "
                "($\\sec^3$, a boomerang). The identities themselves "
                "come from the PRECALC trig track; if this chapter "
                "exposed identity rust, that is the node to revisit.\n\n"
                "FORWARD, immediately:\n"
                "• C204 (trig substitution) is the main customer: "
                "$x = a\\sin\\theta$ turns $\\sqrt{a^2 - x^2}$ integrals "
                "into THIS chapter's $\\cos^2\\theta$ and "
                "$\\sec^3\\theta$ problems. Arrive fluent or C204 "
                "stalls at its midpoint every time.\n"
                "• C205 (partial fractions) completes the technique "
                "core; its strategy overview assumes you can classify "
                "trig integrands on sight.\n"
                "• C206-C208: improper integrals and the geometry "
                "applications (areas, volumes, arc length, surface "
                "area) draw on $\\sin^2$, $\\cos^2$, and $\\sec^3$ "
                "results constantly — the $\\sec^3$ formula reappears "
                "in the arc length of a parabola and the area under a "
                "hyperbola.\n\n"
                "FURTHER OUT. Orthogonality (page 10) is the "
                "foundation stone of Fourier series, which the "
                "analysis track builds into the study of heat flow, "
                "waves, and signal processing; there, TODAY'S definite "
                "integrals become the inner products of an "
                "infinite-dimensional geometry (REALAN treats the "
                "convergence questions). The Wallis formulas — "
                "$\\int_0^{\\pi/2}\\sin^n x\\,dx$ computed by parts-"
                "powered reduction — lead to the Wallis product for "
                "$\\pi$ and appear again in probability (PF.U1.N2) "
                "inside the normalization of the normal distribution. "
                "In ODE.U1.N1 and beyond, forced-oscillation problems "
                "integrate products of sinusoids exactly as page 15 "
                "did."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: orthogonality as geometry, and Wallis", (
                "THE INNER-PRODUCT VIEWPOINT. Define, for functions on "
                "$[-\\pi, \\pi]$,\n"
                "$$\\langle f, g\\rangle = \\int_{-\\pi}^{\\pi} "
                "f(x)g(x)\\,dx.$$\n"
                "This behaves exactly like the dot product of vectors: "
                "linear in each slot, positive on nonzero functions. "
                "Page 10's orthogonality relations then say something "
                "geometric: the functions $\\{\\sin nx, \\cos nx\\}$ "
                "form a system of mutually PERPENDICULAR directions in "
                "an infinite-dimensional space, each of 'length' "
                "$\\sqrt{\\pi}$. Fourier's claim — heretical in 1807, "
                "orthodoxy now — is that these directions SPAN: "
                "essentially any periodic signal is a (possibly "
                "infinite) linear combination, and the coefficient on "
                "each direction is found the way you find any "
                "coordinate: project. "
                "$a_n = \\frac{1}{\\pi}\\langle f, \\cos nx\\rangle$ is "
                "a dot product divided by a squared length. The "
                "convergence subtleties occupied analysts for a "
                "century (REALAN tells that story — Dirichlet, "
                "Riemann, and ultimately Carleson 1966), but the "
                "algebraic skeleton is the trig integrals you learned "
                "this week.\n\n"
                "WALLIS, 1656 — BEFORE CALCULUS. The reduction formula "
                "$\\int_0^{\\pi/2}\\sin^n x\\,dx = \\frac{n-1}{n}"
                "\\int_0^{\\pi/2}\\sin^{n-2}x\\,dx$ (provable by parts) "
                "generates the sequence $\\frac{\\pi}{2}, 1, "
                "\\frac{\\pi}{4}, \\frac{2}{3}, \\frac{3\\pi}{16}, "
                "\\ldots$, and comparing even against odd terms yields "
                "the WALLIS PRODUCT:\n"
                "$$\\frac{\\pi}{2} = \\frac{2}{1}\\cdot\\frac{2}{3}"
                "\\cdot\\frac{4}{3}\\cdot\\frac{4}{5}\\cdot\\frac{6}{5}"
                "\\cdot\\frac{6}{7}\\cdots$$\n"
                "— $\\pi$ as an infinite product of rationals, "
                "discovered by audacious pattern-matching decades "
                "before Newton. The same integrals normalize the "
                "Gaussian in probability and give Stirling's formula "
                "its $\\sqrt{2\\pi}$. A humble reduction formula, "
                "compounding across three centuries."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "IDENTITY BOX. $\\sin^2 + \\cos^2 = 1$; "
                "$\\tan^2 + 1 = \\sec^2$; $1 + \\cot^2 = \\csc^2$; "
                "$\\sin^2 x = \\frac{1 - \\cos 2x}{2}$; "
                "$\\cos^2 x = \\frac{1 + \\cos 2x}{2}$; "
                "$\\sin x\\cos x = \\frac{\\sin 2x}{2}$; product-to-sum "
                "(derive from addition formulas): "
                "$\\sin A\\cos B = \\frac12[\\sin(A{+}B) + "
                "\\sin(A{-}B)]$, $\\sin A\\sin B = "
                "\\frac12[\\cos(A{-}B) - \\cos(A{+}B)]$, "
                "$\\cos A\\cos B = \\frac12[\\cos(A{-}B) + "
                "\\cos(A{+}B)]$.\n\n"
                "STRATEGY: $\\sin^m\\cos^n$ — $n$ odd: save "
                "$\\cos$, $u = \\sin x$; $m$ odd: save $\\sin$, "
                "$u = \\cos x$ (sign!); both even: half-angle, "
                "repeat. $\\tan^m\\sec^n$ — $n$ even: save "
                "$\\sec^2$, $u = \\tan x$; $m$ odd: save "
                "$\\sec\\tan$, $u = \\sec x$; $m$ even & $n$ odd: all "
                "to secants, then parts. csc/cot: mirror with extra "
                "minus signs. Different angles: product-to-sum, "
                "always.\n\n"
                "BASE INTEGRALS. $\\int\\tan = \\ln|\\sec x| + C$; "
                "$\\int\\sec = \\ln|\\sec x + \\tan x| + C$; "
                "$\\int\\sin^2 = \\frac{x}{2} - \\frac{\\sin 2x}{4} + C$; "
                "$\\int\\cos^2 = \\frac{x}{2} + \\frac{\\sin 2x}{4} + C$;\n"
                "$$\\int \\sec^3 x\\,dx = \\frac{\\sec x\\tan x + "
                "\\ln|\\sec x + \\tan x|}{2} + C.$$\n\n"
                "DEFINITE SHORTCUTS. Full-period integrals of "
                "$\\sin(kx)$, $\\cos(kx)$ vanish; "
                "$\\int_0^{2\\pi}\\sin^2 = \\int_0^{2\\pi}\\cos^2 = \\pi$; "
                "orthogonality kills mixed products of distinct "
                "frequencies over $[-\\pi,\\pi]$; odd integrands on "
                "symmetric intervals give 0.\n\n"
                "AUDITS. Two correct answers may differ by a constant. "
                "$\\sqrt{\\cos^2} = |\\cos|$. Positive integrand ⟹ "
                "positive definite integral. And always: differentiate "
                "the answer."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all eight before reading the solutions.\n\n"
                "1. $\\int \\cos^3 x\\,dx$?\n"
                "2. $\\int \\sin^2(3x)\\,dx$?\n"
                "3. $\\int \\tan^2 x\\,dx$?\n"
                "4. $\\int \\sec^4 x\\,dx$?\n"
                "5. $\\int \\sin 4x\\sin 6x\\,dx$?\n"
                "6. $\\int_0^{\\pi/2} \\sin^5 x\\,dx$?\n"
                "7. $\\int \\tan x\\sec^3 x\\,dx$?\n"
                "8. $\\int_{-\\pi}^{\\pi} \\sin 3x\\cos 5x\\,dx$?\n\n"
                "SOLUTIONS.\n"
                "1. Odd cosine: $\\cos^3 = (1 - \\sin^2)\\cos$; "
                "$u = \\sin x$: $\\sin x - \\frac{\\sin^3 x}{3} + C$.\n"
                "2. Half-angle at angle $6x$: "
                "$\\int\\frac{1 - \\cos 6x}{2}dx = \\frac{x}{2} - "
                "\\frac{\\sin 6x}{12} + C$.\n"
                "3. $\\tan^2 = \\sec^2 - 1$: $\\tan x - x + C$.\n"
                "4. Save $\\sec^2$: $\\int(1 + \\tan^2 x)\\sec^2 x\\,dx$; "
                "$u = \\tan x$: $\\tan x + \\frac{\\tan^3 x}{3} + C$.\n"
                "5. Product-to-sum: $\\sin 4x\\sin 6x = "
                "\\frac12[\\cos 2x - \\cos 10x]$: "
                "$\\frac{\\sin 2x}{4} - \\frac{\\sin 10x}{20} + C$.\n"
                "6. Odd power: $(1 - \\cos^2 x)^2\\sin x$; $u = \\cos x$, "
                "bounds $1 \\to 0$: $\\int_0^1 (1 - u^2)^2 du = "
                "1 - \\frac23 + \\frac15 = \\frac{8}{15}$.\n"
                "7. Odd tangent power with secant present: save "
                "$\\sec x\\tan x$: $\\int \\sec^2 x\\,(\\sec x\\tan x\\,"
                "dx)$; $u = \\sec x$: $\\frac{\\sec^3 x}{3} + C$.\n"
                "8. Odd integrand ($\\sin 3x\\cos 5x$ is odd) on a "
                "symmetric interval: $0$, no computation. "
                "(Orthogonality agrees.)\n\n"
                "Scoring guide: 8/8 — proceed. 6-7 — reread the pages "
                "matching your misses (parity choices → 4, 6, 8; "
                "half-angle slips → 13; products of different angles "
                "→ 8-9). ≤5 — rework pages 3-9, re-deriving each "
                "identity you had to look up."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Before leaving this chapter you should be able to "
                "check every box honestly:\n\n"
                "□ Re-derive the half-angle and product-to-sum "
                "identities from $\\cos 2x = \\cos^2 x - \\sin^2 x$ "
                "and the addition formulas, in under a minute "
                "each.\n"
                "□ Classify $\\int\\sin^m x\\cos^n x\\,dx$ by parity "
                "and execute all three cases, including the "
                "$u = \\cos x$ sign.\n"
                "□ Explain WHY parity decides — which substitution's "
                "toll the integrand can pay — rather than reciting "
                "the table.\n"
                "□ Run the sec/tan system: save $\\sec^2$ for "
                "$u = \\tan$, save $\\sec\\tan$ for $u = \\sec$, and "
                "identify the even-$m$/odd-$n$ hard corner on "
                "sight.\n"
                "□ Reproduce $\\int\\sec x\\,dx$ (conjugate trick) and "
                "$\\int\\sec^3 x\\,dx$ (parts + boomerang) from "
                "scratch.\n"
                "□ Convert $\\sin(mx)\\cos(nx)$-type products via "
                "product-to-sum and integrate the result.\n"
                "□ Deploy the definite shortcuts: full-period "
                "vanishing, $\\int_0^{2\\pi}\\sin^2 = \\pi$, "
                "orthogonality, odd-function symmetry.\n"
                "□ Handle $\\sqrt{\\text{trig}^2}$ with absolute "
                "values and split intervals at sign changes.\n"
                "□ Accept form-different answers after verifying they "
                "differ by a constant.\n"
                "□ State where this toolkit feeds next: C204's "
                "radical-clearing substitutions and the Fourier "
                "integrals beyond.\n\n"
                "When every box is checked, hit Practice until parity "
                "classification is instant. Then proceed to C204, "
                "where radicals like $\\sqrt{a^2 - x^2}$ meet the "
                "substitutions that turn them into THIS chapter's "
                "integrals."
            )),
        ],
    },
    "C204": {
        "summary": (
            "A complete course on trigonometric substitution: clearing the "
            "radicals sqrt(a^2-x^2), sqrt(a^2+x^2), and sqrt(x^2-a^2) with "
            "sine, tangent, and secant substitutions, the reference-triangle "
            "method for converting back, completing the square first, "
            "domain and sign care with the secant case, and recognizing when "
            "ordinary substitution is faster."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "One family of integrands has shrugged off every technique so "
                "far: RADICALS OF QUADRATICS. Try $\\int \\sqrt{1 - x^2}\\,dx$ "
                "with your current tools. Substitution $u = 1 - x^2$ demands an "
                "$x\\,dx$ that is not there; parts goes in circles. Yet this "
                "integrand is not exotic — its graph is the upper unit "
                "semicircle, and the integral from $-1$ to $1$ must be "
                "$\\frac{\\pi}{2}$. An area we have known since childhood is "
                "unreachable by chapters C201-C203. Something is missing.\n\n"
                "What is missing is a substitution run in REVERSE. In C201 you "
                "set $u = g(x)$ to simplify the inside of a composite. Here you "
                "will set $x = g(\\theta)$ — describing $x$ in terms of a new "
                "variable — chosen so that a Pythagorean identity COLLAPSES THE "
                "RADICAL. Put $x = \\sin\\theta$ into $\\sqrt{1 - x^2}$ and you "
                "get $\\sqrt{1 - \\sin^2\\theta} = \\sqrt{\\cos^2\\theta} = "
                "\\cos\\theta$: the square root is gone, and what remains is a "
                "trigonometric integral — which is exactly why C203 came "
                "first.\n\n"
                "The geometry is not a coincidence. These radicals ARE circles "
                "and hyperbolas ($\\sqrt{a^2 - x^2}$ is a circle's height, "
                "$\\sqrt{x^2 - a^2}$ a hyperbola's), and sine, tangent, and "
                "secant are precisely the functions that parametrize those "
                "curves. Trig substitution is analytic geometry paying rent.\n\n"
                "In this chapter: the three standard substitutions and exactly "
                "when each applies; the reference-triangle method that "
                "converts answers back to $x$ without memorizing inverse-trig "
                "gymnastics; completing the square to expose hidden standard "
                "forms; the domain and sign care the secant case demands; and "
                "the judgment call — recognizing the many integrals where "
                "plain C201 substitution is faster and trig substitution is a "
                "scenic detour."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "This chapter braids three earlier skill sets together; "
                "check each strand.\n\n"
                "1. TRIG INTEGRALS (C203). After the substitution clears the "
                "radical, you must integrate $\\cos^2\\theta$, "
                "$\\sec^3\\theta$, $\\tan^2\\theta$, and friends. Keep "
                "page 18 of C203 within reach: "
                "$\\int\\cos^2 = \\frac{\\theta}{2} + "
                "\\frac{\\sin 2\\theta}{4} + C$ and the $\\sec^3$ formula "
                "get heavy use.\n\n"
                "2. SUBSTITUTION MECHANICS (C201), including the definite-"
                "integral bound-conversion rule — with $x = g(\\theta)$ the "
                "conversion runs through $g$'s INVERSE, so comfort with "
                "$\\arcsin$ and $\\arctan$ values is required.\n\n"
                "3. INVERSE TRIG AND RIGHT TRIANGLES. Given "
                "$\\sin\\theta = \\frac{x}{3}$ with $\\theta$ acute, you "
                "should be able to draw the right triangle (opposite $x$, "
                "hypotenuse 3, adjacent $\\sqrt{9 - x^2}$) and read off "
                "every other trig function of $\\theta$. This one skill IS "
                "the back-conversion method of page 6.\n\n"
                "Quick self-check (answers follow):\n\n"
                "• Simplify $\\sqrt{1 - \\sin^2\\theta}$ for "
                "$\\theta \\in [-\\frac{\\pi}{2}, \\frac{\\pi}{2}]$. "
                "($\\cos\\theta$ — nonnegative on that interval, so no "
                "absolute value needed.)\n"
                "• $1 + \\tan^2\\theta = {?}$ ($\\sec^2\\theta$.)\n"
                "• If $\\tan\\theta = \\frac{x}{2}$, $\\theta$ acute, what "
                "is $\\sin\\theta$? (Triangle: opposite $x$, adjacent 2, "
                "hypotenuse $\\sqrt{x^2+4}$; so "
                "$\\sin\\theta = \\frac{x}{\\sqrt{x^2+4}}$.)\n"
                "• $\\arcsin\\big(\\frac{1}{2}\\big) = {?}$ "
                "($\\frac{\\pi}{6}$.)\n"
                "• $\\int \\cos^2\\theta\\,d\\theta = {?}$ "
                "($\\frac{\\theta}{2} + \\frac{\\sin 2\\theta}{4} + C$.)\n\n"
                "The triangle question is the one to take seriously: if it "
                "took more than fifteen seconds, practice five more before "
                "page 6, where triangles become the chapter's main "
                "instrument."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "A square root is an obstruction: you cannot integrate "
                "through it, cannot expand it, cannot substitute inside it "
                "profitably unless its derivative happens to sit outside. "
                "The Pythagorean identities are DE-RADICALIZERS — each one "
                "says that a certain combination, under a square root, is a "
                "perfect square:\n"
                "$$1 - \\sin^2\\theta = \\cos^2\\theta, \\qquad "
                "1 + \\tan^2\\theta = \\sec^2\\theta, \\qquad "
                "\\sec^2\\theta - 1 = \\tan^2\\theta.$$\n"
                "Match your radical to the left side of one of these, and "
                "the root evaporates.\n\n"
                "The matching is a pattern-lock. $\\sqrt{a^2 - x^2}$ has the "
                "shape 'constant MINUS square' — that is the first identity, "
                "so set $x = a\\sin\\theta$ and the radical becomes "
                "$a\\cos\\theta$. $\\sqrt{a^2 + x^2}$ is 'constant PLUS "
                "square' — second identity, $x = a\\tan\\theta$, radical "
                "becomes $a\\sec\\theta$. $\\sqrt{x^2 - a^2}$ is 'square "
                "MINUS constant' — third identity, $x = a\\sec\\theta$, "
                "radical becomes $a\\tan\\theta$ (with sign care that "
                "page 10 confronts honestly).\n\n"
                "Notice the direction of the substitution: we are not "
                "naming a piece of the integrand $u$; we are REPLACING $x$ "
                "by a parametrization, the way $x = \\sin\\theta$, "
                "$y = \\cos\\theta$ walks around a circle. This is sometimes "
                "called INVERSE substitution: in C201, $u = g(x)$ and the "
                "new variable is a function of the old; here "
                "$x = g(\\theta)$ and the OLD variable is a function of the "
                "NEW. The bookkeeping is the same theorem read in the other "
                "direction, with $dx = g'(\\theta)\\,d\\theta$ — and one new "
                "obligation. Because we must eventually return from "
                "$\\theta$ to $x$ through $g^{-1}$, we restrict $\\theta$ "
                "to an interval where $g$ is one-to-one (the standard "
                "inverse-trig ranges). That restriction is not pedantry: it "
                "is what makes $\\sqrt{\\cos^2\\theta} = \\cos\\theta$ "
                "legal, absolute-value free — and it is where the secant "
                "case hides its trap."
            )),
            # ---------------------------------------------------------- 4
            (L, "The three substitutions, stated fully", (
                "THE TABLE. For $a > 0$:\n\n"
                "FORM $\\sqrt{a^2 - x^2}$: substitute "
                "$x = a\\sin\\theta$, $dx = a\\cos\\theta\\,d\\theta$, "
                "$\\theta \\in [-\\frac{\\pi}{2}, \\frac{\\pi}{2}]$. Then "
                "$\\sqrt{a^2 - x^2} = a\\cos\\theta$ (cosine is "
                "$\\ge 0$ on this range, so the root resolves with no "
                "absolute value). Return via "
                "$\\theta = \\arcsin\\frac{x}{a}$.\n\n"
                "FORM $\\sqrt{a^2 + x^2}$ (or $a^2 + x^2$ without the "
                "root): substitute $x = a\\tan\\theta$, "
                "$dx = a\\sec^2\\theta\\,d\\theta$, "
                "$\\theta \\in (-\\frac{\\pi}{2}, \\frac{\\pi}{2})$. Then "
                "$\\sqrt{a^2 + x^2} = a\\sec\\theta$ (secant is $> 0$ "
                "there). Return via $\\theta = \\arctan\\frac{x}{a}$.\n\n"
                "FORM $\\sqrt{x^2 - a^2}$: substitute "
                "$x = a\\sec\\theta$, "
                "$dx = a\\sec\\theta\\tan\\theta\\,d\\theta$, with "
                "$\\theta \\in [0, \\frac{\\pi}{2})$ for the branch "
                "$x \\ge a$. Then $\\sqrt{x^2 - a^2} = a\\tan\\theta$. "
                "For the branch $x \\le -a$, sign care is required — "
                "page 10 treats it; on exams the $x \\ge a$ branch is the "
                "default.\n\n"
                "MEMORY AID: the radical's sign pattern points to the "
                "identity. Minus AFTER the constant → sine. Plus → "
                "tangent. Minus BEFORE the constant → secant. The "
                "coefficient $a$ is always $\\sqrt{\\text{the constant}}$, "
                "and a coefficient on $x^2$ joins in: for "
                "$\\sqrt{4 - 9x^2}$, match $9x^2 = (3x)^2$ and set "
                "$3x = 2\\sin\\theta$.\n\n"
                "THE PROCEDURE. (1) Identify the form; write the "
                "substitution AND its $dx$. (2) Convert integrand and "
                "differential completely into $\\theta$; the radical "
                "collapses by design. (3) Integrate with C203's toolkit. "
                "(4) Convert back to $x$ — via the reference triangle "
                "(page 6) for indefinite integrals, or by converting the "
                "BOUNDS (often cleaner) for definite ones. The theorem "
                "being used is still C201's substitution rule; only the "
                "choreography is new."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples", (
                "EXAMPLE 1 (sine case). $\\int \\frac{dx}{\\sqrt{9 - x^2}}$.\n\n"
                "Form $\\sqrt{a^2 - x^2}$ with $a = 3$: set "
                "$x = 3\\sin\\theta$, $dx = 3\\cos\\theta\\,d\\theta$, and "
                "$\\sqrt{9 - x^2} = 3\\cos\\theta$. Then\n"
                "$$\\int \\frac{3\\cos\\theta\\,d\\theta}{3\\cos\\theta} "
                "= \\int d\\theta = \\theta + C "
                "= \\arcsin\\frac{x}{3} + C.$$\n"
                "The 'table integral' for $\\arcsin$ is not a separate fact "
                "to memorize — it is this two-line computation.\n\n"
                "EXAMPLE 2 (tangent case, no radical needed). "
                "$\\int \\frac{dx}{4 + x^2}$.\n\n"
                "The form $a^2 + x^2$ invites $x = 2\\tan\\theta$, "
                "$dx = 2\\sec^2\\theta\\,d\\theta$, and "
                "$4 + x^2 = 4\\sec^2\\theta$:\n"
                "$$\\int \\frac{2\\sec^2\\theta\\,d\\theta}"
                "{4\\sec^2\\theta} = \\frac{1}{2}\\int d\\theta "
                "= \\frac{1}{2}\\arctan\\frac{x}{2} + C.$$\n"
                "Memorize the general result — "
                "$\\int\\frac{dx}{a^2+x^2} = \\frac1a\\arctan\\frac{x}{a} "
                "+ C$ — it recurs constantly in C205.\n\n"
                "EXAMPLE 3 (the semicircle, at last). "
                "$\\int \\sqrt{1 - x^2}\\,dx$.\n\n"
                "Set $x = \\sin\\theta$, $dx = \\cos\\theta\\,d\\theta$, "
                "radical $= \\cos\\theta$:\n"
                "$$\\int \\cos\\theta\\cdot\\cos\\theta\\,d\\theta "
                "= \\int \\cos^2\\theta\\,d\\theta "
                "= \\frac{\\theta}{2} + "
                "\\frac{\\sin\\theta\\cos\\theta}{2} + C,$$\n"
                "using C203's half-angle result (in the form "
                "$\\frac{\\sin 2\\theta}{4} = "
                "\\frac{\\sin\\theta\\cos\\theta}{2}$). Now convert back: "
                "$\\theta = \\arcsin x$, $\\sin\\theta = x$, "
                "$\\cos\\theta = \\sqrt{1 - x^2}$:\n"
                "$$\\int\\sqrt{1 - x^2}\\,dx = \\frac{\\arcsin x + "
                "x\\sqrt{1 - x^2}}{2} + C.$$\n"
                "Differentiate to verify — the check is a pleasant "
                "exercise in C108. The integral that opened the chapter "
                "as unreachable took six lines."
            )),
            # ---------------------------------------------------------- 6
            (L, "The reference triangle: converting back without tears", (
                "An indefinite integral posed in $x$ must be answered in "
                "$x$ (C201's rule), and after trig substitution your "
                "answer is full of $\\theta$'s: perhaps "
                "$\\frac{\\theta}{2} + \\frac{\\sin 2\\theta}{4}$ or "
                "$\\sec\\theta\\tan\\theta$. Converting the bare "
                "$\\theta$ is easy ($\\theta = \\arcsin\\frac{x}{a}$ or "
                "the appropriate inverse). Converting the TRIG FUNCTIONS "
                "of $\\theta$ by identity-chasing is misery. The "
                "REFERENCE TRIANGLE does it by geometry.\n\n"
                "THE METHOD. Your substitution asserts one trig ratio: "
                "$x = a\\sin\\theta$ says "
                "$\\sin\\theta = \\frac{x}{a} = "
                "\\frac{\\text{opposite}}{\\text{hypotenuse}}$. Draw a "
                "right triangle with an angle $\\theta$; label the "
                "OPPOSITE side $x$ and the HYPOTENUSE $a$. Pythagoras "
                "fills in the third side: adjacent "
                "$= \\sqrt{a^2 - x^2}$ — which is exactly the radical "
                "from your integral, no accident. Now EVERY trig "
                "function of $\\theta$ reads off the triangle:\n"
                "$$\\cos\\theta = \\frac{\\sqrt{a^2 - x^2}}{a}, \\quad "
                "\\tan\\theta = \\frac{x}{\\sqrt{a^2 - x^2}}, \\quad "
                "\\sec\\theta = \\frac{a}{\\sqrt{a^2 - x^2}}, \\ldots$$\n\n"
                "The other cases label differently. TANGENT case "
                "($x = a\\tan\\theta$): opposite $x$, ADJACENT $a$, "
                "hypotenuse $\\sqrt{a^2 + x^2}$. SECANT case "
                "($x = a\\sec\\theta$): hypotenuse $x$, adjacent $a$, "
                "opposite $\\sqrt{x^2 - a^2}$. In every case the "
                "radical is the Pythagoras-computed third side — draw "
                "the triangle and the algebra does itself.\n\n"
                "For DEFINITE integrals you can usually skip the "
                "triangle: convert the BOUNDS through "
                "$\\theta = \\arcsin\\frac{x}{a}$ (or the relevant "
                "inverse) at the moment of substitution, evaluate "
                "entirely in $\\theta$, and never come back. Example: "
                "bounds $x = 0 \\to a$ become "
                "$\\theta = 0 \\to \\frac{\\pi}{2}$. Method A versus "
                "Method B from C201 page 6, unchanged. Prefer bound "
                "conversion when the bounds are 'nice' values of sine "
                "or tangent; prefer the triangle when they are not, or "
                "when the integral is indefinite."
            )),
            # ---------------------------------------------------------- 7
            (E, "Worked examples: standard problems", (
                "EXAMPLE 1 (tangent case with triangle conversion). "
                "$\\int \\frac{dx}{x^2\\sqrt{x^2 + 4}}$.\n\n"
                "Set $x = 2\\tan\\theta$: $dx = 2\\sec^2\\theta\\,"
                "d\\theta$, $\\sqrt{x^2+4} = 2\\sec\\theta$:\n"
                "$$\\int \\frac{2\\sec^2\\theta\\,d\\theta}"
                "{4\\tan^2\\theta\\cdot 2\\sec\\theta} "
                "= \\frac{1}{4}\\int \\frac{\\sec\\theta}"
                "{\\tan^2\\theta}\\,d\\theta "
                "= \\frac{1}{4}\\int \\frac{\\cos\\theta}"
                "{\\sin^2\\theta}\\,d\\theta,$$\n"
                "converting to sines and cosines — often the fastest move "
                "for odd mixtures. Now $u = \\sin\\theta$ (a C201 reflex "
                "inside a C204 problem!):\n"
                "$$\\frac{1}{4}\\int \\frac{du}{u^2} = -\\frac{1}"
                "{4\\sin\\theta} + C.$$\n"
                "Triangle (opposite $x$, adjacent 2, hypotenuse "
                "$\\sqrt{x^2+4}$): $\\sin\\theta = "
                "\\frac{x}{\\sqrt{x^2+4}}$. Answer:\n"
                "$$-\\frac{\\sqrt{x^2+4}}{4x} + C.$$\n\n"
                "EXAMPLE 2 (definite, bounds converted — the quarter "
                "circle). $\\int_0^{a} \\sqrt{a^2 - x^2}\\,dx$.\n\n"
                "$x = a\\sin\\theta$; bounds: $x = 0 \\mapsto \\theta = 0$, "
                "$x = a \\mapsto \\theta = \\frac{\\pi}{2}$:\n"
                "$$\\int_0^{\\pi/2} a\\cos\\theta \\cdot "
                "a\\cos\\theta\\,d\\theta = "
                "a^2\\int_0^{\\pi/2}\\cos^2\\theta\\,d\\theta "
                "= a^2\\cdot\\frac{\\pi}{4}.$$\n"
                "Four times this is $\\pi a^2$ — the circle-area formula, "
                "derived rather than remembered. No triangle needed: we "
                "never returned to $x$.\n\n"
                "EXAMPLE 3 (secant case). "
                "$\\int \\frac{\\sqrt{x^2 - 9}}{x}\\,dx$ for $x > 3$.\n\n"
                "$x = 3\\sec\\theta$, $dx = 3\\sec\\theta\\tan\\theta\\,"
                "d\\theta$, $\\sqrt{x^2 - 9} = 3\\tan\\theta$:\n"
                "$$\\int \\frac{3\\tan\\theta}{3\\sec\\theta}\\,"
                "3\\sec\\theta\\tan\\theta\\,d\\theta = "
                "3\\int \\tan^2\\theta\\,d\\theta = "
                "3(\\tan\\theta - \\theta) + C,$$\n"
                "via $\\tan^2 = \\sec^2 - 1$ (C203). Triangle: "
                "$\\tan\\theta = \\frac{\\sqrt{x^2-9}}{3}$, so\n"
                "$$\\sqrt{x^2 - 9} - 3\\arctan\\frac{\\sqrt{x^2-9}}{3} "
                "+ C.$$\n"
                "(Writing $\\theta$ as an arctangent of the triangle's "
                "opposite-over-adjacent, rather than as "
                "$\\operatorname{arcsec}\\frac{x}{3}$, sidesteps the "
                "arcsecant's branch headaches — a small choice that "
                "pays off on the $x < -3$ branch, as page 10 "
                "explains. Differentiate the answer to confirm; the "
                "check exercises exactly the triangle relations you "
                "just used.)"
            )),
            # ---------------------------------------------------------- 8
            (L, "Strategy: completing the square, and when to skip all this", (
                "STEP 0 — CHECK FOR THE FAST LANE. If the derivative of "
                "the quadratic under the root appears (up to a constant) "
                "as a factor, ORDINARY substitution wins: "
                "$\\int x\\sqrt{1 - x^2}\\,dx$ is $u = 1 - x^2$, two "
                "lines, done. Trig substitution also works there — it is "
                "never WRONG — but it is triple the labor. Scan for the "
                "$x\\,dx$ before committing; this single habit saves more "
                "time than any other in the chapter. Odd powers of $x$ "
                "multiplying the radical almost always signal the fast "
                "lane (pair one $x$ with $dx$, convert the even "
                "remainder as in C201 page 9).\n\n"
                "STEP 1 — NORMALIZE THE QUADRATIC. General quadratics "
                "hide the standard forms. COMPLETE THE SQUARE:\n"
                "$$x^2 + 2x + 5 = (x+1)^2 + 4, \\qquad "
                "3 - 2x - x^2 = 4 - (x+1)^2, \\qquad "
                "x^2 + 4x = (x+2)^2 - 4.$$\n"
                "A preliminary shift $w = x + 1$ (a C201 linear "
                "substitution, $dw = dx$) then exposes "
                "$\\sqrt{w^2 + 4}$-type forms. Every quadratic radical "
                "reduces to one of the three standard shapes this way.\n\n"
                "STEP 2 — MATCH AND SUBSTITUTE by the sign table of "
                "page 4. Expect the coefficient pattern "
                "$\\sqrt{b^2x^2 \\pm a^2}$ to need $bx = a\\tan\\theta$ "
                "etc.\n\n"
                "STEP 3 — INTEGRATE IN $\\theta$-LAND with C203: "
                "$\\cos^2$ by half-angle; $\\sec^3$ by the boomerang "
                "formula; mixed powers by the parity table.\n\n"
                "STEP 4 — RETURN: triangle for indefinite, converted "
                "bounds for definite.\n\n"
                "A NOTE ON ALTERNATIVES. Hyperbolic substitutions "
                "($x = a\\sinh t$ for $\\sqrt{a^2 + x^2}$, "
                "$x = a\\cosh t$ for $\\sqrt{x^2 - a^2}$) clear the same "
                "radicals via $\\cosh^2 - \\sinh^2 = 1$ and are sometimes "
                "faster — no $\\sec^3$ appears. They are a beautiful "
                "parallel kit (page 17); most courses standardize on the "
                "trig versions, and so does the practice engine here."
            )),
            # ---------------------------------------------------------- 9
            (E, "Worked examples: harder problems", (
                "EXAMPLE 1 (complete the square, tangent case, power "
                "$\\frac32$). $\\int \\frac{dx}{(x^2 + 2x + 5)^{3/2}}$.\n\n"
                "No radical sign, but the $\\frac{3}{2}$ power is "
                "$(\\sqrt{\\;})^3$ — same machinery. Complete the square: "
                "$x^2 + 2x + 5 = (x+1)^2 + 4$. Set "
                "$x + 1 = 2\\tan\\theta$, $dx = 2\\sec^2\\theta\\,"
                "d\\theta$; then $(x^2+2x+5)^{3/2} = "
                "(4\\sec^2\\theta)^{3/2} = 8\\sec^3\\theta$:\n"
                "$$\\int \\frac{2\\sec^2\\theta\\,d\\theta}"
                "{8\\sec^3\\theta} = \\frac{1}{4}\\int\\cos\\theta\\,"
                "d\\theta = \\frac{\\sin\\theta}{4} + C.$$\n"
                "Triangle for the shifted variable (opposite $x+1$, "
                "adjacent 2, hypotenuse $\\sqrt{(x+1)^2 + 4}$):\n"
                "$$\\frac{x+1}{4\\sqrt{x^2 + 2x + 5}} + C.$$\n"
                "Note how the $\\frac32$ power actually made the "
                "$\\theta$-integral EASIER — the $\\sec^3$ in the "
                "denominator flipped into a lone cosine.\n\n"
                "EXAMPLE 2 (complete the square, secant case, meeting "
                "$\\sec^3$). $\\int \\sqrt{x^2 + 4x}\\,dx$ for "
                "$x > 0$.\n\n"
                "Complete: $x^2 + 4x = (x+2)^2 - 4$. Set "
                "$x + 2 = 2\\sec\\theta$, $dx = 2\\sec\\theta\\tan\\theta"
                "\\,d\\theta$, radical $= 2\\tan\\theta$:\n"
                "$$\\int 2\\tan\\theta\\cdot 2\\sec\\theta\\tan\\theta\\,"
                "d\\theta = 4\\int\\sec\\theta\\tan^2\\theta\\,d\\theta "
                "= 4\\int \\sec^3\\theta\\,d\\theta - "
                "4\\int\\sec\\theta\\,d\\theta,$$\n"
                "using $\\tan^2 = \\sec^2 - 1$. C203's classics finish "
                "it: $4\\cdot\\frac{\\sec\\theta\\tan\\theta + "
                "\\ln|\\sec\\theta + \\tan\\theta|}{2} - "
                "4\\ln|\\sec\\theta + \\tan\\theta| = "
                "2\\sec\\theta\\tan\\theta - 2\\ln|\\sec\\theta + "
                "\\tan\\theta| + C$. Triangle ($\\sec\\theta = "
                "\\frac{x+2}{2}$, $\\tan\\theta = "
                "\\frac{\\sqrt{x^2+4x}}{2}$):\n"
                "$$\\frac{(x+2)\\sqrt{x^2+4x}}{2} - "
                "2\\ln\\Big|x + 2 + \\sqrt{x^2+4x}\\Big| + C,$$\n"
                "where the constant $-2\\ln\\frac12\\cdot(\\ldots)$ from "
                "the triangle's denominators was absorbed into $C$. This "
                "is the chapter's full pipeline — complete, substitute, "
                "$\\sec^3$, triangle — in one problem."
            )),
            # ---------------------------------------------------------- 10
            (L, "Special cases and edge behavior: the secant case's fine print", (
                "THE SIGN ISSUE, honestly. Resolving "
                "$\\sqrt{a^2\\tan^2\\theta} = a\\tan\\theta$ in the secant "
                "case assumed $\\tan\\theta \\ge 0$ — true on "
                "$[0, \\frac{\\pi}{2})$, which corresponds to the branch "
                "$x \\ge a$. But $\\sqrt{x^2 - a^2}$ is also defined for "
                "$x \\le -a$, and there the standard $\\theta$-range "
                "choices make $\\tan\\theta \\le 0$, so honestly "
                "$\\sqrt{x^2 - a^2} = a|\\tan\\theta| = -a\\tan\\theta$. "
                "Consequences: on the left branch a minus sign threads "
                "through the computation, and lazily copying the "
                "right-branch answer can be wrong by a sign — or, "
                "curiously, can turn out right anyway after logarithm "
                "algebra (page 11 case (b) shows the classic instance). "
                "PROTOCOL for this course: state the branch "
                "($x \\ge a$ unless told otherwise), keep "
                "$\\theta \\in [0, \\frac{\\pi}{2})$, and if the left "
                "branch is genuinely required, either track "
                "$|\\tan\\theta|$ carefully or substitute $x = -t$ first "
                "to move to the right branch.\n\n"
                "WHY SINE AND TANGENT ESCAPE THIS. On "
                "$[-\\frac{\\pi}{2}, \\frac{\\pi}{2}]$, cosine and secant "
                "are NONNEGATIVE, so "
                "$\\sqrt{\\cos^2\\theta} = \\cos\\theta$ and "
                "$\\sqrt{\\sec^2\\theta} = \\sec\\theta$ hold without "
                "bars. The standard ranges were CHOSEN to make the "
                "common cases sign-safe; only secant's awkward domain "
                "(it needs $|x| \\ge a$, two disconnected pieces) resists "
                "a uniformly clean choice.\n\n"
                "BOUNDS AT THE EDGE. In definite integrals like "
                "$\\int_0^a\\sqrt{a^2 - x^2}\\,dx$, the endpoint $x = a$ "
                "maps to $\\theta = \\frac{\\pi}{2}$, where "
                "$\\cos\\theta = 0$ — the radical vanishes and "
                "everything stays finite. But endpoints where the "
                "INTEGRAND blows up ($\\int_0^3 \\frac{dx}"
                "{\\sqrt{9 - x^2}}$ at $x = 3$) are IMPROPER integrals: "
                "the substitution handles them gracefully (the "
                "$\\theta$-integral is proper), which is a preview of "
                "C206's limit-based definitions — trig substitution is "
                "one of the standard ways improper integrals get "
                "tamed.\n\n"
                "AND A REMINDER: the fast lane (page 8, step 0) exists. "
                "Before any triangle is drawn, look for the $x\\,dx$."
            )),
            # ---------------------------------------------------------- 11
            (E, "Worked example: edge cases diagnosed", (
                "CASE (a) — the fast lane versus the scenic route. "
                "$\\int x\\sqrt{1 - x^2}\\,dx$.\n\n"
                "Trig substitution works: $x = \\sin\\theta$ gives "
                "$\\int \\sin\\theta\\cos^2\\theta\\,d\\theta = "
                "-\\frac{\\cos^3\\theta}{3} + C = "
                "-\\frac{(1-x^2)^{3/2}}{3} + C$ — after a triangle "
                "conversion. Ordinary substitution: $u = 1 - x^2$, "
                "$x\\,dx = -\\frac{du}{2}$:\n"
                "$$-\\frac{1}{2}\\int\\sqrt{u}\\,du = "
                "-\\frac{u^{3/2}}{3} + C = -\\frac{(1-x^2)^{3/2}}{3} "
                "+ C.$$\n"
                "Same answer, one-third the work. Diagnosis: the "
                "$x\\,dx$ was present; C201 owns this integral. Trig "
                "substitution is for radicals WITHOUT their "
                "derivative.\n\n"
                "CASE (b) — the left branch of the secant case. "
                "$\\int \\frac{dx}{\\sqrt{x^2 - 4}}$ on $x < -2$.\n\n"
                "With $x = 2\\sec\\theta$, $\\theta \\in "
                "(\\frac{\\pi}{2}, \\pi]$ for this branch, "
                "$\\tan\\theta \\le 0$, so "
                "$\\sqrt{x^2 - 4} = -2\\tan\\theta$. Then\n"
                "$$\\int \\frac{2\\sec\\theta\\tan\\theta\\,d\\theta}"
                "{-2\\tan\\theta} = -\\int \\sec\\theta\\,d\\theta = "
                "-\\ln|\\sec\\theta + \\tan\\theta| + C.$$\n"
                "Substitute $\\sec\\theta = \\frac{x}{2}$, "
                "$\\tan\\theta = -\\frac{\\sqrt{x^2-4}}{2}$ and simplify "
                "with $-\\ln|A| = \\ln\\frac{1}{|A|}$ and a conjugate:\n"
                "$$-\\ln\\Big|\\frac{x - \\sqrt{x^2-4}}{2}\\Big| = "
                "\\ln\\Big|\\frac{2}{x - \\sqrt{x^2-4}}\\Big| = "
                "\\ln\\Big|\\frac{x + \\sqrt{x^2-4}}{2}\\Big|,$$\n"
                "since $(x-\\sqrt{x^2-4})(x+\\sqrt{x^2-4}) = 4$. The "
                "final formula $\\ln|x + \\sqrt{x^2-4}| + C$ matches the "
                "right branch's — the signs conspired, THIS time. The "
                "diagnosis to remember: the agreement had to be PROVEN, "
                "not assumed.\n\n"
                "CASE (c) — radical to an odd power. "
                "$\\int (1 - x^2)^{3/2}\\,dx$. No $x\\,dx$; sine case "
                "with the radical CUBED: $x = \\sin\\theta$ gives "
                "$\\int \\cos^4\\theta\\,d\\theta$ — a pure C203 "
                "even-power reduction. Higher powers of the radical "
                "just mean higher trig powers; the pipeline is "
                "unchanged."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'Radical means trig substitution.' The "
                "most expensive habit in this chapter. If the "
                "derivative of the radicand accompanies the radical "
                "($x\\,dx$ next to $\\sqrt{1 - x^2}$), plain "
                "substitution is faster by a factor of three (page 11a). "
                "Trig substitution is the tool of LAST resort for "
                "quadratic radicals, not first.\n\n"
                "MISCONCEPTION 2: 'The radical simplifies like algebra: "
                "$\\sqrt{x^2 - a^2} = x - a$.' Square roots do not "
                "distribute over sums or differences — "
                "$\\sqrt{25 - 9} = 4$, not $2$. The ONLY legal collapse "
                "is via a perfect square, which is exactly what the "
                "Pythagorean identities manufacture. That is the whole "
                "reason the technique exists.\n\n"
                "MISCONCEPTION 3: 'The answer can stay in $\\theta$.' "
                "$\\frac{\\theta}{2} + \\frac{\\sin 2\\theta}{4} + C$ "
                "answers a question nobody asked. The original variable "
                "is $x$; convert via the triangle (after rewriting "
                "double angles in single-angle form: "
                "$\\sin 2\\theta = 2\\sin\\theta\\cos\\theta$ FIRST, "
                "then read the triangle).\n\n"
                "MISCONCEPTION 4: '$dx$ just becomes $d\\theta$.' The "
                "differential transforms by the substitution's "
                "derivative: $x = 3\\sin\\theta$ forces "
                "$dx = 3\\cos\\theta\\,d\\theta$. Forgetting the factor "
                "changes every answer by a function, not a constant — "
                "the error is caught instantly by differentiating the "
                "result, and almost never caught otherwise.\n\n"
                "MISCONCEPTION 5: 'The $\\theta$-range is bureaucratic "
                "fine print.' The range is what makes "
                "$\\sqrt{\\cos^2\\theta} = \\cos\\theta$ true WITHOUT "
                "absolute values, and its failure in the secant case's "
                "left branch is a real sign error waiting to happen "
                "(pages 10-11). The fine print IS the mathematics.\n\n"
                "MISCONCEPTION 6: 'Completing the square changes the "
                "problem.' It is exact algebra — the same quadratic "
                "rewritten. Students distrust it because the integrand "
                "LOOKS different; differentiating the final answer "
                "restores trust."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational errors", (
                "ERROR 1: MISLABELED TRIANGLE. Each case has its own "
                "geometry: sine puts $x$ OPPOSITE with hypotenuse $a$; "
                "tangent puts $x$ opposite with ADJACENT $a$; secant "
                "puts $x$ on the HYPOTENUSE with adjacent $a$. Swapping "
                "roles produces reciprocal answers "
                "($\\frac{\\sqrt{x^2+4}}{x}$ for "
                "$\\frac{x}{\\sqrt{x^2+4}}$). Prevention: the radical "
                "must come out as the Pythagoras side; if your triangle "
                "puts the radical on the hypotenuse in a sine problem, "
                "it is mislabeled.\n\n"
                "ERROR 2: DROPPED $a$ IN $dx$. From $x = 5\\sin\\theta$, "
                "$dx = 5\\cos\\theta\\,d\\theta$ — the 5 multiplies. "
                "Symptom: answers off by a constant factor. The "
                "differentiate-to-check reflex finds it.\n\n"
                "ERROR 3: HALF OF COMPLETING THE SQUARE. "
                "$x^2 + 6x + 13 = (x+3)^2 + 4$: add AND subtract 9, and "
                "mind signs when the $x^2$ coefficient is negative: "
                "$3 - 2x - x^2 = 4 - (x+1)^2$, where students commonly "
                "get $4 + (x+1)^2$. Verify by expanding — five "
                "seconds.\n\n"
                "ERROR 4: BOUND CONVERSION SLIPS. $x = a\\sin\\theta$ "
                "sends $x = \\frac{a}{2}$ to $\\theta = \\frac{\\pi}{6}$ "
                "(not $\\frac{\\pi}{3}$ — that is cosine thinking). "
                "Convert bounds by explicitly solving "
                "$\\sin\\theta = \\frac{x}{a}$ within the standard "
                "range, and write both conversions down before "
                "integrating.\n\n"
                "ERROR 5: DOUBLE-ANGLE LEFT UNCONVERTED. Reading "
                "$\\sin 2\\theta$ 'off the triangle' directly is "
                "meaningless — the triangle knows $\\theta$, not "
                "$2\\theta$. Expand to $2\\sin\\theta\\cos\\theta$ "
                "first. This is the most common back-conversion "
                "error in the sine case.\n\n"
                "ERROR 6: $\\sec^3$ MISQUOTED. The classic result has "
                "BOTH terms halved: $\\frac{\\sec\\theta\\tan\\theta + "
                "\\ln|\\sec\\theta + \\tan\\theta|}{2}$. Re-derive via "
                "the boomerang (C203 page 9) when in doubt; it costs "
                "ninety seconds and is exam-proof."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications", (
                "The quadratic radicals this chapter clears are the "
                "signature of DISTANCE — every $\\sqrt{x^2 + d^2}$ is a "
                "hypotenuse — so trig substitution appears wherever "
                "geometry meets accumulation.\n\n"
                "1. EXACT AREAS OF CONICS. Circle segments, ellipse "
                "areas ($\\int \\sqrt{a^2 - x^2}$ scaled), lens-shaped "
                "intersection regions: all are sine-case integrals. "
                "Page 7's quarter-circle computation is the template, "
                "and C207's area applications industrialize it.\n\n"
                "2. ARC LENGTH AND SURFACES (C207-C208). The arc length "
                "of $y = x^2$ requires "
                "$\\int\\sqrt{1 + 4x^2}\\,dx$ — tangent case, landing "
                "exactly on $\\sec^3$ (which is why C203 drilled it). "
                "Surface areas of revolution produce the same "
                "shapes.\n\n"
                "3. FIELDS OF LINE SOURCES (physics). The electric or "
                "gravitational field of a finite rod at perpendicular "
                "distance $d$ integrates "
                "$\\frac{dx}{(x^2 + d^2)^{3/2}}$ — tangent case, "
                "page 15 works it in full. Antenna radiation and "
                "fluid-flow computations produce the same kernels.\n\n"
                "4. PROBABILITY AND STATISTICS (PF.U1.N2). The Cauchy "
                "density $\\frac{1}{\\pi(1 + x^2)}$ is normalized by "
                "the tangent substitution; arcsine-law probabilities "
                "in random walks are sine-case integrals of "
                "$\\frac{1}{\\sqrt{x(1-x)}}$-type radicands (complete "
                "the square!).\n\n"
                "5. THE INTEGRAL TABLE ITSELF. Entries like "
                "$\\int\\frac{dx}{\\sqrt{x^2 \\pm a^2}} = "
                "\\ln|x + \\sqrt{x^2 \\pm a^2}| + C$ — used constantly "
                "in engineering handbooks — are one secant/tangent "
                "substitution each. This chapter is where those table "
                "rows come from; after it, a table is a convenience "
                "rather than an oracle."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: the field of a finite line charge", (
                "A thin rod of length $2L$ carries uniform charge "
                "density $\\lambda$ (coulombs per meter). Find the "
                "electric field at a point $P$ a perpendicular distance "
                "$d$ from the rod's midpoint.\n\n"
                "STEP 1 — set up (the physics supplies the integral). "
                "Put the rod on the $x$-axis from $-L$ to $L$, $P$ at "
                "$(0, d)$. A slice $dx$ at position $x$ contributes "
                "field $\\frac{k\\lambda\\,dx}{x^2 + d^2}$ directed "
                "along the slice-to-$P$ line; by symmetry the "
                "horizontal parts cancel, leaving the perpendicular "
                "component (factor $\\frac{d}{\\sqrt{x^2+d^2}}$):\n"
                "$$E = k\\lambda d \\int_{-L}^{L} "
                "\\frac{dx}{(x^2 + d^2)^{3/2}}.$$\n"
                "The $\\frac32$-power hypotenuse kernel — tangent "
                "case.\n\n"
                "STEP 2 — substitute. $x = d\\tan\\theta$, "
                "$dx = d\\sec^2\\theta\\,d\\theta$, "
                "$(x^2 + d^2)^{3/2} = d^3\\sec^3\\theta$:\n"
                "$$\\int \\frac{d\\sec^2\\theta\\,d\\theta}"
                "{d^3\\sec^3\\theta} = \\frac{1}{d^2}\\int "
                "\\cos\\theta\\,d\\theta = \\frac{\\sin\\theta}{d^2}.$$\n\n"
                "STEP 3 — convert back with the triangle (opposite $x$, "
                "adjacent $d$, hypotenuse $\\sqrt{x^2 + d^2}$): "
                "$\\sin\\theta = \\frac{x}{\\sqrt{x^2 + d^2}}$, so the "
                "antiderivative is "
                "$\\frac{x}{d^2\\sqrt{x^2 + d^2}}$. Evaluate from $-L$ "
                "to $L$:\n"
                "$$E = k\\lambda d\\cdot\\frac{2L}{d^2\\sqrt{L^2 + d^2}} "
                "= \\frac{2k\\lambda L}{d\\sqrt{L^2 + d^2}}.$$\n\n"
                "STEP 4 — sanity checks, the physicist's habit. "
                "(i) $L \\to \\infty$ (infinite wire): "
                "$E \\to \\frac{2k\\lambda}{d}$ — the textbook "
                "infinite-line formula, recovered as a limit (an "
                "improper integral in disguise: C206). "
                "(ii) $d \\gg L$ (far away): "
                "$E \\approx \\frac{2k\\lambda L}{d^2} = "
                "\\frac{kQ}{d^2}$ with $Q = 2\\lambda L$ — the rod "
                "looks like a point charge, Coulomb's law. One "
                "substitution, one triangle, and both limiting "
                "behaviors of electrostatics fall out correctly."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections", (
                "BACKWARD. This chapter is a confluence: C201 supplied "
                "the substitution theorem (run here in reverse) and the "
                "fast-lane judgment; C203 supplies every "
                "$\\theta$-integral the substitutions produce — "
                "$\\cos^2$, $\\tan^2$, and the parts-powered $\\sec^3$ "
                "from C202's boomerang. The right-triangle trigonometry "
                "is PRECALC's, promoted from homework device to "
                "production tool.\n\n"
                "FORWARD, immediately:\n"
                "• C205 (partial fractions) completes the technique "
                "core. Irreducible quadratic factors there produce "
                "$\\int\\frac{dx}{(x^2 + a^2)^n}$ integrals whose "
                "evaluation IS the tangent substitution; the two "
                "chapters share a border.\n"
                "• C206 (improper integrals): infinite limits like "
                "page 15's $L \\to \\infty$, and endpoint blowups like "
                "$\\int_0^3\\frac{dx}{\\sqrt{9 - x^2}}$, get their "
                "rigorous limit-based treatment; today's substitutions "
                "are the standard evaluation engine there.\n"
                "• C207-C208 (applications: area, volume, arc length, "
                "surface area) generate this chapter's integrands "
                "relentlessly — every arc length carries a "
                "$\\sqrt{1 + (y')^2}$.\n\n"
                "FURTHER OUT. In multivariable calculus the same "
                "radicals reappear as distances in the plane and "
                "space, and POLAR COORDINATES ($x = r\\cos\\theta$) — "
                "a two-dimensional cousin of today's substitutions — "
                "clear them wholesale (the C304 track's change of "
                "variables). Hyperbolic substitutions (page 17) do "
                "this chapter's work in ODE and engineering contexts "
                "where $\\cosh$ arises natively (catenaries, "
                "relativity). And the boundary of the method — "
                "radicals of CUBICS — opens onto elliptic integrals, "
                "the historical gateway to elliptic functions and a "
                "large piece of 19th-century mathematics (page 17)."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: parametrization, hyperbolics, and the elliptic frontier", (
                "WHY THESE SUBSTITUTIONS EXIST — the geometric view. "
                "$\\sqrt{a^2 - x^2}$ is the height of the circle "
                "$x^2 + y^2 = a^2$, and $x = a\\sin\\theta$, "
                "$y = a\\cos\\theta$ is a PARAMETRIZATION of that "
                "circle by a rational-trigonometric map. Trig "
                "substitution is the calculus incarnation of a "
                "classical fact: conics are curves that can be "
                "parametrized by trigonometric (equivalently, "
                "rational) functions. Integrals on a conic pull back, "
                "through the parametrization, to integrals of trig "
                "functions — which C203 can always finish. The "
                "Weierstrass substitution $t = \\tan\\frac{\\theta}{2}$ "
                "pushes this further, turning ANY rational function of "
                "$\\sin\\theta, \\cos\\theta$ into a rational function "
                "of $t$ — proving that every such integral is "
                "elementary, with C205's partial fractions as the "
                "finisher. Euler's substitutions do the analogous job "
                "for $\\sqrt{ax^2 + bx + c}$ directly.\n\n"
                "THE HYPERBOLIC TWINS. $\\cosh^2 t - \\sinh^2 t = 1$ "
                "clears the same radicals: $x = a\\sinh t$ handles "
                "$\\sqrt{a^2 + x^2}$ with "
                "$\\int\\sqrt{a^2+x^2}\\,dx$-type results emerging as "
                "$\\sinh^{-1}$ — which is why the answer can be "
                "written $\\ln|x + \\sqrt{x^2 + a^2}|$: that logarithm "
                "IS $\\sinh^{-1}\\frac{x}{a}$ in closed form. The trig "
                "and hyperbolic routes are two parametrizations of "
                "the same hyperbola; feel free to learn both and use "
                "the shorter per problem.\n\n"
                "THE FRONTIER. Raise the degree under the radical by "
                "one — $\\int\\frac{dx}{\\sqrt{1 - x^4}}$, or the arc "
                "length of an ellipse — and NO substitution of any "
                "kind produces elementary functions. These ELLIPTIC "
                "INTEGRALS resisted the greatest analysts of the "
                "18th century until Legendre systematized them and "
                "Abel and Jacobi (1820s) inverted them, discovering "
                "elliptic FUNCTIONS — doubly periodic marvels that now "
                "underpin number theory and cryptography. The lesson "
                "in miniature: quadratic radicals are conquered "
                "territory; cubic radicals are the border of a "
                "different country, and this chapter walks you right "
                "up to it."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "THE TABLE ($a > 0$).\n\n"
                "$\\sqrt{a^2 - x^2}$: $x = a\\sin\\theta$, "
                "$dx = a\\cos\\theta\\,d\\theta$, radical "
                "$= a\\cos\\theta$, "
                "$\\theta \\in [-\\frac{\\pi}{2}, \\frac{\\pi}{2}]$. "
                "Triangle: opposite $x$, hypotenuse $a$, adjacent "
                "$\\sqrt{a^2 - x^2}$.\n\n"
                "$\\sqrt{a^2 + x^2}$ or $a^2 + x^2$: "
                "$x = a\\tan\\theta$, $dx = a\\sec^2\\theta\\,d\\theta$, "
                "radical $= a\\sec\\theta$, "
                "$\\theta \\in (-\\frac{\\pi}{2}, \\frac{\\pi}{2})$. "
                "Triangle: opposite $x$, adjacent $a$, hypotenuse "
                "$\\sqrt{a^2 + x^2}$.\n\n"
                "$\\sqrt{x^2 - a^2}$: $x = a\\sec\\theta$, "
                "$dx = a\\sec\\theta\\tan\\theta\\,d\\theta$, radical "
                "$= a\\tan\\theta$ on the branch $x \\ge a$ with "
                "$\\theta \\in [0, \\frac{\\pi}{2})$; sign care on "
                "$x \\le -a$. Triangle: hypotenuse $x$, adjacent $a$, "
                "opposite $\\sqrt{x^2 - a^2}$.\n\n"
                "WORKFLOW. (0) Fast lane: derivative of the radicand "
                "present → plain C201 substitution instead. "
                "(1) Complete the square; shift $w = x + h$. "
                "(2) Match signs → choose the substitution; handle "
                "$b^2x^2$ via $bx = a\\sin\\theta$ etc. (3) Integrate "
                "in $\\theta$ with C203 (half-angle for $\\cos^2$; "
                "boomerang for $\\sec^3$). (4) Back-convert: triangle "
                "(indefinite; expand double angles first) or converted "
                "bounds (definite). (5) Differentiate to check.\n\n"
                "RESULTS WORTH KNOWING.\n"
                "$$\\int\\frac{dx}{a^2 + x^2} = "
                "\\frac{1}{a}\\arctan\\frac{x}{a} + C, \\qquad "
                "\\int\\frac{dx}{\\sqrt{a^2 - x^2}} = "
                "\\arcsin\\frac{x}{a} + C,$$\n"
                "$$\\int\\frac{dx}{\\sqrt{x^2 \\pm a^2}} = "
                "\\ln\\big|x + \\sqrt{x^2 \\pm a^2}\\big| + C, \\qquad "
                "\\int\\sqrt{a^2 - x^2}\\,dx = "
                "\\frac{a^2}{2}\\arcsin\\frac{x}{a} + "
                "\\frac{x\\sqrt{a^2 - x^2}}{2} + C.$$"
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all seven before reading the solutions.\n\n"
                "1. $\\int \\frac{dx}{\\sqrt{25 - x^2}}$?\n"
                "2. $\\int \\frac{x^2}{\\sqrt{1 - x^2}}\\,dx$?\n"
                "3. $\\int \\frac{dx}{(x^2 + 9)^{3/2}}$?\n"
                "4. $\\int_0^3 \\sqrt{9 - x^2}\\,dx$?\n"
                "5. $\\int \\frac{dx}{\\sqrt{x^2 + 4x + 13}}$?\n"
                "6. $\\int x\\sqrt{4 - x^2}\\,dx$ — by the FASTEST "
                "correct method?\n"
                "7. $\\int \\frac{dx}{x^2\\sqrt{x^2 - 16}}$ for "
                "$x > 4$?\n\n"
                "SOLUTIONS.\n"
                "1. Sine case, $a = 5$: $\\arcsin\\frac{x}{5} + C$.\n"
                "2. $x = \\sin\\theta$: "
                "$\\int\\sin^2\\theta\\,d\\theta = \\frac{\\theta}{2} - "
                "\\frac{\\sin\\theta\\cos\\theta}{2} + C = "
                "\\frac{\\arcsin x}{2} - \\frac{x\\sqrt{1-x^2}}{2} + C$ "
                "(half-angle, then triangle).\n"
                "3. $x = 3\\tan\\theta$: "
                "$\\frac{1}{9}\\int\\cos\\theta\\,d\\theta = "
                "\\frac{\\sin\\theta}{9} = "
                "\\frac{x}{9\\sqrt{x^2 + 9}} + C$.\n"
                "4. Quarter circle of radius 3 (or $x = 3\\sin\\theta$, "
                "bounds $0 \\to \\frac{\\pi}{2}$): "
                "$\\frac{9\\pi}{4}$.\n"
                "5. Complete the square: $(x+2)^2 + 9$; "
                "$x + 2 = 3\\tan\\theta$ gives "
                "$\\int\\sec\\theta\\,d\\theta = "
                "\\ln|\\sec\\theta + \\tan\\theta| + C = "
                "\\ln\\big|x + 2 + \\sqrt{x^2 + 4x + 13}\\big| + C$ "
                "(constant $-\\ln 3$ absorbed).\n"
                "6. FAST LANE — the $x\\,dx$ is present: "
                "$u = 4 - x^2$, $x\\,dx = -\\frac{du}{2}$: "
                "$-\\frac{(4 - x^2)^{3/2}}{3} + C$. Trig substitution "
                "gets the same answer in nine lines instead of "
                "two.\n"
                "7. $x = 4\\sec\\theta$: "
                "$\\int\\frac{4\\sec\\theta\\tan\\theta\\,d\\theta}"
                "{16\\sec^2\\theta\\cdot 4\\tan\\theta} = "
                "\\frac{1}{16}\\int\\cos\\theta\\,d\\theta = "
                "\\frac{\\sin\\theta}{16} = "
                "\\frac{\\sqrt{x^2 - 16}}{16x} + C "
                "(triangle: hypotenuse $x$, adjacent 4, opposite "
                "$\\sqrt{x^2 - 16}$).\n\n"
                "Scoring guide: 7/7 — proceed. 5-6 — reread the pages "
                "matching your misses (triangle labels → 6 and 13; "
                "completing the square → 8-9; fast lane → 8, 11). "
                "≤4 — rework pages 3-9 before practicing, and re-drill "
                "C203's $\\cos^2$ and $\\sec^3$ results."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Before leaving this chapter you should be able to "
                "check every box honestly:\n\n"
                "□ Match each radical shape to its substitution by the "
                "sign pattern, including coefficient handling "
                "($bx = a\\sin\\theta$) — and state the $\\theta$-range "
                "that makes the radical collapse without absolute "
                "values.\n"
                "□ Transform the differential correctly "
                "($dx = a\\cos\\theta\\,d\\theta$ etc.) every time.\n"
                "□ Finish the resulting $\\theta$-integrals with "
                "C203's toolkit: half-angle for even powers, parity "
                "splits, the $\\sec^3$ boomerang.\n"
                "□ Draw the correct reference triangle for each case "
                "and read off any trig function of $\\theta$ in terms "
                "of $x$ — expanding double angles first.\n"
                "□ Convert definite-integral bounds through the "
                "inverse trig function and stay in $\\theta$-land when "
                "the bounds are nice.\n"
                "□ Complete the square (both signs of the $x^2$ "
                "coefficient) to expose standard forms, shifting with "
                "a linear substitution.\n"
                "□ Articulate the secant case's branch issue and "
                "handle $x \\le -a$ by sign-tracking or reflection.\n"
                "□ Take the fast lane when the radicand's derivative "
                "is present — and explain why it wins.\n"
                "□ Derive the quarter-circle area and the "
                "$\\ln|x + \\sqrt{x^2 \\pm a^2}|$ table entries from "
                "scratch.\n"
                "□ Differentiate every answer as a reflex.\n\n"
                "When every box is checked, hit Practice until the "
                "form-to-substitution match is instant. Then proceed "
                "to C205 — partial fractions — where RATIONAL "
                "functions get their systematic decomposition, the "
                "technique core of Calculus 2 closes, and the "
                "strategy question 'which method, when?' gets its "
                "final, unified answer."
            )),
        ],
    },
}
