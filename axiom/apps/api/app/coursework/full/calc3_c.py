"""Full courses: C305-C308 — Tangent planes, the chain rule, the gradient,
and critical points.

FC-template lessons: 20 full pages each, matching the C101 gold standard
(calc1_a_limits.py) and the C304 chapter (calc3_a_partials.py) in depth and
structure. The four chapters form a continuous arc: linearize (C305), push
rates through compositions (C306), aim the derivative in any direction
(C307), and hunt extrema (C308).
"""

L = "reading"
E = "example"
P = "pitfall"
K = "check"

LESSONS: dict[str, dict] = {
    "C305": {
        "summary": (
            "A complete course on tangent planes and linearization: the tangent "
            "plane to z = f(x,y), the linearization L(x,y) and the total "
            "differential dz, what differentiability really means in two variables "
            "(and the counterexample where partials exist but the plane is a lie), "
            "error estimation with differentials, and tangent planes to level "
            "surfaces via the gradient normal."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "In C304 you learned to take a surface apart: slice it along the "
                "axis directions and measure two slopes, $f_x$ and $f_y$. This "
                "chapter puts the pieces back together. Out of those two numbers we "
                "will build the TANGENT PLANE — the flat sheet that hugs the surface "
                "$z = f(x, y)$ at a point — and its algebraic twin, the "
                "LINEARIZATION $L(x, y)$: the simplest function that imitates $f$ "
                "near the point.\n\n"
                "Why care about a flat imitation? Because 'replace the curved thing "
                "by its best flat approximation' is arguably THE central move of "
                "differential calculus. In one variable it was the tangent line "
                "(C112), and it powered every estimate, every Newton iteration, "
                "every error bound. In two variables the same move powers error "
                "propagation in laboratories, small-signal models in engineering, "
                "the stability analysis of differential equations, and the very "
                "STATEMENT of the chain rule you will meet in C306.\n\n"
                "There is also an honest surprise waiting. In one variable, "
                "'derivative exists' and 'tangent line exists' are the same thing. "
                "In two variables, the existence of both partials is NOT enough to "
                "guarantee a tangent plane — there are functions with perfectly "
                "good $f_x$ and $f_y$ at a point that are not even continuous "
                "there. Pages 10-11 dissect the standard counterexample, and the "
                "notion of DIFFERENTIABILITY that repairs the gap is this "
                "chapter's deepest idea.\n\n"
                "In this chapter you will learn: the tangent plane equation and "
                "where it comes from, the linearization and the total differential "
                "$dz$, what differentiability means and how to check it, how to "
                "estimate errors in computed quantities, and how to find tangent "
                "planes to surfaces given implicitly as $F(x, y, z) = k$ — a form "
                "that quietly introduces the gradient, star of C307."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Three ingredients, all recent.\n\n"
                "1. PARTIAL DERIVATIVES (C304). You must be able to compute "
                "$f_x$ and $f_y$ for any reasonable formula and EVALUATE them at a "
                "point. The tangent plane is assembled from the numbers "
                "$f(a, b)$, $f_x(a, b)$, $f_y(a, b)$ — three evaluations, no "
                "more.\n\n"
                "2. THE ONE-VARIABLE TEMPLATE (C112). Recall the tangent line and "
                "linearization at $x = a$:\n"
                "$$L(x) = f(a) + f'(a)(x - a),$$\n"
                "and the differential $dy = f'(a)\\,dx$ as an estimate of the true "
                "change $\\Delta y$. Every formula in this chapter is that one with "
                "a second variable grafted on. If C112 is fuzzy, reread it first — "
                "this chapter will feel like déjà vu instead of news.\n\n"
                "3. PLANES IN SPACE (C302). A plane through $(x_0, y_0, z_0)$ with "
                "normal vector $\\langle A, B, C \\rangle$ has equation "
                "$A(x - x_0) + B(y - y_0) + C(z - z_0) = 0$, and any equation "
                "$z = c + m(x - a) + n(y - b)$ describes a plane through "
                "$(a, b, c)$ with slopes $m$ and $n$ in the axis directions.\n\n"
                "Quick self-check (answers follow):\n\n"
                "• For $f(x, y) = x^2 y$, what are $f_x(2, 1)$ and $f_y(2, 1)$? "
                "($f_x = 2xy = 4$; $f_y = x^2 = 4$.)\n"
                "• Linearize $g(x) = \\sqrt{x}$ at $a = 4$ and estimate "
                "$\\sqrt{4.1}$. ($L(x) = 2 + \\frac{1}{4}(x - 4)$, so "
                "$\\sqrt{4.1} \\approx 2.025$.)\n"
                "• What is the normal direction of the plane $z = 3 + 2(x - 1) - "
                "5(y + 2)$? ($\\langle 2, -5, -1 \\rangle$ — note the $-1$ paired "
                "with $z$.)\n\n"
                "If all three were quick, proceed."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively: zoom in until it looks flat", (
                "Pull up any smooth surface — a dome, a saddle, a rippled sheet — "
                "and zoom in on one point. Double the magnification, then double it "
                "again. The curvature fades; the surface looks more and more like a "
                "tilted plane, the way the Earth looks flat from your backyard "
                "despite being round. DIFFERENTIABILITY is exactly this property: "
                "under magnification the surface becomes indistinguishable from a "
                "plane. That plane is the tangent plane.\n\n"
                "Which plane is it? Here C304's slices hand us the answer. Stand at "
                "$(a, b)$ on the surface. The slice $y = b$ is a curve through the "
                "point with slope $f_x(a, b)$; its tangent line lives in the "
                "slicing plane. The slice $x = a$ gives a second curve with slope "
                "$f_y(a, b)$ and a second tangent line. Two intersecting lines "
                "determine exactly one plane — and THAT plane, containing both "
                "slice tangents, is the tangent plane. Its slope in the "
                "$x$-direction is $f_x(a, b)$, in the $y$-direction $f_y(a, b)$: "
                "the two numbers from C304 are precisely the two tilts a plane "
                "has.\n\n"
                "Concretely: on the dome $z = 9 - x^2 - y^2$ at $(1, 2, 4)$, the "
                "eastward slice-tangent falls at slope $-2$ and the northward one "
                "at slope $-4$. The tangent plane is the sheet through $(1, 2, 4)$ "
                "tilted exactly that way: walk east and it drops 2 per step, walk "
                "north and it drops 4.\n\n"
                "One caution to carry forward: the two slice tangents determine A "
                "plane, but the surface has to cooperate in EVERY direction — "
                "northeast, south-southwest, all of them — for that plane to "
                "deserve the name 'tangent.' For nice functions it does. Page 10 "
                "shows a misbehaving function where the plane built from the "
                "slices is a fraud. Intuition first, fine print later."
            )),
            # ---------------------------------------------------------- 4
            (L, "The formal definitions: plane, linearization, differential", (
                "THE TANGENT PLANE. If $f$ has continuous partials near $(a, b)$, "
                "the tangent plane to $z = f(x, y)$ at $(a, b, f(a, b))$ is\n"
                "$$z = f(a, b) + f_x(a, b)\\,(x - a) + f_y(a, b)\\,(y - b).$$\n"
                "Read it as: start at the height of the surface, then tilt with "
                "slope $f_x(a,b)$ per unit east and $f_y(a,b)$ per unit north. "
                "Compare $y = f(a) + f'(a)(x - a)$: same skeleton, one more "
                "term.\n\n"
                "THE LINEARIZATION is the same expression viewed as a function,\n"
                "$$L(x, y) = f(a, b) + f_x(a, b)(x - a) + f_y(a, b)(y - b),$$\n"
                "the best linear stand-in for $f$ near $(a, b)$: "
                "$f(x, y) \\approx L(x, y)$ for $(x, y)$ near the center.\n\n"
                "THE TOTAL DIFFERENTIAL repackages the change. Write "
                "$dx = \\Delta x$, $dy = \\Delta y$ for the input changes; then\n"
                "$$dz = f_x(a, b)\\,dx + f_y(a, b)\\,dy$$\n"
                "estimates the true output change $\\Delta z = f(a + \\Delta x,\\, "
                "b + \\Delta y) - f(a, b)$. Each term is (rate) × (change), one "
                "per input; the effects ADD.\n\n"
                "DIFFERENTIABILITY, precisely. $f$ is differentiable at $(a, b)$ "
                "if the error of the linear estimate dies faster than the step:\n"
                "$$\\Delta z = f_x(a,b)\\,\\Delta x + f_y(a,b)\\,\\Delta y + "
                "\\varepsilon_1 \\Delta x + \\varepsilon_2 \\Delta y,$$\n"
                "where $\\varepsilon_1, \\varepsilon_2 \\to 0$ as "
                "$(\\Delta x, \\Delta y) \\to (0, 0)$. In words: the tangent plane "
                "approximates the surface to FIRST ORDER, with leftover error "
                "genuinely smaller than the displacement — from every approach "
                "direction, not just along the axes. This definition, not the mere "
                "existence of $f_x$ and $f_y$, is what 'the surface is locally "
                "flat' means. It is the hypothesis every later theorem (the chain "
                "rule of C306 above all) will quietly demand."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples: building planes and estimates", (
                "EXAMPLE 1 (a paraboloid). Find the tangent plane to "
                "$z = x^2 + y^2$ at $(1, 2)$.\n\n"
                "Three evaluations: $f(1, 2) = 5$; $f_x = 2x$ so $f_x(1,2) = 2$; "
                "$f_y = 2y$ so $f_y(1,2) = 4$. Assemble:\n"
                "$$z = 5 + 2(x - 1) + 4(y - 2).$$\n"
                "Sanity checks (do these EVERY time): at $(x, y) = (1, 2)$ the "
                "formula returns 5 — the plane passes through the point of "
                "tangency; the coefficient of $x$ matches the east-slice slope; "
                "the coefficient of $y$ matches the north-slice slope.\n\n"
                "EXAMPLE 2 (using the plane to estimate). Estimate $f(1.1, 1.9)$ "
                "for the same function. The plane says\n"
                "$$L(1.1, 1.9) = 5 + 2(0.1) + 4(-0.1) = 5 + 0.2 - 0.4 = 4.8.$$\n"
                "The truth: $1.1^2 + 1.9^2 = 1.21 + 3.61 = 4.82$. Error $0.02$ "
                "from steps of size $0.1$ — the error is an order smaller than "
                "the step, exactly the 'first order' promise of page 4.\n\n"
                "EXAMPLE 3 (a product, no calculator). Estimate "
                "$(2.01)^3 (0.98)^2$ by linearizing $f(x, y) = x^3 y^2$ at "
                "$(2, 1)$.\n\n"
                "$f(2, 1) = 8$. $f_x = 3x^2 y^2 \\Rightarrow f_x(2,1) = 12$. "
                "$f_y = 2x^3 y \\Rightarrow f_y(2,1) = 16$. With $dx = 0.01$, "
                "$dy = -0.02$:\n"
                "$$dz = 12(0.01) + 16(-0.02) = 0.12 - 0.32 = -0.20,$$\n"
                "so $(2.01)^3(0.98)^2 \\approx 8 - 0.20 = 7.80$. The exact value "
                "is $7.7990\\ldots$ — three correct decimal places for the price "
                "of two partial derivatives. Notice the workflow: pick a nearby "
                "ROUND point where $f$ and its partials are exact, then let the "
                "differential carry you to the ugly point."
            )),
            # ---------------------------------------------------------- 6
            (L, "The theorems: when the plane is real, and the implicit form", (
                "THEOREM (differentiability made checkable). If $f_x$ and $f_y$ "
                "exist near $(a, b)$ and are CONTINUOUS at $(a, b)$, then $f$ is "
                "differentiable at $(a, b)$. This is the workhorse: polynomials, "
                "exponentials, sines, and their compositions have continuous "
                "partials wherever defined, so their tangent planes are legitimate "
                "everywhere you would compute them. You will rarely verify the "
                "$\\varepsilon$-definition by hand; you will invoke this "
                "theorem.\n\n"
                "THEOREM (differentiable $\\Rightarrow$ continuous). If $f$ is "
                "differentiable at a point, it is continuous there. Contrapositive: "
                "a discontinuous function has NO tangent plane, no matter what its "
                "partials do. Keep the hierarchy straight:\n"
                "$$\\text{continuous partials} \\Rightarrow \\text{differentiable} "
                "\\Rightarrow \\text{partials exist},$$\n"
                "and BOTH arrows are one-way. The gap between the last two is the "
                "genuinely multivariable phenomenon of pages 10-11.\n\n"
                "TANGENT PLANES TO LEVEL SURFACES. Many surfaces arrive implicitly, "
                "as $F(x, y, z) = k$ — a sphere $x^2 + y^2 + z^2 = 14$, say — with "
                "no convenient $z = f(x, y)$. Theorem: the tangent plane at a "
                "point $P_0 = (x_0, y_0, z_0)$ on the surface is\n"
                "$$F_x(P_0)(x - x_0) + F_y(P_0)(y - y_0) + F_z(P_0)(z - z_0) = "
                "0,$$\n"
                "i.e. the vector of partials $\\langle F_x, F_y, F_z \\rangle$ — "
                "the GRADIENT $\\nabla F$, formally introduced in C307 — is NORMAL "
                "to the surface. The graph case is the special case "
                "$F = f(x, y) - z = 0$, whose normal is $\\langle f_x, f_y, -1 "
                "\\rangle$; unpacking the displayed equation recovers page 4's "
                "formula exactly. One geometric idea, two costumes: for graphs the "
                "plane is written as a height function; for level surfaces, via "
                "its normal vector."
            )),
            # ---------------------------------------------------------- 7
            (E, "Worked examples: differentials and implicit surfaces", (
                "EXAMPLE 1 ($dz$ versus $\\Delta z$, side by side). Let "
                "$z = x^2 + 3xy - y^2$, with $x$ moving from 2 to 2.05 and $y$ "
                "from 3 to 2.96.\n\n"
                "Differential: $dz = (2x + 3y)\\,dx + (3x - 2y)\\,dy$ evaluated at "
                "$(2, 3)$ with $dx = 0.05$, $dy = -0.04$:\n"
                "$$dz = (4 + 9)(0.05) + (6 - 6)(-0.04) = 0.65.$$\n"
                "Exact: $f(2.05, 2.96) = 4.2025 + 18.204 - 8.7616 = 13.6449$, and "
                "$f(2, 3) = 13$, so $\\Delta z = 0.6449$. The differential missed "
                "by $0.0051$ — under 1% of the change, computed with a fraction "
                "of the arithmetic. Note the $y$-term contributed ZERO here "
                "because $f_y(2,3) = 0$: the differential also tells you which "
                "inputs the output is insensitive to.\n\n"
                "EXAMPLE 2 (sphere, via the normal). Find the tangent plane to "
                "$x^2 + y^2 + z^2 = 14$ at $(1, 2, 3)$.\n\n"
                "$F = x^2 + y^2 + z^2$, so $\\langle F_x, F_y, F_z \\rangle = "
                "\\langle 2x, 2y, 2z \\rangle = \\langle 2, 4, 6 \\rangle$ at the "
                "point. Plane: $2(x-1) + 4(y-2) + 6(z-3) = 0$, which simplifies "
                "to $x + 2y + 3z = 14$. Geometry bonus: the normal "
                "$\\langle 2, 4, 6 \\rangle$ is parallel to the radius vector "
                "$\\langle 1, 2, 3 \\rangle$ — the plane is perpendicular to the "
                "radius, as every sphere-tangent should be. No solving for $z$, "
                "no square roots, no case split for the two hemispheres: the "
                "implicit method is strictly less work.\n\n"
                "EXAMPLE 3 (linearization at a smart center). Linearize "
                "$f(x, y) = x e^{xy}$ at $(1, 0)$: $f = 1$; $f_x = e^{xy} + "
                "xy e^{xy} = 1$; $f_y = x^2 e^{xy} = 1$. So $L(x, y) = 1 + "
                "(x - 1) + y = x + y$, and $f(1.1, -0.1) \\approx 1.0$ (exact: "
                "$0.985$)."
            )),
            # ---------------------------------------------------------- 8
            (L, "Strategy: a recipe, and how to choose your tools", (
                "THE FOUR-STEP RECIPE (graph form). (1) Identify the center "
                "$(a, b)$ and compute $f(a, b)$. (2) Compute the partial "
                "derivative FUNCTIONS $f_x$, $f_y$. (3) EVALUATE both at "
                "$(a, b)$ — after this step every coefficient is a number. "
                "(4) Assemble $z = f(a,b) + f_x(a,b)(x-a) + f_y(a,b)(y-b)$ and "
                "run the sanity check: plugging in $(a, b)$ must return "
                "$f(a, b)$.\n\n"
                "CHOOSING THE FORM. Surface given as $z = f(x, y)$: use the "
                "recipe. Given as $F(x, y, z) = k$: use the normal-vector form of "
                "page 6 directly — do NOT solve for $z$ unless it is trivial. If "
                "you need a normal vector for a graph (for a normal line, or to "
                "compare planes), rewrite $z = f(x,y)$ as $f(x, y) - z = 0$ and "
                "read off $\\langle f_x, f_y, -1 \\rangle$.\n\n"
                "CHOOSING THE CENTER. For estimation problems the center is "
                "yours to pick: choose the nearest point where $f$ AND its "
                "partials evaluate exactly (perfect squares, angles like "
                "$\\pi/4$, products of integers). The whole payoff of "
                "linearization is trading an ugly evaluation for a clean one "
                "plus a correction.\n\n"
                "$dz$ OR $\\Delta z$? Use $\\Delta z$ (exact recomputation) when "
                "you can afford it and the question demands truth. Use $dz$ when "
                "the inputs are measurements with uncertainties — then $\\Delta$ "
                "is unknowable anyway and $dz$ IS the error estimate — or when "
                "you want the sensitivity structure: $dz$'s two terms display "
                "which input drives the output.\n\n"
                "TRUST RADIUS. The linearization is a LOCAL license. Its error "
                "grows roughly like the square of the distance from the center, "
                "with a constant set by the second partials (C304's $f_{xx}, "
                "f_{xy}, f_{yy}$ — made precise in C308's quadratic "
                "approximation). Steps of a few percent: excellent. Steps of "
                "order one: recompute honestly."
            )),
            # ---------------------------------------------------------- 9
            (E, "Worked examples: harder problems", (
                "EXAMPLE 1 (ellipsoid, fractions and all). Tangent plane to "
                "$\\frac{x^2}{4} + y^2 + \\frac{z^2}{9} = 3$ at $(-2, 1, -3)$.\n\n"
                "$F = \\frac{x^2}{4} + y^2 + \\frac{z^2}{9}$ gives "
                "$\\langle F_x, F_y, F_z \\rangle = \\left\\langle \\frac{x}{2}, "
                "2y, \\frac{2z}{9} \\right\\rangle = \\left\\langle -1, 2, "
                "-\\frac{2}{3} \\right\\rangle$ at the point. Plane:\n"
                "$$-(x + 2) + 2(y - 1) - \\tfrac{2}{3}(z + 3) = 0 "
                "\\;\\Longrightarrow\\; 3x - 6y + 2z + 18 = 0$$\n"
                "(after multiplying by $-3$). Check the point: $-6 - 6 - 6 + 18 "
                "= 0$. ✓\n\n"
                "EXAMPLE 2 (finding WHERE the plane has a given tilt). At what "
                "point is the tangent plane to $z = x^2 + y^2$ parallel to the "
                "plane $z = 4x + 6y - 5$?\n\n"
                "Parallel planes share normals. The target has normal "
                "$\\langle 4, 6, -1 \\rangle$; the graph has normal "
                "$\\langle 2x, 2y, -1 \\rangle$. Matching: $2x = 4$, $2y = 6$, "
                "so $(x, y) = (2, 3)$, where $z = 13$. The tangent plane there is "
                "$z = 13 + 4(x - 2) + 6(y - 3) = 4x + 6y - 13$ — same tilt as the "
                "target, different offset, as parallel demands. This 'match the "
                "normal' template also answers 'where is the tangent plane "
                "horizontal?' (normal $\\parallel \\langle 0, 0, 1 \\rangle$, "
                "i.e. $f_x = f_y = 0$) — which is exactly the critical-point "
                "equation of C308.\n\n"
                "EXAMPLE 3 (normal line). The normal LINE to the sphere "
                "$x^2 + y^2 + z^2 = 14$ at $(1, 2, 3)$ runs along the normal "
                "vector: $\\mathbf{r}(t) = \\langle 1 + 2t,\\, 2 + 4t,\\, 3 + 6t "
                "\\rangle$. At $t = -\\frac{1}{2}$ it passes through the origin — "
                "the sphere's center, as geometry insists. Implicit normals hand "
                "you normal lines for free."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: when the partials lie", (
                "Here is the fine print promised on page 3, and it is not a "
                "curiosity — it is the reason 'differentiable' is a separate "
                "word.\n\n"
                "THE STANDARD COUNTEREXAMPLE. Define\n"
                "$$f(x, y) = \\frac{xy}{x^2 + y^2} \\;\\; (x, y) \\ne (0,0), "
                "\\qquad f(0, 0) = 0.$$\n"
                "Both partials EXIST at the origin: along the $x$-axis "
                "$f(x, 0) = 0$ identically, so $f_x(0,0) = 0$; likewise "
                "$f_y(0,0) = 0$. The would-be tangent plane is therefore "
                "$z = 0$. But walk toward the origin along the diagonal "
                "$y = x$: there $f(x, x) = \\frac{x^2}{2x^2} = \\frac{1}{2}$, "
                "for EVERY $x \\ne 0$. The function sits at height $\\frac12$ "
                "along that whole ray while claiming height 0 at the origin — "
                "it is not even CONTINUOUS there, so by page 6 it cannot be "
                "differentiable, and the plane $z = 0$ is a fraud. The partials "
                "never noticed because they only ever look along the axes; the "
                "surface misbehaves between them.\n\n"
                "WHY THIS CANNOT HAPPEN IN ONE VARIABLE: on a line there are "
                "only two approach directions, and the single derivative "
                "watches both. In the plane there are infinitely many "
                "directions, and two axis-slopes cannot police them all. "
                "Differentiability — the $\\varepsilon$-definition of page 4 — "
                "is precisely the promise that holds in ALL directions at "
                "once.\n\n"
                "A GENTLER FAILURE. The cone $z = \\sqrt{x^2 + y^2}$ is "
                "continuous everywhere, but at the origin the slice "
                "$f(x, 0) = |x|$ has a corner: $f_x(0,0)$ does not even exist. "
                "No partials, no plane — the cone's tip is a genuine point, not "
                "a locally flat spot. Between these two examples you see the "
                "hierarchy of page 6 with both gaps realized: partials without "
                "differentiability, and continuity without partials."
            )),
            # ---------------------------------------------------------- 11
            (E, "Worked example: dissecting the counterexample", (
                "Work through the pathology of page 10 with full computations — "
                "this is a classic exam question in its own right.\n\n"
                "STEP 1 (partials at the origin, from the limit definition — the "
                "shortcut rules do not apply at a patched point):\n"
                "$$f_x(0,0) = \\lim_{h \\to 0} \\frac{f(h, 0) - f(0,0)}{h} = "
                "\\lim_{h \\to 0} \\frac{0 - 0}{h} = 0,$$\n"
                "and symmetrically $f_y(0,0) = 0$. Both exist; both are zero.\n\n"
                "STEP 2 (test continuity along lines $y = mx$): for $x \\ne 0$,\n"
                "$$f(x, mx) = \\frac{m x^2}{x^2 + m^2 x^2} = \\frac{m}{1 + "
                "m^2}.$$\n"
                "The value depends on the slope $m$ and not on the distance: "
                "height $0$ along the axes ($m = 0$), height $\\frac{1}{2}$ "
                "along $m = 1$, height $-\\frac{1}{2}$ along $m = -1$. Different "
                "directions give different limiting heights, so "
                "$\\lim_{(x,y) \\to (0,0)} f$ does not exist and $f$ is "
                "discontinuous at the origin. By 'differentiable $\\Rightarrow$ "
                "continuous,' $f$ is NOT differentiable there, despite Step 1.\n\n"
                "STEP 3 (see the failure in the $\\varepsilon$-definition): with "
                "$\\Delta x = \\Delta y = h$, the claimed linear estimate is "
                "$0$, but $\\Delta z = f(h, h) = \\frac12$. The error is "
                "$\\frac12$, a CONSTANT — it does not die as $h \\to 0$, let "
                "alone die faster than $h$.\n\n"
                "CONTRAST (a clean function passing the same test). For "
                "$f(x, y) = x^2 + y^2$ at the origin: $\\Delta z = \\Delta x^2 + "
                "\\Delta y^2 = (\\Delta x)\\Delta x + (\\Delta y)\\Delta y$, "
                "which is the definition's form with $\\varepsilon_1 = \\Delta "
                "x \\to 0$ and $\\varepsilon_2 = \\Delta y \\to 0$: "
                "differentiable, honestly, from the definition itself."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'The partials exist, so the tangent plane "
                "exists.' The whole lesson of pages 10-11. Existence of $f_x$ "
                "and $f_y$ checks two directions out of infinitely many. The "
                "license to write a tangent plane is DIFFERENTIABILITY, and the "
                "everyday certificate is continuous partials (page 6). For the "
                "standard functions of this course the certificate is routine — "
                "but say the right words when asked WHY the plane exists.\n\n"
                "MISCONCEPTION 2: '$dz$ IS the change in $z$.' $dz$ is the "
                "PLANE's change; $\\Delta z$ is the SURFACE's. They differ by an "
                "error that shrinks like the square of the step — negligible for "
                "small steps, disastrous for large ones. Writing $=$ between "
                "$\\Delta z$ and $dz$ on an exam costs points; writing "
                "$\\approx$ shows you know the difference.\n\n"
                "MISCONCEPTION 3: 'The tangent plane touches the surface at one "
                "point and stays on one side.' Both clauses can fail. At a "
                "saddle — $z = x^2 - y^2$ at the origin, tangent plane "
                "$z = 0$ — the plane SLICES THROUGH the surface, above it in "
                "two quadrants, below it in the others. Tangency is a "
                "first-order matching of tilts, not a geometric caress. (C114's "
                "inflection points were the one-variable version of the same "
                "surprise.)\n\n"
                "MISCONCEPTION 4: 'The linearization is a good approximation.' "
                "Incomplete without the words NEAR THE CENTER. $L(x, y)$ is "
                "excellent within a small neighborhood, worthless far away — "
                "the dome $z = 9 - x^2 - y^2$ has tangent plane $z = 9$ at its "
                "peak, which predicts height 9 forever while the dome dives to "
                "$-\\infty$.\n\n"
                "MISCONCEPTION 5: 'For $F(x,y,z) = k$ you must first solve for "
                "$z$.' Backwards — the normal form exists precisely so you "
                "never have to. Solving for $z$ usually introduces square "
                "roots, case splits, and pain; the gradient normal is three "
                "partials and done."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational errors", (
                "ERROR 1: Leaving the partials UNEVALUATED. "
                "$z = 5 + 2x(x - 1) + 2y(y - 2)$ is not a plane — it is a "
                "quadratic surface, and it means the author skipped step 3 of "
                "the recipe. Every coefficient in a tangent plane is a NUMBER. "
                "If an $x$ or $y$ survives outside the factors $(x - a)$, "
                "$(y - b)$, you are not done.\n\n"
                "ERROR 2: Sign slips at negative centers. At $a = -2$ the "
                "factor is $(x - (-2)) = (x + 2)$. Rushed work produces "
                "$(x - 2)$ and a plane that misses the surface entirely. The "
                "instant check — plug the center in, expect $f(a,b)$ — catches "
                "this every time, in five seconds.\n\n"
                "ERROR 3: Dropping the constant term. Estimates like "
                "'$f(1.1, 1.9) \\approx 0.2 - 0.4 = -0.2$' forget that $dz$ is "
                "the CHANGE, to be added to $f(a, b)$. The linearization has "
                "three terms; the differential is only two of them.\n\n"
                "ERROR 4: The normal to a GRAPH is $\\langle f_x, f_y, -1 "
                "\\rangle$, not $\\langle f_x, f_y, 1 \\rangle$ and not "
                "$\\langle f_x, f_y \\rangle$. The $-1$ comes from writing "
                "$f(x, y) - z = 0$ and differentiating with respect to $z$; "
                "forget it and every parallel-plane and normal-line answer is "
                "wrong. (Either overall sign is fine — $\\langle -f_x, -f_y, 1 "
                "\\rangle$ is the same line — but the RELATIVE sign is not "
                "negotiable.)\n\n"
                "ERROR 5: Error-propagation bookkeeping. A DECREASE enters as a "
                "negative differential ($dy = -0.04$, not $0.04$); maximum-error "
                "questions take ABSOLUTE values of each term (the worst case "
                "compounds, never cancels); and mixing absolute error "
                "($\\pm 0.02$ cm) with relative error ($\\pm 0.4\\%$) in one sum "
                "produces nonsense. Decide which currency the problem wants "
                "before adding."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: the mathematics of measurement error", (
                "The total differential is the working mathematician's error "
                "bar. Every measured quantity comes with uncertainty; every "
                "quantity COMPUTED from measurements inherits uncertainty "
                "through the formula. The differential says exactly how much.\n\n"
                "1. PROPAGATION. If $z = f(x, y)$ with measured $x = a \\pm "
                "\\delta_x$ and $y = b \\pm \\delta_y$, the worst-case error "
                "estimate is $|dz| \\le |f_x(a,b)|\\,\\delta_x + "
                "|f_y(a,b)|\\,\\delta_y$. Laboratories run this computation on "
                "every derived quantity; it is the reason lab reports quote "
                "$g = 9.81 \\pm 0.03$ rather than a bare number.\n\n"
                "2. SENSITIVITY ANALYSIS. The two terms of $dz$ are a budget: "
                "they show WHICH input's uncertainty dominates the output. If "
                "the $x$-term is ten times the $y$-term, improving the "
                "$y$-measurement is wasted effort. Page 15 shows this verdict "
                "emerging from a concrete geometry problem.\n\n"
                "3. RELATIVE ERROR AND LOG DIFFERENTIALS. For products and "
                "powers, relative errors combine by a beautiful rule: if "
                "$z = x^m y^n$ then $\\frac{dz}{z} = m\\frac{dx}{x} + "
                "n\\frac{dy}{y}$ — exponents multiply relative errors. (Proof: "
                "take $\\ln$ first, then $d$.) A 1% error in $x$ becomes an "
                "$m$% error in $z$. This is why quantities measured to a power — "
                "radii in $r^2$ or $r^3$ formulas — deserve your best "
                "instrument.\n\n"
                "4. ENGINEERING LINEARIZATION. Circuit designers replace a "
                "transistor's nonlinear response by its tangent plane around an "
                "operating point ('small-signal model'); control engineers "
                "linearize dynamics near an equilibrium and read stability from "
                "the linearization's eigenvalues (ODE.U7.N2, LA.U7.N1). In each "
                "case an intractable nonlinear object is replaced, locally and "
                "legitimately, by the flat model this chapter builds — the "
                "single most-reused idea you will meet this semester."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: tolerances on a machined cylinder", (
                "A metal cylinder is specified as radius $r = 5$ cm and height "
                "$h = 12$ cm, with machining tolerances $|dr| \\le 0.02$ cm and "
                "$|dh| \\le 0.03$ cm. Question: how much can the volume "
                "$V = \\pi r^2 h$ deviate from spec, and which tolerance should "
                "be tightened first?\n\n"
                "STEP 1 — the differential:\n"
                "$$dV = \\frac{\\partial V}{\\partial r}\\,dr + "
                "\\frac{\\partial V}{\\partial h}\\,dh = 2\\pi r h\\,dr + "
                "\\pi r^2\\,dh.$$\n\n"
                "STEP 2 — evaluate at spec: $2\\pi (5)(12) = 120\\pi$ per cm of "
                "radius error; $\\pi(25) = 25\\pi$ per cm of height error.\n\n"
                "STEP 3 — worst case (absolute values, since errors may "
                "conspire):\n"
                "$$|dV| \\le 120\\pi(0.02) + 25\\pi(0.03) = 2.4\\pi + 0.75\\pi "
                "= 3.15\\pi \\approx 9.9 \\text{ cm}^3,$$\n"
                "about 1.05% of the nominal $V = 300\\pi \\approx 942.5$ "
                "cm³.\n\n"
                "STEP 4 — the sensitivity verdict. The radius contributes "
                "$2.4\\pi$ of the error budget, the height only $0.75\\pi$ — "
                "76% versus 24% — even though the radius tolerance is the "
                "TIGHTER one. The relative-error rule of page 14 says why "
                "instantly: $V = \\pi r^2 h$ gives\n"
                "$$\\frac{dV}{V} = 2\\frac{dr}{r} + \\frac{dh}{h} = 2(0.4\\%) + "
                "0.25\\% = 1.05\\%.$$\n"
                "The radius enters SQUARED, so its relative error counts double. "
                "Engineering answer: money spent improving the lathe's radial "
                "precision pays twice as fast as money spent on height "
                "precision. Two partial derivatives, one management "
                "decision."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: where this chapter plugs into everything else", (
                "BACKWARD. C304 supplied the coefficients: a tangent plane is "
                "two partials and a value, assembled. C112 supplied the "
                "template: everything here is $L(x) = f(a) + f'(a)(x-a)$ with "
                "one more term. C302 supplied the geometry of planes and "
                "normals; C301 and LA.U8.N1 the dot-product language the "
                "normal-vector form is written in.\n\n"
                "FORWARD, immediately:\n"
                "• C306 (chain rule): the multivariable chain rule is FALSE "
                "without differentiability — its proof is one line of this "
                "chapter's $\\varepsilon$-definition. When C306 says 'suppose "
                "$f$ is differentiable,' it means page 4.\n"
                "• C307 (gradient): the vector $\\langle F_x, F_y, F_z "
                "\\rangle$ that kept appearing as a normal gets its name, "
                "$\\nabla F$, and its full geometric story — steepest ascent, "
                "perpendicularity to level sets. Page 6's level-surface "
                "theorem is a C307 theorem met early.\n"
                "• C308 (critical points): where the tangent plane is "
                "HORIZONTAL ($f_x = f_y = 0$), the linear term vanishes and the "
                "second-order terms take over — the Hessian test of C308 is "
                "'what the surface does when the plane goes flat.'\n"
                "• C309 (Lagrange): tangency of level sets — two surfaces "
                "sharing a tangent plane — is the geometric engine of "
                "constrained optimization.\n\n"
                "FURTHER OUT. Newton's method for systems solves the LINEARIZED "
                "equations at each step (NUMERICS). ODE.U7.N2 linearizes a "
                "nonlinear system at an equilibrium and reads stability from "
                "eigenvalues (LA.U7.N1) — tangent planes for dynamics. And "
                "differential geometry rebuilds this chapter's plane as the "
                "'tangent space,' the stage on which curvature itself is "
                "defined. Local flatness is a load-bearing wall of "
                "mathematics."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: the derivative as a linear map", (
                "Rearrange the differentiability definition and a modern idea "
                "emerges. Collect the two partials into a $1 \\times 2$ matrix "
                "$Df(a,b) = [\\,f_x(a,b) \\;\\; f_y(a,b)\\,]$ and write the "
                "step as a column vector $\\mathbf{h} = \\langle \\Delta x, "
                "\\Delta y \\rangle$. The definition of page 4 says exactly:\n"
                "$$\\lim_{\\mathbf{h} \\to \\mathbf{0}} "
                "\\frac{\\left| f(\\mathbf{a} + \\mathbf{h}) - f(\\mathbf{a}) - "
                "Df(\\mathbf{a})\\,\\mathbf{h} \\right|}{|\\mathbf{h}|} = 0.$$\n"
                "Read: THE DERIVATIVE IS NOT A NUMBER BUT A LINEAR MAP — the "
                "matrix that best converts input steps into output steps. The "
                "tangent plane is just the graph of the affine map "
                "$\\mathbf{h} \\mapsto f(\\mathbf{a}) + Df(\\mathbf{a})"
                "\\mathbf{h}$.\n\n"
                "This viewpoint, due to Fréchet (1911), scales without "
                "friction. A map $\\mathbb{R}^n \\to \\mathbb{R}^m$ has an "
                "$m \\times n$ JACOBIAN matrix of partials; the same limit "
                "defines differentiability; and the chain rule of C306 becomes "
                "the cleanest statement in the subject — compose the "
                "functions, MULTIPLY the matrices. One-variable calculus hid "
                "all this because a $1 \\times 1$ matrix looks like a "
                "number.\n\n"
                "It also explains this chapter's pathology in one sentence: "
                "the partials are just the matrix ENTRIES, and knowing a "
                "matrix's entries along the axes does not make the limit above "
                "hold — differentiability is a statement about the map, not "
                "the entries. Nineteenth-century analysts (Cauchy among them) "
                "conflated the two for decades; the counterexamples of page 10 "
                "are the fossils of that confusion.\n\n"
                "Last upgrade: Taylor's theorem. The tangent plane is the "
                "degree-1 Taylor polynomial; C308's quadratic form "
                "$\\frac12(f_{xx}h^2 + 2f_{xy}hk + f_{yy}k^2)$ is degree 2, "
                "and it governs exactly the error this chapter kept calling "
                "'second order.' The story continues there."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "TANGENT PLANE (graph form), at $(a, b, f(a,b))$:\n"
                "$$z = f(a,b) + f_x(a,b)(x - a) + f_y(a,b)(y - b).$$\n"
                "All coefficients are NUMBERS; plane passes through the point "
                "of tangency (always check).\n\n"
                "LINEARIZATION: $L(x, y) =$ the right-hand side above; "
                "$f \\approx L$ near $(a, b)$ only.\n\n"
                "TOTAL DIFFERENTIAL: $dz = f_x(a,b)\\,dx + f_y(a,b)\\,dy "
                "\\approx \\Delta z$. Error shrinks like the square of the "
                "step. Worst-case error: add terms in absolute value. "
                "Relative-error rule: $z = x^m y^n \\Rightarrow \\frac{dz}{z} "
                "= m\\frac{dx}{x} + n\\frac{dy}{y}$.\n\n"
                "DIFFERENTIABILITY: $\\Delta z = f_x\\Delta x + f_y\\Delta y + "
                "\\varepsilon_1\\Delta x + \\varepsilon_2\\Delta y$ with "
                "$\\varepsilon_i \\to 0$. Hierarchy:\n"
                "$$\\text{continuous partials} \\Rightarrow "
                "\\text{differentiable} \\Rightarrow \\text{partials exist "
                "(and } f \\text{ continuous)},$$\n"
                "arrows one-way. Standard counterexample for the gap: "
                "$f = \\frac{xy}{x^2+y^2}$, $f(0,0) = 0$ — partials exist at "
                "the origin, function not even continuous there ($f = "
                "\\frac12$ along $y = x$).\n\n"
                "LEVEL SURFACE $F(x,y,z) = k$: normal is $\\nabla F = \\langle "
                "F_x, F_y, F_z \\rangle$; tangent plane "
                "$F_x(x - x_0) + F_y(y - y_0) + F_z(z - z_0) = 0$. Graph as "
                "level surface: $f(x,y) - z = 0$, normal $\\langle f_x, f_y, "
                "-1 \\rangle$.\n\n"
                "WORKFLOW. (1) Which form — graph or implicit? (2) Compute "
                "partials, EVALUATE at the point. (3) Assemble; sanity-check "
                "with the center. (4) For estimates: round center, $dz$, add "
                "to $f(a,b)$. (5) For error budgets: absolute values, then "
                "read off the dominant term."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all eight before reading the solutions.\n\n"
                "1. Tangent plane to $z = x^2 y$ at $(2, 1)$?\n"
                "2. Linearize $f(x, y) = \\ln(x - 3y)$ at $(7, 2)$ and estimate "
                "$f(6.9, 2.06)$.\n"
                "3. Total differential of $V = \\pi r^2 h$?\n"
                "4. True or false: if $f_x(0,0)$ and $f_y(0,0)$ both exist, "
                "then $f$ is continuous at $(0,0)$.\n"
                "5. Tangent plane to $xyz = 6$ at $(1, 2, 3)$?\n"
                "6. Where is the tangent plane to $z = x^2 - 2x + y^2$ "
                "horizontal?\n"
                "7. Estimate $f(1.95, 1.08)$ for $f(x,y) = "
                "\\sqrt{20 - x^2 - 7y^2}$ (center $(2, 1)$).\n"
                "8. Power $P = V^2/R$: if $V$ reads 2% high and $R$ reads 1% "
                "low, by what percent is the computed $P$ off?\n\n"
                "SOLUTIONS.\n"
                "1. $f(2,1) = 4$, $f_x = 2xy = 4$, $f_y = x^2 = 4$: "
                "$z = 4 + 4(x-2) + 4(y-1) = 4x + 4y - 8$.\n"
                "2. $f(7,2) = \\ln 1 = 0$; $f_x = \\frac{1}{x-3y} = 1$; "
                "$f_y = \\frac{-3}{x-3y} = -3$. $L = (x-7) - 3(y-2)$. "
                "$L(6.9, 2.06) = -0.1 - 0.18 = -0.28$ (exact: $\\ln 0.72 "
                "\\approx -0.329$; the step was largish).\n"
                "3. $dV = 2\\pi r h\\,dr + \\pi r^2\\,dh$.\n"
                "4. FALSE — $\\frac{xy}{x^2+y^2}$ is the counterexample "
                "(pages 10-11); partials at the origin exist yet no limit "
                "exists there.\n"
                "5. $\\nabla F = \\langle yz, xz, xy \\rangle = \\langle 6, 3, "
                "2 \\rangle$: $6(x-1) + 3(y-2) + 2(z-3) = 0$, i.e. "
                "$6x + 3y + 2z = 18$.\n"
                "6. $f_x = 2x - 2 = 0$, $f_y = 2y = 0$: at $(1, 0)$, where "
                "$z = -1$; the plane is $z = -1$.\n"
                "7. $f(2,1) = \\sqrt{9} = 3$; $f_x = \\frac{-x}{f} = "
                "-\\frac23$; $f_y = \\frac{-7y}{f} = -\\frac73$. "
                "$dz = -\\frac23(-0.05) - \\frac73(0.08) = 0.033 - 0.187 = "
                "-0.153$: estimate $2.85$.\n"
                "8. $\\frac{dP}{P} = 2\\frac{dV}{V} - \\frac{dR}{R} = "
                "2(2\\%) - (-1\\%) = 5\\%$ high.\n\n"
                "Scoring: 8/8 — proceed. Misses on 4 → pages 10-11; on 5 → "
                "pages 6-7; on 8 → pages 14-15."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Before leaving this chapter you should be able to check every "
                "box honestly:\n\n"
                "□ Write the tangent plane to $z = f(x,y)$ at a point in one "
                "pass: value, partials, evaluate, assemble — and run the "
                "plug-in-the-center sanity check.\n"
                "□ State the linearization $L(x, y)$ and explain in one "
                "sentence why it is the 'best linear approximation.'\n"
                "□ Use the total differential to estimate a change, an "
                "absolute worst-case error, and a relative error — and know "
                "which currency a problem is asking for.\n"
                "□ State the $\\varepsilon$-definition of differentiability "
                "and the hierarchy: continuous partials $\\Rightarrow$ "
                "differentiable $\\Rightarrow$ partials exist.\n"
                "□ Reproduce the standard counterexample "
                "($\\frac{xy}{x^2+y^2}$): compute its partials at the origin "
                "from the limit definition and show it is not continuous "
                "there.\n"
                "□ Explain why the cone $\\sqrt{x^2+y^2}$ has no tangent plane "
                "at its tip, and which hypothesis fails.\n"
                "□ Find tangent planes to level surfaces $F(x,y,z) = k$ via "
                "the normal $\\langle F_x, F_y, F_z \\rangle$, without solving "
                "for $z$.\n"
                "□ Convert a graph to implicit form and read off its normal "
                "$\\langle f_x, f_y, -1 \\rangle$ — with the $-1$ in place.\n"
                "□ Solve 'where is the tangent plane parallel to a given "
                "plane / horizontal?' by matching normals.\n"
                "□ Run a sensitivity analysis: identify which measurement "
                "dominates an error budget and defend the verdict with the "
                "two terms of $dz$.\n\n"
                "When every box is checked, hit Practice on this node, then "
                "proceed to C306 — where the linear model you just built "
                "starts MOVING, and the chain rule falls out of it."
            )),
        ],
    },
    "C306": {
        "summary": (
            "A complete course on the multivariable chain rule: dependence trees, "
            "the two classical cases and the one-term-per-path general pattern, "
            "why differentiability makes it work, implicit differentiation via "
            "-F_x/F_y, related rates with several moving inputs, and second "
            "derivatives through the chain rule."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "You can differentiate $f(x, y)$ when $x$ and $y$ are free "
                "(C304), and you can replace $f$ by its linear model near a point "
                "(C305). But in the wild, inputs are rarely free — they are "
                "themselves changing. The temperature you feel depends on your "
                "position, and your position depends on time: what is the "
                "temperature's rate of change ALONG YOUR PATH? A firm's cost "
                "depends on steel and energy prices, which both depend on the "
                "price of oil: how sensitive is cost to oil? Every such question "
                "is about a COMPOSITION — functions plugged into functions — and "
                "differentiating compositions is the chain rule's job.\n\n"
                "In one variable the chain rule was a single product: "
                "$\\frac{dy}{dt} = \\frac{dy}{dx}\\frac{dx}{dt}$. With several "
                "variables a new phenomenon appears: influence can flow along "
                "SEVERAL ROUTES at once. Time affects $z$ through $x$ AND through "
                "$y$, and the two influences add. The whole multivariable chain "
                "rule is that one sentence — MULTIPLY ALONG EACH PATH, ADD THE "
                "PATHS — plus bookkeeping. Master the bookkeeping (dependence "
                "trees, page 3) and every case collapses into one pattern.\n\n"
                "The payoff list is long. Implicit differentiation, which felt "
                "like a trick in Calculus 1, becomes a two-line formula. Related "
                "rates graduate to systems with several moving parts. The "
                "directional derivative of C307 IS a chain rule computation. The "
                "proof of Lagrange multipliers (C309) is one line of chain rule. "
                "And the algorithm that trains every neural network — "
                "backpropagation — is this chapter's pattern applied a billion "
                "times (page 17).\n\n"
                "You will learn: the two classical cases, the general "
                "one-term-per-path rule, implicit differentiation via "
                "$-F_x/F_y$, related rates in several variables, and how second "
                "derivatives pass through a composition."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Four ingredients.\n\n"
                "1. PARTIAL DERIVATIVES (C304), fluent and fast — every chain "
                "rule term contains one.\n\n"
                "2. DIFFERENTIALS AND DIFFERENTIABILITY (C305). The chain rule's "
                "hypothesis is '$f$ differentiable,' and its proof is one "
                "manipulation of $\\Delta z = f_x \\Delta x + f_y \\Delta y + "
                "\\varepsilon_1 \\Delta x + \\varepsilon_2 \\Delta y$. If that "
                "display looks unfamiliar, reread C305 page 4.\n\n"
                "3. THE ONE-VARIABLE CHAIN RULE (C105-C110 toolkit): "
                "$\\frac{d}{dt} f(g(t)) = f'(g(t))\\,g'(t)$ — including the "
                "discipline of evaluating $f'$ AT $g(t)$, not at $t$. That "
                "evaluation discipline is where multivariable errors live "
                "too.\n\n"
                "4. PARAMETRIZED CURVES (C303). A pair $x = x(t)$, $y = y(t)$ "
                "traces a path in the plane with velocity components $x'(t)$, "
                "$y'(t)$. This chapter constantly rides functions along such "
                "paths.\n\n"
                "Quick self-check (answers follow):\n\n"
                "• $\\frac{d}{dt}\\sin(t^2)$? (One-variable chain: "
                "$2t\\cos(t^2)$.)\n"
                "• For $f(x, y) = x^2 y$: $f_x$ and $f_y$? ($2xy$ and $x^2$.)\n"
                "• If $x = \\cos t$, $y = \\sin t$, what are $x'(t)$ and "
                "$y'(t)$, and what path is traced? ($-\\sin t$, $\\cos t$; the "
                "unit circle.)\n"
                "• In $\\frac{d}{dt} f(g(t)) = f'(g(t))g'(t)$, why is it wrong "
                "to write $f'(t)$? (The outer rate must be measured where the "
                "outer function actually IS — at input $g(t)$.)\n\n"
                "That last question is the single most transferable habit in "
                "this chapter. If all four were comfortable, proceed."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively: trees of dependence", (
                "Draw the dependencies. Put $z$ at the top. Below it, one node "
                "for each variable $z$ directly depends on ($x$ and $y$). Below "
                "those, one node for each variable THEY depend on (say both "
                "depend on $t$). Connect with edges. You have drawn a DEPENDENCE "
                "TREE:\n\n"
                "$z$ branches to $x$ and $y$; $x$ leads down to $t$; $y$ leads "
                "down to $t$.\n\n"
                "Now the entire chain rule in two sentences. Each EDGE carries a "
                "derivative of the upper variable with respect to the lower "
                "($\\partial z/\\partial x$ on the $z$-$x$ edge, $dx/dt$ on the "
                "$x$-$t$ edge). To find the derivative of the top with respect "
                "to a bottom variable: MULTIPLY the edge-derivatives along each "
                "downward path, then ADD the results over all paths from top to "
                "bottom.\n\n"
                "Why multiplication along a path? Dimensional analysis makes it "
                "obvious. If $z$ changes 3 degrees per meter of $x$, and $x$ "
                "grows 2 meters per second, then through that route $z$ changes "
                "$3 \\times 2 = 6$ degrees per second — rates in series "
                "multiply, units cancelling like fractions. Why addition across "
                "paths? Because for small changes, effects superpose: the total "
                "change in $z$ is (change via $x$) plus (change via $y$). That "
                "superposition is exactly what C305's linear model licenses — "
                "the differential $dz = f_x\\,dx + f_y\\,dy$ already ADDS one "
                "term per input.\n\n"
                "Two paths from $z$ to $t$ means two products to add. Count "
                "paths before you compute: a tree with three intermediates and "
                "two independents has three terms per independent variable, and "
                "knowing that BEFORE differentiating catches most dropped-term "
                "errors before they happen. The tree is not a mnemonic bolted "
                "onto the theorem; it IS the theorem, drawn."
            )),
            # ---------------------------------------------------------- 4
            (L, "The formal statements: the two cases and the general rule", (
                "CASE 1 (curve inputs: $z = f(x, y)$, $x = x(t)$, $y = y(t)$). "
                "If $f$ is differentiable and $x(t), y(t)$ are differentiable, "
                "then $z(t) = f(x(t), y(t))$ is differentiable and\n"
                "$$\\frac{dz}{dt} = \\frac{\\partial z}{\\partial x}\\, "
                "\\frac{dx}{dt} + \\frac{\\partial z}{\\partial y}\\, "
                "\\frac{dy}{dt},$$\n"
                "with both partials evaluated at $(x(t), y(t))$. One path "
                "through $x$, one through $y$: two products, added.\n\n"
                "WHERE IT COMES FROM (the one-line proof sketch). C305 gives "
                "$\\Delta z = f_x \\Delta x + f_y \\Delta y + \\varepsilon_1 "
                "\\Delta x + \\varepsilon_2 \\Delta y$. Divide by $\\Delta t$ "
                "and let $\\Delta t \\to 0$: the quotients become $x'(t)$ and "
                "$y'(t)$, and the $\\varepsilon$ terms die because the "
                "$\\varepsilon$'s go to zero while the quotients stay bounded. "
                "Differentiability is not decoration — without it the "
                "$\\varepsilon$ terms need not vanish and the formula can lie "
                "(page 10 shows it lying).\n\n"
                "CASE 2 (two independent variables: $z = f(x, y)$, "
                "$x = x(s, t)$, $y = y(s, t)$). Then\n"
                "$$\\frac{\\partial z}{\\partial s} = "
                "\\frac{\\partial z}{\\partial x}\\frac{\\partial x}{\\partial s} "
                "+ \\frac{\\partial z}{\\partial y}"
                "\\frac{\\partial y}{\\partial s}, \\qquad "
                "\\frac{\\partial z}{\\partial t} = "
                "\\frac{\\partial z}{\\partial x}\\frac{\\partial x}{\\partial t} "
                "+ \\frac{\\partial z}{\\partial y}"
                "\\frac{\\partial y}{\\partial t}.$$\n"
                "Same tree, two bottom variables; differentiate toward one "
                "while the other rides along frozen (that is why the bottom-edge "
                "derivatives are partials now).\n\n"
                "THE GENERAL RULE. For any dependence tree, however wide or "
                "deep:\n"
                "$$\\frac{\\partial(\\text{top})}{\\partial(\\text{bottom})} = "
                "\\sum_{\\text{paths}} \\;\\prod_{\\text{edges on path}} "
                "(\\text{edge derivative}).$$\n"
                "Every named 'case' in every textbook is this one formula "
                "applied to a particular tree. Learn the pattern, not the "
                "cases."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples: the pattern in action", (
                "EXAMPLE 1 (Case 1, with a full verification). Let "
                "$z = x^2 y$ with $x = \\cos t$, $y = \\sin t$. Find "
                "$\\frac{dz}{dt}$.\n\n"
                "Chain rule, one term per path:\n"
                "$$\\frac{dz}{dt} = \\underbrace{(2xy)}_{\\partial z/\\partial x} "
                "\\underbrace{(-\\sin t)}_{dx/dt} + "
                "\\underbrace{(x^2)}_{\\partial z/\\partial y} "
                "\\underbrace{(\\cos t)}_{dy/dt}.$$\n"
                "Substitute the path ($x = \\cos t$, $y = \\sin t$):\n"
                "$$\\frac{dz}{dt} = -2\\cos t \\sin^2 t + \\cos^3 t.$$\n"
                "VERIFY by substituting first: $z(t) = \\cos^2 t \\sin t$, so by "
                "one-variable rules $z'(t) = 2\\cos t(-\\sin t)\\sin t + "
                "\\cos^2 t \\cos t = -2\\cos t\\sin^2 t + \\cos^3 t$. Identical. "
                "When the composition is explicit you can ALWAYS check this way "
                "— do it while learning.\n\n"
                "EXAMPLE 2 (Case 1, exponential). $w = x e^y$ with $x = t^2$, "
                "$y = 3t$:\n"
                "$$\\frac{dw}{dt} = e^y(2t) + x e^y(3) = e^{3t}\\left(2t + "
                "3t^2\\right),$$\n"
                "where in the last step we substituted $x = t^2$, $y = 3t$ — "
                "the answer to a $\\frac{d}{dt}$ question must be a function of "
                "$t$ ALONE. Leaving $x$'s and $y$'s in a final Case-1 answer is "
                "the most common presentation error.\n\n"
                "EXAMPLE 3 (numeric evaluation). For Example 1, compute the "
                "rate at $t = \\frac{\\pi}{2}$: there $x = 0$, $y = 1$, so "
                "$\\frac{dz}{dt} = (2 \\cdot 0 \\cdot 1)(-1) + (0)(0) = 0$. "
                "Note the workflow: evaluate the PATH first ($x$, $y$, and the "
                "velocities at that $t$), then feed numbers into the chain "
                "rule. No algebra needed when only a number is asked for."
            )),
            # ---------------------------------------------------------- 6
            (L, "Main theorems: implicit differentiation, legalized", (
                "The chain rule's first great dividend is a FORMULA for "
                "implicit differentiation.\n\n"
                "CURVES. Suppose $F(x, y) = 0$ defines $y$ implicitly as a "
                "differentiable function of $x$ (which the Implicit Function "
                "Theorem guarantees near any point where $F_y \\ne 0$). "
                "Differentiate the identity $F(x, y(x)) = 0$ with respect to "
                "$x$ using the chain rule — tree: $F$ on top, $x$ and $y$ "
                "below, both reached from the single independent variable $x$ "
                "(the variable $x$ is its own path, with $\\frac{dx}{dx} = "
                "1$):\n"
                "$$F_x \\cdot 1 + F_y \\cdot \\frac{dy}{dx} = 0 "
                "\\;\\;\\Longrightarrow\\;\\; "
                "\\frac{dy}{dx} = -\\frac{F_x}{F_y}.$$\n"
                "Every implicit-differentiation slog from Calculus 1 is now two "
                "partial derivatives and a division. The MINUS SIGN is "
                "structural, not conventional — it comes from moving $F_x$ "
                "across the equals sign — and forgetting it is the classic "
                "error (page 12).\n\n"
                "SURFACES. If $F(x, y, z) = 0$ defines $z = z(x, y)$ near a "
                "point where $F_z \\ne 0$, the same computation with $y$ frozen "
                "gives\n"
                "$$\\frac{\\partial z}{\\partial x} = -\\frac{F_x}{F_z}, "
                "\\qquad \\frac{\\partial z}{\\partial y} = "
                "-\\frac{F_y}{F_z}.$$\n"
                "These are exactly the slopes whose plane is C305's "
                "level-surface tangent plane — the two chapters are telling one "
                "story from two sides.\n\n"
                "THE HYPOTHESIS MATTERS. Where $F_y = 0$ (curve case), the "
                "curve is running vertically and $\\frac{dy}{dx}$ honestly "
                "fails to exist; the formula's division by zero is the geometry "
                "showing through, not a defect. Checking the denominator "
                "before dividing is part of the method."
            )),
            # ---------------------------------------------------------- 7
            (E, "Worked examples: standard problems", (
                "EXAMPLE 1 (Case 2). $z = e^x \\sin y$, $x = st^2$, "
                "$y = s^2 t$. Find $\\frac{\\partial z}{\\partial s}$ and "
                "$\\frac{\\partial z}{\\partial t}$.\n\n"
                "Tree: $z \\to \\{x, y\\} \\to \\{s, t\\}$; two paths to each "
                "bottom variable.\n"
                "$$\\frac{\\partial z}{\\partial s} = (e^x \\sin y)(t^2) + "
                "(e^x \\cos y)(2st), \\qquad "
                "\\frac{\\partial z}{\\partial t} = (e^x \\sin y)(2st) + "
                "(e^x \\cos y)(s^2).$$\n"
                "Substituting the paths: $\\frac{\\partial z}{\\partial s} = "
                "e^{st^2}\\left(t^2 \\sin(s^2 t) + 2st\\cos(s^2 t)\\right)$, "
                "and similarly for $t$. Notice the SAME two partials of $f$ "
                "appear in both answers — compute them once, reuse.\n\n"
                "EXAMPLE 2 (implicit curve — the folium). Find "
                "$\\frac{dy}{dx}$ for $x^3 + y^3 = 6xy$.\n\n"
                "Set $F = x^3 + y^3 - 6xy$. Then $F_x = 3x^2 - 6y$ and "
                "$F_y = 3y^2 - 6x$, so\n"
                "$$\\frac{dy}{dx} = -\\frac{3x^2 - 6y}{3y^2 - 6x} = "
                "\\frac{2y - x^2}{y^2 - 2x}.$$\n"
                "Compare the Calculus-1 method — differentiate term by term, "
                "collect $y'$, factor, solve — a half page compressed to one "
                "line. At $(3, 3)$: $\\frac{dy}{dx} = \\frac{6 - 9}{9 - 6} = "
                "-1$, the well-known symmetric point of the folium.\n\n"
                "EXAMPLE 3 (implicit surface). Find "
                "$\\frac{\\partial z}{\\partial x}$ for "
                "$x^3 + y^3 + z^3 + 6xyz = 1$.\n\n"
                "$F_x = 3x^2 + 6yz$, $F_z = 3z^2 + 6xy$:\n"
                "$$\\frac{\\partial z}{\\partial x} = "
                "-\\frac{3x^2 + 6yz}{3z^2 + 6xy} = "
                "-\\frac{x^2 + 2yz}{z^2 + 2xy},$$\n"
                "valid wherever the denominator is nonzero. The symmetric "
                "$\\frac{\\partial z}{\\partial y} = -\\frac{y^2 + 2xz}"
                "{z^2 + 2xy}$ comes free by swapping letters — exploit symmetry "
                "whenever the equation has it."
            )),
            # ---------------------------------------------------------- 8
            (L, "Techniques and strategy: trees first, symbols second", (
                "STEP 0: DRAW THE TREE. Before touching a derivative, diagram "
                "who depends on whom. Top: the output. Middle: intermediate "
                "variables. Bottom: the truly independent variables. Count the "
                "paths from top to your target variable — that is your term "
                "count, fixed before any calculus happens. Most chain rule "
                "errors are TOPOLOGY errors (missing path, phantom path), not "
                "differentiation errors, and the tree catches them at the "
                "diagram stage.\n\n"
                "STEP 1: WRITE THE SKELETON. For each path, write the product "
                "of edge-derivatives symbolically — e.g. $\\frac{dz}{dt} = "
                "z_x x' + z_y y'$ — before computing anything. A skeleton with "
                "the right shape cannot silently lose a term midway.\n\n"
                "STEP 2: COMPUTE EDGES, THEN EVALUATE INWARD. Each edge "
                "derivative is a routine C304 computation. The partials of the "
                "outer function are functions of the INTERMEDIATE variables; if "
                "the question is 'at $t = t_0$,' evaluate the path first "
                "($x_0 = x(t_0)$ etc.), then the partials at $(x_0, y_0)$ — "
                "numbers all the way, no algebra.\n\n"
                "WHEN TO SKIP THE CHAIN RULE ENTIRELY. If the composition is "
                "explicit and simple, substituting first can be less work "
                "(Example 1, page 5, is a coin flip). The chain rule wins "
                "decisively when: the outer function is abstract (given only "
                "as $f$ with known partial values — then substitution is "
                "impossible); the composition is algebraically ugly; or you "
                "need the STRUCTURE of the answer (sensitivity per input), not "
                "just its value.\n\n"
                "NOTATION HYGIENE. Reserve $d$ for one-variable dependence and "
                "$\\partial$ for several. If the same letter appears at two "
                "levels of the tree (it happens — page 10), RENAME one "
                "instance before computing. Ten seconds of relabeling prevents "
                "the subtlest wrong answers in the chapter."
            )),
            # ---------------------------------------------------------- 9
            (E, "Worked examples: harder problems — second derivatives", (
                "Differentiating a chain rule answer requires the chain rule "
                "AGAIN, plus the product rule — the step where good students "
                "stumble. Let $z = f(x, y)$, $x = x(t)$, $y = y(t)$, all "
                "smooth.\n\n"
                "FIRST DERIVATIVE: $\\frac{dz}{dt} = f_x\\,x' + f_y\\,y'$.\n\n"
                "SECOND DERIVATIVE. Differentiate each product. The subtlety: "
                "$f_x$ means $f_x(x(t), y(t))$ — it is ITSELF a composite, so "
                "$\\frac{d}{dt} f_x = f_{xx}x' + f_{xy}y'$ by the same chain "
                "rule. Product rule plus that observation gives\n"
                "$$\\frac{d^2 z}{dt^2} = f_{xx}(x')^2 + 2f_{xy}\\,x'y' + "
                "f_{yy}(y')^2 + f_x\\,x'' + f_y\\,y''$$\n"
                "(using Clairaut $f_{xy} = f_{yx}$ from C304 to merge the two "
                "cross terms). Note the anatomy: a QUADRATIC FORM in the "
                "velocities (the same $f_{xx}, f_{xy}, f_{yy}$ pattern that "
                "becomes C308's Hessian) plus first-order terms carrying the "
                "path's acceleration.\n\n"
                "CONCRETE CHECK. $z = x^2 y$, $x = t^2$, $y = 2t$. Direct "
                "route: $z = t^4 \\cdot 2t = 2t^5$, so $z'' = 40t^3$. Formula "
                "route: $f_x = 2xy = 4t^3$, $f_y = x^2 = t^4$, $f_{xx} = 2y = "
                "4t$, $f_{xy} = 2x = 2t^2$, $f_{yy} = 0$; $x' = 2t$, "
                "$x'' = 2$, $y' = 2$, $y'' = 0$. Assemble:\n"
                "$$4t(2t)^2 + 2(2t^2)(2t)(2) + 0 + (4t^3)(2) + 0 = "
                "16t^3 + 16t^3 + 8t^3 = 40t^3. \\checkmark$$\n\n"
                "MORAL. Two independent instincts must fire: product rule on "
                "each term, chain rule on each partial. Students who write "
                "$\\frac{d}{dt}f_x = f_{xx}x'$ (dropping $f_{xy}y'$) lose the "
                "cross term — and the cross term is usually the interesting "
                "physics."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: double roles and broken hypotheses", (
                "DOUBLE-ROLE VARIABLES. Let $w = f(x, y)$ where $y = g(x)$: "
                "the variable $x$ is both a direct input and the driver of "
                "$y$. The tree has $x$ at the bottom reachable two ways, and "
                "the rule gives\n"
                "$$\\frac{dw}{dx} = \\frac{\\partial f}{\\partial x} + "
                "\\frac{\\partial f}{\\partial y}\\,g'(x).$$\n"
                "The left side is the TOTAL derivative (everything moves); "
                "$\\frac{\\partial f}{\\partial x}$ is the PARTIAL (only the "
                "first slot moves). They are different numbers with "
                "near-identical notation — when both appear, say aloud which "
                "variables are held fixed. Thermodynamics writes the fix "
                "explicitly, $\\left(\\frac{\\partial U}{\\partial T}\\right)_V$, "
                "precisely because its variables are all entangled and bare "
                "$\\partial$'s would be ambiguous.\n\n"
                "THE CYCLIC RULE (why $\\partial$'s don't cancel). If "
                "$F(x, y, z) = 0$ ties three variables together, applying "
                "$-F_x/F_y$-type formulas around the cycle gives\n"
                "$$\\frac{\\partial x}{\\partial y}\\, "
                "\\frac{\\partial y}{\\partial z}\\, "
                "\\frac{\\partial z}{\\partial x} = -1,$$\n"
                "not $+1$ as fraction-cancelling intuition insists. Partials "
                "are not fractions; each factor holds a DIFFERENT variable "
                "fixed. Page 11 verifies the $-1$ on the ideal gas law.\n\n"
                "BROKEN HYPOTHESES. The chain rule assumed $f$ "
                "differentiable — not merely 'has partials.' C305's "
                "counterexample $f = \\frac{xy}{x^2 + y^2}$, $f(0,0) = 0$ "
                "shows why: ride it along $x = t$, $y = t$. The formula "
                "predicts $\\frac{dz}{dt} = f_x(0,0)\\cdot 1 + "
                "f_y(0,0)\\cdot 1 = 0$ at $t = 0$. The truth: $z(t) = "
                "\\frac12$ for $t \\ne 0$ but $z(0) = 0$ — $z$ is "
                "discontinuous at 0 and has NO derivative there. Partials "
                "existed, formula computed, answer meaningless. "
                "Differentiability is the license plate the chain rule checks."
            )),
            # ---------------------------------------------------------- 11
            (E, "Worked examples: edge cases", (
                "EXAMPLE 1 (double role, verified). $u = x^2 + y^2$ with "
                "$y = \\sin x$. The total derivative:\n"
                "$$\\frac{du}{dx} = \\frac{\\partial u}{\\partial x} + "
                "\\frac{\\partial u}{\\partial y}\\frac{dy}{dx} = 2x + "
                "2y\\cos x = 2x + 2\\sin x\\cos x.$$\n"
                "Check by substituting first: $u = x^2 + \\sin^2 x$, "
                "$u' = 2x + 2\\sin x \\cos x$. ✓ Note the two different "
                "'derivatives with respect to $x$' in one line: the partial "
                "($2x$) and the total (the whole right side). On an exam, "
                "quoting the partial when the total is asked is a full-credit "
                "error — the question 'how does $u$ respond if $x$ changes?' "
                "includes the induced change in $y$.\n\n"
                "EXAMPLE 2 (cyclic rule on the ideal gas). $PV = nRT$ links "
                "$P, V, T$. Compute the three partials, each from the "
                "explicit solved form:\n"
                "$$\\left(\\frac{\\partial P}{\\partial V}\\right)_T = "
                "-\\frac{nRT}{V^2}, \\qquad "
                "\\left(\\frac{\\partial V}{\\partial T}\\right)_P = "
                "\\frac{nR}{P}, \\qquad "
                "\\left(\\frac{\\partial T}{\\partial P}\\right)_V = "
                "\\frac{V}{nR}.$$\n"
                "Multiply: $-\\frac{nRT}{V^2} \\cdot \\frac{nR}{P} \\cdot "
                "\\frac{V}{nR} = -\\frac{nRT}{PV} = -1$ (using $PV = nRT$). "
                "The advertised $-1$, live. Anyone who 'cancelled the "
                "$\\partial$'s' would have predicted $+1$ and gotten real "
                "thermodynamics wrong by a sign.\n\n"
                "EXAMPLE 3 (abstract outer function). Given only: $f$ is "
                "differentiable, $f_x(2, 4) = 3$, $f_y(2, 4) = -1$; and "
                "$x = t^2$... wait — at $t = ?$. Let $x = 2t$, $y = t^3 + 3t$ "
                "so that $t = 1$ lands at $(2, 4)$; $x'(1) = 2$, "
                "$y'(1) = 3(1)^2 + 3 = 6$. Then\n"
                "$$\\left.\\frac{dz}{dt}\\right|_{t=1} = 3(2) + (-1)(6) = 0.$$\n"
                "No formula for $f$ was ever known — THIS is the situation "
                "where the chain rule is not merely convenient but the only "
                "possible method."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'One product is enough.' Students transfer "
                "the one-variable reflex and write $\\frac{dz}{dt} = "
                "\\frac{\\partial z}{\\partial x}\\frac{dx}{dt}$, full stop — "
                "amputating every path but the first. The term count equals "
                "the PATH count, decided by the tree, not by habit. Two "
                "intermediates, two terms; three, three.\n\n"
                "MISCONCEPTION 2: '$\\partial$'s cancel like fractions.' In "
                "$\\frac{\\partial z}{\\partial x}\\frac{\\partial x}"
                "{\\partial s}$ the $\\partial x$'s appear to cancel, and for "
                "single-path trees the mnemonic accidentally works. It is "
                "still false — each partial holds different variables fixed — "
                "and the cyclic rule's $-1$ (pages 10-11) is the proof that "
                "fraction-thinking eventually produces WRONG SIGNS, not just "
                "sloppy reasoning.\n\n"
                "MISCONCEPTION 3: '$\\frac{dz}{dt}$ and "
                "$\\frac{\\partial z}{\\partial t}$ are interchangeable.' When "
                "$z = f(x, y, t)$ with $x, y$ also depending on $t$, the "
                "correct statement is $\\frac{dz}{dt} = f_x x' + f_y y' + "
                "\\frac{\\partial f}{\\partial t}$ — the total derivative "
                "CONTAINS the partial as one of its terms. Writing one for "
                "the other silently deletes (or double-counts) the direct "
                "dependence. Fluid dynamics names the difference: material "
                "versus local derivative (page 14).\n\n"
                "MISCONCEPTION 4: 'The implicit formula is $+F_x/F_y$.' The "
                "minus sign is forced by algebra: $F_x + F_y y' = 0$ gives "
                "$y' = -F_x/F_y$. Sanity check on a known curve: circle "
                "$x^2 + y^2 = 25$ at $(3, 4)$ must slope $-\\frac34$; "
                "$-F_x/F_y = -\\frac{2x}{2y} = -\\frac34$. ✓ The plus-sign "
                "version fails this instantly.\n\n"
                "MISCONCEPTION 5: 'The chain rule needs a formula for $f$.' "
                "Exactly backwards — its finest hour is ABSTRACT $f$ with "
                "known partial values (page 11, Example 3). If you find "
                "yourself refusing such problems for 'insufficient "
                "information,' the information was sufficient; the pattern "
                "was missing."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational errors", (
                "ERROR 1: Evaluating outer partials at the WRONG POINT. "
                "$f_x$ must be evaluated at the intermediate point "
                "$(x(t_0), y(t_0))$, never at $t_0$ itself. Concretely: "
                "$z = x^2y$, $x = \\cos t$, $y = \\sin t$, $t = \\pi$: "
                "evaluate $f_x = 2xy$ at $(x, y) = (-1, 0)$ — giving $0$ — "
                "not anything involving $\\pi$ directly. Write the "
                "intermediate values down FIRST.\n\n"
                "ERROR 2: Mixing evaluation stages. Half-substituted "
                "monsters like $e^x \\sin(s^2 t) \\cdot t^2$ with $x$ still "
                "loose — decide: symbolic answer (substitute the paths at "
                "the very end, everywhere) or numeric answer (numbers from "
                "the start). Never both in one expression.\n\n"
                "ERROR 3: In second derivatives, differentiating $f_x$ as if "
                "it were a function of $t$ alone: $\\frac{d}{dt}f_x = "
                "f_{xx}x'$ misses $f_{xy}y'$. The partials of $f$ are "
                "composites too; each gets its own full chain rule. (Page "
                "9's cross-term anatomy.)\n\n"
                "ERROR 4: Case-2 subscript slips — computing "
                "$\\frac{\\partial z}{\\partial s}$ but grabbing "
                "$\\frac{\\partial x}{\\partial t}$ mid-formula. Every term "
                "of a $\\partial/\\partial s$ answer must end its path at "
                "$s$. Check each term's last factor before moving on.\n\n"
                "ERROR 5: Implicit differentiation with $F$ not set to zero "
                "form. For $x^3 + y^3 = 6xy$, take $F = x^3 + y^3 - 6xy$ "
                "(everything on one side) BEFORE computing $F_x, F_y$; "
                "differentiating both sides separately and then applying "
                "$-F_x/F_y$ on top double-dips the right-hand side.\n\n"
                "ERROR 6: Related-rates unit mismatches — a rate in cm/min "
                "multiplied by a rate in m/s. Convert everything to one "
                "unit system before the chain rule adds the paths; the "
                "formula adds NUMBERS, and it trusts you that they are "
                "commensurable."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: everything that moves through everything else", (
                "1. RELATED RATES, PROPERLY GENERAL. Calculus 1 related rates "
                "had one chain; now entire systems move at once. Gas in a "
                "cylinder: $P$, $V$, $T$ all drift, tied by $PV = nRT$; "
                "differentiate the constraint along time and any one rate "
                "falls out of the others (page 15 runs the numbers). Same "
                "template: melting snowballs with changing density, "
                "engineering tolerances drifting with temperature, economics "
                "with linked markets.\n\n"
                "2. SENSITIVITY CHAINS IN ECONOMICS. Output $Q(K, L)$ depends "
                "on capital and labor; both respond to the interest rate $r$. "
                "Then $\\frac{dQ}{dr} = Q_K K'(r) + Q_L L'(r)$ decomposes a "
                "policy question into measurable pieces: marginal products "
                "(the partials) times investment responses (the path "
                "derivatives). The chain rule is the accounting standard for "
                "'indirect effects.'\n\n"
                "3. THE MATERIAL DERIVATIVE. A sensor drifting with a fluid "
                "at velocity $\\mathbf{v}$ through a temperature field "
                "$T(x, y, z, t)$ records the rate\n"
                "$$\\frac{DT}{Dt} = \\frac{\\partial T}{\\partial t} + "
                "T_x v_1 + T_y v_2 + T_z v_3,$$\n"
                "the local change plus the change from being carried to "
                "warmer or cooler water — a pure Case-1 chain rule with $t$ "
                "playing a double role (page 10). Written with C307's "
                "gradient it becomes $\\frac{\\partial T}{\\partial t} + "
                "\\mathbf{v} \\cdot \\nabla T$, the opening move of every "
                "fluid dynamics course.\n\n"
                "4. CURVES ON SURFACES. Riding $f$ along a path "
                "$\\mathbf{r}(t)$ gives $\\frac{d}{dt} f(\\mathbf{r}(t)) = "
                "f_x x' + f_y y'$ — the germ of the directional derivative "
                "(C307), the fundamental theorem for line integrals (C314), "
                "and the proof of Lagrange multipliers (C309). One formula, "
                "four chapters of consequences."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: a breathing gas sample", (
                "One mole of an ideal gas obeys $PV = RT$ with "
                "$R = 8.31$ (J/mol·K, pressure in kPa, volume in liters). At "
                "this instant: temperature $T = 300$ K and RISING at "
                "$0.1$ K/s; volume $V = 100$ L and EXPANDING at $0.2$ L/s. "
                "Question: is the pressure rising or falling, and how fast?\n\n"
                "STEP 1 — express the target: $P = \\frac{RT}{V} = "
                "8.31\\,\\frac{T}{V}$, so $P$ is a function of the two moving "
                "quantities $T(t)$ and $V(t)$. Tree: $P \\to \\{T, V\\} \\to "
                "t$; two paths, two terms.\n\n"
                "STEP 2 — chain rule skeleton:\n"
                "$$\\frac{dP}{dt} = \\frac{\\partial P}{\\partial T}\\, "
                "\\frac{dT}{dt} + \\frac{\\partial P}{\\partial V}\\, "
                "\\frac{dV}{dt} = \\frac{8.31}{V}\\,\\frac{dT}{dt} - "
                "\\frac{8.31\\,T}{V^2}\\,\\frac{dV}{dt}.$$\n\n"
                "STEP 3 — evaluate at the instant ($T = 300$, $V = 100$, "
                "$T' = 0.1$, $V' = 0.2$):\n"
                "$$\\frac{dP}{dt} = \\frac{8.31}{100}(0.1) - "
                "\\frac{8.31 \\cdot 300}{100^2}(0.2) = 0.00831 - 0.04986 "
                "\\approx -0.0416 \\text{ kPa/s}.$$\n\n"
                "STEP 4 — interpret. The warming pushes pressure UP "
                "(first term, $+0.008$), the expansion pulls it DOWN (second "
                "term, $-0.050$); the expansion wins and pressure falls at "
                "about $0.042$ kPa/s from its current $P = 8.31 \\cdot "
                "300/100 \\approx 24.9$ kPa. The chain rule did not just "
                "produce a number — it produced a LEDGER, one line per "
                "cause, with signs. That decomposition, unavailable to any "
                "'plug in and differentiate numerically' shortcut, is why "
                "scientists write rates this way."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: where this chapter plugs into everything else", (
                "BACKWARD. C304 built the partials that label every tree "
                "edge. C305 supplied both the hypothesis (differentiability) "
                "and the proof: the chain rule is the differential "
                "$dz = f_x dx + f_y dy$ read along a motion. C303's "
                "parametrized curves are the paths everything rides on; the "
                "one-variable chain rule (C105-C110) is the one-path special "
                "case.\n\n"
                "FORWARD, immediately:\n"
                "• C307 (gradient): the directional derivative is THIS "
                "chapter's Case 1 along the straight path $\\mathbf{r}(t) = "
                "(a + tu_1,\\, b + tu_2)$ — the formula $D_{\\mathbf{u}}f = "
                "\\nabla f \\cdot \\mathbf{u}$ is a two-line corollary, and "
                "C307 proves it exactly that way.\n"
                "• C308 (critical points): the second-derivative anatomy of "
                "page 9 — the quadratic form $f_{xx}(x')^2 + 2f_{xy}x'y' + "
                "f_{yy}(y')^2$ — reappears as the Hessian test's engine.\n"
                "• C309 (Lagrange): differentiate $f(\\mathbf{r}(t))$ along "
                "a curve INSIDE the constraint set, set it to zero at the "
                "optimum, and Lagrange's $\\nabla f = \\lambda \\nabla g$ "
                "falls out. The proof is one application of page 4.\n\n"
                "FURTHER OUT. C310-C312's change of variables in multiple "
                "integrals is governed by the Jacobian — the matrix of "
                "exactly these chain rule coefficients. C313-C314: the "
                "fundamental theorem for line integrals integrates "
                "$\\frac{d}{dt}f(\\mathbf{r}(t)) = \\nabla f \\cdot "
                "\\mathbf{r}'(t)$ — this chapter's formula — from $a$ to "
                "$b$. ODE.U7.N2 linearizes vector fields with the same "
                "Jacobian; LA.U7.N1's eigenvalues then judge stability. And "
                "in machine learning, backpropagation is the general rule of "
                "page 4 industrialized — page 17 tells that story."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: Jacobians, and the trillion-dollar chain rule", (
                "MATRIX FORM. Collect the partials of a map "
                "$\\mathbf{f}: \\mathbb{R}^n \\to \\mathbb{R}^m$ into its "
                "$m \\times n$ JACOBIAN matrix $D\\mathbf{f}$ (row $i$: the "
                "partials of output $i$). Then every case of the chain rule "
                "is ONE statement:\n"
                "$$D(\\mathbf{g} \\circ \\mathbf{f})(\\mathbf{a}) = "
                "D\\mathbf{g}(\\mathbf{f}(\\mathbf{a}))\\; \\cdot\\; "
                "D\\mathbf{f}(\\mathbf{a})$$\n"
                "— compose functions, MULTIPLY matrices. Case 1 is a "
                "$(1 \\times 2)(2 \\times 1)$ product; Case 2 is "
                "$(1 \\times 2)(2 \\times 2)$; the 'sum over paths' of page 4 "
                "is literally the row-times-column sum inside a matrix "
                "product. Linear algebra (LA.U7.N1's world) and calculus "
                "click together: the derivative is a linear map (C305 page "
                "17), and composition of maps is multiplication of "
                "matrices.\n\n"
                "BACKPROPAGATION. A neural network is a long composition "
                "$\\mathbf{f}_k \\circ \\cdots \\circ \\mathbf{f}_2 \\circ "
                "\\mathbf{f}_1$, and training it requires the derivative of "
                "one scalar loss with respect to millions of parameters. The "
                "chain rule gives the answer as a product of Jacobians; the "
                "engineering insight is ASSOCIATIVITY. Multiplying from the "
                "input end (forward mode) costs a matrix-matrix product per "
                "layer; from the output end (reverse mode) the running "
                "product stays a single ROW VECTOR — matrix-vector all the "
                "way, cheaper by a factor of the network's width. Reverse "
                "mode is backpropagation. Every large model trained today "
                "runs this chapter's theorem, ordered cleverly, billions of "
                "times per second.\n\n"
                "HISTORY. The $\\partial$ notation and the functional "
                "dependence bookkeeping were hammered out by Euler and the "
                "thermodynamicists, who needed page 10's subscript "
                "discipline before it had a name. The tree diagram is the "
                "modern classroom's rendering of what Jacobi's matrices said "
                "all along."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "THE PATTERN. Multiply edge-derivatives along each path from "
                "output to target variable; add over paths:\n"
                "$$\\frac{\\partial(\\text{top})}{\\partial(\\text{bottom})} "
                "= \\sum_{\\text{paths}} \\prod_{\\text{edges}} "
                "(\\text{edge derivative}).$$\n\n"
                "CASE 1 ($z = f(x, y)$; $x, y$ functions of $t$):\n"
                "$$\\frac{dz}{dt} = f_x\\big(x(t), y(t)\\big)\\,x'(t) + "
                "f_y\\big(x(t), y(t)\\big)\\,y'(t).$$\n\n"
                "CASE 2 ($x, y$ functions of $s, t$): same shape once per "
                "independent variable; partials on the bottom edges.\n\n"
                "HYPOTHESIS: $f$ DIFFERENTIABLE (C305) — partials alone can "
                "make the formula lie (the $\\frac{xy}{x^2+y^2}$ path "
                "example).\n\n"
                "IMPLICIT DIFFERENTIATION. $F(x, y) = 0$: $\\frac{dy}{dx} = "
                "-\\frac{F_x}{F_y}$ (where $F_y \\ne 0$). Surface "
                "$F(x, y, z) = 0$: $\\frac{\\partial z}{\\partial x} = "
                "-\\frac{F_x}{F_z}$, $\\frac{\\partial z}{\\partial y} = "
                "-\\frac{F_y}{F_z}$. Mind the MINUS.\n\n"
                "DOUBLE ROLES. $w = f(x, g(x))$: $\\frac{dw}{dx} = f_x + "
                "f_y\\,g'$ — total versus partial. With direct time "
                "dependence: $\\frac{dz}{dt} = \\frac{\\partial f}"
                "{\\partial t} + f_x x' + f_y y'$.\n\n"
                "CYCLIC RULE: $\\frac{\\partial x}{\\partial y} "
                "\\frac{\\partial y}{\\partial z} "
                "\\frac{\\partial z}{\\partial x} = -1$. Partials are not "
                "fractions.\n\n"
                "SECOND DERIVATIVE ALONG A PATH:\n"
                "$$\\frac{d^2z}{dt^2} = f_{xx}(x')^2 + 2f_{xy}x'y' + "
                "f_{yy}(y')^2 + f_x x'' + f_y y''.$$\n\n"
                "WORKFLOW. (1) Draw the tree; count paths. (2) Write the "
                "skeleton. (3) Compute edges. (4) Evaluate partials AT THE "
                "INTERMEDIATE POINT. (5) Substitute/evaluate consistently. "
                "(6) Check against direct substitution when possible."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all eight before reading the solutions.\n\n"
                "1. $z = x^2 + y^2$, $x = 3t$, $y = t^2$: find "
                "$\\frac{dz}{dt}$ two ways.\n"
                "2. $w = x/y$, $x = st$, $y = s - t$: find "
                "$\\frac{\\partial w}{\\partial s}$.\n"
                "3. $x^2 + xy + y^3 = 11$: find $\\frac{dy}{dx}$ at "
                "$(1, 2)$.\n"
                "4. $xz^2 + y^2 z = 3$: find "
                "$\\frac{\\partial z}{\\partial y}$.\n"
                "5. $w = f(x, y, z)$ with $x, y, z$ each functions of "
                "$(u, v)$: how many terms in "
                "$\\frac{\\partial w}{\\partial u}$?\n"
                "6. $f$ differentiable, $f_x(0, 1) = 5$, $f_y(0, 1) = -2$; "
                "$x = \\sin t$, $y = \\cos t$: find "
                "$\\frac{dz}{dt}$ at $t = 0$.\n"
                "7. $z = f(x, y)$, $x = 2t$, $y = 3t$, with constant second "
                "partials $f_{xx} = 1$, $f_{xy} = 2$, $f_{yy} = -1$: find "
                "$\\frac{d^2z}{dt^2}$.\n"
                "8. True or false: if $f_x$ and $f_y$ exist everywhere, then "
                "$\\frac{d}{dt}f(x(t), y(t)) = f_x x' + f_y y'$ always "
                "holds.\n\n"
                "SOLUTIONS.\n"
                "1. Chain: $2x(3) + 2y(2t) = 18t + 4t^3$. Direct: "
                "$z = 9t^2 + t^4$, $z' = 18t + 4t^3$. ✓\n"
                "2. $w_x = \\frac{1}{y}$, $w_y = -\\frac{x}{y^2}$; "
                "$x_s = t$, $y_s = 1$: $\\frac{\\partial w}{\\partial s} = "
                "\\frac{t}{s - t} - \\frac{st}{(s - t)^2}$.\n"
                "3. $F = x^2 + xy + y^3 - 11$: $F_x = 2x + y = 4$, "
                "$F_y = x + 3y^2 = 13$: $\\frac{dy}{dx} = -\\frac{4}{13}$.\n"
                "4. $F = xz^2 + y^2z - 3$: $F_y = 2yz$, $F_z = 2xz + y^2$: "
                "$\\frac{\\partial z}{\\partial y} = "
                "-\\frac{2yz}{2xz + y^2}$.\n"
                "5. Three — one path through each of $x$, $y$, $z$.\n"
                "6. At $t = 0$: $(x, y) = (0, 1)$ ✓ (matches the given "
                "point); $x' = \\cos 0 = 1$, $y' = -\\sin 0 = 0$: "
                "$\\frac{dz}{dt} = 5(1) + (-2)(0) = 5$.\n"
                "7. $x' = 2$, $y' = 3$, $x'' = y'' = 0$: "
                "$1(4) + 2(2)(2)(3) + (-1)(9) = 4 + 24 - 9 = 19$.\n"
                "8. FALSE — differentiability is required; "
                "$f = \\frac{xy}{x^2+y^2}$ along $x = y = t$ breaks it "
                "(page 10).\n\n"
                "Scoring: misses on 3-4 → pages 6-7; on 7 → page 9; on 8 → "
                "page 10."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Before leaving this chapter you should be able to check "
                "every box honestly:\n\n"
                "□ Draw the dependence tree for any composite setup and "
                "read off the number of terms BEFORE differentiating.\n"
                "□ State and apply Case 1 ($z = f(x(t), y(t))$), evaluating "
                "the partials at the intermediate point without being "
                "reminded.\n"
                "□ State and apply Case 2, keeping the $s$-paths and "
                "$t$-paths separate.\n"
                "□ Explain, via C305's differential, WHY rates multiply "
                "along a path and add across paths — and which hypothesis "
                "(differentiability) makes the argument legal.\n"
                "□ Derive and use $\\frac{dy}{dx} = -\\frac{F_x}{F_y}$ and "
                "$\\frac{\\partial z}{\\partial x} = -\\frac{F_x}{F_z}$, "
                "minus sign and non-vanishing denominator included.\n"
                "□ Handle double-role variables: distinguish total from "
                "partial derivatives and write "
                "$\\frac{dw}{dx} = f_x + f_y g'$ without conflating the two "
                "sides.\n"
                "□ Verify the cyclic rule's $-1$ on a concrete constraint "
                "and explain why it kills the fraction-cancelling "
                "mnemonic.\n"
                "□ Push a chain rule to second order: product rule on each "
                "term, chain rule on each partial, cross term intact.\n"
                "□ Solve a multivariable related-rates problem end to end, "
                "with a signed ledger of competing effects (page 15's "
                "pattern).\n"
                "□ Work problems where $f$ is abstract — partial values "
                "given, no formula.\n\n"
                "When every box is checked, proceed to C307: aim the "
                "derivative in an arbitrary direction, and watch this "
                "chapter's Case 1 turn into the gradient."
            )),
        ],
    },
    "C307": {
        "summary": (
            "A complete course on the gradient and directional derivatives: the "
            "definition of D_u f, the dot-product formula and its chain rule "
            "proof, unit-vector discipline, the gradient as direction of steepest "
            "ascent, perpendicularity to level curves and surfaces, maximal rates "
            "of change, and a first look at gradient fields."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "C304 measured a surface's slope in exactly two directions — "
                "east ($f_x$) and north ($f_y$). But a hiker on a hillside can "
                "walk in ANY direction, and each bearing has its own slope. This "
                "chapter answers the full question: given a function $f$ and ANY "
                "direction $\\mathbf{u}$, what is the rate of change of $f$ in "
                "that direction? The answer is the DIRECTIONAL DERIVATIVE "
                "$D_{\\mathbf{u}}f$, and the astonishing fact at the chapter's "
                "heart is that the two numbers you already have are enough: for "
                "differentiable $f$, every one of the infinitely many directional "
                "slopes is a simple dot product,\n"
                "$$D_{\\mathbf{u}}f = \\nabla f \\cdot \\mathbf{u},$$\n"
                "where $\\nabla f = \\langle f_x, f_y \\rangle$ is the GRADIENT — "
                "the partials, bundled into a vector and promoted to the single "
                "most important object in multivariable calculus.\n\n"
                "The gradient is not just a container. It POINTS somewhere: in "
                "the direction of steepest ascent, with length equal to that "
                "steepest slope. It stands perpendicular to level curves and "
                "level surfaces — which is why it kept sneaking into C305 as a "
                "normal vector. Set it to zero and you have C308's critical "
                "points; match it to a constraint's gradient and you have C309's "
                "Lagrange multipliers; flow along it and you are doing gradient "
                "descent, the algorithm that trains modern machine learning.\n\n"
                "In this chapter you will learn: the limit definition of "
                "$D_{\\mathbf{u}}f$, the dot-product formula and its two-line "
                "proof via C306's chain rule, the non-negotiable unit-vector "
                "discipline, the steepest-ascent theorem with proof, "
                "perpendicularity to level sets, and a preview of gradient "
                "fields (C313). Two partials in, a compass out."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Four ingredients, three of them fresh.\n\n"
                "1. PARTIAL DERIVATIVES (C304) — the gradient's components.\n\n"
                "2. THE DOT PRODUCT (C301, LA.U8.N1). You need both formulas: "
                "componentwise $\\mathbf{a} \\cdot \\mathbf{b} = a_1b_1 + "
                "a_2b_2$, and geometric $\\mathbf{a} \\cdot \\mathbf{b} = "
                "|\\mathbf{a}||\\mathbf{b}|\\cos\\theta$. The whole steepest-"
                "ascent story is the second formula wearing calculus "
                "clothes.\n\n"
                "3. UNIT VECTORS. To make $\\mathbf{v}$ unit length: "
                "$\\mathbf{u} = \\mathbf{v}/|\\mathbf{v}|$. A direction at "
                "angle $\\theta$ from the positive $x$-axis: $\\mathbf{u} = "
                "\\langle \\cos\\theta, \\sin\\theta \\rangle$.\n\n"
                "4. THE CHAIN RULE, CASE 1 (C306): $\\frac{d}{dt} f(x(t), "
                "y(t)) = f_x x' + f_y y'$ — the proof engine for this "
                "chapter's main formula, and differentiability (C305) rides "
                "along as its hypothesis.\n\n"
                "Quick self-check (answers follow):\n\n"
                "• Unit vector in the direction of $\\mathbf{v} = \\langle 3, "
                "-4 \\rangle$? ($|\\mathbf{v}| = 5$, so $\\langle \\frac35, "
                "-\\frac45 \\rangle$.)\n"
                "• $\\langle 2, -1 \\rangle \\cdot \\langle 3, 4 \\rangle$? "
                "($6 - 4 = 2$.)\n"
                "• For fixed $|\\mathbf{a}|$ and $|\\mathbf{b}|$, when is "
                "$\\mathbf{a} \\cdot \\mathbf{b}$ largest? When zero? (Largest "
                "when parallel, $\\theta = 0$; zero when perpendicular.)\n"
                "• $\\nabla f$ for $f = x^2y$ at $(1, 3)$? ($\\langle 2xy, "
                "x^2 \\rangle = \\langle 6, 1 \\rangle$.)\n\n"
                "The third question IS the steepest-ascent theorem in "
                "embryonic form; if it felt obvious, page 6 will feel "
                "inevitable. Proceed."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively: every bearing has a slope", (
                "Stand on a hillside. Face east: the ground climbs at some "
                "rate. Face north: a different rate. Face northeast, "
                "south-southwest, any bearing whatever: each has its own slope, "
                "a full compass dial of numbers attached to your location. The "
                "directional derivative is that dial, read one bearing at a "
                "time: pick a horizontal unit direction $\\mathbf{u}$, walk an "
                "infinitesimal step along it, and record the rise per unit of "
                "horizontal run.\n\n"
                "Now the surprise. You might expect the dial to be an arbitrary "
                "assignment — infinitely many independent numbers. It is not. "
                "For a differentiable function (C305's locally-flat kind), the "
                "surface near your feet is indistinguishable from a tilted "
                "PLANE, and a plane's slopes are completely determined by two "
                "of them. Knowing the east slope and the north slope, you can "
                "predict every other bearing's slope exactly — by the dot "
                "product formula this chapter proves. Infinitely many answers, "
                "two numbers of data. The gradient $\\nabla f = \\langle f_x, "
                "f_y \\rangle$ is those two numbers packaged as a vector, and "
                "the packaging is not clerical: the VECTOR has direction and "
                "length that mean something on the hillside.\n\n"
                "Picture the dial's readings as $\\mathbf{u}$ sweeps a full "
                "circle. Somewhere the slope is maximal (the fall line, "
                "pointing straight uphill); diametrically opposite it is most "
                "negative (straight downhill); a quarter turn away, in two "
                "places, it reads exactly zero — walking along the CONTOUR, "
                "neither climbing nor descending. Maximum, minimum, and zeros, "
                "all locked in a rigid $\\cos\\theta$ pattern around the "
                "circle: that is the fingerprint of a dot product, and pages "
                "4-6 make every clause exact. One vector — the gradient — "
                "encodes the entire dial."
            )),
            # ---------------------------------------------------------- 4
            (L, "The formal definitions, and the formula with its proof", (
                "THE DIRECTIONAL DERIVATIVE. Let $\\mathbf{u} = \\langle u_1, "
                "u_2 \\rangle$ be a UNIT vector. The directional derivative of "
                "$f$ at $(a, b)$ in the direction $\\mathbf{u}$ is\n"
                "$$D_{\\mathbf{u}} f(a, b) = \\lim_{h \\to 0} "
                "\\frac{f(a + hu_1,\\; b + hu_2) - f(a, b)}{h},$$\n"
                "when the limit exists: the one-variable derivative of $f$ "
                "restricted to the line through $(a,b)$ along $\\mathbf{u}$. "
                "Sanity checks: $\\mathbf{u} = \\langle 1, 0 \\rangle$ gives "
                "$f_x$; $\\mathbf{u} = \\langle 0, 1 \\rangle$ gives $f_y$ — "
                "the partials are directional derivatives along the axes.\n\n"
                "THE GRADIENT. $\\nabla f = \\langle f_x, f_y \\rangle$ "
                "(three components in 3D: $\\langle f_x, f_y, f_z \\rangle$). "
                "Read 'del $f$'; it is a VECTOR-valued function of position — "
                "at each point of the domain, one arrow.\n\n"
                "THE THEOREM. If $f$ is differentiable at $(a, b)$, then for "
                "every unit $\\mathbf{u}$,\n"
                "$$D_{\\mathbf{u}} f(a, b) = \\nabla f(a, b) \\cdot "
                "\\mathbf{u} = f_x(a, b)\\,u_1 + f_y(a, b)\\,u_2.$$\n\n"
                "PROOF (two lines, via C306). Parametrize the walk: "
                "$\\mathbf{r}(t) = (a + tu_1,\\, b + tu_2)$, and set "
                "$g(t) = f(\\mathbf{r}(t))$. The limit above is exactly "
                "$g'(0)$. By the chain rule (Case 1, legal because $f$ is "
                "differentiable):\n"
                "$$g'(0) = f_x(a, b)\\,\\frac{dx}{dt} + f_y(a, b)\\, "
                "\\frac{dy}{dt} = f_x(a,b)\\,u_1 + f_y(a,b)\\,u_2. "
                "\\;\\blacksquare$$\n\n"
                "Note both hypotheses earning their keep: DIFFERENTIABLE "
                "(without it the formula can fail — page 10), and UNIT vector "
                "(the definition measures rise per unit of ground; a non-unit "
                "$\\mathbf{v}$ silently rescales the answer by $|\\mathbf{v}|$ "
                "— page 5 enforces the discipline)."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples: the unit-vector drill", (
                "EXAMPLE 1 (direction given as a vector). Find "
                "$D_{\\mathbf{u}}f$ for $f(x, y) = x^2y + y^3$ at $(1, 2)$ in "
                "the direction of $\\mathbf{v} = \\langle 3, 4 \\rangle$.\n\n"
                "Step 1 — gradient: $\\nabla f = \\langle 2xy,\\; x^2 + 3y^2 "
                "\\rangle$, so $\\nabla f(1, 2) = \\langle 4, 13 \\rangle$.\n"
                "Step 2 — NORMALIZE: $|\\mathbf{v}| = \\sqrt{9 + 16} = 5$, "
                "$\\mathbf{u} = \\langle \\frac35, \\frac45 \\rangle$.\n"
                "Step 3 — dot:\n"
                "$$D_{\\mathbf{u}}f(1,2) = 4 \\cdot \\tfrac35 + 13 \\cdot "
                "\\tfrac45 = 2.4 + 10.4 = 12.8.$$\n"
                "Interpretation: stepping from $(1,2)$ toward $(4, 6)$, $f$ "
                "initially rises 12.8 units per unit of ground. Had we dotted "
                "with $\\mathbf{v}$ itself we would have gotten $64$ — five "
                "times too big, because $\\mathbf{v}$ is five times too "
                "long.\n\n"
                "EXAMPLE 2 (direction given as an angle). Same $f$, same "
                "point, direction $\\theta = \\frac{2\\pi}{3}$ from the "
                "positive $x$-axis: $\\mathbf{u} = \\langle \\cos\\frac{2\\pi}"
                "{3}, \\sin\\frac{2\\pi}{3} \\rangle = \\langle -\\frac12, "
                "\\frac{\\sqrt3}{2} \\rangle$ — already unit length, no "
                "normalization needed.\n"
                "$$D_{\\mathbf{u}}f = 4\\left(-\\tfrac12\\right) + "
                "13\\left(\\tfrac{\\sqrt3}{2}\\right) = -2 + "
                "\\tfrac{13\\sqrt3}{2} \\approx 9.26.$$\n\n"
                "EXAMPLE 3 (direction 'from P toward Q'). At $P = (2, 1)$, "
                "toward $Q = (5, 5)$, for $f = xy$: the direction vector is "
                "$Q - P = \\langle 3, 4 \\rangle$ (destination minus start — "
                "order matters), $\\mathbf{u} = \\langle \\frac35, \\frac45 "
                "\\rangle$; $\\nabla f(2,1) = \\langle y, x \\rangle = "
                "\\langle 1, 2 \\rangle$;\n"
                "$$D_{\\mathbf{u}}f = 1 \\cdot \\tfrac35 + 2 \\cdot "
                "\\tfrac45 = \\tfrac{11}{5} = 2.2.$$\n"
                "Three input formats — vector, angle, two points — one "
                "invariant ritual: gradient, UNIT direction, dot."
            )),
            # ---------------------------------------------------------- 6
            (L, "The main theorems: steepest ascent and level-set geometry", (
                "Everything flows from one rewrite. For unit $\\mathbf{u}$ "
                "and $\\theta$ the angle between $\\nabla f$ and "
                "$\\mathbf{u}$:\n"
                "$$D_{\\mathbf{u}}f = \\nabla f \\cdot \\mathbf{u} = "
                "|\\nabla f|\\,|\\mathbf{u}|\\cos\\theta = "
                "|\\nabla f|\\cos\\theta.$$\n"
                "The direction $\\mathbf{u}$ enters ONLY through "
                "$\\cos\\theta$. Now read off the dial:\n\n"
                "• MAXIMUM: $\\cos\\theta = 1$, i.e. $\\mathbf{u}$ pointing "
                "ALONG $\\nabla f$. The greatest directional derivative is "
                "$|\\nabla f|$ itself. THE GRADIENT POINTS IN THE DIRECTION "
                "OF STEEPEST ASCENT, AND ITS LENGTH IS THAT STEEPEST RATE. "
                "(That is the proof — one line of dot-product geometry.)\n"
                "• MINIMUM: $\\cos\\theta = -1$: steepest DESCENT along "
                "$-\\nabla f$, at rate $-|\\nabla f|$.\n"
                "• ZERO: $\\cos\\theta = 0$: no instantaneous change "
                "perpendicular to $\\nabla f$ — the two contour directions of "
                "page 3.\n\n"
                "PERPENDICULARITY TO LEVEL SETS, proved. Let "
                "$\\mathbf{r}(t)$ be any curve lying inside the level curve "
                "$f = c$ through our point. Then $f(\\mathbf{r}(t)) = c$ "
                "identically; differentiate with C306:\n"
                "$$\\nabla f \\cdot \\mathbf{r}'(t) = 0.$$\n"
                "The gradient is orthogonal to every tangent vector of the "
                "level set: $\\nabla f \\perp$ level curve. In 3D the same "
                "computation makes $\\nabla F$ orthogonal to every curve in "
                "the level SURFACE $F = k$ — which is exactly why "
                "$\\langle F_x, F_y, F_z \\rangle$ served as the tangent "
                "plane's normal back in C305, page 6. That IOU is now "
                "paid.\n\n"
                "Assemble the picture: contour lines on a map, gradient "
                "arrows crossing them at right angles, longest arrows where "
                "contours crowd together. If $\\nabla f = \\mathbf{0}$ the "
                "dial reads zero in every direction — a CRITICAL POINT, "
                "C308's subject."
            )),
            # ---------------------------------------------------------- 7
            (E, "Worked examples: standard problems", (
                "EXAMPLE 1 (the full steepest-ascent package). For "
                "$f(x, y) = xe^y$ at $(2, 0)$: (a) direction of fastest "
                "increase, (b) that fastest rate, (c) directions of zero "
                "change.\n\n"
                "$\\nabla f = \\langle e^y,\\; xe^y \\rangle$, so "
                "$\\nabla f(2, 0) = \\langle 1, 2 \\rangle$.\n"
                "(a) Fastest increase along $\\nabla f$: unit direction "
                "$\\frac{1}{\\sqrt5}\\langle 1, 2 \\rangle$.\n"
                "(b) Rate $= |\\nabla f| = \\sqrt{1 + 4} = \\sqrt5 \\approx "
                "2.24$ per unit step.\n"
                "(c) Zero change perpendicular to the gradient: "
                "$\\pm\\frac{1}{\\sqrt5}\\langle 2, -1 \\rangle$ (swap "
                "components, flip one sign — check: $\\langle 1,2 \\rangle "
                "\\cdot \\langle 2,-1 \\rangle = 0$).\n\n"
                "EXAMPLE 2 (verifying perpendicularity to a contour). The "
                "level curve of $f = x^2 + y^2$ through $(3, 4)$ is the "
                "circle $x^2 + y^2 = 25$. Gradient there: $\\langle 2x, 2y "
                "\\rangle = \\langle 6, 8 \\rangle$ — radially outward. A "
                "tangent vector to the circle at $(3,4)$ is $\\langle -4, 3 "
                "\\rangle$; dot product $6(-4) + 8(3) = 0$. ✓ Gradient "
                "perpendicular to contour, exactly as page 6 proved in "
                "general.\n\n"
                "EXAMPLE 3 (a 3D directional derivative). "
                "$f(x, y, z) = x^2 + 2y^2 + 3z^2$ at $(1, 1, 1)$ toward the "
                "origin. Direction: $\\mathbf{0} - (1,1,1) = \\langle -1, -1, "
                "-1 \\rangle$, normalized $\\mathbf{u} = -\\frac{1}{\\sqrt3} "
                "\\langle 1, 1, 1 \\rangle$. Gradient: $\\langle 2x, 4y, 6z "
                "\\rangle = \\langle 2, 4, 6 \\rangle$.\n"
                "$$D_{\\mathbf{u}}f = \\langle 2, 4, 6 \\rangle \\cdot "
                "\\left(-\\tfrac{1}{\\sqrt3}\\right)\\langle 1, 1, 1 \\rangle "
                "= -\\frac{12}{\\sqrt3} = -4\\sqrt3 \\approx -6.93.$$\n"
                "Negative, as it must be: walking toward the origin descends "
                "this bowl. Always end with a sign sanity check against the "
                "geometry."
            )),
            # ---------------------------------------------------------- 8
            (L, "Techniques and strategy: the ritual, and reading contour maps", (
                "THE RITUAL. Every directional-derivative problem is the same "
                "three moves: (1) GRADIENT — compute $\\nabla f$ "
                "symbolically, then evaluate at the point (numbers before "
                "dotting). (2) UNIT DIRECTION — parse the direction from "
                "whatever costume it wears: a vector (normalize it), an angle "
                "($\\langle \\cos\\theta, \\sin\\theta \\rangle$, radians), "
                "two points (destination minus start, THEN normalize), 'due "
                "southwest' ($\\theta = \\frac{5\\pi}{4}$), 'toward the "
                "origin' (negate the position vector, normalize). "
                "(3) DOT.\n\n"
                "UNIT-VECTOR DISCIPLINE, stated once and forever: the "
                "normalization is not pedantry. $D_{\\mathbf{u}}f$ means rise "
                "per unit of horizontal distance; feeding a length-5 vector "
                "into the dot product reports rise per FIVE units — a "
                "different, wrong quantity. Make 'is it unit?' a reflex "
                "checked at step 2, every time, even when the given vector "
                "looks innocent.\n\n"
                "SUPERLATIVE QUESTIONS. 'Fastest increase' → direction "
                "$\\nabla f/|\\nabla f|$, rate $|\\nabla f|$. 'Fastest "
                "decrease' → the negatives. 'No change' → the two unit "
                "vectors perpendicular to $\\nabla f$. None of these need the "
                "dot product run explicitly — they are pre-solved by page 6's "
                "theorem. Recognizing a superlative question saves half the "
                "work.\n\n"
                "CONTOUR-MAP READING. On a topographic map: gradient arrows "
                "cross contours at right angles, pointing toward HIGHER "
                "values; arrow length is inversely proportional to contour "
                "spacing (tight contours = steep = long gradient). Given a "
                "map and a point, you can SKETCH $\\nabla f$ with no formula "
                "at all — and exam questions ask exactly that.\n\n"
                "SANITY CHECKS. Signs: moving toward a max should give "
                "positive $D_{\\mathbf{u}}f$. Bounds: no directional "
                "derivative can exceed $|\\nabla f|$ in magnitude; if your "
                "answer does, the direction was not unit."
            )),
            # ---------------------------------------------------------- 9
            (E, "Worked examples: harder problems", (
                "EXAMPLE 1 (reconstructing the gradient from two readings). "
                "At a point $P$, the directional derivative of $f$ in the "
                "direction $\\langle 1, 0 \\rangle$ is $3$, and in the "
                "direction $\\frac{1}{\\sqrt2}\\langle 1, 1 \\rangle$ it is "
                "$\\sqrt2$. Find $\\nabla f(P)$ and the maximal rate of "
                "increase.\n\n"
                "First reading: $D_{\\langle 1,0 \\rangle}f = f_x = 3$. "
                "Second: $\\frac{1}{\\sqrt2}(f_x + f_y) = \\sqrt2$, so "
                "$f_x + f_y = 2$, giving $f_y = -1$. Thus $\\nabla f(P) = "
                "\\langle 3, -1 \\rangle$ and the maximal rate is "
                "$|\\nabla f| = \\sqrt{10}$. Two independent directions "
                "determine the gradient — a linear system in disguise, and "
                "another face of page 3's 'two numbers suffice.'\n\n"
                "EXAMPLE 2 (rate along a curved path). A bug walks the "
                "parabola $\\mathbf{r}(t) = (t, t^2)$ through a temperature "
                "field $T = x^2 y$. How fast is its temperature changing at "
                "$t = 1$, per unit TIME and per unit DISTANCE?\n\n"
                "Per time (C306, Case 1): $\\nabla T = \\langle 2xy, x^2 "
                "\\rangle = \\langle 2, 1 \\rangle$ at $(1,1)$; "
                "$\\mathbf{r}' = \\langle 1, 2t \\rangle = \\langle 1, 2 "
                "\\rangle$:\n"
                "$$\\frac{dT}{dt} = \\langle 2, 1 \\rangle \\cdot \\langle 1, "
                "2 \\rangle = 4.$$\n"
                "Per distance: normalize the velocity — $|\\mathbf{r}'| = "
                "\\sqrt5$, so $D_{\\mathbf{u}}T = \\frac{4}{\\sqrt5}$. The "
                "directional derivative is the SPEED-INDEPENDENT part of the "
                "time rate: $\\frac{dT}{dt} = D_{\\mathbf{u}}T \\cdot "
                "|\\mathbf{r}'|$.\n\n"
                "EXAMPLE 3 (3D: tangent plane from the gradient). For the "
                "ellipsoid $F = x^2 + 2y^2 + 3z^2 = 6$ at $(1, 1, 1)$: "
                "$\\nabla F = \\langle 2, 4, 6 \\rangle$ is normal to the "
                "level surface (page 6), so the tangent plane is "
                "$2(x-1) + 4(y-1) + 6(z-1) = 0$, i.e. $x + 2y + 3z = 6$ — "
                "C305's implicit tangent plane, now derived rather than "
                "decreed."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: zero gradients, and directions that lie", (
                "ZERO GRADIENT. Where $\\nabla f = \\mathbf{0}$, the formula "
                "says every directional derivative is zero: the dial is flat. "
                "Such CRITICAL POINTS are where maxima, minima, and saddles "
                "hide, and steepest-ascent language breaks down there (no "
                "direction is steepest). C308 takes over at exactly this "
                "boundary; this chapter's job is only to flag it: "
                "$|\\nabla f| = 0$ means 'the gradient compass is spinning "
                "— ask the second derivatives.'\n\n"
                "NOW THE FINE PRINT, one last time. The dot-product formula "
                "required DIFFERENTIABILITY, and here is what failure looks "
                "like. Define\n"
                "$$f(x, y) = \\frac{x^2 y}{x^4 + y^2} \\;\\;(x,y) \\ne (0,0), "
                "\\qquad f(0,0) = 0.$$\n"
                "At the origin, EVERY directional derivative exists (page 11 "
                "computes them all — the limit definition works fine along "
                "every straight line). Both partials are $0$, so "
                "$\\nabla f(0,0) = \\mathbf{0}$ and the formula predicts "
                "$D_{\\mathbf{u}}f = 0$ for all $\\mathbf{u}$. But the actual "
                "directional derivatives are NOT all zero — the formula and "
                "the definition disagree. Worse: along the parabola "
                "$y = x^2$ the function is constantly $\\frac12$, so $f$ is "
                "not even continuous at the origin. Moral: 'all directional "
                "derivatives exist' is STILL weaker than differentiability — "
                "straight lines cannot detect misbehavior along curves. The "
                "hierarchy of C305 gains a rung:\n"
                "$$\\text{continuous partials} \\Rightarrow "
                "\\text{differentiable} \\Rightarrow \\text{all } "
                "D_{\\mathbf{u}}f \\text{ exist} \\Rightarrow "
                "\\text{partials exist},$$\n"
                "every arrow one-way.\n\n"
                "GRADIENT FIELDS, a preview. Attach $\\nabla f$'s arrow at "
                "EVERY point and you get a VECTOR FIELD — arrows swirling "
                "over the plane, everywhere perpendicular to the contours. "
                "Fields that arise this way (called CONSERVATIVE) are special "
                "among all vector fields, and telling which fields are "
                "gradients is a central question of C313-C314."
            )),
            # ---------------------------------------------------------- 11
            (E, "Worked example: the direction-derivative impostor", (
                "Work page 10's counterexample in full: $f = \\frac{x^2y}"
                "{x^4 + y^2}$, $f(0,0) = 0$.\n\n"
                "STEP 1 (all directional derivatives at the origin, from the "
                "DEFINITION). Take unit $\\mathbf{u} = \\langle u_1, u_2 "
                "\\rangle$ and compute $f$ along the ray:\n"
                "$$\\frac{f(hu_1, hu_2)}{h} = \\frac{1}{h}\\cdot "
                "\\frac{h^3 u_1^2 u_2}{h^4 u_1^4 + h^2 u_2^2} = "
                "\\frac{u_1^2 u_2}{h^2 u_1^4 + u_2^2}.$$\n"
                "If $u_2 \\ne 0$: limit $= \\frac{u_1^2 u_2}{u_2^2} = "
                "\\frac{u_1^2}{u_2}$. If $u_2 = 0$: $f \\equiv 0$ on the "
                "$x$-axis, limit $0$. Every direction has a derivative.\n\n"
                "STEP 2 (the formula's prediction). $f \\equiv 0$ on both "
                "axes, so $f_x(0,0) = f_y(0,0) = 0$: $\\nabla f = "
                "\\mathbf{0}$, predicting $D_{\\mathbf{u}}f = 0$ for all "
                "$\\mathbf{u}$. But Step 1 gave, e.g. for $\\mathbf{u} = "
                "\\frac{1}{\\sqrt2}\\langle 1, 1 \\rangle$: "
                "$\\frac{u_1^2}{u_2} = \\frac{1/2}{1/\\sqrt2} = "
                "\\frac{1}{\\sqrt2} \\ne 0$. Definition and formula "
                "disagree, so $f$ is NOT differentiable at the origin — the "
                "formula's hypothesis fails, not the formula.\n\n"
                "STEP 3 (the deeper disease). Along $y = x^2$: "
                "$f(x, x^2) = \\frac{x^4}{x^4 + x^4} = \\frac12$ for all "
                "$x \\ne 0$, while $f(0,0) = 0$: not continuous. Straight "
                "rays all behave (Step 1!), the parabola does not — "
                "line-based probes miss curve-based misbehavior.\n\n"
                "TAKEAWAYS. (1) $D_{\\mathbf{u}}f$ existing for all "
                "$\\mathbf{u}$ does not imply differentiability, continuity, "
                "or the dot-product formula. (2) When hypotheses hold "
                "(continuous partials — true for essentially every function "
                "you will meet in applications), the formula is bulletproof. "
                "(3) On exams, 'justify' means: cite continuous partials, "
                "then dot."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'The gradient lives on the surface, "
                "pointing uphill in 3D.' No — for $f(x, y)$, $\\nabla f$ is a "
                "vector in the INPUT PLANE (the map, not the mountain). It "
                "has two components, not three. It points in the compass "
                "bearing you should walk; it is not the 3D uphill tangent "
                "vector of the surface. Conflating the two produces "
                "three-component 'gradients' and nonsense normals. (The "
                "related 3D object is the SURFACE normal $\\langle f_x, f_y, "
                "-1 \\rangle$ from C305 — different beast, different "
                "space.)\n\n"
                "MISCONCEPTION 2: '$\\nabla f$ points toward the maximum.' "
                "It points in the locally steepest direction — which may spiral, "
                "wander, or lead to a different peak entirely. Steepest "
                "ascent is a GREEDY instruction, not a route plan; gradient-"
                "descent algorithms famously stall in local minima for "
                "exactly this reason.\n\n"
                "MISCONCEPTION 3: 'The gradient is tangent to the level "
                "curve.' Exactly backwards — it is PERPENDICULAR (page 6's "
                "proof: $\\nabla f \\cdot \\mathbf{r}' = 0$). The tangent "
                "directions are the zero-change directions. If your sketch "
                "shows gradient arrows running along contours, stop and "
                "re-derive.\n\n"
                "MISCONCEPTION 4: '$D_{\\mathbf{u}}f$ is a vector.' It is a "
                "SCALAR — a slope, one number per direction. The gradient is "
                "the vector; the directional derivative is its shadow on a "
                "chosen direction. Writing $D_{\\mathbf{u}}f = \\langle "
                "\\ldots \\rangle$ signals a category error graders hunt "
                "for.\n\n"
                "MISCONCEPTION 5: 'All directional derivatives exist, so "
                "the function is differentiable and the formula applies.' "
                "Pages 10-11's impostor kills this: existence along every "
                "straight line is still not local flatness. The clean "
                "sufficient condition remains continuous partials (C305). "
                "Cite it, then compute."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational errors", (
                "ERROR 1 (the classic): DOTTING WITH A NON-UNIT VECTOR. "
                "Direction $\\langle 3, 4 \\rangle$, gradient $\\langle 4, 13 "
                "\\rangle$, answer $64$ — wrong by the factor "
                "$|\\mathbf{v}| = 5$; the true value is $12.8$. Normalize at "
                "the moment you first write the direction, and flag it: "
                "$\\mathbf{u} = \\frac15\\langle 3, 4\\rangle$. This single "
                "habit prevents the most common lost points in the "
                "chapter.\n\n"
                "ERROR 2: Forgetting to EVALUATE the gradient. "
                "$D_{\\mathbf{u}}f = \\langle 2xy, x^2 + 3y^2 \\rangle \\cdot "
                "\\mathbf{u}$ left with variables is not an answer to 'at "
                "$(1, 2)$.' Numbers first, then dot.\n\n"
                "ERROR 3: Direction 'from P toward Q' computed as $P - Q$. "
                "Destination minus start: $Q - P$. The sign of the answer "
                "flips with the subtraction order, and sign is usually the "
                "graded content ('is the temperature rising?').\n\n"
                "ERROR 4: Angle directions in DEGREES. $\\langle \\cos 60, "
                "\\sin 60 \\rangle$ with a calculator in radian mode — or "
                "$\\theta = 60$ fed to radian formulas — scrambles the "
                "direction entirely. Calculus is radians (same warning as "
                "C101; same exam casualties).\n\n"
                "ERROR 5: Perpendicular-direction slips. Zero-change "
                "directions to $\\nabla f = \\langle a, b \\rangle$ are "
                "$\\pm\\langle -b, a \\rangle$ NORMALIZED — students swap "
                "components without a sign flip ($\\langle b, a \\rangle$ is "
                "NOT perpendicular) or forget both signs count as answers.\n\n"
                "ERROR 6: Maximal-rate answers with direction and magnitude "
                "swapped or merged: 'fastest increase is $\\langle 1, 2 "
                "\\rangle$' (that's the direction; the RATE is $\\sqrt5$), or "
                "'rate is $\\frac{1}{\\sqrt5}\\langle 1,2 \\rangle$' (a "
                "vector is not a rate). Superlative questions have TWO "
                "answers — a unit direction and a scalar rate — and full "
                "credit reports both, labeled."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: following (and fighting) the gradient", (
                "1. GRADIENT DESCENT. To minimize a loss function "
                "$L(\\theta_1, \\ldots, \\theta_n)$ — machine learning's "
                "daily bread — step repeatedly in the direction of steepest "
                "DESCENT: $\\boldsymbol{\\theta}_{k+1} = "
                "\\boldsymbol{\\theta}_k - \\eta \\nabla "
                "L(\\boldsymbol{\\theta}_k)$. Page 6's theorem is the entire "
                "justification: no direction reduces $L$ faster, per unit "
                "step, than $-\\nabla L$. Every large neural network is "
                "trained by this loop, with C306's backpropagation computing "
                "the gradient and this chapter choosing what to do with "
                "it.\n\n"
                "2. PHYSICS WRITES ITS LAWS WITH GRADIENTS. Heat flows "
                "against the temperature gradient (Fourier: $\\mathbf{q} = "
                "-k\\nabla T$); solutes diffuse against concentration "
                "gradients (Fick); electric fields are downhill potential "
                "gradients ($\\mathbf{E} = -\\nabla V$); gravity is "
                "$-\\nabla$ of gravitational potential. The shared logic: "
                "transport runs from high to low, and 'straight from high to "
                "low' IS $-\\nabla$, the steepest descent direction. The "
                "perpendicularity theorem appears too: equipotential "
                "surfaces ($V = $ const) meet field lines at right "
                "angles.\n\n"
                "3. TERRAIN AND NAVIGATION. Water drains along "
                "$-\\nabla(\\text{elevation})$ — stream networks are "
                "gradient-flow diagrams you can see from aircraft. Trail "
                "designers switchback precisely to keep the path's direction "
                "far from $\\pm\\nabla h$, capping $D_{\\mathbf{u}}h$ at a "
                "hikeable grade — a directional-derivative constraint enforced "
                "with a bulldozer.\n\n"
                "4. IMAGE PROCESSING. A digital image is a function "
                "$I(x, y)$ (brightness); $|\\nabla I|$ is large exactly at "
                "EDGES, and edge-detection filters (Sobel, Canny) are "
                "discrete gradient estimators. The gradient's direction "
                "points across the edge, its magnitude scores the contrast — "
                "this chapter's two readings, in every phone camera."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: reading a mountainside", (
                "The elevation (meters) near a trailhead is modeled by\n"
                "$$h(x, y) = 1000 - 0.005x^2 - 0.01y^2,$$\n"
                "$x$ east, $y$ north (meters). You stand at $(60, 40)$, "
                "elevation $h = 1000 - 18 - 16 = 966$ m.\n\n"
                "STEP 1 — gradient: $\\nabla h = \\langle -0.01x,\\; -0.02y "
                "\\rangle$, so $\\nabla h(60, 40) = \\langle -0.6, -0.8 "
                "\\rangle$. Note $|\\nabla h| = \\sqrt{0.36 + 0.64} = 1$.\n\n"
                "STEP 2 — steepest ascent: direction $\\langle -0.6, -0.8 "
                "\\rangle$ (already unit!), i.e. west-southwest; the climb "
                "rate is $1$ meter of rise per meter of ground — a 45° "
                "pitch. Steepest descent: $\\langle 0.6, 0.8 \\rangle$, "
                "east-northeast.\n\n"
                "STEP 3 — a proposed bearing: the trail heads NORTHEAST, "
                "$\\mathbf{u} = \\frac{1}{\\sqrt2}\\langle 1, 1 \\rangle$:\n"
                "$$D_{\\mathbf{u}}h = \\langle -0.6, -0.8 \\rangle \\cdot "
                "\\tfrac{1}{\\sqrt2}\\langle 1, 1 \\rangle = "
                "\\frac{-1.4}{\\sqrt2} \\approx -0.99,$$\n"
                "a punishing 45° descent. Verdict: not hikeable as is.\n\n"
                "STEP 4 — contour walking: zero-change directions are "
                "perpendicular to $\\nabla h$: $\\pm\\langle -0.8, 0.6 "
                "\\rangle$ — roughly WNW or ESE keeps you level.\n\n"
                "STEP 5 — design a 10% grade: we need "
                "$D_{\\mathbf{u}}h = |\\nabla h|\\cos\\theta = \\cos\\theta = "
                "0.10$, so $\\theta = \\arccos(0.10) \\approx 84.3°$ away "
                "from the fall line — nearly along the contour, tilted "
                "slightly uphill. That is why mountain trails traverse and "
                "switchback: the geometry of $\\cos\\theta$ leaves no other "
                "way to cap the grade. One gradient, five decisions."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: where this chapter plugs into everything else", (
                "BACKWARD. C304's partials are the gradient's components. "
                "C305 supplied differentiability — the hypothesis that makes "
                "the dot-product formula true — and had already used "
                "$\\nabla F$ as a normal vector before naming it; page 6 "
                "paid that debt with the perpendicularity proof. C306's "
                "chain rule WAS the proof, twice (the formula on page 4, "
                "perpendicularity on page 6). C301 and LA.U8.N1's dot "
                "product carried the geometry.\n\n"
                "FORWARD, immediately:\n"
                "• C308 (critical points): where $\\nabla f = \\mathbf{0}$, "
                "this chapter's compass goes silent, and classification "
                "passes to second derivatives — the Hessian. C308 begins at "
                "exactly the points this chapter flags.\n"
                "• C309 (Lagrange multipliers): at a constrained optimum, "
                "the level curves of objective and constraint are tangent, "
                "so their gradients are parallel: $\\nabla f = \\lambda "
                "\\nabla g$. The perpendicularity theorem does all the "
                "work.\n\n"
                "FURTHER OUT:\n"
                "• C313 (vector fields): the gradient field $\\nabla f$ is "
                "the prototype vector field; 'which fields are gradients?' "
                "(conservative fields, detected by curl) organizes the "
                "chapter.\n"
                "• C314 (line integrals): the fundamental theorem "
                "$\\int_C \\nabla f \\cdot d\\mathbf{r} = f(B) - f(A)$ "
                "integrates this chapter's formula along a curve — gradient "
                "fields have path-independent work.\n"
                "• ODE.U7.N2: gradient systems $\\mathbf{x}' = -\\nabla V$ "
                "flow downhill to equilibria; stability again reads off the "
                "linearization.\n"
                "• Numerical optimization and ML: gradient descent (page "
                "14) plus C306's backpropagation are the working core of "
                "modern AI. The gradient is the single most-computed "
                "mathematical object on Earth right now."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: why 'steepest' depends on how you measure", (
                "A confession: 'the gradient points in the steepest "
                "direction' hides a convention. The theorem compared "
                "directions using EUCLIDEAN length — all unit vectors, "
                "measured by $\\sqrt{u_1^2 + u_2^2}$. Change the notion of "
                "length and 'steepest' changes with it. If east-west steps "
                "cost twice as much as north-south ones (an anisotropic "
                "metric), the direction of best value-per-cost tilts away "
                "from $\\nabla f$. Formally: the differential $df$ (the "
                "linear map $\\mathbf{u} \\mapsto D_{\\mathbf{u}}f$) is "
                "metric-free, but converting it into a VECTOR requires an "
                "inner product; different inner products yield different "
                "gradient vectors. Differential geometry keeps the "
                "distinction explicit — $df$ is a covector, $\\nabla f$ its "
                "metric-dependent mirror — and Riemannian gradients power "
                "'natural gradient' methods in statistics and ML, where the "
                "right metric on parameter space is not Euclidean.\n\n"
                "THE OPERATOR VIEW. Write $\\nabla = \\left\\langle "
                "\\frac{\\partial}{\\partial x}, \\frac{\\partial}"
                "{\\partial y}, \\frac{\\partial}{\\partial z} "
                "\\right\\rangle$ — a vector OF OPERATORS, waiting for prey. "
                "Applied to a scalar: the gradient. Dotted with a field: "
                "divergence, $\\nabla \\cdot \\mathbf{F}$. Crossed: curl, "
                "$\\nabla \\times \\mathbf{F}$. Applied twice: the Laplacian "
                "$\\nabla^2 = \\nabla \\cdot \\nabla$, star of the heat and "
                "wave equations. C313 develops all four; the notation you "
                "learned this chapter is the shared alphabet.\n\n"
                "HISTORY. The symbol $\\nabla$ ('nabla,' after a Phoenician "
                "harp) was doodled by Hamilton (1830s) and weaponized by "
                "Maxwell, Gibbs, and Heaviside, who compressed "
                "electromagnetic theory from twenty scalar equations into "
                "four vector ones. Good notation is compressed insight: "
                "$\\nabla$ carries this entire chapter in one glyph."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "GRADIENT: $\\nabla f = \\langle f_x, f_y \\rangle$ (2D), "
                "$\\langle f_x, f_y, f_z \\rangle$ (3D) — a vector in the "
                "INPUT space, one arrow per point.\n\n"
                "DIRECTIONAL DERIVATIVE (unit $\\mathbf{u}$ only):\n"
                "$$D_{\\mathbf{u}}f = \\lim_{h \\to 0} "
                "\\frac{f(\\mathbf{a} + h\\mathbf{u}) - f(\\mathbf{a})}{h} "
                "\\;\\overset{f \\text{ diff'able}}{=}\\; \\nabla f \\cdot "
                "\\mathbf{u} = |\\nabla f|\\cos\\theta.$$\n"
                "A SCALAR. Axis directions recover $f_x$, $f_y$.\n\n"
                "THE DIAL. Max rate $|\\nabla f|$, in direction "
                "$\\nabla f / |\\nabla f|$. Min rate $-|\\nabla f|$, opposite "
                "direction. Zero along $\\pm$ the perpendicular. "
                "$\\nabla f \\perp$ level curve / level surface (proof: "
                "differentiate $f(\\mathbf{r}(t)) = c$). $\\nabla f = "
                "\\mathbf{0}$: critical point — C308's territory.\n\n"
                "HIERARCHY (all arrows one-way): continuous partials "
                "$\\Rightarrow$ differentiable $\\Rightarrow$ all "
                "$D_{\\mathbf{u}}f$ exist $\\Rightarrow$ partials exist. "
                "Impostor for the middle gap: $\\frac{x^2y}{x^4 + y^2}$ — "
                "every directional derivative exists at $\\mathbf{0}$, yet "
                "discontinuous there (constant $\\frac12$ on $y = x^2$).\n\n"
                "DIRECTION PARSING. Vector $\\mathbf{v}$: use "
                "$\\mathbf{v}/|\\mathbf{v}|$. Angle $\\theta$ (radians): "
                "$\\langle \\cos\\theta, \\sin\\theta \\rangle$. From $P$ "
                "toward $Q$: normalize $Q - P$. ALWAYS unit before "
                "dotting.\n\n"
                "RATE ALONG A CURVE: $\\frac{d}{dt}f(\\mathbf{r}(t)) = "
                "\\nabla f \\cdot \\mathbf{r}'(t)$; per-distance version "
                "divides by $|\\mathbf{r}'|$.\n\n"
                "WORKFLOW. (1) Gradient, evaluated. (2) Direction, unit. "
                "(3) Dot. (4) Superlatives: answer with direction AND rate. "
                "(5) Sanity: $|D_{\\mathbf{u}}f| \\le |\\nabla f|$, signs "
                "match geometry."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all eight before reading the solutions.\n\n"
                "1. $\\nabla f$ for $f = x^2y + y^3$ at $(1, 2)$?\n"
                "2. Same $f$, same point: $D_{\\mathbf{u}}f$ in the "
                "direction of $\\mathbf{v} = \\langle 3, -4 \\rangle$?\n"
                "3. Maximum rate of increase of $g = xe^y$ at $(2, 0)$, and "
                "its direction?\n"
                "4. Directions of ZERO change of $g$ at $(2, 0)$?\n"
                "5. Show the gradient of $f = x^2 + y^2$ at $(3, 4)$ is "
                "perpendicular to the circle $x^2 + y^2 = 25$ there.\n"
                "6. At $P$: $D_{\\langle 1, 0 \\rangle}f = 5$ and "
                "$D_{\\mathbf{u}}f = \\frac{7}{\\sqrt2}$ for $\\mathbf{u} = "
                "\\frac{1}{\\sqrt2}\\langle 1, 1 \\rangle$. Find "
                "$\\nabla f(P)$.\n"
                "7. A bug at $(1, 1)$ in temperature field $T = x^2y$ walks "
                "with velocity $\\langle 1, 2 \\rangle$: find "
                "$\\frac{dT}{dt}$.\n"
                "8. True or false: if every directional derivative of $f$ "
                "exists at $P$, then $f$ is differentiable at $P$.\n\n"
                "SOLUTIONS.\n"
                "1. $\\langle 2xy, x^2 + 3y^2 \\rangle = \\langle 4, 13 "
                "\\rangle$.\n"
                "2. $\\mathbf{u} = \\frac15\\langle 3, -4 \\rangle$: "
                "$D = 4 \\cdot \\frac35 + 13 \\cdot (-\\frac45) = 2.4 - "
                "10.4 = -8$.\n"
                "3. $\\nabla g(2,0) = \\langle e^y, xe^y \\rangle = \\langle "
                "1, 2 \\rangle$: max rate $\\sqrt5$, direction "
                "$\\frac{1}{\\sqrt5}\\langle 1, 2 \\rangle$.\n"
                "4. $\\pm\\frac{1}{\\sqrt5}\\langle 2, -1 \\rangle$ "
                "(perpendicular to $\\langle 1, 2 \\rangle$; check the dot "
                "product is 0).\n"
                "5. $\\nabla f = \\langle 6, 8 \\rangle$; tangent to the "
                "circle at $(3,4)$ is $\\langle -4, 3 \\rangle$; "
                "$6(-4) + 8(3) = 0$. ✓\n"
                "6. $f_x = 5$; $\\frac{1}{\\sqrt2}(f_x + f_y) = "
                "\\frac{7}{\\sqrt2} \\Rightarrow f_x + f_y = 7 \\Rightarrow "
                "f_y = 2$: $\\nabla f = \\langle 5, 2 \\rangle$.\n"
                "7. $\\nabla T(1,1) = \\langle 2xy, x^2 \\rangle = \\langle "
                "2, 1 \\rangle$: $\\frac{dT}{dt} = \\langle 2, 1 \\rangle "
                "\\cdot \\langle 1, 2 \\rangle = 4$ (velocity NOT "
                "normalized — this is a per-time rate, C306 style).\n"
                "8. FALSE — $\\frac{x^2y}{x^4 + y^2}$ (pages 10-11): all "
                "directional derivatives exist at the origin, but $f$ is "
                "discontinuous there.\n\n"
                "Scoring: misses on 2 → unit-vector drill (pages 5, 13); on "
                "5 → page 6; on 7 → page 9; on 8 → pages 10-11."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Before leaving this chapter you should be able to check "
                "every box honestly:\n\n"
                "□ State the limit definition of $D_{\\mathbf{u}}f$ and "
                "explain why the axis directions recover $f_x$ and "
                "$f_y$.\n"
                "□ Prove $D_{\\mathbf{u}}f = \\nabla f \\cdot \\mathbf{u}$ "
                "in two lines from C306's chain rule, naming where "
                "differentiability is used.\n"
                "□ Execute the ritual — gradient, unit direction, dot — on "
                "directions given as vectors, angles, or point pairs, "
                "normalizing without being reminded.\n"
                "□ Prove that the gradient direction maximizes "
                "$D_{\\mathbf{u}}f$ (the $|\\nabla f|\\cos\\theta$ "
                "argument) and answer superlative questions with BOTH a "
                "unit direction and a scalar rate.\n"
                "□ Prove $\\nabla f \\perp$ level sets by differentiating "
                "$f(\\mathbf{r}(t)) = c$, and use $\\nabla F$ as the normal "
                "for tangent planes to level surfaces (C305's form, now "
                "justified).\n"
                "□ Sketch gradient arrows on a contour map: perpendicular "
                "to contours, toward higher values, longer where contours "
                "crowd.\n"
                "□ Reconstruct $\\nabla f$ from directional derivatives in "
                "two independent directions.\n"
                "□ Separate per-time rates ($\\nabla f \\cdot "
                "\\mathbf{r}'$) from per-distance rates (normalize the "
                "velocity).\n"
                "□ Reproduce the impostor $\\frac{x^2y}{x^4+y^2}$ and place "
                "'all directional derivatives exist' correctly in the "
                "implication hierarchy.\n"
                "□ Explain gradient descent's justification in one "
                "sentence.\n\n"
                "When every box is checked, proceed to C308: the points "
                "where the gradient vanishes, and the second-derivative "
                "machinery that sorts peaks from passes."
            )),
        ],
    },
    "C308": {
        "summary": (
            "A complete course on critical points and the second derivative "
            "test: where and why extrema force the gradient to vanish, saddle "
            "points, the Hessian discriminant D = f_xx f_yy - f_xy^2 and the "
            "four-outcome test (including honest D = 0 examples), absolute "
            "extrema on closed bounded regions via boundary parameterization, "
            "and the eigenvalue meaning of the discriminant."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "This chapter is the payoff the last four have been funding. "
                "Optimization — find the best, the cheapest, the largest, the "
                "most stable — is why most working scientists ever compute a "
                "derivative, and you now own every tool the problem needs. "
                "C307 built the compass: away from special points, "
                "$\\nabla f$ names a direction of increase, so no point with "
                "$\\nabla f \\ne \\mathbf{0}$ can be a summit or a pit. The "
                "hunt for extrema therefore begins where the compass spins: "
                "the CRITICAL POINTS, where $\\nabla f = \\mathbf{0}$ (or "
                "fails to exist).\n\n"
                "Finding them is algebra. CLASSIFYING them is the new "
                "mathematics. In one variable (C113, C114) a critical point "
                "was a max, a min, or a rare degenerate case, and $f''$ told "
                "you which. In two variables a genuinely new creature "
                "appears: the SADDLE POINT — a mountain pass, maximal along "
                "one line and minimal along another, an extremum in no "
                "direction that matters. The classifier is the SECOND "
                "DERIVATIVE TEST, run on the discriminant\n"
                "$$D = f_{xx}f_{yy} - f_{xy}^2,$$\n"
                "a single number distilled from the three second partials of "
                "C304. Four outcomes, one of them honest ignorance "
                "($D = 0$), and this chapter shows examples proving the "
                "ignorance is real.\n\n"
                "Then the second act: ABSOLUTE extrema on a closed, bounded "
                "region — the multivariable version of C113's closed-interval "
                "method — where the boundary becomes a one-variable problem "
                "by parameterization.\n\n"
                "You will learn: what critical points are and why extrema "
                "hide among them, the four-outcome test and where $D$ comes "
                "from, worked classifications, the $D = 0$ menagerie, the "
                "closed-region algorithm, and what $D$ really is: the "
                "determinant of the Hessian, judging the signs of its "
                "eigenvalues (LA.U7.N1)."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Four ingredients.\n\n"
                "1. SECOND PARTIALS AND CLAIRAUT (C304). You need $f_{xx}$, "
                "$f_{yy}$, $f_{xy}$ computed fluently, and Clairaut's theorem "
                "($f_{xy} = f_{yx}$ for continuous second partials) — it is "
                "why THREE numbers, not four, feed the test.\n\n"
                "2. THE GRADIENT (C307). $\\nabla f = \\mathbf{0}$ is the "
                "critical-point equation; the reason it matters is C307's "
                "steepest-ascent theorem.\n\n"
                "3. THE ONE-VARIABLE STORY (C113, C114). Fermat's theorem "
                "(interior extrema have $f' = 0$), the second derivative "
                "test, and the closed-interval method (compare interior "
                "candidates with endpoint values). Every structure here is "
                "the two-variable upgrade of one of those.\n\n"
                "4. SOLVING SMALL NONLINEAR SYSTEMS. Setting $f_x = 0 = f_y$ "
                "yields two equations in two unknowns; you need clean case "
                "analysis (factor, never divide by something that might be "
                "zero).\n\n"
                "Quick self-check (answers follow):\n\n"
                "• All three second partials of $f = x^3y^2$? ($f_{xx} = "
                "6xy^2$, $f_{yy} = 2x^3$, $f_{xy} = 6x^2y$.)\n"
                "• For one-variable $g(x) = x^3 - 3x$: critical points and "
                "their types? ($g' = 3x^2 - 3 = 0$ at $x = \\pm 1$; "
                "$g'' = 6x$: min at $1$, max at $-1$.)\n"
                "• Solve the system $xy = 0$, $x + y = 2$. (Cases: $x = 0 "
                "\\Rightarrow y = 2$; $y = 0 \\Rightarrow x = 2$. Two "
                "solutions — did you get both?)\n"
                "• On $[0, 3]$, where can $g(x) = x^2 - 4x$ attain its "
                "absolute extrema? (Critical point $x = 2$ and endpoints "
                "$0, 3$ — candidates only.)\n\n"
                "The third question's CASE DISCIPLINE is where most "
                "critical-point hunts go wrong. If all four were solid, "
                "proceed."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively: flat spots, and the three kinds", (
                "Walk a mountain landscape $z = f(x, y)$. At a SUMMIT, every "
                "direction leads down. In a PIT (a lake bottom), every "
                "direction leads up. At a PASS between two valleys, the "
                "trail ahead and behind climbs, the slopes to either side "
                "fall away — down along one line, up along another. Summits, "
                "pits, passes: local max, local min, saddle.\n\n"
                "What do all three share? FLATNESS. At each, every slice "
                "through the point has a horizontal tangent line, so both "
                "partials vanish and the tangent plane (C305) is horizontal: "
                "$\\nabla f = \\mathbf{0}$. The argument for why an interior "
                "extremum MUST be flat is pure C307: if $\\nabla f \\ne "
                "\\mathbf{0}$, then walking along $\\nabla f$ increases $f$ "
                "and walking along $-\\nabla f$ decreases it — so the point "
                "is neither a local max nor a local min. Contrapositive: "
                "extrema only at critical points. (Fermat's theorem, C113, "
                "one dimension up.)\n\n"
                "But flatness is a WHERE, not a WHAT. The gradient — a "
                "first-order instrument — reads zero identically at summit, "
                "pit, and pass. Telling them apart requires asking how the "
                "surface CURVES away from its horizontal tangent plane: "
                "bending up in all directions (pit), down in all directions "
                "(summit), or up in some and down in others (pass). "
                "Curvature is second-derivative information, and the whole "
                "technical content of this chapter is compressing the three "
                "second partials $f_{xx}, f_{xy}, f_{yy}$ into a verdict.\n\n"
                "The genuinely two-dimensional surprise is the pass. One "
                "variable has no saddles — with only two directions to "
                "check, 'up one way, down another' is impossible to sustain "
                "smoothly at a flat point. In two variables, saddles are not "
                "rare exceptions; they are as common as extrema, and "
                "mistaking one for a minimum has sunk real engineering "
                "designs. The test exists to prevent exactly that."
            )),
            # ---------------------------------------------------------- 4
            (L, "The formal definitions and the second derivative test", (
                "DEFINITIONS. $f$ has a LOCAL MAXIMUM at $(a, b)$ if "
                "$f(x, y) \\le f(a, b)$ for all $(x, y)$ near $(a, b)$; "
                "LOCAL MINIMUM with $\\ge$. A CRITICAL POINT of $f$ is a "
                "point where $\\nabla f = \\mathbf{0}$ or where a partial "
                "fails to exist. THEOREM (Fermat, two variables): every "
                "interior local extremum occurs at a critical point. A "
                "critical point that is neither a local max nor a local min "
                "is a SADDLE POINT.\n\n"
                "THE SECOND DERIVATIVE TEST. Suppose the second partials of "
                "$f$ are continuous near a critical point $(a, b)$ (so "
                "Clairaut applies), and let\n"
                "$$D = D(a, b) = f_{xx}(a, b)\\,f_{yy}(a, b) - "
                "\\big(f_{xy}(a, b)\\big)^2.$$\n"
                "Four outcomes:\n"
                "1. $D > 0$ and $f_{xx}(a, b) > 0$: LOCAL MINIMUM.\n"
                "2. $D > 0$ and $f_{xx}(a, b) < 0$: LOCAL MAXIMUM.\n"
                "3. $D < 0$: SADDLE POINT.\n"
                "4. $D = 0$: THE TEST IS SILENT — anything can happen "
                "(page 10 exhibits a min, a max, and a saddle-like point "
                "all with $D = 0$).\n\n"
                "How to hold it in memory: $D > 0$ means the two pure "
                "curvatures COOPERATE and dominate the cross term — the "
                "surface bends the same way in all directions, so it is a "
                "true extremum, and $f_{xx}$ (either pure curvature; they "
                "share a sign when $D > 0$) says which. $D < 0$ means the "
                "curvatures fight, or the cross term wins: some directions "
                "bend up, some down — a saddle. Note also the useful "
                "corollary of case 3: whenever $f_{xx}$ and $f_{yy}$ have "
                "OPPOSITE signs, $D < 0$ automatically — instant saddle, no "
                "arithmetic.\n\n"
                "Where $D$ comes from — the quadratic approximation and its "
                "discriminant — is page 6's story; the eigenvalue meaning "
                "is page 17's."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples: the algorithm end to end", (
                "EXAMPLE 1 (a clean minimum). $f(x, y) = x^2 + y^2 - 2x - "
                "6y + 14$.\n\n"
                "Step 1 — critical points: $f_x = 2x - 2 = 0$ and "
                "$f_y = 2y - 6 = 0$ give the single point $(1, 3)$.\n"
                "Step 2 — second partials: $f_{xx} = 2$, $f_{yy} = 2$, "
                "$f_{xy} = 0$.\n"
                "Step 3 — discriminant: $D = (2)(2) - 0^2 = 4 > 0$, and "
                "$f_{xx} = 2 > 0$: LOCAL MINIMUM, value $f(1, 3) = 1 + 9 - "
                "2 - 18 + 14 = 4$.\n"
                "Verification by algebra (possible here because the function "
                "is quadratic): complete the square, $f = (x - 1)^2 + "
                "(y - 3)^2 + 4$ — a bowl with vertex $(1, 3, 4)$. The test "
                "agreed with the truth; when you can check, check.\n\n"
                "EXAMPLE 2 (the canonical saddle). $f(x, y) = y^2 - x^2$.\n\n"
                "$f_x = -2x = 0$, $f_y = 2y = 0$: critical point $(0, 0)$. "
                "Second partials $f_{xx} = -2$, $f_{yy} = 2$, $f_{xy} = 0$: "
                "$D = (-2)(2) - 0 = -4 < 0$: SADDLE. (Opposite-sign pure "
                "curvatures — the instant-saddle shortcut of page 4.) See it "
                "directly: along the $x$-axis, $f = -x^2$ has a max at 0; "
                "along the $y$-axis, $f = y^2$ has a min. Up one way, down "
                "the other: the pass.\n\n"
                "EXAMPLE 3 (critical point without equations). "
                "$f(x, y) = \\sqrt{x^2 + y^2}$ — the cone. At $(0, 0)$ the "
                "partials DO NOT EXIST (C305, page 10: the slice $|x|$ has a "
                "corner), so the origin is a critical point of the second "
                "kind. No test needed to classify: $f(0,0) = 0 \\le f$ "
                "everywhere — a local (indeed absolute) minimum living at a "
                "non-smooth point. Moral: 'partials undefined' points are "
                "real candidates, and the discriminant is not the only tool "
                "— sometimes inspection settles what calculus cannot."
            )),
            # ---------------------------------------------------------- 6
            (L, "Why the test works: the quadratic shadow", (
                "Near a critical point the linear term of the Taylor "
                "expansion vanishes ($\\nabla f = \\mathbf{0}$), so the "
                "surface's local shape is governed by the QUADRATIC "
                "terms:\n"
                "$$f(a + h,\\, b + k) \\approx f(a, b) + \\tfrac{1}{2}\\left( "
                "f_{xx}h^2 + 2f_{xy}hk + f_{yy}k^2 \\right),$$\n"
                "(second partials at $(a,b)$; this is C305's linearization "
                "carried one order deeper, and the same quadratic form that "
                "appeared in C306's second-derivative formula). Classifying "
                "the critical point means answering: is the quadratic form "
                "$Q(h, k) = f_{xx}h^2 + 2f_{xy}hk + f_{yy}k^2$ positive for "
                "all directions, negative for all, or mixed?\n\n"
                "Complete the square (assume $f_{xx} \\ne 0$):\n"
                "$$Q = f_{xx}\\left(h + \\frac{f_{xy}}{f_{xx}}k\\right)^2 + "
                "\\frac{f_{xx}f_{yy} - f_{xy}^2}{f_{xx}}\\,k^2 = "
                "f_{xx}\\left(h + \\frac{f_{xy}}{f_{xx}}k\\right)^2 + "
                "\\frac{D}{f_{xx}}k^2.$$\n"
                "Read the two squares. If $D > 0$, both coefficients share "
                "the sign of $f_{xx}$: $Q$ is one-signed — bowl up "
                "($f_{xx} > 0$, min) or bowl down ($f_{xx} < 0$, max). If "
                "$D < 0$, the coefficients disagree: $Q$ is positive in some "
                "directions, negative in others — saddle. If $D = 0$, the "
                "second square vanishes and $Q$ is degenerate: flat along "
                "some direction, and the quadratic shadow is too coarse to "
                "decide — higher-order terms rule (page 10). The test is "
                "nothing but this completed square, memorized.\n\n"
                "Two footnotes. First, when $D > 0$, $f_{yy}$ carries the "
                "same sign as $f_{xx}$ (their product exceeds $f_{xy}^2 \\ge "
                "0$), so checking $f_{yy}$ instead is legal. Second, the "
                "EXTREME VALUE THEOREM (C113's, upgraded): a continuous $f$ "
                "on a CLOSED, BOUNDED region attains absolute max and min — "
                "the guarantee behind page 8's closed-region algorithm."
            )),
            # ---------------------------------------------------------- 7
            (E, "Worked examples: standard problems", (
                "EXAMPLE 1 (multiple critical points, full protocol). "
                "Classify all critical points of $f(x, y) = x^4 + y^4 - "
                "4xy + 1$.\n\n"
                "Step 1 — the system: $f_x = 4x^3 - 4y = 0$, "
                "$f_y = 4y^3 - 4x = 0$, i.e. $y = x^3$ and $x = y^3$. "
                "Substitute: $x = (x^3)^3 = x^9$, so $x^9 - x = "
                "x(x^8 - 1) = 0$: $x = 0, 1, -1$ (real solutions). Critical "
                "points: $(0, 0)$, $(1, 1)$, $(-1, -1)$.\n\n"
                "Step 2 — second partials: $f_{xx} = 12x^2$, $f_{yy} = "
                "12y^2$, $f_{xy} = -4$; $D = 144x^2y^2 - 16$.\n\n"
                "Step 3 — the table (make one; graders and future-you both "
                "bless it):\n\n"
                "• $(0, 0)$: $D = -16 < 0$ — SADDLE.\n"
                "• $(1, 1)$: $D = 128 > 0$, $f_{xx} = 12 > 0$ — LOCAL MIN, "
                "$f = -1$.\n"
                "• $(-1, -1)$: $D = 128 > 0$, $f_{xx} = 12 > 0$ — LOCAL "
                "MIN, $f = -1$.\n\n"
                "Two equal minima flanking a saddle — the 'egg-carton "
                "corner' shape. Note the case discipline in Step 1: solving "
                "$x^9 = x$ by dividing by $x$ would have silently discarded "
                "$(0, 0)$, the most interesting point on the list.\n\n"
                "EXAMPLE 2 (cross-terms only). Classify the critical point "
                "of $f(x, y) = xy$.\n\n"
                "$f_x = y = 0$, $f_y = x = 0$: origin. $f_{xx} = 0$, "
                "$f_{yy} = 0$, $f_{xy} = 1$: $D = 0 \\cdot 0 - 1 = -1 < 0$: "
                "SADDLE — even though BOTH pure curvatures vanish. The "
                "cross term alone can carry a saddle ($f = xy$ is "
                "$\\frac{u^2 - v^2}{2}$ in rotated coordinates $u = "
                "\\frac{x+y}{\\sqrt2}$, $v = \\frac{x-y}{\\sqrt2}$ — the "
                "canonical saddle in disguise). Never classify from "
                "$f_{xx}$ and $f_{yy}$ alone; page 12 returns to this trap."
            )),
            # ---------------------------------------------------------- 8
            (L, "Strategy: the local protocol and the closed-region algorithm", (
                "LOCAL CLASSIFICATION PROTOCOL. (1) Compute $\\nabla f$; "
                "solve $f_x = 0 = f_y$ with full case analysis — FACTOR, "
                "never divide by an expression that might vanish; collect "
                "ALL real solutions, plus any points where partials fail. "
                "(2) Compute the three second partials once, symbolically. "
                "(3) Evaluate $D$ and $f_{xx}$ AT EACH critical point — a "
                "table with columns $(a, b)$, $f_{xx}$, $D$, verdict, "
                "$f(a,b)$. (4) For any $D = 0$ row, switch tools: inspect "
                "$f$ directly along lines and curves through the point "
                "(page 10's craft).\n\n"
                "ABSOLUTE EXTREMA ON A CLOSED BOUNDED REGION $R$ (the "
                "closed-interval method, one dimension up; EVT guarantees "
                "success). The absolute max and min exist and live either "
                "in the interior (at a critical point) or on the boundary. "
                "Algorithm:\n"
                "1. INTERIOR: find critical points inside $R$; record "
                "$f$-values. (No classification needed — values suffice for "
                "an absolute-extrema hunt.)\n"
                "2. BOUNDARY: reduce to one variable. Parameterize each "
                "boundary piece — edges of a polygon by a line segment, a "
                "circle by $(\\cos t, \\sin t)$ — substitute into $f$, and "
                "optimize the resulting single-variable function on its "
                "parameter interval (C113 methods: its critical points AND "
                "its endpoints).\n"
                "3. CORNERS: where boundary pieces meet, evaluate $f$ "
                "directly (corners are the boundary-curves' endpoints and "
                "are easy to forget).\n"
                "4. COMPARE the full candidate list; largest value is the "
                "absolute max, smallest the absolute min.\n\n"
                "Boundary work is usually the bulk of the labor: an "
                "$n$-sided region means $n$ one-variable problems. Budget "
                "accordingly, label each candidate with its provenance, and "
                "resist skipping step 3 — exam answer keys are full of "
                "corner maxima."
            )),
            # ---------------------------------------------------------- 9
            (E, "Worked example: absolute extrema on a rectangle", (
                "Find the absolute max and min of $f(x, y) = x^2 - 2xy + "
                "2y$ on the rectangle $R = \\{0 \\le x \\le 3,\\; 0 \\le y "
                "\\le 2\\}$.\n\n"
                "STEP 1 — interior critical points: $f_x = 2x - 2y = 0$, "
                "$f_y = -2x + 2 = 0$ give $x = 1$, then $y = 1$: the point "
                "$(1, 1)$, inside $R$, with $f(1, 1) = 1 - 2 + 2 = 1$.\n\n"
                "STEP 2 — the four edges, each a one-variable problem:\n"
                "• Bottom ($y = 0$, $0 \\le x \\le 3$): $f = x^2$, "
                "increasing; extremes at ends: $f(0,0) = 0$, "
                "$f(3,0) = 9$.\n"
                "• Right ($x = 3$, $0 \\le y \\le 2$): $f = 9 - 6y + 2y = "
                "9 - 4y$, decreasing: from $f(3,0) = 9$ down to "
                "$f(3,2) = 1$.\n"
                "• Top ($y = 2$, $0 \\le x \\le 3$): $f = x^2 - 4x + 4 = "
                "(x - 2)^2$: interior minimum $f(2,2) = 0$ at the "
                "one-variable critical point $x = 2$; ends $f(0,2) = 4$, "
                "$f(3,2) = 1$.\n"
                "• Left ($x = 0$, $0 \\le y \\le 2$): $f = 2y$, increasing: "
                "$f(0,0) = 0$ to $f(0,2) = 4$.\n\n"
                "STEP 3 — corners: $(0,0), (3,0), (3,2), (0,2)$ — already "
                "collected above ($0, 9, 1, 4$).\n\n"
                "STEP 4 — compare all candidates: interior $1$; boundary "
                "$0, 9, 1, 0, 4$ (with the top edge's interior critical "
                "point contributing a genuine candidate, $f(2,2) = 0$).\n\n"
                "VERDICT: absolute maximum $f = 9$ at $(3, 0)$; absolute "
                "minimum $f = 0$, attained at BOTH $(0, 0)$ and $(2, 2)$.\n\n"
                "Observations worth internalizing: the interior critical "
                "point won nothing (it is a saddle — check: $D = "
                "(2)(0) - (-2)^2 = -4 < 0$); the max sat on a corner; the "
                "min was shared by a corner and an edge-interior point. All "
                "four candidate species showed up. That is typical, not "
                "contrived."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: the D = 0 menagerie", (
                "When $D = 0$ the quadratic shadow of page 6 degenerates — "
                "flat along at least one direction — and the verdict passes "
                "to higher-order terms, which the test never sees. 'The "
                "test is inconclusive' is a theorem, not an excuse, and "
                "here is the proof: three functions, identical test data, "
                "three different fates. All three have $\\nabla f = "
                "\\mathbf{0}$ at the origin and $f_{xx} = f_{yy} = f_{xy} = "
                "0$ there (each is quartic), hence $D = 0$:\n\n"
                "• $f = x^4 + y^4$: a MINIMUM — $f > 0$ everywhere off the "
                "origin. (A genuine bowl, just flatter than any paraboloid "
                "at its base.)\n"
                "• $f = -x^4 - y^4$: a MAXIMUM, by the same reasoning "
                "upside down.\n"
                "• $f = x^4 - y^4$: NEITHER — positive along the $x$-axis, "
                "negative along the $y$-axis: saddle-like behavior the "
                "discriminant scored as 0, not negative.\n\n"
                "So $D = 0$ genuinely spans min, max, and saddle. Two more "
                "specimens complete the field guide:\n\n"
                "• DEGENERATE VALLEYS: $f = (x - y)^2$. Here $f_{xx} = "
                "f_{yy} = 2$, $f_{xy} = -2$, so $D = 4 - 4 = 0$ — at EVERY "
                "point of the line $y = x$, each of which is a critical "
                "point. The surface is a parabolic trough: a whole LINE of "
                "(non-strict) minima, value 0. Critical points need not be "
                "isolated, and non-strict extrema are exactly what $D = 0$ "
                "often signals.\n\n"
                "• THE MONKEY SADDLE: $f = x^3 - 3xy^2$. Critical at the "
                "origin; $f_{xx} = 6x$, $f_{yy} = -6x$, $f_{xy} = -6y$ all "
                "vanish there: $D = 0$. Along rays at angles $\\theta$, $f "
                "= r^3\\cos 3\\theta$: three uphill sectors alternating "
                "with three downhill — a pass for a rider with two legs AND "
                "a tail. No quadratic surface behaves this way, which is "
                "precisely why the quadratic-based test could not see it.\n\n"
                "WHAT TO DO at $D = 0$: inspect $f$ directly — restrict to "
                "lines and curves through the point, hunt for sign changes "
                "of $f - f(a,b)$, or factor. Page 11 drills the craft."
            )),
            # ---------------------------------------------------------- 11
            (E, "Worked example: classifying when the test is silent", (
                "PROBLEM. Classify the origin for (a) $f = x^4 + y^4$, "
                "(b) $g = x^2y^2$, (c) $h = x^3 + y^3$.\n\n"
                "(a) Test data: all second partials vanish at $\\mathbf{0}$ "
                "(quartic), $D = 0$: silent. Direct inspection: "
                "$f(0,0) = 0$ and $f(x, y) = x^4 + y^4 > 0$ for every "
                "$(x, y) \\ne (0, 0)$. That single inequality IS a proof of "
                "a STRICT ABSOLUTE MINIMUM — stronger than anything the "
                "test ever certifies. Inspection is not a fallback; "
                "sometimes it is the superior instrument.\n\n"
                "(b) $g = x^2y^2$: again every second partial is 0 at the "
                "origin ($g_{xx} = 2y^2$, $g_{yy} = 2x^2$, $g_{xy} = 4xy$), "
                "$D = 0$. Inspection: $g \\ge 0$ everywhere, with $g = 0$ "
                "exactly on BOTH axes. So the origin is a NON-STRICT local "
                "minimum sitting inside a cross-shaped flat set — two whole "
                "lines of minima through one point. (Notice $g$'s critical "
                "set: $g_x = 2xy^2 = 0$ and $g_y = 2x^2y = 0$ hold on the "
                "axes — infinitely many critical points, all sharing the "
                "value 0.)\n\n"
                "(c) $h = x^3 + y^3$: second partials $h_{xx} = 6x$, "
                "$h_{yy} = 6y$, $h_{xy} = 0$ all vanish at the origin, "
                "$D = 0$. Inspection along the $x$-axis: $h(x, 0) = x^3$, "
                "which is positive for $x > 0$ and negative for $x < 0$ — "
                "$h - h(0,0)$ changes sign in every neighborhood, so the "
                "origin is NEITHER a max nor a min (saddle-type, though "
                "flatter than the classic pass).\n\n"
                "THE CRAFT, distilled: at a silent test, (1) try to bound "
                "$f - f(a, b)$ in sign globally near the point (as in (a), "
                "(b)); (2) failing that, hunt for a sign CHANGE along some "
                "line or curve (as in (c) — one line sufficed); (3) report "
                "the evidence, not just the verdict. Sign information is "
                "the whole game once derivatives abstain."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: '$D < 0$ works like $f'' < 0$ — it means "
                "a maximum.' The single most common error in the chapter, "
                "imported from C114 muscle memory. $D$ is NOT a curvature; "
                "it is a discriminant. $D < 0$ means SADDLE, always. The "
                "max/min decision is made by $f_{xx}$, and only after "
                "$D > 0$ has certified that a decision exists.\n\n"
                "MISCONCEPTION 2: '$f_{xx} > 0$ and $f_{yy} > 0$, so it's a "
                "minimum.' The cross term can overrule both. "
                "$f = x^2 + 4xy + y^2$ has $f_{xx} = f_{yy} = 2 > 0$ at the "
                "origin, but $f_{xy} = 4$ gives $D = 4 - 16 = -12 < 0$: "
                "SADDLE (indeed $f(t, -t) = -2t^2$ dives downhill along "
                "the anti-diagonal). Two agreeing pure curvatures prove "
                "nothing until $f_{xy}^2$ has been paid.\n\n"
                "MISCONCEPTION 3: 'Every critical point is a max or a "
                "min.' Saddles are not exotic — in Example 1 of page 7, "
                "one of three critical points was a saddle, and in high "
                "dimensions saddles overwhelmingly outnumber extrema "
                "(page 17). 'Critical' means CANDIDATE.\n\n"
                "MISCONCEPTION 4: '$D = 0$ means saddle' (or 'means "
                "degenerate = ignore it'). $D = 0$ means NO INFORMATION "
                "from this test: page 10's menagerie realizes min, max, "
                "AND neither, all with $D = 0$. The point still must be "
                "classified — by inspection.\n\n"
                "MISCONCEPTION 5: 'The local test settles absolute "
                "extrema.' A local min can sit far above the region's true "
                "minimum, which may live on the boundary (page 9: the "
                "interior critical point lost to a corner). Absolute "
                "questions require the full closed-region algorithm — "
                "interior AND boundary AND corners — every time the region "
                "is bounded, and extra care (existence is not even "
                "guaranteed) when it is not."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational errors", (
                "ERROR 1: LOSING SOLUTIONS by dividing. From $4x^3 - 4y = "
                "0$ and $4y^3 - 4x = 0$, substituting gives $x^9 = x$; "
                "dividing by $x$ discards $x = 0$ and with it the saddle "
                "at the origin (page 7). Always FACTOR: $x(x^8 - 1) = 0$ "
                "keeps every case. The same slip in systems like "
                "$xy = 0$: treat each factor's case separately, "
                "completely.\n\n"
                "ERROR 2: Forgetting to SQUARE $f_{xy}$. $D = f_{xx}f_{yy} "
                "- f_{xy}^2$, not $- f_{xy}$. With $f_{xy} = -4$ the "
                "unsquared version even gets the sign wrong twice. Write "
                "the formula, then substitute.\n\n"
                "ERROR 3: Evaluating $D$ SYMBOLICALLY but classifying "
                "GLOBALLY. $D(x, y) = 144x^2y^2 - 16$ is a function; the "
                "test wants its VALUE at each critical point, one row per "
                "point. A single 'D is positive' verdict smeared across "
                "three critical points is meaningless (and was wrong for "
                "one of them on page 7).\n\n"
                "ERROR 4: Boundary work on the WRONG interval, or "
                "unparameterized. Substituting $y = 2$ must come with "
                "'$0 \\le x \\le 3$' — the one-variable problem inherits "
                "the segment, and its ENDPOINTS are candidates too. For "
                "circles, forgetting that $t \\in [0, 2\\pi]$ wraps (the "
                "endpoints coincide) creates phantom candidates or misses "
                "real ones.\n\n"
                "ERROR 5: Omitting CORNERS. Each corner belongs to two "
                "edges but is an endpoint of both parameterizations; "
                "evaluate $f$ there explicitly. Page 9's absolute max "
                "lived on a corner — routinely, not exceptionally.\n\n"
                "ERROR 6: Classifying when only VALUES are needed. In the "
                "closed-region algorithm, running the $D$-test on interior "
                "critical points is wasted work — absolute extrema are "
                "decided by comparing $f$-values across the candidate "
                "list. Know which question you are answering; the two "
                "protocols share ingredients but not steps."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: optimization is the family business", (
                "1. LEAST SQUARES — the most-run optimization on Earth. To "
                "fit a line $y = mx + b$ to data points, minimize the sum "
                "of squared residuals $E(m, b) = \\sum_i (mx_i + b - "
                "y_i)^2$: two variables, one smooth function. Setting "
                "$E_m = E_b = 0$ yields the NORMAL EQUATIONS — linear, "
                "solvable in closed form — and the Hessian "
                "$D = 4\\left(n\\sum x_i^2 - (\\sum x_i)^2\\right) > 0$ "
                "(Cauchy-Schwarz) with $E_{bb} = 2n > 0$ certifies a "
                "genuine minimum. Every regression, every trendline, is a "
                "C308 critical-point computation (LA.U8.N1's projection "
                "view of the same problem lives in linear algebra).\n\n"
                "2. DESIGN AND ECONOMICS. Minimal-material packaging "
                "(page 15), maximal-profit output mixes $\\pi(q_1, q_2)$, "
                "optimal pricing with substitutes — the daily bread of "
                "operations research. The test matters practically: "
                "shipping a SADDLE point as an 'optimal design' means a "
                "competitor's perturbation beats you in some direction.\n\n"
                "3. PHYSICS: equilibria and stability. A particle in "
                "potential $V(x, y)$ rests where $\\nabla V = \\mathbf{0}$; "
                "the Hessian decides FATE: minimum = stable (restoring "
                "forces all around), maximum or saddle = unstable (some "
                "escape direction). ODE.U7.N2 recovers exactly this from "
                "the linearized dynamics' eigenvalues — same matrix, same "
                "verdict, different vocabulary.\n\n"
                "4. MACHINE LEARNING'S LANDSCAPE. Training minimizes a "
                "loss over millions of parameters. C307's gradient descent "
                "supplies the direction; THIS chapter explains the "
                "obstacles: the flat points where descent stalls. A modern "
                "insight (page 17) is that in very high dimensions almost "
                "all critical points are saddles, not bad minima — which "
                "is, perhaps surprisingly, good news: saddles have escape "
                "directions, and noise finds them."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: the cheapest open-top box", (
                "PROBLEM. An open-top rectangular box must hold 32 cubic "
                "feet. Material costs the same per square foot everywhere. "
                "What dimensions minimize the material (base plus four "
                "sides, no top)?\n\n"
                "STEP 1 — model. Base $x \\times y$, height $z$; "
                "constraint $xyz = 32$. Surface area $S = xy + 2xz + 2yz$. "
                "Eliminate $z = \\frac{32}{xy}$:\n"
                "$$S(x, y) = xy + \\frac{64}{y} + \\frac{64}{x}, \\qquad "
                "x > 0,\\; y > 0.$$\n\n"
                "STEP 2 — critical points: $S_x = y - \\frac{64}{x^2} = 0$ "
                "and $S_y = x - \\frac{64}{y^2} = 0$, so $y = "
                "\\frac{64}{x^2}$ and $x = \\frac{64}{y^2}$. Substitute: "
                "$x = 64 \\cdot \\frac{x^4}{64^2} = \\frac{x^4}{64}$, so "
                "$x^3 = 64$ (we may divide by $x$ here — the domain "
                "excludes $x = 0$), giving $x = 4$, then $y = 4$, "
                "$z = \\frac{32}{16} = 2$.\n\n"
                "STEP 3 — classify: $S_{xx} = \\frac{128}{x^3}$, "
                "$S_{yy} = \\frac{128}{y^3}$, $S_{xy} = 1$. At $(4, 4)$: "
                "$S_{xx} = 2$, $S_{yy} = 2$, $S_{xy} = 1$: $D = 4 - 1 = 3 "
                "> 0$ and $S_{xx} > 0$ — LOCAL MINIMUM, with $S = 16 + 16 "
                "+ 16 = 48$ ft².\n\n"
                "STEP 4 — is it absolute? The domain is open and "
                "unbounded, so EVT does not apply; argue the frontier "
                "instead: as $x \\to 0^+$ or $y \\to 0^+$, the $\\frac{64}"
                "{x}$ or $\\frac{64}{y}$ term blows up; as $x \\to \\infty$ "
                "or $y \\to \\infty$ (with the other fixed), $xy \\to "
                "\\infty$. $S \\to \\infty$ in every escape direction, so "
                "the lone interior minimum is absolute.\n\n"
                "ANSWER: $4 \\times 4 \\times 2$ feet, 48 ft² of material. "
                "Note the shape logic: square base (symmetry), height "
                "HALF the base side — the missing top makes tallness "
                "relatively expensive. The mathematics rediscovered a "
                "packaging engineer's rule of thumb."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: where this chapter plugs into everything else", (
                "BACKWARD. C304 built the second partials and Clairaut's "
                "guarantee that three of them suffice. C305's tangent "
                "plane goes horizontal exactly at critical points, and its "
                "quadratic error term became page 6's classifying form — "
                "via C306's second-derivative chain rule computation. "
                "C307's steepest-ascent theorem is WHY extrema hide at "
                "$\\nabla f = \\mathbf{0}$. And the whole architecture — "
                "candidates by Fermat, classification by second "
                "derivatives, absolute extrema by candidate comparison — "
                "is C113/C114 rebuilt one dimension up.\n\n"
                "FORWARD, immediately:\n"
                "• C309 (Lagrange multipliers): page 8's boundary step "
                "parameterized the constraint; Lagrange handles constraints "
                "that resist parameterization by solving $\\nabla f = "
                "\\lambda \\nabla g$ instead. The two methods are "
                "interchangeable on many problems — learn both, choose per "
                "problem.\n"
                "• C310 (double integrals): optimization and integration "
                "are the two things one DOES with $f(x, y)$; regions and "
                "their boundaries, met here, become domains of "
                "integration there.\n\n"
                "FURTHER OUT:\n"
                "• LA.U7.N1 (eigenvalues): page 17 reveals $D$ as the "
                "determinant of the Hessian and the test as an eigenvalue "
                "sign census — the honest, $n$-dimensional form of "
                "everything here.\n"
                "• ODE.U7.N2 (stability): classifying equilibria of "
                "$\\mathbf{x}' = -\\nabla V$ IS this chapter's "
                "classification, read through the same matrix.\n"
                "• LA.U8.N1 / least squares: page 14's normal equations; "
                "regression as a guaranteed-convex C308 problem.\n"
                "• Numerical optimization: Newton's method for optima "
                "solves $\\nabla f = \\mathbf{0}$ with the Hessian as its "
                "engine; quasi-Newton methods spend their cleverness "
                "approximating it. The Hessian is a working tool, not an "
                "exam artifact."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: the Hessian's eigenvalues tell the truth", (
                "Assemble the second partials into the HESSIAN MATRIX\n"
                "$$H = \\begin{pmatrix} f_{xx} & f_{xy} \\\\ f_{xy} & "
                "f_{yy} \\end{pmatrix},$$\n"
                "symmetric by Clairaut. Then $D = \\det H$ — the "
                "discriminant was a determinant all along. A symmetric "
                "matrix has real eigenvalues $\\lambda_1, \\lambda_2$ "
                "(LA.U7.N1) with perpendicular eigenvectors, and in the "
                "eigenvector coordinates the quadratic form of page 6 "
                "diagonalizes: $Q = \\lambda_1 u^2 + \\lambda_2 v^2$. The "
                "eigenvalues ARE the principal curvatures' signs; the "
                "eigenvectors are the axes of purest bending.\n\n"
                "Now the test decodes itself. $\\det H = "
                "\\lambda_1\\lambda_2$ and $f_{xx}$ tracks the shared sign "
                "when there is one:\n"
                "• $D > 0$: eigenvalues share a sign — both positive "
                "(bowl, min) or both negative (dome, max); $f_{xx}$ "
                "reports which.\n"
                "• $D < 0$: opposite signs — up along one eigenvector, "
                "down along the other: the saddle, with its two special "
                "directions now identified as eigenvectors.\n"
                "• $D = 0$: some eigenvalue is ZERO — the form is flat "
                "along that eigenvector, and higher-order terms inherit "
                "the decision. 'Degenerate critical point' means exactly "
                "'singular Hessian.'\n\n"
                "This is the version that SCALES. In $n$ variables the "
                "test reads: all eigenvalues positive — min; all negative "
                "— max; mixed signs — saddle; any zero — degenerate. (The "
                "two-variable $D$-and-$f_{xx}$ recipe is just a "
                "determinant-and-corner shortcut for the $2 \\times 2$ "
                "census.) Morse theory builds on the census: the INDEX — "
                "how many eigenvalues are negative — is a topological "
                "fingerprint linking a landscape's passes and peaks to the "
                "shape of the terrain itself.\n\n"
                "One caution from the frontier: in one dimension, 'min "
                "along every line through the point' would mean minimum; "
                "in two, it does not. Peano's example $f = (y - x^2)"
                "(y - 3x^2)$ has a local min at $\\mathbf{0}$ along EVERY "
                "straight line, yet $f < 0$ on the parabola $y = 2x^2$ "
                "arbitrarily close by. Lines, once again, are not enough — "
                "the same moral as C305 and C307, now in the second "
                "order."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "CRITICAL POINTS: where $\\nabla f = \\mathbf{0}$ or a "
                "partial fails to exist. Interior local extrema occur ONLY "
                "there (Fermat, via C307: a nonzero gradient always offers "
                "an uphill and a downhill).\n\n"
                "SECOND DERIVATIVE TEST (continuous second partials; "
                "evaluate everything AT the critical point):\n"
                "$$D = f_{xx}f_{yy} - f_{xy}^2.$$\n"
                "• $D > 0$, $f_{xx} > 0$: local min.\n"
                "• $D > 0$, $f_{xx} < 0$: local max.\n"
                "• $D < 0$: saddle.\n"
                "• $D = 0$: silent — classify by inspection ($x^4 + y^4$ "
                "min; $-x^4 - y^4$ max; $x^4 - y^4$, $x^3 + y^3$ neither; "
                "$(x-y)^2$ a line of non-strict minima).\n"
                "Shortcuts: $f_{xx}$, $f_{yy}$ of opposite signs "
                "$\\Rightarrow$ saddle instantly; both same sign proves "
                "NOTHING until $f_{xy}^2$ is subtracted.\n\n"
                "MATRIX VIEW: $D = \\det H$, $H$ the symmetric Hessian; "
                "test = sign census of eigenvalues (all $+$: min; all "
                "$-$: max; mixed: saddle; zero: degenerate).\n\n"
                "ABSOLUTE EXTREMA ON CLOSED BOUNDED $R$ (EVT guarantees "
                "both exist): (1) interior critical points — values only; "
                "(2) each boundary piece parameterized to a one-variable "
                "problem (its critical points AND endpoints); (3) corners; "
                "(4) compare all candidates. Unbounded/open domains: no "
                "EVT — argue behavior at the frontier (page 15's "
                "pattern).\n\n"
                "WORKFLOW. Solve the gradient system by FACTORING (no "
                "dividing by maybe-zero); table the critical points; "
                "evaluate $D$, $f_{xx}$, $f$ per row; verdicts; $D = 0$ "
                "rows go to inspection. For absolute questions, switch "
                "protocols — candidates and values, no classification "
                "needed."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all eight before reading the solutions.\n\n"
                "1. Find and classify the critical point of $f = x^2 + "
                "y^2 - 4x + 6y$.\n"
                "2. Classify $(0, 0)$ for $f = x^4 + y^4 - 4xy + 1$.\n"
                "3. Find and classify ALL critical points of $f = x^3 - "
                "3x + y^2 - 4y$.\n"
                "4. The origin for $f = x^4 + y^4$: what does the test "
                "say, and what is the truth?\n"
                "5. Both $f_{xx} > 0$ and $f_{yy} > 0$ at a critical "
                "point: minimum? Prove or refute.\n"
                "6. Absolute extrema of $f = x^2 - 2x + y^2$ on the disk "
                "$x^2 + y^2 \\le 4$.\n"
                "7. The Hessian at a critical point has eigenvalues $2$ "
                "and $-3$: classify, and find $D$.\n"
                "8. True or false: $D > 0$ with $f_{xx} < 0$ is a local "
                "minimum.\n\n"
                "SOLUTIONS.\n"
                "1. $\\nabla f = \\mathbf{0}$ at $(2, -3)$; $D = (2)(2) - "
                "0 = 4 > 0$, $f_{xx} = 2 > 0$: local min, $f = -13$.\n"
                "2. $f_{xx}(0,0) = 0$, $f_{yy}(0,0) = 0$, $f_{xy} = -4$: "
                "$D = 0 - 16 = -16 < 0$: SADDLE.\n"
                "3. $f_x = 3x^2 - 3 = 0 \\Rightarrow x = \\pm 1$; "
                "$f_y = 2y - 4 = 0 \\Rightarrow y = 2$. At $(1, 2)$: "
                "$D = 12 > 0$, $f_{xx} = 6 > 0$: local min, $f = -6$. At "
                "$(-1, 2)$: $D = -12 < 0$: saddle.\n"
                "4. All second partials vanish: $D = 0$, test silent. "
                "Truth: $f > 0$ off the origin — strict absolute minimum, "
                "by inspection.\n"
                "5. Refuted: $f = x^2 + 4xy + y^2$ has $f_{xx} = f_{yy} = "
                "2 > 0$ but $D = 4 - 16 < 0$: saddle ($f(t, -t) = "
                "-2t^2$).\n"
                "6. Interior: critical point $(1, 0)$, $f = -1$. Boundary "
                "($x^2 + y^2 = 4$): $f = 4 - 2x$, $x \\in [-2, 2]$, "
                "ranging from $0$ at $(2, 0)$ to $8$ at $(-2, 0)$. "
                "Absolute min $-1$ at $(1, 0)$; absolute max $8$ at "
                "$(-2, 0)$.\n"
                "7. Mixed signs: saddle; $D = \\det H = (2)(-3) = -6$.\n"
                "8. FALSE — local MAXIMUM ($D > 0$ certifies an extremum; "
                "negative $f_{xx}$ bends downward).\n\n"
                "Scoring: misses on 2 or 5 → pages 7 and 12; on 4 → pages "
                "10-11; on 6 → pages 8-9; on 7 → page 17."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Before leaving this chapter — and with it the "
                "differential-calculus arc C304-C308 — you should be able "
                "to check every box honestly:\n\n"
                "□ Explain, via C307's gradient, why interior extrema "
                "force $\\nabla f = \\mathbf{0}$, and why points with "
                "undefined partials also count as critical.\n"
                "□ Solve gradient systems with full case discipline — "
                "factoring, never dividing away solutions.\n"
                "□ State the four-outcome second derivative test and run "
                "it from a tidy table: point, $f_{xx}$, $D$, verdict, "
                "value.\n"
                "□ Produce the instant-saddle shortcut (opposite-sign "
                "pure curvatures) and the counterexample killing "
                "'$f_{xx}, f_{yy} > 0 \\Rightarrow$ min' "
                "($x^2 + 4xy + y^2$).\n"
                "□ Derive $D$ by completing the square on the quadratic "
                "form, and say what each case means geometrically.\n"
                "□ Exhibit the $D = 0$ menagerie — min, max, and neither, "
                "all with $D = 0$ — and classify silent cases by sign "
                "inspection.\n"
                "□ Run the closed-region algorithm end to end: interior "
                "values, each boundary piece parameterized and optimized "
                "with its endpoints, corners, final comparison.\n"
                "□ Handle unbounded domains by frontier arguments (page "
                "15) instead of a misapplied EVT.\n"
                "□ Translate the test into Hessian language: $D = \\det "
                "H$, classification as an eigenvalue sign census, "
                "$D = 0$ as a singular Hessian.\n"
                "□ Connect forward: constraints resist parameterization "
                "→ C309's Lagrange multipliers; stability of equilibria → "
                "ODE.U7.N2; regression → page 14's least squares.\n\n"
                "When every box is checked, hit Practice until the "
                "verdicts come without hesitation — then proceed to C309, "
                "where the boundary step grows into a method of its own."
            )),
        ],
    },
}
