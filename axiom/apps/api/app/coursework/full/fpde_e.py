"""Full course: fpde_e — Orthogonality, characteristics, transport, Green's functions.

Four FC-template lessons (20 full pages each) for the Fourier/PDE sequence:
  PF.U7.N3  Orthogonality and eigenfunction expansions
  PF.U8.N1  Characteristic lines
  PF.U8.N2  The transport equation
  PF.U9.N1  Green's functions

Matches the gold standard calc1_a_limits.py in depth, structure, and voice.
"""

L = "reading"
E = "example"
P = "pitfall"
K = "check"

LESSONS: dict[str, dict] = {
    "PF.U7.N3": {
        "summary": (
            "A complete course on orthogonality and eigenfunction expansions: why "
            "Sturm-Liouville eigenfunctions of distinct eigenvalues are orthogonal, "
            "how projection gives expansion coefficients, Fourier sine/cosine series as "
            "the classical case, Bessel/Legendre/quantum expansions as the same geometry, "
            "and how orthogonality completes a separation-of-variables solution."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "You have just learned separation of variables: a PDE like the heat "
                "equation splits into an eigenvalue problem in space plus a simple decay "
                "in time, and the spatial part produces a family of building-block shapes "
                "$X_1(x), X_2(x), X_3(x), \\ldots$ (for the vibrating string, the shapes "
                "$\\sin\\frac{n\\pi x}{L}$). But separation leaves one giant question "
                "unanswered: your actual initial condition $f(x)$ is almost never a single "
                "one of those shapes. It is some arbitrary temperature profile. How do you "
                "write $f$ as a combination of the building blocks so each block can evolve "
                "on its own?\n\n"
                "That is THIS chapter. The answer is ORTHOGONALITY: the eigenfunctions "
                "behave like perpendicular coordinate axes in an infinite-dimensional "
                "space of functions, and writing $f$ in terms of them is exactly like "
                "resolving a vector into components along perpendicular axes. Each "
                "coefficient is found by a projection — an integral — that is deaf to every "
                "building block except its own.\n\n"
                "This single idea is the engine behind Fourier series, and behind their "
                "cousins: Bessel-function expansions on a drum, Legendre expansions on a "
                "sphere, the eigenstate expansions of quantum mechanics. They look like "
                "different subjects, but they are one piece of geometry seen in different "
                "coordinate systems.\n\n"
                "In this course-chapter you will learn: the precise sense in which "
                "eigenfunctions are orthogonal; the PROOF that distinct eigenvalues force "
                "orthogonality, straight from the symmetry of the differential operator; "
                "the projection formula $c_n = \\frac{\\int f X_n\\,dx}{\\int X_n^2\\,dx}$ "
                "that computes every coefficient; and how all of this snaps the final piece "
                "into a separation-of-variables solution. By the end, 'expand $f$ in "
                "eigenfunctions' will feel as routine as 'find the components of a vector.'"
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Four things carry this chapter; all are reviewable in an afternoon.\n\n"
                "1. INTEGRATION, especially $\\int \\sin\\frac{m\\pi x}{L}\\sin\\frac{n\\pi "
                "x}{L}\\,dx$ via product-to-sum identities. You must be comfortable turning "
                "a product of trig functions into a sum: "
                "$\\sin A\\sin B = \\tfrac12[\\cos(A-B)-\\cos(A+B)]$. Integration by parts "
                "will also appear in the central proof.\n\n"
                "2. THE STURM-LIOUVILLE PROBLEM (PF.U7.N2). You should recognize the "
                "self-adjoint form $\\frac{d}{dx}\\!\\left(p(x)\\frac{dX}{dx}\\right) + "
                "q(x)X + \\lambda w(x)X = 0$ on $[a,b]$ with boundary conditions, and know "
                "that it produces real eigenvalues $\\lambda_n$ and eigenfunctions "
                "$X_n(x)$.\n\n"
                "3. VECTOR PROJECTION AND INNER PRODUCTS (LA.U8.N2). Recall that in "
                "$\\mathbb{R}^n$ the component of $\\vec v$ along a unit vector $\\hat e$ is "
                "$\\vec v\\cdot\\hat e$, and along a non-unit $\\vec e$ it is "
                "$\\frac{\\vec v\\cdot\\vec e}{\\vec e\\cdot\\vec e}$. Every formula in this "
                "chapter is that formula with the dot product replaced by an integral.\n\n"
                "4. FOURIER SERIES BASICS (PF.U4.N2). Prior exposure to "
                "$f(x)\\sim\\sum b_n\\sin\\frac{n\\pi x}{L}$ helps, though we rebuild it "
                "here from the orthogonality viewpoint.\n\n"
                "Quick self-check (answers follow):\n\n"
                "• Evaluate $\\int_0^\\pi \\sin x\\,\\sin 2x\\,dx$. (It is $0$; the two "
                "modes are orthogonal.)\n"
                "• What is the component of $\\vec v = (3,4)$ along $\\vec e = (1,0)$? "
                "($3$ — just read off the matching coordinate.)\n"
                "• What functional equation defines an eigenfunction $X_n$ of an operator "
                "$\\mathcal{L}$? ($\\mathcal{L}[X_n] = \\lambda_n X_n$ — the operator only "
                "rescales it.)\n\n"
                "If projection or the SL form felt shaky, revisit LA.U8.N2 and PF.U7.N2 "
                "first; the payoff is immediate once those click."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Picture ordinary 3D space. To describe a vector $\\vec v$ you pick three "
                "PERPENDICULAR axes $\\hat x,\\hat y,\\hat z$ and write "
                "$\\vec v = v_1\\hat x + v_2\\hat y + v_3\\hat z$. The magic of perpendicular "
                "axes is that each component is found INDEPENDENTLY: $v_1 = \\vec v\\cdot\\hat "
                "x$ ignores $\\hat y$ and $\\hat z$ completely, because $\\hat x\\cdot\\hat y = "
                "\\hat x\\cdot\\hat z = 0$. You never have to solve a tangled system; each "
                "axis reports its own share.\n\n"
                "Now make the leap. A FUNCTION $f(x)$ on $[a,b]$ is a 'vector' too — an "
                "infinite-dimensional one, its 'coordinates' being its values at every point. "
                "The dot product becomes an integral:\n"
                "$$\\langle f, g\\rangle = \\int_a^b f(x)\\,g(x)\\,w(x)\\,dx,$$\n"
                "where $w(x)$ is a weight fixed by the problem (often $w=1$). Two functions "
                "are ORTHOGONAL when this integral is zero — the continuous analogue of "
                "'perpendicular.'\n\n"
                "Here is the payoff, promised now and proved next page: the eigenfunctions "
                "$X_1, X_2, \\ldots$ of a Sturm-Liouville problem are mutually orthogonal. "
                "They are the perpendicular axes of function space, handed to you for free by "
                "the differential equation. So to expand\n"
                "$$f(x) = c_1 X_1(x) + c_2 X_2(x) + c_3 X_3(x) + \\cdots,$$\n"
                "you find each $c_n$ the same way you found $v_1$: project $f$ onto $X_n$ and "
                "divide by the length-squared of $X_n$. Every other term drops out of the "
                "integral because $\\langle X_m, X_n\\rangle = 0$ for $m\\ne n$.\n\n"
                "That is the whole chapter in one image: eigenfunctions are perpendicular "
                "axes, and expansion is just reading off components. The rest is making this "
                "precise, proving the perpendicularity, and practicing the integrals."
            )),
            # ---------------------------------------------------------- 4
            (L, "The orthogonality theorem, formally", (
                "THEOREM (SL orthogonality). Let $X_m, X_n$ be eigenfunctions of the "
                "Sturm-Liouville operator $\\mathcal{L}[y] = -(p\\,y')' + q\\,y$ on $[a,b]$, "
                "with $\\mathcal{L}[X_k] = \\lambda_k\\,w\\,X_k$ and self-adjoint boundary "
                "conditions (Dirichlet, Neumann, Robin, or periodic). If $\\lambda_m \\ne "
                "\\lambda_n$, then\n"
                "$$\\int_a^b X_m(x)\\,X_n(x)\\,w(x)\\,dx = 0.$$\n\n"
                "PROOF, from the symmetry of $\\mathcal{L}$. Start with the two eigen-"
                "equations, multiply the first by $X_n$ and the second by $X_m$, and "
                "subtract:\n"
                "$$X_n\\mathcal{L}[X_m] - X_m\\mathcal{L}[X_n] = (\\lambda_m - "
                "\\lambda_n)\\,w\\,X_m X_n.$$\n"
                "Integrate both sides over $[a,b]$. The left side is handled by LAGRANGE'S "
                "IDENTITY: because $\\mathcal{L}[y] = -(py')'+qy$, the $q$ terms cancel in "
                "the difference, and integration by parts twice gives\n"
                "$$\\int_a^b \\big(X_n\\mathcal{L}[X_m] - X_m\\mathcal{L}[X_n]\\big)dx = "
                "-\\Big[\\,p\\,(X_m' X_n - X_m X_n')\\,\\Big]_a^b.$$\n"
                "This boundary term is exactly what the self-adjoint boundary conditions "
                "kill: at each endpoint the condition (e.g. $X=0$, or $X'=0$, or "
                "$X'+hX=0$) forces the bracket to vanish. So the entire left side is "
                "$0$.\n\n"
                "We are left with\n"
                "$$0 = (\\lambda_m - \\lambda_n)\\int_a^b X_m X_n\\,w\\,dx.$$\n"
                "Since the eigenvalues are DISTINCT, $\\lambda_m - \\lambda_n \\ne 0$, so we "
                "may divide it out, forcing $\\int_a^b X_m X_n\\,w\\,dx = 0$. QED.\n\n"
                "Read what the proof actually used: only that $\\mathcal{L}$ is symmetric "
                "(its 'transpose' equals itself once the boundary term dies). Orthogonality "
                "is not a coincidence of sines — it is a structural consequence of a "
                "self-adjoint operator, which is why the SAME conclusion holds for Bessel, "
                "Legendre, and quantum eigenfunctions. The specific functions change; the "
                "symmetry argument does not."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples: verifying orthogonality", (
                "EXAMPLE 1 (the classical sines). Show $X_1 = \\sin\\frac{\\pi x}{L}$ and "
                "$X_2 = \\sin\\frac{2\\pi x}{L}$ are orthogonal on $[0,L]$ with weight "
                "$w=1$. Use $\\sin A\\sin B = \\tfrac12[\\cos(A-B)-\\cos(A+B)]$ with "
                "$A=\\frac{\\pi x}{L}$, $B=\\frac{2\\pi x}{L}$:\n"
                "$$\\int_0^L \\sin\\tfrac{\\pi x}{L}\\sin\\tfrac{2\\pi x}{L}\\,dx = "
                "\\tfrac12\\int_0^L\\Big[\\cos\\tfrac{\\pi x}{L} - \\cos\\tfrac{3\\pi "
                "x}{L}\\Big]dx.$$\n"
                "Integrate: $\\tfrac12\\big[\\frac{L}{\\pi}\\sin\\frac{\\pi x}{L} - "
                "\\frac{L}{3\\pi}\\sin\\frac{3\\pi x}{L}\\big]_0^L$. At $x=L$ both sines are "
                "$\\sin\\pi=0$ and $\\sin 3\\pi=0$; at $x=0$ both are $0$. The result is "
                "$0$: orthogonal, as the theorem promised (their eigenvalues "
                "$(\\pi/L)^2\\ne(2\\pi/L)^2$ differ).\n\n"
                "EXAMPLE 2 (the norm — same integral, matching indices). Now $m=n$: "
                "$\\int_0^L \\sin^2\\frac{n\\pi x}{L}\\,dx$. Use $\\sin^2\\theta = "
                "\\tfrac12(1-\\cos 2\\theta)$:\n"
                "$$\\int_0^L \\tfrac12\\Big(1 - \\cos\\tfrac{2n\\pi x}{L}\\Big)dx = "
                "\\tfrac12\\Big[x - \\tfrac{L}{2n\\pi}\\sin\\tfrac{2n\\pi x}{L}\\Big]_0^L = "
                "\\tfrac{L}{2}.$$\n"
                "The sine term vanishes at both ends, leaving $\\frac{L}{2}$. So the "
                "'length-squared' of every sine mode is $\\frac{L}{2}$ — the number that "
                "will sit in the denominator of every coefficient.\n\n"
                "EXAMPLE 3 (cosines include a constant). On $[0,L]$ the Neumann eigen-"
                "functions are $1, \\cos\\frac{\\pi x}{L}, \\cos\\frac{2\\pi x}{L}, \\ldots$ "
                "Check $\\int_0^L 1\\cdot\\cos\\frac{\\pi x}{L}\\,dx = "
                "\\frac{L}{\\pi}\\sin\\frac{\\pi x}{L}\\big|_0^L = 0$: the constant mode "
                "($\\lambda=0$) is orthogonal to every cosine. Note the norm of the "
                "constant is $\\int_0^L 1^2\\,dx = L$, DIFFERENT from the $\\frac{L}{2}$ of "
                "the cosines — a difference that produces the famous factor of $\\tfrac12$ "
                "on the $a_0$ term of a Fourier cosine series."
            )),
            # ---------------------------------------------------------- 6
            (L, "The projection formula and its consequences", (
                "With orthogonality in hand, coefficient extraction is automatic. Suppose "
                "$f$ can be written as\n"
                "$$f(x) = \\sum_{k=1}^{\\infty} c_k X_k(x).$$\n"
                "To isolate $c_n$, take the weighted inner product of BOTH sides with $X_n$ "
                "— i.e. multiply by $X_n(x)w(x)$ and integrate:\n"
                "$$\\int_a^b f X_n\\,w\\,dx = \\sum_{k=1}^{\\infty} c_k \\int_a^b X_k "
                "X_n\\,w\\,dx.$$\n"
                "Orthogonality annihilates every term with $k\\ne n$; only $k=n$ survives, "
                "leaving $\\int_a^b f X_n w\\,dx = c_n \\int_a^b X_n^2\\,w\\,dx$. Solve:\n"
                "$$\\boxed{\\,c_n = \\dfrac{\\displaystyle\\int_a^b f(x)\\,X_n(x)\\,w(x)\\,dx}"
                "{\\displaystyle\\int_a^b X_n(x)^2\\,w(x)\\,dx}\\,}.$$\n"
                "This is the master formula of the chapter. Numerator: project $f$ onto the "
                "axis $X_n$. Denominator: the length-squared of that axis. It is literally "
                "$c_n = \\langle f, X_n\\rangle / \\langle X_n, X_n\\rangle$ — the vector "
                "projection formula, transplanted to function space.\n\n"
                "Three consequences worth internalizing. (1) DECOUPLING: every coefficient "
                "is computed by its own integral, with no reference to the others. An "
                "infinite system of unknowns collapses to a list of independent one-line "
                "computations. (2) BEST APPROXIMATION: truncating after $N$ terms gives the "
                "closest possible fit to $f$ (in the mean-square sense) using those $N$ "
                "modes — the same optimality that makes orthogonal projection the shortest "
                "path to a subspace. (3) PARSEVAL / ENERGY: "
                "$\\int_a^b f^2 w\\,dx = \\sum_k c_k^2 \\int_a^b X_k^2 w\\,dx$; the total "
                "'energy' of $f$ is the sum of the energies in each mode, because cross "
                "terms integrate to zero. COMPLETENESS (a theorem of SL theory) guarantees "
                "the expansion actually converges to $f$, so no energy is lost to modes you "
                "left out."
            )),
            # ---------------------------------------------------------- 7
            (E, "Standard worked examples: computing expansions", (
                "EXAMPLE 1 (Fourier sine expansion of a constant). Expand $f(x)=1$ on "
                "$[0,L]$ in $X_n=\\sin\\frac{n\\pi x}{L}$. Numerator: $\\int_0^L "
                "\\sin\\frac{n\\pi x}{L}\\,dx = \\frac{L}{n\\pi}\\big(1-\\cos n\\pi\\big) = "
                "\\frac{L}{n\\pi}\\big(1-(-1)^n\\big)$. Denominator: $\\frac{L}{2}$ "
                "(page 5). So\n"
                "$$c_n = \\frac{2}{n\\pi}\\big(1-(-1)^n\\big) = \\begin{cases}\\frac{4}{n\\pi} "
                "& n\\text{ odd}\\\\[2pt] 0 & n\\text{ even}.\\end{cases}$$\n"
                "Hence $1 = \\frac{4}{\\pi}\\big(\\sin\\frac{\\pi x}{L} + "
                "\\tfrac13\\sin\\frac{3\\pi x}{L} + \\tfrac15\\sin\\frac{5\\pi x}{L} + "
                "\\cdots\\big)$ on $(0,L)$ — the classic square-wave series, produced "
                "purely by projection.\n\n"
                "EXAMPLE 2 (Legendre expansion — same geometry, new axes). The Legendre "
                "polynomials $P_0=1$, $P_1=x$, $P_2=\\frac{3x^2-1}{2}$ are the SL "
                "eigenfunctions on $[-1,1]$ with weight $w=1$, and "
                "$\\int_{-1}^1 P_n^2\\,dx = \\frac{2}{2n+1}$. Expand $f(x)=x^2$. Only even "
                "polynomials contribute. Project onto $P_0$: "
                "$a_0 = \\frac{\\int_{-1}^1 x^2\\,dx}{\\int_{-1}^1 1\\,dx} = "
                "\\frac{2/3}{2} = \\frac13$. Project onto $P_2$: numerator "
                "$\\int_{-1}^1 x^2\\cdot\\frac{3x^2-1}{2}\\,dx = "
                "\\frac12\\big(\\frac{6}{5}-\\frac{2}{3}\\big) = \\frac{4}{15}$; divide by "
                "$\\frac{2}{5}$ to get $a_2 = \\frac{2}{3}$. So "
                "$x^2 = \\frac13 P_0 + \\frac23 P_2$. Verify: "
                "$\\frac13(1) + \\frac23\\cdot\\frac{3x^2-1}{2} = \\frac13 + "
                "\\frac{3x^2-1}{3} = x^2$. Exact — a finite expansion, because $x^2$ is "
                "already a combination of low Legendre modes.\n\n"
                "Notice how identical the two examples are procedurally: pick the "
                "eigenfunctions and their weight, integrate $f$ against each, divide by the "
                "norm-squared. The sines and the Legendre polynomials are different "
                "coordinate systems for the same act of measuring components."
            )),
            # ---------------------------------------------------------- 8
            (L, "Techniques and strategy", (
                "A reliable workflow for 'expand $f$ in eigenfunctions.'\n\n"
                "STEP 1 — IDENTIFY THE BASIS FROM THE BOUNDARY CONDITIONS, not from $f$. "
                "The PDE's boundary conditions choose the eigenfunctions: homogeneous "
                "Dirichlet ($u=0$ at both ends) $\\Rightarrow$ sines; homogeneous Neumann "
                "($u'=0$) $\\Rightarrow$ cosines plus a constant; mixed/Robin $\\Rightarrow$ "
                "shifted sines with transcendental eigenvalues; a polar/cylindrical domain "
                "$\\Rightarrow$ Bessel functions; a spherical domain $\\Rightarrow$ Legendre "
                "polynomials. Get this right first; everything downstream depends on it.\n\n"
                "STEP 2 — WRITE THE WEIGHT $w(x)$. For plain second-derivative problems "
                "$w=1$. For Bessel's equation on $[0,R]$ the weight is $w(x)=x$; for "
                "Legendre it is $1$; for quantum problems it is whatever makes the operator "
                "self-adjoint. The weight rides along inside EVERY integral — forgetting it "
                "is the number-one silent error.\n\n"
                "STEP 3 — COMPUTE THE NORM-SQUARED $\\int_a^b X_n^2\\,w\\,dx$ ONCE. These "
                "are standard: $\\frac{L}{2}$ for sines/cosines on $[0,L]$, "
                "$\\frac{2}{2n+1}$ for Legendre, $\\frac{R^2}{2}J_{k+1}(\\alpha_n)^2$ for "
                "Bessel. Keep a table; you will reuse them constantly.\n\n"
                "STEP 4 — PROJECT: $c_n = \\frac{\\int f X_n w\\,dx}{\\int X_n^2 w\\,dx}$. "
                "Exploit symmetry to save work: on a symmetric interval, an even $f$ has "
                "zero coefficients against odd eigenfunctions, and vice versa (Example 2 "
                "used this to skip $P_1$).\n\n"
                "STEP 5 — SANITY-CHECK. Does a truncated sum look like $f$? Does Parseval "
                "roughly balance? Is a coefficient you expected to vanish (by symmetry) "
                "actually zero? A single wrong sign in a product-to-sum step usually shows "
                "up as a coefficient that refuses to decay, so glance at the tail: genuine "
                "coefficients of a reasonable $f$ shrink."
            )),
            # ---------------------------------------------------------- 9
            (E, "Harder worked example: completing a separation solution", (
                "Solve the heat equation $u_t = \\alpha u_{xx}$ on $0<x<L$ with "
                "$u(0,t)=u(L,t)=0$ and initial temperature $u(x,0)=f(x)$. This is where "
                "orthogonality earns its keep.\n\n"
                "SEPARATION gave product solutions $X_n(x)T_n(t) = "
                "\\sin\\frac{n\\pi x}{L}\\,e^{-\\alpha(n\\pi/L)^2 t}$, one for each $n$. "
                "Because the PDE and boundary conditions are linear and homogeneous, any "
                "superposition is again a solution:\n"
                "$$u(x,t) = \\sum_{n=1}^{\\infty} b_n \\sin\\tfrac{n\\pi x}{L}\\, "
                "e^{-\\alpha(n\\pi/L)^2 t}.$$\n"
                "At $t=0$ every exponential is $1$, so the initial condition demands "
                "$f(x) = \\sum b_n \\sin\\frac{n\\pi x}{L}$ — an eigenfunction expansion! "
                "Orthogonality delivers the coefficients:\n"
                "$$b_n = \\frac{\\int_0^L f(x)\\sin\\frac{n\\pi x}{L}\\,dx}{\\int_0^L "
                "\\sin^2\\frac{n\\pi x}{L}\\,dx} = \\frac{2}{L}\\int_0^L f(x)\\sin\\tfrac{n\\pi "
                "x}{L}\\,dx.$$\n\n"
                "CONCRETE CASE 1 — $f(x) = 3\\sin\\frac{2\\pi x}{L}$. The data is already a "
                "single eigenfunction. Projection sees it instantly: $b_2 = 3$ and every "
                "other $b_n = 0$ (orthogonality zeroes them). So "
                "$u(x,t) = 3\\sin\\frac{2\\pi x}{L}\\,e^{-\\alpha(2\\pi/L)^2 t}$. The mode "
                "just decays in place — no other mode was ever excited.\n\n"
                "CONCRETE CASE 2 — $f(x)=1$ (a uniformly heated bar, ends suddenly "
                "chilled). From Example 1, $b_n = \\frac{4}{n\\pi}$ for odd $n$, else $0$, "
                "so\n"
                "$$u(x,t) = \\sum_{n\\text{ odd}} \\frac{4}{n\\pi}\\sin\\tfrac{n\\pi x}{L}\\, "
                "e^{-\\alpha(n\\pi/L)^2 t}.$$\n"
                "The high modes carry a factor $e^{-\\alpha(n\\pi/L)^2 t}$ that decays as "
                "$n^2$, so they die almost immediately; after a short time only the $n=1$ "
                "term survives and the bar cools as a smooth half-sine hump. Orthogonality "
                "converted an arbitrary initial state into a sum of independently decaying "
                "modes — the entire strategy in one solution."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: singular problems and convergence", (
                "The clean theorem of page 4 has important boundary cases.\n\n"
                "SINGULAR STURM-LIOUVILLE PROBLEMS. For Bessel and Legendre the coefficient "
                "$p(x)$ VANISHES at an endpoint ($p=x$ at $x=0$ for Bessel; $p=1-x^2$ at "
                "$x=\\pm1$ for Legendre). There is no ordinary boundary condition there; "
                "instead one demands that $X$ stay BOUNDED. The boundary term "
                "$[p(X_m'X_n-X_mX_n')]$ still vanishes — now because $p\\to0$ — so "
                "orthogonality survives. These are 'singular' SL problems, and they cover "
                "most of the special functions of physics.\n\n"
                "REPEATED EIGENVALUES. The theorem gives orthogonality only for DISTINCT "
                "eigenvalues. If one eigenvalue has two independent eigenfunctions (common "
                "with periodic conditions, e.g. $\\sin\\frac{n\\pi x}{L}$ and "
                "$\\cos\\frac{n\\pi x}{L}$ share $\\lambda$), those two are not forced "
                "orthogonal by the theorem — but you can always Gram-Schmidt them into an "
                "orthogonal pair, and for the standard trig system they already happen to "
                "be orthogonal.\n\n"
                "CONVERGENCE AT JUMPS AND THE GIBBS PHENOMENON. Eigenfunction series "
                "converge to $f$ in the mean-square (energy) sense whenever $f$ has finite "
                "energy — that is what completeness guarantees. But POINTWISE behavior is "
                "subtler: at a jump discontinuity the series converges to the MIDPOINT of "
                "the jump, and the partial sums overshoot by a fixed ~9% near the jump no "
                "matter how many terms you keep. This Gibbs overshoot narrows but never "
                "shrinks in height; it is a feature of truncating an orthogonal expansion, "
                "not an error in your coefficients.\n\n"
                "SMOOTHNESS AND DECAY RATE. The smoother $f$ is, the faster $c_n\\to0$. A "
                "jump gives $c_n\\sim 1/n$; a continuous $f$ with a corner gives "
                "$\\sim 1/n^2$; an infinitely smooth periodic $f$ gives faster-than-any-power "
                "decay. Reading the tail of your coefficients tells you the regularity of "
                "$f$ — a diagnostic used throughout signal processing."
            )),
            # ---------------------------------------------------------- 11
            (E, "Edge-case worked example: Robin conditions", (
                "When the boundary condition is neither pure Dirichlet nor pure Neumann, "
                "the eigenvalues stop being nice — yet orthogonality is untouched. Consider "
                "$X'' + \\lambda X = 0$ on $[0,L]$ with $X(0)=0$ and the ROBIN condition "
                "$X'(L) + hX(L)=0$ ($h>0$), which models a bar fixed at one end and losing "
                "heat by convection at the other.\n\n"
                "The left condition forces $X(x)=\\sin(\\mu x)$ with $\\mu=\\sqrt\\lambda$. "
                "Imposing the right condition: $\\mu\\cos(\\mu L) + h\\sin(\\mu L)=0$, i.e.\n"
                "$$\\tan(\\mu L) = -\\frac{\\mu}{h}.$$\n"
                "This is TRANSCENDENTAL — its roots $\\mu_1,\\mu_2,\\ldots$ have no "
                "closed form; you find them numerically as the intersections of "
                "$y=\\tan(\\mu L)$ with the line $y=-\\mu/h$. There is no tidy "
                "$\\mu_n = n\\pi/L$ here.\n\n"
                "NEVERTHELESS the eigenfunctions $X_n(x)=\\sin(\\mu_n x)$ are still mutually "
                "orthogonal on $[0,L]$ with $w=1$. Why? The proof on page 4 never used the "
                "value of $\\lambda$; it used only that the boundary term "
                "$[X_m'X_n - X_mX_n']_0^L$ vanishes. Check the right end: "
                "$X_m'X_n - X_mX_n'$ at $x=L$ equals "
                "$(-hX_m)X_n - X_m(-hX_n) = -hX_mX_n + hX_mX_n = 0$, using "
                "$X_k'(L)=-hX_k(L)$ for both. At the left end both $X$ vanish. Boundary term "
                "gone, orthogonality intact.\n\n"
                "The norm is no longer simply $\\frac{L}{2}$; you must compute "
                "$\\int_0^L \\sin^2(\\mu_n x)\\,dx = \\frac{L}{2} - "
                "\\frac{\\sin(2\\mu_n L)}{4\\mu_n}$ for each root and put THAT in the "
                "denominator of $c_n$. Moral: transcendental eigenvalues make the "
                "arithmetic messier, but the geometry — perpendicular axes, projection for "
                "coefficients — is exactly the same. The machinery is robust precisely "
                "because it rests on operator symmetry, not on the eigenvalues being "
                "round numbers."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'Orthogonal means the graphs never cross / are "
                "geometrically perpendicular.' No. Orthogonality here means an INTEGRAL is "
                "zero, $\\int X_m X_n w\\,dx = 0$. The curves $\\sin x$ and $\\sin 2x$ cross "
                "repeatedly; they are orthogonal because their product integrates to zero "
                "over the interval, positive and negative lobes cancelling.\n\n"
                "MISCONCEPTION 2: 'The weight $w(x)$ is optional decoration.' It is part of "
                "the inner product. For Bessel expansions $w(x)=x$, and every "
                "orthogonality integral, norm, and coefficient carries that $x$. Drop it "
                "and your 'orthogonal' functions are not orthogonal and your coefficients "
                "are wrong. Always ask what $w$ the problem's operator demands.\n\n"
                "MISCONCEPTION 3: 'Any two different eigenfunctions are automatically "
                "orthogonal.' Only DISTINCT-EIGENVALUE eigenfunctions are guaranteed "
                "orthogonal. Two eigenfunctions sharing an eigenvalue (a degenerate case) "
                "need not be, and must be orthogonalized by hand if you want to project "
                "with them.\n\n"
                "MISCONCEPTION 4: 'The expansion equals $f$ everywhere, pointwise.' It "
                "equals $f$ in the mean-square sense, and pointwise where $f$ is nice; at a "
                "jump it converges to the midpoint, and truncations overshoot (Gibbs). "
                "'$f = \\sum c_n X_n$' is a statement about convergence in energy, not a "
                "promise about every single point.\n\n"
                "MISCONCEPTION 5: 'Fourier series are a separate topic from Legendre / "
                "Bessel / quantum expansions.' They are ONE topic. All are eigenfunction "
                "expansions of self-adjoint operators; all use the same projection formula; "
                "all rest on the same orthogonality proof. Seeing them as one idea (the "
                "LA.U8.N2 geometry of projection) is the entire point of this chapter — "
                "treating them as four unrelated recipes quadruples your workload for no "
                "reason."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational and procedural errors", (
                "ERROR 1: Forgetting the norm in the denominator. Students compute "
                "$\\int f X_n w\\,dx$ and call it $c_n$. That is only the numerator; you "
                "must divide by $\\int X_n^2 w\\,dx$. For sines on $[0,L]$ that norm is "
                "$\\frac{L}{2}$, which is why the coefficient carries the famous factor "
                "$\\frac{2}{L}$ out front. Omit it and every coefficient is off by that "
                "factor.\n\n"
                "ERROR 2: Using the wrong norm for the constant/$a_0$ term. In a cosine "
                "series the constant mode has norm $\\int_0^L 1\\,dx = L$, not "
                "$\\frac{L}{2}$. That is the origin of the $\\frac{a_0}{2}$ convention. "
                "Mixing up the two norms double-counts or halves the average value of "
                "$f$.\n\n"
                "ERROR 3: Product-to-sum sign slips. "
                "$\\sin A\\sin B = \\tfrac12[\\cos(A-B)-\\cos(A+B)]$ has a MINUS on the "
                "sum-argument cosine; $\\cos A\\cos B = \\tfrac12[\\cos(A-B)+\\cos(A+B)]$ "
                "has a plus. Writing the wrong sign flips whole terms and typically leaves "
                "you with a spurious non-decaying coefficient. Write the identity down "
                "before integrating.\n\n"
                "ERROR 4: Integrating $\\sin\\frac{n\\pi x}{L}$ and dropping the "
                "$\\frac{L}{n\\pi}$. The antiderivative is "
                "$-\\frac{L}{n\\pi}\\cos\\frac{n\\pi x}{L}$; the $\\frac{L}{n\\pi}$ chain-"
                "rule factor is where the $\\frac{1}{n}$ decay of the coefficients comes "
                "from. Lose it and your series diverges nonsensically.\n\n"
                "ERROR 5: Evaluating $\\cos n\\pi$ carelessly. $\\cos n\\pi = (-1)^n$, so "
                "$1-\\cos n\\pi$ is $2$ for odd $n$ and $0$ for even $n$. Treating it as a "
                "constant, or getting the parity backwards, deletes exactly the terms that "
                "should survive.\n\n"
                "ERROR 6: Ignoring symmetry and grinding every integral. On a symmetric "
                "interval, an even function has zero overlap with odd eigenfunctions. "
                "Recognizing this halves the work and, more importantly, catches errors: a "
                "coefficient that should vanish by symmetry but comes out nonzero flags an "
                "arithmetic mistake immediately."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: where orthogonality does the work", (
                "1. HEAT, WAVE, AND LAPLACE PROBLEMS. Every separation-of-variables "
                "solution ends by expanding the initial or boundary data in eigenfunctions "
                "so each mode can evolve independently (page 9). Orthogonality is the step "
                "that turns 'a solution for each mode' into 'the solution for my actual "
                "data.' Without it, separation is a half-finished method.\n\n"
                "2. SIGNAL PROCESSING AND COMPRESSION. A sound or image is projected onto "
                "an orthogonal basis (Fourier, cosine, wavelet); the coefficients are the "
                "spectrum. Because the basis is orthogonal, you can DISCARD small "
                "coefficients and lose only their small energy (Parseval) — this is exactly "
                "how JPEG and MP3 compress. The decoupling that made coefficients easy to "
                "compute is the same property that makes lossy compression well-behaved.\n\n"
                "3. QUANTUM MECHANICS. The stationary states $\\psi_n$ of a Hamiltonian are "
                "eigenfunctions of a self-adjoint operator, hence orthogonal. Any state "
                "$\\Psi = \\sum c_n\\psi_n$, and $|c_n|^2$ is the PROBABILITY of measuring "
                "energy $E_n$. The projection $c_n = \\langle\\psi_n,\\Psi\\rangle$ is the "
                "same integral you compute here; orthogonality is why probabilities from "
                "different states do not interfere in the total.\n\n"
                "4. VIBRATION AND ACOUSTICS. A drumhead's motion is a sum of Bessel modes; "
                "a planet's gravitational field is a Legendre (spherical-harmonic) "
                "expansion. Engineers isolate a resonance by projecting measured data onto "
                "the mode shapes — orthogonality guarantees the projection reads one mode "
                "without contamination from the others.\n\n"
                "5. NUMERICAL METHODS (SPECTRAL METHODS). Representing an unknown as a "
                "truncated orthogonal expansion turns a PDE into a small, decoupled system "
                "for the coefficients, converging spectrally fast for smooth solutions. The "
                "orthogonality that decoupled the coefficients by hand is what makes the "
                "resulting linear algebra diagonal and cheap."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: cooling a rod", (
                "A metal rod of length $L=\\pi$ has thermal diffusivity $\\alpha=1$, its "
                "ends held at $0^\\circ$, and initial temperature "
                "$f(x) = 2\\sin x + 5\\sin 4x$ degrees. Find the temperature $u(x,t)$ and "
                "describe how it cools.\n\n"
                "STEP 1 — the eigenfunctions. Dirichlet ends on $[0,\\pi]$ give "
                "$X_n=\\sin(nx)$ with $\\lambda_n = n^2$ and time factors "
                "$e^{-\\alpha n^2 t} = e^{-n^2 t}$.\n\n"
                "STEP 2 — project the initial data. Here $f$ is ALREADY a combination of "
                "eigenfunctions: $f = 2\\sin x + 5\\sin 4x$. Orthogonality reads the "
                "coefficients off directly — projecting onto $\\sin x$ gives $b_1=2$, onto "
                "$\\sin 4x$ gives $b_4=5$, and every other projection is $0$ because "
                "$\\int_0^\\pi \\sin(mx)\\sin(nx)\\,dx=0$ for $m\\ne n$. No integrals to "
                "grind; the data announced its own components.\n\n"
                "STEP 3 — attach the time decay to each surviving mode:\n"
                "$$u(x,t) = 2\\sin x\\,e^{-t} + 5\\sin 4x\\,e^{-16t}.$$\n\n"
                "STEP 4 — read the physics. The $n=4$ mode decays with rate $16$, the "
                "$n=1$ mode with rate $1$ — sixteen times faster. At $t=0.1$ the fast mode "
                "is scaled by $e^{-1.6}\\approx0.20$ (down to $\\approx1.0$ from $5$) while "
                "the slow mode is scaled by $e^{-0.1}\\approx0.90$ (still $\\approx1.8$). By "
                "$t=0.5$ the fast term is $5e^{-8}\\approx0.0017$ — essentially gone — while "
                "$2e^{-0.5}\\approx1.21$ of the slow mode remains. The rod rapidly forgets "
                "its wiggly high-frequency detail and relaxes toward the smooth "
                "fundamental half-sine before that, too, fades to zero.\n\n"
                "This is the universal lesson of eigenfunction expansions for diffusion: "
                "high spatial frequencies (large $n$) carry the fine structure and die like "
                "$e^{-n^2 t}$, so heat flow is a low-pass filter. Orthogonality let us "
                "watch each frequency decay on its own clock."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections", (
                "BACKWARD (what this rested on):\n"
                "• LA.U8.N2 (inner products and projection): the beating heart of this "
                "chapter. Function orthogonality, the coefficient formula, best "
                "approximation, and Parseval's energy identity are the finite-dimensional "
                "projection theorems with the dot product upgraded to an integral. If the "
                "geometry felt familiar, that is why.\n"
                "• PF.U7.N2 (Sturm-Liouville problems): supplied the eigenfunctions and the "
                "self-adjoint structure whose symmetry powers the orthogonality proof.\n"
                "• PF.U4.N2 (Fourier series): the classical, weight-one, sine/cosine "
                "instance of everything here.\n\n"
                "FORWARD:\n"
                "• PF.U5.N1 (separation of variables): this chapter is the missing final "
                "step — expanding the data so each separated mode gets its coefficient. "
                "Every heat/wave/Laplace boundary-value problem closes with a projection.\n"
                "• PF.U9.N1 (Green's functions): the eigenfunction expansion of a Green's "
                "function, $G(x,s)=\\sum \\frac{X_n(x)X_n(s)}{\\lambda_n\\|X_n\\|^2}$, is "
                "orthogonality applied to the impulse response — the same projection, one "
                "layer deeper.\n"
                "• PDEM and PDET (advanced PDE modeling and theory): spectral numerical "
                "methods, spherical harmonics, and the functional-analytic completeness "
                "theorems all generalize this material to higher dimensions and abstract "
                "Hilbert spaces.\n\n"
                "SIDEWAYS: the classification of PDEs (PF.U1.N2) tells you WHICH problems "
                "are amenable to eigenfunction expansion — the self-adjoint elliptic and "
                "parabolic problems — versus the hyperbolic transport problems of "
                "PF.U8, which are better handled by characteristics. Knowing which tool "
                "each PDE type wants is the strategic payoff of seeing the whole "
                "sequence."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: the Hilbert space view", (
                "Everything in this chapter is the first, most concrete chapter of a vast "
                "subject: the theory of HILBERT SPACES. A Hilbert space is a (possibly "
                "infinite-dimensional) vector space with an inner product in which limits "
                "behave well. The space $L^2_w[a,b]$ of functions with finite weighted "
                "energy $\\int_a^b f^2 w\\,dx < \\infty$ is the canonical example, and your "
                "eigenfunctions $\\{X_n\\}$ form an ORTHOGONAL BASIS of it.\n\n"
                "The deep theorem lurking behind 'completeness' is the SPECTRAL THEOREM for "
                "self-adjoint operators. In finite dimensions it says a symmetric matrix "
                "has an orthonormal basis of eigenvectors and real eigenvalues (LA.U8). The "
                "Sturm-Liouville theory is its infinite-dimensional counterpart: a "
                "self-adjoint differential operator has a complete orthogonal set of "
                "eigenfunctions with real, discrete eigenvalues marching to infinity. Your "
                "projection formula and Parseval identity are the same statements you met "
                "for symmetric matrices, now for an operator with infinitely many "
                "eigenvalues.\n\n"
                "Two horizons worth glimpsing. FIRST, not every self-adjoint operator has a "
                "purely discrete spectrum. On an infinite domain the eigenvalues can fill a "
                "CONTINUUM, and the sum $\\sum c_n X_n$ becomes an integral — that is "
                "exactly the Fourier TRANSFORM (a continuous eigenfunction expansion of "
                "$-d^2/dx^2$ on the whole line), the subject that generalizes Fourier "
                "series to non-periodic problems.\n\n"
                "SECOND, the same framework unifies apparently unrelated 'special "
                "functions': Hermite functions (quantum harmonic oscillator), Laguerre "
                "(hydrogen atom radial part), Chebyshev (numerical analysis), and spherical "
                "harmonics (anything on a sphere) are each the orthogonal eigenbasis of "
                "some self-adjoint operator. Learn the geometry once and you own all of "
                "them. The nineteenth century discovered these functions one differential "
                "equation at a time; the twentieth century realized they were a single idea "
                "— orthogonal projection — wearing different coordinates."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "THE INNER PRODUCT. "
                "$\\langle f,g\\rangle = \\int_a^b f\\,g\\,w\\,dx$. Orthogonal means "
                "$\\langle f,g\\rangle = 0$. The weight $w(x)$ is set by the operator "
                "($w=1$ for plain $d^2/dx^2$ and Legendre; $w=x$ for Bessel).\n\n"
                "ORTHOGONALITY THEOREM. SL eigenfunctions of DISTINCT eigenvalues satisfy "
                "$\\int_a^b X_m X_n w\\,dx = 0$. Proof: multiply-subtract the eigen-"
                "equations, integrate, and the self-adjoint boundary conditions kill the "
                "boundary term $[p(X_m'X_n - X_mX_n')]_a^b$, leaving "
                "$(\\lambda_m-\\lambda_n)\\int X_mX_n w\\,dx=0$.\n\n"
                "PROJECTION (MASTER FORMULA). "
                "$$c_n = \\frac{\\int_a^b f X_n w\\,dx}{\\int_a^b X_n^2 w\\,dx} = "
                "\\frac{\\langle f, X_n\\rangle}{\\langle X_n, X_n\\rangle}.$$\n\n"
                "STANDARD NORMS. $\\int_0^L \\sin^2\\frac{n\\pi x}{L}dx = "
                "\\int_0^L \\cos^2\\frac{n\\pi x}{L}dx = \\frac{L}{2}$ (but the constant "
                "mode has norm $L$); $\\int_{-1}^1 P_n^2\\,dx = \\frac{2}{2n+1}$.\n\n"
                "PARSEVAL. $\\int_a^b f^2 w\\,dx = \\sum_n c_n^2 \\int_a^b X_n^2 w\\,dx$ — "
                "energy is the sum of modal energies.\n\n"
                "CONVERGENCE. Mean-square convergence for finite-energy $f$ (completeness); "
                "at a jump the series $\\to$ midpoint with a fixed ~9% Gibbs overshoot; "
                "smoother $f$ $\\Rightarrow$ faster coefficient decay ($1/n$ jump, $1/n^2$ "
                "corner).\n\n"
                "WORKFLOW. (1) Boundary conditions $\\Rightarrow$ basis. (2) Write the "
                "weight $w$. (3) Compute $\\int X_n^2 w\\,dx$ once. (4) Project for each "
                "$c_n$; use symmetry to skip terms. (5) Attach the time/other factor "
                "(separation) and sum. (6) Sanity-check the tail decays and Parseval "
                "balances.\n\n"
                "ONE-LINE TAKEAWAY. Eigenfunctions are perpendicular axes of function "
                "space; expansion is reading off components by projection; every Fourier, "
                "Bessel, Legendre, and quantum series is this one act in different "
                "coordinates."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all seven before reading the solutions.\n\n"
                "1. Compute $\\int_0^\\pi \\sin 2x\\,\\sin 3x\\,dx$ and say what it "
                "means.\n"
                "2. Find the norm $\\int_0^{\\pi}\\sin^2 3x\\,dx$.\n"
                "3. For $f(x)=1$ on $[0,\\pi]$, find $b_2$ and $b_3$ in "
                "$f=\\sum b_n\\sin nx$.\n"
                "4. Expand $f(x)=x$ in Legendre polynomials on $[-1,1]$ (find the "
                "coefficients of $P_0$ and $P_1$).\n"
                "5. True or false: two eigenfunctions of a self-adjoint operator are "
                "always orthogonal.\n"
                "6. Why does the Robin problem of page 11 still yield orthogonal "
                "eigenfunctions despite transcendental eigenvalues?\n"
                "7. If $u(x,0)=7\\sin 5x$ on $[0,\\pi]$ with $u_t=u_{xx}$, Dirichlet ends, "
                "write $u(x,t)$.\n\n"
                "SOLUTIONS.\n"
                "1. $\\tfrac12\\int_0^\\pi[\\cos x - \\cos 5x]dx = "
                "\\tfrac12[\\sin x - \\tfrac15\\sin 5x]_0^\\pi = 0$. The modes $n=2,3$ are "
                "orthogonal (distinct eigenvalues $4\\ne9$).\n"
                "2. $\\int_0^\\pi\\tfrac12(1-\\cos 6x)dx = \\tfrac12[\\pi - 0] = "
                "\\frac{\\pi}{2}$.\n"
                "3. $b_n = \\frac{2}{\\pi}\\int_0^\\pi \\sin nx\\,dx = "
                "\\frac{2}{n\\pi}(1-(-1)^n)$. So $b_2 = 0$ (even), "
                "$b_3 = \\frac{4}{3\\pi}$ (odd).\n"
                "4. $x$ is odd, so its overlap with the even $P_0=1$ is zero: coefficient "
                "of $P_0$ is $0$. For $P_1=x$: "
                "$\\frac{\\int_{-1}^1 x\\cdot x\\,dx}{\\int_{-1}^1 x^2 dx} = "
                "\\frac{2/3}{2/3} = 1$. So $x = P_1$ exactly (as expected).\n"
                "5. FALSE — only for DISTINCT eigenvalues. Degenerate eigenfunctions "
                "sharing an eigenvalue need not be orthogonal.\n"
                "6. The orthogonality proof only needs the boundary term "
                "$[X_m'X_n-X_mX_n']$ to vanish; the Robin condition $X'(L)=-hX(L)$ makes it "
                "cancel there, and $X(0)=0$ handles the other end. Eigenvalues never enter "
                "the argument.\n"
                "7. Data is a single eigenfunction: only $b_5=7$ survives, so "
                "$u(x,t)=7\\sin 5x\\,e^{-25t}$.\n\n"
                "Scoring: 7/7 — proceed with confidence. 5-6 — reread the projection "
                "formula (page 6) and the norm errors (page 13). $\\le4$ — rework pages "
                "3-9 before practicing."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Check each box honestly before leaving this chapter.\n\n"
                "$\\square$ State the definition of orthogonality for functions "
                "($\\int X_m X_n w\\,dx = 0$) and explain why it is the integral analogue "
                "of perpendicular vectors.\n"
                "$\\square$ Reproduce the orthogonality proof: multiply-subtract the "
                "eigen-equations, integrate, and show the self-adjoint boundary conditions "
                "kill the boundary term.\n"
                "$\\square$ Write and apply the projection formula "
                "$c_n=\\frac{\\int f X_n w\\,dx}{\\int X_n^2 w\\,dx}$ from memory.\n"
                "$\\square$ Compute a Fourier sine or cosine expansion of a simple $f$, "
                "including the correct norm and the parity/symmetry shortcuts.\n"
                "$\\square$ Expand a low-degree polynomial in Legendre polynomials by "
                "projection and verify the result.\n"
                "$\\square$ Explain why Bessel, Legendre, and quantum expansions are the "
                "same geometry as Fourier series (self-adjoint operator $\\to$ orthogonal "
                "eigenbasis), and identify the weight $w$ each uses.\n"
                "$\\square$ Complete a separation-of-variables solution by projecting the "
                "initial/boundary data onto the eigenfunctions and attaching each mode's "
                "time factor.\n"
                "$\\square$ Describe convergence carefully: mean-square convergence, "
                "midpoint at jumps, the Gibbs overshoot, and coefficient decay versus "
                "smoothness.\n"
                "$\\square$ Explain the distinct-eigenvalue hypothesis and what to do "
                "about degenerate eigenfunctions.\n"
                "$\\square$ Recognize a Robin/singular problem and argue orthogonality "
                "survives even without closed-form eigenvalues.\n\n"
                "When every box is checked, hit Practice until projecting a function onto "
                "an eigenbasis feels as automatic as finding vector components. Then "
                "proceed to the transport and Green's-function chapters, where these ideas "
                "return as the spectral form of the impulse response."
            )),
        ],
    },
    "PF.U8.N1": {
        "summary": (
            "A complete course on characteristic lines: how a first-order PDE "
            "$a u_t + b u_x = c$ turns into an ODE along the curves $dx/dt = b/a$, why "
            "$u$ is constant along $x-ct=\\text{const}$ for transport, how the wave "
            "equation factors into two characteristic families (the source of "
            "d'Alembert's solution), and how curving characteristics preview shock "
            "formation."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "Separation of variables and eigenfunction expansions handled the "
                "'equilibrium-seeking' PDEs — heat spreading, membranes settling. But a "
                "huge class of physical laws are about TRANSPORT: something is carried "
                "along — a pollutant down a river, a traffic wave down a highway, a signal "
                "down a wire, the very disturbances that make up sound. These are governed "
                "by first-order PDEs, and Fourier machinery is the wrong tool for them. "
                "They have their own, more geometric method: CHARACTERISTICS.\n\n"
                "The central discovery is beautiful and simple. A first-order PDE like "
                "$a u_t + b u_x = c$ looks like it couples two variables and resists "
                "solution. But there exist special curves in the $(x,t)$ plane — the "
                "characteristics — ALONG WHICH the PDE collapses into an ordinary "
                "differential equation. The two-variable partial differential equation "
                "becomes a family of one-variable problems, one ODE per curve. You already "
                "know how to solve ODEs, so you already know how to solve these PDEs, once "
                "you find the right curves.\n\n"
                "For pure transport the ODE is the simplest imaginable: $u$ does not change "
                "at all. The information just RIDES along the characteristic, unchanged. "
                "That is why a wave keeps its shape as it travels.\n\n"
                "In this chapter you will learn: how to derive the characteristic curves "
                "$dx/dt = b/a$ from the PDE; how the PDE becomes an ODE along them; the "
                "special role of $x - ct = \\text{const}$ for the transport equation; why "
                "the second-order wave equation factors into TWO characteristic families "
                "and hence into d'Alembert's left- and right-movers; and a first look at "
                "how characteristics that curve or cross create shocks. Characteristics are "
                "the native language of everything hyperbolic — the entire other half of "
                "PDE theory from the diffusion problems you have been solving."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Three ingredients, all light.\n\n"
                "1. FIRST-ORDER ODEs AND THE CHAIN RULE. You must be fluent with "
                "separable ODEs ($\\frac{dx}{dt}=b/a$ integrates to a line or curve) and "
                "linear ODEs ($\\frac{du}{dt}=c$ or $=-\\lambda u$). Crucially, you need "
                "the MULTIVARIABLE CHAIN RULE: if we move along a curve $x=x(t)$ and watch "
                "$u(x(t),t)$, then $\\frac{d}{dt}u(x(t),t) = u_t + u_x\\frac{dx}{dt}$. This "
                "one identity is the whole method — memorize its shape.\n\n"
                "2. READING THE $(x,t)$ PLANE. Characteristics are curves drawn with $x$ "
                "horizontal and $t$ vertical (or sometimes the reverse). You should be "
                "comfortable sketching lines of the form $x - ct = \\text{const}$ (a family "
                "of parallel lines of slope, in $t$-vs-$x$ axes, $1/c$) and reading initial "
                "data off the line $t=0$.\n\n"
                "3. WHAT A PDE IS (PF.U1.N2). Recognize a first-order linear PDE "
                "$a u_t + b u_x = c$ and its classification as HYPERBOLIC — the type that "
                "transports information at finite speed, as opposed to the parabolic heat "
                "equation that smooths instantly.\n\n"
                "Quick self-check (answers follow):\n\n"
                "• If $x(t)$ solves $\\frac{dx}{dt}=3$ with $x(0)=x_0$, what is $x(t)$? "
                "($x = x_0 + 3t$; equivalently $x-3t=x_0$.)\n"
                "• Along the curve $x=x(t)$, expand $\\frac{d}{dt}u(x(t),t)$. "
                "($u_t + u_x\\,x'(t)$ — the chain rule.)\n"
                "• Solve $\\frac{du}{dt}=-2u$, $u(0)=5$. ($u=5e^{-2t}$.)\n\n"
                "If the chain-rule identity felt unfamiliar, pause and rederive it; the "
                "entire method is that identity read in reverse."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Here is the trick in one move. We have a PDE that mixes $u_t$ and $u_x$:\n"
                "$$a\\,u_t + b\\,u_x = c.$$\n"
                "We cannot integrate it directly because it involves derivatives in two "
                "directions at once. So we ask: is there a DIRECTION to move through the "
                "$(x,t)$ plane along which those two derivatives combine into a single "
                "total derivative?\n\n"
                "Recall the chain rule: moving along a path $x=x(t)$, the rate of change of "
                "$u$ that we actually experience is\n"
                "$$\\frac{du}{dt} = u_t + \\frac{dx}{dt}\\,u_x.$$\n"
                "Compare this to the PDE, rewritten as "
                "$u_t + \\frac{b}{a}u_x = \\frac{c}{a}$. The two match perfectly if we "
                "choose our path so that\n"
                "$$\\frac{dx}{dt} = \\frac{b}{a}.$$\n"
                "Along THAT path — the characteristic — the messy PDE becomes the clean "
                "ODE $\\frac{du}{dt} = \\frac{c}{a}$. We have traded one partial "
                "differential equation for two ordinary ones: a CHARACTERISTIC EQUATION "
                "$\\frac{dx}{dt}=\\frac{b}{a}$ that says where to walk, and a TRANSPORT "
                "EQUATION $\\frac{du}{dt}=\\frac{c}{a}$ that says how $u$ changes as we "
                "walk.\n\n"
                "The picture: the $(x,t)$ plane fills with characteristic curves like the "
                "streamlines of a flow. Initial data sits on the line $t=0$. To find "
                "$u$ at any point $(x,t)$, ride the characteristic through that point back "
                "down to $t=0$, read the starting value, and integrate the simple ODE "
                "forward along the curve. If $c=0$ (pure transport), the ODE says "
                "$\\frac{du}{dt}=0$: $u$ NEVER CHANGES along a characteristic. The value at "
                "$(x,t)$ equals the initial value at the foot of its characteristic. "
                "Information rides the characteristics like beads on wires — and that is "
                "why a transported profile keeps its shape."
            )),
            # ---------------------------------------------------------- 4
            (L, "The method of characteristics, formally", (
                "Given the first-order linear PDE\n"
                "$$a(x,t)\\,u_t + b(x,t)\\,u_x = c(x,t,u), \\qquad u(x,0)=f(x),$$\n"
                "introduce a parameter $s$ measuring progress along a characteristic curve "
                "and write the CHARACTERISTIC SYSTEM:\n"
                "$$\\frac{dt}{ds} = a, \\qquad \\frac{dx}{ds} = b, \\qquad "
                "\\frac{du}{ds} = c.$$\n"
                "The first two ODEs trace the curve through the $(x,t)$ plane; the third "
                "tracks the solution's value along it. Why does this work? Because "
                "$\\frac{du}{ds} = u_t\\frac{dt}{ds} + u_x\\frac{dx}{ds} = a u_t + b u_x = "
                "c$ — the chain rule reproduces the PDE exactly. So any solution of the "
                "characteristic system IS a solution of the PDE.\n\n"
                "For CONSTANT coefficients with the time-derivative normalized ($a=1$), "
                "this simplifies to the pair most problems use:\n"
                "$$\\frac{dx}{dt} = b, \\qquad \\frac{du}{dt} = c.$$\n"
                "The characteristics are the straight lines $x = bt + \\xi$, i.e. "
                "$x - bt = \\xi = \\text{const}$, each labelled by its starting point "
                "$\\xi$ on the $t=0$ axis. Along the line labelled $\\xi$, integrate "
                "$\\frac{du}{dt}=c$ from the initial value $f(\\xi)$.\n\n"
                "THE THREE-STEP RECIPE:\n"
                "1. Find the characteristics by solving $\\frac{dx}{dt}=b/a$; write them as "
                "'label = const', e.g. $\\xi = x - bt$.\n"
                "2. Solve the ODE $\\frac{du}{dt}=c/a$ along a characteristic, getting $u$ "
                "in terms of $t$ and the label $\\xi$, using the initial value at the "
                "foot.\n"
                "3. Substitute the label back: replace $\\xi$ by its expression in $x$ and "
                "$t$ (e.g. $\\xi = x - bt$) to get $u(x,t)$ in the original variables.\n\n"
                "That final substitution — pushing the label back out to $(x,t)$ — is where "
                "the answer is born, and where most sign errors hide. Everything else is "
                "ODE solving you already own."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples", (
                "EXAMPLE 1 (pure transport). Solve $u_t + 3u_x = 0$ with "
                "$u(x,0)=e^{-x^2}$.\n\n"
                "Characteristics: $\\frac{dx}{dt}=3 \\Rightarrow x=3t+\\xi$, so "
                "$\\xi = x-3t$. Along each, $\\frac{du}{dt}=0$: $u$ is constant, equal to "
                "its value at the foot $t=0$, where $x=\\xi$, giving $u=f(\\xi)=e^{-\\xi^2}$. "
                "Substitute the label: $u(x,t)=e^{-(x-3t)^2}$. VERIFY: "
                "$u_t = e^{-(x-3t)^2}\\cdot(-2)(x-3t)(-3)=6(x-3t)e^{-(x-3t)^2}$ and "
                "$u_x = -2(x-3t)e^{-(x-3t)^2}$, so $u_t+3u_x = 6(\\cdots)e - 6(\\cdots)e = "
                "0$. The bump just slides right at speed $3$, undistorted.\n\n"
                "EXAMPLE 2 (a source term $c\\ne0$). Solve $u_t + 2u_x = 3$ with "
                "$u(x,0)=x$.\n\n"
                "Characteristics: $\\frac{dx}{dt}=2\\Rightarrow \\xi = x-2t$. Along a "
                "characteristic, $\\frac{du}{dt}=3$, so $u = 3t + (\\text{value at foot})$. "
                "At the foot $t=0$, $x=\\xi$, so the value is $f(\\xi)=\\xi$. Thus "
                "$u = 3t + \\xi = 3t + (x-2t) = x + t$. VERIFY: $u_t=1$, $u_x=1$, "
                "$u_t+2u_x = 1+2 = 3$; and $u(x,0)=x$. Correct.\n\n"
                "EXAMPLE 3 (leftward transport, reading off the shape). Solve "
                "$u_t - 4u_x = 0$, $u(x,0)=\\cos x$. Characteristics "
                "$\\frac{dx}{dt}=-4\\Rightarrow \\xi = x+4t$, and $u$ constant along them: "
                "$u=\\cos(x+4t)$. The cosine profile marches LEFT at speed $4$. Note the "
                "sign: $b=-4$ gives motion in the $-x$ direction, and the argument "
                "$x+4t$ (not $x-4t$) is the tell. Getting the sign of the shift right is "
                "the single most-tested detail on this page — always trace it back to the "
                "sign of $b$ in $\\frac{dx}{dt}=b$."
            )),
            # ---------------------------------------------------------- 6
            (L, "Properties: constancy, speed, and the wave factorization", (
                "Three structural facts organize everything.\n\n"
                "CONSTANCY ALONG CHARACTERISTICS. For the homogeneous transport equation "
                "$u_t + c u_x = 0$, the solution is CONSTANT on each line $x-ct=\\xi$. "
                "Geometrically, the graph of $u$ at time $t$ is the initial graph "
                "translated by $ct$: $u(x,t)=f(x-ct)$. No spreading, no decay, no change of "
                "shape — the hallmark of first-order hyperbolic transport, and the sharpest "
                "possible contrast with the heat equation, which instantly smears any "
                "profile.\n\n"
                "FINITE PROPAGATION SPEED. The value at $(x,t)$ depends ONLY on the single "
                "initial value at $x-ct$. Disturbances travel at the finite speed $c$; a "
                "bump at the origin cannot affect a distant point until enough time has "
                "passed for a characteristic to connect them. This 'domain of dependence' "
                "is a single point for transport — the tightest dependence in all of PDE "
                "theory.\n\n"
                "THE WAVE EQUATION FACTORS INTO TWO CHARACTERISTIC FAMILIES. The "
                "second-order wave equation $u_{tt} - c^2 u_{xx}=0$ is secretly two "
                "first-order transport equations. The operator factors:\n"
                "$$\\partial_{tt} - c^2\\partial_{xx} = "
                "\\big(\\partial_t - c\\,\\partial_x\\big)\\big(\\partial_t + "
                "c\\,\\partial_x\\big).$$\n"
                "The factor $\\partial_t + c\\partial_x$ transports at speed $+c$ (right-"
                "movers, $x-ct=\\text{const}$); the factor $\\partial_t - c\\partial_x$ "
                "transports at speed $-c$ (left-movers, $x+ct=\\text{const}$). So the wave "
                "equation has TWO characteristic families crossing the plane, and its "
                "general solution is a sum of a right-mover and a left-mover:\n"
                "$$u(x,t) = F(x-ct) + G(x+ct).$$\n"
                "This is d'Alembert's solution, and characteristics are WHY it has that "
                "form — the two families are the two arguments $x\\mp ct$. The next pages "
                "make this factorization do real work."
            )),
            # ---------------------------------------------------------- 7
            (E, "Standard worked examples: variable coefficients and decay", (
                "EXAMPLE 1 (variable coefficient — curved characteristics). Solve "
                "$u_t + x\\,u_x = 0$ with $u(x,0)=f(x)$. Here $b=x$ depends on position, so "
                "characteristics CURVE. Solve $\\frac{dx}{dt}=x$: this is a linear ODE, "
                "$x = x_0 e^{t}$, so the conserved label is $\\xi = x e^{-t}$. Along each "
                "curve $u$ is constant, so $u = f(\\xi) = f(xe^{-t})$. Take $f(x)=x^2$: "
                "$u = (xe^{-t})^2 = x^2 e^{-2t}$. VERIFY: $u_t = -2x^2 e^{-2t}$, "
                "$u_x = 2x e^{-2t}$, and $x u_x = 2x^2 e^{-2t}$, so $u_t + x u_x = 0$. The "
                "characteristics fan out exponentially from $x=0$, which is itself a fixed "
                "characteristic ($x_0=0$ stays $0$).\n\n"
                "EXAMPLE 2 (linear decay term). Solve $u_t + c\\,u_x = -\\lambda u$ with "
                "$u(x,0)=f(x)$, $c$ and $\\lambda>0$ constant. Characteristics "
                "$\\xi=x-ct$. Along each, $\\frac{du}{dt}=-\\lambda u$, whose solution is "
                "$u = (\\text{foot value})\\,e^{-\\lambda t} = f(\\xi)e^{-\\lambda t}$. "
                "Substitute: $u(x,t)=e^{-\\lambda t}f(x-ct)$. The profile still rides right "
                "at speed $c$, but now its amplitude decays exponentially as it travels — a "
                "moving, fading pulse. This is the exact template for an advected quantity "
                "that also leaks or reacts.\n\n"
                "EXAMPLE 3 (growth/source and initial data together). Solve "
                "$u_t + u_x = u$, $u(x,0)=\\sin x$. Characteristics $\\xi=x-t$; ODE "
                "$\\frac{du}{dt}=u\\Rightarrow u = f(\\xi)e^{t} = \\sin(x-t)\\,e^{t}$. "
                "VERIFY: $u_t = -\\cos(x-t)e^t + \\sin(x-t)e^t$, "
                "$u_x = \\cos(x-t)e^t$, sum $= \\sin(x-t)e^t = u$. A right-moving sine whose "
                "amplitude GROWS like $e^t$. The pattern across all three: the label "
                "captures WHERE the data came from, the ODE captures WHAT happened to it en "
                "route."
            )),
            # ---------------------------------------------------------- 8
            (L, "Techniques and strategy", (
                "A dependable playbook for first-order (and factored second-order) "
                "problems.\n\n"
                "STEP 1 — READ OFF $a$, $b$, $c$. Put the PDE in the form "
                "$a u_t + b u_x = c$. Identify the coefficient of $u_t$, of $u_x$, and "
                "everything else on the right (which may depend on $x$, $t$, or even $u$). "
                "A decay term $-\\lambda u$ belongs on the right as part of $c$.\n\n"
                "STEP 2 — SOLVE THE CHARACTERISTIC ODE $\\frac{dx}{dt}=b/a$ FIRST. If "
                "$b/a$ is constant, characteristics are straight lines "
                "$x-(b/a)t=\\text{const}$. If $b/a$ depends on $x$ or $t$, you get a small "
                "ODE — separate or use an integrating factor — and the characteristics "
                "curve. Always express the result as 'conserved label $\\xi = \\ldots$'.\n\n"
                "STEP 3 — SOLVE THE VALUE ODE $\\frac{du}{dt}=c/a$ ALONG A CHARACTERISTIC, "
                "treating $\\xi$ as a constant. Homogeneous ($c=0$): $u$ constant. Source "
                "($c$ constant): $u$ linear in $t$. Decay ($c=-\\lambda u$): exponential. "
                "Position-dependent source: substitute $x$ in terms of $t$ and $\\xi$ "
                "before integrating.\n\n"
                "STEP 4 — APPLY THE INITIAL CONDITION AT THE FOOT. On the curve labelled "
                "$\\xi$, the point with $t=0$ has $x=\\xi$ (for straight lines) or "
                "$x=x_0(\\xi)$ (for curved ones). Set the ODE's constant of integration so "
                "that $u$ equals $f$ there.\n\n"
                "STEP 5 — BACK-SUBSTITUTE THE LABEL. Replace $\\xi$ by its $(x,t)$ "
                "expression to return to physical variables. Then ALWAYS verify by plugging "
                "into the PDE and checking $u(x,0)=f(x)$ — two cheap checks that catch "
                "nearly every mistake.\n\n"
                "FOR THE WAVE EQUATION, change variables to $\\xi=x-ct$, $\\eta=x+ct$; the "
                "equation becomes $u_{\\xi\\eta}=0$, which integrates twice into "
                "$F(\\xi)+G(\\eta)$. Then fit $F$ and $G$ to the two initial conditions — "
                "the technique of the next page."
            )),
            # ---------------------------------------------------------- 9
            (E, "Harder worked example: d'Alembert from characteristics", (
                "Derive d'Alembert's solution of the wave equation "
                "$u_{tt}=c^2 u_{xx}$ with $u(x,0)=\\phi(x)$, $u_t(x,0)=\\psi(x)$, using "
                "characteristic coordinates — then specialize.\n\n"
                "CHANGE VARIABLES to $\\xi = x-ct$, $\\eta = x+ct$ (the two characteristic "
                "families). By the chain rule, $\\partial_x = \\partial_\\xi + "
                "\\partial_\\eta$ and $\\partial_t = -c\\partial_\\xi + c\\partial_\\eta$. "
                "Substituting collapses the wave operator to\n"
                "$$u_{tt} - c^2 u_{xx} = -4c^2\\,u_{\\xi\\eta} = 0 \\;\\Rightarrow\\; "
                "u_{\\xi\\eta}=0.$$\n"
                "Integrate in $\\xi$: $u_\\eta = G'(\\eta)$ (a function of $\\eta$ alone). "
                "Integrate in $\\eta$: $u = F(\\xi) + G(\\eta) = F(x-ct)+G(x+ct)$ — a "
                "right-mover plus a left-mover, exactly as the factorization predicted.\n\n"
                "FIT THE INITIAL DATA. At $t=0$: $F(x)+G(x)=\\phi(x)$. Differentiating "
                "$u_t = -cF'(x-ct)+cG'(x+ct)$ and setting $t=0$: "
                "$-cF'(x)+cG'(x)=\\psi(x)$, so $G'(x)-F'(x)=\\frac1c\\psi(x)$. Integrate "
                "that: $G(x)-F(x)=\\frac1c\\int_0^x\\psi(r)\\,dr + \\text{const}$. Combine "
                "with $F+G=\\phi$ to solve for each, and reassemble at "
                "$(x-ct),(x+ct)$. The constants cancel, leaving D'ALEMBERT'S FORMULA:\n"
                "$$u(x,t)=\\tfrac12\\big[\\phi(x-ct)+\\phi(x+ct)\\big] + "
                "\\frac{1}{2c}\\int_{x-ct}^{x+ct}\\psi(r)\\,dr.$$\n\n"
                "SPECIALIZE: $c=2$, $\\phi(x)=\\sin x$, $\\psi=0$. Then "
                "$u=\\tfrac12[\\sin(x-2t)+\\sin(x+2t)]$. Using the sum-to-product identity, "
                "$u=\\sin x\\cos 2t$ — a STANDING WAVE: the two travelling halves "
                "superpose into a shape that oscillates in place. The initial hump splits "
                "into two half-height copies riding opposite characteristics, and their "
                "interference is the standing pattern. Characteristics explain both the "
                "travelling pieces and, through their sum, the standing result."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: curving characteristics and shocks", (
                "The clean story assumes characteristics are nice, non-crossing curves. "
                "When they CROSS, the method breaks in a physically meaningful way — this "
                "is the birth of shocks.\n\n"
                "QUASILINEAR EQUATIONS. Consider the inviscid Burgers equation "
                "$u_t + u\\,u_x = 0$: the transport SPEED is the solution itself. The "
                "characteristic system reads $\\frac{dx}{dt}=u$, $\\frac{du}{dt}=0$. So $u$ "
                "is still constant along each characteristic — but the characteristic's "
                "SLOPE now depends on that constant value. Characteristics are still "
                "straight lines, $x = x_0 + f(x_0)\\,t$, but with DIFFERENT slopes: points "
                "where $u$ is large race ahead, points where $u$ is small lag behind.\n\n"
                "CROSSING AND MULTIVALUEDNESS. If the initial profile is DECREASING "
                "somewhere ($f'(x_0)<0$), a fast characteristic behind a slow one catches "
                "up. Where two characteristics meet, they carry two different constant "
                "values to the same point — the solution would have to be in two places at "
                "once. The smooth solution ceases to exist; a discontinuity (a SHOCK) "
                "forms.\n\n"
                "BREAKING TIME. Characteristics $x=x_0+f(x_0)t$ first cross at the earliest "
                "time two of them coincide. A short calculation gives the first breaking "
                "time\n"
                "$$t^* = \\frac{-1}{\\min_{x_0} f'(x_0)},$$\n"
                "valid when $f'$ is negative somewhere (else characteristics spread apart "
                "and never cross — a rarefaction — and the smooth solution lasts "
                "forever). Before $t^*$ the method of characteristics gives the exact "
                "smooth solution; at $t^*$ the gradient $u_x$ blows up; after $t^*$ one "
                "must switch to a weak/shock formulation with a jump condition. The full "
                "theory of shocks (Rankine-Hugoniot conditions, entropy) belongs to "
                "PDET, but the SEED — characteristics crossing — is entirely visible here, "
                "and the transport chapter PF.U8.N2 works a concrete example."
            )),
            # ---------------------------------------------------------- 11
            (E, "Edge-case worked example: gradient blow-up", (
                "Watch a smooth solution destroy itself in finite time. Solve the "
                "Burgers-type problem via characteristics with a decreasing initial "
                "profile, using the cleanest case $u(x,0)=-x$ for the equation "
                "$u_t + u\\,u_x = 0$.\n\n"
                "CHARACTERISTICS. Each starts at $(x_0,0)$ carrying the constant value "
                "$u=f(x_0)=-x_0$, and moves with slope $\\frac{dx}{dt}=u=-x_0$. So "
                "$x = x_0 + (-x_0)t = x_0(1-t)$. Every characteristic is a straight line, "
                "and they ALL pass through the single point $x=0$ at $t=1$ (because "
                "$x_0(1-t)=0$ for every $x_0$ when $t=1$). The characteristics converge.\n\n"
                "SOLVE FOR $u$. Along the characteristic, $u=-x_0$; invert "
                "$x = x_0(1-t)$ to get $x_0 = \\frac{x}{1-t}$ (valid for $t\\ne1$), so\n"
                "$$u(x,t) = -\\frac{x}{1-t}.$$\n"
                "VERIFY: $u_t = -\\frac{x}{(1-t)^2}$, $u_x = -\\frac{1}{1-t}$, and "
                "$u\\,u_x = \\left(-\\frac{x}{1-t}\\right)\\left(-\\frac{1}{1-t}\\right) = "
                "\\frac{x}{(1-t)^2}$, so $u_t + u u_x = 0$. Also $u(x,0)=-x$. Correct — for "
                "$t<1$.\n\n"
                "THE BLOW-UP. Look at the slope: $u_x = -\\frac{1}{1-t}\\to-\\infty$ as "
                "$t\\to1^-$. The profile steepens without bound; at $t=1$ it becomes "
                "vertical and the solution loses single-valuedness. This matches the "
                "breaking-time formula: $f'(x_0)=-1$ everywhere, so "
                "$t^* = -1/(-1)=1$. The characteristics told us in advance exactly when the "
                "smooth solution would die — they all aimed at the same point at $t=1$. "
                "Beyond $t=1$ no classical solution exists; a shock must be inserted. This "
                "tiny example is the entire mechanism of wave-breaking and sonic booms, "
                "stripped to its bones."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'The characteristic is where the solution lives / is "
                "biggest.' No — a characteristic is a curve in the $(x,t)$ PLANE along "
                "which the PDE reduces to an ODE. It is a bookkeeping path for propagating "
                "information, not a special location of the solution. Every point of the "
                "plane lies on exactly one characteristic.\n\n"
                "MISCONCEPTION 2: 'For transport, $u$ is constant, so the solution is "
                "constant everywhere.' $u$ is constant ALONG each characteristic, but "
                "different characteristics carry different constants. The solution "
                "$u(x,t)=f(x-ct)$ varies across the plane; it is only constant if you "
                "follow a single moving point $x=ct+\\text{const}$.\n\n"
                "MISCONCEPTION 3: 'The shift is $x+ct$.' The homogeneous transport "
                "$u_t+cu_x=0$ (with $+c$) moves RIGHT and has solution $f(x-ct)$ — a MINUS. "
                "The intuition: at a later time, to find the value now at $x$, look back to "
                "where it came from, $x-ct$. Sign confusion here is the most common error; "
                "anchor it by checking that a right-moving bump has argument $x-ct$.\n\n"
                "MISCONCEPTION 4: 'Characteristics are always straight lines.' Only for "
                "constant coefficients. With variable $b(x,t)$ they curve "
                "($u_t+xu_x=0$ gave exponentials, page 7); with a quasilinear speed "
                "$b=u$ they are straight but with data-dependent slopes that can cross.\n\n"
                "MISCONCEPTION 5: 'The wave equation needs Fourier series; "
                "characteristics are only for first-order PDEs.' The wave equation IS a "
                "product of two first-order transport operators, and d'Alembert's solution "
                "comes straight from its two characteristic families — no Fourier series "
                "required. Fourier is one option (for bounded domains); characteristics are "
                "the more fundamental structure and the right tool on the infinite line."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational and procedural errors", (
                "ERROR 1: Wrong sign in the characteristic label. From "
                "$\\frac{dx}{dt}=c$ you get $x-ct=\\text{const}$, so the label is "
                "$\\xi=x-ct$ and the solution $f(x-ct)$. Writing $\\xi=x+ct$ reverses the "
                "wave's direction. Derive it from $\\frac{dx}{dt}$ every time rather than "
                "guessing.\n\n"
                "ERROR 2: Forgetting to back-substitute the label. Students solve the ODE "
                "and leave the answer in terms of $\\xi$, e.g. 'u = 3t + \\xi', calling it "
                "done. You MUST replace $\\xi$ with its $(x,t)$ expression to get a genuine "
                "solution $u(x,t)$. A leftover $\\xi$ is not an answer.\n\n"
                "ERROR 3: Applying the initial condition at the wrong point. The foot of "
                "the characteristic labelled $\\xi$ is at $t=0$, where $x=\\xi$ (straight "
                "case). Plugging $f(x)$ instead of $f(\\xi)$ mixes current position with "
                "starting position and corrupts the whole solution.\n\n"
                "ERROR 4: Mishandling a position-dependent source. For "
                "$u_t+u_x=x$, the right side is $x$, but along the characteristic "
                "$x=\\xi+t$, so $\\frac{du}{dt}=\\xi+t$ — you must express $x$ in terms of "
                "$t$ and $\\xi$ BEFORE integrating. Integrating '$x$' as if it were "
                "constant in $t$ is wrong.\n\n"
                "ERROR 5: Treating a quasilinear speed as constant. In $u_t+uu_x=0$ the "
                "speed is $u$, which differs from characteristic to characteristic. You "
                "cannot factor out one global speed; each characteristic has slope set by "
                "its own carried value.\n\n"
                "ERROR 6: Skipping verification. The PDE and the initial condition give two "
                "independent checks: plug $u(x,t)$ into $a u_t + b u_x$ and confirm it "
                "equals $c$, and confirm $u(x,0)=f(x)$. These take thirty seconds and catch "
                "sign errors, missing factors, and back-substitution slips almost every "
                "time."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: what rides the characteristics", (
                "1. ADVECTION OF POLLUTANTS AND TRACERS. A dye or contaminant carried by a "
                "steady river current $c$ obeys $u_t + c u_x = 0$ (plus decay/reaction "
                "terms as needed). The concentration profile travels downstream at the flow "
                "speed, and the arrival time at a downstream sensor is read straight off the "
                "characteristic. Add $-\\lambda u$ for chemical decay and the pulse fades as "
                "it goes (page 7).\n\n"
                "2. TRAFFIC FLOW. Car density $\\rho$ obeys a conservation law "
                "$\\rho_t + q(\\rho)_x = 0$; linearizing gives transport, and the full "
                "nonlinear model is quasilinear like Burgers. Characteristics ARE the "
                "trajectories along which traffic information propagates; where they cross, "
                "a shock — the sharp back edge of a traffic jam — forms. Drivers "
                "decelerating abruptly at a jam's edge are literally hitting a "
                "characteristic shock.\n\n"
                "3. ACOUSTICS AND SIGNALS. Small sound disturbances satisfy the wave "
                "equation; its two characteristic families are the leftward- and "
                "rightward-travelling sound fronts. Sonar ranging, echo timing, and the "
                "very notion of a 'wavefront' are characteristic geometry. When "
                "disturbances are not small, characteristics steepen and sound waves shock "
                "up into sonic booms.\n\n"
                "4. GAS DYNAMICS AND SHOCK WAVES. Compressible flow equations are "
                "hyperbolic systems; their characteristics carry pressure and velocity "
                "information, and their crossing produces the shocks of supersonic flight "
                "and explosions. The breaking-time idea of page 10 is the elementary model "
                "of shock formation.\n\n"
                "5. NUMERICAL SCHEMES. Because information travels along characteristics at "
                "finite speed, stable difference schemes for hyperbolic PDEs must respect "
                "that speed (the CFL condition) and 'look upwind,' toward where the "
                "information is coming from — the subject taken up concretely in "
                "PF.U8.N2."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: a decaying river plume", (
                "A chemical spill enters a straight river at $x=0$, $t=0$. The current "
                "carries it downstream at $c=2$ km/hr, and the chemical decays with "
                "half-life $1$ hour, i.e. $\\lambda=\\ln 2\\approx0.693$ per hour. The "
                "initial concentration profile is $u(x,0)=100\\,e^{-x^2}$ (a sharp plume "
                "centered at the spill). Find $u(x,t)$ and locate and size the plume at "
                "$t=3$ hours.\n\n"
                "MODEL: advection with linear decay, $u_t + 2u_x = -\\lambda u$. From page "
                "7's template, characteristics are $\\xi = x - 2t$ and "
                "$u = e^{-\\lambda t}f(\\xi)$, so\n"
                "$$u(x,t) = 100\\,e^{-\\lambda t}\\,e^{-(x-2t)^2}.$$\n"
                "VERIFY the structure: at $t=0$ this is $100e^{-x^2}$; the "
                "$e^{-(x-2t)^2}$ factor rides right at speed $2$; the $e^{-\\lambda t}$ "
                "factor shrinks the whole thing uniformly.\n\n"
                "AT $t=3$ HOURS. The plume CENTER is where the exponent $(x-2t)^2$ "
                "vanishes: $x = 2\\cdot3 = 6$ km downstream. The PEAK concentration is\n"
                "$$100\\,e^{-\\lambda\\cdot3} = 100\\,e^{-3\\ln 2} = "
                "100\\cdot 2^{-3} = 100/8 = 12.5,$$\n"
                "using that three half-lives cut the amount to one-eighth. So after 3 "
                "hours the plume is centered 6 km downstream with peak concentration "
                "$12.5$ (down from $100$), and it has kept its Gaussian SHAPE exactly — "
                "advection-decay translates and scales but does not spread (that would "
                "require a diffusion term $u_{xx}$). A monitoring station at $x=6$ km should "
                "expect the peak arrival at $t=3$ h and a reading near $12.5$. Every number "
                "here came from riding the characteristic and integrating the decay ODE "
                "along it — the method delivering an operational answer."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections", (
                "BACKWARD (what this rested on):\n"
                "• PF.U1.N2 (PDE classification): first-order and wave equations are the "
                "HYPERBOLIC type, defined by real characteristic directions along which "
                "information propagates — exactly the curves of this chapter. Elliptic and "
                "parabolic types have no such real characteristics, which is why they need "
                "the Fourier/eigenfunction machinery instead.\n"
                "• Single-variable ODEs and the multivariable chain rule: the entire "
                "method is 'the chain rule reduces a PDE to an ODE.'\n\n"
                "FORWARD:\n"
                "• PF.U8.N2 (the transport equation): the pure $u_t+cu_x=0$ case and its "
                "variants — decay, sources, and the Burgers nonlinearity — studied as a "
                "numerical and conceptual testbed. This chapter supplies the geometry; the "
                "next supplies the depth and the numerics.\n"
                "• The wave equation and d'Alembert (revisited wherever hyperbolic problems "
                "appear): the factorization here is the analytic backbone; on bounded "
                "domains it meets the Fourier/eigenfunction viewpoint of PF.U5.N1 and "
                "PF.U7.N3, and the two pictures (travelling waves vs standing modes) are "
                "reconciled.\n"
                "• PDEM and PDET (advanced modeling and theory): conservation laws, "
                "Rankine-Hugoniot shock conditions, entropy solutions, and hyperbolic "
                "systems all extend the crossing-characteristics story of page 10 into a "
                "full theory.\n\n"
                "CONTRAST: hold this chapter next to the heat equation. Diffusion has "
                "INFINITE propagation speed and instant smoothing (parabolic); transport "
                "has FINITE speed and perfect shape preservation, even sharpening to shocks "
                "(hyperbolic). Knowing which behavior your PDE has — by reading its "
                "characteristics — is the strategic core of PDE problem-solving."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: geometry, systems, and weak solutions", (
                "The method of characteristics is the tip of a rich geometric theory. For "
                "a FULLY GENERAL first-order PDE $F(x,t,u,u_x,u_t)=0$ — even nonlinear in "
                "the derivatives — there is a characteristic system (Charpit's / Lagrange's "
                "equations, or in the Hamiltonian picture, Hamilton's equations) of FIVE "
                "coupled ODEs for $x,t,u,u_x,u_t$. The Hamilton-Jacobi equation of "
                "classical mechanics is exactly a first-order PDE whose characteristics are "
                "the trajectories of the mechanical system — the deep reason particle paths "
                "and wavefronts are two views of one geometry (the seed of the "
                "ray/wave duality that resurfaces in optics and quantum mechanics).\n\n"
                "HYPERBOLIC SYSTEMS. Replace the scalar $u$ by a vector "
                "$\\mathbf{u}=(u_1,\\ldots,u_n)$ and the coefficient by a matrix: "
                "$\\mathbf{u}_t + A\\,\\mathbf{u}_x = 0$. If $A$ has $n$ real eigenvalues "
                "and a full set of eigenvectors, the system is hyperbolic and has $n$ "
                "characteristic families, one per eigenvalue — the eigenvalues ARE the "
                "propagation speeds. Diagonalizing $A$ decouples the system into $n$ scalar "
                "transport equations. This is how the equations of gas dynamics, "
                "elasticity, and electromagnetism are analyzed, and it ties this chapter "
                "back to the eigenvalue geometry of LA.U8.N2.\n\n"
                "WEAK SOLUTIONS AND ENTROPY. Once characteristics cross (page 10), no "
                "smooth solution exists, and mathematics must widen its notion of "
                "'solution' to allow jumps. A WEAK solution satisfies the conservation law "
                "in an integral sense; the Rankine-Hugoniot condition fixes the shock "
                "speed, and an ENTROPY condition selects the physically correct one among "
                "many candidates. This is a whole modern field (PDET), but its motivating "
                "question — what happens when characteristics collide — is one you can now "
                "pose precisely, having watched a gradient blow up in finite time on "
                "page 11."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "THE PDE. First-order linear: $a u_t + b u_x = c$, data $u(x,0)=f(x)$.\n\n"
                "CHARACTERISTIC SYSTEM. "
                "$\\frac{dt}{ds}=a,\\;\\frac{dx}{ds}=b,\\;\\frac{du}{ds}=c$; for $a=1$, "
                "$\\frac{dx}{dt}=b$ (where to walk) and $\\frac{du}{dt}=c$ (how $u$ "
                "changes). Derivation: the chain rule "
                "$\\frac{du}{dt}=u_t+\\frac{dx}{dt}u_x$ matches the PDE when "
                "$\\frac{dx}{dt}=b/a$.\n\n"
                "RECIPE. (1) Solve $\\frac{dx}{dt}=b/a$; write the label $\\xi$. (2) Solve "
                "$\\frac{du}{dt}=c/a$ along it with the foot value $f(\\xi)$. (3) "
                "Back-substitute $\\xi=\\xi(x,t)$. (4) Verify in the PDE and at $t=0$.\n\n"
                "KEY SOLUTIONS.\n"
                "$$u_t+cu_x=0 \\Rightarrow u=f(x-ct)\\ (\\text{rides right, undistorted}),$$\n"
                "$$u_t+cu_x=-\\lambda u \\Rightarrow u=e^{-\\lambda t}f(x-ct)\\ "
                "(\\text{fades}),$$\n"
                "$$u_{tt}=c^2u_{xx} \\Rightarrow u=F(x-ct)+G(x+ct)\\ "
                "(\\text{two families}).$$\n\n"
                "WAVE FACTORIZATION. "
                "$\\partial_{tt}-c^2\\partial_{xx}=(\\partial_t-c\\partial_x)"
                "(\\partial_t+c\\partial_x)$; d'Alembert: "
                "$u=\\tfrac12[\\phi(x-ct)+\\phi(x+ct)]+\\frac{1}{2c}\\int_{x-ct}^{x+ct}"
                "\\psi$.\n\n"
                "SHOCKS. For $u_t+uu_x=0$, characteristics are straight with slope $=u$; "
                "a decreasing profile makes them cross, and the smooth solution breaks at "
                "$t^*=-1/\\min f'(x_0)$ (with $u_x\\to\\infty$ there).\n\n"
                "SIGN ANCHOR. $+c$ means motion to the RIGHT and argument $x-ct$; derive "
                "the label from $\\frac{dx}{dt}$ rather than memorizing.\n\n"
                "ONE-LINE TAKEAWAY. A first-order PDE is an ODE in disguise — find the "
                "curves along which the disguise drops, ride the data along them, and read "
                "off the answer."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all seven before reading the solutions.\n\n"
                "1. Solve $u_t + 4u_x = 0$, $u(x,0)=\\frac{1}{1+x^2}$.\n"
                "2. Solve $u_t - u_x = 0$, $u(x,0)=e^{x}$; which way does it move?\n"
                "3. Solve $u_t + u_x = 2$, $u(x,0)=x^2$.\n"
                "4. Solve $u_t + 3u_x = -u$, $u(x,0)=\\cos x$.\n"
                "5. Write the general solution of $u_{tt}=9u_{xx}$ and name the two "
                "speeds.\n"
                "6. For $u_t+uu_x=0$ with $u(x,0)$ having $\\min f'(x_0)=-\\tfrac12$, when "
                "does a shock first form?\n"
                "7. True or false: $u_t+cu_x=0$ has solution $f(x+ct)$.\n\n"
                "SOLUTIONS.\n"
                "1. $\\xi=x-4t$, $u$ constant: "
                "$u=\\frac{1}{1+(x-4t)^2}$.\n"
                "2. $\\frac{dx}{dt}=-1\\Rightarrow\\xi=x+t$; $u=e^{x+t}$. Moves LEFT "
                "(speed $1$ in $-x$).\n"
                "3. $\\xi=x-t$; $\\frac{du}{dt}=2\\Rightarrow u=2t+f(\\xi)=2t+(x-t)^2$. "
                "Check: $u_t=2+2(x-t)(-1)$, $u_x=2(x-t)$, $u_t+u_x=2$. Good.\n"
                "4. $\\xi=x-3t$; $\\frac{du}{dt}=-u\\Rightarrow u=e^{-t}\\cos(x-3t)$.\n"
                "5. $c=3$: $u=F(x-3t)+G(x+3t)$; speeds $+3$ (right) and $-3$ (left).\n"
                "6. $t^*=-1/(-\\tfrac12)=2$.\n"
                "7. FALSE — the solution is $f(x-ct)$ (minus). $f(x+ct)$ solves "
                "$u_t-cu_x=0$.\n\n"
                "Scoring: 7/7 — proceed to the transport chapter. 5-6 — reread the sign "
                "discussion (pages 5, 12, 13). $\\le4$ — rework the recipe (pages 3-5) with "
                "verification each time."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Check each box honestly before moving on.\n\n"
                "$\\square$ Derive the characteristic condition $\\frac{dx}{dt}=b/a$ from "
                "the chain rule, explaining why the PDE becomes an ODE along it.\n"
                "$\\square$ Execute the three-step recipe (label, value ODE, "
                "back-substitute) on a constant-coefficient transport problem and verify "
                "the answer in the PDE.\n"
                "$\\square$ Get the direction and sign right: relate $+c$ to rightward "
                "motion and the argument $x-ct$ without guessing.\n"
                "$\\square$ Handle a source term and a decay term, integrating the value "
                "ODE correctly (including position-dependent sources).\n"
                "$\\square$ Solve a variable-coefficient problem with CURVING "
                "characteristics (e.g. $u_t+xu_x=0$).\n"
                "$\\square$ Factor the wave operator into two transport operators and write "
                "the general solution $F(x-ct)+G(x+ct)$.\n"
                "$\\square$ Derive d'Alembert's formula via characteristic coordinates "
                "$\\xi=x-ct$, $\\eta=x+ct$, and specialize it to a concrete initial "
                "profile.\n"
                "$\\square$ Explain how quasilinear characteristics ($b=u$) can cross, and "
                "compute a breaking time $t^*=-1/\\min f'$.\n"
                "$\\square$ Show, on a worked example, a gradient blowing up in finite time "
                "as characteristics converge.\n"
                "$\\square$ Explain why characteristics are the natural tool for hyperbolic "
                "PDEs and Fourier methods for parabolic/elliptic ones.\n\n"
                "When every box is checked, hit Practice until finding characteristics and "
                "riding data along them is automatic. Then proceed to PF.U8.N2, where the "
                "transport equation becomes a laboratory for decay, sources, shocks, and "
                "numerical methods."
            )),
        ],
    },
    "PF.U8.N2": {
        "summary": (
            "A complete course on the transport equation $u_t + c u_x = 0$: the solution "
            "$u=f(x-ct)$ that rides the initial profile right at speed $c$ undistorted; "
            "the variations (decay $\\to e^{-\\lambda t}f$, sources integrated along "
            "characteristics, Burgers $u_t+uu_x=0$ steepening into shocks); and its "
            "central role as the numerical testbed for hyperbolic PDEs."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "The transport (or advection) equation "
                "$$u_t + c\\,u_x = 0$$ is the simplest PDE that is genuinely a PDE. It has "
                "no equilibrium, no smoothing, no eigenvalues to hunt — just pure carrying. "
                "Yet it is the single most important model to understand deeply, for two "
                "reasons. First, it is the elementary atom of ALL wave and flow phenomena: "
                "the wave equation is two transport equations back to back, gas dynamics is "
                "a system of them, and every conservation law reduces to transport when "
                "linearized. Second, it is the universal TESTBED for numerical PDE methods "
                "— if your scheme cannot move a bump across a grid without smearing or "
                "wobbling, it cannot be trusted on anything harder.\n\n"
                "The solution is almost insultingly simple: $u(x,t)=f(x-ct)$. The initial "
                "profile $f$ slides rigidly to the right at speed $c$, keeping its exact "
                "shape forever. No spreading (that would need diffusion), no decay (that "
                "would need a reaction term), no distortion. A photograph of the solution "
                "at time $t$ is the initial photograph shifted by $ct$.\n\n"
                "But this simplicity is a springboard. Small modifications unlock a whole "
                "zoo: add $-\\lambda u$ and the profile FADES as it rides; add a source "
                "$S(x,t)$ and you INTEGRATE along the moving frame; let the speed depend on "
                "$u$ itself ($u_t+uu_x=0$, Burgers) and the profile STEEPENS until it "
                "breaks into a shock. Each variation teaches one new mechanism, and "
                "together they cover the essential behaviors of hyperbolic PDEs.\n\n"
                "In this chapter you will master the base solution and its verification, "
                "the three canonical variations (decay, source, nonlinear steepening), the "
                "shock that ends the smooth story, and the role of transport as the "
                "proving ground for numerical schemes — the CFL condition and the upwind "
                "method. It is a small equation with an enormous reach."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "This chapter builds directly on characteristics; make sure the following "
                "are solid.\n\n"
                "1. THE METHOD OF CHARACTERISTICS (PF.U8.N1). You must know that "
                "$u_t+cu_x=0$ has characteristics $x-ct=\\text{const}$ along which $u$ is "
                "constant, and be able to derive $u=f(x-ct)$ from that fact. If the "
                "phrase 'ride the data along the characteristic' is not yet second nature, "
                "review the previous chapter first.\n\n"
                "2. THE MULTIVARIABLE CHAIN RULE, for verification: "
                "$\\frac{d}{dt}f(x-ct) = f'(x-ct)\\cdot(-c)$ and "
                "$\\frac{d}{dx}f(x-ct)=f'(x-ct)$. You will use these constantly to confirm "
                "solutions.\n\n"
                "3. BASIC INTEGRATION ALONG A PATH, for source terms: given "
                "$\\frac{du}{dt}=S$ where $S$ may depend on $t$ (and, via the "
                "characteristic, on the starting label), integrate in $t$ holding the "
                "label fixed.\n\n"
                "4. GRAPH TRANSLATION. Recognize that the graph of $f(x-ct)$ is the graph "
                "of $f(x)$ shifted RIGHT by $ct$. You should be able to sketch a box, a "
                "triangle, or a Gaussian and slide it.\n\n"
                "Quick self-check (answers follow):\n\n"
                "• What is $\\frac{\\partial}{\\partial t}\\cos(x-5t)$? "
                "($5\\sin(x-5t)$, via the chain rule with inner derivative $-5$.)\n"
                "• The graph of $g(x-6)$ is the graph of $g$ shifted how? (Right by $6$.)\n"
                "• Solve $\\frac{du}{dt}=-3u$, $u(0)=A$. ($u=Ae^{-3t}$ — the decay "
                "template.)\n\n"
                "If any felt shaky, patch it now; the whole chapter is these three moves "
                "(shift, verify by chain rule, integrate a small ODE) recombined."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Think of a long, straight conveyor belt moving right at constant speed "
                "$c$. At time zero someone paints a pattern $f(x)$ on the belt. As the belt "
                "moves, the pattern moves WITH it, rigidly. To know the color at position "
                "$x$ at time $t$, ask: which spot of the belt is at $x$ now? It is the spot "
                "that was at $x-ct$ at time zero (it has since travelled forward by $ct$). "
                "So $u(x,t)=f(x-ct)$. That is the transport equation, in a sentence: the "
                "pattern rides the belt.\n\n"
                "Why does 'ride the belt' correspond to $u_t+cu_x=0$? The equation says the "
                "TOTAL rate of change of $u$, as seen by an observer moving with the belt at "
                "speed $c$, is zero. Precisely: an observer whose position is $x(t)=x_0+ct$ "
                "sees $\\frac{d}{dt}u(x_0+ct,t)=u_t + c\\,u_x = 0$. The moving observer "
                "measures NO change — the color under their feet stays fixed because they "
                "are riding along with it. Standing observers, of course, see the pattern "
                "sweep past.\n\n"
                "This is the physical content of 'constant along characteristics.' The "
                "characteristics $x-ct=\\text{const}$ are the belt's worldlines — the "
                "space-time trajectories of individual belt spots. $u$ is constant on each "
                "because each belt spot carries its color unchanged.\n\n"
                "Everything else in this chapter perturbs the belt story. What if the paint "
                "FADES as it rides (evaporating dye)? Then $u=e^{-\\lambda t}f(x-ct)$ — same "
                "ride, dimming color. What if paint is continuously ADDED along the way (a "
                "leaky nozzle)? Then you accumulate the added amount along the belt's path — "
                "an integral. What if FAST spots move faster than slow spots (color-"
                "dependent speed)? Then the pattern distorts, the fast paint overtakes the "
                "slow, and a sharp edge — a shock — forms. Hold the conveyor-belt image "
                "fixed and each variation becomes intuitive."
            )),
            # ---------------------------------------------------------- 4
            (L, "The transport equation, formally", (
                "THE PROBLEM. Constant-coefficient linear transport on the whole line:\n"
                "$$u_t + c\\,u_x = 0, \\qquad x\\in\\mathbb{R},\\; t>0, \\qquad "
                "u(x,0)=f(x).$$\n"
                "THE SOLUTION. $u(x,t)=f(x-ct)$, valid for any (differentiable) $f$.\n\n"
                "DERIVATION via characteristics. The characteristic ODE is "
                "$\\frac{dx}{dt}=c$, giving lines $x=ct+\\xi$, i.e. label $\\xi=x-ct$. Along "
                "each line $\\frac{du}{dt}=0$, so $u$ equals its value at the foot $t=0$, "
                "where $x=\\xi$: that value is $f(\\xi)$. Back-substitute: "
                "$u=f(x-ct)$.\n\n"
                "VERIFICATION (worth doing once in full generality). Let $u=f(x-ct)$ and "
                "write $w=x-ct$ for the argument. By the chain rule "
                "$u_t = f'(w)\\cdot\\frac{\\partial w}{\\partial t} = f'(w)\\cdot(-c) = "
                "-c f'(w)$, and $u_x = f'(w)\\cdot 1 = f'(w)$. Then\n"
                "$$u_t + c\\,u_x = -cf'(w) + c f'(w) = 0,$$\n"
                "for EVERY $f$. And at $t=0$, $u=f(x)$. So $f(x-ct)$ solves both the PDE and "
                "the initial condition — and, because the characteristic through each point "
                "is unique, it is the ONLY solution.\n\n"
                "READING THE STRUCTURE. Three facts fall out. (1) SHAPE PRESERVATION: the "
                "solution is a rigid translate of $f$; the transport equation neither "
                "smooths nor sharpens. (2) FINITE SPEED / DOMAIN OF DEPENDENCE: "
                "$u(x,t)$ depends on the initial data at the single point $x-ct$ — nowhere "
                "else. A change to $f$ far away cannot be felt at $(x,t)$ unless a "
                "characteristic connects them. (3) DIRECTION: $c>0$ moves right, $c<0$ "
                "left, and the argument is always $x-ct$ (the minus is the 'look back to "
                "where it came from' sign). These three properties are the DEFINING "
                "signature of hyperbolic transport, and every variation in this chapter is "
                "measured against them."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples", (
                "EXAMPLE 1 (a moving box). Solve $u_t + 5u_x=0$ with "
                "$$u(x,0)=\\begin{cases}1 & 0\\le x\\le 2\\\\ 0 & \\text{otherwise},"
                "\\end{cases}$$ a unit box on $[0,2]$. The solution is the box shifted "
                "right by $5t$: $u(x,t)=1$ when $0\\le x-5t\\le 2$, i.e. on "
                "$[5t,\\,2+5t]$, and $0$ elsewhere. At $t=2$ the box occupies $[10,12]$. "
                "Note the shape is UNCHANGED — still a width-2 unit box — just relocated. "
                "(The corners make $f$ non-differentiable there; the transport equation "
                "carries even such 'weak' profiles rigidly, which is why sharp fronts stay "
                "sharp.)\n\n"
                "EXAMPLE 2 (a moving Gaussian, with full verification). Solve "
                "$u_t + 2u_x=0$, $u(x,0)=e^{-x^2}$. Solution: $u=e^{-(x-2t)^2}$. VERIFY: "
                "with $w=x-2t$, $u_t = e^{-w^2}(-2w)(-2)=4w\\,e^{-w^2}$ and "
                "$u_x = e^{-w^2}(-2w)=-2w\\,e^{-w^2}$, so "
                "$u_t+2u_x = 4w e^{-w^2} - 4w e^{-w^2}=0$. The bump glides right at speed "
                "$2$; at $t=3$ its peak sits at $x=6$.\n\n"
                "EXAMPLE 3 (leftward, and reading off the speed). Solve $u_t - 3u_x=0$, "
                "$u(x,0)=\\sin x$. Here $c=-3$: the equation is $u_t + (-3)u_x=0$, so "
                "$u=f(x-ct)=\\sin(x-(-3)t)=\\sin(x+3t)$. The sine wave travels LEFT at "
                "speed $3$; the argument $x+3t$ is the signature of leftward motion. VERIFY: "
                "$u_t=3\\cos(x+3t)$, $u_x=\\cos(x+3t)$, $u_t-3u_x = 3\\cos - 3\\cos=0$. "
                "Across all three examples the lesson repeats: identify $c$ (with sign), "
                "shift the initial profile by $ct$, and confirm with the chain rule. The "
                "transport equation asks nothing more."
            )),
            # ---------------------------------------------------------- 6
            (L, "Properties and the family of variations", (
                "The base equation is a hub; four variations branch from it, each adding "
                "one term and one new behavior.\n\n"
                "PURE TRANSPORT: $u_t+cu_x=0 \\Rightarrow u=f(x-ct)$. Rides right, "
                "undistorted. The reference behavior.\n\n"
                "TRANSPORT WITH DECAY (reaction): $u_t+cu_x=-\\lambda u \\Rightarrow "
                "u=e^{-\\lambda t}f(x-ct)$. Along each characteristic the value obeys "
                "$\\frac{du}{dt}=-\\lambda u$, so it decays exponentially while riding. "
                "Same speed, fading amplitude. Replacing $-\\lambda$ by $+\\lambda$ gives "
                "exponential GROWTH instead.\n\n"
                "TRANSPORT WITH A SOURCE: $u_t+cu_x=S(x,t) \\Rightarrow$ along the "
                "characteristic $x=\\xi+ct$, $\\frac{du}{dt}=S(\\xi+ct,\\,t)$, so\n"
                "$$u(x,t)=f(x-ct)+\\int_0^t S\\big(x-c(t-\\tau),\\,\\tau\\big)\\,d\\tau.$$\n"
                "The homogeneous ride plus the accumulated source, integrated ALONG the "
                "moving path (note the argument $x-c(t-\\tau)$ tracks the characteristic "
                "backward). This 'integrate the forcing along the characteristic' is the "
                "hyperbolic analogue of Duhamel's principle.\n\n"
                "NONLINEAR TRANSPORT (BURGERS): $u_t + u\\,u_x=0$. The speed is $u$ itself. "
                "Characteristics $x=x_0+f(x_0)t$ are straight but with data-dependent "
                "slopes; $u$ is still constant along each. Tall parts move faster than "
                "short parts, so a profile that decreases somewhere STEEPENS, and where "
                "characteristics cross a SHOCK forms (pages 10-11). This is the gateway "
                "from linear to nonlinear PDEs.\n\n"
                "The organizing insight: in EVERY variation the characteristic is still "
                "$dx/dt=c$ (or $=u$ for Burgers), and all the new physics lives in the "
                "VALUE ODE $\\frac{du}{dt}=(\\text{RHS})$ solved along that characteristic. "
                "Master 'solve the value ODE along the ride' and you have mastered the "
                "whole family."
            )),
            # ---------------------------------------------------------- 7
            (E, "Standard worked examples: decay and sources", (
                "EXAMPLE 1 (transport with decay). Solve $u_t + 2u_x + u = 0$, "
                "$u(x,0)=\\sin x$. Rewrite as $u_t+2u_x=-u$: this is decay with $c=2$, "
                "$\\lambda=1$. Template gives $u=e^{-t}\\sin(x-2t)$. VERIFY: "
                "$u_t = -e^{-t}\\sin(x-2t)+e^{-t}\\cos(x-2t)(-2) = "
                "-e^{-t}\\sin(x-2t)-2e^{-t}\\cos(x-2t)$; $u_x=e^{-t}\\cos(x-2t)$, so "
                "$2u_x=2e^{-t}\\cos(x-2t)$. Then $u_t+2u_x = -e^{-t}\\sin(x-2t) = -u$. "
                "Correct: a right-travelling sine whose amplitude decays as $e^{-t}$.\n\n"
                "EXAMPLE 2 (a position-dependent source). Solve $u_t + u_x = x$ with "
                "$u(x,0)=0$. Characteristic: $\\xi=x-t$, so along it $x=\\xi+t$. The value "
                "ODE is $\\frac{du}{dt}=x=\\xi+t$ (express the source in terms of $t$ and "
                "the fixed label!). Integrate from the foot: "
                "$u=\\xi t+\\tfrac12 t^2$ (constant $0$ since $u=0$ at $t=0$). "
                "Back-substitute $\\xi=x-t$: "
                "$$u=(x-t)t+\\tfrac12 t^2 = xt - t^2 + \\tfrac12 t^2 = xt-\\tfrac12 t^2.$$\n"
                "VERIFY: $u_t=x-t$, $u_x=t$, so $u_t+u_x = x-t+t = x$; and $u(x,0)=0$. "
                "Correct. Notice the crucial step: because the source $x$ VARIES along the "
                "characteristic, we substituted $x=\\xi+t$ BEFORE integrating — treating "
                "$x$ as constant would have given the wrong $\\tfrac12$ coefficient.\n\n"
                "EXAMPLE 3 (source via the integral formula). Solve $u_t + 3u_x = e^{-t}$, "
                "$u(x,0)=0$, using "
                "$u=\\int_0^t S(x-3(t-\\tau),\\tau)\\,d\\tau$. Here $S=e^{-\\tau}$ is "
                "independent of position, so "
                "$u=\\int_0^t e^{-\\tau}d\\tau = 1-e^{-t}$. VERIFY: $u_t=e^{-t}$, $u_x=0$, "
                "$u_t+3u_x=e^{-t}$. The forcing accumulates along the ride; here the answer "
                "happens to be position-independent because the source is."
            )),
            # ---------------------------------------------------------- 8
            (L, "Techniques and strategy", (
                "A checklist for any transport-type problem.\n\n"
                "STEP 1 — CLASSIFY THE RIGHT-HAND SIDE. Homogeneous ($0$) $\\Rightarrow$ "
                "rigid shift $f(x-ct)$. Proportional to $u$ ($-\\lambda u$) $\\Rightarrow$ "
                "multiply by $e^{-\\lambda t}$. A given function $S(x,t)$ $\\Rightarrow$ "
                "integrate along the characteristic. Speed equal to $u$ $\\Rightarrow$ "
                "nonlinear/Burgers, watch for shocks.\n\n"
                "STEP 2 — FIND THE CHARACTERISTIC AND ITS FOOT. For constant $c$: "
                "$\\xi=x-ct$, foot at $(\\xi,0)$. Keep $\\xi$ as the fixed label while you "
                "work on a single characteristic.\n\n"
                "STEP 3 — SOLVE THE VALUE ODE ALONG THE RIDE. Homogeneous: $u$ constant "
                "$=f(\\xi)$. Decay: $u=f(\\xi)e^{-\\lambda t}$. Source: "
                "$u=f(\\xi)+\\int_0^t S(\\xi+c\\tau,\\tau)\\,d\\tau$ — and REMEMBER to write "
                "the source's $x$-dependence as $\\xi+c\\tau$ before integrating.\n\n"
                "STEP 4 — BACK-SUBSTITUTE $\\xi=x-ct$ and simplify to $u(x,t)$.\n\n"
                "STEP 5 — VERIFY. Plug into the PDE (chain rule) and check the initial "
                "condition. For decay/source problems this instantly reveals a dropped "
                "factor or a mishandled variable source.\n\n"
                "FOR BURGERS AND NONLINEAR SPEED: draw the characteristics in the "
                "$(x,t)$ plane. If the initial profile is nondecreasing, they spread "
                "(rarefaction) and the smooth solution lasts forever; if it decreases "
                "anywhere, they converge and a shock forms at "
                "$t^*=-1/\\min f'(x_0)$. After the shock, abandon characteristics and use "
                "the Rankine-Hugoniot jump condition "
                "$s=\\frac{q(u_L)-q(u_R)}{u_L-u_R}$ (with flux $q=\\tfrac12 u^2$) for the "
                "shock speed.\n\n"
                "NUMERICAL AWARENESS: whatever the scheme, information moves at speed $c$; "
                "a stable explicit method must not let the numerical domain of dependence "
                "exclude the true one — the CFL condition of page 14."
            )),
            # ---------------------------------------------------------- 9
            (E, "Harder worked example: a source built up along the ride", (
                "Solve $u_t + u_x = x\\,t$ with $u(x,0)=\\cos x$, combining a nontrivial "
                "initial profile with a source that depends on BOTH position and time.\n\n"
                "SET UP THE CHARACTERISTIC. $\\frac{dx}{dt}=1$, so $\\xi=x-t$ and along the "
                "ride $x=\\xi+\\tau$ at time $\\tau$. The value ODE is "
                "$\\frac{du}{d\\tau}=x\\,\\tau=(\\xi+\\tau)\\tau=\\xi\\tau+\\tau^2$, with "
                "$u=\\cos\\xi$ at $\\tau=0$.\n\n"
                "INTEGRATE ALONG THE CHARACTERISTIC.\n"
                "$$u = \\cos\\xi + \\int_0^t (\\xi\\tau+\\tau^2)\\,d\\tau = "
                "\\cos\\xi + \\xi\\frac{t^2}{2} + \\frac{t^3}{3}.$$\n"
                "BACK-SUBSTITUTE $\\xi=x-t$:\n"
                "$$u(x,t) = \\cos(x-t) + (x-t)\\frac{t^2}{2} + \\frac{t^3}{3}.$$\n\n"
                "VERIFY carefully. Compute the pieces. "
                "$\\frac{\\partial}{\\partial t}\\cos(x-t)=\\sin(x-t)$; "
                "$\\frac{\\partial}{\\partial x}\\cos(x-t)=-\\sin(x-t)$. For the middle "
                "term $(x-t)\\frac{t^2}{2}$: "
                "$u_t$-part $=\\frac{\\partial}{\\partial t}\\big[(x-t)\\frac{t^2}{2}\\big] "
                "= -\\frac{t^2}{2} + (x-t)t$, and $u_x$-part $=\\frac{t^2}{2}$. For "
                "$\\frac{t^3}{3}$: $u_t$-part $=t^2$, $u_x$-part $=0$. Now add: "
                "$u_t = \\sin(x-t) - \\frac{t^2}{2} + (x-t)t + t^2$, and "
                "$u_x = -\\sin(x-t) + \\frac{t^2}{2}$. Sum:\n"
                "$$u_t+u_x = \\big(\\tfrac{-t^2}{2}+t^2+\\tfrac{t^2}{2}\\big) + (x-t)t = "
                "t^2 + xt - t^2 = xt.$$\n"
                "It matches the source $xt$, and $u(x,0)=\\cos x$. Correct. The lesson: "
                "with a space-and-time source, write the source entirely in terms of the "
                "label and $\\tau$, integrate, then restore $x$ and $t$ — and let the "
                "verification catch any slip in that bookkeeping."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: steepening and shocks in Burgers", (
                "Make the speed depend on the solution and the placid transport equation "
                "turns violent. The inviscid Burgers equation\n"
                "$$u_t + u\\,u_x = 0$$\n"
                "is the archetype. It is a conservation law $u_t+q(u)_x=0$ with flux "
                "$q(u)=\\tfrac12 u^2$. Characteristics satisfy $\\frac{dx}{dt}=u$ and carry "
                "$u$ unchanged, so they are straight lines whose slope equals the value "
                "they carry: $x=x_0+f(x_0)\\,t$.\n\n"
                "STEEPENING. Suppose $f$ is decreasing on some interval — the profile has a "
                "downslope. Then a point on the left carries a LARGER value (faster) than a "
                "point on its right (slower), so the left point catches up. The downslope "
                "gets steeper and steeper. In terms of characteristics, the lines "
                "emanating from the downslope CONVERGE.\n\n"
                "BREAKING. When two characteristics meet, they deliver two different "
                "constant values to one point: the solution would be multivalued — "
                "physically impossible for a density or velocity. The smooth solution ends. "
                "The first crossing happens at the BREAKING TIME "
                "$t^*=\\frac{-1}{\\min_{x_0}f'(x_0)}$, where the gradient $u_x$ becomes "
                "infinite (the profile goes vertical). For a nondecreasing $f$ there is no "
                "downslope, characteristics spread (a RAREFACTION fan), and the smooth "
                "solution lives forever.\n\n"
                "THE SHOCK. For $t>t^*$ the physically correct solution is a WEAK solution "
                "containing a jump discontinuity — a shock — that moves at the speed given "
                "by the Rankine-Hugoniot condition\n"
                "$$s = \\frac{q(u_L)-q(u_R)}{u_L-u_R} = \\frac{u_L+u_R}{2}$$\n"
                "for Burgers (the average of the left and right states). An additional "
                "ENTROPY condition ($u_L>u_R$ for a shock) picks the physical solution and "
                "forbids unphysical 'expansion shocks.' The full theory is PDET's; here the "
                "point is that the transport equation, once made nonlinear, spontaneously "
                "creates discontinuities from smooth data — the mathematical origin of "
                "shock waves."
            )),
            # ---------------------------------------------------------- 11
            (E, "Edge-case worked example: a compression shock", (
                "Track a Burgers profile from smooth steepening into a shock, with numbers. "
                "Take $u_t + u\\,u_x=0$ and the compressive initial data\n"
                "$$f(x)=\\begin{cases}1 & x\\le 0\\\\ 1-x & 0\\le x\\le 1\\\\ 0 & x\\ge 1,"
                "\\end{cases}$$\n"
                "a plateau at height $1$ that ramps DOWN to $0$ across $[0,1]$.\n\n"
                "CHARACTERISTICS. From $x_0\\le0$: speed $1$, lines $x=x_0+t$. From "
                "$x_0\\ge1$: speed $0$, vertical lines $x=x_0$. From $x_0\\in[0,1]$: speed "
                "$f(x_0)=1-x_0$, lines $x=x_0+(1-x_0)t$. The fast plateau chases the "
                "stationary zero region, and the ramp characteristics converge.\n\n"
                "BREAKING TIME. On the ramp $f(x_0)=1-x_0$, so $f'(x_0)=-1$, and "
                "$\\min f'=-1$. Thus $t^*=-1/(-1)=1$. Indeed all the ramp characteristics "
                "$x=x_0+(1-x_0)t$ pass through the single point $x=1$ at $t=1$: setting "
                "$t=1$ gives $x=x_0+(1-x_0)=1$ for every $x_0\\in[0,1]$. At $t=1$ the "
                "downslope has compressed to a vertical jump from $1$ to $0$ at $x=1$.\n\n"
                "THE SHOCK for $t>1$. A discontinuity separates $u_L=1$ (left) from "
                "$u_R=0$ (right). Its speed by Rankine-Hugoniot is\n"
                "$$s=\\frac{u_L+u_R}{2}=\\frac{1+0}{2}=\\frac12.$$\n"
                "So after $t=1$ the shock sits at $x=1+\\tfrac12(t-1)$, marching right at "
                "speed $\\tfrac12$ — slower than the plateau behind it (speed $1$) and "
                "faster than the still region ahead (speed $0$), which is exactly the "
                "entropy condition $u_L>u_R$ that certifies it as a genuine shock. This "
                "little problem contains the entire life cycle: smooth data, gradient "
                "steepening, finite-time breaking at $t^*=1$, and a shock propagating at "
                "the averaged speed. It is the transport equation showing its nonlinear "
                "teeth."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'The transport equation spreads or smooths the profile "
                "over time.' It does neither. $u=f(x-ct)$ is a RIGID translation; the "
                "shape at $t=10$ is identical to the shape at $t=0$, just moved. Spreading "
                "requires a second-derivative (diffusion) term $u_{xx}$; the pure transport "
                "equation has none.\n\n"
                "MISCONCEPTION 2: 'Larger $c$ means a taller or bigger solution.' $c$ is a "
                "SPEED, not an amplitude. Larger $c$ moves the same profile faster; the "
                "height is set entirely by $f$. Confusing speed with size is common when "
                "first reading $f(x-ct)$.\n\n"
                "MISCONCEPTION 3: 'A source term just adds $S$ to the answer.' No — the "
                "source is integrated ALONG the characteristic: "
                "$\\int_0^t S(x-c(t-\\tau),\\tau)\\,d\\tau$, not $S$ itself and not "
                "$\\int S\\,dt$ at fixed $x$. The forcing accumulates in the moving frame, "
                "and its spatial argument slides with the characteristic.\n\n"
                "MISCONCEPTION 4: 'Burgers is basically transport with $c$ replaced by a "
                "number.' The replacement is by $u$, which VARIES in space and time. That "
                "single change makes characteristics cross, produces shocks, and breaks "
                "linear superposition entirely. Nonlinearity is a difference in kind, not "
                "degree.\n\n"
                "MISCONCEPTION 5: 'Once a shock forms, the PDE has no solution.' The "
                "CLASSICAL (smooth) solution ceases to exist, but a WEAK solution — "
                "satisfying the conservation law in integral form, with a jump moving at "
                "the Rankine-Hugoniot speed — continues perfectly well and is the physically "
                "correct answer. 'No smooth solution' is not 'no solution.'\n\n"
                "MISCONCEPTION 6: 'Numerical schemes automatically handle the direction of "
                "flow.' They do not. A scheme that differences in the WRONG (downwind) "
                "direction is violently unstable; you must upwind — look toward where the "
                "information comes from — as page 14-15 shows."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational and procedural errors", (
                "ERROR 1: Sign of the shift. $u_t+cu_x=0$ (with $+c$) has solution "
                "$f(x-ct)$, not $f(x+ct)$. The profile moves toward larger $x$ for $c>0$. "
                "Anchor by checking a specific point: the crest initially at $x=0$ is at "
                "$x=ct$ later, so the argument must vanish there, forcing $x-ct$.\n\n"
                "ERROR 2: Chain-rule factor in verification. "
                "$\\frac{\\partial}{\\partial t}f(x-ct)=-c\\,f'(x-ct)$ — the inner "
                "derivative is $-c$, not $c$ or $1$. Dropping it makes the PDE seem "
                "unsatisfied and sends students hunting for nonexistent errors.\n\n"
                "ERROR 3: Treating a variable source as constant during integration. For "
                "$u_t+u_x=x$, along the characteristic $x=\\xi+t$, so the source is "
                "$\\xi+t$ — a function of the integration variable. Integrating '$x$' as if "
                "constant gives $u=xt$ instead of the correct $xt-\\tfrac12 t^2$. Always "
                "express the source via the label before integrating.\n\n"
                "ERROR 4: Forgetting the homogeneous (initial-data) part. The full "
                "source solution is $f(x-ct)+\\int_0^t S\\,d\\tau$; students sometimes "
                "report only the integral and lose the transported initial condition, or "
                "vice versa.\n\n"
                "ERROR 5: Wrong Rankine-Hugoniot flux. For Burgers the flux is "
                "$q=\\tfrac12 u^2$, so the shock speed is $\\frac{u_L+u_R}{2}$, NOT "
                "$\\frac{u_L-u_R}{2}$ and NOT $u_L$ or $u_R$. Using the transport speed "
                "$c$ (which does not exist for Burgers) is a frequent slip.\n\n"
                "ERROR 6: CFL violation in numerics. Choosing a time step so that the "
                "Courant number $\\nu=c\\,\\Delta t/\\Delta x$ exceeds $1$ makes an "
                "explicit upwind scheme blow up. Students often shrink $\\Delta x$ for "
                "accuracy but forget to shrink $\\Delta t$ proportionally, breaking "
                "stability. Check $\\nu\\le1$ before running."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications and the numerical testbed role", (
                "1. ADVECTIVE TRANSPORT IN NATURE AND ENGINEERING. Contaminants in rivers "
                "and air, heat carried by a moving fluid, salinity in ocean currents, drugs "
                "in bloodstream flow — all are, to leading order, advected quantities "
                "$u_t+cu_x=(\\text{reactions})$. The transport equation predicts arrival "
                "times and, with a decay term, remaining concentration (as in PF.U8.N1's "
                "plume example).\n\n"
                "2. TRAFFIC AND CROWD FLOW. Vehicle or pedestrian density obeys a "
                "conservation law that is Burgers-like; the transport equation is its "
                "linearization, and its shocks are the sharp upstream edges of jams. "
                "Signal-timing and evacuation models rest on this.\n\n"
                "3. THE UNIVERSAL NUMERICAL TESTBED. Because its exact solution is known "
                "(a rigid shift), the transport equation is THE benchmark for judging "
                "numerical schemes for hyperbolic PDEs. A method is tested by advecting a "
                "square pulse or Gaussian one revolution around a periodic domain and "
                "comparing to the exact translate. Two failure modes appear immediately: "
                "NUMERICAL DIFFUSION (the pulse smears and shortens — first-order upwind "
                "does this) and DISPERSION/OSCILLATION (spurious wiggles near sharp edges — "
                "central and higher-order schemes do this). Every serious CFD or "
                "weather-model scheme is first vetted on this one equation.\n\n"
                "4. THE CFL CONDITION. For the explicit upwind scheme "
                "$u_i^{n+1}=u_i^n-\\nu(u_i^n-u_{i-1}^n)$ with Courant number "
                "$\\nu=\\frac{c\\,\\Delta t}{\\Delta x}$, stability requires "
                "$0\\le\\nu\\le1$. The physical meaning is that information must not travel "
                "more than one grid cell per time step — the numerical domain of dependence "
                "must contain the true one. This Courant-Friedrichs-Lewy condition, "
                "discovered on exactly this equation, governs explicit schemes for all "
                "hyperbolic problems.\n\n"
                "5. UPWINDING. The scheme differences toward the direction the flow comes "
                "FROM ($u_{i-1}$ for $c>0$). Differencing the other way (downwind) is "
                "unconditionally unstable — a lesson the transport equation teaches with "
                "brutal clarity."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: one upwind step", (
                "Take $u_t + u_x=0$ ($c=1$), grid spacing $\\Delta x=0.5$, time step "
                "$\\Delta t=0.25$. Compute one step of the explicit upwind scheme and "
                "compare to the exact solution.\n\n"
                "COURANT NUMBER. $\\nu=\\frac{c\\,\\Delta t}{\\Delta x}=\\frac{1\\cdot "
                "0.25}{0.5}=0.5$. Since $0\\le\\nu\\le1$, the scheme is stable — good.\n\n"
                "THE SCHEME (upwind, $c>0$ so look left): "
                "$u_i^{n+1}=u_i^n-\\nu\\,(u_i^n-u_{i-1}^n)$. Suppose at time level $n$ the "
                "data is a step: $u_{i-1}^n=1$ and $u_i^n=0$ (the left cell is 'on,' the "
                "current cell 'off'). Then\n"
                "$$u_i^{n+1}=0-0.5\\,(0-1)=0.5.$$\n"
                "The value at cell $i$ rose from $0$ to $0.5$: information propagated from "
                "the left, exactly as $c>0$ demands. The upwind bias — using $u_{i-1}$, the "
                "cell the flow comes from — is what makes this physical and stable.\n\n"
                "COMPARE TO EXACT. The true solution shifts the step right at speed $1$; in "
                "one step it moves $c\\,\\Delta t=0.25$, which is HALF a grid cell "
                "($0.25/0.5=0.5$). A sharp step advanced by half a cell, when sampled back "
                "onto the grid, sits halfway between $0$ and $1$ at cell $i$ — i.e. $0.5$. "
                "The numerical value $0.5$ matches this linear interpolation of the exact "
                "shift. (For $\\nu=0.5$ upwind is quite diffusive; had we chosen $\\nu=1$ "
                "exactly, the scheme would shift the step perfectly by one whole cell with "
                "NO smearing — the special 'unit CFL' case where upwind is exact.)\n\n"
                "TRY $\\nu=2$ (say $\\Delta t=1$) and watch it break: "
                "$u_i^{n+1}=0-2(0-1)=2$, overshooting the data range $[0,1]$ — the seed of "
                "the exponential blow-up the CFL condition forbids. One step of arithmetic "
                "demonstrates the whole stability theory."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections", (
                "BACKWARD (what this rested on):\n"
                "• PF.U8.N1 (characteristic lines): supplied the derivation "
                "$u=f(x-ct)$, the constancy along $x-ct=\\text{const}$, and the "
                "crossing-characteristics mechanism behind shocks. This chapter is that "
                "geometry pushed into every variation.\n"
                "• PF.U1.N2 (PDE classification): transport and Burgers are the "
                "hyperbolic, finite-speed, shape-preserving (and shock-forming) end of the "
                "PDE spectrum — the polar opposite of the smoothing heat equation.\n\n"
                "FORWARD:\n"
                "• PDEM and PDET (advanced PDE modeling and theory): conservation laws, "
                "weak solutions, Rankine-Hugoniot and entropy conditions, and full "
                "numerical analysis (finite-volume methods, flux limiters) all grow from "
                "the Burgers example here.\n"
                "• The wave equation and d'Alembert (via PF.U8.N1): the wave equation is a "
                "pair of transport equations; understanding a single transported profile is "
                "prerequisite to understanding its left/right decomposition.\n\n"
                "CONTRAST WITH THE FOURIER SIDE:\n"
                "• PF.U5.N1 (separation of variables) and PF.U7.N3 (eigenfunction "
                "expansions) solve BOUNDED-domain diffusion and wave problems by summing "
                "standing modes. Transport on the infinite line is the complementary "
                "picture: travelling profiles, no modes, no smoothing. On a bounded "
                "periodic domain the two views meet — a travelling wave is a superposition "
                "of Fourier modes with matched phase speeds — and seeing both is the point "
                "of studying the whole PF sequence.\n\n"
                "• PF.U9.N1 (Green's functions): the source term "
                "$\\int_0^t S(x-c(t-\\tau),\\tau)d\\tau$ is already a Green's-function "
                "(impulse-response) construction for the transport operator — superposing "
                "the responses to forcing spread over space and time. The next chapter "
                "makes that impulse-response viewpoint the central object."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: conservation laws and viscosity", (
                "The transport equation is the doorway to the theory of CONSERVATION LAWS, "
                "$u_t + q(u)_x = 0$, which govern how a conserved quantity (mass, momentum, "
                "cars, charge) redistributes when its flux $q(u)$ depends on the local "
                "state. Linear flux $q=cu$ gives transport; convex flux $q=\\tfrac12u^2$ "
                "gives Burgers; realistic flux functions give traffic and gas-dynamics "
                "models. The integral (weak) form $\\frac{d}{dt}\\int_a^b u\\,dx = "
                "q(u(a))-q(u(b))$ survives even where the differential form fails at a "
                "shock — which is why weak solutions are the RIGHT notion once "
                "discontinuities appear.\n\n"
                "THE VISCOSITY CONNECTION. Real fluids have a little diffusion. The viscous "
                "Burgers equation $u_t+uu_x=\\varepsilon u_{xx}$ has smooth solutions for "
                "all time: the $\\varepsilon u_{xx}$ term arrests the steepening just before "
                "it becomes a true jump, replacing the shock with a thin, steep but smooth "
                "transition layer of width $\\sim\\varepsilon$. As $\\varepsilon\\to0$ these "
                "smooth solutions converge to the shock — the 'vanishing viscosity' "
                "solution, which is precisely the entropy-satisfying weak solution. "
                "Remarkably, viscous Burgers can be solved EXACTLY: the Cole-Hopf "
                "substitution $u=-2\\varepsilon\\,(\\ln\\phi)_x$ turns it into the LINEAR "
                "heat equation $\\phi_t=\\varepsilon\\phi_{xx}$ — a rare and beautiful "
                "bridge between the nonlinear/hyperbolic and linear/parabolic worlds.\n\n"
                "NUMERICS ECHO THE PHYSICS. The 'numerical diffusion' of the upwind scheme "
                "is a discrete $\\varepsilon u_{xx}$ that the scheme adds automatically; it "
                "is what lets upwind capture shocks without oscillating, at the cost of "
                "smearing them. Modern high-resolution schemes (flux limiters, ENO/WENO) "
                "are the art of adding just enough numerical viscosity near shocks and "
                "almost none in smooth regions. All of this — conservation, entropy, "
                "vanishing viscosity, shock-capturing — radiates from the little equation "
                "$u_t+cu_x=0$ and its nonlinear sibling. Small equation; entire discipline."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "BASE EQUATION. $u_t+cu_x=0$, $u(x,0)=f(x)$ $\\Rightarrow$ "
                "$u(x,t)=f(x-ct)$: the profile rides right at speed $c$, undistorted. "
                "Depends only on $f$ at $x-ct$ (finite speed, single-point domain of "
                "dependence).\n\n"
                "VARIATIONS.\n"
                "$$\\text{decay: } u_t+cu_x=-\\lambda u \\;\\Rightarrow\\; "
                "u=e^{-\\lambda t}f(x-ct);$$\n"
                "$$\\text{source: } u_t+cu_x=S \\;\\Rightarrow\\; "
                "u=f(x-ct)+\\int_0^t S(x-c(t-\\tau),\\tau)\\,d\\tau;$$\n"
                "$$\\text{Burgers: } u_t+uu_x=0 \\;\\Rightarrow\\; "
                "\\text{characteristics } x=x_0+f(x_0)t,\\ u=f(x_0).$$\n\n"
                "VERIFICATION. $\\frac{\\partial}{\\partial t}f(x-ct)=-cf'$, "
                "$\\frac{\\partial}{\\partial x}f(x-ct)=f'$; the $-c$ inner factor is "
                "essential.\n\n"
                "SHOCKS (Burgers). Decreasing $f$ $\\Rightarrow$ characteristics converge; "
                "breaking time $t^*=-1/\\min f'(x_0)$; afterward a shock at speed "
                "$s=\\frac{u_L+u_R}{2}$ (Rankine-Hugoniot with flux $\\tfrac12u^2$), valid "
                "when $u_L>u_R$ (entropy).\n\n"
                "NUMERICS. Upwind (for $c>0$): "
                "$u_i^{n+1}=u_i^n-\\nu(u_i^n-u_{i-1}^n)$, Courant "
                "$\\nu=c\\Delta t/\\Delta x$; STABLE iff $0\\le\\nu\\le1$ (CFL); exact when "
                "$\\nu=1$. Look upwind, toward incoming information.\n\n"
                "STRATEGY. (1) Classify RHS. (2) Characteristic $\\xi=x-ct$. (3) Solve the "
                "value ODE along the ride (source expressed via the label). (4) "
                "Back-substitute. (5) Verify in the PDE and at $t=0$.\n\n"
                "ONE-LINE TAKEAWAY. The transport equation is a rigidly moving picture; add "
                "decay to dim it, a source to build it up along the ride, or a "
                "state-dependent speed to steepen it into a shock — and it doubles as the "
                "benchmark every hyperbolic solver must pass."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all seven before reading the solutions.\n\n"
                "1. Solve $u_t + 4u_x=0$, $u(x,0)=e^{-x^2}$; where is the peak at "
                "$t=2$?\n"
                "2. Solve $u_t - 2u_x=0$, $u(x,0)=\\frac{1}{1+x^2}$; direction?\n"
                "3. Solve $u_t + u_x = -3u$, $u(x,0)=\\sin x$.\n"
                "4. Solve $u_t + u_x = t$, $u(x,0)=0$.\n"
                "5. For $u_t+uu_x=0$ with $\\min f'=-\\tfrac14$, find the breaking time.\n"
                "6. A Burgers shock has $u_L=3$, $u_R=1$; find its speed and check the "
                "entropy condition.\n"
                "7. Upwind scheme with $c=2$, $\\Delta x=0.1$: what is the largest stable "
                "$\\Delta t$?\n\n"
                "SOLUTIONS.\n"
                "1. $u=e^{-(x-4t)^2}$; peak where $x-4t=0$, so $x=8$ at $t=2$.\n"
                "2. $c=-2$: $u=\\frac{1}{1+(x+2t)^2}$, moving LEFT at speed $2$.\n"
                "3. Decay $\\lambda=3$, $c=1$: $u=e^{-3t}\\sin(x-t)$.\n"
                "4. $\\xi=x-t$; $\\frac{du}{dt}=t\\Rightarrow u=\\tfrac12 t^2$ (source "
                "independent of position). Check: $u_t=t$, $u_x=0$, sum $=t$. Good.\n"
                "5. $t^*=-1/(-\\tfrac14)=4$.\n"
                "6. $s=\\frac{u_L+u_R}{2}=\\frac{3+1}{2}=2$; entropy $u_L>u_R$ "
                "($3>1$) holds, so it is a valid shock.\n"
                "7. Need $\\nu=\\frac{c\\Delta t}{\\Delta x}\\le1$, so "
                "$\\Delta t\\le\\frac{\\Delta x}{c}=\\frac{0.1}{2}=0.05$.\n\n"
                "Scoring: 7/7 — you own the transport equation. 5-6 — revisit the source "
                "handling (pages 7, 9) and the CFL rule (pages 14-15). $\\le4$ — rework the "
                "base solution and its verification (pages 4-5) first."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Check each box honestly before moving on.\n\n"
                "$\\square$ Write down $u=f(x-ct)$ for $u_t+cu_x=0$ and verify it via the "
                "chain rule, getting the $-c$ inner factor right.\n"
                "$\\square$ Sketch the solution as a rigid translation and state the shape-"
                "preservation and finite-speed properties.\n"
                "$\\square$ Solve a transport-with-decay problem, producing the "
                "$e^{-\\lambda t}$ amplitude factor.\n"
                "$\\square$ Solve a transport-with-source problem, correctly expressing a "
                "position-and-time source along the characteristic before integrating.\n"
                "$\\square$ State and apply the source integral "
                "$\\int_0^t S(x-c(t-\\tau),\\tau)d\\tau$.\n"
                "$\\square$ Explain why Burgers characteristics have data-dependent slopes "
                "and can cross.\n"
                "$\\square$ Compute a breaking time $t^*=-1/\\min f'$ and a shock speed via "
                "Rankine-Hugoniot $s=\\frac{u_L+u_R}{2}$, checking the entropy condition.\n"
                "$\\square$ Explain the transport equation's role as a numerical testbed "
                "and name the two failure modes (numerical diffusion and dispersion).\n"
                "$\\square$ State the CFL condition $\\nu=c\\Delta t/\\Delta x\\le1$ and "
                "compute one upwind step by hand.\n"
                "$\\square$ Explain upwinding and why downwind differencing is unstable.\n\n"
                "When every box is checked, hit Practice until advecting a profile — with "
                "or without decay, sources, or nonlinearity — is routine. Then proceed to "
                "PF.U9.N1, where the impulse-response idea hiding in the source integral "
                "becomes the star: Green's functions."
            )),
        ],
    },
    "PF.U9.N1": {
        "summary": (
            "A complete course on Green's functions: the impulse response $G$ solving "
            "$L[G]=\\delta$ with the problem's boundary conditions; solving any forcing by "
            "superposition $u=\\int G(x,s)f(s)\\,ds$; constructing $G$ for two-point "
            "boundary-value problems by gluing homogeneous solutions with the correct "
            "derivative jump; symmetry/reciprocity; and the heat kernel as the "
            "time-dependent analogue. Caps the Fourier/PDE sequence."
        ),
        "steps": [
            # ---------------------------------------------------------- 1
            (L, "Where you are and why this matters", (
                "Throughout this sequence you have solved differential equations with "
                "specific forcing — a specific initial temperature, a specific source. "
                "Green's functions answer a more powerful question: can we solve a "
                "differential equation for EVERY POSSIBLE forcing, once and for all, by "
                "solving it for a single, idealized 'unit poke'? The answer is yes, and the "
                "tool is the Green's function — the impulse response of the differential "
                "operator.\n\n"
                "The idea is the continuous version of a principle you already trust for "
                "linear systems. If you know how a system responds to a single sharp "
                "impulse at one location, then because the system is LINEAR, its response to "
                "any distributed input is the superposition — the sum, or integral — of "
                "shifted, scaled copies of that single impulse response. Strike a bell once "
                "and you learn its tone; then any way of striking it is a combination of "
                "those tones. Green's functions make this exact for differential "
                "equations.\n\n"
                "Concretely: to solve $L[u]=f$ with given boundary conditions, first solve "
                "$L[G]=\\delta(x-s)$ — the response to a unit impulse (a Dirac delta) placed "
                "at $s$ — with the SAME boundary conditions. Then the solution for ANY $f$ "
                "is\n"
                "$$u(x)=\\int G(x,s)\\,f(s)\\,ds.$$\n"
                "The forcing $f$ is decomposed into a continuum of point impulses "
                "$f(s)\\,\\delta(x-s)$, each producing the response $f(s)G(x,s)$, and the "
                "results are summed. One function $G$ solves infinitely many problems.\n\n"
                "In this chapter you will learn: what $\\delta$ and $L[G]=\\delta$ mean; how "
                "superposition gives $u=\\int Gf\\,ds$; how to CONSTRUCT $G$ for two-point "
                "boundary-value problems by gluing homogeneous solutions with the right "
                "derivative jump; why $G$ is symmetric ($G(x,s)=G(s,x)$ — reciprocity); and "
                "how the heat kernel is the time-dependent Green's function. This is the "
                "capstone: it reunites the eigenfunction, separation, and impulse-response "
                "viewpoints of the whole Fourier/PDE sequence into one framework."
            )),
            # ---------------------------------------------------------- 2
            (L, "Prerequisites check", (
                "Four things carry this chapter.\n\n"
                "1. SECOND-ORDER LINEAR ODEs. You must find the general solution of "
                "homogeneous equations like $u''=0$ (giving $u=Ax+B$) or "
                "$u''+k^2u=0$ (giving $\\sin,\\cos$), and impose boundary conditions to fix "
                "constants. Green's-function construction is built from two such "
                "homogeneous solutions.\n\n"
                "2. THE DIRAC DELTA, intuitively. Think of $\\delta(x-s)$ as an "
                "infinitely tall, infinitely thin spike at $x=s$ with unit area: "
                "$\\int \\delta(x-s)\\,dx=1$ and $\\int g(x)\\delta(x-s)\\,dx=g(s)$ (it "
                "'samples' $g$ at $s$). It is not an ordinary function but a limit of tall "
                "narrow bumps; the rigor is in PDET, but the operational rules above "
                "suffice here.\n\n"
                "3. SUPERPOSITION FOR LINEAR OPERATORS. If $L$ is linear and "
                "$L[u_1]=f_1$, $L[u_2]=f_2$, then $L[u_1+u_2]=f_1+f_2$ and "
                "$L[cu]=cf$. This is the engine that turns 'response to one impulse' into "
                "'response to any forcing.'\n\n"
                "4. THE HEAT EQUATION AND SEPARATION (PF.U5.N1, PF.U7.N3), for the "
                "time-dependent analogue and the eigenfunction form of $G$.\n\n"
                "Quick self-check (answers follow):\n\n"
                "• Evaluate $\\int_{-\\infty}^{\\infty} (x^2+1)\\,\\delta(x-3)\\,dx$. "
                "($10$ — the delta samples $x^2+1$ at $x=3$.)\n"
                "• General solution of $u''=0$? ($u=Ax+B$.)\n"
                "• If $L$ is linear and $L[G_s]=\\delta(x-s)$, what solves "
                "$L[u]=f(s_1)\\delta(x-s_1)+f(s_2)\\delta(x-s_2)$? "
                "($f(s_1)G_{s_1}+f(s_2)G_{s_2}$ — superposition, the discrete preview of "
                "the integral.)\n\n"
                "If the delta's sampling property is unfamiliar, absorb it now — it is the "
                "one new object in the chapter."
            )),
            # ---------------------------------------------------------- 3
            (L, "The core idea, intuitively", (
                "Imagine a guitar string pinned at both ends, and you press down at a "
                "single point $s$ with a unit force. The string deflects into a shape — a "
                "taut triangle, peaked under your finger, straight on either side. Call that "
                "shape $G(x,s)$: the deflection at position $x$ caused by a unit point force "
                "at $s$. It is the string's INFLUENCE FUNCTION.\n\n"
                "Now load the string not with one finger but with a distributed weight "
                "$f(s)$ per unit length. Think of that load as a dense crowd of tiny point "
                "forces: an amount $f(s)\\,ds$ at each location $s$. Each contributes its "
                "own triangular deflection $G(x,s)\\,f(s)\\,ds$, and because the string "
                "responds LINEARLY, the total deflection is the sum — the integral — of all "
                "these contributions:\n"
                "$$u(x)=\\int G(x,s)\\,f(s)\\,ds.$$\n"
                "That is a Green's function in one sentence: the response to a unit point "
                "source, from which the response to any source is assembled by "
                "superposition.\n\n"
                "The Dirac delta $\\delta(x-s)$ is the mathematical name for the 'unit point "
                "source at $s$.' The differential operator $L$ is whatever law governs the "
                "system ($L=\\frac{d^2}{dx^2}$ for the string's shape). The defining "
                "equation\n"
                "$$L[G(x,s)]=\\delta(x-s)$$\n"
                "reads: '$G$ is the response of the system $L$ to a unit impulse at $s$,' "
                "and $G$ must also satisfy the SAME boundary conditions as the real problem "
                "(the string is still pinned at both ends).\n\n"
                "Why is this powerful? Because it SEPARATES the operator from the forcing. "
                "The hard part — inverting the differential operator subject to its boundary "
                "conditions — is done ONCE, encoded in $G$. After that, every new forcing "
                "$f$ is handled by a single integration. The Green's function is, quite "
                "literally, the inverse of the differential operator written as an integral "
                "kernel: where $L$ differentiates, $\\int G\\,\\cdot\\,ds$ undoes it."
            )),
            # ---------------------------------------------------------- 4
            (L, "The formal definition and the superposition theorem", (
                "THE SETUP. Let $L$ be a linear differential operator on $[a,b]$ (for "
                "concreteness $L[u]=u''$, or more generally the Sturm-Liouville form "
                "$L[u]=(p u')'+q u$), with homogeneous boundary conditions "
                "$B[u]=0$ (e.g. $u(a)=u(b)=0$). We want to solve $L[u]=f$ subject to "
                "$B[u]=0$ for arbitrary $f$.\n\n"
                "DEFINITION. The Green's function $G(x,s)$ is the solution, as a function "
                "of $x$ for each fixed source location $s$, of\n"
                "$$L[G(x,s)]=\\delta(x-s), \\qquad B[G]=0.$$\n"
                "It is the response at $x$ to a unit impulse at $s$, respecting the same "
                "boundary conditions.\n\n"
                "SUPERPOSITION THEOREM. Given $G$, the solution of $L[u]=f$, $B[u]=0$ is\n"
                "$$u(x)=\\int_a^b G(x,s)\\,f(s)\\,ds.$$\n"
                "PROOF (formal, and illuminating). Apply $L$ (which differentiates in $x$, "
                "so it passes through the $ds$ integral):\n"
                "$$L[u](x)=\\int_a^b L_x[G(x,s)]\\,f(s)\\,ds = \\int_a^b "
                "\\delta(x-s)\\,f(s)\\,ds = f(x),$$\n"
                "using $L_x[G]=\\delta(x-s)$ and then the delta's sampling property. And "
                "each $G(x,s)$ satisfies $B[G]=0$, so the integral $u$ does too "
                "(the boundary conditions are linear). Hence $u$ solves the problem. QED.\n\n"
                "Read what happened: the delta's sampling property is EXACTLY what makes "
                "the integral collapse back to $f$. The operator $L$ and the integral "
                "operator with kernel $G$ are inverse to each other. Two consequences. "
                "(1) UNIQUENESS: if the homogeneous problem $L[u]=0$, $B[u]=0$ has only the "
                "trivial solution, then $G$ exists and is unique, and so is $u$. (2) "
                "STRUCTURE: solving a differential equation becomes evaluating an integral — "
                "often far easier, and always giving the solution's dependence on the "
                "forcing in closed form. Page 6 shows how to build $G$; this page is why "
                "building it is worth the trouble."
            )),
            # ---------------------------------------------------------- 5
            (E, "First worked examples: the delta and superposition", (
                "EXAMPLE 1 (the delta's sampling property in action). Compute "
                "$\\int_0^{10} (s^2-2s)\\,\\delta(s-3)\\,ds$. The delta picks out the "
                "value of $s^2-2s$ at $s=3$: $9-6=3$. Answer $3$. And "
                "$\\int_0^{10}(s^2-2s)\\delta(s-11)\\,ds=0$ because the spike at $s=11$ "
                "lies OUTSIDE $[0,10]$ — the delta must sit inside the interval to "
                "contribute. This sampling is the only property of $\\delta$ we ever "
                "use.\n\n"
                "EXAMPLE 2 (superposition of point responses). Suppose for some operator "
                "the impulse responses are $G(x,1)$ and $G(x,4)$, and a forcing consists of "
                "two point loads: $f=2\\,\\delta(x-1)+5\\,\\delta(x-4)$. By linearity the "
                "solution is $u(x)=2\\,G(x,1)+5\\,G(x,4)$ — each impulse response scaled by "
                "its load and added. The integral $\\int G(x,s)f(s)\\,ds$ reproduces this: "
                "$\\int G(x,s)[2\\delta(s-1)+5\\delta(s-4)]ds = 2G(x,1)+5G(x,4)$. The "
                "continuous formula contains the discrete one as a special case.\n\n"
                "EXAMPLE 3 (checking that $\\int Gf\\,ds$ inverts $L$, symbolically). "
                "Suppose $G(x,s)$ solves $L[G]=\\delta(x-s)$ and let "
                "$u(x)=\\int_a^b G(x,s)f(s)\\,ds$. Apply $L$ in $x$: "
                "$L[u]=\\int_a^b L_x[G]\\,f(s)\\,ds=\\int_a^b \\delta(x-s)f(s)\\,ds=f(x)$. "
                "So $u$ solves $L[u]=f$ — verifying the superposition theorem on a concrete "
                "manipulation. These three examples isolate the two moving parts: the delta "
                "samples, and linearity lets responses add. Everything remaining in the "
                "chapter is CONSTRUCTING the particular $G$ for a given operator and "
                "boundary conditions."
            )),
            # ---------------------------------------------------------- 6
            (L, "Constructing G for a two-point boundary-value problem", (
                "Here is the recipe that builds $G$ for $L[u]=(pu')'+qu$ on $[a,b]$ with "
                "separated boundary conditions at $a$ and $b$. The Green's function is "
                "assembled from two homogeneous solutions glued at $x=s$.\n\n"
                "STEP 1 — TWO HOMOGENEOUS SOLUTIONS. Find $y_1(x)$ solving $L[y_1]=0$ AND "
                "the LEFT boundary condition, and $y_2(x)$ solving $L[y_2]=0$ AND the RIGHT "
                "boundary condition. Away from the source the impulse response must satisfy "
                "$L[G]=0$, so $G$ is built from these.\n\n"
                "STEP 2 — PIECEWISE FORM. Because the delta acts only at $x=s$, write\n"
                "$$G(x,s)=\\begin{cases}A(s)\\,y_1(x) & a\\le x<s\\ \\ (\\text{left of the "
                "source})\\\\ B(s)\\,y_2(x) & s<x\\le b\\ \\ (\\text{right of the source})."
                "\\end{cases}$$\n"
                "The left piece uses $y_1$ so the left BC holds; the right piece uses "
                "$y_2$ so the right BC holds. Two unknown coefficients $A,B$ remain.\n\n"
                "STEP 3 — TWO MATCHING CONDITIONS AT $x=s$. (i) CONTINUITY: $G$ is "
                "continuous across the source, $A y_1(s)=B y_2(s)$. (ii) DERIVATIVE JUMP: "
                "integrating $L[G]=\\delta$ across the spike gives a unit jump in the flux. "
                "For $L=(pu')'+qu$,\n"
                "$$\\frac{\\partial G}{\\partial x}\\Big|_{x=s^+} - "
                "\\frac{\\partial G}{\\partial x}\\Big|_{x=s^-} = \\frac{1}{p(s)}.$$\n"
                "(For $L=u''$, $p=1$, the jump is exactly $1$.) The value is continuous but "
                "the SLOPE kinks — the visible signature of a point source, like the corner "
                "of the loaded string.\n\n"
                "STEP 4 — SOLVE FOR $A,B$. The two conditions give a $2\\times2$ system. "
                "The clean result, using the Wronskian "
                "$W=y_1y_2'-y_1'y_2$, is\n"
                "$$G(x,s)=\\frac{y_1(x_<)\\,y_2(x_>)}{p(s)\\,W(s)}, \\qquad "
                "x_<=\\min(x,s),\\ x_>=\\max(x,s).$$\n"
                "Memorize this compact form: smaller-argument solution times larger-argument "
                "solution, over $pW$. It automatically encodes continuity, the jump, and "
                "both boundary conditions."
            )),
            # ---------------------------------------------------------- 7
            (E, "Standard worked examples: building and using G", (
                "EXAMPLE 1 (the canonical string, $L=\\frac{d^2}{dx^2}$). Build $G$ for "
                "$u''=f$ on $[0,1]$ with $u(0)=u(1)=0$. Homogeneous solutions of $u''=0$ "
                "are lines. Left BC $u(0)=0$: take $y_1=x$. Right BC $u(1)=0$: take "
                "$y_2=x-1$. Wronskian $W=y_1y_2'-y_1'y_2 = x(1)-(1)(x-1)=1$, and $p=1$, so "
                "$pW=1$. Thus\n"
                "$$G(x,s)=y_1(x_<)y_2(x_>)=\\begin{cases}x(s-1) & x\\le s\\\\ "
                "s(x-1) & x\\ge s.\\end{cases}$$\n"
                "CHECK THE JUMP at $x=s$: for $x>s$, $G=s(x-1)$ so $G_x=s$; for $x<s$, "
                "$G=x(s-1)$ so $G_x=s-1$. Jump $=s-(s-1)=1=1/p$. Correct. (Note $G<0$: a "
                "downward unit load makes the string sag — the shape is a taut "
                "triangle.)\n\n"
                "EXAMPLE 2 (use it — solve $u''=1$). With $f=1$, "
                "$u(x)=\\int_0^1 G(x,s)\\,ds = \\int_0^x s(x-1)\\,ds + "
                "\\int_x^1 x(s-1)\\,ds$ (for $s<x$ use the $x\\ge s$ branch $s(x-1)$; for "
                "$s>x$ use $x(s-1)$). First integral: $(x-1)\\frac{x^2}{2}$. Second: "
                "$x\\big[\\frac{(s-1)^2}{2}\\big]_x^1 = x\\big(0-\\frac{(x-1)^2}{2}\\big) = "
                "-\\frac{x(x-1)^2}{2}$. Sum: "
                "$\\frac{(x-1)}{2}\\big[x^2 - x(x-1)\\big] = \\frac{(x-1)}{2}\\cdot x = "
                "\\frac12 x(x-1)$. So $u=\\tfrac12 x(x-1)=\\tfrac12(x^2-x)$.\n\n"
                "VERIFY against direct integration: $u''=1\\Rightarrow "
                "u=\\tfrac12x^2+Cx+D$; $u(0)=0\\Rightarrow D=0$; "
                "$u(1)=\\tfrac12+C=0\\Rightarrow C=-\\tfrac12$; "
                "$u=\\tfrac12x^2-\\tfrac12x=\\tfrac12 x(x-1)$. Identical. The Green's "
                "function reproduced the answer — and, unlike direct integration, it now "
                "solves $u''=f$ for EVERY $f$ by the same single integral."
            )),
            # ---------------------------------------------------------- 8
            (L, "Techniques, strategy, and symmetry", (
                "A workflow, plus the deep property that halves the labor.\n\n"
                "CONSTRUCTION CHECKLIST. (1) Confirm the homogeneous problem "
                "$L[u]=0,B[u]=0$ has ONLY the trivial solution — otherwise $G$ does not "
                "exist (page 10). (2) Find $y_1$ (left BC) and $y_2$ (right BC). (3) Form "
                "$G=\\frac{y_1(x_<)y_2(x_>)}{pW}$, or equivalently glue $Ay_1$ and $By_2$ "
                "using continuity and the jump $[G_x]=1/p(s)$. (4) SANITY-CHECK: does $G$ "
                "satisfy both boundary conditions? Is it continuous at $x=s$? Does the slope "
                "jump by $1/p$? (5) To solve $L[u]=f$, integrate "
                "$u=\\int G(x,s)f(s)ds$, splitting at $s=x$ to use the correct branch on "
                "each side.\n\n"
                "SYMMETRY / RECIPROCITY. For a SELF-ADJOINT operator (the Sturm-Liouville "
                "form with self-adjoint boundary conditions), the Green's function is "
                "SYMMETRIC:\n"
                "$$G(x,s)=G(s,x).$$\n"
                "The response at $x$ to a source at $s$ equals the response at $s$ to a "
                "source at $x$. This is MAXWELL'S RECIPROCITY in elasticity, and the "
                "reciprocity theorem in circuits and optics. Physically: push here, measure "
                "there — you get the same reading as pushing there and measuring here. The "
                "compact form $G=\\frac{y_1(x_<)y_2(x_>)}{pW}$ makes symmetry manifest, "
                "since swapping $x$ and $s$ swaps which is smaller but leaves the product "
                "$y_1(x_<)y_2(x_>)$ unchanged. Use symmetry as a CHECK: if your constructed "
                "$G$ is not symmetric for a self-adjoint problem, you have an error.\n\n"
                "EIGENFUNCTION FORM (the bridge to PF.U7.N3). When the eigenfunctions "
                "$X_n$ and eigenvalues $\\lambda_n$ of $L$ are known, "
                "$$G(x,s)=\\sum_n \\frac{X_n(x)\\,X_n(s)}{\\lambda_n\\,\\|X_n\\|^2}.$$\n"
                "This is orthogonal projection applied to the impulse response: each mode "
                "contributes independently, weighted by $1/\\lambda_n$. It also exposes the "
                "danger of a zero eigenvalue — a term blows up — foreshadowing page 10's "
                "solvability condition."
            )),
            # ---------------------------------------------------------- 9
            (E, "Harder worked example: the heat kernel", (
                "The time-dependent Green's function of the heat equation is the HEAT "
                "KERNEL — the temperature response to a unit spike of heat. Solve "
                "$u_t=u_{xx}$ on the whole line with $u(x,0)=f(x)$.\n\n"
                "THE KERNEL. The Green's function solves the heat equation with a point "
                "initial condition: $G_t=G_{xx}$ with $G(x,s,0)=\\delta(x-s)$. The result is "
                "the Gaussian\n"
                "$$G(x,s,t)=\\frac{1}{\\sqrt{4\\pi t}}\\,"
                "\\exp\\!\\left(-\\frac{(x-s)^2}{4t}\\right).$$\n"
                "It describes a unit amount of heat, initially concentrated at $s$, "
                "spreading into a bell curve that widens as $\\sqrt{t}$ and shortens to "
                "conserve area (total heat $\\int G\\,dx=1$ for all $t$).\n\n"
                "SUPERPOSITION gives the full solution: decompose the initial profile into "
                "point sources $f(s)\\delta(x-s)$ and sum their spreading Gaussians,\n"
                "$$u(x,t)=\\int_{-\\infty}^{\\infty} G(x,s,t)\\,f(s)\\,ds = "
                "\\frac{1}{\\sqrt{4\\pi t}}\\int_{-\\infty}^{\\infty} "
                "e^{-(x-s)^2/4t}\\,f(s)\\,ds.$$\n"
                "This single convolution solves the heat equation for ANY initial data — "
                "the exact analogue of $u=\\int Gf\\,ds$ for the steady problem, now with "
                "time as a parameter.\n\n"
                "VERIFY the kernel solves the PDE (take $s=0$). With "
                "$G=(4\\pi t)^{-1/2}e^{-x^2/4t}$: differentiating, "
                "$G_x=-\\frac{x}{2t}G$, then "
                "$G_{xx}=\\big(-\\frac{1}{2t}+\\frac{x^2}{4t^2}\\big)G$; and "
                "$G_t=\\big(-\\frac{1}{2t}+\\frac{x^2}{4t^2}\\big)G$ (a short computation "
                "from the $t^{-1/2}$ prefactor and the exponent). The two agree, so "
                "$G_t=G_{xx}$. CONCRETE READING: a unit heat spike at the origin has, at "
                "$t=1$, central value $G(0,0,1)=1/\\sqrt{4\\pi}\\approx0.282$ and has spread "
                "to a Gaussian of standard deviation $\\sqrt{2t}=\\sqrt2$. Heat conduction "
                "is convolution against a spreading Gaussian — the time-dependent Green's "
                "function at work."
            )),
            # ---------------------------------------------------------- 10
            (L, "Edge behavior: when the Green's function fails", (
                "The existence of $G$ is not automatic. It hinges on a solvability "
                "condition, and the failure case is instructive.\n\n"
                "THE RESONANCE / ZERO-EIGENVALUE OBSTRUCTION. The construction of page 6 "
                "silently assumed $y_1$ and $y_2$ are INDEPENDENT (nonzero Wronskian). They "
                "fail to be independent exactly when the homogeneous problem "
                "$L[u]=0,\\,B[u]=0$ has a NONTRIVIAL solution — i.e. when $\\lambda=0$ is an "
                "eigenvalue. Then $y_1$ and $y_2$ are proportional, $W=0$, and "
                "$G=\\frac{y_1y_2}{pW}$ is undefined. In the eigenfunction form "
                "$G=\\sum\\frac{X_nX_n}{\\lambda_n\\|X_n\\|^2}$, a term with $\\lambda_n=0$ "
                "divides by zero. The ordinary Green's function does not exist.\n\n"
                "THE FREDHOLM ALTERNATIVE. When $\\lambda=0$ is an eigenvalue with "
                "eigenfunction $\\phi$ (the homogeneous solution), the equation $L[u]=f$ is "
                "solvable ONLY IF the forcing is orthogonal to $\\phi$: "
                "$\\int_a^b f\\,\\phi\\,dx=0$. If $f$ has a component along $\\phi$, no "
                "solution exists (you are 'driving the system at its resonance'); if $f$ is "
                "orthogonal to $\\phi$, infinitely many solutions exist (differing by "
                "multiples of $\\phi$). This is the differential-equation version of 'a "
                "singular matrix $A$ makes $Ax=b$ solvable only when $b\\perp$ the null "
                "space' — the same linear algebra, in function space.\n\n"
                "THE MODIFIED GREEN'S FUNCTION. In the solvable case one repairs $G$ by "
                "REMOVING the offending zero mode: define a modified Green's function "
                "solving $L[G_M]=\\delta(x-s)-\\frac{\\phi(x)\\phi(s)}{\\|\\phi\\|^2}$ (the "
                "delta with its $\\phi$-component projected out), which then exists. The "
                "solution becomes $u=\\int G_M f\\,ds$ plus an arbitrary multiple of "
                "$\\phi$. Page 11 works a concrete Neumann example where this happens. The "
                "moral: a Green's function inverts $L$, and just as a singular matrix has no "
                "inverse, an operator with a zero eigenvalue has no ordinary Green's "
                "function — the boundary conditions and the forcing must cooperate."
            )),
            # ---------------------------------------------------------- 11
            (E, "Edge-case worked example: the Neumann obstruction", (
                "Watch a Green's function fail to exist, and see the solvability condition "
                "take over. Consider $u''=f$ on $[0,1]$ with pure NEUMANN conditions "
                "$u'(0)=u'(1)=0$ (an insulated rod: no heat flux at either end).\n\n"
                "THE HOMOGENEOUS PROBLEM HAS A NONTRIVIAL SOLUTION. Solve $u''=0$ with "
                "$u'(0)=u'(1)=0$: the general solution $u=Ax+B$ has $u'=A$, and "
                "$u'(0)=A=0$ forces $A=0$, leaving $u=B$ — ANY constant. So $\\phi=1$ is a "
                "nontrivial homogeneous solution ($\\lambda=0$ is an eigenvalue). By "
                "page 10, the ordinary Green's function does NOT exist: both $y_1$ and "
                "$y_2$ would be constants, proportional, with $W=0$.\n\n"
                "THE SOLVABILITY CONDITION. When can $u''=f$, $u'(0)=u'(1)=0$ be solved at "
                "all? Integrate the equation over $[0,1]$: "
                "$\\int_0^1 u''\\,dx = u'(1)-u'(0)=0-0=0$, so we need "
                "$\\int_0^1 f\\,dx=0$. This is the Fredholm condition "
                "$\\int f\\,\\phi\\,dx=\\int f\\cdot1\\,dx=0$: the net heat input must be "
                "ZERO, or the insulated rod's temperature would rise forever with no "
                "steady state.\n\n"
                "CONCRETE FAILURE. Take $f=1$ (uniform heating). Then "
                "$\\int_0^1 1\\,dx=1\\ne0$: NO solution. Indeed integrating "
                "$u''=1$ gives $u'=x+C$; $u'(0)=C=0$ but then $u'(1)=1\\ne0$ — the boundary "
                "conditions are contradictory. The physics is transparent: you cannot "
                "insulate a rod, pump heat in everywhere, and expect a steady temperature.\n\n"
                "CONCRETE SUCCESS. Take $f(x)=x-\\tfrac12$, which has "
                "$\\int_0^1(x-\\tfrac12)dx=0$. Now a solution exists (unique up to an "
                "additive constant, the leftover $\\phi=1$ mode): integrate twice to get "
                "$u=\\tfrac{x^3}{6}-\\tfrac{x^2}{4}+D$, and one checks "
                "$u'(0)=u'(1)=0$. The modified Green's function of page 10 delivers exactly "
                "this family. Lesson: a zero eigenvalue does not end the story — it imposes "
                "a compatibility condition on the forcing and a residual freedom in the "
                "answer."
            )),
            # ---------------------------------------------------------- 12
            (P, "Common misconceptions", (
                "MISCONCEPTION 1: 'The Green's function is the solution.' It is the "
                "solution ONLY for a unit point source, $L[u]=\\delta(x-s)$. For a general "
                "forcing $f$ you must still integrate, $u=\\int G(x,s)f(s)\\,ds$. $G$ is the "
                "building block, not the finished building.\n\n"
                "MISCONCEPTION 2: '$G$ depends only on $x$.' It depends on TWO variables: "
                "the observation point $x$ and the source point $s$. That is what lets you "
                "place the impulse anywhere and superpose. Writing $G(x)$ instead of "
                "$G(x,s)$ loses the whole mechanism.\n\n"
                "MISCONCEPTION 3: 'The delta is a tall skinny ordinary function you can "
                "evaluate.' $\\delta(x-s)$ has no pointwise values in the ordinary sense; "
                "it is defined by what it does inside integrals ($\\int g\\delta=g(s)$). "
                "Treating '$\\delta(0)=\\infty$' as a usable number leads nowhere; use the "
                "sampling property instead.\n\n"
                "MISCONCEPTION 4: 'A Green's function always exists.' It exists iff the "
                "homogeneous problem has only the trivial solution (no zero eigenvalue). "
                "With incompatible boundary conditions (the insulated-rod Neumann case) the "
                "ordinary $G$ fails and a solvability condition governs — page 11.\n\n"
                "MISCONCEPTION 5: 'Symmetry $G(x,s)=G(s,x)$ is obvious / automatic.' It "
                "holds for SELF-ADJOINT operators with self-adjoint boundary conditions. "
                "Non-self-adjoint problems (e.g. with a first-derivative convection term "
                "and mismatched boundary conditions) have $G(x,s)=G^*(s,x)$ where $G^*$ "
                "belongs to the ADJOINT operator — not the same function. Reciprocity is a "
                "theorem with hypotheses, not a triviality.\n\n"
                "MISCONCEPTION 6: 'The jump is in $G$ itself.' No — $G$ is CONTINUOUS at "
                "$x=s$; it is the DERIVATIVE $G_x$ that jumps (by $1/p(s)$). Confusing a "
                "jump in value with a jump in slope produces a discontinuous, wrong $G$."
            )),
            # ---------------------------------------------------------- 13
            (P, "Common computational and procedural errors", (
                "ERROR 1: Wrong boundary solution on the wrong side. $y_1$ must satisfy the "
                "LEFT boundary condition and multiply the piece for $x<s$; $y_2$ the RIGHT "
                "condition, for $x>s$. Swapping them gives a $G$ that violates the boundary "
                "conditions. Anchor: the piece touching an endpoint must obey that "
                "endpoint's condition.\n\n"
                "ERROR 2: Forgetting the $1/p(s)$ in the jump. For general "
                "$L=(pu')'+qu$ the derivative jump is $1/p(s)$, not $1$. Only when $p=1$ "
                "(the plain $u''$ case) is the jump exactly $1$. Using $1$ for a Bessel or "
                "variable-coefficient operator scales $G$ wrongly.\n\n"
                "ERROR 3: Sign of the jump. $G_x(s^+)-G_x(s^-)=+1/p$ for $L=(pu')'+qu$ with "
                "$L[G]=\\delta$. If your operator is written $-u''$ (common, to make it "
                "positive), the sign flips to $-1$. Track which sign convention the operator "
                "uses before assigning the jump.\n\n"
                "ERROR 4: Not splitting the integral at $s=x$. In "
                "$u=\\int_a^b G(x,s)f(s)ds$ the kernel has different formulas for $s<x$ and "
                "$s>x$. You must break the integral at $s=x$ and use the correct branch on "
                "each part. Using one branch throughout gives a wrong (often "
                "discontinuous-derivative) answer.\n\n"
                "ERROR 5: Applying the integral formula when $G$ does not exist. If the "
                "homogeneous problem has a nontrivial solution (zero eigenvalue), "
                "blindly writing $u=\\int Gf\\,ds$ is meaningless. First check solvability "
                "$\\int f\\phi\\,dx=0$ and use the modified Green's function.\n\n"
                "ERROR 6: Losing symmetry as a check. For a self-adjoint problem the final "
                "$G$ MUST satisfy $G(x,s)=G(s,x)$. If it does not, an arithmetic slip "
                "occurred (a mislabeled branch, a dropped Wronskian factor). Verifying "
                "symmetry costs nothing and catches most construction errors."
            )),
            # ---------------------------------------------------------- 14
            (L, "Applications: the impulse response everywhere", (
                "Green's functions are one of the most reused ideas in mathematical "
                "physics, because 'linear system + known impulse response = solved for all "
                "inputs' recurs across every field.\n\n"
                "1. ELECTROSTATICS. The potential of a unit point charge, "
                "$G=\\frac{1}{4\\pi\\varepsilon_0 |x-s|}$ in 3D, is the Green's function of "
                "the Laplacian $-\\nabla^2$. Any charge distribution $\\rho(s)$ produces the "
                "potential $\\int G(x,s)\\rho(s)\\,ds$ — Coulomb's law IS superposition of "
                "point-charge Green's functions. Boundary conditions (grounded conductors) "
                "are handled by image charges, a Green's-function technique.\n\n"
                "2. STRUCTURAL MECHANICS. The deflection of a beam or string under a unit "
                "point load is its influence function — literally $G$. Engineers tabulate "
                "influence functions and integrate against real load distributions; "
                "reciprocity (Maxwell-Betti) lets them measure at accessible points and "
                "infer inaccessible ones.\n\n"
                "3. HEAT AND DIFFUSION. The heat kernel (page 9) gives the temperature from "
                "any initial distribution by convolution; the same Gaussian is the "
                "probability density of Brownian motion, tying diffusion to random walks.\n\n"
                "4. QUANTUM MECHANICS AND FIELD THEORY. The Green's function of the "
                "Schrodinger or wave operator is the PROPAGATOR — the amplitude to go from "
                "one spacetime point to another. Feynman diagrams are, at bottom, products "
                "and sums of Green's functions; the entire perturbative apparatus is "
                "impulse-response superposition.\n\n"
                "5. SIGNAL PROCESSING AND CONTROL. A linear time-invariant system's output "
                "is the convolution of its input with the impulse response $h(t)=G$ — "
                "identical mathematics. Transfer functions are Green's functions in the "
                "frequency domain. Whenever you have a linear system and can probe it with "
                "an impulse, you have a Green's function waiting to solve every other "
                "input."
            )),
            # ---------------------------------------------------------- 15
            (E, "Applied worked example: point load on a string", (
                "A string is stretched with unit tension between $x=0$ and $x=1$ and pinned "
                "at both ends. Its small transverse deflection $u(x)$ under a distributed "
                "downward load $w(x)$ obeys $-u''=w$, $u(0)=u(1)=0$. Find the Green's "
                "function and use it for a point load at the center.\n\n"
                "BUILD $G$ for $-u''=\\delta(x-s)$, $u(0)=u(1)=0$. Homogeneous solutions of "
                "$-u''=0$ are lines. Left BC: $y_1=x$. Right BC: $y_2=1-x$. Piecewise "
                "$G=Ax$ for $x<s$, $G=B(1-x)$ for $x>s$. Continuity: $As=B(1-s)$. The jump "
                "for $-u''=\\delta$ is $G_x(s^+)-G_x(s^-)=-1$: here "
                "$-B-A=-1$, so $A+B=1$. Solving, $B=s$, $A=1-s$, giving\n"
                "$$G(x,s)=\\begin{cases}(1-s)\\,x & x\\le s\\\\ s\\,(1-x) & x\\ge s,"
                "\\end{cases}$$\n"
                "i.e. $G(x,s)=x_<(1-x_>)$ — symmetric in $x,s$ (reciprocity holds, as it "
                "must for this self-adjoint problem), and positive (a downward load makes a "
                "downward deflection).\n\n"
                "POINT LOAD AT THE CENTER. A unit point load at $a=\\tfrac12$ means "
                "$w=\\delta(x-\\tfrac12)$, so $u(x)=G(x,\\tfrac12)$ directly. Evaluate: for "
                "$x\\le\\tfrac12$, $u=(1-\\tfrac12)x=\\tfrac{x}{2}$; for $x\\ge\\tfrac12$, "
                "$u=\\tfrac12(1-x)$. The deflection is a symmetric triangle, peaking at the "
                "center with value $u(\\tfrac12)=\\tfrac12\\cdot\\tfrac12=\\tfrac14$. "
                "Physically exact: a point-loaded ideal string forms two straight segments "
                "meeting at a corner under the load — precisely the shape whose slope jumps "
                "by the load, as the Green's function jump condition dictates.\n\n"
                "DISTRIBUTED LOAD, for free. For any $w$, "
                "$u(x)=\\int_0^1 G(x,s)w(s)\\,ds$. E.g. uniform $w=1$ gives "
                "$u=\\int_0^x s(1-x)ds+\\int_x^1 x(1-s)ds=\\tfrac12 x(1-x)$ — the familiar "
                "parabolic sag of a uniformly loaded string, obtained by one integration "
                "against the same $G$."
            )),
            # ---------------------------------------------------------- 16
            (L, "Connections: capping the Fourier/PDE sequence", (
                "This chapter gathers the whole sequence into one framework.\n\n"
                "BACKWARD (what this rested on and reunified):\n"
                "• PF.U7.N3 (eigenfunction expansions): the eigenfunction form "
                "$G=\\sum\\frac{X_n(x)X_n(s)}{\\lambda_n\\|X_n\\|^2}$ IS orthogonal "
                "projection applied to the impulse response. Green's functions and "
                "eigenfunction expansions are two representations of the same operator "
                "inverse — a modal sum versus a closed-form kernel.\n"
                "• PF.U5.N1 (separation of variables): the heat kernel (page 9) is what "
                "separation's mode sum converges to; both solve the same initial-value "
                "problem, one as a series of decaying modes, the other as a convolution.\n"
                "• LA.U8.N2 (projection and self-adjointness): symmetry/reciprocity "
                "$G(x,s)=G(s,x)$ is the operator analogue of a symmetric matrix, and the "
                "Fredholm solvability condition is the null-space compatibility "
                "$b\\perp\\ker A$ from linear algebra.\n"
                "• PF.U8.N2 (transport): its source integral "
                "$\\int_0^t S(x-c(t-\\tau),\\tau)d\\tau$ was already a Green's-function "
                "superposition for the transport operator — this chapter names and "
                "generalizes that pattern.\n\n"
                "SIDEWAYS AND FORWARD:\n"
                "• PF.U1.N2 (classification): each PDE type has its own Green's function — "
                "the $1/|x-s|$ potential (elliptic/Laplace), the Gaussian heat kernel "
                "(parabolic), the travelling-front kernel (hyperbolic/wave). The operator's "
                "type is visible in the character of its impulse response.\n"
                "• PDEM and PDET (advanced modeling and theory): boundary-integral methods, "
                "the method of images, distributions, fundamental solutions in higher "
                "dimensions, and propagators in physics all extend this material. The "
                "Green's function is the through-line linking ODE boundary-value problems, "
                "the heat equation, potential theory, and quantum field theory — the "
                "capstone concept of the entire Fourier/PDE arc."
            )),
            # ---------------------------------------------------------- 17
            (L, "Going deeper: operators, distributions, and propagators", (
                "The Green's function is best understood as the INVERSE of a differential "
                "operator. Where $L$ maps a function to its 'forcing,' the integral operator "
                "$\\mathcal{G}[f](x)=\\int G(x,s)f(s)ds$ maps forcing back to solution, and "
                "$L\\mathcal{G}=\\mathcal{G}L=I$ (the identity) on the space respecting the "
                "boundary conditions. In this light $L[G]=\\delta$ says '$\\mathcal{G}$ "
                "applied to a point source returns the impulse response,' and $\\delta$ is "
                "the kernel of the identity operator — the continuous analogue of the "
                "identity matrix. Everything about matrix inverses (existence iff no zero "
                "eigenvalue, symmetry for symmetric matrices, spectral decomposition) has a "
                "Green's-function echo.\n\n"
                "DISTRIBUTIONS make the delta rigorous. Laurent Schwartz's theory of "
                "distributions (generalized functions) defines $\\delta$ not by its "
                "'values' but as a linear functional — 'evaluate the test function at "
                "$s$' — and gives differentiation of discontinuous objects a precise "
                "meaning. In this language $G_x$ jumping by $1/p$ is exactly the statement "
                "that $G_{xx}$ (or $(pG_x)'$) CONTAINS a delta of the right strength — the "
                "construction of page 6 is a distributional identity, not a heuristic.\n\n"
                "PROPAGATORS AND CAUSALITY. For time-dependent operators the Green's "
                "function becomes a PROPAGATOR $G(x,t;s,\\tau)$ carrying the response of an "
                "impulse fired at $(s,\\tau)$ forward in time. Physical propagators are "
                "CAUSAL — zero before the impulse — which for the wave operator concentrates "
                "$G$ on the light cone (Huygens' principle in odd space dimensions) and for "
                "the heat operator spreads it instantly but with exponentially small tails. "
                "Quantum field theory is built almost entirely from such propagators; a "
                "Feynman diagram is a recipe for multiplying and integrating Green's "
                "functions. The humble two-point boundary-value problem you solved on "
                "page 7 — glue two solutions, jump the derivative — is the first, most "
                "transparent instance of the single most pervasive computational device in "
                "theoretical physics. That is a fitting place to close the sequence."
            )),
            # ---------------------------------------------------------- 18
            (L, "Summary and reference sheet", (
                "DEFINITION. The Green's function solves "
                "$L[G(x,s)]=\\delta(x-s)$ with the problem's homogeneous boundary "
                "conditions $B[G]=0$: the impulse response of $L$ at $x$ to a unit source "
                "at $s$.\n\n"
                "SUPERPOSITION. The solution of $L[u]=f$, $B[u]=0$ is "
                "$u(x)=\\int_a^b G(x,s)f(s)\\,ds$. Proof: $L$ passes through the integral "
                "and the delta samples $f$.\n\n"
                "CONSTRUCTION (two-point BVP, $L=(pu')'+qu$). $y_1$ solves $L[y]=0$ + left "
                "BC; $y_2$ solves $L[y]=0$ + right BC. Then\n"
                "$$G(x,s)=\\frac{y_1(x_<)\\,y_2(x_>)}{p(s)\\,W(s)}, \\quad "
                "W=y_1y_2'-y_1'y_2.$$\n"
                "$G$ is CONTINUOUS at $x=s$; its slope JUMPS: "
                "$G_x(s^+)-G_x(s^-)=1/p(s)$ (sign $-1$ if the operator is written "
                "$-(pu')'$).\n\n"
                "SYMMETRY. Self-adjoint operators give $G(x,s)=G(s,x)$ — reciprocity. Use "
                "as a check.\n\n"
                "EIGENFUNCTION FORM. "
                "$G(x,s)=\\sum_n\\frac{X_n(x)X_n(s)}{\\lambda_n\\|X_n\\|^2}$ — orthogonal "
                "projection of the impulse response; a zero eigenvalue breaks it.\n\n"
                "EXISTENCE. $G$ exists iff the homogeneous problem has only the trivial "
                "solution. If $\\lambda=0$ is an eigenvalue with mode $\\phi$: solvable only "
                "if $\\int f\\phi\\,dx=0$ (Fredholm alternative); use the modified Green's "
                "function.\n\n"
                "HEAT KERNEL (time-dependent analogue). "
                "$G(x,s,t)=\\frac{1}{\\sqrt{4\\pi t}}e^{-(x-s)^2/4t}$; "
                "$u(x,t)=\\int G(x,s,t)f(s)ds$ solves $u_t=u_{xx}$, $u(x,0)=f$.\n\n"
                "WORKFLOW. (1) Check existence. (2) Find $y_1,y_2$. (3) Assemble "
                "$G=y_1(x_<)y_2(x_>)/(pW)$. (4) Check continuity, jump, symmetry, BCs. (5) "
                "Integrate $u=\\int Gf\\,ds$, splitting at $s=x$.\n\n"
                "ONE-LINE TAKEAWAY. Solve the operator once for a unit impulse; then every "
                "forcing is one integral away — $G$ is the differential operator's inverse "
                "written as a kernel."
            )),
            # ---------------------------------------------------------- 19
            (K, "Self-test (with worked solutions)", (
                "Attempt all seven before reading the solutions.\n\n"
                "1. Evaluate $\\int_{-\\infty}^\\infty (3s+1)\\delta(s-2)\\,ds$.\n"
                "2. For $u''=f$, $u(0)=u(1)=0$, state $G(x,s)$ and its value at "
                "$x=s=\\tfrac12$.\n"
                "3. What is the derivative jump of $G$ at $x=s$ for the operator "
                "$L=u''$? For $L=(x u')'$ at $s=2$?\n"
                "4. Solve $u''=6x$, $u(0)=u(1)=0$ using $G$ from Q2 (or directly) and give "
                "$u(x)$.\n"
                "5. Why does $u''=f$, $u'(0)=u'(1)=0$ fail to have a Green's function, and "
                "what condition must $f$ satisfy?\n"
                "6. State the heat kernel and its central value at $t=4$, $s=0$.\n"
                "7. True or false: for a self-adjoint problem, $G(x,s)=G(s,x)$.\n\n"
                "SOLUTIONS.\n"
                "1. Sample $3s+1$ at $s=2$: $3(2)+1=7$.\n"
                "2. $G=x(s-1)$ for $x\\le s$, $s(x-1)$ for $x\\ge s$; at "
                "$x=s=\\tfrac12$: $\\tfrac12(\\tfrac12-1)=-\\tfrac14$.\n"
                "3. For $u''$: jump $=1/p=1$. For $(xu')'$: $p=x$, so jump "
                "$=1/p(2)=\\tfrac12$.\n"
                "4. Direct: $u''=6x\\Rightarrow u=x^3+Cx+D$; $u(0)=0\\Rightarrow D=0$; "
                "$u(1)=1+C=0\\Rightarrow C=-1$; $u=x^3-x$. (The Green integral "
                "$\\int_0^1 G(x,s)6s\\,ds$ gives the same.)\n"
                "5. The homogeneous problem $u''=0$, $u'(0)=u'(1)=0$ has the nontrivial "
                "solution $\\phi=1$ ($\\lambda=0$), so $W=0$ and $G$ does not exist; "
                "solvability requires $\\int_0^1 f\\,dx=0$.\n"
                "6. $G=\\frac{1}{\\sqrt{4\\pi t}}e^{-x^2/4t}$; at $t=4,s=0$, center value "
                "$\\frac{1}{\\sqrt{16\\pi}}=\\frac{1}{4\\sqrt\\pi}\\approx0.141$.\n"
                "7. TRUE — reciprocity for self-adjoint operators with self-adjoint "
                "boundary conditions.\n\n"
                "Scoring: 7/7 — the capstone is yours. 5-6 — reread construction (page 6) "
                "and existence (pages 10-11). $\\le4$ — rework pages 3-7 before "
                "practicing."
            )),
            # ---------------------------------------------------------- 20
            (K, "Mastery checklist", (
                "Check each box honestly before completing the sequence.\n\n"
                "$\\square$ State what $L[G]=\\delta(x-s)$ means and why $G$ is called the "
                "impulse response of the operator.\n"
                "$\\square$ Use the delta's sampling property "
                "$\\int g(s)\\delta(s-s_0)ds=g(s_0)$ fluently.\n"
                "$\\square$ Prove the superposition formula $u=\\int G f\\,ds$ by passing "
                "$L$ through the integral and sampling.\n"
                "$\\square$ Construct $G$ for a two-point BVP: pick $y_1$ (left BC) and "
                "$y_2$ (right BC), and impose continuity plus the derivative jump "
                "$1/p(s)$.\n"
                "$\\square$ Recall and apply the compact form "
                "$G=y_1(x_<)y_2(x_>)/(pW)$.\n"
                "$\\square$ Use $G$ to solve $L[u]=f$ for a given $f$, splitting the "
                "integral at $s=x$, and verify against direct integration.\n"
                "$\\square$ State and use symmetry/reciprocity $G(x,s)=G(s,x)$ as a "
                "correctness check.\n"
                "$\\square$ Explain when $G$ fails to exist (zero eigenvalue), state the "
                "Fredholm solvability condition, and identify the modified Green's "
                "function.\n"
                "$\\square$ Write the heat kernel, verify it solves the heat equation, and "
                "use it as the time-dependent Green's function via convolution.\n"
                "$\\square$ Connect the Green's function to eigenfunction expansions "
                "($G=\\sum X_nX_n/(\\lambda_n\\|X_n\\|^2)$) and to physical impulse "
                "responses (electrostatics, beams, propagators).\n\n"
                "When every box is checked, you have closed the Fourier/PDE sequence: "
                "separation of variables, eigenfunction expansions, characteristics, "
                "transport, and now the impulse-response framework that unifies them. Hit "
                "Practice until constructing and applying a Green's function is automatic — "
                "it is the single most transferable tool you will carry into advanced PDEs, "
                "physics, and engineering."
            )),
        ],
    },
}
