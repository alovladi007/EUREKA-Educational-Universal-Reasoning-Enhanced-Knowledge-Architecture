// Shared AXIOM UI primitives. The math renderer is the first extracted piece;
// graphing (Mafs) and the whiteboard canvas are candidates to follow once a
// second app or service consumes this package.
export { InlineMath, BlockMath, RichMath, segmentMath } from './Math';
