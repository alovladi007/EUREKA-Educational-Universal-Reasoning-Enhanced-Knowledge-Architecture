# Pins the pytest-asyncio fixture loop scope, which otherwise prints a
# deprecation notice at the top of every run. Function scope is the value
# the suite has always effectively used.
[pytest]
asyncio_default_fixture_loop_scope = function
