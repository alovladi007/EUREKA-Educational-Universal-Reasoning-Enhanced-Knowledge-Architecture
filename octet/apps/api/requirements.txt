# Pinned. The build prompt requires RDKit and pint pinned at Phase 0.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pyjwt==2.10.1
# LTI 1.3 signs with RS256, which PyJWT cannot do without a crypto backend.
# Pinned at the version AXIOM's image resolves so both verticals sign alike.
cryptography==49.0.0
# An LTI 1.3 launch arrives as an HTML form post, which Starlette cannot parse
# without this. Pinned at the version AXIOM's image resolves.
python-multipart==0.0.32
httpx==0.28.1

# Chemistry and exact arithmetic. Do not hand roll any of this.
rdkit==2024.3.5
sympy==1.13.3
pint==0.24.4
periodictable==1.7.1

# Worker
celery==5.4.0
redis==5.2.1

# Test
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0
